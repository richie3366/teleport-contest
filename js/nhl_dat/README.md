# NHL dat files (browser deploy)

Verbatim copies from NetHack 5.0.0 (`NetHack-5.0.0_Release` tag), paths `nethack-c/upstream/dat/`.

Used by `js/dat_upstream.js` when `/play/<owner>/` cannot rely on the git submodule checkout.
