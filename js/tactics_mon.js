// tactics_mon.js — C wizard.c tactics(); monmove.c dochug covetous branch (~782–787).
// Full strategy/mnexto/rloc/mnearto not ported; stub preserves call order when no RNG draws here.

/**
 * C: **`wizard.c`** **`tactics(mtmp)`** — covetous only; **`mstate`** may become non-**`MON_FLOOR`**
 * (**`m_into_limbo`** via **`mnexto`** overcrowding).
 * @param {import('./gstate.js').game} g
 * @param {*} mtmp
 */
export async function tacticsMonsterDochugStubLikeC(g, mtmp) {
    void g;
    void mtmp;
    /* TODO: strategy + switch (STRAT_HEAL / STRAT_NONE harass rn2 + mnexto, default goal moves). */
}
