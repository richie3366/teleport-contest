// engrave.js — Floor engravings at the hero’s square.
// C ref: engrave.c read_engr_at()

/** C: read_engr_at(x, y) — subset of pickup() on restore. */
export function readEngrAt(x, y) {
    void x;
    void y;
}
