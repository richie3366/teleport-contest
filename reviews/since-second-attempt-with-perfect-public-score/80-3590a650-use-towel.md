# Review 80 — `3590a650` — use_towel + wet/dry helpers

## Métadonnées
- Hash complet / court : `3590a650856e0c797a539e0477cd6abef2f95a26` / `3590a650`
- Parent : `a045ab1f04725f778895c2c19f299c9b608229ca`
- Auteur, date : Raphaël Hervier, 2026-07-22 06:13:31 +0200
- D-id : D-1009 **+ cadence #1280**
- Stats : 12 files, +349/−66
- JS : `apply.js` (`use_towel`), `weapon.js` (wet/dry/finish/`is_wet_towel`), `trap.js` (`burnarmor` dry + import), `do_wear.js` (`export Blindf_off`)

## Intention vs livrable
Sujet : porter `use_towel` + helpers wet/dry. Body : **« Cadence #1280 full sessions stay 43/44 »**. Journal `#1280 cadence + D-1009`. Même mélange que D-1004.

Forteresse : **43/44**, Scr 11404/11405, RNG 100%. Speed `30+0.26/turn`. **Pas** de récupération 44/44. La cadence documente l’immobilité.

Le port towel, lui, est réel et plutôt serré sur `apply.c:112` + `weapon.c:1020+` + `trap.c:burnarmor` walk.

## Inventaire
| Fichier | Rôle |
|---|---|
| `js/apply.js` | `use_towel` ; doapply TOWEL |
| `js/weapon.js` | `is_wet_towel`, `wet_a_towel`, `dry_a_towel`, `finish_towel_change` ; `s_suffix_towel` **encore faux** |
| `js/trap.js` | `burnarmor` dry avant `rn2(5)` slots ; water_damage appelle wet exporté (supprime le local) |
| `js/do_wear.js` | `Blindf_off` exporté (push towel) |
| CURRENT | Score #1280 43/44 |

## Fidélité C ↔ JS

### `use_towel` — `apply.c:112–192`

C / JS :
1. `drying_feedback = (obj == uwep)`
2. `!freehand` → no free HAND, ECMD_OK
3. `obj == ublindf` → cannot use wearing, ECMD_OK
4. **si cursed** `switch (rn2(3))` :
   - **2** : `old = Glib & TIMEOUT` ; `make_glib(old + rn1(10,3))` ; filthier/slimy ; dry −1 si wet ; TIME
   - **1** : pas ublindf → ucreamed += `rn1(10,3)` ; Yecch ; `make_blinded(BlindedTimeout + delta, TRUE)` ; sinon lenses/other towel/blindfold ; cursed ublindf → `rn2(2)` cock-eyed/crooked ; else Blindf_off + `dropx` ; dry ; TIME
   - **0** : **fall through** (pas return)
5. si `Glib` : `make_glib(0)` ; wipe hands/gloves ; dry ; TIME
6. sinon `u.ucreamed` : `incr_itimeout(&HBlinded, -ucreamed)` ; ucreamed=0 ; si !Blinded glop + `gulp_blnd_check` sinon FACE clean ; dry ; TIME
7. already clean FACE+HANDS ECMD_OK

JS `case 0: break` puis Glib/cream : **fall-through C respecté**. `rn2(3)` / `rn1(10,3)` / `rn2(2)` aux mêmes loci.

Glib JS wipe : `(u.Glib|0) & TIMEOUT` vs C macro `Glib` = `u.uprops[GLIB].intrinsic` (souvent le timeout). Si glib extrinsèque sans bits TIMEOUT, C wipe, JS skip — edge.

Cream : C `incr_itimeout(&HBlinded, -ucreamed)` ; JS `make_blinded(BlindedTimeout()-cream, false)` puis si !Blind `make_blinded(1,false); make_blinded(0,true)` pour mimer `set_itimeout(1)+make_blinded(0,TRUE)` car **gulp_blnd_check deferred = always false**. Deux `make_blinded` peuvent avoir des side-effects vision en trop ou en moins vs `incr_itimeout`. Approximation nommée gulp, pas la chaîne timeout.

`gloves_simple_name_towel` local vs C `gloves_simple_name` (discovery). D-log nomme.

`Blindf_off` + `dropx` : export do_wear nécessaire. Si `dropx` async mal câblé, extra `--More--`. Hors revue fine.

### wet / dry — `weapon.c`

C `wet_a_towel` : `newspe = amt<=0 ? spe-amt : amt` ; verbose **seulement si newspe>spe** ; carried / mcarried+canseemon ; `finish_towel_change`.

C `dry_a_towel` : `amt<0 ? spe+amt : amt` ; **0 n’est pas no-op** (contrairement à wet amt==0) ; verbose si newspe&lt;spe.

C `finish_towel_change` : clamp 0..7 ; `uwep` → `gu.unweapon = !is_wet_towel` ; `update_inventory` si carried.

JS : mêmes formules ; `game.gu.unweapon` ; update_inventory **sauté** (nommé). `is_wet_towel` : TOWEL && spe&gt;0 ≡ `obj.h`.

`Yobjnam2` thin. `s_suffix_towel` : **même erreur D-1005** (x/z/ch/sh). Pline monstre « fox' towel dries ».

`towel_mcarried` : `!!obj.ocarry` vs C `mcarried` (dans minvent). Un ocarry dangling = faux positif.

### `burnarmor` dry — `trap.c:99–108`

C : `item = hitting_u ? carrying(TOWEL) : m_carrying(victim, TOWEL)` ; while item : si wet `dry_a_towel(item, rn2(oldspe+1), TRUE)` ; break si spe change ; **`item = item->nobj`** (suite de la chaîne, pas « next towel »).

JS héros : `findIndex` TOWEL puis for i++ sur **le tableau invent** — équivalent nobj si invent est l’ordre C. Monstre : `m_carrying` + `nobj` — **C**.

`rn2(oldspe+1)` **avant** dry, même si dry no-op possible (dry amt = 0..oldspe). Si premier towel sec, C continue nobj **en brûlant d’autres `rn2`** pour towels suivants. JS aussi. Puis **ensuite** `for(;;) switch(rn2(5))` slots. Ordre dry-walk **puis** slot RNG : conforme. Avant ce commit JS sautait tout le dry → **`rn2(oldspe+1)` manquants** avant le `rn2(5)` feu. C’est un vrai fix de flux RNG burnarmor.

`water_damage` : passe du wet local trap.js à `weapon.js` `await wet_a_towel(-rnd(7-spe), true)` — unifie. C `wet_a_towel(..., TRUE)`.

### `make_glib` / `TIMEOUT`
C cursed case 2 : `old = (Glib & TIMEOUT)` puis `make_glib((int)old + rn1(10,3))`. JS `(u.Glib|0) & TIMEOUT` + `make_glib` import potion.js. Si `make_glib` JS écrit `u.Glib` mais pas `uprops[GLIB].intrinsic`, le wipe ultérieur `Glib` macro C vs JS `u.Glib & TIMEOUT` diverge. Préexistant potion, activé ici.

### `water_damage` TOWEL
C : `otyp==TOWEL && spe<7` → `wet_a_towel(obj, -rnd(7-spe), TRUE)` return ER_NOTHING. JS : `await wet_a_towel` même formule. `rnd(7-spe)` **avant** wet. Un towel spe=6 : `rnd(1)` toujours 1, spe→7. Conforme. Verbose true : pline damp/wetter si carried.

### `finish_towel_change` uwep
C `if (obj==uwep) gu.unweapon = !is_wet_towel(obj)`. JS `game.gu.unweapon`. Un towel qui sèche en main redevient « unweapon » (bash message) comme C. Si `gu` JS n’est pas lu par wield, dette d’affichage pas RNG.

## Constitution / playbook
Grep RAS. Rule #2 OK. Cadence+port = process smell. `Blindf_off` export : surface élargie, pas un await hors nhgetch. Pas de FORCE. `TOWEL` indexOf local apply **et** weapon **et** trap — trois constantes, même otyp.

## Densité (§2b)
Le **port** towel+wet/dry+burnarmor est right-size (C callers de wet/dry). Coller #1280 full sessions : même tache D-1004. Too-big **process**.

## Documentation
D-1009 Status fixed. Symptom : doapply TOWEL fell through ; wet/dry local-only ; burnarmor dry deferred. Deferred : gulp_blnd_check ; gloves_simple_name gauntlets discovery ; update_inventory ; crystal ball.

**Ne re-nomme pas** `s_suffix` faux (déjà D-1005). CURRENT @#1280 **43**/44 Scr 11404/11405 identique. Journal `#1280 cadence + D-1009` + « stay 43/44 ». `absent.md` / `debt.md` touchés. Rotation journal `…iter1280.md` dans le même SHA.

INDEX : « Cadence 43/44 @#1280 » collé au D-id — même tache process que D-1004.

## Vérification
Green + apply/trap 15/16. Cadence 43/44. seed0009 toujours Scr FAIL. Aucune session publique n’essuie une serviette cursed `rn2(3)`. La preuve utile est **burnarmor** désormais aligné sur des `rn2` towel — non isolée dans le journal.

## Risques / dette
1. Mélange cadence : 43/44 cosmétique.
2. `s_suffix_towel` non-C (bis).
3. `gulp_blnd_check` → double `make_blinded` approximatif.
4. `freehand_towel` local vs `freehand` steed D-1008 : deux copies.
5. Invent hero `findIndex` vs `carrying()` C (premier TOWEL) : si invent JS n’est pas nobj-order, dry d’une autre serviette.

## Complément — cursed `rn2(3)` et burnarmor

C `switch (rn2(3))` **sans** `default` utile : 0 fall-through, 1 et 2 return TIME. JS `default: break` = 0. Si `rn2(3)` JS pouvait renvoyer &gt;2 (non, rn2(3) ∈ {0,1,2}), default mort. OK.

Case 1 ublindf cursed : **`rn2(2)`** cock-eyed/crooked **sans** dry encore — dry **après** le if/else ublindf, toujours si wet. C identique. Case 1 !ublindf : `rn1(10,3)` cream **puis** make_blinded **puis** dry. Deux jets cursed case 1 (rn2(3) déjà tiré) + maybe dry amt −1 **sans** autre rn2 (amt fixe −1). `dry_a_towel(obj, -1, drying_feedback)` : pas de `rn2` dans dry si amt négatif (newspe = spe-1). Burnarmor au contraire `rn2(oldspe+1)` **fixe** la cible. Deux API, JS les distingue. Bon.

`carrying(TOWEL)` C = premier objet otyp TOWEL dans invent. JS `findIndex`. Puis C `item=nobj` continue **même** sur non-TOWEL (is_wet_towel false, pas de rn2). JS `for i++` idem : non-towel skip sans rn2. Premier towel **sec** : C n’appelle pas dry (pas wet) donc **pas** `rn2(oldspe+1)` ; JS `is_wet_towel` false skip. Conforme. Premier towel **wet** : `rn2(spe+1)` ; si newspe==oldspe (dry amt tombe sur spe) C **ne break pas**, continue nobj. JS `if (spe !== oldspe) break`. `dry_a_towel` avec amt = `rn2(spe+1)` ∈ [0,spe] : newspe=amt (amt≥0 set). amt==spe → newspe==oldspe → pas de finish, pas de break, continue. amt&lt;spe → dries, break. C identique.

Cadence #1280 : même Scr 11404/11405 que #1275 et #1270. Le peel towel **n’a pas** touché seed0009. Process : full suite pour un tampon.

## Tableau branches (D-1009)

| Site | RNG | Fidélité |
|---|---|---|
| cursed use_towel | rn2(3) | fall-through 0 **oui** |
| case 2 glib | rn1(10,3) | identique |
| case 1 cream | rn1(10,3) | identique |
| case 1 blindfold cursed | rn2(2) | identique |
| dry after slapstick | amt −1, pas de rn2 | identique |
| wipe Glib / cream | — | gulp approx |
| wet_a_towel water_damage | rnd(7-spe) | identique, maintenant shared |
| burnarmor dry | rn2(oldspe+1) par wet towel jusqu’à change | **était manquant → aligné** |
| burnarmor slots | rn2(5) après dry | ordre C |
| finish unweapon | — | identique |
| s_suffix mon pline | — | **faux x/z/ch/sh** |

Helpers wet/dry sortis de trap.js local : un seul corps C `weapon.c`. Bon 1:1. Cadence collée : process.

C `use_towel` cursed (`apply.c:122–163`) :

```122:163:nethack-c/upstream/src/apply.c
    } else if (obj->cursed) {
        switch (rn2(3)) {
        case 2:
            old = (Glib & TIMEOUT);
            make_glib((int) old + rn1(10, 3));
            /* filthier / slimy */ dry -1; return ECMD_TIME;
        case 1:
            if (!ublindf) { ucreamed += rn1(10, 3); make_blinded(...); }
            else if (ublindf->cursed) You("push ... %s.", rn2(2) ? "cock-eyed" : "crooked");
            else { Blindf_off; dropx; }
            dry -1; return ECMD_TIME;
        case 0:
            break;
        }
    }
    if (Glib) { make_glib(0); wipe; dry; return TIME; }
```

JS `case 0: break` **puis** le `if (Glib)` : c’est le fall-through. Un `rn2(3)==0` **ne return pas** : on tombe sur wipe Glib / cream / already clean. C identique. Rateur classique de port (traduire 0 en no-op return) — **évité**.

`gulp_blnd_check` C (`apply.c:178`) : si cream wipe rend `!Blinded`, C appelle `gulp_blnd_check()` ; si false, `set_itimeout(&HBlinded, 1L)` + `make_blinded(0L, TRUE)` (flash vision). JS suppose gulp **toujours false** et fait `make_blinded(1,false); make_blinded(0,true)`. Un héros avalé par un jelly : C gulp peut **garder** aveugle sans le set_itimeout(1) ; JS force le flash. Approximation nommée. Pas de `rn2` dans gulp typique — écart d’état Blind, pas de keystream towel.

C `burnarmor` (`trap.c:99–113`) : walk towels `rn2(oldspe+1)` **puis** `for(;;) switch(rn2(5))` slots. JS **avant** D-1009 sautait le walk → le premier `rn2(5)` feu tombait trop tôt dans le keystream. Après : walk puis slots. C’est le peel qui **peut** bouger une session publique (feu / explosion / brûler l’armure), contrairement à `use_towel` cursed rarement vu. Le journal ne sépare pas cette preuve. Cadence 43/44 inchangée ⇒ soit aucune session publique n’atteint `burnarmor`+wet towel, soit le keystream coincidait encore (peu de towels wet).

`wet_a_towel` amt≤0 : `newspe = spe - amt` (amt négatif **augmente** spe). `water_damage` passe `-rnd(7-spe)` : mouille. JS unifie le local trap vers `weapon.js`. Un seul corps 1:1 `weapon.c`. Bon.

`s_suffix_towel` : copie de `s_suffix_leash` D-1005 (x/z/ch/sh). Un monstre « fox » qui sèche une serviette : C `fox's` ; JS `fox'`. Même dette, **nouveau** caller pline. Le peel aurait dû importer un `s_suffix` unique ou citer C `hacklib.c`. Process : on répète l’erreur 5 peels plus tard.

`freehand_towel` local vs `freehand` steed.js D-1008 : deux copies. C `freehand()` unique `pickup.c`. Un weld bimanual : les deux JS utilisent `oc_big` ≠ `oc_bimanual`. Même approximation, deux sites.

`is_wet_towel` : `otyp==TOWEL && spe>0`. C `obj.h`. Un towel spe=0 est sec : `use_towel` cursed case 2/1 **skip** `dry_a_towel` (`if (is_wet_towel)`). JS identique. `finish_towel_change` clamp 0..7 : spe ne dépasse pas 7 (max wet). `wet_a_towel` amt positif **set** spe ; amt≤0 **ajoute**. Les deux API dans le même fichier weapon.js. C aussi. Un appel `dry_a_towel(obj, -1)` (use_towel) vs `dry_a_towel(obj, rn2(oldspe+1), TRUE)` (burnarmor) : signes opposés, JS les distingue. Bon.

`doapply` TOWEL : C `use_towel` return ECMD_OK (already clean / no freehand / wearing) vs TIME (cursed slapstick / wipe). JS `res & ECMD_TIME`. Un towel propre **ne consomme pas** de tour. Conforme. Cadence #1280 n’exerce pas ce return.

`make_glib(0)` wipe : C clear GLIB intrinsic. JS `make_glib` potion.js. Case 2 cursed **ajoute** `rn1(10,3)` **avant** ce wipe (si fall-through 0 ensuite Glib encore posé → wipe le retire le même apply). Ordre C : cursed switch return TIME sur 1 et 2, donc **pas** de wipe immédiat après slimy. JS `return` TIME cases 1/2. Identique. Case 0 : pas de slimy, puis wipe si Glib préexistant.

`body_part(HAND)` / `FACE` C vs JS `HAND` string. D-1008 deferral body_part HAND : towel l’utilise aussi. Un poly sans mains : C « no free paw/claw » ; JS peut dire « hand ». Écran, pas RNG.

## Verdict
- Verdict : **PROCESS-SMELL**
- Note : **6.5/10**
- Si je ne devais retenir qu’une critique : **`use_towel` `rn2(3)` avec fall-through 0 et le `rn2(oldspe+1)` burnarmor sont du C sérieux**, gâchés par une cadence #1280 qui répète 43/44 et un `s_suffix` déjà faux au peel laisse.
