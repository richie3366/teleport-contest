# Review 1454 — c6909a83 — `cmd.c` help_dir whole-body port (D-2495)

Metadata: SHA `c6909a83`, `js/lock.js` +105/−52 only. C `cmd.c:4168–4296`
(`help_dir`, staticfn) + `:4121–4165` (`show_direction_keys`, staticfn).
D-log: D-2495.

## Intent vs deliverable

Promise: restart thin `help_dir` in C order — live-binding grid
(`cmd_from_dir` through `visctrl`), live ESC/`SELF` spkeys instead of
hardcoded `27`/`./s`. Diff delivers exactly that plus a same-module
`show_direction_keys(lines, centerchar, nodiag)` with verbatim Sprintf
layouts. Promise = deliverable.

## Inventory

- Changed: `help_dir_move_lines` → `show_direction_keys` (live keys);
  `help_dir` restarted (WIZ_ONLY_LIST, live `getdir_spkey(NHKF_ESC)`,
  dothat, `#if 0` reasoning, msg arm, `highc`/ctrl/`dowhatdoes_core`
  arm, Valid-keys line, grid, up/down/self extras, suppress line).
- `sym.mjs` (required): `cmd_from_dir` js/dokeylist.js:296 sync (LIVE);
  `show_direction_keys` NOT EXPORTED + 2 locals (js/dokeylist.js:378
  pre-existing keylist clone — named in D-log, different owner untouched;
  js/lock.js:203 the new C-matched port — correct shape for a C
  `staticfn`); `getdir_spkey` NOT EXPORTED + 1 local js/lock.js:97 —
  same-module helper for C `gc.Cmd.spkeys[]` array access, not a
  cross-module clone.

## C ↔ JS fidelity

`show_direction_keys` ≡ C `:4121–4165`, arm by arm: falsy centerchar →
`' '` (`:4129–4130` ✓); nodiag cardinal grid N/W-center-E/S (`:4133–4146`
✓); 8-way grid NW-N-NE / W-center-E / SW-S-SE (`:4147–4164` ✓).
Key source: C `cmd_from_dir(d, m)` **is** `cmd_from_func(move_funcs[d][m])`
(`cmd.c:3029`), so JS `cmd_from_dir(dir, MV_WALK)` through `visctrl` ≡ C
`visctrl(cmd_from_func(do_move_*))` whenever the JS move_funcs table
mirrors C's — and the mapping was empirically pinned: the iteration's
first verify FAILed reach on explore-seed0116-wizard-wear-shop (row 7
`b j n` vs `n j b`), fixed to `key(7),key(6),key(5)` = SW,S,SE in C arg
order (`:4158–4162`) ✓. A port that caught and fixed its own ordering bug
against a recorded screen is the opposite of trace tailoring.

`help_dir` ≡ `:4168–4296`: `prefixhandling = spkey != spkeys[NHKF_ESC]`
(`:4184–4185`, live binding not `27` ✓); `dothat = "do that"` ✓;
`:4193–4229` `#if 0` compiled-out → `*buf` always empty, msg arm owns
`:4239–4247` ✓; `!win → FALSE` (`:4232–4234` ✓); letter/`[` gate with
`highc`, `ctrl = sym-'A'+1`, `dowhatdoes_core` + wiz-only gate
(`:4249–4263` ✓); Valid-keys Sprintf shape (`:4265–4268` — JS template
`" to " + dothat` ≡ C `" to %s", dothat` ✓); `NODIAG(u.umonnum)` ≡
PM_GRID_BUG compare (`hack.h:1414` `#define NODIAG(monnum)
((monnum) == PM_GRID_BUG)` ✓); self key `num_pad ? SELF2 : SELF` with
`%4s` → `padStart(4)` (`:4277–4281` ✓); suppress line under msg
(`:4288–4293` ✓); show + return TRUE (`:4294–4296` ✓).
Sole C caller `cmd.c:4101` passes the ESC spkey — the D-log's "always
false there" is consistent with the call site.

Callee closure: all LIVE (`cmd_from_dir`, `visctrl`, `getdir_spkey`
same-module, `highc`, `dowhatdoes_core`, `show_text_pages`) or named omits
(`#if 0` arms, dead `*buf`, NULL-window, dokeylist clone). No STUB in a
live arm. No RNG in either body.

## Hallucinations / overclaim

None. The reach FAIL→fix is disclosed in the D-log with the exact screen
row. Nit only: D-log cites `:4171–4296`, csym prints `:4168–4296` (comment
header lines) — cite drift, not a C-wrong.

## Density

One small C staticfn pair, one file, +105/−52. Right-sized for the
function's C size.

## Verification

Re-ran here (`--base c6909a83~1 --reach-all`):

```text
verify help_dir: baseline c6909a83~1 — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
smoke help_dir: no RNG-tagged reach; fixed smoke spread (24 run): 24 PASS, 0 regressed → REACH-OK
```

0 blocked both sides (vacuous, as stated — coverage row). Rule #2 clean
per `--rulecheck` (SHA 1453 run, tree unchanged in this respect). Diff
grep: no FORCE/DIAG/`getRngLog`/seed/fastforward/coords.

## Actionable C-wrongs

None. No Must-fix.

Verdict: **ACCEPT**
