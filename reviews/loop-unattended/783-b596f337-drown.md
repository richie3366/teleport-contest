# Review 783 — b596f337 — trap.c drown crawl-out (D-1814)

## Metadata
- Full / short hash: `b596f337d4c037729e98d068f9e1fcc00b18eb20` / `b596f337`
- Parent: `373d8905` (D-1813 AWD). Map-driven Open.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 08:05:35 +0200
- D-id: **D-1814**
- Stats: `js/trap.js` +135/−60; `js/eat.js` +12/−1. `js/` insertions **147** ≤250. Band **80–350**.
- Claims to close: Open `trap.c` `drown` remaining: `rnd_nextto_goodpos` / `emergency_disrobe` / crawl-out. Not `lava_effects`.
- JS / map: those three + `reset_faint` export. `c-js-map/data.md`. Archive **Addressed:** D-1814 — fill hash `b596f337` this audit.

## Intent vs deliverable

Git subject promises: Match C `trap.c` `drown` so `emergency_disrobe` / `rnd_nextto_goodpos` / crawl-out `teleds` actually run, instead of a stub that always returns TRUE.

`node scripts/csym.mjs drown` → `trap.c:5058–5199`. `emergency_disrobe` `:4896–4941`. `rnd_nextto_goodpos` `:4944–4972`. `reset_faint` `eat.c:3353–3358`. `teleds` callers include `trap.c:5164`. `--callers emergency_disrobe`: `trap.c:5157`. `--callers reset_faint`: `trap.c:5149` (+ minion/vault named).

Parent: `emergency_disrobe` `{lost:false; return true}`; `is_u` treated missing `mtmp` as hero; thin `teleds_drown`. The diff **does** walk undroppables, shuffle crawl tiles, wake/faint, and call live `teleds(..., TELEDS_ALLOW_DRAG)`. Subject is delivered.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `drown` | LIVE repaired | crawl cluster; drowning `done()` **OMIT named** |
| `emergency_disrobe` | LIVE local | C `staticfn`; Punished ≡ `uball` |
| `rnd_nextto_goodpos` | LIVE repaired | `mtmp === youmonst` only |
| `reset_faint` | LIVE | export `eat.js`; `afternmv === unfaint` |
| `teleds` | LIVE | `teleport.js:1423` (replaces `teleds_drown`) |
| `inv_cnt` / `remove_worn_item` | LIVE | `steal.js` (hack.js / do_wear / steed clones remain) |
| `dropx` / `unmul` / `is_fainted` | LIVE | |
| Amphibious wade / teleport / steed / `feel_newsym` | OMIT named | |

`node scripts/sym.mjs` (clone → import / new):

```
drown            js/trap.js:4857   ASYNC
emergency_disrobe NOT EXPORTED — 1 LOCAL trap.js:4773
rnd_nextto_goodpos js/trap.js:4821   sync
reset_faint      js/eat.js:467   ASYNC
teleds           js/teleport.js:1423   ASYNC
unmul            js/hack.js:1034   ASYNC
inv_cnt          js/steal.js:61   !! ALSO hack.js:272
remove_worn_item js/steal.js:160   !! ALSO do_wear.js / steed.js
dropx            js/do.js:2244   ASYNC
```

`--can trap.js` → teleport `teleds` / eat `reset_faint` / steal `inv_cnt` / do `dropx`: **ALREADY**. FORCE/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. `DIAG` hits are `WT_TOOMUCH_DIAGONAL` only. Rule #2 **clean**.

## C ↔ JS fidelity

**Crawl gate (`:5145–5168`).** `usleep` → `unmul("Suddenly you wake up!")`; `is_fainted()` → `reset_faint()`. Then `multi>=0 && youmonst.data->mmove && rnd_nextto_goodpos`. Waterlevel skips disrobe else `emergency_disrobe(&lost)`. `hliquid("water")`; dump-gear pline; success `teleds(x,y,TELEDS_ALLOW_DRAG)` (`hack.h:1425` ≡ 1); else “But in vain.” **Match.**

**`emergency_disrobe` (`:4896–4941`).** `inv_cnt(TRUE)`; while `near_capacity() > (Punished ? UNENCUMBERED : SLT_ENCUMBER)` with Punished ≡ `uball`. `rn2(invc)` then invent walk skipping loadstone-cursed / amulet / rings / blindfold / body/cloak/gloves/boots/shirt / cursed helm-or-shield / `welded` / `stealoid` / `in_use`; `--i<0 && otmp` break. None → FALSE. `owornmask` → `remove_worn_item(FALSE)`; `*lostsome=TRUE`; `dropx`; `invc--`. JS invent is an array (D-1691) — same `rn2`/`--i` mark. **Match.**

**`rnd_nextto_goodpos`.** Fisher–Yates `N_DIRS`; `is_u = (mtmp == &youmonst)`; `crawl_destination` vs `goodpos(..., 0)`. Parent `!mtmp` hero lie is gone. **Match.**

**`reset_faint`.** `ga.afternmv == unfaint` then `unmul("You revive.")`. JS `game.afternmv === unfaint` (eat.js sets that function at `:570`). **Match.**

**`teleds`.** LIVE `teleport.js` (vault/ball/swallow/`u_on_newpos`/`spoteffects`). Not the deleted drown-only clone.

**Callee closure.** Crawl callees LIVE. Named omits are earlier `drown` arms (wade/teleport/steed/`done` loop), not stubs inside the crawl `if`.

## Hallucinations / overclaim

Do **not** stamp “Match C Amphibious/Breathless/Swimming wade `set_uinwater`.” Do **not** stamp “Match C Teleportation `dotele` / steed `dismount_steed`.” Do **not** stamp “Match C drowning `done(DROWNING)` / `safe_teleds` loop” — JS still `pline('You drown.'); return true`. Do **not** stamp “Match C `feel_newsym` waterwall.” Public pool crawl on seed paths is possible; the `done` loop is **public-unhit**.

## Density

§2b: remaining `drown` crawl cluster + `reset_faint`. +147. Did **not** glue `lava_effects`. Right size.

## Verification

D-log: save-oracle skip; green + cohort 7/7. This audit: `csym` `:5058–5199` / `:4896–4941` / `:4944–4972` / `eat.c:3353–3358` vs HEAD `js/trap.js:4773–4901` / `js/eat.js:467–471`. Rule #2 clean.

## Actionable C-wrongs

None for Must-fix. Named: Amphibious wade; teleport; steed; drowning `done()` loop; `feel_newsym`; leftover `inv_cnt` / `remove_worn_item` clones; worm.c `rnd_nextto_goodpos` clone.

Verdict: **ACCEPT-WITH-DEBT**
