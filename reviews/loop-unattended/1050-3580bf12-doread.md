# Review 1050 — 3580bf12 — doread Blind gate / mail conduct / SCR_TAMING (D-2080)

## Metadata

- SHA: `3580bf12` — `read.c doread Blind formula/book gate, mail illiterate-conduct envelope and SCR_TAMING allowlist (queue owner doread) (D-2080).`
- JS diff: `js/read.js` +~35/−8 (pickup_prev line, Blind block, mail block, confused one-liner, allowlist token, header reword).
- Docs: D-2080 D-log/D-index/CURRENT/NOTES/queue/turns.md map.
- Next index: 1050.

## Intent vs deliverable

Subject promises three doread gaps: the blind spellbook read drawing
study_book «memorize» where C refuses, the mail
illiterate-conduct confirmation, and the SCR_TAMING allowlist entry
so the Knight's taming scroll disappears with «Nothing interesting
happens». Diff actually adds: `pickup_prev = 0`, a Blind gate block,
a SCR_MAIL `y_n` gate, `&& otyp !== SCR_MAIL` on `confused`, and
`SCR_TAMING` in the unimplemented-scroll allowlist, plus a header
comment rewording. Promise == diff; no scope creep.

## Inventory

- Changed: `doread` (read.js:1981+) — three gated blocks, no new
  helpers.
- No deleted symbols (no `sym.mjs` re-point duty), no new imports
  (`y_n` already imported; SCR_TAMING a same-module const).
- Diff grep: no `FORCE`/`DIAG`/`getRngLog`/seed names in control
  flow, no `fastforward`, no hardcoded coordinates.

## C ↔ JS fidelity

`csym.mjs doread` range: `read.c:329–647`. Checked arm by arm
against the C text (read directly):

`pickup_prev = 0` is verbatim C `:359` (`scroll->pickup_prev = 0;
/* no longer 'just picked up' */`).

Blind gate is verbatim C `:561–576`:

```c
} else if (Blind && otyp != SPE_BOOK_OF_THE_DEAD) {
    const char *what = 0;
    if (otyp == SPE_NOVEL)
        what = "words";
    else if (scroll->oclass == SPBOOK_CLASS)
        what = "mystic runes";
    else if (!scroll->dknown)
        what = "formula on the scroll";
    if (what) {
        pline("Being blind, you cannot read the %s.", what);
        return ECMD_OK;
    }
}
```

JS keeps Dead exempt, the novel→words / book→mystic-runes /
unknown-scroll→formula ladder, the `return 0` (= ECMD_OK) before
conduct, and the position after the oclass check. (C's `else if`
chain collapses to sequential returns because the
fortune/shirt/cap/credit/marker/coin/orb/candy arms stay deferred —
equivalent for all live arms.) C `Blind` vs JS
`u.Blind || u.ublind` is the same idiom the pre-existing disappear
block below uses.

Mail gate is verbatim C `:579–597`: `!literate` + `!spe` guards and
the exact prompt with two spaces before «Read anyway?». Confused
override is verbatim C `:580` (`confused = FALSE` → JS
`&& otyp !== SCR_MAIL`; the pre-existing `HConfusion || Confusion`
reading is untouched).

SCR_TAMING arm ships with live callees only: `seffect_taming`
dispatch already live (read.js:1908, D-1502), `maybe_tame` a
pre-existing C-cited clone (read.js:927, C `:1043–1063`), `y_n`
live (getline.js:1531). No stub in a live arm. Deferred arms
(fortune/shirt/cap/credit/grease/marker/coin/orb + Braille lines)
stay map-named in the reworded header.

## Hallucinations / overclaim

None. «No-candidate path is draw-free» is consistent with the
observed Knight outcome (moves to a later owner, no new RNG stall).

## Density

~35 lines of JS on one C function envelope — right-sized per §2b
(the accepted corpus-row exception covers the small end).

## Verification

D-log Verify bullet: `verify --fn doread` → `1 PASS, 1 moved past,
0 unchanged, 0 worse → PROGRESS` (Rogue-92019 PASS; Knight-92105
27→`fprefx`@99) + green + strict + cohort. Re-measured myself:
`hidden-proxy.mjs verify doread --base 3580bf12~1` →
`1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (identical
destinations, both strictly forward). Rule #2 clean
(`imports.mjs --rulecheck`). Claim reproduced exactly.

## Actionable C-wrongs

None.

## Verdict

Verdict: **ACCEPT**
