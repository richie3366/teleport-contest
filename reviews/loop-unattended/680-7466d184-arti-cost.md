# Review 680 — 7466d184 — artifact.c arti_cost + shk.c getprice (D-1719)

## Metadata
- Full / short hash: `7466d1840f1559c861919652cf66b9fe3a301b07` / `7466d184`
- Parent: `ee797b68` (D-1718). This file audits **this SHA only** (third of nine `js/` commits since review **677**). Archive **Addressed:** D-1719 `7466d184`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-30 07:57:43 +0200
- D-id: **D-1719**
- Stats: `js/artifact.js` +18/−1; `js/shk.js` +6/−4; `js/generated/artifacts_data.js` +34/−34. Total `js/` insertions **58** <250. Band **150–350**. C body is 10 lines + table extract.
- Claims to close: Open `arti_cost` after D-1718 / review **679**. Not Hallu currency. Not `artifact_score`. `reviews/loop-2026-08-15/` has no unpaid arti-price Must-fix.
- JS / map: `artifact.js` `arti_cost`; `shk.js` `getprice`; extractor. `c-js-map/data.md` + `turns.md`.
- Prior: **679** named this.

## Intent vs deliverable

Git subject promises: shop `getprice` uses `artilist.cost` (else `100× oc_cost`) instead of table `oc_cost`.

`node scripts/csym.mjs arti_cost` → `artifact.c:2308–2317`. `--callers`: `shk.c:4324` `getprice`; `end.c:920` `artifact_score`. `artifact.h:58` `long cost`. `artilist.h` A() `gs, gv, cost, clr, bn` (`:9–10`). Dummy `#0` cost `0L` (`:81–83`). Excalibur `4000L` (`:88`).

```2308:2317:nethack-c/upstream/src/artifact.c
long
arti_cost(struct obj *otmp)
{
    if (!otmp->oartifact)
        return (long) objects[otmp->otyp].oc_cost;
    else if (artilist[(int) otmp->oartifact].cost)
        return artilist[(int) otmp->oartifact].cost;
    else
        return (100L * (long) objects[otmp->otyp].oc_cost);
}
```

Parent: `getprice` `oartifact` arm left table `oc_cost` (still `/4` when buying); `artilistRaw` had no `cost`. The diff **does** extract A() `args[14]`, copy onto `artilist[].cost`, export `arti_cost`, assign in `getprice`. It **does not** call `arti_cost` from `end.c` `artifact_score`. Named. It **does not** extract `gen_spe`/`gift_value` (`args[12–13]`). Named.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `arti_cost` | LIVE new | `artifact.c:2308–2317` |
| `artilist[].cost` | LIVE extract | A() `cost`; 0 → 100× |
| `getprice` oartifact | LIVE repaired | `tmp = arti_cost` then `/4` if buying |
| `objects()` | LIVE import | already; now also in `artifact.js` |
| `artifact_score` | OMIT named | `end.c:907–939`; `arti_cost` then `*5/2` |
| `gen_spe` / `gift_value` | OMIT named | A() gs/gv |
| Palantir `#if 0` | OMIT named | extractor already skips |

`node scripts/sym.mjs`:

```
arti_cost        js/artifact.js:391   sync
getprice         NOT EXPORTED — 1 LOCAL js/shk.js:2927
artilist         NOT EXPORTED — 1 LOCAL js/artifact.js:265
artifacts_globals_init js/artifact.js:216   sync
objects          js/objects.js:72   sync
artifact_score   (not in js — named omit)
```

Re-point: `getprice` local comment → import `arti_cost` from `artifact.js` (new export, not a stolen local). `--can js/shk.js js/artifact.js arti_cost`: **ALREADY** (this SHA added the import). `--can js/artifact.js js/objects.js objects`: **ALREADY** (module was imported; this SHA adds `objects` to the specifier). `artifact.js` does **not** import `shk.js` — no TDZ. Used inside `arti_cost` / `getprice`, not at module top level. Do **not** add `arti_cost` #2 in `shk.js`. FORCE/DIAG/`getRngLog`/`fastforward`/seed names: none. Extractor is `scripts/` (not scored). `node scripts/imports.mjs --rulecheck`: **Rule #2 clean**.

## C ↔ JS fidelity

**`arti_cost` (`:2308–2317` vs `artifact.js:391–397`).** C `!oartifact` → `objects[otyp].oc_cost`; else `artilist[oartifact].cost` if nonzero else `100 * oc_cost`. JS `objects()?.[otyp]?.oc_cost`; `artilist()[oartifact]`; `if (art?.cost)` (0/`undefined` both fall through). **Match.** Index 0 dummy is `cost: 0` so a confused `oartifact==0` still takes the first arm (`!oartifact`). No RNG.

**Table (`artilist.h` A() vs `artilistRaw`).** Extractor `args[14]` is `cost` in `A(nam, typ, s1, s2, mt, atk, dfn, cry, inv, al, cl, rac, gs, gv, cost, clr, bn)`. Spot-check vs C: dummy `0`; Excalibur `4000`; Stormbringer `8000`; Mjollnir `4000`; Cleaver `1500`; Grimtooth `1200`; Orcrist `2000`; Sting `800`. Generated rows match. `4000L` regex `\d+[Ll]?` **Match**. `artifacts_globals_init` copies `raw.cost | 0`.

**`getprice` (`shk.c:4319–4327`).** C starts from `objects[otyp].oc_cost`, then if `oartifact` replaces with `arti_cost` and `/4` when `shk_buying`. JS the same `Math.trunc` for C integer `/`. **Match.**

```4319:4327:nethack-c/upstream/src/shk.c
    long tmp = (long) objects[obj->otyp].oc_cost;

    if (obj->oartifact) {
        tmp = arti_cost(obj);
        if (shk_buying)
            tmp /= 4;
    }
```

**`get_cost` vs `getprice` polarity.** C `get_cost` (`shk.c:2966–2981`) calls `getprice(obj, FALSE)` — so **no** `/4` inside `getprice` — then, if `oartifact`, `tmp *= 4` after charisma (`:2978–2981` comment: inflate shop price without affecting score). JS `get_cost` still does that `*4`. **Match.** Do **not** treat the `*4` as a second `arti_cost`. Shop **buying from** the hero uses `getprice(obj, TRUE)` so the `/4` sticks. Dummy `artilist[0].cost==0` never reaches the 100× arm when `!oartifact`.

**`artifact_score` (`end.c:907–939`).** C `arti_cost(otmp)` then `*5/2` for the dumplog points, **not** shop `/4`. JS still omits the function. Named. Do **not** route score through `getprice`.

**Callee closure (`getprice` oartifact arm).** LIVE: `arti_cost`, `objects()`, `artilist`. CLONE: none. OMIT named: `artifact_score`. STUB: **none**. Not “dispatch ported, callee stubbed.”

**Extractor vs scored `js/`.** `scripts/` dumps `cost`; `js/generated/artifacts_data.js` embeds the column. Contest Rule #2: no `fs` in scored `js/`. The generated file is the embed. **Match the extract policy.** Do **not** read `artilist.h` at runtime.

## Hallucinations / overclaim

Subject “shop getprice uses artilist.cost (else 100× oc_cost) instead of table oc_cost”: **true**. D-log “get_cost still *4 shop inflate; score uses raw arti_cost”: **true**. Do **not** stamp “Match C `artifact_score` `:907`.” Do **not** stamp “Match C `gen_spe`/`gift_value`.” Do **not** stamp “Match C Palantir.” Journal “fortress held” is not an Excalibur-shop-price proof. Public artifact shop **public-unhit** on scored seeds. Admit that.

## Density

§2b: `arti_cost` + the `getprice` caller C uses + the cost column that function reads. One cluster. +58. Did not glue Hallu currency / `artifact_score`.

## Verification

D-log / journal: save-oracle skip (untagged `artifact.c:arti_cost` / `shk.c:getprice`); private canary **16**/16; green+strict seed8000/0900; focused seed0116; CURRENT cohort **7**/7 + strict. Public **public-unhit** for billed artifacts. Canaries are the cost-column check.

## Actionable C-wrongs

None for Must-fix. Named: Hallu `currency` ROLL_FROM (next); `end.c` `artifact_score` (`:907–939`); A() `gen_spe`/`gift_value`; Palantir `#if 0`; `corpsenm_price_adj`; `Is_candle` age `/2`; bill-price reuse FIXME; `costly_gold`. Do **not** add `arti_cost` #2 in `shk.js`. Do **not** skip `get_cost` `*4` (that is shop inflate, not score). Do **not** treat cost `0` as a sold price (C uses 100×). Do **not** restore table `oc_cost` for `oartifact`. Do **not** extract `args[12]` as cost.

Verdict: **ACCEPT-WITH-DEBT**
