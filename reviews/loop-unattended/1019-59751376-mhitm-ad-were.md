# Review 1019 — 59751376 — mhitm_ad_were mhitu lycanthropy arm (D-2049)

Metadata: SHA `59751376`, D-2049, Open-row port
(queue owner `mhitm_ad_were`, RNG-first `rn2(4)`,
2 sessions). js/ touches 2 files: `mhitu.js`
(+32/−3), `mhitm.js` (comment-only +3/−4). No
stamp owed.

## Intent vs deliverable

Subject promises: new `mhitm_ad_were_u` in the
D-2043 slow-arm shape (unconditional hitmsg, exact
C short-circuit, feverish + exercise + set_ulycn),
`AD_WERE` case in `mhitm_adtyping_u`, mhitm.js
comments re-pointed. Diff actually adds exactly
that, plus two import-name extensions on
pre-existing edges. Promise ≡ diff.

## Inventory

- New JS: `mhitm_ad_were_u` (`js/mhitu.js`,
  non-exported, async — matches sibling
  `mhitm_ad_slow_u` shape); `case AD_WERE` in
  `mhitm_adtyping_u` + doc-list line.
- `sym.mjs`: `defends js/artifact.js:2096 sync`,
  `set_ulycn js/were.js:231 sync` — both LIVE,
  both extend already-imported names on existing
  lines (no new edge; `--can` ALREADY per D-log,
  same-SCC runtime calls only). `AD_WERE=29`
  file-local const matches `mhitm.js:268`.
- No symbol deleted or re-pointed.

## C ↔ JS fidelity

C locus `nethack-c/upstream/src/uhitm.c:4264-4293`
(30 lines, full body via `csym.mjs`).
Branch-by-branch confirm of the mhitu arm
(`mdef==&youmonst`, `:4276-4286`):

- `hitmsg(magr,mattk)` unconditional, RNG-free —
  JS `await hitmsg(mtmp,mattk)` first ✓ (C
  `hitmsg` `:29-81` draws no RNG; D-log cites it).
- Short-circuit order `!rn2(4) && ulycn==NON_PM
  && !Protection && !defends(AD_WERE,uwep) &&
  !mgc-negated(TRUE)` — JS preserves conjunct
  order exactly, so the `rn2(4)` draw position is
  C-identical ✓ (the load-bearing RNG fact: both
  blocked sessions diverged on exactly this draw).
- `urgent_pline("You feel feverish.")` +
  `exercise(A_CON,FALSE)` + `set_ulycn(monsndx(pa))`
  ✓. `pa=magr->data`; call site
  (`mhitu.js:2690`, hitmu path) passes the
  aggressor as `mtmp`, so `mtmp?.data?.mndx ??
  mtmp?.mnum` ≡ `monsndx(pa)` ✓.
- `mhitm_mgc_atk_negated(mtmp,null,true)`:
  null-defender hero idiom shared with 10+
  existing call sites in the same file (grep);
  `verbose=TRUE` matches C's `mdef==&youmonst`
  "avoid harm" path ✓.
- Leftover `d()` kept (`void mhm`): C mhitu arm
  never zeroes damage, like FAMN/SLOW ✓. The old
  `default: mhm.damage = 0` fall-through for
  AD_WERE is gone — correct, since C draws no
  zeroing there.
- `retouch_equipment(2)` named omit (same
  untouchable/retouch chain as eat.js D-0945;
  fires only on a contracted-infection turn).
  Honest caveat in D-log: both blocked C draws
  are nonzero `rn2(4)`, so the feverish arm is
  C-cited but corpus-unreached — the corpus
  movement comes from hitmsg + the draw itself.

Callee closure: hitmsg/defends/set_ulycn/mgc-negated
all LIVE; retouch_equipment OMIT with C citation
(`artifact.c:2640-2705` read, deliberately not
ported). Arm ships.

## Hallucinations / overclaim

None. "1 PASS, 1 moved past" re-measured
identically below. The D-2049-Next pointer
correctly refuses to pop parked `ready_weapon`.

## Density

+35/−7 across the one function family + comment
re-points. One falsifier, one C locus. Right-sized.

## Verification

- Diff-hunk grep: no FORCE/DIAG/getRngLog/seed
  gates/fastforward (only hit is the commit message
  quoting its own Verify line).
- Re-measured `hidden-proxy verify mhitm_ad_were
  --base 59751376~1`: `1 PASS, 1 moved past,
  0 unchanged, 0 worse → PROGRESS`
  (Knight-92149 PASS; Rogue-92184 → ready_weapon
  @132, was 54) — matches the D-log line-for-line.
- Green 2/2 + strict ×2, cohort 7/7, full 44/44
  per pasted tails.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
