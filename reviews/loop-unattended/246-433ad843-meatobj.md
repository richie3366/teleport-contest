# Review 246 — 433ad843 — mon.c meatobj (D-1284)

## Metadata
- Full / short hash: `433ad843af0d57fdbc15b941b1521d5f8fe9c4ff` / `433ad843`
- Parent: `5b4788e1` (D-1283). This file audits **this SHA only**. Archive row **Addressed:** D-1284 `433ad843` already has the short hash.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-20 15:30:43 +0200
- D-id: **D-1284**
- Stats: 10 files, +245 / −35 — `js/mon.js` +151; `js/monmove.js` +14 / −4.
- Claims to close: Open `mon.c` `meatobj` (named from D-1271 / review **233**). Not meatcorpse. `reviews/loop-2026-08-15/` has no unpaid meatobj Must-fix.
- JS / map: `mon.js` `meatobj`; `monmove.js` `postmov` cube call; `c-js-map/turns.md`. meatcorpse / `m_consume_obj` meatbox/poly named.
- Prior reviews this SHA claims to close: **233** named omit gelatinous-cube `meatobj` after `meatmetal`.

## Intent vs deliverable

Git subject promises: “Match C mon.c meatobj so a gelatinous cube that postmovs onto a floor pile engulfs inedibles and eats organics, instead of leaving the pile for mpickstuff.”

C `meatobj` (`mon.c:1531–1648`): tame return 0; walk `level.objects[mx][my]` via `nexthere`; prize skip; rider corpse `revive_corpse` then break; petrify-corpse / `ROCK_CLASS` / uball / uchain / `SCR_SCARE_MONSTER` continue; else `!is_organic || obj_resists(5,95) || !touch_artifact` (plus strangulation / slow-digest / poison / `mstoning` / green-slime) engulf `obj_extract_self`+`mpickobj`; else devour `m_consume_obj` (YUM YUM even if `!verbose`). Poly/death `ptr != original_ptr` → `!ptr ? 2 : 1`. Engulf summary `pline1` / You_hear slurping. Caller `monmove.c` `postmov` `:1669–1672` `ptr == &mons[PM_GELATINOUS_CUBE]` after `meatmetal`, before `meatcorpse` / `mpickstuff`.

Old JS: named omit after D-1271; cube `postmov` fell through to `mpickstuff`.

The diff **does** live `meatobj` and the cube `postmov` call (`etmp >= 2` return). It does **not** port `meatcorpse` or expand `m_consume_obj` meatbox/poly/uball/grow/stone/`mon_givit`. Named.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `meatobj` | C `:1533–1648`, **new** | |
| postmov cube call | C `:1669–1672`, **wired** | mndx; `mons()` allocates |
| `m_consume_obj` | C `:1392`, **imported live partial** | heal `oc_weight` + `delobj`; meatbox/poly named |
| `mpickobj` / `obj_extract_self` | C, **imported live** | |
| `obj_resists(5,95)` | C `zap.c`, **imported live** | short-circuit after `!is_organic` |
| `touch_artifact` | C `artifact.c`, **imported live** | monster blast named (D-1271) |
| `is_organic` / prize helpers | C `objclass.h` / `obj.h`, **imported live** | |
| `revive_corpse` | C `do.c`, **imported live** | dynamic `do.js` |
| `ofood_meat` / `mstoning_meat` | C `ofood` / `mstoning`, **clone** | Medusa eggs engulf |
| `objdescr_is_meat` | C `o_init.c:352`, **clone** | `oc_descr_idx` like `objnam.js` |
| `You_hear_meat` | C `You_hear`, **local clone** | Unaware/Underwater named |
| `meatcorpse` | C `:1656`, **named omit this SHA** | D-1285 |

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` writes / seed names / recorded coordinates. Rule #2 clean. **New RNG:** organic `obj_resists` `rn2(100)`; `touch_artifact` may roll. Metal `!is_organic` short-circuits **before** `obj_resists` (C same; canary “no rn2 on metal engulf” is C).

## C ↔ JS fidelity

Pinned C inedible vs edible (`mon.c:1585–1633`):

```
        } else if (!is_organic(otmp) || obj_resists(otmp, 5, 95)
                   || !touch_artifact(otmp, mtmp)
                   || (AMULET_OF_STRANGULATION || RIN_SLOW_DIGESTION)
                   || (opoisoned && !resists_poison)
                   || (mstoning && !resists_ston)
                   || (GLOB_OF_GREEN_SLIME && !slimeproof)) {
            ++ecount; distant_name; extract; mpickobj;
        } else {
            ++count; cansee pline_mon eat; YUM YUM; m_consume_obj;
            if (ptr != original_ptr) return !ptr ? 2 : 1;
        }
```

JS copies that `else if` / `else`, including short-circuit polarity. Prize / rider / petrify-corpse / ROCK / ball&chain / scare continue match `:1554–1583`. Engulf summary is `pline(buf)` ≡ C `pline1`, not `pline_mon`. YUM YUM is inside `cansee` and **not** verbose-gated (C `:1620–1623`). Poly check is mndx because `mons()` allocates (D-0928 #1130) — same analog as `meatmetal`.

`m_consume_obj` is **not** a no-op: non-pet `healmon(oc_weight)` then `delobj`. C also meatbox / poly / grow / stone / `mon_givit`. Named. Devour still removes the object. This is **not** “Match C dispatch, callee is a stub.”

Caller: JS `postmov` cube after `meatmetal`, `etmp >= 2` return (C same, including off-level 3 which C comments unimplemented). Ate/engulfed (1) still falls through to `mpickstuff`. `meatcorpse` still named this SHA.

## Hallucinations / overclaim

Subject + D-1284 say a cube `postmov`s onto a floor pile and engulfs inedibles / eats organics. **The function + cube caller are the hunk.** Stamping **Addressed:** D-1284 is fair. Do **not** stamp “Match C `meatcorpse`.” Do **not** stamp “Match C `m_consume_obj` meatbox/poly/`mon_givit`.” Do **not** stamp “Match C `mon_would_consume_item`.” Do not stamp “Match C engulf `pline_mon`” (C is `pline1`).

## Density

One C function plus the one `postmov` site C uses. ~151 JS lines. Sibling `meatcorpse` is a named omit of another function, not glued. Right size.

## Branch-by-branch confirm

1. Tame cube: return 0. Match `:1542–1543`.
2. Empty cell: 0, then `mpickstuff`. Match.
3. Metal (not organic): engulf, **no** `obj_resists` `rn2`. Match short-circuit.
4. Two metals: ecount 2, “several objects” / several slurps. Match `:1605–1606`.
5. Boulder / scare / mines-or-soko prize: continue. Match skip arms.
6. Cockatrice corpse, `!resists_ston`: continue, neither eat nor engulf. Match `:1571–1573`.
7. Green-slime glob, `!slimeproof`: engulf. Match `:1596–1597`.
8. Organic food: `pline_mon` eat, `m_consume_obj` heal+delobj. Match devour.
9. YUM YUM scroll, cansee: `Yum!` / `Yum.` even if quiet. Match.
10. postmov cube vs jackal: only cube mndx. Match caller. Public-unhit unless a cube `postmov`s onto a pile.

## Anti-pattern / Rule #2 (this SHA `js/`)

No FORCE / fs / seed-named gates. Dynamic `import('./do.js')` is relative ESM. Plain ESM.

## Verification

Journal: private canary **23**/23 (C body+caller; tame / empty; metal engulf no `obj_resists` rn2; two metals; boulder / scare / prize skip; cockatrice skip; slime glob engulf; food eat+heal; YUM YUM; postmov cube vs jackal; Rule #2); green+strict seed8000/0900; cohort **7**/7 + strict 1500/1800/0012/0004/0007/2200/0383. **Public-unhit** unless a cube `postmov`s onto a floor pile. Cadence this audit: full `sessions` at HEAD `9486280d` **44**/44 Scr **11,405**/11,405 RNG **100%**.

## Actionable C-wrongs

None for Must-fix. Branch order and `obj_resists` short-circuit match C call-for-call; engulf/devour callees are live `mpickobj`/`delobj`, not stubs.

Named omits (map, not Must-fix):

1. `meatcorpse` (next SHA)
2. `m_consume_obj` meatbox / poly / uball / grow / stone / `mon_givit`
3. rider off-level return 3 (C unimplemented)
4. `touch_artifact` monster blast; `You_hear` Unaware/Underwater

Do not Must-fix “mndx instead of `ptr != original_ptr`.” Do not Must-fix “local `mstoning_meat`.” Do not pull meatcorpse this SHA.

## Callers / RNG ledger

C: `postmov` cube only. JS: same. New positional RNG only on organic `obj_resists` / rare `touch_artifact`. Public fortress is not evidence a cube slurped a pile.

## Verdict

- Verdict: **ACCEPT-WITH-DEBT**
- One sentence: cube `postmov` now engulfs inedibles via live `mpickobj` and eats organics via live `delobj`; meatcorpse stays named.
- Must-fix stays empty for this SHA; archive already has **Addressed:** D-1284 `433ad843`.
