# Review 823 — 64050628 — mkmaze.c makemaz knox load_special (D-1853)

## Metadata

- Full / short hash: `640506289124c776e6ad1f89b0f4ed0d54633032` / `64050628`
- Parent: `f0f42bea` (observer docs). Map-driven Open: `makemaz` `knox` (Fort Ludios).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-05 11:30:58 +0200
- D-id: **D-1853**
- Stats: `js/mklev.js` +262/−~2. `js/` insertions **262** >250. Band **80–450**.
- Claims to close: `knox` proto from `dat/knox.lua` (167 ln, in pinned upstream). No corpus movement claimed — map-driven row like D-1852.
- JS / map: `load_knox` / `load_special_proto`. `c-js-map/data.md`.

## Intent vs deliverable

Git subject promises: `load_knox` from the lua body in order — solidfill, 76×20 map, non_diggable, branch + tele regions, throne COURT, three percent-50 swaps, vault iterate, towers, ZOO, arrival room, unlit walls, BARRACKS, 11 doors, stock, gems, standard epilogue. The diff **does** exactly that in lua order. Nothing else touched.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_knox` | LIVE new (local, 1st) | knox.lua arm-by-arm (below) |
| `l_levregion` / `l_teleport_region` | pre-existing exports | via `levregion_add` → flipped store (correct pattern) |
| `selection_iterate_lua` | pre-existing local | y-outer `:2435–2442` ≡ `nhlsel.c:925–950` |
| `lua_random2` | pre-existing local | `lo+rn2(hi-lo+1)` ≡ `math.random` |
| `add_room` / `topologize` / `add_doors_to_room` / `flood_fill_rm` / `litstate_rnd` / `mkgold` / `maketrap` / `mktrap_seen_victim` / `mksobj_at` | pre-existing, reused | no clone #2 written |
| humidity `get_location`; `ensure_way_out`; `count_level_features`/solidify/premap | OMIT named | in this commit |

No deleted/re-pointed symbol (pure add). No cycle claim. FORCE/DIAG/`getRngLog`/`fastforward`: **none**. Coords are lua content. Rule #2: clean.

## C ↔ JS fidelity

**Map.** `KNOX_MAP` 20×76 byte-equal to lua at runtime (row 10 `\\\\` in source = `\\` runtime ≡ lua `\\` throne glyph).

**Order/counts vs knox.lua.** non_diggable whole-map walls/bars → branch (08,16) + up/down tele (06,15,09,16) via storing helpers → COURT (37,08,46,11) `needfill FILL_NORMAL` + topologize + doors → percent-50 Croesus row (43,10 else 43,09 + `\`→THRONE / `.`→ROOM) → percent-50 S/`|` at 47,09/10 → vault light + treasury iterate → percent-50 `|`/S at 36,09/10 → 4 tower lights → irregular ZOO → arrival OROOM `needfill 0` → unlit entry walls → irregular BARRACKS → 11 doors in lua order with lua states (fixed state skips `rnddoor` ✓) → 16 soldiers + lieutenant + stone giant + 4 D + 4 eels in lua order → 12 corner gems. All coords match lua verbatim (spot-verified: soldiers, dragons, eels, gems, doors).

**RNG.** 9 draw sites, all in lua position: 3× `percent(50)`; per-treasury-cell `600+lua_random2(0,300)` then `rn2(3)==0` trap → `rn2(3)==0` spiked-else-mine (≡ `math.random(0,2)==0` twice); `litstate_rnd(1)` ×2; `flip_level_rnd(3)` tail (C default `allow_flips=3`). Y-outer iterate confirmed at `:2435`. **Match call-for-call.**

**Regions (contrast Val-strt).** knox uses the storing helpers (`levregion_add` + `get_location` at `:725–751`) while origin is set, so `flip_level` flips them and `fixup_special` places them — the C pipeline (`sp_lev.c:700–726`, `mkmaze.c:606`). Correct; Val-strt's direct post-flip place (review 822) remains the family outlier under the standing `data.md:696` row.

**Callee closure.** One `knox` arm. Every callee LIVE or pre-existing local. No STUB in a live arm.

## Hallucinations / overclaim

None. No corpus claim beyond the explicit no-blocked note; `SPLEV_CHAR2TYP` glyph table cited with the actual mapping.

## Density

§2b: one proto (`knox.lua` 167 ln → +262 JS with calling convention). Same envelope as prior single-proto ports. Right size.

## Verification

This audit: `node scripts/hidden-proxy.mjs verify makemaz --base 64050628~1` → **0 sessions blocked** — D-log's note accurate, not presented as PASS. Public gates carry it (green/strict/cohort/full 44/44 cited); cadence re-checks at end of iteration.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
