# Review 792 — 7c76fdb5 — mkmaze.c makemaz bigrm-1/10/13 load_special (D-1822)

## Metadata
- Full / short hash: `7c76fdb52892dd5c8340262895a645eb673ae3df` / `7c76fdb5`
- Parent: `0c3e8ca5` (D-1821). Map-driven Open. Completes Big Room 13/13. Three `makemaz` `load_special` rows (§2b).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 20:33:23 +0200
- D-id: **D-1822**
- Stats: `js/mklev.js` +442/−4. `js/` insertions **442** → review ceiling **450**. Band **80–350**.
- Claims to close: Open `bigrm-1`/`-10`/`-13`. Not minend-3.
- JS / map: `load_bigrm_1` / `_10` / `_13`; `lua_random2`; `selection_line_rel` / `rect_rel` / `fillrect_rel`; `lspo_terrain_sel`; `splev_apply_map_at`. `c-js-map/data.md`. Archive **Addressed:** D-1822 `7c76fdb5`.

## Intent vs deliverable

Git subject promises: those `rnd(13)` variants fell through to a blank maze; C loads `dat/bigrm-{1,10,13}.lua`.

`node scripts/csym.mjs makemaz` → `mkmaze.c:1126–1223`. `lspo_map` `sp_lev.c:6075+` — table `coord` with no `croom` sets `gx.xstart = x` **absolute** (`:6179–6183`). `get_table_xy_or_coord` `:3188–3204`. `lua_random2` ≡ nhlib `math.random(lo,hi)` → `lo+rn2(hi-lo+1)`. `l_selection_line` / `rect` / `fillrect` via `get_location_coord` then `selection_do_line`.

Parent: 1/10/13 omitted. The diff **does** add the three loaders and the line/rect/terrain helpers. Loop bounds 15/6/28 match all three luas.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_bigrm_1` / `_10` / `_13` | LIVE new | lua bodies |
| `lua_random2` | LIVE | `lo+rn2(hi-lo+1)` |
| `selection_line_rel` / `rect_rel` / `fillrect_rel` | LIVE | C nhlsel after `get_location_coord` |
| `lspo_terrain_sel` | LIVE | |
| `splev_apply_map_at` | LIVE | nested map; absolute coord |
| `splev_reset_xystart_size_keep_spmap` | LIVE | C `reset_xystart_size` |
| `l_teleport_region` / `splev_mazewalk` / `levregion_add` | LIVE | bigrm-10 |
| `wallify_map` | LIVE | bigrm-13 `des.wallify` |
| ensure_way_out / humidity / dry-boulder | OMIT named | |

`node scripts/sym.mjs`:

```
load_bigrm_1     NOT EXPORTED — 1 LOCAL mklev.js:2325
load_bigrm_10    NOT EXPORTED — 1 LOCAL :2431
load_bigrm_13    NOT EXPORTED — 1 LOCAL :2518
lua_random2      NOT EXPORTED — 1 LOCAL :12317
splev_apply_map_at NOT EXPORTED — 1 LOCAL :1786
```

No clone→import. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**.

## C ↔ JS fidelity

**bigrm-1.** 18×75 room. `percent(80)` then `math.random(1,#terrains)` + `math.random(0,5)`. JS `lua_random2(1,5)-1` and `lua_random2(0,5)` — six choices, last is none. Terrain −/F/L/T/C → HWALL/IRONBARS/LAVAPOOL/TREE/CLOUD. Line / twin / plus / brackets (`rect` then `.` lines) / snake (`fillrect` + 8 holes `i=0..7`) match lua `:35–61`. Lit `area(01,01,73,16)`. 15/6/28. noflip. **Match those arms.**

**bigrm-10.** 19×71 fog. `percent(40)` then `chance=5` C→`.` then remaining C→ L/}/T/-/F (`lua_random2(1,5)`). Lit whole; **no** `non_diggable`; teleport_region dir=down exclude fog; 15/6/28 **before** mazewalk; `mazewalk` (4,2) south `stocked=0`; `levregion` stair-up exclude fog; `des.stair("down")` only. JS order matches. **Match.** No extra up-stair.

**bigrm-13.** 19×75. `idx = math.random(1,8)`. Eight filters: all / x odd / checker / y odd / y even / `math.random(0,1)==0` / Lua `(x/3)%2==y%2` (JS float `/` then `%`) / `(x+1)//3==y` (`Math.floor`). Nested `y=0..2`, `x=0..6`, `coord={12+x*9, 4+y*5}`. C `lspo_map` no croom: `xstart = x` absolute (`:6182–6183`). JS `splev_apply_map_at(PILLAR, 12+x*9, 4+y*5)` then `reset_xystart_size` (keep SpLev_Map). Lit after reset; `wallify` no-arg bounds; nondig; stairs; 15/6/28. **Match the nested-map arm.**

**Callee closure.** Three `load_special` variants; line/rect/fillrect/terrain/map-at/mazewalk/levregion/wallify LIVE. Named OMITs only. No STUB in a shipped arm.

## Hallucinations / overclaim

Do **not** stamp ensure_way_out / humidity. Do **not** treat lua `math.random` as JS `Math.random`. Do **not** add `non_diggable` or a second up-stair on bigrm-10. Nested map coord is **absolute**, not outer-map-relative.

## Density

§2b: remaining three Big Room protos + the selection/terrain callees they need. +442. Completes 13/13. Did **not** glue minend-3. Right size (large but one family).

## Verification

`verify.mjs --fn makemaz` → PASS syntax; PASS rule2; **PASS hidden (no corpus session blocked on makemaz)**; green + cohort + full 44/44. Public suite does not walk these variants. This audit: lua 1/10/13 vs HEAD `:2325–2607`; `csym` `makemaz` `:1126–1223`; `lspo_map` `:6169–6184`. Rule #2 at end-of-iter.

## Actionable C-wrongs

None that must block the next port. Named (map): ensure_way_out; humidity; dry-boulder.

Verdict: **ACCEPT-WITH-DEBT**
