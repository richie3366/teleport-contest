# Review 782 — 373d8905 — trap.c untrap remaining disarm helpers (D-1813)

## Metadata
- Full / short hash: `373d8905a69f0e66cb472629fdce310609ded92f` / `373d8905`
- Parent: `12298526` (D-1812 AWD). Map-driven Open.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 07:50:16 +0200
- D-id: **D-1813**
- Stats: `js/trap.js` +528/−60. `js/` insertions **528** >250 → ceiling **450**. Band **80–350**.
- Claims to close: Open `trap.c` `untrap` remaining holding / landmine / shooting / box / pit-rescue. Not `dotrap`.
- JS / map: floor `ttyp` switch + those helpers. `c-js-map/turns.md`. Archive **Addressed:** D-1813 `373d8905`.

## Intent vs deliverable

Git subject promises: Match C `trap.c` `untrap` so holding / landmine / shooting / box / pit-rescue actually run, instead of returning 0 on a seen floor trap.

`node scripts/csym.mjs untrap` → `trap.c:5847–6096`. `untrap_prob` `:5287–5337`. `cnv_trap_obj` `:5340–5371`. `try_disarm` `:5440–5527`. `disarm_holdingtrap` `:5551–5591`. `disarm_landmine` `:5593–5603`. `disarm_shooting_trap` `:5663–5673`. `try_lift` `:5676–5696`. `help_monster_out` `:5699–5791`. `disarm_box` `:5793–5817`. `untrap_box` `:5820–5844`. `--callers untrap`: artifact `#invoke`, lock autounlock, `trap.c:5253` `#untrap`. `--callers move_into_trap` is only `try_disarm` `:5516`.

Parent: `if (can_reach_floor(false)) return 0` on `tseen`; container arm skipped `untrap_box`. The diff **does** run those five families. Subject is delivered. SQKY_BOARD / `stumble_on_door_mimic` / adjacent-Whoops `move_into_trap` are **named omits**, not silent default-0.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `untrap` | LIVE repaired | |
| `untrap_prob` / `try_disarm` / `disarm_*` / `untrap_box` / `try_lift` / `help_monster_out` | LIVE local | C `staticfn` |
| `cnv_trap_obj` | LIVE | export (`dig.c` caller) |
| `reward_untrap` / `chest_trap` / `reset_utrap` / `instapetrify` | LIVE | already in `trap.js` |
| `fill_pit` | LIVE | `dig.js:839` |
| `has_magic_key` / `ynq` / `can_reach_floor` | LIVE | static imports |
| `check_capacity` | CLONE inlined | `hack.c:4398` `near_capacity>=EXT_ENCUMBER` |
| `disarm_squeaky_board` | OMIT named | default “cannot disable” |
| `move_into_trap` | OMIT named | Whoops adjacent |
| `stumble_on_door_mimic` | OMIT named | |

`node scripts/sym.mjs` (clone → import / new):

```
untrap           js/trap.js:5537   ASYNC
untrap_prob      NOT EXPORTED — 1 LOCAL trap.js:5203
cnv_trap_obj     js/trap.js:5236   ASYNC
try_disarm       NOT EXPORTED — 1 LOCAL trap.js:5262
disarm_holdingtrap NOT EXPORTED — 1 LOCAL trap.js:5361
fill_pit         js/dig.js:839   sync
chest_trap       js/trap.js:5790   ASYNC
has_magic_key    js/artifact.js:1837   sync
ynq              js/getline.js:1522   sync
```

`--can trap.js` → engrave / dig / artifact / getline: **ALREADY**. FORCE/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. `DIAG` hits are `WT_TOOMUCH_DIAGONAL` only. Rule #2 **clean**.

## C ↔ JS fidelity

**`untrap` envelope.** `has_magic_key` → force; `getdir(null)`; container → `untrap_box` return 1; autounlock door skips floor prompt; `tseen` only; `here` `boxcnt`; `deal_with_floor_trap = can_reach_floor(false)`; can’t-reach pline; pit+box skip; `ynq` trap-vs-box; `u.utrap` return 1; furniture/object mimic `stumble_onto_mimic`; switch; then box `ynq`/`disarm_box`/`untrap_box`; door luck-skip **unchanged** (D-1495) with `ynq`. **Match the shipped control flow.**

**Switch (`:5965–5988`).** BEAR/WEB → `disarm_holdingtrap`; LANDMINE; DART/ARROW `disarm_shooting_trap`; PIT/SPIKED_PIT `help_monster_out`. SQKY_BOARD C calls `disarm_squeaky_board` — JS **default** cannot-disable (**OMIT named**).

**`untrap_prob`.** chance 3; web blade/Sting/fire 1 else `!webmaker` 7; Conf/Hallu +1; Blind +1; `Stunned`≡`HStun` (`youprop.h:81`) JS `HStun\|\|u.Stunned`; Fumbling ×2; `madeby_u` −1; Ranger bear ≤3 → 0; Rogue `rn2(2*MAXULEV)` / questart; Ranger −1; `chance<1`→1; `rn2(chance)`. **Match.**

**`try_disarm`.** Occupant / boulder / diagonal `bad_rock`+`WT_TOOMUCH_DIAGONAL` / `can_reach_floor` / `rider_cant_reach`. Fail: `rnl(5)` Whoops — bear `abuse_dog`+`rnd(4)`+`killed`; web stick `maketrap`/`dotrap(NOWEBMSG)`/`dismount_steed`; `under_u` `dotrap(FAILEDUNTRAP)`; **else `move_into_trap` named no-op**. Else “difficult to disarm.” **Match except that named else.**

**Holding / landmine / shooting.** `try_disarm` then extract/`cnv_trap_obj` / web cut-or-burn / `50-rnl(50)`. `the_your[madeby_u]`. C `bare_artifactname(uwep)` even when `wep` is swap — JS same. **Match.**

**`help_monster_out`.** `check_capacity(NULL)` inlined; `untrap_prob`+`helpless`; cockatrice `instapetrify`; lift `try_lift` twice; `reward_untrap`; `fill_pit`. **Match.**

**Box.** `untrap_box` find `rn2(MAXULEV+1-ulevel)<10` / `ynq`; `disarm_box` `ACURR(A_DEX)+ulevel` Rogue ×2 / `chest_trap(FINGER,TRUE)`. **Match.**

**Callee closure.** Subject arms: callees LIVE or **OMIT named** (`move_into_trap`, squeaky, door-mimic). Not an unnamed stub-in-live-arm.

## Hallucinations / overclaim

Do **not** stamp “Match C `disarm_squeaky_board`.” Do **not** stamp “Match C `move_into_trap` / `stumble_on_door_mimic`.” Do **not** stamp “Match C `test_move` export.” `#untrap` on a seen bear trap is public-possible; squeaky / Whoops-step / door-mimic are **public-unhit**.

## Density

§2b: remaining `untrap` floor+box cluster. +528. Did **not** glue `drown`. Right size.

## Verification

D-log: save-oracle skip; green + cohort 7/7. This audit: `csym` `:5847–6096` and helper ranges vs HEAD `js/trap.js:5203–5534` / `:5537+`. Rule #2 clean.

## Actionable C-wrongs

None for Must-fix. Named: `disarm_squeaky_board`; `move_into_trap`; `stumble_on_door_mimic`. Pickup.js `check_capacity` clone remains.

Verdict: **ACCEPT-WITH-DEBT**
