# Review 1766 — e1ef155a9 — bhito wand-on-object (D-2807)

- SHA: `e1ef155a9` (coverage; the otyp switch)
- Files: `js/zap.js` `bhito` (`:5393`)
- Queue row: Open coverage, 0 corpus blocks cited
- Banned grep: 0 hits in the hunk. `imports.mjs --rulecheck` on this SHA: "Rule #2 clean".
- `imports.mjs --can zap.js hacklib.js upstart` → `ALREADY`. Same for `sndprocs.js Soundeffect`. `se_crumbling_sound` is `js/generated/seffects_data.js:63`. `--can` basenames its target, so `generated/seffects_data.js` is not a graph key it can see. The import is static and the const is read inside `bhito`, not at module top.

## Intent vs deliverable

Subject promises one `bhito`: `res` starts at 1, make-invisible leaves it, slow/speed/nothing/healing set it to 0, the default `impossible` does not clear it, undead turning calls `revive(obj, true)` then the see / healer / appears lines, polymorph bumps `polypiles` and re-hides cover, striking uses `newsym_force` and hallucinated `rndmonnam`, teleport calls `maybe_unhide_at` on the old spot, and a stray bypass bit is cleared. The function body does that.

## Inventory

| JS symbol | Class | C |
|-----------|-------|---|
| `bhito` | C body, async, file-local | `zap.c:2119–2424` |
| `do_osshock` | same file, sync | `rn2` per quantity; shop bill still deferred inside it |
| `hideunder` | imported from `mon.js:3673` (sync) | not the `monmove.js:1280` clone |
| `maybe_unhide_at` | imported, awaited | `mon.c:4696–4720`; hero `u_at` arm still absent |
| `Soundeffect` | imported, empty body | `sndprocs.js:38` |
| `learnwand` / `revive_egg` / `unpunish` | sync | live |
| `stone_to_flesh_obj` | same file | returns 0/1 (`:4935`, `:5046`) |

`csym --callers bhito` lists the direct calls and the comments: `zap.c:2981` invent stone-to-flesh → `zap.js:4828`; `:3402` upward hideunder → `:6656`; `:2451` is a comment; `extern.h:3979` and `obj.h:144` are not calls. Function-pointer uses that the name search misses, read at the D-log lines: `bhitpile(obj, bhito, …)` at `zap.c:3244` → `zap.js:6476` and at `:3384` → `:6649`; `bhit(…, bhitm, bhito, &obj)` at `:3448` → `:6858`. No `muse.c` reference.

`sym.mjs`:

```
upstart          js/hacklib.js:220   sync
Soundeffect      js/sndprocs.js:38   sync
se_crumbling_sound js/generated/seffects_data.js:63   export const
cxname_singular  js/objnam.js:1344   sync
corpse_revive_type js/mkobj.js:3993   sync
noname_monnam    js/do_name.js:1091   sync
type_is_pname    js/do_name.js:590   sync
pline_The        js/display.js:7677   ASYNC — await required
newsym_force     js/display.js:5088   sync
maybe_unhide_at  js/monmove.js:1347   ASYNC — await required
hideunder        js/mon.js:3673   sync
learnwand        js/zap.js:2564   sync
```

## C ↔ JS fidelity

Self-hit (`zap.c:2130`) returns 0. Bypass (`:2165–2170`): `context.bypasses` returns 0; otherwise the bit is cleared. `debugpline1` "pulsate" is not called. Named, and the bit clear still happens. A null `obj`/`otmp` returns 0 before that. C would not be passed null (`NONNULLARG12`).

Floor check (`:2178–2179`) calls `impossible` and continues. Ball sets `res = 0`. Chain + opening/knock sets `learn_it` and `unpunish` and leaves `res` at 1. Other chain zaps set `res = 0`. Both skip the switch. JS `:5411–5427` matches.

Polymorph (`:2191–2220`): unpolyable sets `res = 0`. `!uconduct.polypiles++` livelogs once. Box calls `boxlock` and ignores the return. `obj_shudders` then `cansee` → `learn_it`, `do_osshock` (its `rn2(luck+45)` loop is sync), then `hideunder` if cover was the pile head under an undetected hider. Otherwise `poly_obj(obj, STRANGE_OBJECT)` and `newsym` on the result. JS `:5429–5468` matches. Cover is `objects_at(ux, uy)`, the pile head.

Probing (`:2222–2274`): `res = !dknown`, `observe_object`, container/statue sets `cknown`/`lknown`, trap line, empty line, Schroedinger `rndmonnam` only when hallucinating, else contents `observe_object` and `display_cinventory`, then `res = 1`. Tin and egg gates match, including egg `res = 1` only when `!known && corpsenm != NON_PM`. `if (res) learn_it`. `You` is `vpline`, so the `%s` line is not left literal.

Cancellation `newsym`. Drain `drain_item(obj, TRUE)` and does not set `learn_it`. Striking (`:2275–2312`): `maybelearnit = cansee || !Deaf` once. Boulder `Soundeffect` then see/hear, `fracture_rock`. The JS `Soundeffect` body is empty (`sndprocs.js:38–41`), matching the non-integrated macro; it does not roll RNG. Statue: `rndmonnam` only when `break_statue` and `cansee` and `Hallucination`. The other arm calls `breaks` or `hero_breaks`, not both, then `newsym_force` only when something broke, and sets `res = 0` even when it broke. Boulder and statue leave `res` at 1. JS `:5521–5558` matches.

Teleport saves `ox, oy`, `rloco`, `maybe_unhide_at`. Make-invisible breaks with `res` still 1. Undead turning: egg `revive_egg`. Corpse samples `by_u = !mon_moving`, `corpse_revive_type`, `cxname_singular`, and location before `revive(obj, TRUE)`. Fail restores `norevive` and sets `res = 0`. Success: see + spot → resurrected + `learn_it = by_u ? true : zap_oseen`; see + not spot → `The(corpsname)` unless a pname, then "disappears"; blind healer hears the revive or a defibrillator; `canspotmon` "appears"; `exercise(A_WIS)` only when `learn_it`. `game._zap_oseen` is what `muse.js` sets for `gz.zap_oseen`. A non-corpse, non-egg leaves `res` at 1. JS `:5572–5621` matches. `revive`'s second argument is `true`, not `by_u`.

Opening/locking: `Is_box` ? `boxlock` : `res = 0`, then `learn_it` when `res`. Slow, speed, nothing, healing, extra healing set `res = 0`. Stone to flesh assigns `stone_to_flesh_obj` and does not set `learn_it`. Default `impossible("What an interesting effect (%d)", otyp)` leaves `res` at 1. `learnwand` when `learn_it`. JS `:5623–5658` matches.

`maybe_unhide_at` (`mon.c:4704–4711`) uses `m_at` or, if none, `u_at` → `youmonst` / `u.uundetected`. JS (`monmove.js:1347–1350`) returns when `m_at` is null. The hero arm is the named omit. The monster arm (undetected, hides-under with no object / trapped / not `can_hide_under_obj`, or eel out of water) matches.

## Hallucinations / overclaim

The subject says a monster zap used to pass `!mon_moving` into `revive`. The body now passes `true` (`:5588`), and `by_u` is only the `learn_it` ternary (`:5599`, `:5613`). That split is `zap.c:2340` versus `:2352`.

D-2807's caller list includes the `bhitpile` / `bhit` function-pointer sites. `csym --callers` does not print those, because the name is an argument rather than `bhito(`. The lines themselves are the calls, and the JS sites pass `bhito`.

## Density

The switch is the whole `bhito`, not one otyp. `debugpline1` and the hero arm of `maybe_unhide_at` are named. `muse.c` does not reference `bhito` in the caller list, so there is no missing `mbhit` wire to enqueue from this function.

## Verification

Re-run on this SHA: `node scripts/hidden-proxy.mjs verify bhito --base e1ef155a9~1 --reach-all`.

```
verify bhito: baseline e1ef155a9~1 (scoreboard at 7215d8d6a) — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
smoke bhito: no RNG-tagged reach; fixed smoke spread (12 run, 3.1s): 12 PASS, 0 regressed → REACH-OK
```

The queue row cited 0 blocks. D-2807's green 2/2, strict ×2, and cohort 7/7 were not re-run here.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
