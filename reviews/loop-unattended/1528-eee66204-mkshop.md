# Review 1528 — eee66204 — mkroom.c mkshop restart (D-2569)

## Metadata

- SHA: `eee66204`
- D-id: D-2569. Next index: 1528.
- Files: `js/mklev.js` (+70/−~30: restarted `mkshop`, no new imports).
- C locus: `nethack-c/upstream/src/mkroom.c:94–216` (`mkshop`, 123 L; `csym.mjs` range). Sole C caller `mkroom` (staticfn family, same file).

## Intent vs deliverable

Subject promises: whole-body restart in C order — wizard/ep arms, gottype walk with sentinel + impossible guard, doorct arm, light loop, `rnd(100)` pick with isbig clamp, rtype/topologize/needfill — with the SHOPTYPE endpoint as a named Rule #2 omit and no new RNG draws. Diff delivers exactly that. Promise matches deliverable.

## Inventory

- Restarted: `mkshop()` (`js/mklev.js:24737`, sync; `sym.mjs`: single file-local — correct, C is `staticfn`).
- No new imports, no new cross-module edges. Callees: `has_dnstairs`/`has_upstairs`/`invalid_shop_shape`/`isbig`/`topologize`/`rnd` all live; `impossible` live (`js/display.js:7992`, ASYNC — see note below); the `:104–153` SHOPTYPE dispatch callees (`mkzoo` all arms, `mktemple`, `mkswamp`) verified live file-locals in `js/mklev.js` (`sym.mjs`).
- No deleted symbols → no clone→import audit.

## C ↔ JS fidelity

Branch-by-branch vs C `:94–216`: `i = -1`, `ep = null` ✓; `wizard` gate — C `flag.h:30` is `#define wizard flags.debug`, JS ORs `flags.debug || flags.wizard`, matching 4+ existing sites (allmain.js:817, mklev.js:24419/25198, options.js:492): codebase convention, and unobservable here since `ep` is always null so the only `wizard` use (the `:173` multi-door arm, ported faithfully) can never fire ✓; `nh_getenv("SHOPTYPE")` endpoint named-omit (Rule #2, makemaz SPLEVTYPE precedent; map data.md updated in this commit) with the dispatch block carried as a cited comment — OMIT class, map-named with C ranges ✓; gottype walk — sentinel `hx < 0` return, past-nroom `impossible` + return, OROOM/stairs gates, doorct arm, `invalid_shop_shape → continue` else break ✓ (null-cand guard is a safe extension for sparse JS arrays); light loop ✓ (pre-existing body, untouched); `rnd(100)` walk + isbig wand/book→general clamp, now on the outer `i` (unshadowed, matching C) ✓; `SHOPBASE + i`, 1-arg `topologize` (SPECIALIZATION off), `needfill = FILL_NORMAL` ✓. RNG call-for-call: the single `rnd(100)` is untouched — REACH-safe by construction as claimed.

Note (not a C-wrong): `impossible(...)` is async and is called without await here. Same pattern already exists 3× in sync `splev_create_monster` (js/mklev.js:19125/19173/19178) — established in-file precedent for sync level-gen contexts, not introduced by this SHA. The message still emits (floating microtask); the branch is can't-happen.

## Hallucinations / overclaim

None. The D-log names the omit, the dead-by-omit dispatch, and the precedent for the sync call.

## Cited evidence

C gottype walk (`mkroom.c:157–178`, via `csym.mjs` — the heart of the restart):

```c
 gottype:
    for (sroom = &svr.rooms[0];; sroom++) {
        if (sroom->hx < 0)
            return;
        if (sroom - svr.rooms >= svn.nroom) {
            impossible("rooms[] not closed by -1?");
            return;
        }
        if (sroom->rtype != OROOM)
            continue;
        if (has_dnstairs(sroom) || has_upstairs(sroom))
            continue;
        if (sroom->doorct == 1 || (wizard && ep && sroom->doorct != 0)) {
            if (invalid_shop_shape(sroom))
                continue;
            else
                break;
        }
    }
```

JS mirrors it arm-for-arm (`js/mklev.js:24755+`): unbounded `for (;; idx++)`, `!cand || hx < 0 → return` (null guard is a sparse-array extension), `idx >= nroom → impossible + return`, OROOM/stairs gates, the `doorct == 1 || (wizard && ep && doorct != 0)` arm verbatim (dead via the null-`ep` omit, kept for fidelity), `invalid_shop_shape → continue` else break.

Flag + callee checks (re-run here):

```text
#define wizard flags.debug                       # nethack-c/upstream/include/flag.h:30 — exact, not an OR
mkshop      file-local js/mklev.js:24737        # sym.mjs: single local — correct, C is staticfn
mkzoo/mktemple/mkswamp  file-locals in mklev.js # dispatch callees, all live in-file
impossible  js/display.js:7992   ASYNC          # fire-and-forget here follows splev_create_monster (3× un-awaited at :19125/19173/19178)
```

The `flags.debug || flags.wizard` OR matches 4+ existing sites (allmain.js:817, mklev.js:24419/25198, options.js:492) — codebase convention, and unobservable inside `mkshop` (the only `wizard` use is the dead `wizard && ep` arm).

Verify output (re-run here):

```text
verify mkshop: baseline eee66204~1 — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
reach mkshop: 53 baseline-PASS session(s) reach it (53 run, 22.9s): 53 PASS, 0 regressed → REACH-OK
```

## Density

One 123-line C function, one file, ~70 insertions. Right-sized per §2b. Scoreboard hunk is a commit/at re-stamp only.

## Verification

- D-log: `verify.mjs --fn mkshop` → PASS.
- Re-run here: `hidden-proxy.mjs verify mkshop --base eee66204~1 --reach-all` → **53 baseline-PASS sessions reach it, 53 PASS, 0 regressed → REACH-OK** (real reach, not smoke). Matches the D-log's "53-session REACH" claim.
- `imports.mjs --rulecheck`: clean (re-run this iteration). Diff grep: 0 hits for FORCE/DIAG/getRngLog/fastforward.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
