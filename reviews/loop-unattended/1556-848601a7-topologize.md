# Review 1556 — 848601a7 — `mklev.c` topologize whole-body restart (D-2597)

- Commit: `848601a7` (2026-09-20) — "`mklev.c` topologize whole-body restart (subroom recursion, C-order cites) (D-2597)."
- Queue row: coverage gap (thin local painted innards + edges, never recursed — nested des/quest rooms kept the parent's roomno).
- JS touched: `js/mklev.js` only (23 insertions, 3 deletions).

## Intent vs deliverable

Subject promises the whole `topologize` body with subroom recursion. Diff actually delivers: restarted `topologize` (roomno/bounds, nsubrooms snapshot, already-done/irregular skip in C order, innards, top/bottom edges, sides, new subroom recursion), SPECIALIZATION arms named. The one real fix (recursion `:1648–1654`) is present. Promise matches diff.

## Inventory

Restarted: `topologize` (module-local — correct: C is extern but JS consolidates mkroom/sp_lev into `mklev.js`, so all 43 references are in-module). No imports, no deletions.

## C ↔ JS fidelity

C locus: `nethack-c/upstream/src/mklev.c:1595–1656` (csym; body `:1601–1655`).

Arm-by-arm confirm (SPECIALIZATION-off build):

- roomno: C `(croom - svr.rooms) + ROOMOFFSET` → `(croom.roomnoidx ?? -1) + ROOMOFFSET`. `roomnoidx` is written by `add_subroom` (`js/mklev.js:20062/20066`) and reindexed on sort (`:27919–27920`); same convention as the other roomno sites (`:16820`, `:25019`, `:25761`). Equivalent under the live indexing invariant (formula line itself unchanged from the thin version).
- already-done/irregular skip (`:1612–1614`): old code tested `!croom || irregular` first; new code tests `at(lowx,lowy).roomno === roomno` first, then `irregular` — C order. Same outcomes (both return), order now cited.
- innards (`:1619–1627`): unconditional `roomno` — the SPECIALIZATION-off arm. The `rtype==OROOM → NO_ROOM` arm is compiled out: `global.h:120` shows `/* #define SPECIALIZATION */` commented — verified, not taken on faith. Named OMIT, legitimate (same ground as the `:24751` note).
- top/bottom edges (`:1629–1636`) and sides (`:1638–1645`): `edge = true` (file boolean convention for C bitfield `edge,1`) + `roomno ? SHARED : roomno` — exact, including the degenerate 1-wide/1-tall strides (`x += hix-lowx+2`, `y += hiy-lowy+2` verbatim).
- recursion (`:1648–1654`): snapshot `nsubrooms` up front (`:1609`) + `sbrooms[subindex]` 1-arg recursion — the missing arm, now present; `if (sub)` null-tolerance is benign JS-only (C entries are non-null).
- JS-only `!croom` / `!game.level` guards: C takes nonnull (`extern.h` NONNULLARG1) — disclosed, benign.

Callers: C sites (`mklev.c:1564/1566`, `mkroom.c:207/209`, `sp_lev.c:2824/2826/5687/5689`) all resolve into `js/mklev.js` (no mkroom.js/sp_lev.js exist; 43 in-module references, pre-existing call sites intact). No RNG in body. No callees beyond `at()` — nothing to classify.

## Hallucinations / overclaim

None. The SPECIALIZATION claim is verified at the cited header line, not asserted.

## Density

23 insertions for a 62-line C function — small because the thin local already had 4 of 6 arms; the iteration's value is the recursion arm + C-order + cites. Acceptable (C is that small).

## Verification

- Banned-pattern grep: 0. No RNG/seed/coordinate logic.
- Re-ran here: `hidden-proxy.mjs verify topologize --base 848601a7~1 --reach-all` → 0 blocked at parent (vacuous note, pre-stated) + smoke 24/24 REACH-OK. Matches D-log.

## Cited evidence

C edges + recursion (`mklev.c:1629–1655`, verified against the restart):

```c
/* top and bottom edges */
for (x = lowx - 1; x <= hix + 1; x++)
    for (y = lowy - 1; y <= hiy + 1; y += (hiy - lowy + 2)) {
        levl[x][y].edge = 1;
        if (levl[x][y].roomno)
            levl[x][y].roomno = SHARED;
        else
            levl[x][y].roomno = roomno;
    }
/* sides */
for (x = lowx - 1; x <= hix + 1; x += (hix - lowx + 2))
    for (y = lowy; y <= hiy; y++) {
        levl[x][y].edge = 1;
        if (levl[x][y].roomno)
            levl[x][y].roomno = SHARED;
        else
            levl[x][y].roomno = roomno;
    }
}
/* gs.subrooms */
for (subindex = 0; subindex < nsubrooms; subindex++)
#ifdef SPECIALIZATION
    topologize(croom->sbrooms[subindex], (boolean) (rtype != OROOM));
#else
    topologize(croom->sbrooms[subindex]);
#endif
```

SPECIALIZATION proof (`global.h:120`): `/* #define SPECIALIZATION */` — commented out, so the 1-arg form is live and the `do_ordinary`/`rtype != OROOM`/`OROOM → NO_ROOM` arms are compiled out (named omit, same ground as the `js/mklev.js:24751` note).

roomnoidx mechanism (≡ `croom - svr.rooms`): written by `add_subroom` (`js/mklev.js:20062/20066`, `roomnoidx: idx` + `g.level.rooms[idx] = croom`), reindexed on sort (`:27919–27920`), and consumed the same way at `:16820`, `:25019`, `:25761`. C callers (`mklev.c:1564/1566`, `mkroom.c:207/209`, `sp_lev.c:2824/2826/5687/5689`) all resolve into `js/mklev.js` — no `mkroom.js`/`sp_lev.js` exist in `js/` (43 in-module references), so module-local is correct and every caller was already wired before this diff.

Tool outputs pasted (required):

```
$ node scripts/sym.mjs topologize
topologize       NOT EXPORTED — but 1 LOCAL CLONE(S) in 1 file(s):
               js/mklev.js:27936
$ node scripts/hidden-proxy.mjs verify topologize --base 848601a7~1 --reach-all
verify topologize: baseline 848601a7~1 — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
smoke topologize: no RNG-tagged reach; fixed smoke spread (24 run, 3.4s): 24 PASS, 0 regressed → REACH-OK
```

(`sym.mjs` "NOT EXPORTED / LOCAL CLONE" is its wording for any module-local function; C `topologize` is extern but JS consolidates the three C files into `mklev.js`, so local is the right shape — same precedent as `mksink`/`mkgrave`.) Banned-pattern grep: 0. No RNG/seed/coordinate logic.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
