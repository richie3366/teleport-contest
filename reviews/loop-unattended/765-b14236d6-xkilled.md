# Review 765 — b14236d6 — mon.c xkilled LEVEL_SPECIFIC + pool gate (D-1796)

## Metadata
- Full / short hash: `b14236d61df4d8abfe4e6c3265cb3a8d489a4f1d` / `b14236d6`
- Parent: `efcb3fd4` (D-1795 QUALITY-RISK `nomul`/`usleep`). Map-driven Open. This SHA does not close that Must-fix.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 01:17:10 +0200
- D-id: **D-1796**
- Stats: `js/uhitm.js` +66/−34; `js/mon.js` +13; artifact/game/mhitm/mklev/monmove/trap. Total `js/` insertions **105** ≤250. Band **80–350**.
- Claims to close: Open `mon.c` `xkilled` LEVEL_SPECIFIC_NOCORPSE + `accessible||is_pool` + artifact un-create. Not `make_corpse`.
- JS / map: `uhitm.js` `xkilled`; `mon.js` `LEVEL_SPECIFIC_NOCORPSE`; `monmove.js` `accessible` export; `artifact.js` `artifact_exists` `!mod`. `c-js-map/data.md`.
- Archive **Addressed:** D-1796 `b14236d6`.

## Intent vs deliverable

Git subject promises: Match C `mon.c` `xkilled` so `LEVEL_SPECIFIC_NOCORPSE`, the `accessible||is_pool` gate, and artifact un-create actually run, instead of always dropping treasure on every tile.

`node scripts/csym.mjs xkilled` → `mon.c:3476–3740`. Macro `LEVEL_SPECIFIC_NOCORPSE` `mon.c:44–47`. `accessible` `monmove.c:2187–2194`. `artifact_exists` `artifact.c:369–405`. `--callers xkilled`: 37 code sites. `corpse_chance` `:3242` duplicates the macro. `artifact_exists` un-create caller `:3608–3610`.

Parent: `!nocorpse` then always `!rn2(6)` + `corpse_chance` on every tile; `!mod` only zeroed `oartifact`; `accessible` used `loc.typ` not `SURFACE_AT`; tut-1 `nodeathdrops` flag that C never reads. The diff **does** export the macro, skip treasure/corpse on that goto, gate remaining drops on `accessible||is_pool`, un-create `artiexist[m]`, wire `SURFACE_AT`, set `deathdrops` default true / tut-1 false. Subject is delivered.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `xkilled` | LIVE repaired | goto-cleanup as `else if` |
| `LEVEL_SPECIFIC_NOCORPSE` | LIVE new | short-circuit ≡ C `\|\|` |
| `accessible` | LIVE export | `SURFACE_AT`; teleport.js clone stays |
| `artifact_exists` `!mod` | LIVE | `artiexist[i]` zeroed |
| `corpse_chance` (3 locals) | CLONE | mhitm/trap/uhitm now call the macro; do **not** write #4 |
| `xkilled_treasure_drop` | LIVE helper | C’s `!rn2(6)` body |
| `flooreffects` | OMIT named | JS `else` places; C `!flooreffects` then place |
| MAIL / wasinside / `sobj_at` boulder / quest adjalign | OMIT named | |

`node scripts/sym.mjs`:

```
xkilled          js/uhitm.js:566   ASYNC
LEVEL_SPECIFIC_NOCORPSE js/mon.js:580   sync
accessible       js/monmove.js:644   sync  + teleport.js:142 — do NOT add #3
artifact_exists  js/artifact.js:900   sync
SURFACE_AT       js/hack.js:1355   sync
mkobj / delobj   js/mkobj.js  sync
safe_oname       js/do_name.js:1068   sync
corpse_chance    NOT EXPORTED — 3 LOCALS (C is one fn; do not add #4)
make_corpse      js/mhitm.js:1877   ASYNC
change_luck      js/attrib.js:569   sync
m_carrying       js/mon.js:373   sync
```

`--can uhitm.js mon.js LEVEL_SPECIFIC_NOCORPSE`: **ALREADY**. `--can uhitm.js monmove.js accessible`: **ALREADY**. `--can uhitm.js artifact.js artifact_exists`: **ALREADY**. `--can mhitm.js/trap.js mon.js LEVEL_SPECIFIC_NOCORPSE`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**Macro (`:44–47`).** Rogue → true (no `rn2`). Else `!deathdrops` → true. Else `graveyard && is_undead && rn2(3)`. GameMap default `deathdrops: true` (C `clear_level_structures`); tut-1 `deathdrops=false` (C `nodeathdrops`). **Match short-circuit.** `corpse_chance` clones call it (`mon.c:3241–3242`); xkilled also (`:3574`) — C draws twice on graveyard undead that fail the first roll; JS does too.

**Goto cleanup (`:3574–3575`).** `nocorpse \|\| LEVEL_SPECIFIC` skips treasure, MAIL, pool gate, corpse, wasinside, `newsym`. JS `else if (!(nocorpse \|\| …))` then luck/xp always. Stoned still skips the drop block then cleanup. Lifesave returns before cleanup (C `:3552–3560`; “Maybe not…” named with vamp_rise). **Match.**

**`accessible \|\| is_pool` (`:3582`).** `ACCESSIBLE(SURFACE_AT) && !closed_door` (`:2187–2194`, `rm.h:146–149`). Then `!rn2(6)` with G_NOCORPSE / same-tile / KOP / cloned filters; `mkobj(RANDOM_CLASS)`; food `delobj`; small+bulky artifact un-create then `delobj`; else place (flooreffects named). Corpse: `!wasinside && corpse_chance` then zombify/`make_corpse`/reset. Bury via `m_carrying(BOULDER)` (floor `sobj_at` nocorpse named). **Match RNG order on the ported path.**

**Un-create (`artifact.c:398–400`).** `oartifact=0`, RID spe 0, `artiexist[m]=zero`. **Match.**

**Cleanup luck.** Human murder luck-2 + “You murderer!” (see_monsters named). Peaceful `!rn2(2)` \|\| mtame luck-1 (short-circuit as C). Unicorn same-align luck-5. Then experience. Quest/priest adjalign named; tame -15 kept. **Match the arms that shipped.**

**Callee closure.** LIVE: macro, `accessible`, `is_pool`, `mkobj`, `delobj`, `artifact_exists`, `safe_oname`, `corpse_chance` clones, `make_corpse`. OMIT named: flooreffects / MAIL / wasinside / `sobj_at` / quest. STUB in a live arm: **none** (flooreffects skip is the named omit, not a silent stub inside an unmarked arm).

## Hallucinations / overclaim

Subject is **true**. Do **not** stamp “Match C `flooreffects` / MAIL / wasinside `spoteffects`.” Do **not** export `accessible` a third time (teleport clone). Do **not** write `corpse_chance` #4. D-log “green + combat cohort hold” is this peel’s focused gate; **public `seed0030` already FAIL at parent D-1795** (same 39912/105529 at HEAD) — not this SHA’s first desync. Do not invent a seed0030 xkilled peel.

## Density

§2b: one C function + the macro it shares with `corpse_chance` + un-create + `accessible` export. +105. Did **not** glue `dochug`. Right size.

## Verification

D-log canary 19/19; green + cohort 7/7. save-oracle skip. This audit: `csym` `:3476–3740` vs HEAD `js/uhitm.js:529–679`. seed0030 still FAIL at the D-1795 offset (bisect). Rule #2 clean.

## Actionable C-wrongs

None for Must-fix on this SHA. Named: flooreffects; floor-boulder `sobj_at`; MAIL; wasinside `spoteffects`; Blind_telepat `see_monsters`; quest adjalign; be_sad / vamp_rise / thrownobj-into-engulfer. Do **not** add `accessible` clone #3. Do **not** draw `rn2(6)` when `LEVEL_SPECIFIC` already jumped to cleanup. **Next port pops Must-fix 764 (`nomul`/`usleep`), not Open `dochug`.**

Verdict: **ACCEPT-WITH-DEBT**
