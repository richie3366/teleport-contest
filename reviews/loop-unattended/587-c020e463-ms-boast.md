# Review 587 — c020e463 — sounds.c domonnoise MS_BOAST hostile giants (D-1626)

## Metadata
- Full / short hash: `c020e463b1e998f47118878b5bb2ca6e92247b03` / `c020e463`
- Parent: `1d6a8b20` (D-1625). This file audits **this SHA only** (sixth of nine `js/` commits since review **581**). Archive **Addressed:** D-1626 `c020e463`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-29 05:43:29 +0200
- D-id: **D-1626**
- Stats: `js/sounds.js` +42/−11, `js/fountain.js` +2/−1. Band **150–350** (js/ insertions **45**; C arm is 18 lines).
- Claims to close: Open MS_BOAST after D-1618. Not MS_HUMANOID body. Not remaps. `reviews/loop-2026-08-15/` has no unpaid MS_BOAST Must-fix.
- JS / map: `sounds.js` `domonnoise`; `fountain.js` `mhis` export. `c-js-map/data.md`.
- Prior reviews this SHA claims to close: **579** named MS_BOAST `:1006–1023` + peaceful FALLTHROUGH.

## Intent vs deliverable

Git subject promises: hostile giants `rn2(4)` gem/`mhis`, mutton, or Fee-Fie plus `wake_nearto(7*7)` (peaceful FALLTHROUGH into live MS_HUMANOID), instead of silent `ECMD_OK` after D-1618.

Pinned C `sounds.c` `domonnoise` MS_BOAST `:1006–1023` (`node scripts/csym.mjs` via the switch; cite `:1006–1023`). Epilogue `:1222–1241` always `ECMD_TIME`. Peaceful FALLTHROUGH into MS_HUMANOID `:1025–1104` (D-1618). `you.h:324` `#define mhis(mtmp) (genders[pronoun_gender(mtmp, PRONOUN_HALLU)].his)`. `--callers mhis` includes `sounds.c:1011`. `wake_nearto` `mon.c` `:4401–4405` (`--callers` includes `:1018`). `monflag.h:58` `MS_BOAST = 43`. `--callers domonnoise`: `dochat` / dogmove / steed.

```1006:1025:nethack-c/upstream/src/sounds.c
    case MS_BOAST: /* giants */
        if (!mtmp->mpeaceful) {
            switch (rn2(4)) {
            case 0:
                pline("%s boasts about %s gem collection.", Monnam(mtmp),
                      mhis(mtmp));
                break;
            case 1:
                pline_msg = "complains about a diet of mutton.";
                break;
            default:
                pline_msg = "shouts \"Fee Fie Foe Foo!\" and guffaws.";
                wake_nearto(mtmp->mx, mtmp->my, 7 * 7);
                break;
            }
            break;
        }
        FALLTHROUGH;
    case MS_HUMANOID:
```

Old JS (D-1618): HUMANOID live; MS_BOAST unnamed → silent `ECMD_OK`.

The diff **does** `MS_BOAST=43`, hostile `rn2(4)` (0 immediate gem+`mhis` then `ECMD_TIME`; 1 mutton `pline_msg`; default Fee-Fie + `wake_nearto(7*7)`), peaceful `msound===MS_HUMANOID \|\| MS_BOAST` into live HUMANOID, export fountain `mhis` (no 5th clone). It **does not** port guardian/isshk/gecko remaps, SetVoice / Soundeffect, `map_invisible` before talk, or other MS_*. Named.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `domonnoise` MS_BOAST | C `:1006–1023`, **LIVE this SHA** | |
| `rn2(4)` | C `:1008`, **LIVE** | rng.js |
| `mhis` | C you.h `:324`, **LIVE this SHA** | fountain export; mhitu/sit/uhitm still clones |
| `pronoun_gender` | C mondata, **CLONE** | fountain; PRONOUN_HALLU `rn2(4)` |
| `Monnam` | C, **LIVE** | do_name.js |
| `wake_nearto` | C mon `:4401–4405`, **CLONE** | sounds.js local (pre-existing); mon.js already exports |
| `pline` / epilogue | C `:1222–1241`, **LIVE** | case 0 skips empty epilogue, still `ECMD_TIME` |
| MS_HUMANOID FALLTHROUGH | C `:1023–1025`, **LIVE this SHA** | |
| remaps / SetVoice / other MS_* | **OMIT named** | |

`node scripts/csym.mjs mhis` → `you.h:324`. `wake_nearto` → `mon.c:4401-4405`. `--callers mhis` `sounds.c:1011`. `--callers wake_nearto` includes `:1018`.

RNG: one `rn2(4)` on the hostile arm, same as C. `mhis` may `rn2(4)` under Hallucination (C `PRONOUN_HALLU`). No seed gate.

`node scripts/sym.mjs` on new / re-pointed names:

```
mhis             js/fountain.js:532   sync
             !! ALSO 3 LOCAL CLONE(S) in 3 files — IMPORT the export; do NOT add another
               js/mhitu.js:214  js/sit.js:204  js/uhitm.js:1041
wake_nearto      js/mon.js:1104   ASYNC — await required
             !! ALSO 4 LOCAL CLONE(S) in 4 files — IMPORT the export; do NOT add another
               js/dbridge.js:76  js/explode.js:276  js/sounds.js:120  js/trap.js:1044
domonnoise       js/sounds.js:609   ASYNC — await required
Monnam           js/do_name.js:694   sync
rn2              js/rng.js:61   sync
```

`--can sounds.js fountain.js mhis`: `ALREADY: sounds.js already statically imports fountain.js.` `--can sounds.js mon.js wake_nearto`: IN-SCC, `VERDICT: SAFE` (hoisted). This SHA **reused** the existing sounds.js clone (bark/growl already used it); it did not add clone #5. Do **not** stamp “cycle-forced clone.” Do not add `mhis` in `sounds.js`. Do not add `wake_nearto` #5.

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` / seed names / coordinates in this SHA’s `js/` hunks. `node scripts/imports.mjs --rulecheck`: Rule #2 clean.

## C ↔ JS fidelity

`MS_BOAST=43` matches `monflag.h:58`. Hostile gate `!mpeaceful` then `rn2(4)`. **Match `:1007–1008`.**

case 0. C `pline("%s boasts about %s gem collection.", Monnam, mhis)` then `break` out of MS_BOAST (empty `pline_msg`) then epilogue still `ECMD_TIME`. JS `await pline(...)` then `return ECMD_TIME` (skips empty epilogue). **Match the message and the return.** Not a stub of the epilogue: C’s empty path has no SetVoice/verbalize.

case 1. `pline_msg = "complains about a diet of mutton."` then epilogue `Monnam + msg`. **Match `:1014–1015` + `:1222–1223`.**

default (2 and 3). Fee-Fie string + `wake_nearto(mx,my,7*7)`. JS same then epilogue. **Match `:1017–1018`.** Local `wake_nearto` walks `fmon`, `dist2 < distance`, `wake_msg` + clear sleep/WAITMASK. Pre-existing clone vs `mon.c:4401`. Distance `7*7` is the C argument.

Peaceful. C FALLTHROUGH into HUMANOID. JS `msound === MS_HUMANOID \|\| msound === MS_BOAST` after the hostile-boast arm. Hostile boast does not say `"threatens you."`. **Match `:1021–1031`.** Peaceful boast uses D-1618 flee/moan/Huh/race. **Match.**

`mhis`. C macro `genders[pronoun_gender(..., PRONOUN_HALLU)].his`. JS `['his','her','its','their'][pronoun_gender]` with hallu `rn2(4)`. **Match the hallu index.** Export, not a sounds.js clone.

Callee closure (hostile MS_BOAST arm). LIVE: `rn2`, `Monnam`, `mhis`, `pline`. CLONE: `wake_nearto` (existing sounds.js). OMIT named: remaps, SetVoice. STUB: none. The arm may ship. Not “dispatch ported, callee stubbed.”

JS arm (this SHA) vs C `:1006–1023`:

```673:689:js/sounds.js
    if (msound === MS_BOAST && !mtmp.mpeaceful) {
        switch (rn2(4)) {
        case 0:
            await pline(
                `${Monnam(mtmp)} boasts about ${mhis(mtmp)} gem collection.`);
            return ECMD_TIME;
        case 1:
            pline_msg = 'complains about a diet of mutton.';
            break;
        default:
            pline_msg = 'shouts "Fee Fie Foe Foo!" and guffaws.';
            await wake_nearto(mtmp.mx, mtmp.my, 7 * 7);
            break;
        }
    } else if (msound === MS_HUMANOID || msound === MS_BOAST) {
```

C `wake_nearto` `mon.c:4401–4405` is a thin wrapper over `wake_nearto_core`. JS `sounds.js:120–131` walks `fmon` with `dist2 < distance`, `wake_msg`, clear `msleeping` / `STRAT_WAITMASK`. Pre-existing clone (bark/growl already called it). This SHA only adds the `7*7` argument from `:1018`.

C epilogue `:1222–1241` always `return ECMD_TIME`. JS `sounds.js:767–775` returns `ECMD_TIME` when `pline_msg` or `verbl_msg` is set, else `ECMD_OK`. Hostile boast case 1/default set `pline_msg` so they hit `ECMD_TIME`. Case 0 early-returns `ECMD_TIME`. Unknown `msound` still `ECMD_OK` (named, pre-D-1626).

`--callers domonnoise` (this SHA did not change the callers): `dochat` `sounds.c`, `dog_mon_can_reach` / steed chat, `m_respond`. Public `#chat` is the live player path.

## Hallucinations / overclaim

Subject hostile `rn2(4)` gem/`mhis` / mutton / Fee-Fie+`wake_nearto(7*7)` and peaceful FALLTHROUGH: **true.** D-log case 0 still `ECMD_TIME`: **true.** Do **not** stamp “Match C `mon.js` `wake_nearto` import” (local clone). Do **not** stamp “Match C guardian/isshk/gecko remaps.” Do **not** stamp “Match C SetVoice / `map_invisible`.” Do **not** stamp “Match C MS_HUMANOID body” (D-1618). Do **not** stamp “Match C epilogue always `ECMD_TIME` for unknown MS_*” (JS unknown still `ECMD_OK` — pre-existing). Public `#chat` giant is unhit.

## Density

+45 for an 18-line C arm plus exporting `mhis` the gem line needs. §2b C-that-small exception. Did not glue remaps or other MS_*. Not a one-`if` peel.

## Branch-by-branch confirm

1. Hostile `rn2(4)` 0 / 1 / default. **Match this SHA.**
2. Peaceful FALLTHROUGH HUMANOID. **Match.**
3. `mhis` hallu. **Match.**
4. `wake_nearto(7*7)`. **Match argument; clone body pre-existing.**
5. Remaps / other MS_*. **Named.**

## Callers / RNG ledger

Wired: `dochat` → `domonnoise` (and existing dogmove/steed). Conf: `rn2(4)` once per hostile boast. Hallu `mhis` may `rn2(4)`. No seed gate.

## Anti-pattern / Rule #2 (this SHA `js/`)

Plain ESM. No FORCE. Do not add `mhis` in `sounds.js`. Do not add `wake_nearto` #5. Do not import `fs`. `--can` says importing `mon.js` `wake_nearto` is SAFE — do not invent a cycle excuse for a new clone.

## Verification

D-log green+strict seed8000/0900; cohort **7**/7 + seed2200/0383 + strict. **Public-unhit** for `#chat` giant / `rn2(4)` arms / `wake_nearto(7*7)`. Fortress has no hill-giant `#chat`. Peaceful boast HUMANOID unhit unless a session chats a peaceful giant.

## Actionable C-wrongs

None for Must-fix. Named (map, not Must-fix): guardian/isshk/gecko remaps; SetVoice / Soundeffect; `map_invisible`; other MS_* ; unknown-msound `ECMD_OK` vs C `ECMD_TIME`. Do not glue those into MS_HUMANOID. Do not add `mhis` #4. Do not re-port HUMANOID.

Verdict: **ACCEPT-WITH-DEBT**
