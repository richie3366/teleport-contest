# Review 238 — 2860794e — objnam.c doname EGG (D-1276)

## Metadata
- Full / short hash: `2860794ee846304de98d4f9132ee1e849d856bf5` / `2860794e`
- Parent: `18bec04d` (D-1275). This file audits **this SHA only**. Archive row **Addressed:** D-1276 `2860794e` already has the short hash.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-20 13:07:45 +0200
- D-id: **D-1276**
- Stats: 11 files, +228 / −152 — `js/objnam.js` +20 / −4; comment `js/do.js`; journal rotation.
- Claims to close: Open `objnam.c` doname EGG (named from D-1255 / review **217**). Not MEAT_RING. `reviews/loop-2026-08-15/` has no unpaid doname-egg Must-fix.
- JS / map: `objnam.js` `doname`; `c-js-map/turns.md`. MEAT_RING / candle `partly used` named.
- Prior reviews this SHA claims to close: **217** named omit doname EGG species / `(laid by you)`.

## Intent vs deliverable

Git subject promises: “Match C objnam.c doname so a known or learned typed egg shows the monster name and (laid by you), instead of always formatting as an egg.”

C `doname_base` FOOD EGG (`objnam.c:1524–1535`): `stale_egg` is `#if 0`; then `ismnum(omndx)` and `(known || MV_KNOWS_EGG)` `Strcat` `mons[omndx].pmnames[NEUTRAL]` + space; `spe==1` `Concat(bp, " (laid by you)")`. `omndx = obj->corpsenm` (`:1238`). `known = obj->known` (`:1257`) unless `override_ID`. `bp = xname(obj)` first (`:1247`), so “ named ” is already in `bp` before Concat. `xname` FOOD stays bare `"egg"`. MEAT_RING `goto ring` (`:1536–1538`) named.

Old JS: FOOD `oeaten` / CORPSE `corpse_xname` (D-1255) only; EGG stayed `"an egg"`.

The diff **does** the `ismnum` + known/learned prefix and the laid suffix after `named`. It does **not** wire MEAT_RING, candle `partly used`, `learn_egg_type` in invent, or `food_xname`. Named.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `doname` FOOD EGG prefix | C `:1529–1532`, **wired** | `objectNames[otyp]==='EGG'` ≡ `obj->otyp==EGG` |
| `ismnum` | C `monst.h:285`, **imported live** | JS `pm >= LOW_PM` (no `NUMMONS` upper); empty `pmnames` → `''` |
| `MV_KNOWS_EGG` | C `monflag.h:212` `0x08`, **imported** | `timeout.js` `learn_egg_type` already sets it |
| `pmnames[omndx][NEUTRAL]` | C `mons[].pmnames[2]`, **imported live** | `monsters.js` `NEUTRAL=2`; generated `[null,null,"newt"]` |
| `(laid by you)` | C `:1533–1534` Concat on `bp`, **wired** | after ` named `; only if type shown |
| `stale_egg` | C `#if 0`, **correct skip** | |
| MEAT_RING `goto ring` | C `:1536–1538`, **named omit** | |
| candle `partly used` | C, **named omit** | |
| `food_xname` | C `eat.c:216`, **named omit** | eat messages still bare egg |

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` writes / seed names / recorded coordinates. Rule #2 clean. **No RNG.** This is **not** “Match C dispatch, callee is a stub”: the string is built in `doname`.

## C ↔ JS fidelity

Pinned C (`objnam.c:1504–1535`), after `bp = xname(obj)` and `known = obj->known`:

```
    case FOOD_CLASS:
        if (obj->oeaten)
            Strcat(prefix, "partly eaten ");
        if (obj->otyp == CORPSE) { ... }
        else if (obj->otyp == EGG) {
#if 0
            if (known && stale_egg(obj))
                Strcat(prefix, "stale ");
#endif
            if (ismnum(omndx)
                && (known || (svm.mvitals[omndx].mvflags & MV_KNOWS_EGG))) {
                Strcat(prefix, mons[omndx].pmnames[NEUTRAL]);
                Strcat(prefix, " ");
                if (obj->spe == 1)
                    Concat(bp, 0, " (laid by you)");
            }
        } else if (obj->otyp == MEAT_RING) {
            goto ring;
        }
```

JS: `known = !!obj.known`; CORPSE arm unchanged; `else if` EGG uses `obj.corpsenm`, `mvitals[omndx].mvflags & MV_KNOWS_EGG` (`0x08`), `pmnames[omndx][NEUTRAL]`, `spe===1` flag. Prefix then `just_an` redo (`"a newt"` / `"an ochre"`). `bp = prefix + base` with `base` still pretty `"egg"` / plural `"eggs"`. Then `dknown` ` named `; then laid. C Concat is after xname (which already appended named). Match order: species in the article prefix, laid after named, containing still later.

Hidden `spe==1` without known/learned: C does not Concat; JS leaves `eggLaidByYou` false. Generic `NON_PM` / `!ismnum`: no prefix. Stack `quan!=1`: `"3 newt eggs"` (count prefix, no `a ` redo). Partly eaten sits before the monster name. Blessed BUC still earlier in prefix. Match.

`stale_egg` stays out (`#if 0`). Do not treat that as a miss.

## Hallucinations / overclaim

Subject + D-1276 say a known or learned typed egg shows the monster name and `(laid by you)` instead of always `"an egg"`. **The FOOD EGG arm is the hunk.** Stamping **Addressed:** D-1276 is fair. Do **not** stamp “Match C MEAT_RING `goto ring`.” Do **not** stamp “Match C `food_xname` / invent `learn_egg_type` on identify.” Do **not** stamp “Match C `stale_egg`” — C compiles it out. `MV_KNOWS_EGG===0x08` is C’s flag, not a trace index.

## Density

One `doname_base` FOOD arm (~12 lines of C; 20 JS). Right size for a named sibling of D-1255. Did not glue hurtle `switch_terrain`.

## Branch-by-branch confirm

1. Known newt egg, quan 1: `"a newt egg"`. Match.
2. Known ochre jelly: `"an ochre jelly egg"` (`just_an`). Match.
3. `spe==1` known: `"… egg (laid by you)"` after named. Match.
4. Named + laid: `"a newt egg named Foo (laid by you)"`. Match xname-then-Concat.
5. `MV_KNOWS_EGG` without `obj.known`: still prepends. Match.
6. Generic / `NON_PM`: `"an egg"`, no laid. Match.
7. Hidden laid (`spe==1`, unknown, unlearned): `"an egg"`. Match.
8. Partly eaten known: `"a partly eaten newt egg"`. Match.
9. Stack: count + species + `"eggs"`. Match.
10. MEAT_RING / candle / `food_xname`: still named. Match the skip.

## Anti-pattern / Rule #2 (this SHA `js/`)

No FORCE / fs / seed-named gates. `" (laid by you)"` is C’s Concat string, not a session quote used as a branch. Plain ESM.

## Verification

Journal: private canary **25**/25 (C EGG/`#if 0`/MEAT_RING still-goto; JS known newt/cockatrice/ochre-jelly; MV_KNOWS_EGG without known; generic NON_PM; hidden laid-by-you; stack; partly eaten; blessed; named-then-laid; xname bare; Rule #2); green+strict seed8000/0900; cohort **7**/7 + strict 1500/1800/0012/0004/0007/2200/0383. **Public-unhit** unless a public session shows a typed known/learned egg. Cadence this audit: full `sessions` at HEAD `851d3e08` **44**/44 Scr **11,405**/11,405 RNG **100%**.

## Actionable C-wrongs

None for Must-fix. The arm is the C body; `pmnames[NEUTRAL]` is live generated data; `learn_egg_type` already exists in `timeout.js`. Laid-only-when-typed matches C’s inner `if`. JS `ismnum` missing `NUMMONS` is pre-existing and idle when `corpsenm` is a real egg type (empty name → no fake species word).

Named omits (map, not Must-fix):

1. doname MEAT_RING `goto ring` (`objnam.c:1536–1538`)
2. candle `partly used`; `iflags.partly_eaten_hack`
3. `food_xname` eat path; invent `fully_identify_obj` `learn_egg_type` call site if still unwired

Do not Must-fix “JS `ismnum` lacks `pm < NUMMONS`” on this SHA. Do not Must-fix “`stale_egg` omitted.” Do not pull hurtle this SHA.

## Callers / RNG ledger

C: `doname` / `doname_base` only for this arm. JS `doname`. No RNG. Public fortress is not evidence a newt egg printed `"a newt egg"`.

## Verdict

- Verdict: **ACCEPT-WITH-DEBT**
- One sentence: known/learned typed eggs now take `pmnames[NEUTRAL]` and optional `(laid by you)`; MEAT_RING / candle stay named.
- Must-fix stays empty for this SHA; archive already has **Addressed:** D-1276 `2860794e`.
