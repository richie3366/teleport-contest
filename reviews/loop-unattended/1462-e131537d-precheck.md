# Review 1462 — e131537d — `muse.c` precheck whole-body port (D-2503)

Metadata: SHA `e131537d`, `js/muse.js` +141/−40 (whole-file stat 129+/65− is docs-heavy). C `muse.c:58–160` (`precheck`, 103 lines, staticfn). D-log: D-2503.

## Intent vs deliverable

Promise: restart in C order — milky ghost arm, smoky djinni arm, cursed-wand backfire — fixing an inverted `rn2(2)` gate, a hand-rolled paralyze, and awand arm that rolled damage and discarded it. Diff delivers all three arms with `:line` cites. Promise = deliverable.

## Inventory

- Restarted: `precheck` (js/muse.js:2305, module-local like C staticfn — single local, no clone drift).
- Imports extended (all same-edge words except): `rndmonnam`, `a_monnam`, `set_malign`, `Something`, `AD_RBRE`, `SetVoice`, `se_zap_then_explosion`, `paralyze_monst`. All verified LIVE below.
- `sym.mjs` (required): nothing deleted or re-pointed. `precheck` NOT EXPORTED, 1 local (correct for staticfn). `m_useup` has a live exact export (mthrowu.js:174) alongside the muse.js:1266 local — binding unchanged by this SHA, see debt.

## C ↔ JS fidelity

Milky `:73–101` ≡ C call-for-call: `G_GONE` + `POTION_OCCUPANT_CHANCE` gate ✓; `enexto` early-0 ✓; `mquaffmsg` → `m_useup` → `makemon(MM_NOMSG)` ✓; `!mtmp` → `pline(empty)` = C `pline1` ✓; vis pair with `Hallucination() ? rndmonnam(null) : 'ghost'` ✓ and `Monnam(mon)` frightened line ✓; `paralyze_monst(mon, 3)` replaces the hand-rolled `mfrozen+3/mcanmove=0` (C also clears `meating`/`STRAT_WAITFORU`) ✓; `return 2` ✓.

Smoky `:102–131` ≡ C: skeleton ✓; `pline_mon('In a cloud of smoke…', a_monnam)` ✓; `pline('%s speaks.', vis ? Monnam : Something)` ✓; `SetVoice(mtmp, 0, 80, 0)` ✓; `if (rn2(2))` freed/peaceful/`set_malign` else about-time/vanish/`mongone` — inversion fixed ✓; `return 2` ✓.

Wand `:133–159` ≡ C: `dam = d(spe+2, 6)` rolled before messages ✓; vis `pline_mon` with `Monnam(mon)`, `an(xname(obj))` ✓; else `couldsee`-threshold range, `Soundeffect`, `You_hear` nearby/distance text (template form matches `You_hear(text)` semantics) ✓; `m_useup`, `mhp -= dam`, `DEADMONSTER → monkilled(mon, '', AD_RBRE)` + `return 1`, else clear `has_defense/offense/misc` ✓. RNG order preserved throughout.

Callers: C `:805` (use_defensive), `:1837` (use_offensive, non-potion gate), `:2392` (use_misc) ↔ JS muse.js:2415, 946 (with `oclass !== POTION_CLASS` gate ✓), 3046 — all `!== 0` returns ✓.

Callee closure: `verbalize`, `mongone`, `You_hear`, `paralyze_monst`, `set_malign`, `SetVoice`, `monkilled` all LIVE imports. `objdescr_is` local clone verified C-exact against `o_init.c:351–365` (null→false; `impossible` log-only delta). Named: none — accurate for the ported body.

## Hallucinations / overclaim

None. "Every C arm is live" holds for the body; the pre-existing helper clones are disclosed in the D-log parenthetical, not hidden.

## Density

Whole 103-line C function, one module, 141 JS insertions. Breadth-phase right size.

## Verification

Re-ran `hidden-proxy.mjs verify precheck --base e131537d~1 --reach-all`: 0 blocked at baseline and working tree (vacuous note, row cited 0 blocks — correctly no `--base` re-run owed); reach 2/2 baseline-PASS sessions PASS, 0 regressed → REACH-OK. Matches the D-log; not vacuous-by-rewrite. Diff grep: only hit is `FORCETRAP`/`FORCEBUNGLE` const names — benign. Rule #2 clean globally (`imports.mjs --rulecheck`).

## Actionable C-wrongs

1. `precheck`'s three `m_useup` calls bind the divergent muse.js:1266 local clone instead of the live C-exact `mthrowu.js:174` export: clone skips the `obj.owt = weight(obj)` recompute on the quan>1 path (C `mthrowu.c:1161–1170`) and hand-unlinks instead of `extract_from_minvent` (C `:1153–1158`, worn/container handling). Narrow paths (occupant-gated, backfire-gated), clone pre-exists with its own owner — review-debt, unqueued.

## Evidence appendix

`sym.mjs precheck` → NOT EXPORTED, 1 local (js/muse.js:2305) — correct for
a C staticfn; no drift. `csym --callers precheck` → C `:805` (use_defensive),
`:1837` (use_offensive, non-potion gate), `:2392` (use_misc) ↔ JS muse.js
`:946` (inside `if (otmp.oclass !== POTION_CLASS)` ✓), `:2415`, `:3046`,
all `if (i !== 0) return i` ✓.

C `m_useup` (`mthrowu.c:1161–1170`): quan>1 → `quan--` + `owt = weight(obj)`;
else `m_useupall` → `extract_from_minvent(mon, obj, TRUE, FALSE)` + obfree.
Live export js/mthrowu.js:174 ports both arms exactly (GC stands in for
obfree, documented). Local muse.js:1266 does quan>1 → `quan-1` with no
weight recompute, else a manual minvent unlink with no worn handling —
divergent on both arms, hence the debt item above. `sym.mjs m_useup`
confirms the export + 2 locals (muse.js:1266, zap.js:1462); this SHA
changed neither the def nor the call bindings.

`objdescr_is` local (muse.js:1237) vs C `o_init.c:351–365` (null →
`impossible` + FALSE; missing descr → FALSE; `strcmp`): JS matches on all
value arms; the `impossible` log-only delta is immaterial. Verified CLONE.

Re-run output: `verify precheck: baseline e131537d~1 — 0 blocked (0 at
baseline, 0 working)` + `reach precheck: 2 baseline-PASS reach sessions
(2 run): 2 PASS, 0 regressed → REACH-OK`. The D-log's "no --base re-run
owed" is correct: the queue row cited 0 blocks, and the vacuous note names
the re-run rule explicitly.

Verdict: **ACCEPT-WITH-DEBT**
