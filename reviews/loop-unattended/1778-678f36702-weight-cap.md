# Review 1778 — 678f36702 — weight_cap (D-2819)

- SHA: `678f36702` (coverage; carry cap plus the diagonal crevice caller)
- Files: `js/invent.js` `weight_cap` `:1024`; `js/dothrow.js` `hurtle_step` `:3048`; `js/do_wear.js` `Boots_on` now exported `:1436`
- Queue row: Open coverage, 0 corpus blocks cited
- Banned grep: 0 hits in the hunk. `imports.mjs --rulecheck` on the working tree: "Rule #2 clean".
- `imports.mjs --can`: `invent.js` → `polyself.js` `float_vs_flight`, `invent.js` → `do_wear.js` `Boots_on`, `dothrow.js` → `invent.js` `weight_cap`, `dothrow.js` → `mon.js` `bad_rock`: all `ALREADY`. Uses are inside the functions, not top-level reads.

## Intent vs deliverable

Subject promises one `weight_cap` in C order: defer levitation boots while `afternmv == Boots_on`, ignore trapped-floor `I_SPECIAL` for the cap, scale a poly form, then `MAX_CARR_CAP` for levitation, air, or a strong steed, else the wounded-leg reduct while not flying, then restore E/B and the diagonal crevice in `hurtle_step`. The diff is that body, the `Boots_on` export, and the crevice `else if`. `Passes_walls` and the universe-edge arm stay omitted, as the subject says.

## Inventory

| JS symbol | Class | C |
|-----------|-------|---|
| `weight_cap` | C body | `hack.c:4294–4346` |
| `float_vs_flight` | imported `polyself.js:660` | `polyself.c:130–154` |
| `Boots_on` | exported async; compared, not called | `do_wear.c` function pointer |
| `inv_weight` | existing `invent.js:1117` | `hack.c:4350–4365` |
| `bad_rock` | imported `mon.js:180` | crevice corners |
| `bigmonst` | imported `monsters.js:325` | `msize >= MZ_LARGE` |
| `strongmonst` | imported `monsters.js:738` | `mflags2 & M2_STRONG` |
| `is_flyer` | imported | steed in `Flying` |

`csym --callers weight_cap`: `ball.c:968` → `js/ball.js:159`; `dokick.c:226` → `js/dokick.js:991`; `dokick.c:1432` → `js/dokick.js:1761`; `dothrow.c:826` → `js/dothrow.js:3054`; `hack.c:968` → `js/mon.js:223`; `hack.c:4363` inside `inv_weight`; `trap.c:5463` → `js/trap.js:7163`. `decl.h` / `weight.h` are not calls.

`sym.mjs`:

```
weight_cap       js/invent.js:1024   sync
float_vs_flight  js/polyself.js:660   sync
Boots_on         js/do_wear.js:1436   ASYNC — await required
inv_weight       js/invent.js:1117   sync
bad_rock         js/mon.js:180   sync
bigmonst         js/monsters.js:325   sync
```

## C ↔ JS fidelity

`weight_cap` (`hack.c:4294–4346`). Save `ELevitation` and `BLevitation` (`youprop.h:236` extrinsic, `:239` blocked). JS saves the flat and the uprops word and ORs them, because `do_wear.js:395` writes the worn mask on `u.ELevitation` only. `afternmv === Boots_on` matches `do_wear.js:3201`, which stores that function. If `(saveE & W_ARMF)` (`0x20`), both stores lose `W_ARMF` and `float_vs_flight()` runs. Then `B &= ~I_SPECIAL` (`0x20000000`), only writing when the bit was set. C's unconditional clear is the same value.

`carrcap = 25 * (ACURRSTR + ACURR(A_CON)) + 50` (`weight.h:12–14`). `Upolyd` is `umonnum != umonster` (`you.h:554`). Nymph `mlet === 'S_NYMPH'` → `MAX_CARR_CAP` (1000). No `cwt` → `msize / MZ_HUMAN`. Else the C `!strongmonst || (strongmonst && cwt > WT_HUMAN)` scale by `cwt / 1450`. A null `youmonst.data` skips the scale. Named.

`Levitation` (`youprop.h:240`) is `(H || E) && !B` with flats OR'd into the uprops words. `Flying` (`:253–255`) adds `usteed && is_flyer(usteed.data)` and `!BFlying`. Levitation, air, or a strong steed sets `MAX_CARR_CAP`. Otherwise clamp, and while not flying subtract 100 per `EWounded_legs` side (`LEFT_SIDE` is `LEFT_RING`). Sticky `u.Levitation` is not read.

Restore: if the OR'd E or B differs from the saved OR, each store goes back to its own saved word and `float_vs_flight()` runs again. Return `max(carrcap, 1)`. That order matches, including the second call sitting after the return value is chosen.

`float_vs_flight` writes `u.BFlying` and `u.BLevitation` only (`polyself.js:666–674`). C writes `uprops[FLYING].blocked` and `uprops[LEVITATION].blocked`. The cap is computed after the `I_SPECIAL` clear and before the second call, so `carrcap` does not depend on those writes. The second call can set `I_SPECIAL` on the flats while uprops stays at the restored word.

`hurtle_step` (`dothrow.c:822–832`). After the boulder `else if`, `!may_pass` is the universe edge (`:818–821`). JS skips that arm (named) and, with `may_pass` left true, runs the diagonal: both `bad_rock` corners, then `gi.invent && inv_weight() + weight_cap() > 600`, then `bigmonst || too_much` and the `You` wedging line. `inv_weight` (`hack.c:4359–4360`) adds `owt` only when `otyp != BOULDER || !throws_rocks(youmonst.data)`. `js/invent.js:1122–1124` adds `owt` for every non-coin. The sum is `wt - wc + wc`, so the extra boulder weight makes `too_much` true for a rock-thrower C would not wedge. `trap.c:5463` uses the same sum.

## Hallucinations / overclaim

The subject says the diagonal arm calls `inv_weight() + weight_cap()` the way C does. The expression is the C expression. The weight inside `inv_weight` is not. The boots defer, the strong-steed max, and the flying wounded-leg gate are the C arms. The named null-data skip and the named `Passes_walls` / universe-edge holes match the code.

## Density

One function plus the one caller arm in `hurtle_step` that the row's symptom named. The rest of `hurtle_step` is the previous body.

## Verification

Re-run on this SHA: `node scripts/hidden-proxy.mjs verify weight_cap --base 678f36702~1 --reach-all`.

```
verify weight_cap: baseline 678f36702~1 (scoreboard at b05a6b770) — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
smoke weight_cap: no RNG-tagged reach; fixed smoke spread (12 run, 3.5s): 12 PASS, 0 regressed → REACH-OK
```

The queue row cited 0 blocks, so the empty verify is the vacuous note, not a false PASS. No `REGRESSED` session. D-2819's green, strict, and cohort were not re-run here. The boulder miss is not on that smoke path.

## Actionable C-wrongs

1. `inv_weight` (`hack.c:4359–4360`): skip `owt` when `otyp == BOULDER && throws_rocks(youmonst.data)`. `js/invent.js:1122` always adds `owt`. `hurtle_step` (`dothrow.c:826`) then treats that sum as carried weight.

Verdict: **QUALITY-RISK**
