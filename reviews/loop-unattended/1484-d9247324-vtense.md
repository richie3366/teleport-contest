# Review 1484 — d9247324 — objnam.c vtense whole body (D-2525)

## Metadata

- SHA: `d9247324`
- D-id: D-2525. Next index: 1484.
- Files: `js/objnam.js` only (119 diff lines, one-function restart + one
  small helper).
- C locus: `nethack-c/upstream/src/objnam.c:2562–2653` (`vtense`, 92 L).

## Intent vs deliverable

Subject promises: whole `vtense` in C order (THIN → live), no new
cross-module import, throwaway probe kept out of the repo. Diff actually
adds: restarted `vtense` (`js/objnam.js:2282`) + file-local `startsWithCI`;
the old thin clone (a/an-only singular test, unconditional `endsWith('o')`
→ +es, `length > 1` y-guard, no markers/specials/they-you) is replaced.
`/tmp/vtense-probe.mjs` is not in the repo (verified absent from the stat).
Promise matches deliverable.

## Inventory

- Changed: `vtense` (restart, exported sync, same `(subj, verb)` signature —
  all existing JS callers in apply/artifact/dbridge/do pass through
  unchanged).
- New: `startsWithCI` (only added symbol; `SPECIAL_SUBJS`,
  `MAKEPLURAL_VOWELS`, `plural_lowc`, `eqCI`, `strcasecpy_at` all
  pre-date the diff and sit above the use — no TDZ surface).
- `nextobuf` needs no port (C returns a static buffer; JS returns an
  immutable string; all 88 C call sites consume it transiently). Agreed.

## C ↔ JS fidelity

Only new logic is `startsWithCI` + the restart wiring; every helper claim
re-checked here:

- `:2581–2582` a/an: `strncmpi(subj,"a ",2)/"an ",3)` ≡ `startsWithCI`
  (length gate first, so short subjs can't match — C hits NUL → nonzero;
  same outcome). The "NUL-short-safe" claim holds. Confirm.
- `:2583–2593` marker scan: first-space walk, `i !== 0` for C `sp != subj`,
  break on first marker, `spot = i - 1`. Walk step (`indexOf(' ', i+1)` ≡
  `strchr` after `++sp`) and index-0 guard both exact. Confirm.
- `:2594–2596` head fallback: `spot >= 0 ? slice(0, spot+1) : subj` ≡
  `spot = subj + len - 1`. Confirm.
- `:2600–2607` plural guess: `*spot=='s' && spot!=subj && !strchr("us",…)`
  ≡ `head[last]==='s' && last>0 && prev∉{u,s}`; eeth/feet/ia/ae tails
  behind `last-3>=0` / `last-1>=0` guards ≡ BSTRNCMPI underrun arms.
  Confirm.
- `:2609–2620` veto: `SPECIAL_SUBJS` (11 entries, verbatim vs C
  `:2550–2556` including `detect monsters`/`shape changers`; CI compare
  both sides) with exact-head and `<prefix> <spec>` space-tail arms
  (`head[len-1-spe.length]===' '` ≡ `*(spot-ltmp)==' '`). Old code had no
  veto at all (`the invisible erinys` misread plural). Real fix. Confirm.
- `:2625–2627` they/you full-string CI match on `subj` (not head — matches
  C). Confirm.
- `:2630–2652` `vtenseSing`: are→is at 0, have→has at len-2, z/x/s + ch/sh
  + len-2-only -o → +es at len, consonant-y (`MAKEPLURAL_VOWELS =
  'aeiou'` ≡ C `vowels`) → -ies at len-1, else +s — all via
  `strcasecpy_at`, whose overwrite/overrun case rule matches C
  `strcasecpy` (probe cases `Are`/`HAVE` cover the case arms). Two real
  fixes vs the old clone: `-o` restricted to len 2 (`do`/`go`, not
  `embargo`), and 1-letter verbs take the C path. Confirm.
- Guard: `if (subj)` (`:2284`) keeps `""`/null on the sing path, so the
  `head[-1]` read I probed is unreachable — no throw surface. Confirm.
- No RNG in C; none in JS. Noomap: `mkobj.js`/`apply.js` file-local
  `otense` clones are a different C function (`otense`, not `vtense`) —
  correctly out of scope.

## Hallucinations / overclaim

None. The probe claim (43 cases) is plausible and the named cases are
exactly the arms a thin port would miss; the probe file itself is
throwaway by design, not reviewable evidence — the arm-by-arm walk above
is the evidence.

## Density

One 92-line C function, one module, map + verify in one handoff.
Right-sized per §2b.

## Verification

- D-log: syntax (1 changed) · rule2 · hidden note (0 blocked) · smoke
  24/24 · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS.
- Re-run here: `hidden-proxy.mjs verify vtense --base d9247324~1
  --reach-all` → 0 blocked at baseline and working tree (vacuous note,
  honestly reported — the row cited no blocks) + smoke 24 PASS,
  0 regressed → REACH-OK. Matches.
- Diff grep: no FORCE/DIAG/getRngLog/fastforward/seed/coordinate logic;
  no new `js/` import.

## Actionable C-wrongs

None. No Must-fix, no CURRENT change.

Verdict: **ACCEPT**
