# Review 752 — 22730962 — dog.c keepdogs stay_behind + leash (D-1783)

## Metadata
- Full / short hash: `22730962d744adb523a73c4d80ca8a5ad6a2a833` / `22730962`
- Parent: `fe542a1d` (D-1782). This file audits **this SHA only**.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-03 20:11:07 +0200
- D-id: **D-1783**
- Stats: `js/dog.js` +114/−17; `js/dungeon.js` +6/−2; `js/do.js` +1/−1; `js/end.js` +1/−1. Total `js/` insertions **122** ≤250. Band **150–350**.
- Claims to close: Open `dog.c` keepdogs leash. Not `mon_leave`. Both C callers (`do.c:1624`, `end.c:1298`) now `await keepdogs`.
- JS / map: `dog.js` `keepdogs`/`keep_mon_accessible`; `dungeon.js` `on_level` export. `c-js-map/turns.md`.
- Archive **Addressed:** D-1783 `22730962` — leash/stay_behind logic; `fmon` walk is **not** C’s `nmon` save.

**Addressed:** D-1789 `3cb13f27`

## Intent vs deliverable

Git subject promises: Match C `dog.c` `keepdogs` so a pet left behind has its leash snapped and one out of range goes slack, instead of both keeping a silently intact leash.

`node scripts/csym.mjs keepdogs` → `dog.c:788–884`. `--callers`: `do.c:1624` `keepdogs(FALSE)`; `end.c:1298` `keepdogs(TRUE)`. `keep_mon_accessible` `:766–785`. `migrate_to_level` mutates `fmon`.

```788:797:nethack-c/upstream/src/dog.c
    for (mtmp = fmon; mtmp; mtmp = mtmp2) {
        mtmp2 = mtmp->nmon;
        if (DEADMONSTER(mtmp))
            continue;
```

The diff **does** port stay_behind (eating/trapped/amulet), both leash arms, `mintrap` escape, steed `mdrop_special_objs`, `keep_mon_accessible` → `migrate_to_level(MIGR_EXACT_XY)`, `on_level` export, and `await keepdogs` at both live callers. It **does not** port `mon_leave` / `relmon` (named). It **does** walk live `game.fmon` with `for-of` while `migrate_to_level` **splices** that same array. C saves `nmon` because `relmon` / migrate mutate `fmon`. Parent `keepdogs` never spliced mid-loop. This SHA added that splice.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `keepdogs` | LIVE repaired | now async |
| `keep_mon_accessible` | LIVE new (local) | C `staticfn` |
| `on_level` | LIVE export | not clone #14; 12 clones remain |
| `mintrap` / `m_unleash` / `mdrop_special_objs` | LIVE | |
| `migrate_to_level` | LIVE partial | **splices `fmon`** |
| `mon_leave` / `relmon` | OMIT named | |
| `fmon` walk | **C-wrong** | `for-of` alias, not `nmon` snapshot |

`node scripts/sym.mjs`:

```
keepdogs         js/dog.js:411   ASYNC — await required
keep_mon_accessible NOT EXPORTED — 1 LOCAL js/dog.js:374
on_level         js/dungeon.js:1117   sync
             !! ALSO 12 LOCAL CLONE(S) — do NOT add another
migrate_to_level js/teleport.js:2706   sync
mdrop_special_objs js/mon.js:1459   sync
mintrap          js/trap.js:4491   ASYNC
m_unleash        js/apply.js:1429   ASYNC
mon_leave        NOT FOUND
relmon           NOT FOUND
```

`--can do.js dog.js keepdogs`: **ALREADY**. `--can end.js dog.js keepdogs`: **IN-SCC SAFE**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**Stay_behind / leash (`:823–883`).** Follower gate, then `mintrap` if trapped, then steed (clear trap/meal + `mdrop_special_objs`) **else** eating/trapped pline **else** `mon_has_amulet` pline. Stay_behind snaps leash (`humanoid` → Her/His/Its + `m_unleash(FALSE)`). Else-if `keep_mon_accessible` migrates. Else slack (`s_suffix(Monnam)` + `m_unleash(FALSE)`). Snap vs slack strings **match**. `do.js` / `end.js` await **match** C’s two live callers.

**Steed vs amulet.** C’s steed arm is `if / else if / else if`, so a mounted hero never takes the amulet stay_behind path. JS the same. **Match.**

**`keep_mon_accessible` (`:766–785`).** Wizard always; shk/priest/gd only when off the `mextra` home level. **Match.**

**`on_level` export.** Body matches `dungeon.c:1438–1443`. 12 local clones remain. Not clone #14.

**Iteration vs C `mtmp2 = nmon` — live C-wrong.** JS:

```411:421:js/dog.js
export async function keepdogs(pets_only = false) {
    const list = game.fmon || [];
    const stay = [];
    for (const mtmp of list) {
```

`const list = game.fmon || []` is an **alias**, not a copy. `migrate_to_level`:

```2711:2713:js/teleport.js
    const list = game.fmon || [];
    const idx = list.indexOf(mtmp);
    if (idx >= 0) list.splice(idx, 1);
```

A `for-of` Array iterator then **skips** the monster that slides into the removed slot; `game.fmon = stay` at the end **drops** unvisited monsters (not on `mydogs`, not on `migrating_mons`). One accessible Wizard / off-home shk/priest/gd that is not last in `fmon` is enough. C cannot skip the rest of `fmon`.

**RNG.** `keepdogs` itself has no `rn2`. New live draws: `mintrap`; `mdrop_special_objs` → `obj_resists(obj,0,0)` `rn2(100)` on ordinary minvent. Helper logic matches; the walk is the defect.

**Callee closure of the new arms.** LIVE: `mintrap`, `m_unleash`, `mdrop_special_objs`, `migrate_to_level`, `on_level`. OMIT named: `mon_leave`. STUB: **none**. QUALITY-RISK is the iterator, not a stubbed callee.

## Hallucinations / overclaim

“Match C keepdogs” is true for stay_behind **messages** and both leash **strings**. It is **not** true for “C’s follower path in C’s order” once `keep_mon_accessible` runs. Do **not** stamp “Match C `mon_leave`.” Journal “fortress held” is not a leashed-pet-left-behind screen; that path is public-unhit (commit admits it). The splice skip would not show up unless `keep_mon_accessible` fired with a later `fmon` sibling.

## Density

§2b: one C function + `keep_mon_accessible` + `on_level` + both callers async. +122. Did **not** glue `mon_leave`. Right size. The splice is in-scope.

## Verification

Commit: green+strict; 44/44; private probes for adjacent follow / eating snap / trapped follow / slack. **Public-unhit** for leashed stay_behind. This audit read `keepdogs`’s `for-of` against `migrate_to_level`’s `splice` and C’s `mtmp2 = nmon`.

## Actionable C-wrongs

1. **`keepdogs` must not `for-of` the live `fmon` array while `migrate_to_level` splices it.** Snapshot (`[...game.fmon]`) or save the next pointer the way C does `mtmp2 = mtmp->nmon`, so a Wizard / off-home shk/priest/gd cannot delete later monsters when `fmon = stay` runs. One port iter.

Named: `mon_leave`; `relmon`; remaining 12 `on_level` clones; `levl_follower` wizard-amulet; `wizcmds.c:116` `keepdogs(TRUE)` (no JS call — wizard `#wizmakemap` path). Do **not** skip `await mintrap` on a trapped follower. Do **not** snap and slack the same pet. Do **not** write `on_level` clone #14.

Verdict: **QUALITY-RISK**
