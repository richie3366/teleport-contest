# Review 1572 — e4f72681 — mhitm.c fightm whole-body port (D-2613)

**Metadata:** SHA `e4f72681`, `mhitm.c` `fightm`, D-2613.
JS: `js/mhitm.js` (+100/−36: `fightm` restart + 2 import names),
`js/monmove.js` (1 word: `itsstuck` gains `export`).

## Intent vs deliverable

Subject promises: whole-body restart in C order — `resist_conflict` gate,
`ustuck→itsstuck` entry gate, `has_u_swallowed` snapshot, next-link chain
walk with mtmp skip, `!rn2(4)` release/break, `bhitpos`/`notonhead` stamps
before both `mattackm` calls, `(void)` retaliation. Diff actually adds:
the restarted `fightm`, `itsstuck`+`set_ustuck` import names, and the
`export` on `itsstuck`. Promise matches deliverable.

## Inventory

- `fightm(mtmp)` (`js/mhitm.js`, export kept, still async) — full C-order
  restart with per-arm `:line` cites.
- `itsstuck` (`js/monmove.js:1747`) — export-only change; body untouched.

## C ↔ JS fidelity

C locus `mhitm.c:105–172` (68 L, via `node scripts/csym.mjs fightm`;
caller `mon.c:1317` via `--callers`). Full C body read here.
Branch-by-branch confirm against the diff:

- `:110–111` `resist_conflict → return 0` — exact.
- `:112–116` `u.ustuck == mtmp → itsstuck → return 0` — exact
  (`if (u.ustuck === mtmp) { if (await itsstuck(mtmp)) return 0; }`).
  `itsstuck` body re-read here and C-exact: `sticks(youmonst.data) &&
  mtmp === ustuck && !uswallow` + `pline_mon "%s cannot escape from you!"`
  (C `monmove.c:1053–1060`, shown above) — export-only change, no behavior
  delta.
- `:117` `has_u_swallowed` snapshot before the loop — exact.
- `:119–124` chain walk: C captures `nmon = mon->nmon` pre-body and skips
  past mtmp (`if (nmon == mtmp) nmon = mtmp->nmon`). JS has no out-link, so
  the live-array index stands in: `nmon` computed at loop top from
  `indexOf`, with the same skip-past-mtmp step. Both capture next before
  the body runs, so body-side chain mutation affects neither the same way;
  mtmp occurs exactly once in `fmon`, so the skip lands identically.
  Faithful emulation, cited in-code — not a clone, not a stub.
- `:125–129` `mon != mtmp && !DEADMONSTER` — JS `mon !== mtmp &&
  (mon.mhp|0) >= 1` (DEADMONSTER ≡ `mhp < 1`, `monst.h:214`; the `mon`
  null-guard is a JS-safety extra with no C-visible effect).
- `:130` `monnear` gate; `:131–137` `!uswallow && mtmp == ustuck →
  !rn2(4)` release (`set_ustuck(null)` + `pline "%s releases you!"`)
  else `break` — exact.
- `:141–142` stamps (`gb.bhitpos.x/y = mon->mx/my`, `gn.notonhead =
  FALSE`) before the strike; `:162–163` re-stamp onto mtmp before the
  retaliation — both exact (the `if (!game.bhitpos)` guard is a JS-init
  idiom; C's `gb` struct is always present).
- `:145–146` AGR_DIED → 1; `:147–151` swallowed → 0; `:153–164`
  retaliation gate + movement decrement + `(void)` return attack as bare
  `await`; `:166` final return — all exact.

RNG call-for-call: C draws `rn2(4)` (release), `rn2(4)` + `rn2(NORMAL_SPEED)`
(retaliation gate) — JS has all three in the same arms, same order. No
seed/step/coordinate reads.
Caller: C `mon.c:1317` wired at `js/mon.js:3363` (`&& (await fightm(mtmp))`).

Callee closure: all LIVE — `resist_conflict`/`monnear`/`Monnam`/
`engulfing_u`/`mattackm` pre-imported; `itsstuck` now exported async
(`sym.mjs`: `js/monmove.js:1747 ASYNC`, awaited ✓); `set_ustuck` sync
export (`sym.mjs`: `js/mhitu.js:1640 sync`). Required `sym.mjs` output:
`set_ustuck` also has 1 pre-existing local clone (`js/uhitm.js:3103`) —
not added or touched here; this diff imports the canonical export, which
is the correct direction (no clone #2). `mattackm` async, awaited ✓.
No stub, no silent omit; "Named: none new" accurate.

## Hallucinations / overclaim

No dispatch/stub split. The `mattackm`-owns-`:379`-read deferral is
correctly scoped to that function, not this row. The D-log's "full 44/44
auto on shared-file change" is a gate claim consistent with the
shared-file rule (monmove.js export line).

## Density

One C function (68 L), one module + 1-word export. Right-sized.

## Verification

- `node scripts/imports.mjs --rulecheck` → Rule #2 clean (whole `js/`).
- Diff grep: no `FORCE`/`DIAG`/`getRngLog`/`fastforward`/seed reads in
  `js/` hunks.
- Re-measured: `hidden-proxy.mjs verify fightm --base e4f72681~1
  --reach-all` → `0 session(s) blocked` at baseline and working tree
  (vacuous-note path, correctly framed) + `smoke 24/24 PASS, 0 regressed
  → REACH-OK`. Both summary lines cited; matches D-log.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
