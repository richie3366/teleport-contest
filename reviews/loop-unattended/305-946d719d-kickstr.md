# Review 305 — 946d719d — dokick.c kickstr (D-1343)

## Metadata
- Full / short hash: `946d719d7dbc32a4b003601766c0adbe6cd946a1` / `946d719d`
- Parent: `34de9f33` (D-1342). This file audits **this SHA only**. Archive **Addressed:** D-1343 `946d719d` already has the short hash (filled by D-1344).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-21 06:46:21 +0200
- D-id: **D-1343**
- Stats: 10 files, +238 / −138 — `js/dokick.js` +54 / −10. Journal rotate in this SHA.
- Claims to close: Open `dokick.c` `kickstr` (named from D-1335 / review **297**). Not eat choke. `reviews/loop-2026-08-15/` has no unpaid kickstr Must-fix.
- JS / map: `dokick.js` `kickstr` / `kick_ouch` / `dokick` `game.maploc`; `c-js-map/turns.md`. Drawbridge `find_drawbridge` remap; `abuse_dog` / martial knockback; eat/zap/dothrow `killer_xname` still named at this SHA.
- Prior reviews this SHA claims to close: **297** named `kickstr` after `killer_xname` kickobjnam; **298** / D-1336 named it after `maybe_mnexto`.

## Intent vs deliverable

Git subject promises: “Match C dokick.c kickstr so a kick that kills the hero is named "kicking a door" (or the object), instead of a raw kickobjnam or "a wall".”

C `kickstr` (`dokick.c:794–830`):

```
    if (*kickobjnam)
        what = kickobjnam;
    else if (gm.maploc == &gn.nowhere)
        what = "nothing";
    else if (IS_DOOR(typ)) what = "a door";
    … TREE / STWALL / OBSTRUCTED / THRONE / FOUNTAIN / GRAVE / SINK /
      ALTAR / DRAWBRIDGE / STAIRS / LADDER / IRONBARS / "something weird"
    return strcat(strcpy(buf, "kicking "), what);
```

C `kick_ouch` (`:903`): `losehp(..., kickstr(buf, kickobjnam), KILLED_BY)`. C `dokick` (`:1386–1391`): `!isok` → `gm.maploc = &gn.nowhere` then `kick_ouch(..., "")`; else `gm.maploc = &levl[x][y]` **before** pool/object/door. C `kick_ouch` (`:892–897`) remaps drawbridge walls via `find_drawbridge` **before** `kickstr` — still named.

Old JS: `kick_ouch` used `kickobjnam \|\| 'a wall'` with no `"kicking "` prefix and no terrain chain.

The diff **does** port the prefix, the nowhere sentinel (`game.maploc = null`), the terrain chain in C order, `dokick` writing `game.maploc` before those tests, and `kick_ouch(..., kickobjnam.value)` on the object-miss path. It does **not** remap drawbridge walls. Named. `STAIRS` / `LADDER` / `IRONBARS` were already imported from `const.js`. `IS_TREE` already includes arboreal `STONE` (`const.js` ≡ `rm.h:123–124`).

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `kickstr` | C `:794–830`, **wired** | returns string; C writes `buf` |
| `kick_ouch` caller | C `:903`, **wired** | `KILLED_BY` |
| `dokick` maploc | C `:1387`/`:1391`, **wired** | null ≡ `&gn.nowhere` |
| `IS_DOOR`/`IS_TREE`/`IS_STWALL`/… | C `rm.h`, **imported live** | |
| `STAIRS`/`LADDER`/`IRONBARS` | C `rm.h` types, **imported live** | |
| `IS_DRAWBRIDGE` | C `rm.h:136–137`, **imported live** | this SHA added the import |
| drawbridge remap | C `:892–897`, **named omit** | DBWALL still `"a wall"` via `IS_STWALL` |
| airlevel/Levitation hurtle | C `:904–905`, **named omit** | pre-existing |

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` writes / seed names / recorded coordinates. Rule #2 clean. **New gameplay RNG inside `kickstr`:** none. `kick_ouch` still `rn2(3)` / `rnd(5)` / `rnd(CON)` as before.

## C ↔ JS fidelity

`if (kickobjnam)` vs C `if (*kickobjnam)`: default `''` and object path `kickobjnam.value` (already `killer_xname` from D-1335). Empty string is falsy. Match. Non-empty `"a dart"` → `"kicking a dart"`. Match `:798–799` + `:830`.

Nowhere: `!game.maploc` → `"nothing"`. `dokick` `!isok` sets null then `kick_ouch` with default `''`. Match `:1387–1388` / `:800–801`.

Terrain chain order matches `:802–829` byte-for-byte on the nouns (`"a door"` … `"the stairs"` … `"an iron bar"` … `"something weird"`). `IS_TREE` before `IS_STWALL` so arboreal `STONE` is `"a tree"` not `"a wall"`. Match. `IS_STWALL` is `typ <= DBWALL` (`rm.h:118` / `const.js:2103–2105`), so a drawbridge **wall** cell is `"a wall"` until the named remap. That is the C predicate **without** `:892–897`, stated in the D-log.

`game.maploc = loc` uses `game.level.at(x,y)` which already has `.typ` (pool/door tests in the same function). Not a glyph stand-in.

Hallucination check: “Match C `kickstr`” while **drawbridge remap is omitted** is an overclaim on DBWALL deaths. Dispatch is live. Do **not** stamp “Match C `find_drawbridge`.” Do **not** stamp “Match C eat choke.”

## Hallucinations / overclaim

Subject says a kick that kills the hero is named `"kicking a door"` (or the object) instead of a raw kickobjnam or `"a wall"`. **True for doors/trees/objects/nowhere/stairs.** False for an unmapped drawbridge wall until remap. D-1343 **Not this iter** names that. Stamping **Addressed:** D-1343 for `kickstr` + maploc is fair. Do **not** treat fortress PASS as a kick-death tombstone.

## Density

One C function plus its one `losehp` caller and the two `gm.maploc` writes that feed it. ~45 lines. Playbook §2b. Did not glue choke / `dozap`. Acceptable size.

## Branch-by-branch confirm

1. Object kickobjnam set: `"kicking " + killer_xname`. Match `:798–799`.
2. `!isok`: `"kicking nothing"`. Match `:800–801`.
3. Door / tree / wall / rock / furniture / drawbridge-down / stairs / ladder / bars. Match `:802–827`.
4. Arboreal `STONE`: `"a tree"`. Match `IS_TREE`.
5. Unknown typ: `"kicking something weird"`. Match `:828–829`.
6. Drawbridge **wall** without remap: `"kicking a wall"` (named). C after remap: `"kicking a drawbridge"`.
7. `kick_ouch` still `KILLED_BY` (no extra article). Match `:903`.
8. **Public-unhit** unless a session dies from a kick.

## Anti-pattern / Rule #2 (this SHA `js/`)

No FORCE / fs / seed-named gates. `game.maploc` is the C global, not a recorded coordinate. Plain ESM.

## Verification

Journal: private canary **41**/41; green+strict seed8000/0900; cohort **8**/8 + strict 1500/1800/0012/0004/0007/2200/0383 + seed0060. **Public-unhit** on kick-death. Cadence on review **307**. I did not re-run the private canary. seed0060 kicking is not a tombstone proof.

## Actionable C-wrongs

None for Must-fix. `kickstr` control flow and `dokick` maploc writes match C `:794–830` / `:1386–1391`. Drawbridge remap is a named omit of a later `kick_ouch` assignment, not a broken `kickstr` body.

Named omits (map, not Must-fix):

1. `kick_ouch` `find_drawbridge` remap of maploc
2. airlevel / Levitation hurtle after `losehp`
3. `abuse_dog` / martial knockback (other Open rows)

Do not Must-fix “object deaths should drop the `kicking ` prefix” (C always prefixes). Do not Must-fix “nowhere should say `a wall`” (C says `nothing`).

## Callers / RNG ledger

C: `kick_ouch` → `kickstr` (no RNG) → `losehp`. JS: same. `kick_ouch` RNG is unchanged. Public fortress is not a kick-death.

## Verdict

- Verdict: **ACCEPT-WITH-DEBT**
- One sentence: `losehp` now stores `"kicking a door"` / object / `"nothing"` from `kickstr`; drawbridge-wall remap stays named.
- Must-fix stays empty for this SHA.
