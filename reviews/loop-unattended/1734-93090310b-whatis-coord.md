# Review 1734 — 93090310b — whatis_coord option (D-2775)

- SHA: `93090310b` (`options.c` handler_whatis_coord + optfn_whatis_coord, D-2775)
- Files: `js/options.js` (+140/−3), docs
- Queue row: Open coverage (MISSING), 0 corpus blocks cited
- Banned grep: 0 hits. `imports.mjs --rulecheck`: "Rule #2 clean" (re-run this audit).

## Intent vs deliverable

Subject promises the handler + optfn with the option live. Diff
delivers: `optfn_whatis_coord` (do_init/do_set/get_val, do_handler
async-split), `handler_whatis_coord`, the `doset_optfn_do_handler` arm,
allopt optfn wiring, doset row with live value + handler, the
`getpos_coords = GPCOORDS_NONE` init, two rc do_set sites, and an
`initial` parameter on `string_for_env_opt` so the rc parser reaches
the C opt_initial path. Complete; no gap found.

## Inventory

| JS symbol | Class | C (csym range) |
|-----------|-------|----------------|
| `optfn_whatis_coord` (new, export) | C body minus do_handler arm | `options.c:4702–4745` |
| `handler_whatis_coord` (new, export) | C body | `options.c:6205–6276` |
| `string_for_env_opt` 4th param | C gate, behavior-preserving | `options.c:6682–6690` |
| doset arm + allopt optfn + rc sites + init | C callers | doset `:8935`, parseoptions `:636`, initoptions_init `:7190` |

Nothing deleted or re-pointed. Callees LIVE: `string_for_env_opt`,
`string_for_opt`, `lowc` (hacklib import), `select_menu_pick_one`,
`windowport_tty`, `set_optbuf`, `allopt_name`. GPCOORDS_* values match
`flag.h:177–181` exactly; optlist.h `:868` has `has_handler=Yes` (h
column of NHOPTC) ✓.

## C ↔ JS fidelity

- do_set walked against `:4710–4730`: negated → NONE + OK first ✓;
  `op` reassigned from `string_for_env_opt(name, opts, FALSE)` ✓;
  `!== EMPTY_OPTSTR` ⟺ `!= empty_optstr` ✓; five-char set +
  `lowc(*op)` + `c && strchr` ⟺ `includes` ✓ (`op` is never empty
  here — `string_for_opt` guarantee — so the length guard is dead
  code both ways); `"Unknown %s parameter"` arm → OPTN_ERR named ✓;
  empty arm → OPTN_ERR without storing ✓. get_val five-way chain
  exact ✓.
- Handler walked against `:6205–6276`: five rows in C order with
  verbatim texts, `a_char` + letter accelerator = GPCOORDS_* ✓,
  omitted `gselector` ⟺ C `gacc 0` (helper only matches present
  gselectors — verified) ✓, preselect on live `gpc` ✓, blank + map
  line `<1,0>/<79,20>` + verbose tail ✓, non-tty-only screen-offset
  line via `!windowport_tty()` ✓, `#if COLNO == 80` is true for the
  contest build so the hardcoded `; column 80 is not used` tail is
  exact ✓, `[%02d,%02d]` → `[02,01]` / `[22,79]` ✓, pick stores
  `a_char` ✓, `pick_cnt > 1` fold named (file precedent) ✓.
- `string_for_env_opt` default `initial = game.go?.opt_initial`
  preserves the sole other caller (`warning_opts` `:7527`) ✓; rc
  sites pass literal `true`, matching C `go.opt_initial=TRUE`
  during initial load ✓.
- Init: C `:7190` confirmed (`iflags.getpos_coords =
  GPCOORDS_NONE`); JS sets it pre-`!rc` return and `g.iflags`
  spreads rc values last ✓. Unlike D-2774, the site-2 `lname` is
  safe here — no name `strncmp` reads `opts` case. No RNG.

## Hallucinations / overclaim

None. Spot-checks (`''→n`, `map→m`, `Screen→s`, `!→n`, `x→err`,
`f→f`) all reproduce C, including `lowc` on the first char only.

## Density

~120 behavior lines for two C functions + wiring: in range. Named
omits (error sink, glyph columns, `n>1` fold) in the map this commit.

## Verification

Re-ran `node scripts/hidden-proxy.mjs verify handler_whatis_coord
--base 93090310b~1 --reach-all`: 0 blocked + vacuous note (expected)
+ smoke 24/24 REACH-OK. Matches the D-log.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
