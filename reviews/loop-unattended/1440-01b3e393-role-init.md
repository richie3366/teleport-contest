# Review 1440 — 01b3e393 — role_init whole-body port (D-2481)

Metadata: SHA `01b3e393`, `js/roles.js` (+292) + `js/hacklib.js`
(+72) + `js/u_init.js` (−111, five helpers deleted) +
`js/allmain.js` (+8) + `scripts/role_init.test.mjs`. C
`role.c:1980–2117` + `:718–744`/`:786–810`/`:915–940`/
`:1664–1721` + `hacklib.c:555–621`. D-log: D-2481.

## Intent vs deliverable

Promise: whole `role_init` in C order (selection + quest-pm +
pantheon arms), split u_init helpers merged, newgame order per C
`:785–786`. Diff delivers that: five deleted helpers reappear
arm-for-arm inside `roles.js role_init`; the Priest god-fill
carry-over compensates the pre/post-shape move.

## Inventory

- Added: `randrole`/`randrole_filtered`/`randrace`/`randalign`/
  `plnamesuffix`/`role_init`, `strNsubst`/`findword`, MS_* consts
  (file-local idiom, 8-file precedent).
- Deleted (re-pointed, not stubbed): `role_init_pantheon/
  _godgend/_cleric_spe_light/_quest_pm_fixup/_nemesis_gender` —
  required `sym.mjs`: no remaining refs in `js/` (grep: zero
  hits outside the tombstone comment).
- Callee closure (all LIVE, single defs): `ok_*`/`valid*`
  (player_selection.js:223–350), `tty_askname` (askname.js:96,
  async, awaited), `findword` (hacklib.js:338), `strNsubst`
  (hacklib.js:297), `commit_pm_fixup`, `PM_CLERIC`
  (roles.js:14 import), `P_CLERIC_SPELL`/`PL_CSIZ`/`ROLE_*MASK`.
  u_init's dropped imports leave zero dangling uses (grep clean).

## C ↔ JS fidelity

- Selection: `randrole` (`rn2(roles.length)`, no-terminator
  confirmed: 13 roles/5 races), `randrole_filtered` (same
  `ok_*` arg order), `randrace` (x100 + `Math.trunc` int-div;
  fallback `rn2(races.length)` = C `SIZE-1`), `randalign`,
  gender flip gated on `pantheon===-1`, `pl_character` copy +
  `[PL_CSIZ-1]` cut — all verbatim vs `:718–2117`.
- `plnamesuffix`: genericusers + dash-token walk + `do…while`
  + comma→space, vs `:1664–1721` verbatim; `findword` (4-arg,
  incl. `p+1` rescan and NUL-vs-undefined terminator) and
  `strNsubst` (Nth/all, empty-orig append case, BUFSZ-1 cap,
  count-unobservable-at-sole-callsite) verbatim vs
  `hacklib.c:555–621`.
- Tail: leader/guardian/nemesis fixups (assign-vs-OR preserved:
  `msound=` assign, flags `|`/`&~`), `rn2(100)<50 → 1/0`
  genders, pantheon `trycnt<100` + linear scan, god fill,
  `godgend` via `align_gtitle(urole, alignmnt)` — C
  `pray.c:2628` reads `gu.urole` by alignment with
  leading-`_` goddess test; JS takes urole explicitly (same
  fields, fill already applied) — equivalent. Cleric
  SPE_LIGHT, `#if 0` infravision omitted like C.
- Order: allmain sets `pantheon=-1` + awaits `role_init()`
  before init_dungeons/u_init, exactly C `allmain.c:785–786`.
  ROLE_NONE=-1 so the `str2role < 0` gate matches C.
- Priest carry-over (`_prevUrole` lgod/ngod/cgod restore) is
  required by the move, not a shim: fill runs on the spread,
  shaping rebuilds from the table (Priest lgod null) — verified
  by the REACH line below, not by reading.

## Hallucinations / overclaim

None. The "3 Priest sessions screen-failed until this" is
reported as a found-and-fixed development observation, and the
ship state is proven by full 44/44 + 91/91 REACH.

## Density

Large but single-function-family: one C body + its helpers,
207 net lines. Within cap.

## Verification

- `hidden-proxy verify role_init --base 01b3e393~1 --reach-all`
  (re-run): 0 blocked (coverage row, as stated); **REACH 91/91
  baseline-PASS sessions execute it — 91 PASS, 0 regressed**.
  Strongest gate in this batch; matches the D-log.
- `role_init.test.mjs` 6/6 per D-log (arm pins; full-session
  parity rests on the REACH line above).
- Diff grep: no FORCE/DIAG/seed/fastforward/coords.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
