# Review 500 — 719506a4 — artifact.c set_artifact_intrinsic cspfx W_ART (D-1539)

## Metadata
- Full / short hash: `719506a45e6fa2c1a190d2941c8224f2355f156f` / `719506a4`
- Parent: `e7574dc9` (D-1538). This file audits **this SHA only** (ninth of nine `js/` commits since review **491**). Archive **Addressed:** D-1539 `719506a4`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-26 07:47:03 +0200
- D-id: **D-1539**
- Stats: 13 files, +229 / −98 — `js/artifact.js` +70 / −7, `js/generated/artifacts_data.js` +68 / −34, `js/invent.js` +10 / −3, `js/u_init.js` +9 / −3. Band 150–350 (js/ insertions ~157).
- Claims to close: Open `artifact.c` cspfx W_ART (named from D-1538 / D-1514 SPFX_WARN wield). Not a public FAIL. `reviews/loop-2026-08-15/` has no unpaid cspfx Must-fix.
- JS / map: extractor + `artifacts_data.js` + `artifact.js` + `u_init.js` `addinv` + `invent.js` `freeinv_core`. `c-js-map/data.md`.
- Prior reviews this SHA claims to close: none unpaid.

## Intent vs deliverable

Git subject promises: carrying MKoT / Orb of Fate confers WARN (and the other carry bits) via `W_ART`, not a named omit that zeroed `cspfx` and skipped invent conferral.

Pinned C `artifact.c` `set_artifact_intrinsic` `:770` `spfx = (wp_mask != W_ART) ? oart->spfx : oart->cspfx`; drop strip `:771–778` `spfx &= ~art->cspfx`; ESP `:798–805`, STLTH `:806–811`, TCTRL `:818–823`, WARN `:824–839`, EREGEN `:841–846`, HSPDAM `:847–852`, HPHDAM `:853–858`. `artilist.h` A() s2 (4th field). Callers `invent.c` `addinv_core1` `:991` (from `addinv_core0` `:1082`, before merge/link) and `freeinv_core` `:1383` (`freeinv` extracts first `:1405–1407`). `prop.h` `W_ART 0x00001000`. `artifact.h` bit values match JS `SPFX_*`.

```770:778:nethack-c/upstream/src/artifact.c
    spfx = (wp_mask != W_ART) ? oart->spfx : oart->cspfx;
    if (spfx && wp_mask == W_ART && !on) {
        for (obj = gi.invent; obj; obj = obj->nobj)
            if (obj != otmp && obj->oartifact) {
                art = get_artifact(obj);
                if (art != &artilist[ART_NONARTIFACT])
                    spfx &= ~art->cspfx;
            }
    }
```

```279:282:nethack-c/upstream/include/artilist.h
    A("The Master Key of Thievery", SKELETON_KEY,
      (SPFX_NOGEN | SPFX_RESTR | SPFX_INTEL | SPFX_SPEAK),
      (SPFX_WARN | SPFX_TCTRL | SPFX_HPHDAM), 0, NO_ATTK, NO_DFNS, NO_CARY,
```

Old JS: extractor skipped args[3]; conferral forced `spfx=0` on `W_ART`; addinv/freeinv never called the function with that mask.

The diff **does** extract s2, use `oart.cspfx` on `W_ART`, port the listed SPFX arms + other-art strip, and wire addinv on / freeinv_core off. It **does not** port defn/cary resist (`:740–767`), SPFX_SEARCH/REGEN/XRAY/PROTECT, `inv_prop` `arti_invoke` on drop (`:880–885`), questart `artitouch`, zap poly `addinv_core1`, or Sunsword EBlnd. Named.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| extractor A() s2 | C `artilist.h`, **LIVE this SHA** | `eval_spfx(args[3])` |
| `set_artifact_intrinsic` `:770` | C, **LIVE this SHA** | no longer zeros cspfx |
| drop strip `:771–778` | C, **LIVE this SHA** | |
| ESP/STLTH/TCTRL/WARN/EREGEN/HSPDAM/HPHDAM | C `:798–858`, **LIVE this SHA** | WARN was D-1514; now also cspfx |
| `recalc_telepat_range` | C `do_wear.c`, **LIVE** | import |
| `see_monsters` | C `display.c`, **LIVE** | |
| `set_spfx_extrinsic` | C `|=` / `&=~`, **CLONE** | local helper |
| `addinv` / `freeinv_core` | C `:991` / `:1383`, **LIVE this SHA** | |
| defn/cary / SEARCH/REGEN/XRAY/PROTECT / `arti_invoke` | C `:740–767` / `:781–877` / `:880`, **OMIT named** | |

`node scripts/sym.mjs set_artifact_intrinsic recalc_telepat_range addinv freeinv_core spec_m2 see_monsters`:

```
set_artifact_intrinsic js/artifact.js:586   sync
recalc_telepat_range js/do_wear.js:297   sync
addinv           js/u_init.js:894   ASYNC — await required
freeinv_core     js/invent.js:3589   sync
spec_m2          js/artifact.js:359   sync
see_monsters     js/display.js:2990   sync
```

This SHA does not delete a local clone; it stops zeroing `cspfx` and imports the already-exported conferral into invent/addinv. `invent.js` ↔ `artifact.js` cycle: `export function` is hoisted.

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` / seed names / recorded coordinates in scored `js/` (extractor is `scripts/`). `u_init.js` `SPE_FORCE_BOLT` is a spell otyp, not a gate. Rule #2 clean. **No gameplay RNG.**

## C ↔ JS fidelity

Extract. A() s2 → `cspfx`. MKoT `393248` = `SPFX_WARN|TCTRL|HPHDAM` (`0x20|0x40000|0x20000`). Fate `196640` = `WARN|HSPDAM|HPHDAM`. Heart `8192` = STLTH. Mirror / Longbow `4096` = ESP. Detection / PYEC `69632` = ESP|HSPDAM. Eye `98304` = EREGEN|HSPDAM. Sting/Orcrist cspfx `0` (WARN is wield `spfx`). **Match `artilist.h` and `artifact.h` bits.**

Select. `wp_mask !== W_ART` → wield/worn `spfx`; else `cspfx`. **Match `:770`.** `W_ART` is `0x1000` (`prop.h`).

Drop strip. Only `W_ART && !on && spfx`. Walk invent, skip self / non-art / dummy, `spfx &= ~other.cspfx`. **Match `:771–778`.** C `freeinv` unlinks first, so the droppee is already absent; JS `obj === otmp` still skips if a caller has not unlinked. Same bits off.

Carry arms this SHA (order vs C): HALRES (pre-existing mask helper, not `make_hallucinated` talk — named), ESP (`ETelepat` + `recalc_telepat_range` + `see_monsters`), STLTH, TCTRL, WARN (`spec_m2` → `EWarn_of_mon`+`warntype.obj`+`see_monsters`, else `EWarning`), EREGEN, HSPDAM, HPHDAM. **Match `:798–858` call-for-call for those bits.** Skipped SEARCH (`:781`), REGEN (`:812`), XRAY (`:859`), PROTECT (`:873`) — **named.** No live `cspfx` row uses those four (Palantir is `#if 0`; Trollsbane REGEN / Eyes XRAY live in **s1** wield/worn `spfx`).

Callers. JS `addinv` confers `on, W_ART` before merge — **Match `addinv_core0` `:1082` before link.** JS `freeinv_core` confers off — **Match `:1383`.** `addinv_nomerge` goes through `addinv`. zap poly `addinv_core1` still named.

Callee closure (W_ART conferral arm). LIVE: extract, `get_artifact`, `set_spfx_extrinsic` (verified `|=`/`&=~`), `recalc_telepat_range`, `see_monsters`, `spec_m2`, addinv/freeinv_core. OMIT named: cary/defn, SEARCH/REGEN/XRAY/PROTECT, `arti_invoke`, questart, zap poly. STUB: none. **The arm may ship.** Do **not** zero `cspfx` on `W_ART` again.

## Hallucinations / overclaim

Subject carry WARN via W_ART + invent callers: **true.** D-log “Sting W_ART silent vs W_WEP MATCH_WARN”: **true** (cspfx 0). Do **not** stamp “Match C `cary` AD_MAGM” (Orb of Detection still named). Do **not** stamp “Match C `arti_invoke` on drop.” Do **not** stamp “Match C SPFX_REGEN wield” (Staff / Trollsbane s1). This is **not** “dispatch ported, callee stubbed.”

## Density

One C function’s carry select + the SPFX bits that appear in s2 + the two invent callers. Did not glue `ghostfruit`. §2b OK.

## Branch-by-branch confirm

1. Wield `W_WEP`: still `oart.spfx` (Sting WARN). **Match.**
2. Carry MKoT: WARN+TCTRL+HPHDAM on `W_ART`. **Match.**
3. Carry Fate: WARN+HSPDAM+HPHDAM. **Match.**
4. Carry Sting: cspfx 0, no WARN. **Match.**
5. Drop MKoT while Fate remains: strip `&= ~Fate.cspfx` so WARN/HPHDAM stay. **Match.**
6. ESP carry: recalc + `see_monsters`. **Match.**
7. SEARCH/REGEN/XRAY/PROTECT: still skipped. **Named.**

## Callers / RNG ledger

C: `addinv_core1` / `freeinv_core`; worn.c still W_WEP/armor. JS the same plus existing wield.js. Public-unhit (no public quest-art carry). No seed gate.

## Anti-pattern / Rule #2 (this SHA `js/`)

Plain ESM. Extractor writes `js/generated/`. No scored `fs`. No FORCE.

## Verification

D-log canary **16**/16 (grep; extract MKoT/Fate/Sting/Heart/Detection/Eye; W_ART on/off; Sting silent vs W_WEP; two-art drop strip; addinv/freeinv_core; Rule #2); green+strict; cohort **7**/7. **Public-unhit.** Admit it.

## Actionable C-wrongs

None for Must-fix. Named: defn/cary; SEARCH/REGEN/XRAY/PROTECT; `arti_invoke`; questart `artitouch`; zap poly `addinv_core1`.

Verdict: **ACCEPT-WITH-DEBT**
