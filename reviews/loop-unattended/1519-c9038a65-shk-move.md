# Review 1519 — c9038a65 — shk.c shk_move (D-2560)

## Metadata

- SHA: `c9038a65`
- D-id: D-2560. Next index: 1519.
- Files: `js/shk.js` (+150/−~60: restarted
  `shk_move`, `Your` + `HAND` import joins,
  header omit-list refresh).
- C locus: `nethack-c/upstream/src/shk.c:4879–4993`
  (`shk_move`, 115 L; `csym.mjs` range). Caller:
  `monmove.c:1807` (pre-existing JS call,
  unchanged by this SHA).

## Intent vs deliverable

Subject promises: the whole 113-line body in C
order with the following/followmsg envelope and
missing arms ported, the 1 C caller wired. Diff
delivers exactly that. Three real behavior fixes
inside the restart: `resist_conflict` now gates
`Conflict` (was stubbed-false), the ANGRY chase
gains `m_canseeu` (was `mcansee` only), and the
Fast+`sobj_at` pickaxe arm plus `z > 0 →
after_shk_move` are wired (were deferred).
Promise matches deliverable. RNG (`rn2(9)`)
in C position.

## Inventory

- Restarted: `shk_move(shkp)` (exported async).
- Callees, all LIVE or verified file-local:
  `resist_conflict`/`m_canseeu` (already
  imported), `Displaced` (`:4271`), `hero_deaf`
  (`:1566`), `muteshk` (`:217`), `rile_shk`
  (`:4465`), `SetVoice`/`Hello`/`verbalize`/
  `Shknam`/`noit_mhis`/`shkname` (already
  imported), `mbodypart` via lazy polyself
  import (`u_entered_shop :646` precedent, same
  SCC, no new static edge), `Fast` (attrib.js
  function form), `sobj_at`, file-local
  `holetime` mirror (`:4032`), `move_special`,
  file-local `after_shk_move` (`:4594`).
- No deleted or re-pointed symbols → no
  clone→import audit (`Your`/`HAND` join
  existing display/const edges).

## C ↔ JS fidelity

Body vs C `:4879–4993`, in order:

- `:4892–4893` `shk_fixes_damage` stays named
  (map + header) ✓; `:4895` `dist2` ≡ `distu`
  ✓; `:4897` Conflict short-circuit keeps the
  no-draw-on-ANGRY order ✓; `:4898–4900`
  Displaced `Your` + `mattacku` ✓.
- `:4903–4931` following envelope exact:
  `strncmp(·, PL_NSIZ)` ≡ dual `slice(0, 32)`
  (`PL_NSIZ = 32` verified); Deaf/mute gates;
  both verbalize texts; palm-out else arm;
  `followmsg = moves`; `rn2(9)` + `rile_shk`;
  `udist < 2` return ✓.
- `:4935–4950` appr/gtx/gty/satdoor; the
  following-or-holetime gate — JS hoists the
  pure `holetime()` call above the `||`, which
  is unobservable (state read, no RNG/side
  effects, verified body `:4032–4041`) ✓;
  `:4947–4948` far-follower `-1` ✓.
- `:4951–4955` ANGRY chase with the
  `mcansee && m_canseeu` conjunction ✓;
  `:4960–4981` Invis/steed, door-stand
  (`badinv` triple incl. Fast floor-pickaxe),
  `*ushops && distu > 8` (symmetric `dist2`;
  `''`-falsy ≡ NUL-first) ✓; `GDIST < 3` +
  `appr = gtx = gty = 0` ✓.
- `:4987–4992` `move_special` args unchanged;
  `z > 0 → after_shk_move` newly wired;
  `check_special_room(FALSE)` stays map-named
  (file header) ✓.
- Caller `monmove.c:1807` → `monmove.js:1836`
  pre-existing, `-2`/`-1` handling intact ✓.

## Hallucinations / overclaim

None. "No new live-arm stubs" holds — the two
named items are map-registered subsystems, and
the header omit-list was correctly shrunk
(angry-Displaced pline, following verbalize,
`m_canseeu`, Fast+`sobj_at` all shipped here).

## Density

One 115-line C function, one file, ~150
insertions. Right-sized per §2b.

## Verification

- D-log: `verify.mjs --fn shk_move` → PASS,
  honestly framed as shopkeeper-follow path /
  0-blocked.
- Re-run here: `hidden-proxy.mjs verify shk_move
  --base c9038a65~1 --reach-all` → 0 blocked
  both trees (vacuous, honestly reported) +
  smoke 24 PASS, 0 regressed → REACH-OK. Matches.
- `imports.mjs --rulecheck`: clean (re-run this
  iteration). Diff grep: 0 hits for FORCE/DIAG/
  getRngLog/fastforward.

## Actionable C-wrongs

None. Branch order, RNG position, message
texts, caller wiring, and the named omissions
all check out against pinned C.

Verdict: **ACCEPT**
