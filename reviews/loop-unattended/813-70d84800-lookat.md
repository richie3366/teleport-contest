# Review 813 — 70d84800 — pager.c lookat glyph_is_unexplored (D-1843)

## Metadata
- Full / short hash: `70d84800ea42d6a92ad0c2cb19eb2ee0adc06756` / `70d84800`
- Parent: `9dc829a9` (D-1842). Map-driven Open: 4 corpus `"unexplored area"` vs `"unexplored"`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-05 05:03:47 +0200
- D-id: **D-1843**
- Stats: `js/pager.js` +254/−70; `js/display.js` +96/−3; `js/getpos.js` +21/−245. `js/` insertions **294** >250. Band **80–450**.
- Claims to close: Open `lookat` unexplored-area string. Not `do_screen_description` symbol table.
- JS / map: `lookat` / `glyph_to_cmap` / `brief_at` / `auto_describe_text`. `c-js-map/turns.md`. Archive **Addressed:** D-1843 `70d84800`. Must-fix: extra `S_room`/`S_darkroom` arms (this review).

## Intent vs deliverable

Git subject promises: C `"unexplored area"` vs JS `"unexplored"`; owner `pager.c:736` `glyph_is_unexplored`; port glyph-first lookat and `glyph_to_cmap`.

`node scripts/csym.mjs lookat` → `pager.c:656–802`. `--callers lookat`: `pager.c:1599` (`do_screen_description`). `glyph_to_cmap` `glyphs.c:199–231`. `auto_describe` `getpos.c:639–662` (`do_screen_description` then firstmatch). `look_at_monster` `:421–555` (`:710`). `look_at_object` `:379–419`.

Parent had no named `lookat`; `brief_at` / `auto_describe_text` used `!seenv` / STONE → `"unexplored"`. The diff **does** add `lookat` + `glyph_to_cmap` and delete the getpos cmap clones. It also **adds `S_room`/`S_darkroom` switch arms C does not have**.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `lookat` | LIVE new | glyph-first; extra room arms |
| `glyph_to_cmap` | LIVE new | `glyphs.c:199–231` |
| `glyph_to_warning` | LIVE new | `display.h:730–731` |
| `brief_at` / `auto_describe_text` | LIVE re-point | lookat buf + blocked-stair rewrite |
| getpos `cmap_defsym_explanation` / `door_cmap_explanation` / `stair_ladder_explanation` / `self_lookat_brief` / `look_at_monster_brief` / `mon_at_xy` | deleted clones | rebound |
| `look_at_monster_buf` | CLONE partial | named health/stuck/leash/trap/hallu/tail |
| `look_at_object` | LIVE | named doname_with_price / buried suffixes |
| `self_lookat` | LIVE | named steed/utrap |
| `do_screen_description` table | OMIT named | later owner of the four |
| `ice_descr` / underwater unreconnoitered | OMIT named | |

`node scripts/sym.mjs` (deleted clones → lookat):

```
lookat           js/pager.js:985   sync
glyph_to_cmap    js/display.js:649   sync
glyph_to_warning js/display.js:674   sync
auto_describe_text js/getpos.js:535   sync
glyph_is_unexplored js/display.js:789   sync
mon_at_xy / self_lookat_brief / look_at_monster_brief /
stair_ladder_explanation / door_cmap_explanation /
cmap_defsym_explanation  NOT FOUND
```

FORCE/DIAG/`getRngLog`/`fastforward`: **none**. Rule #2: clean.

## C ↔ JS fidelity

**Unexplored (`:730–737` vs `:779–787` vs else `:799`).** `glyph_is_unexplored` → `"unexplored area"` (Underwater land/unknown named). cmap `S_stone` + `!seenv` → `"unexplored"`; else stone/SCORR `"stone"` then FALLTHROUGH defsyms. JS same three strings. **Match that RNG-free string split.** That is what moved the four sessions.

**`glyph_to_cmap` (`:199–231`).** Stone off, five wall banks, A/altar/B/C, zap `% 4`, swallow `& 0x7`, explosion `% nexpl`, else MAXPCHARS. JS matches. Compact `glyph_is_cmap` (`display.h:723–725`) stops before zap/swallow — both sides. **Match.**

**Other lookat arms.** Self / swallow / mon / obj / trap / warning / invisible / nothing / cmap altar, ndoor, cloud, waterbody, engraving, else `"unexplored area"`. Branch order matches `:665–800`. `look_at_monster` health/stuck/leash/trap/hallu/worm-tail look coords named. `look_at_object` still `doname` not `doname_with_price` — named.

**C-wrong: `S_room`/`S_darkroom`.** C cmap switch has no such cases. After `S_stone` FALLTHROUGH, default is `defsyms[symidx].explanation` (`:794–795`). JS inserts:

```1104:1111:js/pager.js
        case S_room:
        case S_darkroom:
            buf = room_cmap_explanation(x, y, loc);
            break;
```

That reconstructs `newsym` DARKROOMSYM (`!cansee && (!waslit \|\| dark_room)`) **inside lookat** because “JS gbuf often stores `S_room` for both”. That is not a C `lookat` `if`. It is a clone that contradicts the shipped function. D-0812 belongs in `newsym` paint, not here.

**Callee closure.** One `lookat` family. `glyph_to_cmap` / `trap_description` / `look_at_object` LIVE. `look_at_monster_buf` is a named-partial CLONE. `do_screen_description` OMIT. The extra room arms are not LIVE/OMIT/verified-CLONE of C lookat.

## Hallucinations / overclaim

“Port lookat cmap switch” is true for unexplored / S_stone / altar / ndoor / cloud / water / engraving. It is **false** for floor: those two cases are not in C. Do **not** stamp `do_screen_description`. Four moved past — not corpus PASS of the later owner. D-log 0 PASS / 4 moved is true.

## Density

§2b: `lookat` + `glyph_to_cmap`, getpos clones deleted. +294. Did **not** glue `summonmu`. Right size. The miss is the extra floor arms, not thinness.

## Verification

This audit: `node scripts/hidden-proxy.mjs verify lookat --base 70d84800~1` → `4 session(s) blocked`. Summary: **`0 PASS, 4 moved past, 0 unchanged, 0 worse → PROGRESS`** (`explore-seed0360-wizard-world-tour-19199bfa` → `do_screen_description` step 836 was 826; `77350e1f` 835 was 832; `explore-seed0367-priest-quest-tour-1cbaa856` 314 was 313; `b0096089` 326 was 323). Matches the D-log. Not vacuous.

## Actionable C-wrongs

1. **`lookat` cmap switch must not special-case `S_room`/`S_darkroom`.** Match `pager.c:779–795`: `S_stone` then `default: defsyms[symidx].explanation`. Delete the `room_cmap_explanation` arms. If dark-room strings then fail, port `display.c` `newsym` DARKROOMSYM (D-0812) so the gbuf glyph is already `S_darkroom`. Falsifier: `node scripts/csym.mjs lookat` has no `S_room` case; JS `lookat` switch matches that. `node scripts/hidden-proxy.mjs verify lookat --base 70d84800~1` must not WORSE.

Verdict: **QUALITY-RISK**
