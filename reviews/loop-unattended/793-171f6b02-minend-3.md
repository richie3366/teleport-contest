# Review 793 — 171f6b02 — mkmaze.c makemaz minend-3 load_special (D-1823)

## Metadata
- Full / short hash: `171f6b027665f36406749bb4f547b169c5be9a24` / `171f6b02`
- Parent: `7c76fdb5` (D-1822). Map-driven Open. Completes Mine’s End 3/3.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 20:45:41 +0200
- D-id: **D-1823**
- Stats: `js/mklev.js` +231/−4. `js/` insertions **231** ≤250. Band **80–350**.
- Claims to close: Open `minend-3`. Not `castmu`.
- JS / map: `load_minend_3`; `load_special_proto`. `c-js-map/data.md`. Archive **Addressed:** D-1823 `171f6b02`.

## Intent vs deliverable

Git subject promises: `makemaz` had no `minend-3` loader so Mine’s End was a 33% blank-level coin flip (`rnd(3)`); C loads `dat/minend-3.lua`.

`node scripts/csym.mjs makemaz` → `mkmaze.c:1126–1223`. `lspo_level_init` `sp_lev.c:3837–3875` (solidfill `fg` → `filling`). `lspo_map` valign bottom `:6191–6237` (odd `xstart`/`ystart`). `lspo_mazewalk` `:5769–5869`. `lspo_wallify` `:5965–5989`. `sel_set_door` `:4647–4662`. `sel_set_feature` `:4633–4644`. `create_trap` `:1812–1846`. `load_special` `:6454–6502`. `--callers lspo_mazewalk`: Lua registration only (`sp_lev.c:144`).

Parent: `minend-1`/`-2` dispatched; `-3` fell through. The diff **does** add `load_minend_3` and the proto branch. Loop counts match the lua.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_minend_3` | LIVE new | lua body |
| `nhlib_shuffle` | LIVE | existing; Lua 1-based `place[1]`/`[2]` |
| `splev_map_aligned_start` | LIVE | center/bottom + odd bump |
| `splev_mazewalk` | LIVE | west, `stocked=false` |
| `l_create_stairway` | LIVE | packed upstair 42,8 |
| `wallify_map` | LIVE | lua no-arg bounds |
| `splev_create_object` / `_trap` / `_monster` | LIVE | class letters / random traps / M V Z e |
| `placeObjAt` / `placeObjRnd` / `meDoor` | CLONE | achievement / DRY / keep-SDOOR |
| `link_doors_rooms` / `map_cleanup` / `count_level_features` | OMIT named | `load_special` tail |
| `ensure_way_out` | OMIT named | lua has no `inaccessibles` so C skips it here |

`node scripts/sym.mjs`:

```
load_minend_3    NOT EXPORTED — 1 LOCAL mklev.js:8378
nhlib_shuffle    NOT EXPORTED — 1 LOCAL :19611
splev_mazewalk   NOT EXPORTED — 1 LOCAL :12149
l_create_stairway js/mklev.js:13012   sync
placeObjAt       NOT FOUND (inner)
meDoor           NOT FOUND (inner)
```

No clone→import. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**.

## C ↔ JS fidelity

**Init.** lua `solidfill` `fg="-"` → filling HWALL (`lspo_level_init` `:3862`); lit BOOL_RANDOM. `mazelevel`+`nommap`; allow_flips stays 3. JS matches. HWALL fill is the Catacombs trick: `walkfrom` only carves map `' '` STONE.

**Map.** 17×76 center/bottom. lua `:14–30` vs JS `:8395–8411` including trailing spaces on the last three rows. `lspo_map` `lit=FALSE` then lava-forced lit. **Match.**

**Shuffle / prize.** `{ {1,15},{68,6},{1,13} }`; luckstone `place[2]` + flint `place[1]`; Lua `place[3]` unused. JS `place[1]` / `place[0]`. Achievement arm clones C `create_object` `:2391–2403` (`mines_prize_oid` / `nomerge`). Level-teleports on the same two cells after seven random traps.

**Objects / mons.** 16 gem-ish (D/* /E/R/A counts), 5 `?`, 4 `+`, 3 `des.object()`, 7 `des.trap()`, 5 `M` + ettin mummy + `V` + 5 `Z` + `V` + 4 `e`. **Match those loops.**

**Mazewalk / stair / wallify.** Packed 36,8 west unstocked; `get_location_coord` then parity walk (`:5818–5866`) ≡ JS `splev_mazewalk`. Packed upstair 42,8. lua `wallify()` no-arg → `xstart-1 .. xstart+xsize+1`. Then C `load_special` wallification / `flip_level_rnd(3)` / `fixup_special`. **Match that order.**

**Doors / fountains.** Four `closed` on map `S` — keep SDOOR, `D_CLOSED` (C `:4652–4660`). Fountain skip `IS_FURNITURE` (C `:4641–4643`). `meDoor` skips `set_door_orientation` (same sibling clone as other maps).

**Callee closure.** One `load_special` variant. Init/map/mazewalk/stair/wallify/create_* LIVE or verified CLONE. Named OMITs only. No STUB in the shipped arm.

## Hallucinations / overclaim

Do **not** stamp ensure_way_out / link_doors_rooms / map_cleanup as shipped. Do **not** treat lua `math.random` as JS `Math.random`. Do **not** fill STONE instead of HWALL. Public 44/44 does not walk minend-3.

## Density

§2b: remaining Mine’s End proto. +231. Did **not** glue `castmu`. Right size.

## Verification

`verify.mjs --fn makemaz` → PASS syntax; PASS rule2; **PASS hidden (no corpus session blocked on makemaz)**; green + cohort + full 44/44. This audit: lua `:9–107` vs HEAD `:8378–8589`; `csym` `makemaz` `:1126–1223`; `lspo_mazewalk` `:5769–5869`; `load_special` `:6454–6502`. Rule #2 at end-of-iter.

## Actionable C-wrongs

None that must block the next port. Named (map): link_doors_rooms; map_cleanup; count_level_features; humidity; dry-boulder; `set_door_orientation` on the local door clone.

Verdict: **ACCEPT-WITH-DEBT**
