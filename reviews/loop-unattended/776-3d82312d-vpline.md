# Review 776 — 3d82312d — pline.c vpline msgtype_type / execplinehandler / maybe_play_sound (D-1807)

## Metadata
- Full / short hash: `3d82312db947fc761ffe241360f428143e2979b2` / `3d82312d`
- Parent: `2ac1a112` (D-1806 QR on cmdassist iflags — not this locus).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 05:06:38 +0200
- D-id: **D-1807**
- Stats: options +157; display +70/−9; hacklib +27; sounds +14; invent +8; const +6; readobjnam −18 clone. `js/` insertions **283** (>250 → ceiling **450**). Band **80–350**.
- Claims to close: Open `pline.c` `vpline` msgtype_type / execplinehandler / maybe_play_sound. Not the `pline()` wrapper.
- JS / map: `display.js` gate + `execplinehandler`; `options.js` msgtype list + `MSGTYPE=`; `hacklib.js` `str_start_is`; `invent.js` `dolook`. `c-js-map/turns.md`. Archive **Addressed:** D-1807 `3d82312d`.

## Intent vs deliverable

Git subject promises: Match C `pline.c` `vpline` so `msgtype_type`, `execplinehandler`, and `maybe_play_sound` actually run, instead of Norep short-circuiting locally and skipping MSGTYPE= hide/norep/stop.

`node scripts/csym.mjs vpline` → `pline.c:152–291` (gate `:247–278`). `execplinehandler` `:640–686`. `msgtype_type` `options.c:7796–7810`. `msgtype_add` `:7730–7754`. `msgtype_parse_add` `:7843–7866`. `hide_unhide_msgtypes` `:7813–7828`. `Norep` `:326–336`. `urgent_pline` `:311–324`. `maybe_play_sound` `sounds.c:1658–1673`. `--callers maybe_play_sound`: only `pline.c:251` under `#ifdef USER_SOUNDS`. `cnf_line_MSGTYPE` `cfgfiles.c:631–635`. `dolook` `invent.c:4318–4331`. `str_start_is` `hacklib.c:212–237`.

Parent: `Norep` compared `_prevmsg` locally; `gp.plinemsg_types` empty; execplinehandler never called. The diff **does** walk MSGTYPE, gate NOSHOW/NOREP, STOP `more()`, `execplinehandler`’s first return, and `dolook` hide_unhide. **`maybe_play_sound` is not called from `vpline`.** That matches contest C (`#ifndef USER_SOUNDS`), not the subject’s “actually run.”

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `pline` / `vpline_msgtyp_gate` | LIVE repaired | C `vpline` gate |
| `Norep` / `urgent_pline` | LIVE repaired | `PLINE_NOREPEAT` / `URGENT_MESSAGE` |
| `msgtype_type` / `msgtype_add` / `msgtype_parse_add` / `msgtype_free` | LIVE new | prepend; first regex wins |
| `hide_unhide_msgtypes` | LIVE new | `dolook` MASK_REP_SHOW |
| `execplinehandler` | LIVE local | first guard; UNIX fork **OMIT named** |
| `maybe_play_sound` | OMIT named | USER_SOUNDS compiled out; export is `void msg` |
| `str_start_is` | LIVE | clone → `hacklib.js` |
| `regex_*` | CLONE | JS RegExp vs POSIX ERE **OMIT named** |
| doset MSGTYPE menu | OMIT named | |
| SOUND= soundmap | OMIT named | |

`node scripts/sym.mjs` (clone → import / new):

```
pline            js/display.js:6037   ASYNC
Norep            js/display.js:6026   ASYNC
urgent_pline     js/display.js:6136   ASYNC
msgtype_type     js/options.js:258   sync
msgtype_add      js/options.js:224   sync
msgtype_parse_add js/options.js:285   sync
hide_unhide_msgtypes js/options.js:272   sync
execplinehandler NOT EXPORTED — 1 LOCAL display.js:5987 (C staticfn; do NOT add #2)
maybe_play_sound js/sounds.js:75   sync
str_start_is     js/hacklib.js:97   sync
dolook           js/invent.js:6068   ASYNC
```

`--can display.js options.js msgtype_type`: **ALREADY**. `--can readobjnam.js hacklib.js str_start_is`: **ALREADY**. `--can display.js sounds.js maybe_play_sound`: **SAFE** (hoisted) — they correctly did **not** add the edge. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**Norep / flags (`:326–336`, `hack.h:1375–1378`).** `gp.pline_flags = PLINE_NOREPEAT` then `vpline` then clear. `urgent_pline` sets `URGENT_MESSAGE`. Values 1/2/8 match. JS `try/finally` is the C sequence. **Match.**

**`msgtype_type` (`:7796–7810`).** Walk `gp.plinemsg_types`; first `regex_match` wins (negative types still returned). Default `norepeat ? NOREP : NORMAL`. Names table `options.c:7680–7686`: show/hide/noshow/stop/more/norep so `str_start_is` `"n"` → noshow not norep. `msgtype_add` prepends. **Match.**

**vpline gate (`:247–278`).** `OVERRIDE_MSGTYPE==0` → `msgtype_type(line, PLINE_NOREPEAT)`. Suppress when not URGENT and (NOSHOW or (NOREP && line==prevmsg)). Skip putmesg / execplinehandler / `last_msg` on suppress. Else putmesg, `execplinehandler`, `iflags.last_msg = PLNMSG_UNKNOWN`, prevmsg, MSGTYP_STOP `more()`. JS `vpline_msgtyp_gate` then `vpline_after_putmesg`. **Match the compiled path.** USER_SOUNDS `maybe_play_sound` between type and suppress is compiled out — JS does not call it.

**`execplinehandler` (`:640–649`).** `!use_pline_handler || !sysopt.msghandler` return. Contest leaves msghandler unset. UNIX fork/execv **OMIT named** (Rule #2). If handler were set, JS takes the `#else` disable-flag arm. **Match the live contest return.**

**`dolook` (`:4318–4331`).** hide TRUE MASK_REP_SHOW, `look_here(0, LOOKHERE_NOFLAGS)`, hide FALSE. **Match.**

**`str_start_is` (`:212–237`).** `--n` ASCII `lowc`; chkstr may be shorter. Deleted readobjnam `toLowerCase` clone. **Match.**

**Callee closure.** msgtype_* / hide_unhide / Norep / urgent / dolook / str_start_is LIVE. `maybe_play_sound` call site compiled out = **OMIT named**, not a stub in a live arm. POSIX ERE / SOUND= / doset menu / UNIX fork **OMIT named**.

## Hallucinations / overclaim

Do **not** stamp “Match C `maybe_play_sound` inside `vpline`.” Contest C does not call it. Do **not** stamp “Match C UNIX `execv` msghandler.” `MSGTYPE=` parse is live; doset menu is not. Public sessions with no MSGTYPE lines do not exercise hide/stop — not an overclaim of the C body.

## Density

§2b: one C `vpline` gate + the options.c table it reads + `dolook` hide + one `str_start_is` export. +283. Did **not** glue SOUND= or doset. Right size.

## Verification

D-log: save-oracle skip; msgtype probe; green + cohort 7/7. Public-unhit for hide/stop unless the recorder rc has MSGTYPE. This audit: `csym` `:247–278` / `:7796–7810` / `:640–686` vs HEAD `js/display.js:6000–6130` and `js/options.js:224–298`. Rule #2 clean.

## Actionable C-wrongs

None for Must-fix. Named: USER_SOUNDS / SOUND=; UNIX msghandler fork; doset MSGTYPE menu; full POSIX ERE vs JS RegExp; `config_error_add` on bad MSGTYPE.

Verdict: **ACCEPT-WITH-DEBT**
