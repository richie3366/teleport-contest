# Review 637 — 6441842f — iactions.c IA_BUY_OBJ shop pay (D-1676)

## Metadata
- Full / short hash: `6441842f5850663f448ca3eec285b27449f47660` / `6441842f`
- Parent: `9613be3b` (D-1675). This file audits **this SHA only** (second of nine `js/` commits since review **635**). Archive **Addressed:** D-1676 `6441842f`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-29 19:26:30 +0200
- D-id: **D-1676**
- Stats: `js/iactions.js` +29/−10; `js/shk.js` +38/−6. Total `js/` insertions **67** <250. Band **150–350**.
- Claims to close: Open IA_BUY_OBJ shop pay after D-1675. Not IA_TWOWEAPON. Not Traditional itemize. Not `cheapest_item`. `reviews/loop-2026-08-15/` has no unpaid shop-pay Must-fix.
- JS / map: `iactions.js` `'p'` row + pushkeys; `shk.js` `pay_take_canned_billed`. `c-js-map/turns.md`.
- Prior reviews this SHA claims to close: **626** / **636** named remaining buy. Not **626** Actionable #1.

## Intent vs deliverable

Git subject promises: unpaid shop items offer Pay and queue `dopay` plus invlet, instead of omitting the menu and leaking the canned letter after D-1675.

Pinned C `itemactions_pushkeys` IA_BUY_OBJ `:203–206` (`node scripts/csym.mjs itemactions_pushkeys` → `:139-274`). `--callers`: `:11` / `:707`. Menu `:485–494`. `dopay` `:1742–2035`. `--callers dopay`: `cmd.c:81`; `uhitm.c:494`. `pay_billed_items` `:2042–2167` (`--callers` `:90` / `:2006`). `menu_pick_pay_items` `:1666–1739` (`--callers` `:89` / `:2094`). `shop_keeper` `:1051–1080`. `inhishop` `:1038–1048`. `in_rooms` `:3497–3560`.

```203:206:nethack-c/upstream/src/iactions.c
    case IA_BUY_OBJ:
        cmdq_add_ec(CQ_CANNED, dopay);
        cmdq_add_key(CQ_CANNED, otmp->invlet);
        break;
```

```485:494:nethack-c/upstream/src/iactions.c
    if (otmp->unpaid
        && (mtmp = shop_keeper(*in_rooms(u.ux, u.uy, SHOPBASE))) != 0
        && inhishop(mtmp)) {
        Sprintf(buf, "Buy this unpaid %s",
                (otmp->quan > 1L) ? "stack" : "item");
        ia_addmenu(win, IA_BUY_OBJ, 'p', buf);
    }
```

C `dopay` has **no getobj**. `pay_billed_items` has **no** `cmdq_pop`. `queuedpay` is set only by `menu_pick_pay_items` (`:1734`) after `select_menu` PICK_ANY (`:1724`). `add_menu(..., 0, 0, ...)` (`:1719–1720`) so letters are sequential `a`…, **not** `obj->invlet`. `win/tty` does not `cmdq_pop`. After `dopay` returns, C `rhack` `:3810–3827` `return`s; leftover `CMDQ_KEY` is the **next** `rhack` keystroke (`:3642–3651`). There is **no** C `pay_take_canned_billed`.

Old JS: no `'p'` row; default-break; leftover invlet became the next rhack key. The diff **does** the `'p'` predicate and `cmdq_add_ec(dopay)+invlet`. It **also** invents `pay_take_canned_billed`: if the canned KEY equals a billed `obj.invlet`, pop it, set that row `queuedpay`, **skip** `menu_pick_pay_items`. That is not named Traditional itemize. It **does not** port `cheapest_item` (`:2075`) or used-up containers. Named those.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `IA_BUY_OBJ` pushkeys | C `:203–206`, **LIVE this SHA** | `dopay` + invlet |
| itemactions `'p'` | C `:485–494`, **LIVE this SHA** | `unpaid` + `shop_keeper(*in_rooms)` + `inhishop` |
| `dopay` | C `:1742–2035`, **LIVE** (not rewritten) | comment-only this SHA |
| `shop_keeper` | C `:1051–1080`, **LIVE** | string `charCodeAt(0)` ≡ C `*in_rooms` |
| `in_rooms` | C `:3497–3560`, **LIVE** | dynamic import `hack.js` |
| `inhishop` | C `:1038–1048`, **LIVE** (pre-existing clone) | JS `roomno===shoproom`; C `on_level` + `in_rooms` |
| `menu_pick_pay_items` | C `:1666–1739`, **LIVE** | skipped when canned hits |
| `pay_billed_items` | C `:2042–2167`, **LIVE + invented arm** | canned bypass |
| `pay_take_canned_billed` | **not a C callee** | local invention; `sym.mjs` “clone” is JS-only |
| `cheapest_item` | C `:2075`, **OMIT named** | `sym.mjs` NOT FOUND |
| Traditional itemize yn | C `:2099–2105`, **OMIT named** | |
| used-up / `buy_container` | C `:2123–2135`, **OMIT named** | |
| remaining pushkeys rub/swap/X | **OMIT named** | X is D-1677 |

RNG: none in `:203–206` / `:485–494` / `pay_take_canned_billed`. `dopay` helpless `rn2(2)` is pre-existing, not this SHA. No seed gate.

`node scripts/sym.mjs` on new / deleted / re-pointed names:

```
pay_take_canned_billed NOT EXPORTED — but 1 LOCAL CLONE(S) in 1 file(s):
               js/shk.js:3766
             => Do NOT write clone #2. (C has zero functions with this name.)
dopay            js/shk.js:4017   ASYNC — await required
shop_keeper      js/shk.js:219   sync
inhishop         js/shk.js:494   sync
             !! ALSO 2 LOCAL CLONE(S): js/sounds.js:106  js/teleport.js:375
in_rooms         js/hack.js:1133   sync
             !! ALSO 1 LOCAL CLONE: js/mklev.js:1203
menu_pick_pay_items NOT EXPORTED — 1 LOCAL js/shk.js:3860
pay_billed_items NOT EXPORTED — 1 LOCAL js/shk.js:3957
cheapest_item    NOT FOUND in js/** (no export, no local function/const).
```

`--can do_wear.js cmd.js` is unrelated. `iactions.js` reaches `dopay` / `shop_keeper` / `inhishop` / `in_rooms` via dynamic `import()`, not a new static edge. Do **not** stamp “cycle-forced clone.” Do **not** add `pay_take_canned_billed` #2. Do **not** add `inhishop` #4. Do **not** add `cheapest_item` as a silent stand-in.

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` / seed names / coordinates in this SHA’s `js/` hunks. `node scripts/imports.mjs --rulecheck`: Rule #2 clean (full `js/`).

## C ↔ JS fidelity

**Menu `'p'`.** C unpaid + `shop_keeper(*in_rooms(..., SHOPBASE))` + `inhishop`. JS `shop_keeper(in_rooms(...))` — `shop_keeper` takes the first char of the room-number string (`charCodeAt(0)`), which is C `*ptr`. Empty rooms → 0 → null. Buy string `stack`/`item` matches `:491–493`. **Match the predicate and the row.** Pre-existing `inhishop` skips C `on_level` / `in_rooms(shkp)` SHARED; not rewritten here.

**Pushkeys.** C `dopay` + invlet. JS `cmdq_add_ec(dopay)` (bare function, same as other public commands) + `cmdq_add_key`. **Match `:203–206`.**

**`pay_billed_items` canned.** C after no-gold (`:2060–2064`) runs `cheapest_item` (named omit) then `via_menu` (`:2084–2098`) then **always** `menu_pick_pay_items` on the menu path (`:2093–2096`), setting `queuedpay` from **menu** selection. JS: `pay_take_canned_billed` peeks KEY, matches **`obj.invlet`**, `q.shift()`, sets `queuedpay` on that Bill row, **does not** call `menu_pick_pay_items`. C menu letters are `a`+index, not invlets, so even a faithful `nhgetch`/`select_menu` would not treat canned `'l'` as “pay the unpaid `l`”. C also does **not** pop that KEY inside `dopay`; it leaks to the next `rhack`. JS invents a getobj-shaped consume that C never runs. **C-wrong.** Hallucination: D-log “consume a canned KEY matching a billed invlet as `queuedpay`” describes the JS invention, not `pay_billed_items` `:2042–2167`. Subject “instead of … leaking” frames a C leak as a JS bug to fix.

```3965:3974:js/shk.js
    const canned = pay_take_canned_billed(ibill);
    if (canned) {
        canned.queuedpay = true;
        queuedpay = true;
    } else if (!await menu_pick_pay_items(ibill)) {
        return true;
    } else {
        queuedpay = true;
    }
```

Callee closure (IA_BUY_OBJ). LIVE: `dopay` queue, `'p'` row, `shop_keeper`, `in_rooms`, `inhishop` (pre-existing). CLONE: `menu_pick_pay_items` (skipped on canned hit). STUB/invention: **`pay_take_canned_billed` inside live `pay_billed_items`**. OMIT named: `cheapest_item`, Traditional itemize, `buy_container`. Combined-arm: one non-C helper in the live pay arm. “Dispatch ported, callee stubbed” is **true** for canned `#pay`: the queue matches C; the billed walk does not.

## Hallucinations / overclaim

Subject “offer Pay and queue dopay plus invlet”: **true** for the menu and cmdq. Subject “instead of leaking the canned letter” as Match C: **false** — C `dopay` leaves the KEY for the next `rhack`. D-log “`pay_take_canned_billed`”: **not** a C function. Do **not** stamp “Match C `pay_billed_items` canned invlet.” Do **not** stamp “Match C `cheapest_item`.” Do **not** stamp “Match C Traditional itemize.” Do **not** stamp “Match C `inhishop` `on_level`.” Private canned-billed canary proves the invention, not C `select_menu`. Public-unhit (no itemactions `'p'` in sessions).

## Density

+67: one IA_BUY_OBJ envelope (row + queue + billed consume). §2b cluster. Did not glue two-weapon / itemize. The extra helper is the quality miss, not an unrelated subsystem.

## Verification

Wired: `'p'` when unpaid in a tended shop; `dopay`+invlet; canned invlet skipped out of the pay menu. Unwired C: `menu_pick_pay_items` on that canned path; leftover KEY as next command; `cheapest_item`; itemize yn; `buy_container`. Conf: no extra `rn2` in the new helper. No seed gate.

Journal: private canary (menu predicate + canned billed pay); green+strict seed8000/0900; cohort **9**/9 + strict. Cadence **#2090** at HEAD: **44**/44. Fortress does not hit shop itemactions `'p'`.

## Actionable C-wrongs

1. `pay_billed_items` (`shk.c:2042–2097`): delete `pay_take_canned_billed`. After IA_BUY_OBJ, take the C `via_menu` arm into `menu_pick_pay_items`; do not skip that menu because a canned KEY equals `obj.invlet`. Leftover `CMDQ_KEY` after `dopay` is C `rhack` `:3642–3651`. Do not port `cheapest_item` / Traditional itemize / `buy_container` in that iter unless that is the Open row.

Named (map, not Must-fix): Traditional itemize yn; `cheapest_item` early return; used-up containers; remaining pushkeys rub/swap/whatis; `inhishop` `on_level` / `in_rooms(shkp)`. Do **not** add `pay_take_canned_billed` #2. Do **not** add `inhishop` #4. Do **not** add `cheapest_item` as a no-op. Do **not** re-port `dopay` resident/getpos. Do **not** re-port D-1675 unwield/eat.

Verdict: **QUALITY-RISK**
