# Review 1423 — 4238093f — verbalize whole-body port (D-2464)

Metadata: SHA `4238093f`, `js/display.js` only (verbalize restart +
one const import). Coverage gap (0 blocked), not a corpus fix.
D-log: D-2464.

## Intent vs deliverable

Promise: C-order verbalize — `PLINE_VERBALIZE` set, quote-then-format,
`vpline` path, flag cleared. Diff ships exactly that. No second
subsystem.

## Inventory

- Restarted: `verbalize(line, ...args)` (exported, correct — C is
  global). `PLINE_VERBALIZE` joins the existing const.js edge.
- No new helpers, no deleted/re-pointed clones for `sym.mjs`.

## C ↔ JS fidelity

C `pline.c:475–490` vs JS:

- `gp.pline_flags |= PLINE_VERBALIZE` before, `&= ~PLINE_VERBALIZE`
  after — exact, including clearing only that bit (unlike Norep's
  full reset at `js/display.js:7514`). `gp` is the live gstate import
  (`:120`); try/finally is the correct async adaptation (C cannot
  interleave; JS `pline` awaits).
- Quote-then-format: `"..."` wrap first, then `%s/%d/%ld/%%` per the
  file's livelog_printf/impossible convention — matches C's
  Strcpy/Strcat-then-`vpline(tmp, args)` order (format applies to the
  quoted buffer, as in C's `vsprintf`).
- `You_buf` shared-buffer growth named unneeded — correct (immutable
  strings; the buffer is unobservable).
- Routing: `await pline(tmp)` is the same call the old body made, so no
  path change; the jsdoc's vpline-split claim matches the Open
  coverage row's `split?` note.
- Format reachable today by nobody: grep confirms zero multi-arg
  callers (all ~60 pass one pre-formatted string), so the no-args path
  is byte-identical to the old body and the `%`-handling of exotic
  specifiers is unobservable. No RNG either side.

Named omits (all verified in kind): `md_rush` (mail daemon — no JS
counterpart on the scored tree); `eat.c:2588` (inside `fpostfx`, its
own Open row — shipped next as D-2465); lua binding (no JS lua by
design); `verbalize("")` no-op guard is pre-existing, and the SND path
that would observe the flag (`SoundSpeak`) is a documented no-op
without SND_LIB — so the flag write is currently behavior-free but
C-faithful.

## Hallucinations / overclaim

None. The D-log does not claim a corpus fix or a reader for the flag.

## Density

One 16-line C function, one module: minimal but complete (C is that
small — the density floor explicitly allows this).

## Verification

- `hidden-proxy verify verbalize --base 4238093f~1 --reach-all`
  (re-run): 0 blocked both sides (vacuous, as stated); smoke 24/24
  PASS, 0 regressed → REACH-OK. Matches (plus full 44/44 in the D-log
  for the shared-file change).
- Diff grep: no FORCE/DIAG/seed/coordinate logic.

## Actionable C-wrongs

None. Whole body ports C in order.

Verdict: **ACCEPT**
