# Review 1725 — 40908907f — wiz_smell whole-body port (D-2766)

- SHA: `40908907f` (`wizcmds.c` wiz_smell + `#wizsmell` EXT_CMDS row, D-2766)
- Files: `js/wizcmds.js` (+73/−4), `js/getline.js` (+10), docs
- Queue row: Open (coverage MISSING), 0 corpus blocks cited
- Banned grep: 0 hits. `imports.mjs --rulecheck`: "Rule #2 clean".

## Intent vs deliverable

Subject promises the #wizsmell cursor-pick sniff loop with the extended
command wired. Diff adds `wiz_smell` in C order plus one runnable
`EXT_CMDS` row (the autocomplete list row at `getline.js:355` already
existed). Promise kept.

## Inventory

| JS symbol | Class | C / sym.mjs |
|-----------|-------|-------------|
| `wiz_smell` (export, async) | C body | `wizcmds.c:884–939` (csym) |
| `olfaction` | LIVE | `js/monsters.js:709 sync` |
| `getpos` | LIVE | `js/getpos.js:1254 ASYNC` |
| `usmellmon` | LIVE | `js/makemon.js:1573 ASYNC` |
| `u_at` | LIVE | `js/const.js:3189 sync` (2 clones elsewhere, not used here) |
| `glyph_at` / `glyph_is_monster` / `glyph_is_invisible_id` | LIVE | `display.js:823 / :796 / :800` |
| `map_invisible` / `unmap_invisible` | LIVE | `display.js:1335 / :1441` |
| `body_part`, `You` | LIVE | `polyself.js:593 sync`, `display.js:7632 ASYNC` |

Nothing deleted or re-pointed.

## C ↔ JS fidelity

Walked `:893–938`: cursor starts on the hero ✓; `!olfaction` → You
message + ECMD_OK ✓; the one-time cursor message before the loop ✓;
loop body: prompt pline, `getpos(&cc, TRUE, "a monster")` — JS writes
`ccp.x = -1` on abort (`getpos.js:1355/1437/1600`), so `ans < 0 ||
cc.x < 0` → ECMD_CANCEL ✓; `is_you = FALSE` reset each pass ✓; the
`u_at` / steed / youmonst / `m_at` else-if chain, with `m_at` evaluated
only when not on the hero, as in C ✓; `glyph_at` read before the test
✓. The `mptr` arm: self sniff under ARM, `!usmellmon` → "You seem" /
"That monster seems" ✓, `!glyph_is_monster` → `map_invisible` ✓. The
empty arm: message, then `glyph_is_invisible(glyph)` on an int glyph —
the JS `glyph_is_invisible` takes a location, so using
`glyph_is_invisible_id` (int `=== GLYPH_INVISIBLE`) is the correct
C mapping ✓. `glyph_at` returns `disp_glyph` as an int, so both glyph
predicates receive the shape they expect. No RNG in the body (usmellmon
is message-only).

Caller: `cmd.c:1994–1995` `wizsmell` `IFBURIED | AUTOCOMPLETE |
WIZMODECMD`; the new row uses `wiz: true, autocomplete: true`, the same
shape as the neighbouring wiz rows. `csym --callers wiz_smell` finds 0
direct calls (function-pointer table only), so the EXT_CMDS row is the
only caller to wire ✓.

## Hallucinations / overclaim

None. "Named: none" is accurate — every callee resolves to a live
export.

## Density

~83 JS lines for a 56-line C body and its sole caller: right-sized.

## Verification

Re-ran `node scripts/hidden-proxy.mjs verify wiz_smell --base
40908907f~1 --reach-all`:
- `0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)`
- `smoke … 24 run, 3.0s: 24 PASS, 0 regressed → REACH-OK`

Matches the D-log.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
