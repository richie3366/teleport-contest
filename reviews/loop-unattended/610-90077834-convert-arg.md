# Review 610 — 90077834 — questpgr.c convert_arg remaining % codes (D-1649)

## Metadata
- Full / short hash: `900778344099f3575354dbfff503c8e06084046a` / `90077834`
- Parent: `979dd522` (D-1648). This file audits **this SHA only** (second of nine `js/` commits since review **608**). Archive **Addressed:** D-1649 `90077834`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-29 12:40:54 +0200
- D-id: **D-1649**
- Stats: `js/questpgr.js` +106/−58. Band **150–350** (`js/` insertions **106** <250; id >454).
- Claims to close: Open `convert_arg` after D-1634. Not convert_line `%Xh` (D-1634). Not qt_pager common. `reviews/loop-2026-08-15/` has no unpaid convert_arg Must-fix.
- JS / map: `questpgr.js` `convert_arg` / `homebase` / `intermed` / `neminame`. `c-js-map/turns.md`.
- Prior reviews this SHA claims to close: **595** named convert_arg catalogue.

## Intent vs deliverable

Git subject promises: `%c`/`%G`/`%A`/`%D`/`%C`/`%N`/`%L`/`%Z` and artiname `%o` fill the pager buffer, instead of defaulting those codes to empty after D-1634.

Pinned C `questpgr.c` `convert_arg` `:235–325` (`node scripts/csym.mjs convert_arg`). `--callers convert_arg`: `questpgr.c:343` only (`convert_line`). Callees `homebase` `:141–145` (`--callers` `:288`); `intermed` `:60–64` (`:264`); `neminame` `:123–131` (`:279`); `artiname` `artifact.c:150–156` (`questpgr.c:268`); `align_gtitle` `pray.c:2627–2649` (`:285`); `align_str` `insight.c:3186–3200` (`:291/:294`); `align_gname` (`:297/:300`).

```244:325:nethack-c/upstream/src/questpgr.c
    case 'c':
        str = (flags.female && gu.urole.name.f) ? gu.urole.name.f
                                               : gu.urole.name.m;
        break;
    ...
    case 'G':
        str = align_gtitle(u.ualignbase[A_ORIGINAL]);
        break;
    case 'a':
        str = align_str(u.ualignbase[A_ORIGINAL]);
        break;
    case 'A':
        str = align_str(u.ualign.type);
        break;
    case 'D':
        str = align_gname(A_LAWFUL);
        break;
    case 'Z':
        str = svd.dungeons[0].dname;
        break;
```

Old JS: those cases fell through `default` → `''`; `%o` read `artilistRaw` not `artiname`; `%a` inlined three strings; `neminame`/`homebase`/`intermed` inlined. The diff **does** C switch order into a `str` then return (C `Strcpy(gc.cvt_buf)`), the missing cases, C-home helpers, `artiname`+`the`+`strstri` for `%O`. It **does not** port qt_pager common fallback, array `rn2`, pauper_legacy, or `align_str` `A_NONE` `"unaligned"`. Named.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `convert_arg` | C `:235–325`, **LIVE this SHA** | local static analogue; fills return string |
| `homebase` | C `:141–145`, **CLONE** (C-home local) | `game.urole.homebase` |
| `intermed` | C `:60–64`, **CLONE** | |
| `neminame` | C `:123–131`, **CLONE** | was inline; `M2_PNAME` ≡ `type_is_pname` |
| `guardname` / `ldrname` | C same file, **LIVE** | not re-ported |
| `artiname` | C artifact.c `:150–156`, **LIVE** | re-point from `artilistRaw` |
| `the` | C objnam, **LIVE** | lock.js clone #2 — **do not add #3** |
| `strstri` | C hacklib, **LIVE** | attrib/write clones — **do not add #3** |
| `align_gtitle` | C pray.c `:2627–2649`, **LIVE** | JS extra `urole` arg |
| `align_gname` | C pray.c, **LIVE** | `%d` original, `%D` `A_LAWFUL` |
| `align_str` | C insight.c `:3186–3200`, **LIVE** | A_NONE `"unaligned"` **OMIT named**; quest.js clone #2 — **do not add #3** |
| `rank_of` | C, **LIVE** | `%r`/`%R` |
| qt_pager common / array rn2 | C com_pager, **OMIT named** | |

`node scripts/csym.mjs convert_arg` → `questpgr.c:235-325`. `homebase` → `:141-145`. `intermed` → `:60-64`. `neminame` → `:123-131`. `artiname` → `artifact.c:150-156`. `align_str` → `insight.c:3186-3200`. `align_gtitle` → `pray.c:2627-2649`. `--callers convert_arg`: `:343`. `--callers homebase`: `:288`. `--callers neminame`: `:279`. `--callers artiname`: includes `questpgr.c:268`. `--callers align_str`: includes `:291/:294`. `--callers align_gtitle`: includes `:285`. `--callers intermed`: `:264`.

RNG: none in `convert_arg`. No seed gate.

`node scripts/sym.mjs` on new / re-pointed names:

```
convert_arg      NOT EXPORTED — 1 LOCAL js/questpgr.js:500
homebase         NOT EXPORTED — 1 LOCAL js/questpgr.js:477
intermed         NOT EXPORTED — 1 LOCAL js/questpgr.js:482
neminame         NOT EXPORTED — 1 LOCAL js/questpgr.js:458
artiname         js/artifact.js:325   sync
align_str        js/roles.js:737   sync
             !! ALSO 1 LOCAL CLONE(S) in 1 files — IMPORT the export; do NOT add another
               js/quest.js:172
align_gtitle     js/roles.js:755   sync
align_gname      js/roles.js:744   sync
the              js/objnam.js:1314   sync
             !! ALSO 1 LOCAL CLONE(S) in 1 files — js/lock.js:879
strstri          js/hacklib.js:217   sync
             !! ALSO 2 LOCAL CLONE(S) — js/attrib.js:305  js/write.js:90
```

`--can questpgr.js artifact.js artiname`: ALREADY. `--can questpgr.js roles.js align_str`: ALREADY. `--can questpgr.js objnam.js the`: ALREADY. `--can questpgr.js hacklib.js strstri`: ALREADY. Do **not** stamp “cycle-forced clone.” Do **not** add `align_str` #3 in questpgr. Do **not** restore `artilistRaw` for `%o`.

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` / seed names / coordinates in this SHA’s `js/` hunks. `node scripts/imports.mjs --rulecheck`: Rule #2 clean.

## C ↔ JS fidelity

Switch. C cases in order `p c r R s S l i O/o n g G H a A d D C N L x Z % default`, then `Strcpy(gc.cvt_buf, str)`. JS the same cases into `str` then `return str` (`convert_line` already concatenates). **Match `:240–325`.**

`%c`. Female and `name.f` else `name.m`. JS `urole.name?.f` / `.m`. **Match `:244–247`.**

`%G`. C `align_gtitle(u.ualignbase[A_ORIGINAL])` → `"god"`/`"goddess"` from leading `_`. JS `align_gtitle(urole, aOrig)` with `aOrig = ualignbase.original` (JS object, not `A_ORIGINAL` index). **Match the original-align title.**

`%a`/`%A`. C `align_str` original vs `u.ualign.type`. JS `align_str(aOrig)` / `align_str(u.ualign?.type)`. Live `roles.js` returns lawful/chaotic else **neutral** — C also has `A_NONE` `"unaligned"` and default `"unknown"`. Quest `%a`/`%A` are lawful/neutral/chaotic in shipped lua. **Match the three alignments.** A_NONE is **named**, not a silent stub in these arms.

`%D`. C `align_gname(A_LAWFUL)` (role’s lawful god, not current align). JS `align_gname(urole, A_LAWFUL)`. **Match `:300`.** `%d` remains original-align `align_gname`. **Match.**

`%C`/`%N`/`%L`. Literals chaotic/neutral/lawful. **Match.** `%Z` `game.dungeons[0].dname` — dungeon init writes `game.dungeons[dngidx]` with 0 = C `svd.dungeons[0]`. **Match `:316–317`.**

`%o`/`%O`. C `the(artiname(questarti))`; `%O` `strstri(str, " of ")` then `*p=0`. JS `artiname` (empty if `<=0` or `>NROFARTIFACTS`, **Match `artifact.c:150–156`**) then `the`; `strstri` returns the tail; `slice(0, str.length - p.length)` is `*p=0`. **Match `:267–276`.** Old `artilistRaw` skipped the bounds empty-string. This SHA **fixes** that.

`neminame`. C `type_is_pname` then optional `"the "` + `pmnames[NEUTRAL]`. JS `M2_PNAME` (`mondata.h` `:135`) and `names[2]` (NEUTRAL). Extra `NON_PM` → `''` vs C indexing `mons[neminum]` (always set). Analogue. **Match `:123–131`.**

Callee closure (full switch). LIVE: `ldrname`, `guardname`, `rank_of`, `artiname`, `the`, `strstri`, `align_gtitle`, `align_gname`, `align_str`. CLONE: `homebase`/`intermed`/`neminame` (C statics, matched here). OMIT named: qt_pager common, array rn2, `align_str` A_NONE. STUB: **none**. Combined-arm ships. Not “dispatch ported, callee stubbed.”

## Hallucinations / overclaim

Subject catalogue + artiname `%o`: **true.** D-log C switch order / `%O` strstri: **true.** Do **not** stamp “Match C `qt_pager` common fallback.” Do **not** stamp “Match C `align_str` A_NONE unaligned.” Do **not** stamp “Match C convert_line `%Xh`” (D-1634). Public `%c`/`%Z` are **role-hit** on live quest lua; other-role bodies remain embed-unhit.

## Density

+106: C `convert_arg` 91 + three tiny statics + `%o` re-point. §2b one `convert_arg` family after D-1634. Did not glue qt_pager. Above a one-`if` peel.

## Verification

Wired: every C case letter; `%O` of-truncation; `artiname` empty. Unwired C: common fallback; A_NONE. Conf: no `rn2`. No seed gate.

D-log green+strict seed8000/0900; cohort **7**/7 + strict. **Public-unhit** for `%Z`/`%c` unless a session’s quest text uses those codes. Fortress does not prove `%D` lawful-god.

## Actionable C-wrongs

None for Must-fix. Named (map, not Must-fix): qt_pager common fallback / array `rn2` / pauper_legacy; `align_str` A_NONE `"unaligned"` (roles.js, not a second convert_arg). Do **not** add `align_str` #3. Do **not** add `strstri` #3. Do **not** restore `artilistRaw` for `%o`. Do **not** re-port convert_line `%Xh` (D-1634). Do **not** re-port `com_pager_core` (D-1622).

Verdict: **ACCEPT-WITH-DEBT**
