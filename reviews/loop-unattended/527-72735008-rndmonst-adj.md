# Review 527 — 72735008 — makemon.c rndmonst_adj rogue/elem filters (D-1566)

## Metadata
- Full / short hash: `72735008bb3123ecc69d9499332ae3eb9c240343` / `72735008`
- Parent: `224bd3a6` (D-1565). This file audits **this SHA only** (ninth of nine `js/` commits since review **518**). Archive **Addressed:** D-1566 was missing `%h` at review time (fill `72735008`).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-28 06:16:13 +0200
- D-id: **D-1566**
- Stats: `js/makemon.js` +59 / −3. Band 150–350 (js/ insertions **59**).
- Claims to close: Open rogue/elem `rndmonst_adj` after D-0532 / D-0747 / D-1565. Not mkclass. `reviews/loop-2026-08-15/` has no unpaid rndmonst Must-fix.
- JS / map: `makemon.js` `rndmonst_adj`; `c-js-map/data.md`.
- Prior reviews this SHA claims to close: **526** named `rndmonst_adj` rogue/elem.

## Intent vs deliverable

Git subject promises: rogue levels skip a–z `monsym` classes; elemental planes reject off-element types via `wrong_elem_type`.

Pinned C `makemon.c` `rndmonst_adj` `:1658–1732`. Callers: `rndmonst` `:1654`; dog.c `:130`; mkobj.c `:402`. Filters `:1673–1686` after difficulty, before `uncommon` / Inhell. `wrong_elem_type` `:55–75` (static). `is_home_elemental` `:32–50`.

```1673:1691:nethack-c/upstream/src/makemon.c
    upper = Is_rogue_level(&u.uz);
    elemlevel = In_endgame(&u.uz) && !Is_astralevel(&u.uz);
    ...
        if (montooweak(mndx, minmlev) || montoostrong(mndx, maxmlev))
            continue;
        if (upper && !isupper(monsym(ptr)))
            continue;
        if (elemlevel && wrong_elem_type(ptr))
            continue;
        if (uncommon(mndx))
            continue;
        if (Inhell && (ptr->geno & G_NOHELL))
            continue;
```

```55:73:nethack-c/upstream/src/makemon.c
    if (ptr->mlet == S_ELEMENTAL) {
        return (boolean) !is_home_elemental(ptr);
    } else if (Is_earthlevel(&u.uz)) {
        /* no restrictions? */
    } else if (Is_waterlevel(&u.uz)) {
        if (!is_swimmer(ptr))
            return TRUE;
    } else if (Is_firelevel(&u.uz)) {
        if (!pm_resistance(ptr, MR_FIRE))
            return TRUE;
    } else if (Is_airlevel(&u.uz)) {
        if (!(is_flyer(ptr) && ptr->mlet != S_TRAPPER) && !is_floater(ptr)
            && !amorphous(ptr) && !noncorporeal(ptr) && !is_whirly(ptr))
            return TRUE;
```

Old JS: comment deferred upper/elemlevel; Inhell hellish live; `monsym_isupper` already used by `select_newcham_form`.

The diff **does** set `upper`/`elemlevel`, `continue` those two gates in C order, and add C-home `wrong_elem_type` + `is_home_elemental`. It **does not** wire `ndemon`/aligned `mkclass`, `newmonhp` ×3 (`:1043`), or `grow_up` `:2087`. Named. Reservoir `rn2(totalweight)` unchanged.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `rndmonst_adj` upper/elem | C `:1673–1686`, **LIVE this SHA** | after difficulty, before uncommon/Inhell |
| `monsym_isupper` | C `isupper(monsym(ptr))`, **CLONE** (pre-existing) | A–Z mlets; not a new body |
| `wrong_elem_type` | C `:55–75` static, **LIVE this SHA** | C home; do not add #2 |
| `is_home_elemental` | C `:32–50`, **LIVE this SHA** at C home | mon.js `:1283` / teleport.js `:2707` keep cycle clones; do **not** add #4 |
| `Is_rogue_level` / `In_endgame` / `Is_astralevel` | C, **LIVE** | const.js |
| `is_swimmer` / `is_flyer` / `is_floater` / `is_whirly` / `pm_resistance` | C, **LIVE** | imported |
| Inhell `G_NOHELL` | **LIVE unchanged** | D-0747 |
| `qt_montype` | **LIVE unchanged** | D-0532 |
| `ndemon` / `mkclass` | **OMIT named** | |
| `newmonhp` / `grow_up` `is_home_elemental` | **OMIT named** | comment still at newmonhp |

`node scripts/csym.mjs rndmonst_adj` → `makemon.c:1658-1732`. `--callers`: dog `:130`; makemon `:1654`; mkobj `:402`. `wrong_elem_type` → `:55-75` (only `rndmonst_adj:1686`). `is_home_elemental` → `:32-50` (wrong_elem `:59`; newmonhp `:1043`; grow_up `:2087`; mhitu `:437`; mon.c `:3906`; polyself `:869`; teleport `:2035`).

RNG: this SHA adds **no** extra `rn2`/`rnd`. Skipped mndx never enter the reservoir, so later `rn2(totalweight)` sees a smaller pool — **C’s intended consumption**, not a new call.

`node scripts/sym.mjs` on new / re-pointed names:

```
is_home_elemental NOT EXPORTED — but 3 LOCAL CLONE(S) in 3 file(s):
               js/makemon.js:385  js/mon.js:1283  js/teleport.js:2707
             => Do NOT write clone #4.
wrong_elem_type  NOT EXPORTED — but 1 LOCAL CLONE(S) in 1 file(s):
               js/makemon.js:407
monsym_isupper   NOT EXPORTED — but 1 LOCAL CLONE(S) in 1 file(s):
               js/makemon.js:973
rndmonst_adj     js/makemon.js:445   sync
qt_montype       js/makemon.js:427   sync
Is_rogue_level   js/const.js:3024   sync
Is_astralevel    js/const.js:2994   sync
```

C `wrong_elem_type` is static — one local is the home. C `is_home_elemental` is exported; JS C-home is this new makemon local (cycle clones stay). `node scripts/imports.mjs --can`: no new module edge (locals + existing const/mondata imports). `is_flyer`/`is_swimmer`/`is_whirly` already imported from the same edge.

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` / seed names / recorded coordinates in this SHA’s `js/`. `node scripts/imports.mjs --rulecheck`: Rule #2 clean.

## C ↔ JS fidelity

Loop order. too-weak/strong → upper → elem → uncommon → Inhell → weight/`rn2`. **Match `:1682–1691`.** Valley is neither rogue nor elemlevel. **Match.**

Rogue. `Is_rogue_level` then `!monsym_isupper` continue. **Match `:1684–1685`** given the existing A–Z mlet clone (ants/jackals skip; angels/bats stay). Not a new `monsym()` char table.

Elemlevel. `In_endgame && !Is_astralevel`. Astral does **not** filter. **Match `:1674`.**

`wrong_elem_type`. S_ELEMENTAL → `!is_home_elemental` (off-plane elementals rejected). Earth: empty arm, not wrong. Water: `!is_swimmer`. Fire: `!pm_resistance(MR_FIRE)`. Air: wrong unless (flyer and not trapper) or floater or amorphous or noncorporeal or whirly. **Match `:57–73` including C’s earth comment.**

`is_home_elemental`. `mlet === S_ELEMENTAL` then mndx air/fire/earth/water vs `Is_*level`. **Match `:32–50`.** JS `pm('AIR_ELEMENTAL')` ≡ C `PM_AIR_ELEMENTAL`.

Callee closure (`rndmonst_adj` filter arm). LIVE: `Is_rogue_level`, `In_endgame`, `Is_astralevel`, `monsym_isupper`, `wrong_elem_type`, `is_home_elemental`, `is_swimmer`/`is_flyer`/`is_floater`/`is_whirly`/`amorphous`/`noncorporeal`/`pm_resistance`, Inhell. CLONE: `monsym_isupper` (pre-existing); `is_home_elemental` vs mon/teleport cycle copies. OMIT named: `ndemon`/`mkclass`; newmonhp ×3; grow_up. STUB: **none**. Arm may ship. Not “dispatch ported, callee stubbed.”

## Hallucinations / overclaim

Subject rogue uppercase + plane `wrong_elem_type`: **true**. D-log “not mkclass”: **true**. Do **not** stamp “Match C `newmonhp` `is_home_elemental` ×3.” Do **not** stamp “Match C `monsym()` exported.” Do **not** stamp “Match C `ndemon`.” This is **not** “dispatch ported, callee stubbed.”

## Density

One C loop’s two remaining continues + the static callee they need. +59 JS. Did not glue mkclass/newmonhp. §2b OK.

## Branch-by-branch confirm

1. Ordinary Dungeon: `upper`/`elemlevel` false; pool unchanged. **Match.**
2. Rogue + ant (`a`): skip. **Match.**
3. Rogue + angel (`A`): keep (then uncommon/Inhell/weight). **Match.**
4. Astral: `elemlevel` false. **Match.**
5. Fire plane + non-MR_FIRE non-elemental: skip. **Match.**
6. Fire plane + fire elemental: home, not wrong. **Match.**
7. Fire plane + water elemental: S_ELEMENTAL !home → skip. **Match.**
8. Water plane + non-swimmer: skip. **Match.**
9. Earth plane + dwarf: not wrong. **Match.**
10. Air + trapper flyer: `mlet === S_TRAPPER` fails the flyer conjunct → may still keep if floater/amorphous/…. **Match C’s `&& mlet != S_TRAPPER`.**

## Callers / RNG ledger

C: `rndmonst(0,0)`; dog `max`; mkobj statue/figurine adj. Cohort includes seed0013-rogue. Filter skips change which mndx enter `rn2(totalweight)` — same call sites, smaller pool. No seed gate.

## Anti-pattern / Rule #2 (this SHA `js/`)

Plain ESM. No FORCE. Locals at C home; cycle clones left in mon/teleport. Not a cycle dodge.

## Verification

D-log canary **21**/21 (C/JS locus + order; ordinary lowercase; rogue uppercase-only; fire/water/earth/air; astral not elemlevel; Rule #2); green+strict seed8000/0900; cohort **7**/7 + seed0013-rogue + strict. Rogue session is in the public PASS list — **not** public-unhit for the rogue gate. Plane filters remain **public-unhit**. Admit the plane half.

## Actionable C-wrongs

None for Must-fix. Named: `ndemon`/aligned `mkclass`; `newmonhp` `:1043` ×3; `grow_up` `:2087`; `'r'` reversed. Do not add `wrong_elem_type` #2 or `is_home_elemental` #4. Do not import makemon’s new local into mon.js (cycle).

Verdict: **ACCEPT-WITH-DEBT**
