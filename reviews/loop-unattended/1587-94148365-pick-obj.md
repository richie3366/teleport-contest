# Review 1587 — 94148365 — pickup.c pick_obj whole-body restart (D-2628)

**Metadata:** SHA `94148365`, `pickup.c` `pick_obj`, D-2628.
JS: `js/pickup.js` only (+35/−12).
Coverage row (PARTIAL → live). No prior review claimed closed.

## Intent vs deliverable

Subject promises: whole-body restart fixing the engulfer-minvent path
(`get_obj_location` never called, stale floor coords), the invented
`if (!otmp) return` guard (C is NONNULLARG1), and fromfloor/ushops
comparisons without C integer/NUL shape. Diff delivers all three plus
the `in_rooms(...).charAt(0)` null-guard. Promise matches deliverable.

## Inventory

- `pick_obj()` — restarted export, C order with per-arm cites.
- `get_obj_location` joined to the existing timeout.js import (no new
  edge — `imports.mjs --can`: ALREADY).
- No deleted symbol, no local→import re-point (nothing to paste from
  `sym.mjs` on that axis).

## C ↔ JS fidelity

C locus `pickup.c:1896–1942` (47 L, via `csym.mjs pick_obj`; caller
`:1879` `pickup_object`; `shk.c:3008` is a comment; decl
`extern.h:2451`). Full C body read here. Arm-by-arm confirm:

- `:1900` fromfloor sampled before extract mutates `where`, with `|0`
  int compare — exact (old code compared without int shape).
- `:1901–1905` `(void) get_obj_location(otmp, &ox, &oy, 0)` via the
  live timeout.js export; `null → 0,0` matches C's `*xp = *yp = 0;
  return FALSE`. Export body read (`js/timeout.js:1514`): INVENT →
  hero pos, FLOOR → ox/oy, MINVENT → carrier mx/my, else null —
  matches C's invent/floor/minvent + flags arms for locflags 0.
- `:1907` robshop gate drops both invented guards (`!otmp`, `u &&`) —
  exact; `u.uball` is the C global `uball` (module precedent).
- `:1908–1910` extract + newsym-when-fromfloor — exact.
- Shop arm `:1924–1934`: fake-ushops round-trip, restore, recompute
  off *restored* ushops. `''` models `'\0'` correctly:
  `strchr(s,'\0')` is always non-null so C reads false, and `''` is
  falsy so JS reads false — exact on both the empty and non-empty
  paths. `(in_rooms(...)||'').charAt(0)` fixes a null crash the old
  code had.
- `:1937–1940` addinv then remote_burglary-when-robbed-outside —
  exact. Async awaits are engine-only (sync in C).

Caller wiring: C `:1879` → JS `pickup.js:1497`
(`obj = await pick_obj(obj)`, already wired, untouched) — exact.
`shk.c:3008` correctly not wired (comment).

Callee closure: all LIVE — `get_obj_location` (timeout.js:1514
sync), `costly_spot`/`addtobill`/`remote_burglary` (shk.js),
`obj_extract_self` (mkobj.js), `newsym` (display.js), `in_rooms`
(hack.js), `addinv` (u_init.js). `sym.mjs get_obj_location`:

```text
get_obj_location js/timeout.js:1514   sync
             !! ALSO 1 LOCAL CLONE(S) in 1 files — IMPORT the export; do NOT add another
               js/shk.js:781
```

The shk.js:781 clone is pre-existing drift, untouched by this
commit — disclosed in the D-log body, not this commit's debt
(own row if the coverage tool emits it).

## Hallucinations / overclaim

None. D-log reports the vacuous hidden verify plainly ("no corpus
session is blocked — coverage row, no cited blocks") and does not
claim the shk.js clone as resolved.

## Density

47-line C function, one module, +35/−12. Right-sized
breadth-phase handoff.

## Verification

- `node scripts/imports.mjs --rulecheck` → Rule #2 clean (whole `js/`).
- Diff grep: 0 `FORCE`/`DIAG`/`getRngLog`/`fastforward`/seed names/
  hardcoded coordinates in control flow.
- Re-measured: `hidden-proxy.mjs verify pick_obj --base
  94148365~1 --reach-all` → `0 session(s) blocked` (vacuous-note
  path, honestly labeled in the D-log) + `fixed smoke spread (24
  run): 24 PASS, 0 regressed → REACH-OK`. Both summary lines cited;
  no REGRESSED session.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
