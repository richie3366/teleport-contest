# Review 797 — e782f134 — mkmaze.c makemaz water + save_waterlevel (D-1827)

## Metadata
- Full / short hash: `e782f1343194d5c0c9c57b7748e07e828c135b92` / `e782f134`
- Parent: `159fda3d` (D-1826). Map-driven Open. Endgame plane 4 of 5.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 22:10:52 +0200
- D-id: **D-1827**
- Stats: `js/mklev.js` +284/−27; `js/lev_json.js` +37/−2; `js/do.js` +9; `js/save.js` +3/−1. `js/` insertions **333** >250. Band **80–450**.
- Claims to close: Open `water` + `save_waterlevel`/`restore_waterlevel`. Not astral.
- JS / map: `load_water`; JSON bubble persist. `c-js-map/data.md`. Archive **Addressed:** D-1827 `e782f134`.

## Intent vs deliverable

Git subject promises: no `water` loader (blank `create_maze`); C loads `dat/water.lua`; leaving the plane lost `bbubbles`.

`node scripts/csym.mjs makemaz` → `mkmaze.c:1126–1223`. `save_waterlevel` `:1722–1745`; `--callers`: `save.c:612`. `restore_waterlevel` `:1749–1798`; `--callers`: `restore.c:1383`. `unsetup_waterlevel` `:1859–1870` (from save `release_data`). `set_wportal` `:1800–1809`; `--callers`: `mkmaze.c:1555` (`movebubbles`). `setup_waterlevel` `:1811–1857`. `fixup_special` `:569–584` runs `setup_waterlevel` **before** `place_lregion`. `lspo_levregion` `:5471–5494`.

The diff **does** add `load_water`, `l_levregion`, JSON `save_waterlevel`/`restore_waterlevel`/`unsetup_waterlevel`, `set_wportal` from `movebubbles`, `goto_level` persist+free, `serLevel`/`deserLevel` blob.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_water` | LIVE new | lua body |
| `l_levregion` | LIVE | `lspo_levregion` |
| `setup_waterlevel` / `mk_bubble` | LIVE | pre-existing; water skip `rn2` |
| `save_waterlevel` | LIVE analogue | JSON of n/xmin..ymax/`Sfo_bubble` |
| `restore_waterlevel` | LIVE analogue | rebuild + `mv_bubble(ini)` |
| `unsetup_waterlevel` | LIVE | also drops `wportal` |
| `set_wportal` | LIVE | first `MAGIC_PORTAL`; `impossible` OMIT |
| cons pickup / `maybe_adjust_hero_bubble` | OMIT named | |
| empty-restore `impossible` | OMIT named | |

`node scripts/sym.mjs`:

```
load_water       NOT EXPORTED — 1 LOCAL mklev.js:9323
save_waterlevel  js/mklev.js:11910   sync
restore_waterlevel js/mklev.js:11949   sync
unsetup_waterlevel js/mklev.js:11937   sync
set_wportal      js/mklev.js:11885   sync
l_levregion      js/mklev.js:797   sync
setup_waterlevel NOT EXPORTED — 1 LOCAL :11669
movebubbles      js/mklev.js:11779   sync
```

`imports.mjs --can do.js mklev.js save_waterlevel` / `save.js mklev.js restore_waterlevel`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`: **none**. Rule #2: clean.

## C ↔ JS fidelity

**Lua counts (`dat/water.lua`).** Flags mazelevel+noteleport+hardfloor+shortsighted. Message string matches `:10`. Map 76×20 `W`. Tele `{0,0,25,19}`. Portal `{51,0,75,19}` name `astral`. Monsters: 8 giant eel, 8 electric, 9 kraken, 4 shark, 4 piranha, 4 jellyfish, 4 `;`, **19** `water elemental` `peaceful=0` (`:83–101`). JS `waterMons` `:9357–9375` same arity. **Match.**

**Bubble order.** C `fixup_special` `:579–584` `setup_waterlevel` then lregions. JS `load_water` wallify+`flip_level_rnd(3)` then `setup_waterlevel` then apply TELE/PORTAL then `fixup_special` with the array already drained (so JS `fixup_special` does **not** run setup twice). `setup_waterlevel` bounds 3/1/78/20, stone→WATER, `xskip=10+rn2(10)` / `yskip=4+rn2(4)`, `mk_bubble(..., rn2(7))`. **Match that RNG.**

**Save/restore.** C `save_waterlevel` counts bubbles, writes xmin..ymax, `Sfo_bubble`, then `release_data` → `unsetup_waterlevel`. JS JSON `{xmin,ymin,xmax,ymax,bubbles[]}` from `goto_level`/`serLevel`; then `unsetup_waterlevel`. Restore rebuilds the chain and `mv_bubble_move(..., true)` like C `:1773`. Cons pointers named 0. Empty-n `impossible` named skip.

**Callee closure.** One `load_special` water arm. Monsters/`l_levregion`/`setup_waterlevel`/`place_lregion` LIVE. Save analogue LIVE. No STUB in the shipped loader. `set_wportal` `impossible` OMIT.

## Hallucinations / overclaim

Do **not** stamp cons pickup / `maybe_adjust_hero_bubble` / humidity / `ensure_way_out`. JSON is the VFS analogue of `NHFILE`, not a C `Sfo_*` bitstream — field set matches. Public 44/44 does not walk the Water plane. Vacuous hidden verify is not a corpus PASS.

## Density

§2b: water proto + the save/restore callees that make leaving the plane not drop `bbubbles`. +333. Did **not** glue astral. Right size.

## Verification

This audit: `node scripts/hidden-proxy.mjs verify makemaz --base e782f134~1` → `0 session(s) blocked` / `no corpus session is blocked on it`. Queue row was a blank-plane Open, not N corpus blocks. Lua `:41–101` vs HEAD `:9357–9375`. D-log green + cohort + full 44/44.

## Actionable C-wrongs

None that must block the next port. Named stay on the map.

Verdict: **ACCEPT-WITH-DEBT**
