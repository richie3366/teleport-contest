# Review 1415 — fd174200 — mv_bubble whole-body port (D-2456)

Metadata: SHA `fd174200`, `js/mklev.js` only (+61/−15). Coverage
MISSING → same-named live. D-log: D-2456.

## Intent vs deliverable

Promise: rename split-named `mv_bubble_move` → `mv_bubble` (C
staticfn posture) and add the three missing arms — four
out-of-bounds pline+clamp diagnostics (`:1980–1997`), cons-switch
default `impossible` (`:2078–2080`), unblock/block_point on the
`mk_bubble` ini paint (`:2017–2025`). Diff does exactly that, plus
doc updates. No new edges (`pline` joins the existing display.js
import — ALREADY).

## Inventory

- Renamed: module-local `mv_bubble_move` → `mv_bubble` (both call
  sites updated; no stale refs — grep clean). `sym.mjs` lists it as
  the single module-local def: correct staticfn posture, not drift.
- Added: 4 pline+clamp arms, cons `else impossible`, ini
  unblock/block_point.
- Deposit gate widened `isWater && cons?.length` → `isWater` with
  `b.cons = null` unconditional — matches C `:2028`/`2084`
  (`b->cons = 0` inside the water gate regardless of emptiness).

## C ↔ JS fidelity

C `mkmaze.c:1952–2107` vs JS: pline texts (`bubble xmin/xmax/ymin/
ymax` with values) and clamp assignments (`:1980–1997`) verbatim in
C order; bounce/boing untouched; cons-switch default message
verbatim (`:2078–2080`); ini paint `unblock_point` on water /
`block_point` on air (`:2020`/`2024`) — exact. Bounds-as-params
cover C's file-scope gbxmin statics. No RNG touched by the new arms
(plines/clamps/`impossible`/block flags draw nothing). Confirm.
Pre-existing (not this diff, not re-litigated): unconditional
typ/lit paint in the `mk_bubble` ini replica (C `:2017–2025` gates
typ/lit inside the water/air branches); the D-2427 ini context owns
that shape.

## Hallucinations / overclaim

None. "Provably no-ops here" for clamp/bounce on the ini path is
sound (dx=dy=0, pre-clamped bounds, cons null → deposit loop over
`[]`, `b.cons=null` idempotent).

## Density

Small whole-function completion (61 insertions, one module):
right-sized.

## Verification

- `hidden-proxy verify mv_bubble --base fd174200~1 --reach-all`
  (re-run): 0 blocked both sides (vacuous, as stated); smoke 24/24,
  0 regressed → REACH-OK. Matches. Full 44/44 D-log-reported.
- Diff grep: no FORCE/DIAG/seed/coordinate logic.

## Actionable C-wrongs

None. All three arms match C.

Verdict: **ACCEPT**
