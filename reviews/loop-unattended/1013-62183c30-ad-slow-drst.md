# Review 1013 — 62183c30 — mhitm_ad_slow mhitu + ad_drst uhitm arms (D-2043)

Metadata: SHA `62183c30`, D-2043, Open-row port (two
screen/RNG-first rows under one shared gate,
`mhitm_mgc_atk_negated`). js/ touches 3 files:
`mhitu.js` (+26), `uhitm.js` (+58), `mhitm.js`
(export keyword). No stamp owed.

## Intent vs deliverable

Subject promises: the missing AD gates behind
`mhitm_mgc_atk_negated` — mhitu SLOW arm + uhitm DRST
arm — in exact C branch order. Diff actually adds:
`mhitm_ad_slow_u` + `case AD_SLOW` + `AD_SLOW = 13`;
`damageum_ad_drst` + `case` + file-local `Your_u` and
`mpoisons_subj_u`; import extensions only. Promise ≡
diff. Named-omits list is explicit (mhitm/uhitm sibling
arms, full `defended()`, uhitm AD_SLOW).

## Inventory

- New JS: `mhitm_ad_slow_u` (`mhitu.js`), `Your_u` +
  `mpoisons_subj_u` + `damageum_ad_drst` (`uhitm.js`).
- `sym.mjs`: `mhitm_mgc_atk_negated js/mhitm.js:1973
  ASYNC` (awaited at both new call sites ✓);
  `u_slow_down js/mhitu.js:457 ASYNC` (awaited ✓);
  `MON_WEP js/weapon.js:82 sync`; `resists_poison_mm`
  single definition, newly exported (no clash).
  `AD_DRST` resolves via the `export {...}` list
  (`mhitm.js:679`) — `sym.mjs` reports NOT FOUND for
  export-list consts (indexer gap, not a code issue).
  No symbol deleted or re-pointed.

## C ↔ JS fidelity

C loci (all via `csym.mjs`): `mhitm_ad_slow`
`uhitm.c:3651-3687`; `mhitm_mgc_atk_negated`
`uhitm.c:74-99`; `mhitm_ad_drst` `uhitm.c:3121-3165`;
`mpoisons_subj` `mhitu.c:144-159`;
`mon_resistancebits`/`resists_poison`/`Resists_Elem`
(`monst.h:270-277`, `mondata.c:129-195`).

Branch-by-branch confirm:

- SLOW mhitu arm (C `:3671–3675`): `hitmsg` then
  `!negated && HFast && !rn2(4)` → `u_slow_down`.
  JS is gate → hitmsg → same triple → awaited
  `u_slow_down`. Gate-first matches C `:3656`
  (rn2(10) burns before `hitmsg` and before the
  `defended()` early return). `defended()` omit is
  named and RNG-free (no `rn2/rnd` in
  `mondata.c:89-130`; artifact/scales predicate
  only). HFast is intrinsic-only on both sides
  (`youprop.h`: `HFast = uprops[FAST].intrinsic`;
  JS tests `u.HFast || uprops[FAST].intrinsic`,
  extrinsic correctly excluded — the attrib.js:1011
  idiom, not a new invention). Leftover `d()` kept
  like FAMN matches C per-attack order
  (`mhitu.c:1187–1194`, damage before adtyping).
- DRST uhitm arm (C `:3129–3144`): gate → `!rn2(8)`
  → `Your poisoned` → `resists_poison(mdef)` →
  `!rn2(10)` deadly (`damage = mhp`) else
  `damage += rn1(10, 6)`. JS follows call-for-call;
  `pline_The` ≡ the hardcoded `"The poison…"`
  capital. `resists_poison_mm` ≡
  `mon_resistancebits & MR_POISON` verbatim
  (`monst.h:270`: `data->mresists | mextrinsics |
  mintrinsics`); the artifact/worn tail
  (`mondata.c:173-195`, a deterministic inventory
  scan — no RNG) is named in the helper doc comment,
  RNG-safe, and unreachable on the corpus arm
  (iron golem wields nothing).
- `mpoisons_subj_u` CLONE: the youmonst→`uwep` arm
  (`mhitu.c:150`) is C-required — confirmed the
  mm-variant (`mhitm.js:1253`) reads `MON_WEP`
  unconditionally, which is wrong for a poly'd hero.
  Nit (not a wrong): the extra `|| _youmonst`
  disjunct is dead at the single call site (`magr`
  is assigned `game.youmonst` literally two lines
  up) — harmless JS-ism, behavior-neutral here.
- `Your_u` CLONE (`"Your " + rest`) ≡ C `Your()`;
  zap/mhitu precedent, verified shape.

Callee closure per arm: SLOW — gate/hitmsg/u_slow_down
all LIVE. DRST — gate/resists/mon_nam/rn LIVE,
`Your_u` + `mpoisons_subj_u` verified CLONEs,
`defended` + sibling mhitm/mhitm arms named OMITs.
No STUB in a live arm.

## Hallucinations / overclaim

None. "Exact C branch order" holds on both arms;
"no `--can` call: pre-existing edges" is accurate
(uhitm→mhitm, uhitm→weapon edges pre-exist; the
commit adds names, not edges).

## Density

~86 insertions / 3 files, one falsifier (gate-draw
order), one C locus family. Right-sized §2b cluster.

## Verification

- Diff-hunk grep: no FORCE/DIAG/getRngLog/seed gates
  (full `--rulecheck` once for the iteration — see
  review 1017).
- Re-measured `hidden-proxy verify
  mhitm_mgc_atk_negated --base 62183c30~1`: `1 PASS,
  2 moved past, 1 unchanged, 0 worse → PROGRESS`
  (Caveman-92118 PASS; Valkyrie-92195 →
  u_stuck_cannot_go@172; Archeologist-92038 →
  do_statusline1@185; Monk-92013 still
  gate@75 screen-only `--More--` residual) —
  matches the D-log line-for-line, including the
  honestly-carried D-2005-Next display residual.
- Green 2/2 + strict ×2, cohort 7/7 per pasted tail.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
