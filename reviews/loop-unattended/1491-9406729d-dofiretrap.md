# Review 1491 — 9406729d — trap.c dofiretrap (D-2532)

## Metadata

- SHA: `9406729d`
- D-id: D-2532. Next index: 1491.
- Files: `js/trap.js` (+96/−40ish, single function + 1 const word).
- C locus: `nethack-c/upstream/src/trap.c:4232–4314` (`dofiretrap`,
  staticfn, 83 L).

## Intent vs deliverable

Subject promises: whole `dofiretrap` in C order (PARTIAL → live),
closing the box/carried/golem/shieldeff omits. Diff actually adds:
steam-arm box gate, tower `the(box?xname:surface)`, resistance
`shieldeff`+`monstseesu`, the full golem switch, dual botl,
`monstunseesu` in both arms, unconditional `melt_ice`. Promise
matches deliverable.

## Inventory

- Changed: `dofiretrap` (restart in place, still module-local —
  `sym.mjs` reports "1 LOCAL in js/trap.js:4681", which is correct
  here: C declares it `staticfn`, all 3 C callers are same-file).
- Changed: `M_SEEN_FIRE` joins the existing const.js specifier (no
  new edge; `--can` correctly deemed unnecessary).
- No symbol deleted or re-pointed; no clone→import paste owed.

## C ↔ JS fidelity

`csym.mjs` body `:4232–4314` vs JS, in order:

- `:4241` `orig_dmg = num = d(2,4)` — one shared draw. Confirm.
- `:4244–4253` steam arm: `(box && !carried(box)) ?
  is_pool(box ox,oy) : Underwater` — JS identical, with
  `u.uinwater` for C `Underwater` (verified: zero writes to
  `.Underwater` anywhere in `js/`; `u.uinwater` is the live
  field). Spray `the(box ? xname(box) : surface(ux,uy))`,
  `rnd(3)` boil, early return. Confirm.
- `:4254–4256` tower pline with `bursts` (box) / `erupts`
  (floor). Confirm.
- `:4257–4261` resistance: `shieldeff` + `monstseesu(M_SEEN_FIRE)`
  + `num = rn2(2)` — both calls were missing before, now present
  and awaited/sync-correct. Confirm.
- `:4262–4284` Upolyd golem switch: PAPER alt=mhmax, STRAW /2,
  WOOD /4, LEATHER /8, default 0, `Math.trunc` for C integer
  division (mhmax ≥ 0, so trunc ≡ `/`). `alt > num → num = alt`
  (the removed `num = orig_dmg` reset was a no-op: num still
  equals orig_dmg at that point). `mhmax > mlevel →
  mhmax −= rn2(min(mhmax, num+1))` + botl; `mh > mhmax` clamp +
  botl; `monstunseesu`. Single `rn2`, C order. Confirm.
- `:4285–4300` ordinary arm: second `d(2,4)` re-roll,
  `minuhpmax(1)`/`olduhpmax` burn, `setuhpmax(min(...), FALSE)`,
  `!Drain_resistance → losexp(null)` (line `:4749`, kept),
  `uhp` clamp, `monstunseesu`. Dual `flags.botl`+`disp.botl`
  mirrors this file's botl convention. Confirm.
- `:4301–4305` `!num → You uninjured / else losehp(num, ...,
  KILLED_BY_AN)` + `burn_away_slime` (static timeout.js import
  `:141`, dynamic import deleted). `pline('You …')` →
  `You('…')` rewordings keep identical text. Confirm.
- `:4307–4310` `burnarmor(you) || rn2(3)` short-circuit kept
  (JS `||` — `rn2(3)` drawn only when burnarmor is false, as in
  C) → `destroy_items(AD_FIRE, orig_dmg)` + `ignite_items`.
  Confirm.
- `:4311–4312` floor-only `burn_floor_objects` smell. Confirm.
- `:4313–4314` `melt_ice` moved OUT of `if (!box)` —
  unconditional now, matching C (box case previously skipped it).
  Genuine C-order fix inside this body. Confirm.
- RNG call-for-call: d, [rnd(3) | rn2(2) | rn2(min)],
  [d(2,4)+rn2(min)], rn2(3)-gated. No added/removed draws.

Callee closure: every word live or file-local (`shieldeff`,
`monstseesu`/`monstunseesu`, `carried`, `is_pool`, `surface`,
`xname`/`the`, `burn_away_slime`, `ignite_items`,
`burn_floor_objects`, `melt_ice`, `minuhpmax`/`setuhpmax`,
`losexp`). Omits: none. No STUB in any live arm.

## Hallucinations / overclaim

None. The retired omit list (box/carried, shieldeff/monstseesu,
golem alts, surface) is exactly what the diff wires.

## Density

One 83-line C staticfn in its home module. Right-sized per §2b.

## Verification

- D-log: syntax (1 changed) · rule2 · hidden note (0 blocked) ·
  smoke 24/24 · green 2/2 · strict ×2 · cohort 7/7 · full skipped
  (no shared file changed) → VERIFY: PASS.
- Re-run here: `hidden-proxy.mjs verify dofiretrap
  --base 9406729d~1 --reach-all` → 0 blocked both trees (vacuous
  note, honestly reported) + smoke 24 PASS, 0 regressed →
  REACH-OK. Matches.
- Diff grep: no FORCE/DIAG/getRngLog/fastforward/seed/coordinate
  logic.

## Actionable C-wrongs

None. No Must-fix, no CURRENT change.

Verdict: **ACCEPT**
