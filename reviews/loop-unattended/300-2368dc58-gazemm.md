# Review 300 — 2368dc58 — mhitm.c gazemm (D-1338)

## Metadata
- Full / short hash: `2368dc58191f58c237bdd27040c6fcd38b5cd5ae` / `2368dc58`
- Parent: `38b3ff39` (reviews **296–299** + cadence **#1695**). This file audits **this SHA only**. Archive **Addressed:** D-1338 `2368dc58` already has the short hash (filled by D-1339).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-21 05:30:52 +0200
- D-id: **D-1338**
- Stats: 12 files, +364 / −124 — `js/mhitm.js` +184; `js/mhitu.js` comment only.
- Claims to close: Open `mhitm.c` gazemm (named from D-1328 / review **299**). Not explmm. `reviews/loop-2026-08-15/` has no unpaid gazemm Must-fix.
- JS / map: `mhitm.js` `gazemm` / `mattackm` `AT_GAZE` / `mhitm_ad_blnd` leftover; `c-js-map/turns.md`. explmm / AT_HUGS / `shade_miss` / ston leftover still named at this SHA (later SHAs in this batch).
- Prior reviews this SHA claims to close: **299** named gazemm as next Open after `splash_lit`; **290** named gazemm after `gazemu`.

## Intent vs deliverable

Git subject promises: “Match C mhitm.c gazemm so an AT_GAZE monster actually gazes at another monster (Medusa reflect/stone, Archon blind), instead of falling out of mattackm.”

C `mattackm` (`mhitm.c:492–495`):

```
        case AT_GAZE:
            strike = 0;
            res[i] = gazemm(magr, mdef, mattk);
            break;
```

C `gazemm` (`:736–803`): mimic `seemimic` + `mundetected=0`; vis `Adjmonnam`/`Monnam` gazes toward/at; miss if `mcan` / `!mdef.mcansee` / (Archon `resists_blnd` else `!magr.mcansee`) / invis / sleep; Medusa `mon_reflects` then `monstone(magr)`; else Archon `mhitm_ad_blnd(mhm=NULL)` + `rn2(2)` stun; always `mdamagem(..., NULL, 0)`.

Old JS: no `AT_GAZE` arm (fell through default). No `gazemm`.

The diff **does** export `gazemm`, wire `strike=0` (ranged; no distmin skip), port the mhitm `mhitm_ad_blnd` arm, and add leftover AD_BLND in `mdamagem`. It does **not** port explmm / AT_HUGS / `shade_miss` / `mhitm_ad_ston`. Named.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `gazemm` | C `:736–803`, **wired** | new export; C is `staticfn` |
| `mattackm` `AT_GAZE` | C `:492–495`, **wired** | `strike=0`; no distmin |
| `mhitm_ad_blnd` | C `uhitm.c:2986–3011` mhitm arm, **clone** | Archon extra `mhm=null`; leftover zeros dice |
| `resists_blnd_mm` | C `mondata.c:248–272` monster arm, **clone** | `resists_blnd_by_arti` named |
| `can_blnd_mm` | C `mondata.c:331–339` gaze/expl, **clone** | `mon_perma_blind` / raven-vs-raven named |
| `dmgtype_fromattack` | C `mondata.c`, **clone** | mattk adtyp+aatyp |
| `mon_reflects_mm` | C `muse.c:2797–2833`, **pre-existing clone** | `arti_reflects(MON_WEP)` named D-1328 |
| `monstone` | C `mon.c`, **imported live** | reflect stones magr |
| `seemimic` / `perceives` / `Adjmonnam` | C, **imported live** | |
| `mdamagem` AD_BLND | C leftover, **wired** | second `d()` then zero |
| explmm / AT_HUGS / `shade_miss` | C later, **named omit** | this SHA |
| `mhitm_ad_ston` leftover | C `uhitm.c:4254–4261`, **named omit** | non-reflect Medusa victim |

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` writes / seed names / recorded coordinates. Rule #2 clean. **New gameplay RNG:** Archon `d(damn,damd)` in extra blind, then `rn2(2)` stun, then leftover `d()` that zeros; cancelled/sleep/invis miss burns none.

## C ↔ JS fidelity

Miss predicate is call-for-call `:758–763`. Archon uses `resists_blnd_mm(mdef)` not `!magr.mcansee` — a blinded Archon still gazes (`altmesg`). `PM_ARCHON`/`PM_MEDUSA` via **mndx** (D-0928), not `mons()` identity. Mimic `mlet === 'S_MIMIC'` is the JS mlet string (C `S_MIMIC`).

Medusa: test `mon_reflects_mm(mdef,null)` then visible fmt `"The gaze is reflected away by %s %s."`; if `mdef.mcansee`, test magr reflect (double-reflect miss), invis notice-miss, else `pline_mon` + `monstone` + AGR_DIED unless live. Blind reflecting defender skips the inner stone and falls through to leftover `mdamagem`. Match `:766–790`.

Archon extra: `mhitm_ad_blnd(..., null)` then `rn2(2)` `mstun=1`. C `can_blnd` for AT_GAZE is `!mcan && !resists_blnd`. After extra sets `mcansee=0`, leftover `can_blnd` is false so the second `d()` for `mblinded` does **not** run; leftover still zeros HP `d()`. RNG order matches `:791–802` then `mdamagem` `:1025` + `:2986`.

`mon_reflects_mm` is shield / amulet / silver DSM / silver|chromatic dragon. C inserts `arti_reflects(MON_WEP)` between shield and amulet. Named. `resists_blnd_mm` has the monster arm + yellow-light/Archon `dmgtype_fromattack`; Sunsword `resists_blnd_by_arti` named. `can_blnd_mm` gaze/expl arm matches `:331–339`; other aatyps returning true is named.

Hallucination check for “Match C dispatch, callee is a stub” is clean: `gazemm` is the body; `monstone` is live. This is **not** “Match C `mhitm_ad_ston`.” Reflect-stones-**magr** is the hunk; leftover stones-**mdef** is named.

## Hallucinations / overclaim

Subject + D-1338 say an AT_GAZE monster actually gazes (Medusa reflect/stone, Archon blind) instead of falling out of `mattackm`. **The case plus `gazemm` plus leftover AD_BLND are the hunk.** Stamping **Addressed:** D-1338 is fair. Do **not** stamp “Match C explmm / AT_HUGS / `shade_miss`.” Do **not** stamp “Match C `arti_reflects(MON_WEP)`.” Do **not** stamp “Match C Medusa leftover `do_stone_mon`.” Do **not** treat fortress PASS as a Medusa-vs-nymph stone.

## Density

One C function plus its `mattackm` arm and the leftover `mhitm_ad_blnd` callee it always reaches. ~180 lines in `mhitm.js`. Playbook §2b cluster. Did not glue explmm. Acceptable size.

## Branch-by-branch confirm

1. `AT_GAZE`: `strike=0`, `gazemm`, no distmin skip. Match `:492–495`.
2. Cancelled / blind target / invis / sleep: “but nothing happens.”, `M_ATTK_MISS`. Match `:758–763`.
3. Blind Archon vs seeing target: still blinds (`altmesg` toward). Match `:739–742`.
4. Archon vs Archon: `resists_blnd` miss. Match.
5. Medusa vs shield: `monstone(magr)` AGR_DIED. Match `:766–789`.
6. Double reflect: miss. Match `:770–774`.
7. Archon extra `d()` + `rn2(2)` then leftover zeros HP. Match `:791–802`.
8. `mdamagem` AD_STON / CONF / STUN / FIRE leftover. Still omitted. Named.
9. **Public-unhit** unless a session has mon-vs-mon gaze.

## Anti-pattern / Rule #2 (this SHA `js/`)

No FORCE / fs / seed-named gates. Plain ESM. `mhitu.js` is a comment that gazemm is no longer named there.

## Verification

Journal: private canary **18**/18; green+strict seed8000/0900; cohort **7**/7 + strict 1500/1800/0012/0004/0007/2200/0383. **Public-unhit** on gazemm. Cadence this audit: full `sessions` at HEAD `e3a30202` **44**/44 Scr **11,405**/11,405 RNG **792,838**/792,838 (100%) speed `36+0.29/turn` (R² 0.85). I did not re-run the private canary. Fortress PASS is not evidence an Archon blinded a nymph.

## Actionable C-wrongs

None for Must-fix. `gazemm` + `AT_GAZE` + leftover AD_BLND match C `:736–803` / `:492–495` / `:2986–3011` on live `monstone` / `d` / `rn2`.

Named omits (map, not Must-fix):

1. `mhitm_ad_ston` leftover (non-reflect Medusa victim)
2. `arti_reflects(MON_WEP)` in `mon_reflects_mm`
3. `resists_blnd_by_arti` / `mon_perma_blind` / raven-vs-raven
4. explmm / AT_HUGS / `shade_miss` (later SHAs)

Do not Must-fix “gaze skips at distmin>1” (C does not). Do not Must-fix “stone the defender on reflect” (C stones **magr**).

## Callers / RNG ledger

C: `mattackm` AT_GAZE → `gazemm` → optional `rn2(2)` + leftover `d()`. JS: same. Public fortress is not those rolls.

## Verdict

- Verdict: **ACCEPT-WITH-DEBT**
- One sentence: AT_GAZE now gazes (Medusa reflect stones magr; Archon blinds); leftover ston and `arti_reflects` stay named.
- Must-fix stays empty for this SHA.
