# Review 1581 — 17b44606 — mkobj.c obj_meld whole-body restart (D-2622)

**Metadata:** SHA `17b44606`, `mkobj.c` `obj_meld`, D-2622.
JS: `js/mkobj.js` restart + 1-line `await` each in `js/do.js:861`,
`js/mhitm.js:2943` (+47/−31 total).

## Intent vs deliverable

Subject promises: a restart fixing every boundary divergence — null /
same pointees returned the surviving glob instead of NULL, null holders
skipped the `impossible()` misuse arm, the `ox` tail fired `newsym`
through a floating lazy import with no `cansee` gate, and
`maybe_unhide_at` was deferred. Diff delivers all four fixes plus
`await` at both call sites. Promise matches deliverable.

## Inventory

- `obj_meld(p1, p2)` — restarted export (sync → async).
- Two caller updates (add `await`).
- No import changes, no deleted symbol, no local→import re-point.

## C ↔ JS fidelity

C locus `mkobj.c:3767–3814` (48 L, via `csym.mjs obj_meld`; callers
`do.c:312`, `mon.c:727`). Full C body read here. Branch-by-branch
confirm:

- `result = null` (`:3771`) — exact.
- Holder-level `if (p1 && p2)` on the `{obj}` pairs, not the pointees
  (`:3774` — C's `struct obj **`, the old pointee-check C-wrong fixed).
- Pointee `otmp1 && otmp2 && !==` gate with null/same falling through
  to null (`:3777` — the old survive-glob return fixed).
- `:3779–3788` FIXME ported as-is (mid-drop free glob, shore/pool —
  C handles neither).
- Floor+free veto + heavier-or-`rn2(2)` tiebreak (`:3789–3792`) —
  single RNG call, call-for-call with C.
- Absorbed-away floor coords (`:3793–3794` / `:3797–3798`) — exact.
- `if (ox)` + `cansee` gate + `newsym` (`:3803–3805` — the old
  floating lazy import without gate fixed).
- Awaited `maybe_unhide_at` (`:3809`; `sym.mjs` → `js/monmove.js:1343`
  ASYNC, already top-level imported) — exact.
- `else impossible(...)` misuse arm (`:3811–3813`), return (`:3814`) —
  exact.
- `obj_absorb` called WITHOUT await — correct, `sym.mjs` →
  `js/mkobj.js:2765` sync.

Caller wiring (both C sites checked in context): `do.c:312`
flooreffect globby loop with `(void)` discard → `do.js:861` discards
and reads `r1.obj` — exact. `mon.c:727` `obj = obj_meld(&obj,&otmp)`
loop → `mhitm.js:2943` assigns — exact. Both are the only JS callers
(grep-confirmed); both await the now-async export, so no floating
promise.

Callee closure: all LIVE (`cansee` vision.js, `newsym`/`impossible`
display.js, `maybe_unhide_at` monmove.js, `obj_absorb` file-local,
`rn2` rng.js) — no new edge, none named. "Named: none new" accurate.

## Hallucinations / overclaim

None. The message's "fixes every boundary divergence" enumerates four
concrete old-vs-new behaviors, each confirmed in the hunks.

## Density

Small-function restart (48 L C) + two caller lines. Right-sized.

## Verification

- `node scripts/imports.mjs --rulecheck` → Rule #2 clean (whole `js/`).
- Diff grep: only the C `rn2(2)` line plus message-text hits; 0
  `FORCE`/`DIAG`/`getRngLog`/`fastforward`/seed/coordinate reads.
- Re-measured: `hidden-proxy.mjs verify obj_meld --base 17b44606~1
  --reach-all` → `0 session(s) blocked` at baseline and working tree
  (vacuous-note path, honestly labeled) + `smoke 24/24 PASS, 0
  regressed → REACH-OK`. Both summary lines cited; green/strict/
  cohort + full 44/44 (shared file changed) per D-log.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
