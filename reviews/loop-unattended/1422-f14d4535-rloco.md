# Review 1422 — f14d4535 — rloco whole-body port (D-2463)

Metadata: SHA `f14d4535`, `js/teleport.js` restart (async) + 4 one-line
caller awaits (`dokick.js`, `mon.js`, `trap.js`, `zap.js`). Coverage gap
(0 blocked), not a corpus fix. D-log: D-2463.

## Intent vs deliverable

Promise: Rider-corpse revive, extract-then-read, dndest restricted_fall +
W-tower gate, `flooreffects("fall")`, shop bill/stolen_value, place +
newsym. Diff ships all of it in C order. No second subsystem.

## Inventory

- Restarted: `rloco` (now async — C is sync, but the body awaits the
  live async `revive_corpse`/`flooreffects`/`addtobill`/`stolen_value`;
  the async conversion is forced, not elective).
- All 4 JS call sites re-pointed with `await` in this commit (grep
  confirms no remaining un-awaited `rloco(` in `js/`); all four callers
  are async.
- No new static edge (dynamic do.js/shk.js imports follow the file's
  `rloc_maybe_minvent_shop_bill` cycle pattern); `mons` joins the
  existing monsters.js edge; local `CORPSE` const. `On_W_tower_level` is
  the canonical dungeon.js export (`js/teleport.js:61`); the potion.js
  clone is untouched.
- No deleted/re-pointed clones for `sym.mjs`.

## C ↔ JS fidelity

C `teleport.c:2101–2187` vs JS, arm by arm:

- Rider (`:2109–2112`): `CORPSE && is_rider` → revive-or-continue —
  exact, `revive_corpse` async + awaited.
- Extract-then-read (`:2114–2117`): order fixed to C (old code read
  ox/oy before extract); `restricted_fall = otx==0 && dndest.lx` keeps
  C's otx-only test — exact.
- Pick loop (`:2118–2139`): both draws precede the `try_limit` break
  check; restricted-fall dndest/nlx arms and the W-tower inside/outside
  XOR exact. `within_bounded_area` returns a real boolean, so the `!==`
  comparison is sound (= C `!=`).
- Flooreffects (`:2141–2147`): consume → old-loc newsym unless
  trap-door `(0,0)` + FALSE — exact.
- Trap-door no-op (`:2148`) — exact (`;` preserved).
- Shop block (`:2150–2180`): `find_objowner`/`costly_spot`/
  `costly_adjacent` sync, un-awaited; `subfrombill` sync, un-awaited;
  `addtobill`/`stolen_value` async, awaited — all per `sym.mjs`. The
  `char h/oo` + `strchr` logic is mirrored with `''→'\0'` + `includes`
  (single-char `includes` = `strchr`) — exact, including the same-shop
  `oo` verification on both sub- and add-bill arms. `newsym(otx,oty)`
  placement exact. RNG (`rn1`/`rn2`) in C order.
- Place + newsym pair + TRUE (`:2181–2185`) — exact.

Named omits (both verified, not stubs): `mkcorpstat` x==0&&y==0 —
`js/mkobj.js` contains no `rloco` call, so nothing runs detached; the
async cascade is genuinely its own row. Hurtle boulder-TELEP —
`js/dothrow.js hurtle_step` is hero-only, no boulder counterpart
exists. The `within_bounded_area` local predates `rect.js:69` and serves
two other callers — documented, untouched.

## Hallucinations / overclaim

None. "No new static edge" is accurate; both Named lines were verified
above rather than trusted.

## Density

One 87-line C function + mechanical caller awaits, 96 insertions:
right-sized.

## Verification

- `hidden-proxy verify rloco --base f14d4535~1 --reach-all` (re-run):
  0 blocked both sides (vacuous, as stated); smoke 24/24 PASS,
  0 regressed → REACH-OK. Matches.
- Diff grep: no FORCE/DIAG/seed/coordinate logic.

## Actionable C-wrongs

None. Whole body ports C in order.

Verdict: **ACCEPT**
