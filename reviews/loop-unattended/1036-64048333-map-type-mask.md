# Review 1036 — 64048333 — monst.h M_AP_TYPE unmasked F_DKNOWN (D-2066)

## Metadata

- SHA: `64048333` — `monst.h M_AP_TYPE unmasked F_DKNOWN flips monster_nearby rest-safety (queue owner mcalcmove, writer M_AP_TYPE) (D-2066).`
- JS diff: `js/const.js` +5/−1 (one-line body change + 4-line C-cite comment).
- Docs: D-2066 D-log/D-index/CURRENT/NOTES/queue/turns.md map.
- Next index: 1036.

## Intent vs deliverable

Subject promises: mask `m_ap_type` with `M_AP_TYPMASK` so a mimicking
Large Mimic (`m_ap_type=10`, OBJECT|F_DKNOWN) is skipped by
`monster_nearby` and the `'.'` rest proceeds. Diff actually changes one
return expression. Promise == diff.

## Inventory

- Changed: `M_AP_TYPE` (js/const.js:3188) — expression only.
- No new/deleted symbols (no `sym.mjs` re-point check owed), no new
  imports/edges, no RNG, no branches touched.
- Diff grep: no `FORCE`/`DIAG`/`getRngLog`/seed/step/coordinate reads,
  no `fastforward`, no hardcoded coordinates.

## C ↔ JS fidelity

C locus (`nethack-c/upstream/include/monst.h:69–73`, read directly):
`M_AP_TYPMASK 0x7`, `M_AP_F_DKNOWN 0x8`, `#define M_AP_TYPE(m)
((m)->m_ap_type & M_AP_TYPMASK)` — `10 & 7 = 2 = M_AP_OBJECT`.
JS `M_AP_TYPMASK` is a live export (js/const.js:1557); the reference
inside the function body is call-time, so no TDZ by construction.
Verbatim port, confirmed branch-free.

Caller check — C `monster_nearby` (`hack.c:4105–4127`, via `csym.mjs`):
skips `M_AP_FURNITURE`/`M_AP_OBJECT` via the masked macro before the
hostile/helpless/onscary/canspotmon gates. JS with raw `10 !== 2` fell
through to `return 1` → rest blocked (`ECMD_OK`, no time) → whole-block
skip with impaired trailing dice — exactly the recorded symptom (sync
through block 179 proven by the 7 equal values, divergence at block
180). Mechanism confirmed end to end, not inferred.

Blast radius: ~40 call sites inherit the mask. The dozen sites that
already masked inline are idempotent under a second `& 7` (no behavior
change there — verified by reasoning, and the full-suite/cohort greens
below show no regression). Raw `m_ap_type` readers that bypass
`M_AP_TYPE` (objnam/sounds/music/mhitu/eat) are explicitly named in the
D-log and map as future rows, not silently fixed — correct per §2b
(one locus, no glued envelope).

## Hallucinations / overclaim

None. «Call-time reference, no TDZ, no new import/edge» verified true.
Falsification record (onscary false both sides, geom-probe 0 cells,
identical monster data) is quoted from the Parked diagnosis, not
re-proven here — acceptable, it is cited, not claimed as new.

## Density

6-line diff on a non-Must-fix Open row is below the §2b ~40 guideline,
but the C locus is a one-line macro and the fix is one expression —
C is that small. No padding, no glued arms. Acceptable.

## Verification

D-log Verify bullet: `node -e` import check (10→2, 8→0, 3→3, 0→0,
null→0) + `verify --fn mcalcmove` → 1 PASS / 1 moved past / 1 unchanged
+ green/strict/cohort. Re-measured myself: `hidden-proxy.mjs verify
mcalcmove --base 64048333~1` → `1 PASS, 1 moved past (1 still mcalcmove
at a later step), 1 unchanged, 0 worse → PROGRESS` (Archeologist PASS;
Knight 17→mcalcmove@20; Rogue still mcalcmove@26 on the parked
slime/lifesave writer). Claim reproduces exactly. No vacuous check (3
sessions at baseline, all named). `skip full` justified (const.js is
data + pure helpers; D-2070 later ran full 44/44 over the shared-file
change).

## Actionable C-wrongs

None.

## Verdict

Verdict: **ACCEPT**
