# Review 809 — 65036888 — role.c roles[] ldrnum / homebase / questarti (D-1839)

## Metadata
- Full / short hash: `65036888e0d6ffba963ae98cd0aa1fa684a82141` / `65036888`
- Parent: `27758e2a` (D-1838). Map-driven Open: 4 tour sessions `"from :"` vs leader names.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-05 02:44:41 +0200
- D-id: **D-1839**
- Stats: `js/roles.js` +67/−2. `js/` insertions **67** ≤250. Band **80–350**.
- Claims to close: remaining nine roles’ quest identity fields; Arc/Bar `guardnum`. Not `attributes_enlightenment` body.
- JS / map: `roles[]`. `c-js-map/data.md`. Archive **Addressed:** D-1839 `65036888`.

## Intent vs deliverable

Git subject promises: `ldrname`/`convert_arg` `%l` already live; `roles[]` only had those five fields for Arc/Bar/Pri/Wiz so `ldrnum` was `NON_PM` → empty `%l`.

`node scripts/csym.mjs ldrname` → `questpgr.c:49–57`. `convert_arg` `:235–325` (`%l` `:260–262`). `roles` is the `role.c` table `roles[]` `:30–573` (csym has no function of that name). `--callers ldrname`: `convert_arg` `%l`.

The diff **does** copy `homebase`/`intermed`/`ldrnum`/`guardnum`/`questarti` for Cav/Hea/Kni/Mon/Rog/Ran/Sam/Tou/Val plus Arc `PM_STUDENT` / Bar `PM_CHIEFTAIN`.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `roles[]` five quest fields | LIVE data | C table `:30–573` |
| `ldrname` / `convert_arg` `%l` | LIVE already | not this SHA |
| `attributes_enlightenment` body | OMIT named | heuristic owner |
| `roles[].filecode` / rest of `role_init` | OMIT named | |

`node scripts/sym.mjs`:

```
ldrname          js/questpgr.js:447   sync
convert_arg      NOT EXPORTED — 1 LOCAL questpgr.js:500
roles            js/roles.js:64   sync   export const
```

FORCE/DIAG/`getRngLog`/`fastforward`: **none**. Rule #2: clean.

## C ↔ JS fidelity

**Table vs `role.c`.** Arc `:43–48,54` College / Toltec / Carnarvon / **Student** / Orb. Bar `:85–89,95` Duali / Pelias / **Chieftain** / Heart. Cav `:125–130` Caves / Dragon's Lair / Karnov / Neanderthal / Sceptre. Hea `:166–171,177` Epidaurus / Coeus / Hippocrates / Attendant / Aesculapius. Kni Camelot / Isle of Glass / Arthur / Page / Merlin (JS). Mon Chan-Sune / Earth-Lord / Grand Master / Abbot / Eyes. Rog `:330–335,341` Thieves' / Assassins' / Master of Thieves / Thug / Master Key. Ran Orion / wumpus / Orion / Hunter / Diana. Sam `:425–430` Taro / Shogun / Lord Sato / Roshi / Muramasa. Tou `:465–470` Ankh-Morpork / Thieves' Guild / Twoflower / Guide / Express Card. Val `:505–510` Destiny / Surtur / Norn / Warrior / Orb of Fate. **Match those strings and `pm()` / `ART_*` ids.**

**`ldrname` `:49–57`.** `type_is_pname` ? `""` : `"the "` + `pmnames[NEUTRAL]`. Rogue Master of Thieves is a pname → no `"the "`; Norn is not → `"the Norn"`. Corpus strings follow. **Match.** No RNG.

**Callee closure.** Data table only. `ldrname` already LIVE. No STUB in a shipped field.

## Hallucinations / overclaim

Do **not** stamp `attributes_enlightenment` itself or `roles[].filecode`. The proxy owner was a string-literal heuristic; the D-log names that honestly.

## Density

§2b: remaining `roles[]` quest-identity cluster. +67. Right size for a table peel.

## Verification

This audit, `js/` at `65036888`: `node scripts/hidden-proxy.mjs verify attributes_enlightenment --base 65036888~1` → `4 session(s) blocked`. Summary: **`4 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS`**. Matches the D-log (Rogue/Samurai/Tourist/Valkyrie tours). Not vacuous.

## Actionable C-wrongs

None that must block the next port. Named stay on the map.

Verdict: **ACCEPT-WITH-DEBT**
