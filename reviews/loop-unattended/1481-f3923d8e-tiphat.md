# Review 1481 — f3923d8e — sounds.c tiphat whole body (D-2522)

## Metadata

- SHA: `f3923d8e`
- D-id: D-2522. Next index: 1481.
- Files: `js/sounds.js` (+168/−13), `js/pickup.js` (+9/−2, caller wire).
- C locus: `nethack-c/upstream/src/sounds.c:1426–1537` (`tiphat`, 112 L)
  + same-file staticfn `responsive_mon_at` `:1412–1423` (12 L).

## Intent vs deliverable

Subject promises: whole `tiphat` body in C order (coverage MISSING → live).
Diff actually adds: file-local `responsive_mon_at` + exported async `tiphat`
with `:line` cites, extended imports (all on pre-existing edges), and the
`dotip` worn-helm wire in `pickup.js` (retiring that named omit). No stubs,
no second subsystem. Promise matches deliverable.

## Inventory

- New: `responsive_mon_at` (file-local — correct home, C `staticfn`),
  `tiphat` (exported async; `sym.mjs`: `js/sounds.js:1699` ASYNC).
- Changed: `dotip` worn-helm arm (`js/pickup.js:4789`).
- No symbol deleted or re-pointed (fresh names), so no clone→import
  `sym.mjs` paste is owed beyond the closure below.

## C ↔ JS fidelity

`csym.mjs` ranges: tiphat body printed `:1426–1537`; responsive `:1412–1423`;
callers: exactly one, `pickup.c:3671`. Branch walk:

- `:1432–1435` no-helm/res gates: verbatim (`res = bknown ? 0 : 1`).
- `:1437–1438` cursed arm. C `cursed()` is a real function
  (`do_wear.c:1892–1917`), not a flag test: stuck =
  `(otmp==uwep) ? welded(otmp) : otmp->cursed`, message + `set_bknown`.
  JS `cursed_check` (`js/do_wear.js:294`, LIVE) ports exactly that
  (welded arm, plural rule, stored message + `bknown = 1`);
  the Glib/`fingers_or_gloves` arm it defers is unreachable for a helm
  (C condition requires `uwep`/`W_WEP|W_RING`), so the deferral cannot
  fire on this path. Message plined from `game._cursed_takeoff_msg`
  (do_takeoff precedent). LIVE callee, verified. Confirm.
- `:1442–1450` getdir ESC / `res = 1` / doff `You`: verbatim order.
- `:1452–1463` nodir arms: steed + `helpless` inline
  (`monst.h:251` ≡ `msleeping || !mcanmove` — exact), dz up/down, lout.
  Confirm.
- `:1465–1495` ray walk: init, `BOLT_LIM + 1` bound, isok/couldsee
  step-back, `mon_at` for `m_at` (`rm.h:510` MON_AT-guarded head;
  `mon_at` LIVE `js/uhitm.js:4199`, dochat precedent), `canseemon`,
  `glyph_at`, `unseen = glyph_is_invisible_id` where C is
  `glyph_is_invisible` (`display.h:773` ≡ `== GLYPH_INVISIBLE`) and the JS
  helper (`js/display.js:793`) is exactly that comparison — name differs,
  semantics identical. Statue arm: `glyph_is_statue` LIVE +
  `objects_at(x,y)` (`js/mkobj.js:2912`) for `vobj_at`
  (`display.h:22` ≡ pile head; `rebuildObjectsAt` leaves the head as the
  map value — dochat precedent holds). M_AP clear, then the four break
  arms incl. range-1 `responsive_mon_at && !is_silent` with `is_silent`
  (`mondata.h:62` ≡ `msound == MS_SILENT`, MS_SILENT = 0 same-file)
  inlined exact, and `accessible || IRONBARS` last. Confirm.
- `responsive_mon_at` vs C `:1412–1423`: `isok ? mon_at : null`, then
  helpless / `!mcansee` / `!haseyes` / `Invis() && !perceives` /
  worm-tail clear — arm for arm, and correctly *without* a silence check
  (C has none here either). `Invis()` (LIVE `js/timeout.js`),
  `perceives` (LIVE `js/mon.js`), `haseyes` (LIVE monsters edge).
  Verified CLONE. Confirm.
- `:1497–1534` react: ignoring / not-notice / `nada` fallthroughs
  (`nothing_happens` — LIVE const edge, added to existing import),
  `mstrategy &= ~STRAT_WAITMASK`, peaceful wave/grasp/tip
  (`which_armor` LIVE `js/worn.js`, `mhis` LIVE fountain edge,
  `helm_simple_name` LIVE do_wear edge, `bknown = 1` on grasp),
  hostile table with `which = !Deaf ? rn2(3) : rn1(2,1)` and
  `twice = (Deaf || which > 0 || rn2(3)) ? 0 : rn1(2,1)` — JS `||`
  short-circuits identically, so the `rn2(3)` draw is skipped in exactly
  the same states: RNG call-for-call. `Deaf` idiom
  (`HDeaf||EDeaf||u.Deaf||uroleplay.deaf`) is the D-1967 repo-wide form
  of `youprop.h:125`. `next2u` → `dist2 <= 2` (you.h:558 adjacency).
  `domonnoise` same-file LIVE, `map_invisible` LIVE display edge.
  Confirm.
- Caller: C `pickup.c:3671` (`else if (uarmh && cobj == uarmh) return
  tiphat() ? ECMD_TIME : ECMD_OK;`) → JS `if (u.uarmh && cobj === uarmh)
  return (await tiphat()) ? ECMD_TIME : ECMD_OK;` — guard, mapping, and
  ECMD consts (already imported) exact. The single C caller is the only
  JS site; no wiring from a site C never calls from. Confirm.

Callee closure: every new import name rides an ALREADY edge
(`imports.mjs --can`: worn, do_wear, monmove, timeout, mhitu all ALREADY —
no new edge, no TDZ surface). No STUB in any live arm; named omits: none
in the port (caller-side spill/statue arms stay named in `dotip` —
separate C arms, correct to leave).

## Hallucinations / overclaim

None material. One doc nit (not a C-wrong, not queued): the D-log Fix line
says `responsive_mon_at` carries "`:1418–1422` helpless/is_silent cites"
and the JS comment block likewise names `is_silent` — but neither C nor JS
`responsive_mon_at` tests silence (the check lives at `:1488` in the ray
loop, where it is correctly ported). Code is right; the cite is misplaced.

## Density

One C function family (112 + 12 L C → ~150 L JS) + its single caller wire,
two files that already call each other. Right-sized per §2b.

## Verification

- D-log: syntax (2 changed) · rule2 · hidden note (0 blocked) · smoke
  24/24 · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS.
- Re-run here: `hidden-proxy.mjs verify tiphat --base f3923d8e~1
  --reach-all` → 0 blocked at baseline and working tree (vacuous note,
  honestly reported — no queue row cited blocks) + smoke 24 PASS,
  0 regressed → REACH-OK. Matches.
- Diff grep: no FORCE/DIAG/getRngLog/fastforward/seed/coordinate logic.

## Actionable C-wrongs

None. No Must-fix, no CURRENT change.

Verdict: **ACCEPT**
