# Review 1537 — 09224e39 — mkobj.c weight (D-2578)

## Metadata

- SHA: `09224e39`
- D-id: D-2578. Next index: 1537.
- Files:
  - `js/mkobj.js` (+72/−36: restarted `weight()`, `HEAVY_IRON_BALL`
    const, `mksobj` birth `owt` 1→0).
- C locus:
  - `nethack-c/upstream/src/mkobj.c:1887–1976` (`weight`).
  - `nethack-c/upstream/src/mkobj.c:1184` (`mksobj` zeroobj).
  - Both via `node scripts/csym.mjs` / direct read (ranges above).

## Intent vs deliverable

Subject promises:

> `mkobj.c` weight whole-body port (statue/iron-ball/candelabrum arms,
> C arm order, mksobj owt zero) (D-2578).

Diff delivers all of it, including the `mksobj` companion fix. Promise
matches deliverable.

## Inventory

- Restarted: `weight(obj)` (`js/mkobj.js`, export retained).
- New const: `HEAVY_IRON_BALL` via `objectNames.indexOf`, beside
  `BOULDER` (`js/mkobj.js:110–111`, same mechanism).
- `ismnum` joins the existing const.js import (ALREADY-edge).
- Callees: `ismnum`, `eaten_stat`, `simpleonames`, `impossible` (all
  live); `mons()`/`objects` tables.
- No RNG. No deleted symbols. No clones.

## C ↔ JS fidelity

Walked against C `:1887–1976`, arm by arm:

- Base `oc_weight` (`:1890`). Match.
- `quan ?? 1` — the old `|| 1` masked quan 0 past the impossible arm;
  fixed — plus `quan<1` impossible + return 0 (`:1892–1896`). Match.
- Globby → owt as-is (`:1901–1910`). Match.
- Container/STATUE gate (`:1911`) with the `:1915–1933` statue arm:
  `3*cwt/2` via `Math.trunc` (= C `/` on non-negatives), minwt floor,
  `× quan` kept per `:1933`. Match.
- Recursive cwt (`:1935–1937`). Match.
- BoH ternary (`:1950–1954`, D-2422 intact): cursed-×2, blessed
  `(x+3)/4`, uncursed `(x+1)/2`, `Math.trunc` = C integer division on
  non-negatives. Match.
- Else-if chain now in C `:1957–1973` order (the subject's C-order
  claim verified against the body):
  - CORPSE with `ismnum` (not the old `>= 0`), LARGEST_INT clamp,
    oeaten (`:1957–1963`). Match.
  - FOOD oeaten (`:1964–1965`). Match.
  - COIN `(quan+50)/100`, min 1 (`:1966–1969`; `Math.trunc` exact
    since quan ≥ 1 past the early return). Match.
  - Iron-ball owt kludge (`:1970–1971`; `|0 !== 0` = `!= 0`). Match.
  - Candelabrum `spe × tallow` (`:1972–1973`). Match.
- Tail `wt ? wt*quan : (quan+1)>>1` (`:1975`). Match, verbatim.
- Two cleanups verified correct, not just trusted:
  - Dropping the redundant `|| otyp === GOLD_PIECE`: gold is
    COIN_CLASS and C tests oclass only — the disjunct was dead.
  - Dropping the stale punish-levy comment: the new `:1970` arm
    supersedes it.

Companion fix checked, not trusted:

- `mksobj` birth `owt: 1` → `0` per `*otmp = cg.zeroobj` (`:1184`) — a
  nonzero placeholder would trip the new `:1970` kludge and
  self-perpetuate (fresh ball finalizes to owt 1 instead of base).
- Grepped here: no `owt === 1` / `== 1` sentinel exists in-tree, and
  `|0` / `|| 0` readers agree with 0. Safe.
- The first-verify regression (levied ball 161 vs 640) is honestly
  disclosed in the subject as the finder, not hidden.

## Hallucinations / overclaim

None.

## Density

~72 `js/` insertions for a 90-line C function + const + 1-line
companion fix — right size (§2b).

## Verification

- D-log claims `verify.mjs --fn weight --full` → PASS (coverage row).
- Re-ran here (required):
  - `hidden-proxy verify weight --base 09224e39~1 --reach-all`
  - → 0 blocked both sides (vacuous note, expected)
  - → smoke 24/24 REACH-OK.
- Claim confirmed. No RNG in the function.
- Worktree bisection for the audit's wish-pair regression exonerates
  this SHA (both sessions PASS with js@09224e39's predecessor state
  and fail only at D-2577 — see review 1536).

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
