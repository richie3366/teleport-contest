# Review 1836 — 59a5900cf — strstri (D-2877)

- SHA: `59a5900cf` (coverage; `hacklib.c` `strstri`)
- Files: `js/hacklib.js`, `js/attrib.js`, `js/dungeon.js`, `js/invent.js`, `js/mondata.js`, `js/objnam.js`, `js/uhitm.js`, `js/write.js`. 151 `js/` insertions.
- Queue row: Open coverage, 0 corpus blocks cited
- Banned grep: no `FORCE`, `DIAG`, `getRngLog`, `fastforward`, or seed names in the `js/` hunk. `imports.mjs --rulecheck`: "Rule #2 clean".

## Intent vs deliverable

Subject promises one `strstri`: empty `sub` returns `str`; signed 8-bit nibble tables of size `0x20` count `*s & 31` while `k` becomes `strlen(str) - strlen(sub)`; `k < 0` or an over-subscribed nibble returns null; otherwise `lowc` windows, and success is the tail at `&str[i]`. The diff replaces `toLowerCase`/`indexOf` with that body and deletes the `attrib.js` boolean clone, the `write.js` `{ index, len }` clone, and `strstri_objnam`. Eight call sites that used those clones now use the tail. `sym.mjs`:

```
strstri          js/hacklib.js:343   sync
strstri_objnam   NOT FOUND in js/** (no export, no local function/const).
```

No remaining local `strstri`.

## Inventory

| JS symbol | Class | C |
|-----------|-------|---|
| `strstri` | export `hacklib.js:343` | `hacklib.c:739–779` |
| `ascii_lowc_ch` | existing helper | `lowc` (`A–Z` `\| 0x20`) |
| `from_what` pair / `STRANGLED` | caller rewrite | `attrib.c:971–975` |
| `lev_by_name` / `find_branch` | caller rewrite | `dungeon.c:2117`, `:2149` |
| `cinv_doname` | caller rewrite | `invent.c:5404–5416` |
| `name_to_monplus` / `name_to_monclass` | caller rewrite | `mondata.c:933–934`, `:1162–1164` |
| `dowrite` armour | caller rewrite | `write.c:139–142` |
| silver hit line | caller rewrite | `uhitm.c:1681` |
| `gloves_simple_name` / `killer_xname` | boolean use of the export | `objnam.c:5542`, `:1995` |

## C ↔ JS fidelity

`csym` body is `hacklib.c:739–779`. No RNG. `!*sub` returns `str` (`:752–753`). JS treats a null argument as `""` and stops each scan at the first `\\0`. Tables are `Int8Array(0x20)`. The first loop increments `tstr[*s & 31]` and `k` through `str`; the second increments `tsub` and decrements `k` through `sub`. `Int8Array` stores the `++` as a signed byte, so 128 wraps to −128 the way a gcc `char` counter does. `k < 0` returns null. `tsub[i] > tstr[i]` is the signed compare (`:764–768`). Windows run `i` from 0 through `k`. Each step compares `ascii_lowc_ch` (`hacklib.js:151–152`, `A–Z` only) and returns `s.slice(i, sEnd)` once the needle is consumed, which is `&str[i]` through the C terminator. A miss returns null.

`from_what` (`attrib.c:971–975`): `copynchars(p + 1, p + 9, BUFSZ)` (`hacklib.c:286–297`) keeps `p[0]` and copies from `p + 9`, stopping at NUL or newline. JS keeps one character of the `" pair of "` tail and cuts the copy at `\\n`. The `STRANGLED` arm is the `else if` and writes the terminator at the match. `lev_by_name` strips only when the returned tail length is 6 (`p == eos(nam) - 6`). `find_branch` passes `toTail.slice(4)` (`p + 4`). `cinv_doname` requires `otrapped` and `strlen + 9 <= QBUFSZ` (`sizeof "trapped "`), then `strsubst` on the earlier of `" locked"` and `" unlocked"`, then `"an trapped "` → `"a trapped "`. `name_to_monplus` replaces from `s + 4` with `"ex"` and drops the rest, matching `Strcpy`'s NUL. `name_to_monclass` requires the previous character to be the start or a space, and `p[len]` to be the end or a space. `dowrite` writes the 7-byte `" armor "` over the match and runs `mungspaces` on `bp + 1` only. The silver line uses the tail as a boolean. `gloves_simple_name` and `killer_xname` use the same truthiness. No branch in the new body calls `rn2`.

## Hallucinations / overclaim

The subject says the old search could match a byte outside `A–Z` and would still search a string with 128 of one nibble. That was the deleted `toLowerCase`/`indexOf` path. The new body rejects the over-subscribed nibble. Empty `sub` returns the whole JS string, including characters after an embedded NUL; a successful non-empty match is sliced at that NUL. Callers of this function do not pass that empty-sub case. The map sentence names the unwired sites and points at D-2877 for the rest of the list.

## Density

The 41-line function is the whole body. The clones that this commit deleted now call the export. Remaining C callers (`apply.c:1412`, `botl.c`, `objnam.c` wish parser, `boots_simple_name` `includes('shoes')`, and the rest of the D-2877 list) are named in `docs/c-js-map/data.md` in this commit. 151 insertions.

## Verification

Re-run: `node scripts/hidden-proxy.mjs verify strstri --base 59a5900cf~1 --reach-all`.

```
verify strstri: baseline 59a5900cf~1 (scoreboard at 40e1ad634) — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
smoke strstri: no RNG-tagged reach; fixed smoke spread (12 run, 3.4s): 12 PASS, 0 regressed → REACH-OK
```

The queue row cited 0 blocks, so the empty verify is the vacuous note. No `REGRESSED` session.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
