# Review 1819 — 905870b38 — peffect_oil (D-2860)

- SHA: `905870b38` (coverage; `potion.c` `peffect_oil`)
- Files: `js/potion.js` (+77/−27)
- Queue row: Open coverage, 0 corpus blocks cited
- Banned grep: no `FORCE`, `DIAG`, `getRngLog`, seed name, or `fastforward` in the hunk. `imports.mjs --rulecheck`: "Rule #2 clean".

## Intent vs deliverable

Subject promises the lit `likes_fire` drink, otherwise the face burn and `d((!Fire_resistance || Cold_resistance) ? 4 : 2, 4)`, then `burn_away_slime` on every lit quaff, unlit cursed castor oil, else "That was smooth!", and `exercise(A_WIS)` unless `done(DIED)` does not return. The diff is that body, `hero_element_resistance`, and `peffects` returning 0 when `gameover` is still set.

## Inventory

| JS symbol | Class | C |
|-----------|-------|---|
| `peffect_oil` | async local `potion.js:375` | `potion.c:1259–1294` (`staticfn`) |
| `likes_fire` | LIVE `monsters.js:432` | `mondata.h:196–198` |
| `Fire_resistance` | LIVE `trap.js:4811` | flats only |
| `Cold_resistance` | LIVE `zap.js:578` | flats only |
| `hero_element_resistance` | local OR of flat + `uprops` | C's one `uprops` field |
| `burn_away_slime` | LIVE `timeout.js:1623` | `timeout.c:447–453` |
| `losehp` | LIVE `hack.js:1779` | sets `_losehp_needs_done` |

`sym.mjs`: `likes_fire` `monsters.js:432` sync; `burn_away_slime` `timeout.js:1623` async; `Fire_resistance` exported from `trap.js:4811` and `zap.js:572` (this file imports the trap one); `Cold_resistance` `zap.js:578` sync. Nothing deleted.

## C ↔ JS fidelity

`csym` body is `potion.c:1259–1294`. The only call is `peffects` at `potion.c:1412`.

Lit and `likes_fire(youmonst.data)`: `"Ahh, a refreshing drink."` and `good_for_you`. The macro is fire vortex, flaming sphere, or `likes_lava`. JS compares `mndx` to those two plus `likes_lava` (`monsters.js:428–434`). Otherwise `You("burn your %s.", body_part(FACE))` — `You` is `vpline(\`You ${fmt}\`, ...args)` (`display.js:7671`). The green-slime note at `potion.c:1269–1273` is a comment. No extra damage arm.

`vulnerable = !Fire_resistance || Cold_resistance`. Cold is not read when fire resistance is absent. One `d(vulnerable ? 4 : 2, 4)`, killer `"quaffing a burning potion of oil"`, `KILLED_BY`. Then `burn_away_slime()` for every lit quaff, including the refreshing drink. Unlit cursed: `"This tastes like castor oil."` Else `"That was smooth!"`. `exercise(A_WIS, good_for_you)` last. `burn_away_slime` is `if (Slimed) make_slimed(0, "The slime that covers you is burned away!")`.

`hero_element_resistance` ORs the flat export with `uprops[FIRE_RES|COLD_RES]` intrinsic and extrinsic. `confer_oc_oprop` stores the ring on the slot and does not set `EFire_resistance`. The OR is how a ring becomes the C macro. Short-circuit matches: the cold call is inside `if (!vulnerable)`.

`losehp` is sync. On `uhp < 1` it sets `gameover` and `_losehp_needs_done` and does not call `done`. This arm awaits `finish_losehp_done` (`end.js:1720`: `"You die..."` then `done(DIED)`). A life-save clears `gameover` (`end.js:1915`) and execution continues into `burn_away_slime` and `exercise`. A real death returns from `peffect_oil` before those two, and `peffects` returns 0. `dopotion` (`potion.js:2140`) treats `retval >= 0` as already finished: `return retval ? 1 : 0`, so it does not `makeknown` or `useup`. Non-fatal low HP sets `_needs_maybe_wail`; this arm awaits `finish_maybe_wail` before slime. Polymorphed `mh < 1` still takes the fatal flag (`rehumanize` deferred inside `losehp`). Named.

## Hallucinations / overclaim

`potionhit` and `potionbreathe` are not C callers of `peffect_oil`. Saying they do not call it matches `csym --callers` (the prototype and `potion.c:1412` only). The subject does not claim `rehumanize` runs.

## Density

The whole 36-line function and its one caller. Under the 200-line floor because C is that small.

## Verification

Re-run: `node scripts/hidden-proxy.mjs verify peffect_oil --base 905870b38~1 --reach-all`.

```
verify peffect_oil: baseline 905870b38~1 (scoreboard at d2761e4d9) — 0 session(s) blocked on it (0 at baseline, 0 in the working scoreboard)
smoke peffect_oil: no RNG-tagged reach; fixed smoke spread (12 run, 3.3s): 12 PASS, 0 regressed → REACH-OK
```

The queue row cited 0 blocks, so the empty verify is the vacuous note. No `REGRESSED` session. Full 44 was skipped in the commit because `potion.js` is outside the shared-file set.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
