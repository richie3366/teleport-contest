# Review 759 — 31181641 — do_name.c mon_nam_too / monverbself + makeplural pronouns (D-1790)

## Metadata
- Full / short hash: `311816415cd9b27df2e56527162915980556e3d6` / `31181641`
- Parent: `3cb13f27` (D-1789). First Open after Must-fix emptied. No prior review QUALITY-RISK on this locus.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-03 22:57:18 +0200
- D-id: **D-1790**
- Stats: `js/do_name.js` +70/−3; `js/objnam.js` +26/−3; `js/steed.js` +8/−13; `js/mhitm.js` +1/−10; `js/apply.js` +7/−3; `js/muse.js` +7/−3. Total `js/` insertions **116** ≤250. Band **80–350**.
- Claims to close: Open `do_name.c` `mon_nam_too` + `monverbself`. Not `pronoun_gender` (D-1776). Not unwired `muse.c:218` / `:3145` / `uhitm.c:4168`.
- JS / map: `do_name.js` both exports; `objnam.js` `makeplural`; clone deleted in `mhitm.js`. `c-js-map/turns.md`.
- Archive **Addressed:** D-1790 `31181641`.

## Intent vs deliverable

Git subject promises: Match C `do_name.c` `mon_nam_too`/`monverbself` so a monster names itself in the mirror, wand and steed plines, instead of three stand-ins that dropped the reflexive.

`node scripts/csym.mjs mon_nam_too` → `do_name.c:1189–1216`. `monverbself` `:1219–1249`. `--callers monverbself`: `apply.c:1126`, `:1158`; `muse.c:184`, `:218`, `:3145`; `steed.c:429`. `--callers mon_nam_too`: `do_name.c:1230`; `mhitm.c:87/:704/:707`; `uhitm.c:4168`. `makeplural` pronoun block `objnam.c:2853–2869`. `pronoun_gender` `mondata.c:1188–1207` (`Hallucination` → `rn2(4)` first).

Parent: `monverbself` NOT FOUND; three live JS callers dropped “itself/himself”; `mhitm.js` clone used `is_neuter`/`female` (zero `rn2(4)`). The diff **does** export both from `do_name.js`, delete the clone (six `mhitm` uses rebound), port the `makeplural` pronoun block, and wire `apply.c:1126/:1158`, `muse.c:184`, `steed.c:429`. Named: three C callers with no JS site. Subject is delivered for the live JS callers.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `mon_nam_too` | LIVE new export | was CLONE in `mhitm.js` |
| `monverbself` | LIVE new | was three stand-ins |
| `makeplural` pronoun loop | LIVE repaired | C `:2853–2869` |
| `vtense` | LIVE | already; plural-stay test |
| `pronoun_gender` / `PRONOUN_HALLU` | LIVE | D-1776; `rn2(4)` |
| `genders` / `highc` | LIVE | `roles.js` / `hacklib.js` |
| `mhitm.js` clone | deleted | rebound to import |
| `muse.c:218/:3145`, `uhitm.c:4168` | OMIT named | no JS sites |

`node scripts/sym.mjs` (clone → import):

```
mon_nam_too      js/do_name.js:861   sync
monverbself      js/do_name.js:895   sync
makeplural       js/objnam.js:1511   sync
vtense           js/objnam.js:1608   sync
pronoun_gender   js/mondata.js:726   sync
genders          js/roles.js:627   sync
highc            js/hacklib.js:94   sync
             !! ALSO 1 LOCAL CLONE js/dokeylist.js:25 — do NOT add #2
mon_nam          js/do_name.js:843   sync
Monnam           js/do_name.js:953   sync
```

`--can mhitm.js do_name.js mon_nam_too`: **ALREADY**. `--can do_name.js objnam.js makeplural`: **ALREADY**. `--can apply.js/steed.js/muse.js do_name.js monverbself`: **ALREADY**. `--can do_name.js mondata.js pronoun_gender`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow in **JS**: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**`mon_nam_too` (`:1189–1216`).** `mon !== other` → `mon_nam`. Else `pronoun_gender(..., PRONOUN_HALLU)`: 0 himself, 1 herself, 3 themselves, default/2 itself. C lists `default:`/`case 2` **before** `case 3`; both are distinct labels, so 3 still hits themselves. JS puts `case 3` first then `default`/`case 2` — **same mapping**. Hallu draws `rn2(4)` **first** (`mondata.c:1199–1200`). Parent clone never drew. **Match.** Six `mhitm.js` sites now import the export.

**`monverbself` (`:1219–1249`).** `selfbuf = mon_nam_too(mon, mon)`; `verbs = vtense(selfbuf, verb)` (verb arrives plural); if `verb === verbs` (stayed plural) then `makeplural` the subject and the genders[3] fixup. Append `verbs`, optional `othertext`, `selfbuf`. **Match call order.** `vtense("themselves","rouse")` stays plural (`spot==='s'`, prev≠u/s); `"himself"` → `"rouses"`. LIVE, not a stub.

**genders[3] arm — as C writes it.** Comment wants “they”; code: `strcmpi(monnamtext, genders[3].he)` then `Strcpy(..., genders[3].him)`. `genders[3].he==="they"`, `.him==="them"`. `makeplural("It")` hits `genders[2].he` first → `"They"`, then this arm rewrites to `"Them"`. `"They"` matches none of `[0..2]` → default `s` → `"Theys"`, and `"theys" !== "they"` so no rewrite. JS the same, with a comment not to “fix” it. **Match the binary, not the comment.**

**`makeplural` `:2853–2869`.** `i=0..2`, first of he/him/his, capitalise if `oldstr[0]==highc(...)`. `"it"` → they (`.he` before `.him`). **Match.** Remaining makeplural omits named (man→men, ae/eaux).

**Live callers.** `apply.c:1125–1127` `"%s in the dark."` + othertext `"too far away to see"`. `:1157–1159` `"%s in your %s."` admire/NULL. `muse.c:183–185` `"%s with %s!"` zap/NULL + `doname`. `steed.c:429` `"%s!"` + `He` (already one `rn2(4)` via `mhe`; `monverbself` draws a second — C comment “He rouses herself”). JS strings **match**. Parent steed table also drew twice; count unchanged, wording now C’s.

**Callee closure.** LIVE: `mon_nam`, `pronoun_gender`, `vtense`, `makeplural`, `genders`, `highc`. OMIT named: `muse.c:218/:3145`, `uhitm.c:4168`, `vtense` len==2 `"o"` guard. STUB **in these arms**: **none**.

## Hallucinations / overclaim

Subject “names itself … instead of three stand-ins that dropped the reflexive” is **true** for the four wired JS sites. D-log probe listing “jackal breaths fire on itself” exercises the **helper**, not a JS `muse.c:3145` site (named omit). Do **not** stamp those three C callers ported. Do **not** “correct” Them/Theys. Do **not** stamp “Match C `pronoun_gender`” (already D-1776). Fortress 44/44 is no-regression; hallu steed rouse is public-unhit, probed.

## Density

§2b: one C pair + the `makeplural` block `monverbself` actually calls + the live JS callers + clone deletion. +116. Did **not** glue `uhitm.c:4168`. Right size.

## Verification

D-log: probe 29/29 (pronouns, vtense, `mon_nam_too`, live strings, three hallu shapes); green+strict incl. seed0383; full `sessions` 44/44. save-oracle `do_name.c:monverbself` untagged skip. Rule #2 clean. This audit: `csym` `:1189–1249` / `objnam.c:2853–2869` / `apply.c:1125` vs HEAD `js/do_name.js:861–909` / `js/objnam.js:1511–1531`.

## Actionable C-wrongs

None for Must-fix. Named: `muse.c` `mplayhorn` `:218`; fire-breath `:3145`; `uhitm.c:4168`; `vtense` len==2 `"o"`; remaining `makeplural` irregulars. Do **not** rewrite genders[3] to the comment. Do **not** restore the `mhitm.js` `is_neuter` clone. Do **not** drop the second `rn2(4)` in `kick_steed`.

Verdict: **ACCEPT-WITH-DEBT**
