# Review 798 — c306d211 — mkmaze.c makemaz astral (D-1828)

## Metadata
- Full / short hash: `c306d211c58413c93723c82a2a50ac52753b8aaa` / `c306d211`
- Parent: `e782f134` (D-1827). Map-driven Open. Endgame plane 5 of 5.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 22:26:50 +0200
- D-id: **D-1828**
- Stats: `js/mklev.js` +319/−2; `js/do.js` +3/−2. `js/` insertions **322** >250. Band **80–450**.
- Claims to close: Open `astral`. `%d` via `convert_line`. Not Knight/Rogue.
- JS / map: `load_astral`; `deliver_splev_message`. `c-js-map/data.md`. Archive **Addressed:** D-1828 `c306d211`.

## Intent vs deliverable

Git subject promises: no `astral` loader (blank maze); C loads `dat/astral.lua`; arrival `%d` skipped `convert_line`.

`node scripts/csym.mjs makemaz` → `mkmaze.c:1126–1223`. `load_special` `:6453–6502`. `create_altar` `sp_lev.c:2445–2486` (`shrine==2` → `priestini(..., TRUE)` then `AM_SHRINE|AM_SANCTUM`). `priestini` `priest.c:219–276`. `deliver_splev_message` `questpgr.c:654–664` → `deliver_by_pline` `:422–436` `convert_line` then `pline("%s")`. `--callers convert_line`: `:433`, `:450`, `:606`.

The diff **does** add `load_astral` + proto branch, and `deliver_splev_message` now `convert_line`s each non-empty line.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_astral` | LIVE new | lua body |
| `priestini` | LIVE clone | `mklev.js:19534`; sanctum=true |
| `convert_line` | LIVE callee | `questpgr.js:633` |
| `lua_random2` | LIVE | `lo+rn2(hi-lo+1)` ≡ Lua `math.random(4,9)` |
| `splev_irregular_oroom` / `selection_floodfill` / `solidify_map` | LIVE | |
| `astralDoor` | CLONE | `sel_set_door` |
| TEMPLE `filled=2` rest | OMIT named | `FILL_LVFLAGS` / `has_temple` only |
| humidity / `ensure_way_out` / `m_dowear` / `G_UNIQ` | OMIT named | |

`node scripts/sym.mjs`:

```
load_astral      NOT EXPORTED — 1 LOCAL mklev.js:9427
convert_line     js/questpgr.js:633   sync
priestini        NOT EXPORTED — 1 LOCAL mklev.js:19534
deliver_splev_message js/do.js:2035   ASYNC — await required
lua_random2      NOT EXPORTED — 1 LOCAL :14233
solidify_map     NOT EXPORTED — 1 LOCAL :1920
```

No clone→import. FORCE/DIAG/`getRngLog`/`fastforward`: **none**. Rule #2: clean.

## C ↔ JS fidelity

**Flags / message / map.** lua `:8–12` mazelevel+noteleport+hardfloor+nommap+shortsighted+solidify; three messages. JS lev_message newline-joined; `convert_line` expands `%d`. Map 20-line temple string vs lua `:13–34` including trailing spaces. String-form lit=FALSE then lava-forced lit. **Match.**

**Wings.** `percent(60)` twice; i=1 area(17,14)–(30,18) / i=2 (44,14)–(57,18); wallify; temp `|` then floodfill then `.`; `math.random(4,9)` Angels `align=noalign` + 50% extra. JS `lua_random2(4,9)` inclusive; `hallMon` `selection_rndcoord(hall,true)` ≡ `rndcoord(1)`. **Match that RNG.**

**Courts / sanctums / doors.** Irregular OROOMs flood from (1,5)/(31,1)/(61,5). Three TEMPLE rects. Altars (7,9)/(37,5)/(67,9) from shuffled `align[1..3]` → `splev_align[0..2]` after `nhlib_shuffle_align` Fisher-Yates `rn2(i)`. `create_altar` shrine=2: `priestini(..., true)` then `AM_SHRINE|AM_SANCTUM`. Nine doors closed/locked match `:93–101`. Nondig+nonpass 00,00–74,19 (STWALL/TREE/bars).

**Monsters.** Moloch west/south/east + Pestilence/Death/Famine `place:rndcoord(1)` (removeit). Aligned-horde coords/align/peaceful bits match `:139–176`. 3×L, 3×V, 3×D. **Match those lists.**

**Epilogue.** link / cleanup / wallify / `flip_level_rnd(3)` / `solidify_map` / `fixup_special`. **Match C `load_special` tail for solidify.**

**Callee closure.** One proto. `priestini` / `convert_line` / `splev_create_monster` LIVE. Named OMITs only. No STUB in a shipped arm.

## Hallucinations / overclaim

Do **not** stamp `fill_special_room` beyond `FILL_LVFLAGS`, humidity, `m_dowear`, or `G_UNIQ`. `%d` is `convert_line`, not a hardcoded deity. Public 44/44 does not walk Astral. Vacuous hidden verify is not a corpus PASS.

## Density

§2b: one remaining endgame proto. +322. Did **not** glue Knight quest. Right size.

## Verification

This audit: `node scripts/hidden-proxy.mjs verify makemaz --base c306d211~1` → `0 session(s) blocked` / `no corpus session is blocked on it`. Queue row was a blank-plane Open. lua `:38–187` vs HEAD `:9513–9731`. D-log green + cohort + full 44/44.

## Actionable C-wrongs

None that must block the next port. Named stay on the map.

Verdict: **ACCEPT-WITH-DEBT**
