# Review 1427 — 981a50f7 — do_look whole-body port (D-2468)

Metadata: SHA `981a50f7`, `js/pager.js` only (+208/−74 net per
numstat 208 ins). Coverage gap → whole `do_look` + clicklook +
supplemental lore. D-log: D-2468.

## Intent vs deliverable

Promise: `do_look(mode, click_cc)` in C order — quick/clicklook
gates, cmdq `goto dowhatiscmd`, full switch, describe loop,
`do_supplemental_info`, menu suppression arms. Diff ships all of
that plus the `do-look-clicklook` test (2/2). No second subsystem.

## Inventory

- Changed: `export async function do_look(mode = 0, click_cc = null)`
  (C global `pager.c:1672–1963`, 292 L).
- Added: module-local `do_supplemental_info` (C staticfn
  `:2252–2316`), `SUPTEXT1`/`SUPTEXT2` consts, `WHAT_IS_A_LOCATION`.
- Changed: `whatis_menu_choice` — swallowed/hallucination
  suppression + lootabc compat keys.
- Edges: `mungspaces` (getline.js LIVE `js/getline.js:1288`,
  replaces an inline trim/regex clone in the `?` arm),
  `is_orc` (monsters.js LIVE `:585`). Both ALREADY edges.
- No deleted/re-pointed clones for `sym.mjs` (both imports are
  pre-existing exports, not local clones).

## C ↔ JS fidelity

- Mode gates (`:1675–1676`): `quick = mode==1`,
  `clicklook = mode==2` — exact.
- cmdq `goto dowhatiscmd`: `have_cmdq` tracks it; canned input runs
  the switch even in clicklook mode, clicklook-without-cmdq skips
  the switch and takes `cc` from `click_cc` with `sym = 0`,
  `from_screen = FALSE` (`:1802–1807`) — exact, including the C
  subtlety that clicklook+cmdq ignores `click_cc` (JS only assigns
  it when `!have_cmdq`). JS `sym = 0` init is a safe equivalent for
  C's conditionally-unset `sym` (every switch path reaching the
  loop sets it).
- Switch arms `q/y///i/?/m/M/o/O/t/T/e/E` in order; `?` arm keeps
  the single-space guard (`!== ' '`, = C `strcmp`) then live
  `mungspaces`; `out_str[1]` → `length > 1`; ESC/`\0` return —
  exact.
- Loop: per-round holder reset; getpos only `if (from_screen)`
  inside `if (from_screen || clicklook)`; `do_screen_description`
  always called with `(from_screen || clicklook)` (`:1917`);
  `putmixed` → single-arg `pline` literal (named, D-0330; DUMPLOG
  block retired D-1776); checkfile gate
  (`found==1`, `ans` kinds, `help && !quick`) gains `!clicklook` (=
  C) plus `do_supplemental_info(supplemental_name,
  supplemental_pm, ans == LOOK_VERBOSE)`; loop condition gains
  `!clicklook`; verbose save/restore — all exact.
- `do_supplemental_info`: `is_orc && len < BUFSZ-1` gate, `" of "` /
  `" the Fence"` search, QBUFSZ-truncated `y_n` prompt
  (`slice(0, QBUFSZ-1-(len+2))` = C `copynchars`), `bp+4` gang via
  `slice(4)` (`strstri` returns the C-pointer-like suffix, verified
  `js/hacklib.js:261`), `subs++ ? gang : fullname`, C's `*bp =
  '\0'` correctly omitted (no observable effect — `fullname` was
  already copied, `name` unused after). SUPTEXT1/2 verified
  byte-identical against `pager.c:2231–2251` incl. trailing spaces
  and the two-vs-one `%s` counts.
- Menu: `/`+`i`+`?` always; separator + m/M/o/O/t/T/e/E only when
  `!uswallow && !Hallucination` (`:1755`); lootabc abandons `y`/`n`
  compat; `^`/`"`/`` ` ``/`|` map to t/T/e/E only when lootabc off
  and rows present (C: no entry → no accelerator → bell; JS
  falls through to re-prompt) — exact.
- Callers: C `dowhatis`/`quickwhatis` (`:2324/2331`) stay wired
  (`js/pager.js:2749/2754`); default `click_cc = null` keeps them.

## Hallucinations / overclaim

None. "Dispatch ported, callee stubbed" does not occur — every new
callee is LIVE; windowing/`putmixed`/`decode_mixed`/GC omits are
named with mechanism reasons.

## Density

One 292-line C function + static lore helper, one module: right-sized.

## Verification

- `hidden-proxy verify do_look --base 981a50f7~1 --reach-all`
  (re-run): 0 blocked both sides — vacuous, as the D-log states
  ("no corpus session blocked", "the function draws no RNG").
  Smoke 24/24 PASS → REACH-OK. Matches. Plus
  `do-look-clicklook.test.mjs` 2/2 (cited in D-log).
- Diff grep: no FORCE/DIAG/`getRngLog`/seed/fastforward/coords
  (only `_pending_message` more() idiom, pre-existing).

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
