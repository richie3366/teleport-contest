# Review 760 — 2619827e — eat.c newuhs hunger messages / faint / end_running (D-1791)

## Metadata
- Full / short hash: `2619827e111364320cf663317dc7a9bd300734bb` / `2619827e`
- Parent: `d38ea9c7` (docs-only gap ranking; previous `js/` SHA `31181641` D-1790). Map-driven Open. No prior QUALITY-RISK on this locus.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-03 23:32:37 +0200
- D-id: **D-1791**
- Stats: `js/eat.js` +215/−40; `js/hack.js` +43/−9; `js/cmd.js` +7/−55; plus await rebinds. Total `js/` insertions **254** >250 → ceiling **450**. Band **80–450**.
- Claims to close: Open `eat.c` `newuhs`. Not `gethungry` ring polish. Not `sit.js` lay-egg.
- JS / map: `eat.js` `newuhs`/`unfaint`; `hack.js` `end_running`; cmd clone deleted. `c-js-map/turns.md`.
- Archive **Addressed:** D-1791 `2619827e`.

## Intent vs deliverable

Git subject promises: Match C `eat.c` `newuhs` so hunger messages, faint/starve and `end_running` actually run, instead of a field-update stub.

`node scripts/csym.mjs newuhs` → `eat.c:3361–3513`. `unfaint` `:3335–3344`. `end_running` `hack.c:4129–4158`. `nomul` `:4160–4173` calls `end_running(TRUE)`. `--callers newuhs`: eat internals + fountain/potion/uhitm/polyself. `--callers morehungry` includes `sit.c:394`.

```3369:3416:nethack-c/upstream/src/eat.c
    newhs = (h > 1000) ? SATIATED : (h > 150) ? NOT_HUNGRY
            : (h > 50) ? HUNGRY : (h > 0) ? WEAK : FAINTING;
    /* occupation == eatfood || force_save_hs → stash, return */
    if (newhs == FAINTING) {
        /* sgn*trunc; is_fainted → FAINTED;
           u.uhs <= WEAK || rn2(20 - uhunger_div_by_10) >= 19 */
```

Parent: 14-line `u.uhs = newhs`. cmd.js `end_running` always zeroed `multi`. The diff **does** port the C body, `unfaint` afternmv, async `gethungry`/`morehungry`, and move `end_running` to `hack.js` (`multi` only when `> 0`). Subject is true for the helper and the callers this SHA awaited.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `newuhs` | LIVE repaired | now async |
| `unfaint` | LIVE new (C `staticfn`) | `afternmv`; not a second clone |
| `gethungry` / `morehungry` / `lesshungry` | LIVE async | callers awaited except sit.js |
| `end_running` | LIVE new export | cmd.js clone **deleted** |
| `Hear_again` / `bot` / `selftouch` / `classify_terrain` | LIVE | |
| `incr_itimeout_prop` | CLONE of `incr_itimeout` | TIMEOUT bits; `HDeaf` |
| `sit.js` `morehungry` | OMIT named | `sit.c:394`; **unawaited** |
| `findtravelpath` `end_running(FALSE)` | OMIT named | C `hack.c:1276` |
| `gt.travelmap` `selection_free` | OMIT named | |
| `polyself.c:431/:436` | OMIT named | no JS caller |

`node scripts/sym.mjs` (clone → import / new async):

```
newuhs           js/eat.js:518   ASYNC — await required
unfaint          NOT EXPORTED — 1 LOCAL js/eat.js:453   (C staticfn)
end_running      js/hack.js:940   sync
gethungry        js/eat.js:666   ASYNC — await required
morehungry       js/eat.js:764   ASYNC — await required
Hear_again       js/eat.js:1933   sync
bot              js/display.js:5566   ASYNC — await required
selftouch        js/trap.js:2853   ASYNC — await required
eatfood / opentin NOT EXPORTED — locals in eat.js (C staticfn)
classify_terrain js/hack.js:2073   sync
```

`--can eat.js hack.js end_running`: **ALREADY**. `--can cmd.js hack.js end_running`: **ALREADY**. `--can eat.js trap.js selftouch`: **ALREADY**. `--can eat.js display.js bot`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**Thresholds / occupation stash (`:3369–3408`).** Same cutoffs. `eatfood` / `force_save_hs` save first `uhs`, set `newhs`, return; meal end restores `save_hs` then computes the endpoint message. JS `game.occupation === eatfood`. **Match.**

**Faint RNG (`:3410–3431`).** `uhunger_div_by_10 = sgn(h)*trunc((abs(h)+5)/10)`. `is_fainted` → `FAINTED`. **`u.uhs <= WEAK` short-circuits `rn2`.** Else `rn2(20 - div) >= 19`. Then `!is_fainted && multi >= 0`: stop, pline, `incr_itimeout(&HDeaf, duration)`, `nomul(-duration)`, `afternmv = unfaint`, `newhs = FAINTED`, `!Levitation` → `selftouch("Falling, you")`. JS the same `||` order. `Levitation` inlined as `(H\|\|E) && !B` (`youprop.h:240`). **Match RNG.** Already-fainted still draws when `uhs > WEAK`.

**Starve (`:3437–3446`).** `uhunger < -(100+10*ACURR(A_CON))` is the **else** of the faint `if` — a successful faint roll never starves this call. `done(STARVING)` then return (lifesave re-enters `newuhs`). **Match.**

**ATEMP / messages / `end_running` (`:3450–3511`).** WEAK crossover ±1 on `ATEMP(A_STR)` before the pline. HUNGRY/WEAK hallu vs incr vs Wizard/Elf/Valkyrie `"needs food, badly!"` (`urole.name.m` vs `"Elf"`). `incr && occupation && != eatfood && != opentin` → `stop_occupation`. Then `end_running(TRUE)`. `bot()`; HP < 1 exhaustion `done(STARVING)`. SATIATED/NOT_HUNGRY/FAINTED have **no** switch pline. **Match.**

**`end_running` (`:4129–4158`).** If `run`: clear run; `flags.time` → `time_botl`; `terrainstatus` → `terrain_typ = MAX_TYPE` + `classify_terrain`. `and_travel` clears travel/travel1/mv. **`multi` only when `> 0`.** Parent cmd clone zeroed `multi` always (would smash `nomul(-duration)`). **Match C; clone gone.** `nomul` now calls `end_running(true)` (`:4171`). Named: `travelmap`; `end_running(FALSE)` at `hack.c:1276`.

**Callee closure (`newuhs`).** LIVE: `eatfood`/`opentin` identity, `rn2`, `Hear_again` (`rn2(2)`), `nomul`, `selftouch`, `end_running`, `bot`, `done`, `stop_occupation`, `Hallucination`, `acurr`. CLONE: `incr_itimeout_prop` (TIMEOUT add). OMIT named: sit.js await; travelmap; findtravelpath FALSE; polyself callers. STUB **inside `newuhs`**: **none**.

## Hallucinations / overclaim

Subject “hunger messages, faint/starve and `end_running` actually run” is **true** of the helper and awaited callers. Do **not** stamp “Match C `sit.c:394`.” `sit.js:1115` still `morehungry(...)` **without** `await` after this SHA made it async — named, still a live JS hole (uhunger decrements; `newuhs` races). Do **not** stamp “Match C `end_running(FALSE)`.” Do **not** stamp “Match C `make_deaf`” (`Hear_again` still clears `HDeaf` TIMEOUT only). Probe 25/25 is thresholds/ATEMP/stash/faint-short-circuit, not sit-egg.

## Density

§2b: one C function + `unfaint` + the `end_running` clone it needs + await fan-out. +254. Did **not** glue `gethungry` ring polish / `findtravelpath`. Right size; large only because of the cmd rebind.

## Verification

D-log: probe 25/25; green; cohort 8/8 incl. seed1800 eat + seed0361 `gethungry`. save-oracle skip. Rule #2 clean. Public suite can hit Hungry via `gethungry` — not public-unhit. This audit: `csym` `:3361–3513` / `:4129–4158` vs HEAD `js/eat.js:518–651` / `js/hack.js:940–960`.

## Actionable C-wrongs

None for Must-fix. Named: `sit.js` lay-egg `await morehungry` (`sit.c:394`); `hack.c:1276` `end_running(FALSE)`; `gt.travelmap`; `polyself.c:431/:436`; `reset_faint` `:3353`; `Hear_again` `make_deaf`. Do **not** restore cmd.js `multi = 0` always. Do **not** draw faint `rn2` when `uhs <= WEAK`. Do **not** bless mapping/create (unrelated).

Verdict: **ACCEPT-WITH-DEBT**
