# Review 804 — 68aa6457 — invent.c getobj wear/puton/throw/drink/remove (D-1834)

## Metadata
- Full / short hash: `68aa6457b557cda232d8ee777e01a54c6c75651a` / `68aa6457`
- Parent: `6a441166` (D-1833). Map-driven Open: 7 corpus blocks on `getobj`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-05 01:32:00 +0200
- D-id: **D-1834**
- Stats: `js/do_wear.js` +104/−101; `dothrow.js` +34/−118; `potion.js` +9/−164; `cmd.js` +8/−2; `invent.js` +2/−1. `js/` insertions **157** ≤250. Band **80–350**.
- Claims to close: empty wear/puton “else”, drink missing-letter `--More--`, throw ranks, `'R'` → `doremring`. Not leftover WIN_STATUS.
- JS / map: live `getobj` + `equip_ok`/`throw_ok`. `c-js-map/turns.md`. Archive **Addressed:** D-1834 `68aa6457`.

## Intent vs deliverable

Git subject promises: local `getobj_wear`/`_puton`/`_throw`/`_drink` always prompted when empty; throw ranks swapped; `'R'` unbound.

`node scripts/csym.mjs getobj` → `invent.c:1751–2089`. Empty `:1912–1914`. Missing letter `:2058–2062`. `equip_ok` `do_wear.c:3403–3447`. `wear_ok`/`puton_ok`/`remove_ok` `:3450–3468`. `throw_ok` `dothrow.c:316–348`. `doremring` `:1873–1889`. `dodrink` `potion.c:525–615` (`drink_ok_extra` `:535–571`).

The diff **does** delete those four clones, wrap live `getobj`, add `equip_ok`/`throw_ok`, bind `'R'`. Underwater `drink_ok_extra` still named-omitted.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `getobj` | LIVE callee | wear/puton/throw/drink/remove |
| `equip_ok` / `wear_ok` / `puton_ok` / `remove_ok` | LIVE new | C staticfns |
| `throw_ok` | LIVE repaired | now GETOBJ_* not THROW_SUGGEST |
| `doremring` | LIVE new | rhack `'R'` |
| `drink_ok_extra++` fountain/sink | LIVE | |
| `getobj_wear`/`_puton`/`_throw`/`_drink` | **deleted** | |
| remaining getobj_* drop/wield/apply/write/takeoff/dip | OMIT named | |
| `canwearobj` polyform / underwater / Strangled | OMIT named | |

`node scripts/sym.mjs` (deleted clones + re-points):

```
getobj           js/invent.js:6931   ASYNC — await required
equip_ok         NOT EXPORTED — 1 LOCAL do_wear.js:1808
throw_ok         NOT EXPORTED — 1 LOCAL dothrow.js:193
wear_ok / puton_ok / remove_ok  NOT EXPORTED — 1 LOCAL each
doremring        js/do_wear.js:1235   ASYNC
getobj_wear / getobj_puton / getobj_throw / getobj_drink  NOT FOUND
drink_ok         NOT EXPORTED — 1 LOCAL potion.js:268
```

FORCE/DIAG/`getRngLog`/`fastforward`: **none**. Rule #2: clean.

## C ↔ JS fidelity

**Empty / missing (`:1912–1914`, `:2058–2062`).** Live `getobj` already had `You don't have anything %sto %s` and `You don't have that object.` The clones never hit them. Wear/puton/drink now do. **Match those arms.**

**`equip_ok` (`:3403–3447`).** `!obj` EXCLUDE. `removing ^ is_worn` → JS `!==` EXCLUDE_INACCESS. Class gate + meat-ring/blindfold/towel/lenses. `accessory ^ (oclass != ARMOR)` DOWNPLAY. Armor `!removing && !canwearobj(..., FALSE)` DOWNPLAY — JS `canwearobj_silent` (named polyform). Removing `inaccessible_equipment` EXCLUDE_INACCESS. **Match the ranks C actually compiles.**

**`throw_ok` (`:316–348`).** welded DOWNPLAY; AutoReturn+Mjollnir STR SUGGEST; lone uwep/uswapwep DOWNPLAY; coins / `!uslinging` weapons / sling gems / `throws_rocks` boulder SUGGEST; else DOWNPLAY. **Match.** Comment “hands EXCLUDE” is `!obj`, not a fourth return.

**`doremring` (`:1873–1889`).** No worn → pline. `Naccessories != 1 || ParanoidRemove || cmdq` → `getobj("remove", remove_ok)`. Else the single accessory from `count_worn_stuff`. rhack `'R'`. **Match that control flow.**

**`dodrink` (`:540–571`).** Fountain/sink `n` increments `drink_ok_extra` so empty getobj says “else”. Underwater named. **Match the shipped yn.**

**Callee closure.** One `getobj` caller cluster. Deleted clones. `equip_ok`/`throw_ok` LIVE. Remaining getobj_* OMIT named. No STUB in a shipped live arm.

## Hallucinations / overclaim

Do **not** stamp drop/wield/apply/write/takeoff/dip clones, full `canwearobj` polyform, underwater/Strangled drink, or `item_action_in_progress`. `random-seed0200` → `js-throw` is a later owner, not NO MOVEMENT presented as an omit.

## Density

§2b: five `getobj` callers that shared the empty/rank bug. +157 (net clone delete). Did **not** glue `describe_decor`. Right size.

## Verification

This audit, `js/` at `68aa6457`: `node scripts/hidden-proxy.mjs verify getobj --base 68aa6457~1` → `7 session(s) blocked`. Summary: **`5 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS`** (`random-seed0015` → `menu_remarm` step 42; `random-seed0200` → `js-throw` step 29). Matches the D-log. Not vacuous.

## Actionable C-wrongs

None that must block the next port. Named stay on the map.

Verdict: **ACCEPT-WITH-DEBT**
