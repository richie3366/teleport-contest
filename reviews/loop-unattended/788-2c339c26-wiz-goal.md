# Review 788 — 2c339c26 — mkmaze.c makemaz Wiz-goal load_special (D-1818)

## Metadata
- Full / short hash: `2c339c26b73d8c2f08461dbed6d1af150fc536e1` / `2c339c26`
- Parent: `0ac444d3` (hidden-proxy). Map-driven Open. No prior QUALITY-RISK on this proto.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 19:42:34 +0200
- D-id: **D-1818**
- Stats: `js/mklev.js` +196/−6; `js/timeout.js` −1 comment (seed token). `js/` insertions **196** ≤250. Band **80–350**.
- Claims to close: Open `makemaz` `Wiz-goal` from `dat/Wiz-goal.lua` (Wiz 4/5 → 5/5). Not Bar-goal.
- JS / map: `js/mklev.js` `load_wiz_goal` + `load_special_proto`. `c-js-map/data.md`. Archive **Addressed:** D-1818 `2c339c26`.

## Intent vs deliverable

Git subject promises: `makemaz` had no `Wiz-goal` loader; C loads `dat/Wiz-goal.lua` via `sp_lev.c` `load_special`.

`node scripts/csym.mjs makemaz` → `mkmaze.c:1126–1223`. Success path `:1188–1193` `load_special` then `dmonsfree` return. `--callers` not needed for the proto body; the lua is the spec. `get_table_align` `sp_lev.c:3114–3128` looks up **`"align"`**, default `"random"`. `lspo_altar` `:4281–4317` `type` default `"altar"` → shrine=0. `create_altar` `:2445–2486`. `sel_set_door` `:4646–4662`. `create_monster` `:1925+`.

Parent: `load_special_proto` dispatched Wiz-strt/loca/fila/filb only. The diff **does** add `load_wiz_goal` and the proto string. `timeout.js` is comment-only.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_wiz_goal` | LIVE new | lua body as JS |
| `load_special_proto` | LIVE repaired | `Wiz-goal` arm |
| `splev_initlev` / `splev_apply_centered_map` | LIVE | |
| `add_room` / `light_region` | LIVE | temple FILL_LVFLAGS; lit grows |
| `wizDoor` | CLONE of `sel_set_door` | no `set_door_orientation` |
| `mkstairs` / `create_object` / `splev_create_object` / `splev_create_trap` | LIVE | |
| `splev_create_monster` / `mk_mplayer` / `christen_monst` | LIVE | rogue/wizard → mplayer |
| `induced_align` | LIVE local | missing lua `"align"` key |
| humidity `get_location` | OMIT named | |
| `spo_end_moninvent` `m_dowear` | OMIT named | |
| `fill_special_room` TEMPLE | OMIT named | beyond `has_temple` |
| `G_UNIQ` extinct early return | OMIT named | already on `splev_create_monster` |
| fakewiz | OMIT named | |

`node scripts/sym.mjs`:

```
load_wiz_goal    NOT EXPORTED — 1 LOCAL mklev.js:4305 (do NOT add #2)
splev_create_monster js/mklev.js:12415   sync
create_object    NOT EXPORTED — 1 LOCAL mklev.js:12582
christen_monst   js/do_name.js:413   sync
induced_align    NOT EXPORTED — 1 LOCAL mklev.js:18605
mk_mplayer       js/mplayer.js:169   sync
splev_create_object / splev_create_trap / light_region — 1 LOCAL each
```

`--can mklev.js mplayer.js mk_mplayer`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none** in the new loader (timeout comment *removed* a seed token).

## C ↔ JS fidelity

**Dispatch.** `protofile === 'Wiz-goal'` before Pri-strt. C `load_special("Wiz-goal"+LEV_EXT)`. **Match the missing arm.**

**Map / flags.** lua `solidfill` + `mazelevel` + the 20-line map. JS `LVLINIT_SOLIDFILL` STONE, `is_maze_lev`, `splev_apply_centered_map`. Door/region coordinates are map-relative + `splev_xstart/ystart`.

**Temple + lit (`lspo_region`).** Table region `{13,10,18,12}` `type="temple"` `filled=2` → `add_room` + `needfill = FILL_LVFLAGS`. Argc-2 `selection.area` lit/unlit: JS `light_region` grows when lit (`:18433–18437`), not when unlit. **Match those arms.**

**Doors.** 16 lua `des.door("locked", …)` vs JS list — same cells, `D_LOCKED`. C `sel_set_door` keeps SDOOR, else DOOR, then `set_door_orientation` + `doormask`. JS `wizDoor` keeps SDOOR / sets DOOR / mask, **skips** `set_door_orientation` (`mklev.js:10349` exists). Map `+`/`S` already ran orientation at apply; this is still a thinner clone.

**Altar.** lua `aligned="noncoaligned"` is **not** C’s `"align"` key → default `AM_SPLEV_RANDOM` → `induced_align(80)` (`:1907–1917`). `type="altar"` → shrine=0 → `create_altar` returns before `priestini` even inside TEMPLE (`:2477–2478`). JS does that XOR and skips shrine. **Match C’s binding, not the lua identifier.**

**Objects / traps / monsters.** Eye via `create_object` named + bless (`curse_state: 1`) + spe 0. 15 `des.object()` / 6 `des.trap()`. Dark One at (16,11); 11× class B `peaceful=0`; 7× i; 8× vampire bat; 1× i; eight captives. `splev_create_monster` role ids → `mk_mplayer`; JS then `christen_monst` Pug/Newt (the helper’s christen omit). Counts match the lua.

**Post.** `link_doors_rooms` → `remove_boundary_syms` → `map_cleanup` → `wallification` → `flip_level_rnd(3,false)` → `fixup_special`. **Match `load_special` tail.**

**RNG.** `induced_align(80)`; find_montype gender; random object/trap/monster `get_location`; flip. No FORCE. Humidity-aware `get_location` stays named — not presented as a completed omit.

**Callee closure.** Live arms use LIVE `create_object` / `splev_create_*` / `mk_mplayer` / `christen_monst` / `induced_align`. `wizDoor` is a verified-thin CLONE. Named OMITs only. No STUB in a shipped arm.

## Hallucinations / overclaim

Do **not** stamp “Match C lua `aligned=noncoaligned`.” Do **not** stamp humidity `get_location`, `m_dowear`, TEMPLE fill, or `G_UNIQ` extinct. Do **not** add a second `load_wiz_goal`. `timeout.js` is not a Deaf re-port.

## Density

§2b: one proto, one C `load_special` family. +196. Did **not** glue Bar-goal. Right size.

## Verification

`verify.mjs --fn makemaz` → PASS syntax; PASS rule2; **PASS hidden (no corpus session blocked on makemaz)**; PASS green 2/2; PASS strict; PASS cohort 7/7; PASS full 44/44 (shared `mklev.js`). This audit: lua 132 lines vs HEAD `load_wiz_goal` `:4305–4490`; `csym` `makemaz` `:1126–1223`, `get_table_align` `:3114–3128`, `create_altar` `:2445–2486`, `sel_set_door` `:4646–4662`. Rule #2 at end-of-iter.

## Actionable C-wrongs

None that must block the next port (named omits stay in the map). Residual clone: `wizDoor` vs C `sel_set_door` `:4659` `set_door_orientation` — same thin door helper as other quest maps, not a new Must-fix family.

Verdict: **ACCEPT-WITH-DEBT**
