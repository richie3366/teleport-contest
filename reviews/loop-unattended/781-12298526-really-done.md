# Review 781 — 12298526 — end.c really_done remaining callees (D-1812)

## Metadata
- Full / short hash: `122985268c1dcf59c5541eeb7cb7c0dc57c9cd1a` / `12298526`
- Parent: `9b42dedf` (D-1811 AWD). Map-driven Open.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 07:33:39 +0200
- D-id: **D-1812**
- Stats: end +84; vault +72; trap +54; allmain +20; priest +19; files +18; invent +12. `js/` insertions **~279** >250 → ceiling **450**. Band **80–350**.
- Claims to close: Open `end.c` `really_done` remaining: fixup_death / launch / clearlocks / pickinv / timet_delta / clearpriests / paygd. Not DUMPLOG.
- JS / map: those callees wired in C order. `c-js-map/turns.md`. Archive **Addressed:** D-1812 `12298526`.

## Intent vs deliverable

Git subject promises: Match C `end.c` `really_done` so those seven actually run, instead of skipping them after paybill.

`node scripts/csym.mjs really_done` → `end.c:1129–1590`. `fixup_death` `:364–384`. `paygd` `vault.c:1204–1247`. `clearpriests` `priest.c:918–929`. `clearlocks` `files.c:732–750`. `free_pickinv_cache` `invent.c:3043–3050`. `timet_delta` `allmain.c:995–1001`. `launch_drop_spot` `trap.c:3221–3233` / `launch_in_progress` `:3235–3241` / `force_launch_placement` `:3243–3250`. `--callers paygd`: `end.c:1239`. `--callers clearpriests`: `end.c:1240`. `--callers clearlocks`: `end.c:1244` (+ cmd/tty named). `--callers launch_in_progress`: `end.c:1203`. `--callers launch_drop_spot`: `trap.c:3361` plus nine clear sites.

Parent: `// paygd / clearpriests deferred`. The diff **does** call them in C order plus launch/clearlocks/pickinv/`timet_delta`. Subject is delivered. DUMPLOG stays retired.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `really_done` | LIVE repaired | still local in `end.js` |
| `fixup_death` / `death_fixups` | LIVE local | C `staticfn` + table `:352–361` |
| `paygd` | LIVE | `vault.js` |
| `clearpriests` | LIVE | `priest.js` |
| `clearlocks` | LIVE | JSON analogue; POSIX **OMIT named** |
| `free_pickinv_cache` | LIVE thin | sentinel; setter **OMIT named** |
| `timet_delta` | LIVE | contest `getnow` seconds |
| `launch_drop_spot` | LIVE local | C `staticfn` |
| `launch_in_progress` / `force_launch_placement` | LIVE | `trap.js` |
| `delete_levelfile` / `findgd` / `make_grave` / `mnexto` | LIVE | |

`node scripts/sym.mjs` (clone → import / new):

```
really_done      NOT EXPORTED — 1 LOCAL end.js:918
fixup_death      NOT EXPORTED — 1 LOCAL end.js:387
clearlocks       js/files.js:455   sync
free_pickinv_cache js/invent.js:3057   sync
timet_delta      js/allmain.js:1090   sync
clearpriests     js/priest.js:478   ASYNC
paygd            js/vault.js:946   ASYNC
launch_in_progress js/trap.js:2070   sync
force_launch_placement js/trap.js:2078   sync
launch_drop_spot NOT EXPORTED — 1 LOCAL trap.js:2056
```

`--can end.js vault.js paygd` / files / priest / invent: **ALREADY**. `--can end.js trap.js force_launch_placement`: **IN-SCC SAFE** (hoisted). `--can end.js allmain.js timet_delta`: **NEW-CYCLE SAFE** (dynamic import; hoisted). FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**Order in `really_done` (`:1146–1244`, `:1376–1378`).** `gameover`; `something_worth_saving=0`; `done_hup` → `done_stopprint++`; `vision_inited=false`; `done_object_cleanup`; `perm_invent=false`; `endtime=getnow()`; `urealtime.finish_time` + `timet_delta`; `at_night`/`at_midnight`; `bones_ok`; if bones `launch_in_progress` → `force_launch_placement`; QUIT/ESCAPED killer; **`fixup_death`**; `paybill` then **`paygd`/`clearpriests`**; **`clearlocks`**; later **`free_pickinv_cache`**. **Match that sequence.**

**`fixup_death`.** STONING/`getting stoned`/unmulti; STARVING/`fainted from lack of food`→`fainted`. `strcmp` ≡ exact string. **Match.**

**`paygd`.** `!umoney \|\| !grd` return. `uinvault` → pline + drop at hero. Peaceful off-vault → `mongone` **without** placing gold (C `goto remove_guard`). Hostile → `mnexto(RLOC_NOMSG)`, grave at `rooms[vroom].lx/ly+rn2(2)`, then coin walk. **Match.** `pmname` accepts mndx.

**`clearpriests`.** `fmon` skip dead; `ispriest && !on_level(shrlevel, u.uz)` → `mongone`. **Match.**

**`clearlocks`.** `preserve_locks` return; `for (x = n_dgns ? maxledgerno() : 0; x >= 0; x--) delete_levelfile(x)`. POSIX signal **named**. JSON analogue (Rule #2). **Match the loop.**

**Launch.** `launch_drop_spot` at `:3361` start cell; null at C `:3404`/`:3411`/`:3427`/`:3457`/`:3487`/`:3498`/`:3511`/`:3553`/`:3567`. `force_launch_placement` does **not** clear `launchplace` (C same). **Match.**

**`timet_delta` / `newgame`.** `allmain.c:835–836` `realtime=0; start_timing=getnow()` + `something_worth_saving++`. Contest datetime freeze → deltas 0. Other `timet_delta` callers **named**.

**Callee closure.** Every subject callee is LIVE or a named POSIX/window analogue. No silent stub in the death path this SHA claimed.

## Hallucinations / overclaim

Do **not** stamp “Match C POSIX `signal`/`sethanguphandler`.” Do **not** stamp “Match C `destroy_nhwindow` on `cached_pickinv_win`” — setter still omitted. Do **not** stamp “Match C DUMPLOG / `record_achievement` ACH_BLND/NUDE/UWIN” (not this peel). Do **not** stamp “Match C `grddead`.” Insight/`savegamestate`/`#suspend`/`#shell` `timet_delta` remain named. Public death screens may hit paybill; vault-guard `paygd` / rolling-boulder bones are **public-unhit**.

## Density

§2b: one `really_done` callee cluster + the `launch_obj` marks those bones need. +279. Did **not** glue `untrap`. Right size.

## Verification

D-log: save-oracle skip; green + cohort 7/7. This audit: `csym` ranges above vs HEAD `js/end.js:918+`, `vault.js:946`, `priest.js:478`, `files.js:455`, `invent.js:3057`, `trap.js:2056–2084`, `allmain.js:700–704` / `:1090`. Rule #2 clean.

## Actionable C-wrongs

None for Must-fix. Named: POSIX clearlocks; `grddead`; pickinv cache setter; other `timet_delta` callers; DUMPLOG.

Verdict: **ACCEPT-WITH-DEBT**
