# Review 1579 — d0319e92 — cmd.c act_on_act whole-body port (D-2620)

**Metadata:** SHA `d0319e92`, `cmd.c` `act_on_act` + `cmdq_add_dir` /
`cmdq_add_userinput` + `doclicklook`, D-2620. JS: `js/cmd.js` only
(+301/−4).

## Intent vs deliverable

Subject promises: the whole `[t]herecmdmenu` action dispatcher (36 MCMD
arms queueing CQ_CANNED ec/key/dir/userinput nodes) plus its two queue
helpers and the clicklook command — no same-named JS symbol existed.
Diff actually adds: `act_on_act`, `doclicklook`, `move_funcs_walk`,
completed MCMD enum, otyp consts, both queue helpers, three static-edge
joins (`sgn`, `docallcmd`, `dotalk`). The row's `there_cmd_menu_far`
dead-callee note is corrected in-message (sibling, not callee).
Promise matches deliverable.

## Inventory

- `act_on_act(act, dx, dy)` (new module-local) — C `:4658–4838`.
- `doclicklook()` (new file-local) — C `:5381–5392`.
- `cmdq_add_dir` / `cmdq_add_userinput` (new exports) — C `:294–331`.
- `move_funcs_walk`, MCMD ids, otyp consts (new data).
- No deleted symbol; `doidtrap` / `auto_describe` are new lazy refs to
  nonexistent exports (`sym.mjs`: both NOT FOUND — pasted below).

## C ↔ JS fidelity

C locus `cmd.c:4657–4838` (182 L, via `csym.mjs act_on_act`; callers
`:4880`/`:4892` disclosed unwired — next2u/far builders are own rows).
Full C body read here. Arm-by-arm confirm against the C text:

- sgn clamp with throw/travel/look raw (`:4666–4677`) — exact
  (`sgn` live `js/eat.js:2687`).
- TRAVEL stamp + `dotravel_target` (`:4680–4688`); the queued clone at
  cmd.js:2740 verified against C `:5347–5377` (isok gate, already-here,
  travel/travel1) — verified CLONE.
- OPEN / LOCK skeleton→pick→card chain / UNTRAP / KICK / CLOSE / SEARCH
  (`:4694–4725`) — exact, `carrying` canonical (`js/hack.js:3038`,
  cmd.js:107 import).
- LOOK_TRAP ec+dir (`:4726–4729`) — dispatch exact; the queued closure
  lazy-imports `doidtrap` from pager.js, which does not exist yet
  (see below).
- MOVE_DIR / ATTACK_NEXT2U via `move_funcs_walk` (`:4734` / `:4756`):
  first-column order W/NW/N/NE/E/SE/S/SW matches C `:2070–2078` —
  exact (`xytodir` live `js/const.js:244`).
- RIDE / REMOVE_SADDLE (`do_reqmenu` is the 'm' PREFIXCMD, cmd.js:352)
  / APPLY_SADDLE carrying-guard / TALK / NAME('m') / QUAFF('y') /
  DIP(userinput+'y') / SIT / UP / DOWN / DISMOUNT / MONABILITY /
  PICKUP / LOOT / TIP('y') / EAT('y') / DROP / INVENTORY / REST /
  LOOK_HERE (`:4738–4818`) — exact, C 'y' keys kept.
- LOOK_AT clicklook_cc stamp + `doclicklook` (`:4819–4823`) — exact;
  `doclicklook` matches C `:5380–5390` (isok guard, move=FALSE,
  auto_describe, ECMD_OK) except the callee is missing (below).
- UNTRAP_HERE dir(0,0,1) / OFFER dosacrifice+userinput / CAST_SPELL /
  default no-op (`:4824–4837`) — exact.
- MCMD ids verified against the C enum `:4379–4418` (sequential 0–36,
  every new const exact).
- `cmdq_add_dir` / `cmdq_add_userinput` match C `:294–331` tail-append
  shapes (DIR dirx/diry/dirz; USERINPUT key `'\0'` per the getline.js
  node shape).

RNG: none in C, none in JS. Dountrap/dodip/dosit/doride/domonability/
dosacrifice/dothrow/doopen/doapply/dokick targets all LIVE (async,
`sym.mjs`-confirmed); `dotalk` LIVE (`js/sounds.js:1670`),
`docallcmd` LIVE (`js/do_name.js:1532`), `sgn` LIVE, `do_reqmenu`
live in-file.

Callee closure: `doidtrap` NOT FOUND in js/**; `auto_describe` NOT
FOUND in js/** (both `sym.mjs` outputs pasted in this review). Both
are OMIT — named in `turns.md` in this commit with `pager.c:2336` /
`getpos.c:640` cites — so both arms may ship under the rule. Both
closures are currently unreachable: `act_on_act`, `doclicklook`, and
both queue helpers have no JS caller yet (only `js/cmd.js` references
them), which the commit discloses. Hardening note (not a C-wrong, no
Must-fix): when executed before the callees land, the closures throw
a bare `TypeError` (destructured `undefined`) rather than the file's
loud `named omit` Error — a future wiring iter should guard them.

## Hallucinations / overclaim

None. "Match C" is claimed for dispatch; the two stubbed callees are
explicitly named with their 1:1 homes, not sold as live.

## Density

Whole-function row, 301 lines in one module (>250 js/ insertions —
ceiling 450 applies; this file is well under it). Right-sized.

## Verification

- `node scripts/imports.mjs --rulecheck` → Rule #2 clean (whole `js/`).
- Diff grep: 0 `FORCE`/`DIAG`/`getRngLog`/`fastforward`/RNG/seed/
  coordinate reads in added lines (sole "seed8000+seed0900" hit is the
  commit message's strict line).
- Re-measured: `hidden-proxy.mjs verify act_on_act --base d0319e92~1
  --reach-all` → `0 session(s) blocked` at baseline and working tree
  (vacuous-note path, honestly labeled) + `smoke 24/24 PASS, 0
  regressed → REACH-OK`. Both summary lines cited; green/strict/
  cohort per D-log.

## Actionable C-wrongs

None. (Named omits stay in the map, not Must-fix.)

Verdict: **ACCEPT**
