# Review 290 — b21765a2 — mhitu.c gazemu + mattacku AT_GAZE (D-1328)

## Metadata
- Full / short hash: `b21765a2f222d53dc6b7403e2b746040c8bc748a` / `b21765a2`
- Parent: `2c9dff6a` (D-1327). This file audits **this SHA only**. Archive **Addressed:** D-1328 `b21765a2` already has the short hash (filled by D-1329).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-21 02:13:22 +0200
- D-id: **D-1328**
- Stats: 11 files, +459 / −49 — `js/mhitu.js` +378 / −~20; `js/mon.js` +9 / −2; `js/monmove.js` comment.
- Claims to close: Open `mhitu.c` gazemu (named from D-1314 / review **276**). Not explmu. Not mhitu AD_DRIN. `reviews/loop-2026-08-15/` has no unpaid gazemu Must-fix.
- JS / map: `mhitu.js` `gazemu` / `mattacku` `AT_GAZE`; `mon.js` `m_respond_medusa`; `c-js-map/turns.md`. gazemm / `arti_reflects` W_WEP / mhitu AD_DRIN still named at this SHA.
- Prior reviews this SHA claims to close: **276** named `gazemu` as a **no-op** after `m_respond_medusa` walked AT_GAZE; **271** named gaze with explmu/hugs.

## Intent vs deliverable

Git subject promises: “Match C mhitu.c gazemu so an AT_GAZE monster actually gazes (Medusa stone/reflect, umber confuse, pyrolisk fire), instead of falling out of mattacku.”

C `mattacku` (`mhitu.c:832–837`):

```
        case AT_GAZE: /* can affect you either ranged or not */
            /* Medusa gaze already operated through m_respond in
               dochug(); don't gaze more than once per round. */
            if (mdat != &mons[PM_MEDUSA])
                sum[i] = gazemu(mtmp, mattk);
            break;
```

C `m_respond_medusa` (`mon.c:4109–4118`): first `AT_GAZE` slot → `gazemu`. C `gazemu` (`mhitu.c:1668–1898`): `m_seenres` miss; `Reflecting && couldsee && is_medusa` → `reflectable`; `(Hallucination && rn2(4)) || (Unaware && !reflectable)` forces `cancelled`; switch AD_STON / CONF / STUN / BLND / FIRE; `#ifdef PM_BEHOLDER` AD_SLEE/AD_SLOW (MON is `#if 0` in this tree — compiled out); cancelled looks-X `reactions[]` with Hallu `rn2(3)` reshuffle + prefix `rn2(3)`/`rn2(2)`.

Old JS: `mattacku` no `AT_GAZE`; `m_respond_medusa` `void atks[i]`.

The diff **does** the full switch (minus BEHOLDER), skip-Medusa by **mndx** (D-0928), and dynamic-import `gazemu` from `m_respond_medusa`. It does **not** port `arti_reflects(MON_WEP)` / W_WEP identity in `ureflects`. Named. It does **not** port gazemm or mhitu AD_DRIN.

## Inventory

| Symbol | Kind | Notes |
|--------|------|--------|
| `gazemu` | C `:1668–1898`, **new export** | not a stub |
| `mattacku` `AT_GAZE` | C `:832–837`, **wired** | skip `PM_MEDUSA` via `mndx` |
| `m_respond_medusa` | C `:4109–4118`, **wired callee** | was no-op; now `await gazemu` |
| `m_seenres` / `cvt_adtyp_to_mseenres` | C `mondata.c`, **imported live** | |
| `Hallucination` | C `youprop.h`, **imported live** | `do_name.js` H && !resist (sticky `u.Hallucination` OR is pre-existing) |
| `Unaware` | C `youprop.h`, **clone** | `multi<0 && (usleep\|\|u.Unaware)`; `unconscious`/`is_fainted` named |
| `Reflecting` | C `youprop.h`, **clone** | H\|\|E plus otyp stand-in; setworn `EReflecting` bits named |
| `ureflects` | C `muse.c:2836–2866`, **clone** | outermost→in; `arti_reflects` W_WEP named |
| `mon_reflects` | C `muse.c:2797–2833`, **clone** | shield/amulet/silver DSM/silver+chromatic dragon; `arti_reflects(MON_WEP)` named |
| `killed` / `polymon` / `done(STONING)` | C, **imported live** | |
| `make_confused` / `make_stunned` | C `potion.c`, **imported live** | |
| `make_blinded` | C `potion.c`, **local clone** | TIMEOUT + vision; talk unused (`FALSE`) |
| `burnarmor` / `destroy_items` / `ignite_items` / `burn_away_slime` | C, **imported live** | FIRE arm |
| `shieldeff` / `monstseesu` / `monstunseesu` / `m_canseeu` | C, **imported live** | |
| BEHOLDER AD_SLEE/AD_SLOW | C `#ifdef`, **compiled out** | correct omit |
| gazemm | C `mhitm.c`, **named omit** | |

No `FORCE` / `DIAG` / `getRngLog(` / `readFileSync` / `from 'fs'` / `node:` / `fastforward` writes / seed names / recorded coordinates. Rule #2 clean. **New gameplay RNG on AT_GAZE:** `m_seenres` miss burns none; Hallu `rn2(4)` when Hallucinating; AD_STON Hallu serpent `!rn2(3)`; CONF/STUN/FIRE `rn2(5)` then `d()` + `rn2(6)` on `mspec_used`; BLND `d(damn,damd)` / cancelled `rn1(2,2)` / Archon `rn2(5)` / stun `rnd(3)`; FIRE two `lev > rn2(20)` plus resist `d(12,6)` golem; looks-X `rn2(3)` / `rn2(SIZE)` / prefix `rn2(3)`/`rn2(2)`. Medusa skip in `mattacku`: **no** gazemu RNG that round (`m_respond` already did).

## C ↔ JS fidelity

`m_seenres` early miss matches `:1684–1685`. `reflectable` is Reflecting && `couldsee(mx,my)` && Medusa — C `:1688`. Cancel assignment matches `:1698–1699`. AD_STON cancelled: silent if `!canseemon`; Unaware react 4/2; Hallu `!rn2(3)` serpent-cut else ineffectual / “doesn’t look all that ugly” (`react` is still −1 here so C `!react` is also false — both skip the ugly string at this site). Reflect path: `ureflects` then `mon_reflects` (null fmt when `!useeit`) then `!m_canseeu` notice-break then `killed` with `context.stoned`; live agr → break; else `M_ATTK_AGR_DIED`. Meet-gaze: `canseemon && couldsee && !Stone_resistance && !Unaware` then `poly_when_stoned`+`polymon(STONE_GOLEM)` or `urgent_pline` + `done(STONING)`. `poly_when_stoned(ptr, mvitals)` is the live `monsters.js` export (G_GENOD bit). Killer `pmname` uses `female ? FEMALE : MALE` vs C `Mgender` — Medusa is female; named-quality clone, not a drop on the stone path.

AD_CONF/STUN: `mcanseeu && !mspec_used && rn2(5)` then cancelled react or `d(3,4)`/`d(2,6)` + `mspec_used += dice + rn2(6)`. Confusion test is `HConfusion \|\| u.Confusion` vs C `Confusion` ≡ `HConfusion` only — extra sticky OR is pre-existing prop debt, not a rewrite of this if’s RNG order.

AD_BLND: `canseemon && !resists_blnd_you() && dist2u <= BOLT_LIM²`. Archon cancelled `rn2(5)` clears react. Eyes-clear vs `max(oldstun, rnd(3))` matches `:1812–1819`. `resists_blnd_by_arti` named on the clone.

AD_FIRE: same `rn2(5)` gate; resist `shieldeff` + `d(12,6)` `ugolemeffects` + `dmg=0`; `burn_away_slime`; two independent `lev > rn2(20)` burnarmor / destroy+ignite; leftover `mdamageu`. Callees live.

Looks-X after switch: Hallu `rn2(3)` then `rn2(8)` reactions; prefix `!rn2(3)` empty else already `"quite "` else `rn2(2)` a-bit/somewhat. Match `:1887–1895`. Always `return M_ATTK_MISS` except AD_STON AGR_DIED.

`mattacku` skip Medusa by `mdat.mndx !== PM_MEDUSA` is C `mdat != &mons[PM_MEDUSA]` under D-0928 (do not compare `mons()` identity). `m_respond_medusa` now actually calls `gazemu` — review **276**’s “dispatch exists; callee is a no-op” is **closed**. Dynamic `import('./mhitu.js')` is the mon↔mhitu cycle, not filesystem.

Clones (`ureflects`/`mon_reflects`/`Reflecting` otyp) compensate named setworn `EReflecting` bits and omit `arti_reflects`. That is named debt on reflection **identity**, not a stub `gazemu`. A shield-of-reflection hero still reflects via otyp OR bits; a reflecting **weapon** artifact is the named omit.

Hallucination check: the subject claimed gazemu body, not “Match C `arti_reflects`.” Callee is the ported function, not a no-op.

## Hallucinations / overclaim

Subject + D-1328 say an AT_GAZE monster actually gazes (Medusa stone/reflect, umber confuse, pyrolisk fire). **Those arms plus skip-Medusa plus `m_respond_medusa`→`gazemu` are the hunk.** Stamping **Addressed:** D-1328 is fair. Do **not** stamp “Match C `arti_reflects(uwep)`.” Do **not** stamp “Match C gazemm.” Do **not** stamp “Match C BEHOLDER sleep/slow” (compiled out). Do **not** stamp “Match C mhitu AD_DRIN.” Do **not** treat fortress PASS as a Medusa reflect kill.

## Density

One C function (~230 lines) plus its two call sites (`mattacku`, `m_respond_medusa`) and the `ureflects`/`mon_reflects` pair C calls on the STON path. ~300 JS lines — top of §2b, not “half of `mon.c`.” AD_DRIN correctly not glued. Right size.

## Branch-by-branch confirm

1. `m_seenres`: miss, no further RNG. Match `:1684`.
2. Hallu `rn2(4)` → cancelled. Match `:1698`.
3. Unaware && !reflectable → cancelled. Match (clone Unaware).
4. Cancelled Medusa, cansee: ineffectual / serpent-cut. Match `:1705–1718`.
5. Reflecting Medusa, `mon_reflects` false, `m_canseeu`: `killed` stoned / AGR_DIED. Match `:1721–1745`.
6. Meet-gaze, stone-resist: no `done(STONING)`. Match `:1747`.
7. Umber `rn2(5)` confuse / `d(3,4)`. Match `:1760–1774`.
8. Pyrolisk FIRE `rn2(5)` / resist / two `rn2(20)`. Match `:1824–1852`.
9. `mattacku` Medusa: skip. Match `:835–836`.
10. `m_respond` Medusa: `gazemu` live. Match `:4115`.
11. `arti_reflects` W_WEP. Named omit.
12. **Public-unhit** unless a session faces AT_GAZE (seed0002 reflection is hero Reflecting vs zaps, not Medusa).

## Anti-pattern / Rule #2 (this SHA `js/`)

No FORCE / fs / seed-named gates. `PM_MEDUSA` mndx compare is D-0928, not a seed. Dynamic import is ESM cycle. Plain ESM.

## Verification

Journal: private canary **20**/20; green+strict seed8000/0900; cohort **7**/7 + strict 1500/1800/0012/0004/0007/2200/0383. **Public-unhit** on monster gaze. Cadence this audit: full `sessions` at HEAD `a7a5a835` **44**/44 Scr **11,405**/11,405 RNG **792,838**/792,838 (100%) speed `37+0.30/turn` (R² 0.84). I did not re-run the private canary. Fortress PASS is not evidence `gazemu` `rn2(4)` fired.

## Actionable C-wrongs

None for Must-fix. Branch order and RNG on AD_STON/CONF/STUN/BLND/FIRE match C `:1668–1898`. `gazemu` is not a stub. Review **276**’s Medusa no-op is gone.

Named omits (map, not Must-fix):

1. `arti_reflects` W_WEP (`muse.c:2807` / `ureflects` W_WEP bit)
2. gazemm (`mhitm.c`)
3. full `Unaware` (`unconscious` / `is_fainted`)
4. mhitu AD_DRIN (next SHA)

Do not Must-fix BEHOLDER arms (`#if 0` MON). Do not Must-fix `sprintf2` for `%s %s`. Do not wrap `wildmiss` as `pline_mon`.

## Callers / RNG ledger

C: `dochug` → `m_respond_medusa` → `gazemu`; non-Medusa `mattacku` AT_GAZE → `gazemu`. JS: same. Public fortress is not evidence Hallu `rn2(4)` or pyrolisk `rn2(5)`.

## Verdict

- Verdict: **ACCEPT-WITH-DEBT**
- One sentence: AT_GAZE now runs live `gazemu` (Medusa skip in `mattacku`, `m_respond` actually gazes, STON/CONF/FIRE RNG); reflecting artifacts stay named.
- Must-fix stays empty for this SHA; archive **Addressed:** D-1328 `b21765a2` already filled by the next port commit.
