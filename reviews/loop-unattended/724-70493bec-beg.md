# Review 724 — 70493bec — sounds.c beg (D-1763)

## Metadata
- Full / short hash: `70493becc09c143bc94db929f6bfd1862d976215` / `70493bec`
- Parent: `ac94ec34` (D-1762). This file audits **this SHA only** (sixth of nine `js/` commits since review **718**). Archive **Addressed:** D-1763 `70493bec`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-03 05:15:52 +0200
- D-id: **D-1763**
- Stats: `js/sounds.js` +42/−3. Total `js/` insertions **42** <250. Band **150–350**.
- Claims to close: Open `beg` after D-1762. Not `maybe_gasp`. Not `dog_hunger`. `reviews/loop-2026-08-15/` has no unpaid beg Must-fix.
- JS / map: `sounds.js` `beg`. `c-js-map/data.md`.
- Prior: **723** named `beg`.

## Intent vs deliverable

Git subject promises: `sounds.c` `beg` so helpless/diet gate then animal `domonnoise`, humanoid `map_invisible`+SetVoice+verbalize, and middle famished pline instead of omitting the helper after D-1762.

`node scripts/csym.mjs beg` → `sounds.c:518–542`. `--callers beg`: `dogmove.c:383` (`dog_hunger`); `extern.h:3004`. `dog_hunger` `:361–394`. `helpless` `monst.h:251`. `is_silent` `mondata.h:62`. `carnivorous`/`herbivorous` `mondata.h:90–91`. `canspotmon` `display.h:129`. `map_invisible` `display.c:377–385`.

```518:542:nethack-c/upstream/src/sounds.c
void
beg(struct monst *mtmp)
{
    if (helpless(mtmp)
        || !(carnivorous(mtmp->data) || herbivorous(mtmp->data)))
        return;
    if (!is_silent(mtmp->data) && mtmp->data->msound <= MS_ANIMAL) {
        (void) domonnoise(mtmp);
    } else if (mtmp->data->msound >= MS_HUMANOID) {
        if (!canspotmon(mtmp))
            map_invisible(mtmp->mx, mtmp->my);
        SetVoice(mtmp, 0, 80, 0);
        verbalize("I'm hungry.");
    } else {
        if (canspotmon(mtmp))
            pline("%s seems famished.", Monnam(mtmp));
    }
}
```

Parent: no `beg`; `dog_move` omits `dog_hunger`. The diff **does** port the C body (helpless/diet; animal `domonnoise`; humanoid I-glyph+SetVoice+verbalize; middle famished). It **does not** wire `dog_hunger` into `dog_move`. Named. It **does not** add `is_silent` clone #2 (inlines `msound !== MS_SILENT`).

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `beg` `:518–542` | LIVE new | async; no production caller |
| `helpless` | CLONE local | sounds.js:609 ≡ `monst.h:251` |
| `carnivorous` / `herbivorous` | LIVE import | monsters.js |
| `is_silent` | CLONE inlined | `msound !== MS_SILENT`; region.js keeps #1 |
| `domonnoise` | LIVE | same module |
| `map_invisible` / `canspotmon` | LIVE import | display.js |
| `SetVoice` / `verbalize` | LIVE | D-1752 / display |
| `dog_hunger` | OMIT named | NOT FOUND |

`node scripts/sym.mjs`:

```
beg              js/sounds.js:462   ASYNC
helpless         NOT EXPORTED — 6 LOCAL  sounds.js:609  => Do NOT write #7
carnivorous      js/monsters.js:908   sync
herbivorous      js/monsters.js:911   sync
is_silent        NOT EXPORTED — 1 LOCAL  region.js:205  => Do NOT write #2
domonnoise       js/sounds.js:787   ASYNC
dog_hunger       NOT FOUND
map_invisible    js/display.js:1004   sync
canspotmon       js/display.js:996   sync
```

Re-points: none deleted. New: `beg`; imports `map_invisible`/`canspotmon`/`carnivorous`/`herbivorous`. `node scripts/imports.mjs --can sounds.js display.js map_invisible` / `monsters.js carnivorous`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed names: **none**. `node scripts/imports.mjs --rulecheck`: **Rule #2 clean**.

## C ↔ JS fidelity

**Gate (`:521–523`).** `helpless || !(carnivorous || herbivorous)` return. JS local `helpless` is `msleeping || !mcanmove` (`monst.h:251`; comment wrongly says mondata.h). LIVE diet macros. **Match.**

**Animal (`:526–527`).** `!is_silent && msound <= MS_ANIMAL` → `domonnoise`. JS `msound !== MS_SILENT && msound <= MS_ANIMAL`. `MS_ANIMAL=17` (`monflag.h`; same as `MS_TRUMPET`). LIVE `domonnoise`. **Match.** Deaf/canseemon live inside `domonnoise`, not here.

**Humanoid (`:528–532`).** `msound >= MS_HUMANOID` (25). `!canspotmon` → `map_invisible(mx,my)`; `SetVoice(mtmp,0,80,0)`; `verbalize("I'm hungry.")`. JS the same. **Match.** Contest SetVoice empty.

**Middle (`:533–540`).** Else (SHREIK..MUMBLE plus SILENT if it reached here) `canspotmon` → famished pline. JS the same. **Match.** SILENT is excluded by the first arm (`is_silent`) so it lands here — C comment. JS too.

**Caller (`dogmove.c:380–383`).** `cansee` → confused-hunger `pline_mon`; else `couldsee` → `beg`; else You_feel worried. JS `dog_hunger` NOT FOUND; `dog_move` does not call `beg`. Named.

**RNG.** `beg` itself burns none; `domonnoise` may. Unwired → zero public extra. seed0006 still PASS at D-1764 (after this SHA). **Match.**

**Callee closure.** LIVE: `carnivorous`, `herbivorous`, `domonnoise`, `canspotmon`, `map_invisible`, `SetVoice`, `verbalize`, `Monnam`, `pline`. CLONE verified: `helpless`; `is_silent` inline. OMIT named: `dog_hunger`. STUB: **none**. Not “dispatch ported, callee stubbed.”

## Hallucinations / overclaim

Subject “helpless/diet then animal/humanoid/middle”: **true**. D-log “did not wire `dog_hunger`”: **true**. Do **not** stamp “Match C `dog_hunger`/`dog_starve`.” Do **not** add `is_silent` #2. Journal “fortress held” is not a hungry-pet screen. **Public-unhit.** Admit that. Cadence FAILs start at D-1765, not here.

## Density

§2b: one C function (`beg`). +42. Did **not** glue `dog_hunger` or `peacefuls_respond`. Did **not** reopen D-1762.

## Verification

D-log: save-oracle skip (untagged `sounds.c:beg`); node canary (helpless / no-diet / SILENT+laugh unseen / Deaf animal `domonnoise` / humanoid verbalize); green+strict seed8000/0900; CURRENT cohort **7**/7 + strict. Rule #2 clean. Hungry-pet **public-unhit**. Admit that.

## Actionable C-wrongs

None for Must-fix (`beg` matches C; remaining named). Named: `dog_hunger` / `dog_starve`; `peacefuls_respond`. Do **not** add `beg` #2. Do **not** add `is_silent` #2. Do **not** add `helpless` #7. Do **not** wire `dog_move`→`dog_hunger` in the same peel as this helper.

Verdict: **ACCEPT-WITH-DEBT**
