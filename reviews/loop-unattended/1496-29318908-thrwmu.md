# Review 1496 — 29318908 — mthrowu.c thrwmu (D-2537)

## Metadata

- SHA: `29318908`
- D-id: D-2537. Next index: 1496.
- Files: `js/mthrowu.js` (+55/−20ish, one function + 3 import
  words).
- C locus: `nethack-c/upstream/src/mthrowu.c:1173–1264`
  (`thrwmu`, 92 L).

## Intent vs deliverable

Subject promises: whole `thrwmu` in C order (THIN → live),
retiring the polearm-arm omit. Diff actually adds: the
`:1198–1240` polearm arm inside `thrwmu_body`, with the
autoreturn/lined-up tails re-commented in C order. Promise
matches deliverable.

## Inventory

- Changed: `thrwmu_body` (polearm arm added; autoreturn +
  lined-up arms kept, comment-only touch).
- Changed: +`MON_POLE_DIST` (const.js specifier), +`mswings_verb`
  (mhitu.js), +`pline_mon` (display.js).
- No symbol deleted or re-pointed; no clone→import paste owed.

## C ↔ JS fidelity

`csym.mjs` body `:1173–1264` vs JS, in order:

- `:1186–1191` wield gate (`NEED_WEAPON || !MON_WEP` →
  NEED_RANGED_WEAPON, nonzero `mon_wield_item` returns).
  `!== 0` awaited. Confirm.
- `:1194–1196` `select_rwep`, `!otmp` returns. Confirm.
- `:1198–1240` polearm: must-be-wielded return; `rang =
  dist2(mx,my,mux,muy)` vs `MON_POLE_DIST`/`couldsee` return;
  `canseemon`-gated `pline_mon '%s %s %s.'` with `Monnam`,
  `mswings_verb(otmp, rang <= 2)` (bash iff adjacent — C
  `(rang <= 2) ? TRUE : FALSE`, JS boolean arg identical),
  `obj_is_pname ? the : an`. Then `dmgval(otmp, youmonst)`,
  `hitv = 3 − distmin`, `−4` clamp, `bigmonst` bump,
  `+ 8 + spe`, `dam < 1 → 1`, `thitu(hitv,
  Maybe_Half_Phys(dam), &otmp, NULL)` (JS `{ obj: otmp }`
  holder, discarded — nothing reads it after), `return`.
  Confirm, every line.
- `mswings_verb` verified two levels: C (`mhitu.c:103–126`,
  via `csym`) mixes thrust/swing with positional `rn2(2)`;
  JS (`js/mhitu.js:354`) reproduces the lash/thrust/rn2
  chain and is `sym.mjs`-sync — safe as an unawaited
  `pline_mon` argument, draw preserved. Confirm.
- `:1241–1247` autoreturn `else if` with C short-circuit
  (`autoreturn_weapon` → `!mwelded`), range/`couldsee`
  return, `always_toss = true` set once. Confirm.
- `:1249–1259` `!lined_up || (URETREATING &&
  (!always_toss && rn2(BOLT_LIM − distmin)))` — the
  `!always_toss`-before-`rn2` short-circuit kept, so tethered
  weapons draw no retreat roll. URETREATING expansion
  (dist-vs-old-dist) pre-existing and untouched. Confirm.
- `:1261–1263` `monshoot` + `nomul(0)`. Confirm.
- New edge audit: `mswings_verb` import is on the mhitu.js
  edge — `--can mthrowu.js mhitu.js mswings_verb`: ALREADY,
  no new edge (the D-log's arg-order note covers the
  evaluation side). MON_POLE_DIST/pline_mon extend existing
  specifiers. Confirm.

Callee closure: all LIVE/file-local/imported-above
(`mon_wield_item`, `select_rwep`, `is_pole`, `dist2`,
`couldsee`, `canseemon`, `xname`, `Monnam`, `mswings_verb`,
`obj_is_pname`, `the`/`an`, `dmgval`, `distmin`, `bigmonst`,
`thitu`, `maybe_half_phys`, `stop_occupation`,
`autoreturn_weapon`, `mwelded`, `lined_up`, `monshoot`,
`nomul`). Omits: none. No STUB in any live arm.

## Hallucinations / overclaim

None. The retired polearm omit is exactly the added arm; the
"one C caller wired" (thrwmu's own caller, unchanged) is not
oversold.

## Density

One 92-line C function, one arm added, one file. Right-sized
per §2b.

## Verification

- D-log: syntax (1 changed) · rule2 · hidden note (0 blocked)
  · smoke 24/24 · green 2/2 · strict ×2 · cohort 7/7 · full
  skipped (no shared file changed) → VERIFY: PASS (plus the
  `--reach-all` re-run, identical).
- Re-run here: `hidden-proxy.mjs verify thrwmu
  --base 29318908~1 --reach-all` → 0 blocked both trees
  (vacuous note, honestly reported) + smoke 24 PASS,
  0 regressed → REACH-OK. Matches.
- Diff grep: no FORCE/DIAG/getRngLog/fastforward/seed/coordinate
  logic.

## Actionable C-wrongs

None. No Must-fix, no CURRENT change.

Verdict: **ACCEPT**
