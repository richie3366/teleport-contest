# Review 761 — 9c160502 — timeout.c nh_timeout dialogues + stone_luck (D-1792)

## Metadata
- Full / short hash: `9c16050247f7e5543370405f1edce116332d9cc1` / `9c160502`
- Parent: `2619827e` (D-1791). Map-driven Open. No prior QUALITY-RISK on this locus.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-03 23:57:25 +0200
- D-id: **D-1792**
- Stats: `js/timeout.js` +478/−20; `js/eat.js` +65/−6; `js/attrib.js` +21/−1; `js/hack.js` +1/−1. Total `js/` insertions **546** >250 → ceiling **450**. Band **80–450**.
- Claims to close: Open `timeout.c` `nh_timeout` property dialogues + `stone_luck`. Not `make_blinded`. Not `region_dialogue`.
- JS / map: `timeout.js` luck + seven dialogues; `attrib.js` `stone_luck`; `eat.js` `Popeye`; `hack.js` `carrying` export. `c-js-map/turns.md`.
- Archive **Addressed:** D-1792 `9c160502`.

## Intent vs deliverable

Git subject promises: Match C `timeout.c` `nh_timeout` so stoned/slime/vomiting/choke/sickness/levitation/phaze dialogues and luck timeout actually run, instead of skipping the countdown plines and `uluck` step.

`node scripts/csym.mjs nh_timeout` → `timeout.c:587–948`. Luck `:597–621` **before** `if (u.uinvulnerable) return` `:622`. Dialogues `:623–641`. `stone_luck` `attrib.c:421–437`. `Popeye` `eat.c:3915–3956`. Callees: `stoned_dialogue` `:136–185` … `phaze_dialogue` `:533–543`. `--callers nh_timeout`: `allmain.c:273`.

Parent: no luck block; no those dialogues; STONED/… flats never entered generic `--`. The diff **does** run luck first, dialogues after the invulnerable return, add those props to `TIMEOUT_FLAT`, export `carrying` (not clone #5), port `stone_luck`/`Popeye`. Named: `region_dialogue`/`sleep_dialogue`; STONED/SLIMED `done_timeout`. Subject is true for luck + the seven wired dialogues.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `nh_timeout_luck` | LIVE new (local) | C `:597–621` |
| `stone_luck` | LIVE new | `attrib.c:421` |
| `carrying` | LIVE export | was local; 3 clones remain |
| `confers_luck` | LIVE | artifact.js |
| seven dialogues | LIVE new (local) | C staticfns |
| `Popeye` + inlined `polyfood` | LIVE new | `eat.c:3915`; `obj.h:321` |
| `region_dialogue` / `sleep_dialogue` | OMIT named | C `:553` / `:267` — **not called** |
| STONED/SLIMED expiry | OMIT named | silent `--` still |
| `surface()` Underwater | OMIT named | levi uses `hliquid` |

`node scripts/sym.mjs` (new + clone→export):

```
stone_luck       js/attrib.js:585   sync
Popeye           js/eat.js:3320   sync
confers_luck     js/artifact.js:430   sync
carrying         js/hack.js:2229   sync
             !! ALSO 3 LOCAL CLONES (dog/quest/shk) — do NOT add #5
nh_timeout       js/timeout.js:697   ASYNC — await required
```

`--can timeout.js attrib.js stone_luck`: **ALREADY**. `--can timeout.js eat.js Popeye`: **ALREADY**. `--can timeout.js hack.js carrying`: **ALREADY**. `--can attrib.js artifact.js confers_luck`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**Luck before invulnerable — match C.** `baseluck`: FULL_MOON +1, friday13 −1, `killed_leader` −4, archeologist+fedora +1. Period 300 if `uhave.amulet || ugangr` else 600. `uluck != baseluck && moves % period == 0`: `time_luck = stone_luck(FALSE)`, `nostone = !carrying(LUCKSTONE) && !stone_luck(TRUE)`, then `--` good luck if `nostone || time_luck < 0`, `++` bad luck if `nostone || time_luck > 0`. **Two invent walks, no RNG.** Then `uinvulnerable` return — dialogues and `--` skip; luck already ran. **Match.**

**`stone_luck` (`:421–437`).** `confers_luck`: cursed −quan, else if blessed or `include_uncursed` +quan; `sgn`. JS inlines sgn. `polyfood` inlines `is_shapeshifter` ≡ `mflags2 & M2_SHAPESHIFTER` (`mondata.h:94` / `pm_to_cham`). **Match.**

**Dialogue order (`:623–637`).** Stoned, Slimed, Vomiting, Strangled, Sick, `HLevitation & TIMEOUT`, `HPasses_walls & TIMEOUT`. JS the same seven `if`s. **Does not** call `region_dialogue` / `sleep_dialogue` (`:638–641`) — named.

**RNG call-for-call in wired dialogues.** `stoned_dialogue`: no `rn2`; `Popeye(STONED)` at i==4. `slime_dialogue`: Hallu `rndmonnam` on `%s` arms except i==4 `hcolor` if `!Blind`. `vomiting_dialogue`: switch `(TIMEOUT-1)` **before** `--`; case 6 `d(2,4)` stun then FALLTHROUGH case 9 `d(2,4)` confuse (JS has no `break` after 6). `choke_dialogue`: `Breathless || !rn2(50)` → texts2+neck else texts+blue+`stop_occupation`. JS `hero_magical_breath` covers C `Breathless` (`H\|\|E Magical_breathing \|\| breathless(form)`). `sickness_dialogue`: odd TIMEOUT, no own `rn2`; Hallu `mhe` may `rn2(4)`. `levitation_dialogue` / `phaze_dialogue`: no RNG. **Match the wired arms.**

**Callee closure (this peel).** LIVE: `stone_luck`, `carrying`, `confers_luck`, `Popeye`, `urgent_pline`, `stop_occupation`, `nomul`, `make_stunned`/`make_confused`, `morehungry`, `vomit`, `rndmonnam`, `hcolor`, `exercise`. OMIT named: region/sleep dialogues; `done_timeout`/`slimed_to_death`; `surface()`. STUB **inside the seven dialogues**: **none**. Expiry of STONED still silent — named, not a stub inside the countdown arm.

## Hallucinations / overclaim

Subject “dialogues and luck timeout actually run” is **true** for luck + seven countdown helpers. Do **not** stamp “Match C `region_dialogue`/`sleep_dialogue`.” Do **not** stamp “Match C `done_timeout(STONING)`.” Do **not** “correct” luck to skip when invulnerable. CURRENT’s seed4500 Screen 1801/1814 (`#wizintrinsic` DEAF `[2]`) is dated to this SHA; RNG full — DEAF expiry was already a dedicated arm, not this peel’s new dialogues. Public-unhit for Stoned countdown; luck at 600 is reachable. Probed.

## Density

§2b: one C prefix (luck + dialogue `if`s) + those callees + `stone_luck`/`Popeye` they need. +546. Did **not** glue `done_timeout` / `region_dialogue`. Large but one locus family.

## Verification

D-log: stone_luck signs; stoned 5/4/3; slime t=9/t=1; vomit/choke/sick/levi/phaze strings; luck ± at 600 vs 599; blessed luckstone keeps good luck; invulnerable skips dialogues still times luck; Stoned 5→4 after dialogue. Green + cohort. save-oracle skip. Rule #2 clean. This audit: `csym` `:587–641` / `:136–185` / `attrib.c:421` vs HEAD `js/timeout.js:641–714`.

## Actionable C-wrongs

None for Must-fix. Named: `region_dialogue` `:553`; `sleep_dialogue` `:267`; STONED/SLIMED `done_timeout` / `slimed_to_death`; `surface()` Underwater `"bottom"`; `ugallop`; 3 leftover `carrying` clones. Do **not** add `carrying` clone #5. Do **not** move luck after `uinvulnerable`. Do **not** `break` after vomiting case 6.

Verdict: **ACCEPT-WITH-DEBT**
