# Review 1483 — 4ef9d444 — sounds.c dochat + shop_object/price_quote (D-2524)

## Metadata

- SHA: `4ef9d444`
- D-id: D-2524. Next index: 1483.
- Files: `js/sounds.js` (+~120/−~70 restart), `js/shk.js` (+~113),
  `scripts/dochat-shop-quote.test.mjs` (+92, 8 its).
- C locus: `sounds.c:1256–1409` (`dochat`, staticfn, 154 L),
  `shk.c:5384–5402` (`shop_object`, 19 L), `shk.c:5405–5465`
  (`price_quote`, 61 L) + staticfn `shk_embellish` `:5467–5503` (37 L).

## Intent vs deliverable

Subject promises: whole `dochat` in C order + `shop_object`/`price_quote`
(PARTIAL → live). Diff actually adds exactly that, plus a focused test.
The old 72 L thin clone's documented gaps (no gates, no shop arm, unconditional
sleeping pline, `mfrozen||msleeping` for `helpless`, magic 1/2 for M_AP) are
all closed in the restart. Promise matches deliverable.

## Inventory

- Changed: `dochat` (restart, same name/signature; caller `dotalk` untouched).
- New: `shop_object` (exported), `shk_embellish` (file-local — correct home,
  C `staticfn`), `price_quote` (exported async), `THE_CONTENTS_OF` (`:63`),
  `AMULET_CLASS`/`RING_CLASS` import words.
- Zero clones added (grep count 0); all helpers below pre-date the diff.

## C ↔ JS fidelity

`dochat` arm walk (`csym.mjs` body printed above):

- `:1262–1278` four gates: silent-poly (`msound == MS_SILENT` inline,
  `mondata.h:62`), Strangled (`uprops[STRANGLED].intrinsic` — apply
  precedent), uswallow, Underwater (`u.uinwater` — dokick precedent).
  Order + ECMD_OK returns kept. Confirm.
- `:1280–1290` shop arm: `!Deaf && !Blind` (youprop.h forms per D-log) +
  `shop_object` → `price_quote` → ECMD_TIME. Confirm.
- `:1292–1325` getdir/steed/dz/self: ECMD_CANCEL on ESC; steed
  (`helpless` = file-local `js/sounds.js:792`, monst.h:251 — no new clone)
  with ECMD_TIME notice vs `domonnoise` tail; dz up/down; self-talk
  (C's `#if 0` Ettin arm correctly unported — compiled out). Confirm.
- `:1330–1369`: isok, `mon_at` for `m_at` (D-0140 precedent), statue arm
  (`pline_The` + Hallu `rndmonnam(null)` for C `(char *)0`), wall/SDOOR
  with blind-mapped and Hallu `walltalk[rn2(10)]`-clamp arms (kept lines
  `:1619–1620`). The `!mtmp||mundetected` block returns ECMD_OK
  unconditionally where C falls through to the `:1371` check — same
  outcome, since that check also returns ECMD_OK for those states.
  Confirm.
- `:1371–1386`: M_AP named consts (old magic 1/2 gone), sleeping
  `helpless && !ispriest` with `canspotmon` message gate (old code plined
  unconditionally — real fix), `mstrategy &= ~STRAT_WAITMASK` (old
  hardcoded bits gone). Confirm.
- `:1388–1408`: tame `!Deaf` + `map_invisible` before the noisy-eating
  pline (both missing before — real fixes), Deaf `humanoid`/`mon_nam`
  response (`mon_nam`, not `Monnam` — matches C), `domonnoise` tail.
  RNG: only `rn2(10)` (wall) in this body; draw preserved. Confirm.

`shop_object` vs `:5386–5403`: `in_rooms`→`shop_keeper` (LIVE `js/hack.js:
1596` / `js/shk.js:253`; `rooms.charCodeAt(0)` follows the pre-existing
`:492` convention; null-safe where C dereferences), `inhishop` LIVE,
`objects_at` head + `nexthere` walk skipping COIN_CLASS (≡ `vobj_at` +
chain walk — head-map verified in review 1481), `costly_spot` LIVE,
`NOTANGRY`/`muteshk` pre-existing same-file helpers, no_charge note kept.
Confirm.

`price_quote` vs `:5405–5465`: shkp guard, menu lines, coin skip,
no_charge/uball/uchain zero-cost, `contentsonly`, `contained_cost`,
globby units, price strings, `cnt>1` menu paint (dynamic pager import —
no static edge), `cnt==1` single-quote reword + `SetVoice`/`verbalize`
(LIVE async, awaited), `cnt==0` silent. `cost`/`contentsonly`/`buf` reuse
in the single path matches C's "already set up" (one iteration). Confirm.

`shk_embellish` vs `:5467–5503`: `rn2(3)` then `rn2(5)` (draw order kept),
choice-0 cost tiers, case-4 cost<10 break falling through to `"."` exactly
like C, FOOD/painstakingly/superb arms, 3/2/1 strings, artifact arm,
final `"."`. RNG call-for-call. Confirm.

Callee closure: every name LIVE or pre-existing same-file
(`get_cost` `:3241`, `contained_cost` `:2495`, `get_pricing_units` `:972`,
`NOTANGRY` `:200`, `muteshk` `:219` — all pre-date this diff);
`--can sounds.js shk.js shop_object`: ALREADY (shk↔sounds static cycle
pre-existed via `shk_chat`). No STUB in any live arm; omits: none.

## Hallucinations / overclaim

None. The restart's "real fix" list (sleeping gate, tame arms, `helpless`,
M_AP consts) is verified against the old lines shown as `-` in the diff.
Two non-queueable notes: the new local `Deaf`/`Blind` predicates sit
alongside pre-existing `u.Deaf` / `u.Blind||u.ublind` uses in the wall and
statue arms (kept lines) — same repo idioms, no divergence introduced;
the `mstrategy` null-guard is kept defensive local style, behaviorally
identical on sane state.

## Density

One 154-line C function + two small same-story callees (19 + 61 + 37 L C),
two files that already import each other, map + test in one handoff.
Right-sized per §2b (the test file is `scripts/`, outside scored `js/`).

## Verification

- D-log: syntax (2 changed) · rule2 · hidden note (0 blocked) · smoke
  24/24 · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS; focused test
  8/8.
- Re-run here: `hidden-proxy.mjs verify dochat --base 4ef9d444~1
  --reach-all` → 0 blocked at baseline and working tree (vacuous note,
  honestly reported — the row cited no blocks) + smoke 24 PASS,
  0 regressed → REACH-OK; focused test re-run here: 8 pass, 0 fail.
- Diff grep: no FORCE/DIAG/getRngLog/fastforward/seed/coordinate logic.

## Actionable C-wrongs

None. No Must-fix, no CURRENT change.

Verdict: **ACCEPT**
