# Review 846 — 305998a7 — trap.c climb_pit shared pit-escape port (D-1876)

## Metadata

- SHA: `305998a7` ("trap.c climb_pit shared pit-escape port (climb_pit corpus owner) (D-1876).")
- D-id: D-1876. Queue row: Open (`climb_pit` corpus owner), popped in order.
- Files: `js/trap.js` (+72/−1), `js/hack.js` (+4/−3), `js/do.js` (+8/−2), docs + map + scoreboard.

## Intent vs deliverable

Subject promises: port shared `climb_pit` (trap.c) + wire both C call sites
(`doup`, `trapmove` TT_PIT). Diff actually adds:

- `js/trap.js`: `Passes_walls()` (new file-local), `m_easy_escape_pit(mtmp)`
  (new file-local), exported async `climb_pit()`; imports `Norep`, `YMonnam`.
- `js/hack.js`: `trapmove` TT_PIT arm replaces the climb stub with
  `await climb_pit()` (still `break` → `false`); import added.
- `js/do.js`: `doup()` pit gate (`utrap && utraptype === TT_PIT` →
  `await climb_pit(); return ECMD_TIME`); omits comment updated.

No more, no less. Promise kept.

## Inventory

| JS function | Status |
|---|---|
| `climb_pit` (trap.js:1817, async export) | new port of C `trap.c:4182–4230` |
| `m_easy_escape_pit` (file-local) | new port of C `trap.c:3725–3730` (staticfn) |
| `Passes_walls()` (file-local) | new port of C `youprop.h:286` macro |
| `doup` gate / `trapmove` TT_PIT arm | wiring into both C call sites |

## C ↔ JS fidelity

C locus `climb_pit` (`nethack-c/upstream/src/trap.c:4182–4230`, via
`node scripts/csym.mjs climb_pit`), callers `do.c:1309`, `hack.c:1585`
(`--callers`: exactly these two). Branch-by-branch vs C body:

1. Guard `!u.utrap || utraptype != TT_PIT → return` — JS identical
   (`(u.utrap|0)`, `!== TT_PIT`). ✓
2. `Passes_walls` ascend: `You ascend` → `reset_utrap(FALSE)` →
   `fill_pit` → `vision_full_recalc = 1`. JS: same order, same args. ✓
3. `!rn2(2) && sobj_at(BOULDER)` crevice: two plines with
   `display_nhwindow` between. JS: `!rn2(2) && sobj_at(...)` first-draw
   RNG in the right position; `flush_topl_more()` for the display call,
   `clear_nhwindow` explicitly named as omitted. ✓ (RNG call-for-call:
   `rn2(2)` is the first draw on this path in both.)
4. `(Flying || is_clinger(youmonst.data)) && !Sokoban` climb-out via
   `u_locomotion("climb")` → reset/fill/recalc. JS identical shape. ✓
5. `!(--u.utrap) || m_easy_escape_pit(&youmonst)` crawl-out with the
   Sokoban+Levitation float variant and `u.usteed ? ride : crawl`. JS:
   decrement-then-test matches C pre-decrement; ternary order matches. ✓
6. `u.dz || flags.verbose` Norep tail: steed arm first, then
   `(Hallucination && !rn2(5))` short-circuit (`rn2(5)` drawn only when
   Hallu true — JS `Hallucination() && !rn2(5)` preserves this). ✓

`m_easy_escape_pit`: C `trap.c:3725–3730`
(`data == &mons[PM_PIT_FIEND] || msize >= MZ_HUGE`); JS
`data === mons[PM_PIT_FIEND] || (data?.msize|0) >= MZ_HUGE`. ✓

Callee closure (all arms): `trapname` LIVE (trap.js:1501),
`reset_utrap` LIVE (trap.js:2509), `fill_pit` LIVE (dig.js:839),
`flush_topl_more` LIVE async (display.js:6062, awaited),
`Norep` LIVE async (display.js:6306, awaited), `YMonnam` LIVE
(do_name.js:1180), `is_clinger` LIVE (monsters.js:468),
`body_part` LIVE (polyself.js:352), `Hallucination` LIVE export
(display.js:926 — the C-locus one). Clones reused, all pre-existing in
trap.js at parent, all verified: `sobj_at` (trap.js:2122) matches C
`invent.c:1465–1475` line-for-line; `hero_Flying`/`hero_Levitation`
(trap.js:1400–1410, D-1070 idiom, steed-flyer included);
`u_locomotion_pit('climb')` (trap.js:1753) matches C
`u_locomotion` (`hack.c:1816–1829`) for Lev/Fly/default, poly-form
`locomotion(data)` verbs explicitly named as deferred — same deferral
as the three existing clones, so consistent debt, not new drift.
No STUB in any live arm.

Two nits, neither behavioral: (a) `Passes_walls()` adds a `u.Passes_walls
||` disjunct the C macro `(HPasses_walls || EPasses_walls)`
(`youprop.h:286`) lacks — but `u.Passes_walls` is never written anywhere
in `js/`, so the disjunct is dead, not divergent. (b) `Sokoban` ORs in
`game.Sokoban`, which `mklev.js` maintains alongside level flags — live
field, redundant, not invented. (c) `doup` calls `stairway_at` after the
pit gate where C (`do.c:1295–1312`) calls it before `set_move_cmd` — pure
lookup, no RNG/side effects, unobservable reorder; `u_rooted` stays a
named pre-existing omit.

## Hallucinations / overclaim

D-log says "`imports.mjs --can` ALREADY on both" — confirmed:
`--can trap.js display.js Norep` and `--can trap.js do_name.js YMonnam`
both return ALREADY (same-edge additions only). "1 corpus moved past"
matches the Verify bullet (`moved → erode_armor at step 47`). No
dispatch-vs-callee overclaim: every callee in the shipped arms is LIVE
or a verified clone.

## Density

One C function + its staticfn + both call sites, ~80 JS lines with map
update and verify in one handoff. Textbook §2b density.

## Verification

- `node scripts/imports.mjs --rulecheck`: clean (no fs/node, no
  DIAG/FORCE/seed gates; diff grep for FORCE/DIAG/getRngLog/fastforward/
  coordinates: no hits).
- D-log Verify: syntax PASS, rule2 PASS, hidden PROGRESS (moved past),
  green 2/2 + strict, cohort 7/7, full 44/44.
- Re-measured myself: `node scripts/hidden-proxy.mjs verify climb_pit
  --base 305998a7~1` → `1 PASS, 0 moved past, 0 worse → PROGRESS`
  (`tour-Healer-70025-d5-8-15-17-22: PASS`). Better than the D-log's
  moved-past because later tree SHAs (D-1883 `erode_armor`) resolved step
  47 — confirms the claim direction, not a vacuous check.

## Actionable C-wrongs

None. The dead `u.Passes_walls` disjunct and `stairway_at` reorder are
not queueable C-wrongs (no JS↔C behavioral contradiction).

Verdict: **ACCEPT**
