# Review 686 — 32c02560 — calendar.c hhmmss hour*10000+min*100+sec (D-1725)

## Metadata
- Full / short hash: `32c0256026a78c9cd2d9dbd6f707aa1672f476b4` / `32c02560`
- Parent: `7ee8ad1d` (D-1724). This file audits **this SHA only** (ninth of nine `js/` commits since review **677**). Archive **Addressed:** D-1725 — this review commit fills `%h` `32c02560`.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-08-30 09:08:55 +0200
- D-id: **D-1725**
- Stats: `js/calendar.js` +12. Total `js/` insertions **12** <250. Band **150–350**.
- Claims to close: Open `hhmmss` after D-1710 / review **671** (`yyyymmddhhmmss` cemetery `when[]`; helper omitted). Not cemetery `when[]`. Not dump_fmtstr / paniclog files. `reviews/loop-2026-08-15/` has no unpaid hhmmss Must-fix.
- JS / map: `calendar.js` `hhmmss`. `c-js-map/startup.md` calendar row.
- Prior: **671** named `hhmmss`.

## Intent vs deliverable

Git subject promises: wall-clock time is `hour*10000+min*100+sec` (`date==0` → getlt), instead of omitting the helper after D-1710 `yyyymmddhhmmss`.

`node scripts/csym.mjs hhmmss` → `calendar.c:79–92`. `--callers hhmmss`: `files.c:2824` paniclog; `windows.c:1179` dump_fmtstr `%d`; `:1185` `%D`. `yyyymmdd` `:55–77`. `yyyymmddhhmmss` `:94–117` (D-1710; not this function). `getlt` / `getnow` already live.

```79:92:nethack-c/upstream/src/calendar.c
long
hhmmss(time_t date)
{
    long timenum;
    struct tm *lt;

    if (date == 0)
        lt = getlt();
    else
        lt = localtime((LOCALTIME_type) &date);

    timenum = lt->tm_hour * 10000L + lt->tm_min * 100L + lt->tm_sec;
    return timenum;
}
```

```1176:1185:nethack-c/upstream/src/windows.c
            case 'd': /* game start, YYYYMMDDhhmmss */
                if (fullsubs)
                    Sprintf(tmpbuf, "%08ld%06ld",
                            yyyymmdd(ubirthday), hhmmss(ubirthday));
            case 'D': /* current time, YYYYMMDDhhmmss */
                if (fullsubs)
                    Sprintf(tmpbuf, "%08ld%06ld", yyyymmdd(now), hhmmss(now));
```

```2822:2824:nethack-c/upstream/src/files.c
            (void) fprintf(lfile, "%s %08ld %06ld %d %c: %s %s\n",
                           version_string(buf, sizeof buf),
                           yyyymmdd(now), hhmmss(now),
```

Parent: `hhmmss` NOT FOUND; `lt_for_date` already shared by `yyyymmdd` / `yyyymmddhhmmss`. The diff **does** export `hhmmss` via `lt_for_date`. It **does not** call it from dump_fmtstr / paniclog. Named (Rule #2 — those callers `fopen`/`fprintf` dump and panic files). It **does not** change cemetery `when[]` (still `yyyymmddhhmmss`). It **does not** port `getyear`. Named.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `hhmmss` | LIVE new | returns unpadded long; `%06ld` is the caller |
| `lt_for_date` | CLONE / shared | C `date==0` → `getlt()` else `localtime`; contest UTC-4 inverse |
| `getlt` / `getnow` | LIVE | already |
| `yyyymmdd` | LIVE | D-1710 date-aware |
| `yyyymmddhhmmss` | LIVE | cemetery `when[]`; **not** this peel |
| `dump_fmtstr` `%d`/`%D` | OMIT named | `windows.c:1176–1185`; Rule #2 dump file |
| `paniclog` | OMIT named | `files.c:2822–2824`; Rule #2 panic file |
| `getyear` | OMIT named | `mhitu.c` `ld()` |

`node scripts/sym.mjs`:

```
hhmmss           js/calendar.js:141   sync
yyyymmdd         js/calendar.js:128   sync
yyyymmddhhmmss   js/calendar.js:152   sync
getlt            js/calendar.js:55   sync
lt_for_date      NOT EXPORTED — but 1 LOCAL CLONE(S) in 1 file(s):
               js/calendar.js:100
             => Do NOT write clone #2.
dump_fmtstr      NOT FOUND in js/** (no export, no local function/const).
             This index includes js/generated/. Do not add a local clone.
paniclog         NOT FOUND in js/** (no export, no local function/const).
             This index includes js/generated/. Do not add a local clone.
```

No re-point (new export). Do **not** add `lt_for_date` #2. Do **not** add `dump_fmtstr` / `paniclog` clones (filesystem). No new `--can` edge (`calendar.js` already owns `getlt`). FORCE/DIAG/`getRngLog`/`fastforward`/seed names: none. `node scripts/imports.mjs --rulecheck`: **Rule #2 clean**.

## C ↔ JS fidelity

**`date==0` (`:85–88`).** C `getlt()` else `localtime(&date)`. JS `lt_for_date`: `if (!date) return getlt()` else UTC-4 inverse (contest `time_from_yyyymmddhhmmss` treats civil stamps as UTC-4 — same helper D-1710 already used for `yyyymmdd`). Falsy `0` matches C `date==0`. **Match the branch.** Nonzero date is the contest clock, not a host `localtime` timezone. Named as the existing calendar policy, not a new divergence.

**Arithmetic (`:90`).** C `tm_hour * 10000L + tm_min * 100L + tm_sec`. JS `lt.tm_hour * 10000 + lt.tm_min * 100 + lt.tm_sec`. No padding inside the function. C callers pad with `%06ld` / concatenate onto `%08ld` `yyyymmdd`. **Match the return.** Canary `datetime=20260830090105` → `90105` (hour 9, not `090105`). `time_from_*('19991231235959')` → `235959`. **Match unpadded long.** No `rn2`.

**Not `yyyymmddhhmmss`.** That helper is a 14-char string (`%04ld%02d…`). `hhmmss` is a `long`. Cemetery `when[]` stays the 14-digit string (D-1710). Dump `%d`/`%D` would be `yyyymmdd` padded 8 + `hhmmss` padded 6. Do **not** feed `hhmmss` into `when[]`.

**Callers.** C paniclog writes a line to a log file; dump_fmtstr substitutes into a dump filename/template. JS has neither function (`sym` NOT FOUND). Leaving `hhmmss` uncalled from those sites is the Rule #2 omit, not a stubbed dispatch: the **helper** is LIVE; the **filesystem callers** are named. Combined-arm (the one function body) has every callee LIVE (`getlt` / `lt_for_date`).

**Callee closure.** LIVE: `getlt`, `lt_for_date` (shared). CLONE: none added. OMIT named: dump_fmtstr, paniclog, `getyear`. STUB: **none**. Not “dispatch ported, callee stubbed.”

## Hallucinations / overclaim

Subject “wall-clock is hour*10000+min*100+sec, instead of omitting the helper”: **true**. D-log “padding is the caller’s `%06ld`”: **true**. Do **not** stamp “Match C dump_fmtstr `%d`/`%D`.” Do **not** stamp “Match C paniclog fprintf.” Do **not** stamp “Match C `getyear`.” Do **not** stamp “Match C cemetery `when[]` via `hhmmss`.” Journal “fortress held” is not a dump-filename proof. Public sessions **do not** hit dump/paniclog (Rule #2). Helper **public-unhit**; canary is the arithmetic check. Admit that.

## Density

§2b: one C `hhmmss` after D-1710 named it. Same `:79–92`. +12. Did not glue dump_fmtstr / paniclog / `getyear`. Did **not** reopen cemetery `when[]`. Below ~40 would be allowed for a Must-fix; this is map-driven and still one function.

## Verification

D-log: save-oracle skip (untagged `calendar.c:hhmmss`); node canary `90105` / `235959`; green+strict seed8000/0900; CURRENT cohort **7**/7 + strict. Rule #2 clean. Public dump/paniclog **unhit** (named omit). Admit that.

## Actionable C-wrongs

None for Must-fix (the helper matches C; callers are named filesystem). Named: `dump_fmtstr` `%d`/`%D` (`windows.c:1176–1185`); `paniclog` (`files.c:2822–2824`); `getyear`; dump_everything datetimebuf (already `yyyymmddhhmmss`). Do **not** add `dump_fmtstr` #1. Do **not** add `paniclog` #1. Do **not** `fopen` a dump/panic file from scored `js/`. Do **not** pad inside `hhmmss`. Do **not** add `lt_for_date` #2. Do **not** write cemetery `when[]` from `hhmmss`. Do **not** re-port D-1710 `yyyymmddhhmmss`.

Verdict: **ACCEPT-WITH-DEBT**
