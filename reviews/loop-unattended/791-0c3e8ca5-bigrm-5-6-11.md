# Review 791 — 0c3e8ca5 — mkmaze.c makemaz bigrm-5/6/11 load_special (D-1821)

## Metadata
- Full / short hash: `0c3e8ca56522cdb633ecb84b1133298dec050897` / `0c3e8ca5`
- Parent: `42afdca4` (D-1820). Map-driven Open. Three consecutive `makemaz` `load_special` rows (§2b).
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 20:21:04 +0200
- D-id: **D-1821**
- Stats: `js/mklev.js` +264/−9. `js/` insertions **264** → review ceiling **450**. Band **80–350**.
- Claims to close: Open `bigrm-5`/`-6`/`-11` (three smallest Big Rooms). Not bigrm-1/10/13.
- JS / map: `load_bigrm_5` / `_6` / `_11`; `splev_create_trap` specified-kind; `lspo_replace_terrain_sel`; `selection_iterate_lua`. `c-js-map/data.md`. Archive **Addressed:** D-1821 `0c3e8ca5`.

## Intent vs deliverable

Git subject promises: those `rnd(13)` variants fell through to a blank maze; C loads `dat/bigrm-{5,6,11}.lua`.

`node scripts/csym.mjs makemaz` → `mkmaze.c:1126–1223`. `lspo_level_init` `sp_lev.c:3865` `rm_deadends = !get_table_boolean_opt(..., "deadends", TRUE)`. `l_selection_iterate` `nhlsel.c:925–957` y-outer, `cvt_to_relcoord`. `lspo_replace_terrain` `:5123–5130` x-outer y-inner. `mktrap` `mklev.c:2066–2068` specified `num`. `percent` is nhlib.lua `nh.rn2(100) < threshold` (no C function).

Parent: bigrm-2/3/4/7/8/9/12 only; `splev_create_trap` ignored named types. The diff **does** add the three loaders, the replace_terrain selection arm, iterate, and the mktrap specified-kind arm. Object/trap/monster loop bounds match the luas (15/6/28; 11 uses 6 rolling-boulder).

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `load_bigrm_5` / `_6` / `_11` | LIVE new | lua bodies |
| `percent` | LIVE | `rn2(100) < n` |
| `lspo_replace_terrain_sel` | LIVE new | C selection arm |
| `selection_iterate_lua` | LIVE new | y-outer; absolute analogue of relcoord+des |
| `splev_create_trap(type)` | LIVE repaired | specified kind |
| `splev_initlev` maze | LIVE | `corrwid=3+rn2(3)`, `rm_deadends=!percent(50)` |
| `splev_create_stair` / `splev_non_diggable` / `splev_create_object` / monster | LIVE | |
| ensure_way_out / humidity / dry-boulder | OMIT named | |
| icedpool on replace ICE | OMIT named | |

`node scripts/sym.mjs`:

```
load_bigrm_5     NOT EXPORTED — 1 LOCAL mklev.js:2121
load_bigrm_6     NOT EXPORTED — 1 LOCAL :2193
load_bigrm_11    NOT EXPORTED — 1 LOCAL :2269
splev_create_trap NOT EXPORTED — 1 LOCAL :13090
selection_iterate_lua NOT EXPORTED — 1 LOCAL :2255
lspo_replace_terrain_sel NOT EXPORTED — 1 LOCAL :19650
```

No clone→import. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**.

## C ↔ JS fidelity

**bigrm-5.** Map 19×74. `if percent(25)` then `match(".")` `:percentage(2)` `:grow()` then `replace_terrain` to `percent(50) ? ICE : CLOUD`. JS `percent(25)` / `selection_filter_percent(..., 2)` / `selection_grow` / `percent(50) ? ICE : CLOUD` / `lspo_replace_terrain_sel(..., 100)`. C selection loop x-outer y-inner + `rn2(100)<chance`. Lit `area(00,00,72,18)` via `light_region` (grow on lit — C argc-2). 15/6/28. noflip. **Match those arms.** ICE icedpool named.

**bigrm-6.** Four-lobe map with `T`/`{`. Lit `area(01,01,72,17)`. Same 15/6/28 / noflip. **Match.**

**bigrm-11.** Flags then `level_init` maze. Lua `corrwid = 3+nh.rn2(3)` then `deadends=t_or_f()` → `percent(50)`; C `rm_deadends = !deadends`. JS same order. `match(".w.") | match(".\nw\n.")` iterate replace wall→ROOM+boulder, then leftover `.w.` iterate. C iterate y-outer, `cvt_to_relcoord`, lua `des.*` adds origin. JS iterate y-outer on **absolute** `selection_match_mapfrag` points and `sel_set_ter`/`mksobj_at` there — the analogue, not a double origin. 6× `splev_create_trap(ROLLING_BOULDER_TRAP)`: C `mktrap` `:2066` `kind = num` (skips Gehennom fire `rn2(5)`). **Match the specified arm.**

**Shared trap helper.** Unspecified path still Inhell `rn2(5)` then `traptype_rnd`. Specified path does not. One STUB would have blocked the 11 arm; the named-type arm is LIVE in this commit.

**Callee closure.** Three `load_special` variants; every C callee those arms reach is LIVE or named OMIT. `create_maze` is LIVE via `splev_initlev`. Right §2b cluster.

## Hallucinations / overclaim

Do **not** stamp ensure_way_out / humidity / icedpool / dry-boulder. Do **not** stamp “Match C `l_selection_iterate` pushes relative coords into JS” — JS skips the round-trip. Do **not** add a second `splev_create_trap`.

## Density

§2b: three `makemaz` `load_special` protos + the trap/replace callees they need. +264. Did **not** glue bigrm-1/10/13. Right size.

## Verification

`verify.mjs --fn makemaz` → PASS syntax; PASS rule2; **PASS hidden (no corpus session blocked on makemaz)**; green + cohort + full 44/44. Public suite does not walk these variants. This audit: lua 5/6/11 vs HEAD `:2121–2318`; `csym` `makemaz` `:1126–1223`; `nhlsel.c:935–937`; `sp_lev.c:5123–5124`; `mklev.c:2066`. Rule #2 at end-of-iter.

## Actionable C-wrongs

None that must block the next port. Named (map): ensure_way_out; humidity; dry-boulder; icedpool on ICE replace.

Verdict: **ACCEPT-WITH-DEBT**
