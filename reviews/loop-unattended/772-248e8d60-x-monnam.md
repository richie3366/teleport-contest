# Review 772 — 248e8d60 — do_name.c x_monnam remaining + nextmbuf/just_an (D-1803)

## Metadata
- Full / short hash: `248e8d6053dd037dfbfcc7324c456dad1a57417f` / `248e8d60`
- Parent: `5c960c16` (D-1802 AWD). Map-driven Open.
- Author, date: Raphaël Hervier (Co-authored-by Cursor), 2026-09-04 03:41:30 +0200
- D-id: **D-1803**
- Stats: `js/do_name.js` +195/−78; `js/objnam.js` +29/−13; `js/hacklib.js` +17; plus clone deletes. Total `js/` insertions **249** ≤250. Band **80–350**.
- Claims to close: Open `do_name.c` `x_monnam` remaining + `nextmbuf` / `lcase` / `just_an`. Not `mon_nam_too`.
- JS / map: `do_name.js` `x_monnam`; `hacklib.js` `lcase`; `objnam.js` `just_an`; `const.js` `EXACT_NAME` 0x1F. `c-js-map/turns.md`. Archive **Addressed:** D-1803 `248e8d60`.

## Intent vs deliverable

Git subject promises: Match C `do_name.c` `x_monnam` so `nextmbuf`, youmonst, AUGMENT_IT, mappear, invis/saddle, is_mplayer `rank_of`+`lcase`, and ARTICLE_A `just_an` actually run, instead of omitting those arms and using the wrong `EXACT_NAME` bits.

`node scripts/csym.mjs x_monnam` → `do_name.c:826–1032`. `nextmbuf` `:19–27`. `lcase` `hacklib.c:89–98`. `just_an` `objnam.c:2108–2142`. `some_mon_nam` `:1063–1071`. `Some_Monnam` `:1091–1098`. `hack.h` `EXACT_NAME` 0x1F.

Parent: half-port; `EXACT_NAME` was IT|INVIS|HALLU|**NAME**. The diff **does** port those arms, fix the mask, export `just_an` exceptions, and rebind apply `l_monnam` + dothrow/mhitu/steal `Some_Monnam`. Subject is delivered.

## Inventory

| Symbol | Class | Notes |
|---|---|---|
| `x_monnam` | LIVE repaired | remaining C body |
| `nextmbuf` | LIVE local | NUMMBUF=5; do **not** write #2 |
| `lcase` | LIVE new | hacklib.js |
| `just_an` | LIVE repaired | one/eu/uke/unicorn/uranium/useful + x-consonant |
| `some_mon_nam` / `Some_Monnam` / `l_monnam` | LIVE | clones deleted at apply/dothrow/mhitu/steal |
| `rank_of` | LIVE | roles.js |
| `priestname` | OMIT named | NOT FOUND; fall through |
| `Some_Monnam_mm` | leftover clone | mhitm.js — not this SHA’s callers |

`node scripts/sym.mjs`:

```
x_monnam         js/do_name.js:758   sync
nextmbuf         NOT EXPORTED — 1 LOCAL (do_name.js:110)
lcase            js/hacklib.js:107   sync
just_an          js/objnam.js:1826   sync
some_mon_nam     js/do_name.js:956   sync
Some_Monnam      js/do_name.js:1073   sync
l_monnam         js/do_name.js:920   sync
priestname       NOT FOUND
rank_of          js/roles.js:715   sync
```

`--can do_name.js` hacklib `lcase` / objnam `just_an` / roles `rank_of`: **ALREADY**. `--can apply.js do_name.js l_monnam` / `dothrow.js Some_Monnam`: **ALREADY**. FORCE/DIAG/`getRngLog`/`fastforward`/seed-in-control-flow: **none**. Rule #2 **clean**.

## C ↔ JS fidelity

**`EXACT_NAME`.** C 0x1F = IT|INVIS|HALLU|SADDLE|MAPPEAR (`hack.h:1016–1021`). Parent ORed `SUPPRESS_NAME` (0x20) so `m_monnam` dropped given names. JS now matches 0x1F. **That was a C-wrong; it is gone.**

**`nextmbuf` / youmonst.** Ring of 5; every `x_monnam` including `"you"` consumes a slot (`ret()`). `is_youmonst` is `=== youmonst \|\| === game.youmonst` (C `&gy.youmonst`) plus `_youmonst` alias.

**AUGMENT_IT (`:877–880`).** `!augment_it ? "it" : (!do_hallu ? s_one : !rn2(2)) ? "someone" : "something"`. **Match RNG.** `some_mon_nam` passes `AUGMENT_IT` (± `SUPPRESS_SADDLE` if named).

**Adjectives / mappear / mplayer.** `"invisible "` when `minvis` and not suppressed; `"saddled "` when `W_SADDLE` && !Blind && !Hallu. `do_mappear` → `pmname(mappearance)`. Named player `" the "` split via `strstri` tail (hacklib returns the matching slice, ≡ C pointer). Else `lcase(rank_of(m_lev, mndx, female))` off-endgame. **Match.**

**ARTICLE_A.** C `just_an(buf2, buf)` not `an()`. JS `just_an(buf)` then prefix. Exceptions: `strncmpi` “one” with 4th-char `-_ ` gate; eu/uke/ukulele/unicorn/uranium/useful; `x`+non-vowel2 → “an ”. **Match.**

**Callee closure.** `just_an`/`lcase`/`rank_of`/`strstri`/`rndmonnam` LIVE. `priestname` **OMIT named** (ispriest/isminion fall through). No STUB in a shipped live arm. mhitm `Some_Monnam_mm` still a local clone (not this peel’s files).

## Hallucinations / overclaim

Subject is **true**. Do **not** stamp “Match C `priestname`.” Do **not** add `nextmbuf` clone #2. Do **not** glue `mon_nam_too` (D-1790). `do_exact` exists only for `priestname` in C — unused until that omit ships.

## Density

§2b: one C function’s remaining body + the three helpers those arms call. +249. Did **not** glue priestname. Right size.

## Verification

D-log: green + named cohort. save-oracle skip. Public-unhit for AUGMENT_IT `rn2(2)` and mplayer `rank_of`. This audit: `csym` `:826–1032` vs HEAD `js/do_name.js:758–914`; `just_an` vs `:2108–2142`. Rule #2 clean.

## Actionable C-wrongs

None for Must-fix. Named: `priestname`. Leftover: mhitm `Some_Monnam_mm` clone.

Verdict: **ACCEPT-WITH-DEBT**
