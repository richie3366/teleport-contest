# Review 1434 — f8881130 — prscore whole-body port (D-2475)

Metadata: SHA `f8881130`, `js/topten.js` only (239 ins). C
`topten.c:1193–1353` (161 L) + static `score_wanted` (`:1112–1192`).
D-log: D-2475.

## Intent vs deliverable

Promise: whole `prscore` + `score_wanted` in C order (arg
validation, `-s`/`-v` splits, selector, record scan, header/entry
or cannot-find + usage). Diff ships that. No second subsystem.

## Inventory

- Added: module-local `score_wanted` (C staticfn ✓),
  `export async function prscore` (C global ✓), `ch()`/`getuid()`
  helpers, `raw_print` closure → panel `emit`.
- Edges: `BUFSZ` (const), `str2role`/`str2race` (roles LIVE,
  pre-existing edge). `init_dungeons` via dynamic import (sync
  target, no new static cycle). `read_record_entries`/
  `outheader`/`outentry(rank, t1, false, emit)` all live; the
  emit-param convention predates this commit (topten() uses it).
- No deleted/re-pointed symbols for `sym.mjs`.

## C ↔ JS fidelity

- `score_wanted`: version gate; `pers_is_uid && !playerct &&
  uid===` (uid 0 matches own entries — `topten()` records
  `t0.uid = 0`, verified); `-uname` strip; `-p/-r/-u` + next-arg
  with the `i++`-consume-inside-the-arm (skip-next even on
  no-match, = C); `strchr("pru",…)` NUL-match quirk mirrored by
  `'pru\0'.includes(ch(…))` with the OOB-as-NUL `ch()` helper;
  `all`/name-prefix (slice(0,NAMSZ) ≡ strncmp semantics both
  directions)/`-<roleletter>`/maxrank (`digit`→range test,
  `atoi`→`parseInt`, equivalent on digit-led args) — exact,
  FIXME-union semantics preserved (no AND-"fix", correctly).
- `prscore`: `ln`-up-to-space bad-args arm; missing VFS record =
  failed `fopen_datafile`; AMIGA named; `dlevel==0` →
  `init_dungeons` (named `dlb_init`); `-s`/`--scores`/`-s<anything>`
  split and `-v` (exact `"-v"`, no `-v<anything>`) via local
  slice-copies (argv-mutation modeled without touching the
  caller); uid/plname/`all`/explicit selector; blank line;
  record scan with `points==0` break + per-entry rank (rank
  counts unread and unwanted entries, = C); match → header +
  re-scan output, else cannot-find arm with exact BUFSZ overflow
  branches (`-u` skip/`continue`, `-p`/`-r` space-vs-colon with
  the same strchr-NUL reading, trailing `.`) + Usage lines
  (`'nethack'` for argv[0]-derived `gh.hname`, named) — exact.
  `fclose`/`free_dungeons`/`dlb_cleanup`/`free_ttlist`/AMIGA tail
  named with mechanism reasons.
- VFS-for-fopen + panel-for-raw_print are the file's Rule #2 /
  D-2471 surface analogues, applied uniformly (all arms funnel
  through one surface, as C's raw_printf path does).
- RNG: none in C, none added.

## Hallucinations / overclaim

None. Every callee is live or named; the 25/25 probe claim is a
scratch-file result (not re-run here, stated as such).

## Density

One 161-line C function + 81-line static, one module: right-sized.

## Verification

- `hidden-proxy verify prscore --base f8881130~1 --reach-all`
  (re-run): 0 blocked both sides — vacuous, as the D-log states.
  Smoke 24/24 → REACH-OK. Matches.
- Diff grep: no FORCE/DIAG/`getRngLog`/seed/fastforward.

## Actionable C-wrongs

None.

Verdict: **ACCEPT**
