# Loop work queue

Unattended **port** iterations pop the **first unchecked** item and treat it
as the only cluster. Do not combine items. Do not invent a substitute.

Review iterations may **prepend** a Keep’d C-wrong. Cadence iterations do
not touch this file except to note score.

Check an item only after it is committed C-faithful JS (named omissions
stay named in `c-js-map`).

## Open

- [ ] `apply.js` pole targeting: `glyph_is_poleable_at` / `find_poleable_mon` must follow C `apply.c` `use_pole` (live monster/map, not a glyph-only stand-in). D-1022 Keep’d C-wrong. Not `thitmonst`, not `yname`.
- [ ] Pole `thitmonst` hit-vs-miss envelope for `use_pole` (D-1022). Not names.
- [ ] Whip/pole/grapple names: real `yname` / `Amonnam` / `mbodypart` instead of local apply clones (D-1022).
- [ ] `sit.c` `dosit` water / pool / gremlin sit (after trap, before sink). Not the furniture list.
- [ ] `sit.c` `dosit` sink / altar / grave / stairs / ladder sit messages only.
- [ ] `sit.c` `dosit` lava / ice / drawbridge sit (terrain, not trap-lava already in D-1039).
- [ ] tut-1 `des` kelp only. Not stairs / box / key / `place_lregion`.
- [ ] tut-1 stairs only.
- [ ] tut-1 large-box contents only.
- [ ] tut-1 food objects only.
- [ ] tut-1 `place_lregion` only.
- [ ] tut-1 `tut_key` / eckey only.
- [ ] tut-1 nhcore callback disable on enter/leave.

## Parked (do not pop)

- D-0006 seed1800 pet movement — needs C state/candidate capture.
