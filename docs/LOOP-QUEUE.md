# Loop work queue

Unattended **port** iterations pop the **first unchecked** item, preferring
**Must-fix** over Open. Do not combine **unrelated** items. Consecutive
Open rows that share one C `file.c:function` may ship in one port iff
every C callee those arms reach is **live** (imported, C body), a
**clone** matched to C in this commit, or **named omitted** in the map
in this commit. A **stub** in a live arm → split that arm back out.
Must-fix stays **one item, first, not glued** to Open. Do not invent a
substitute.
Live file is **unchecked-only**. Done rows live in
`docs/archive/LOOP-QUEUE-DONE.md`. Rows come from the **scenario corpus**
(`node scripts/hidden-proxy.mjs queue`; `docs/HIDDEN-PROXY.md` §5) — a
row names the C function that owns a recorded C-vs-JS first divergence,
how many corpus sessions it blocks, and the exact probe. Map-omission
rows (`c-js-map/*.md` singletons with no corpus session and no C
RNG/message surface) are **deferred** while any corpus family is below
90 % PASS (Constitution §10.13).

**Keep 8–12 open `- [ ]` rows** (`check-hot-docs.mjs` reports the count). If Must-fix+Open drops below **8**
(including after you archive this iter’s item), **refill Open** in the
**same commit** up to **12**. Sources, in order: `node scripts/hidden-proxy.mjs queue` (corpus
owners, most sessions blocked first), then `PORT-GAP-TOP30.md` rows the
corpus reaches, then named omits in `docs/c-js-map/*.md` only when the
corpus is ≥ 90 % PASS. One C function/family per line; cite C file +
function and the probe. A level-gen owner
(`mineralize`, `bound_digging`, `wallification`, `place_lregion`…) is where
C *noticed* the difference: its falsifier is `node scripts/geom-probe.mjs
<session>`, and the shipped D-log cites the C writer actually changed.
Do not duplicate live or archived rows. Do not invent FAIL peels. Do
not enqueue parked D-0006 or parked `dog_invent`.

## Must-fix (from reviews) — pop first

Written reviews are not theater. Each item is a Keep’d **C-wrong** (JS
contradicts C, not a named omit). After shipping: stamp the cited review
`**Addressed:** D-NNNN` (D-id only), mark the queue line `- [x]`, then
run `node scripts/archive-loop-queue-done.mjs` **in this same commit**.
Do **not** leave `- [x]` in this file. Do **not** put this commit’s hash
in the same SHA (chicken-egg), amend, or make a stamp-only follow-up.
The **next** real commit fills the short hash on the review (and on the
archive row) from `git log -1 --format=%h` of the fix.

Review iterations **prepend** new Keep’d C-wrongs here (not under Open).

A **JS throw** in any corpus session (`hidden-proxy status` owner
`js-throw …`, or a `ReferenceError` in `.cache/hidden/scores.json`
`error`) is always a Must-fix row: it forfeits every later screen of that
session (Constitution §10.14).

## Open (corpus-driven, after Must-fix is empty)

Ranked by corpus sessions blocked (`hidden-proxy queue`, scoreboard at
`6f87bee1`+scenario cohort, 2026-09-06: **262/540 PASS**). Every row is
a recorded C-vs-JS fact; the fix is the owning C function's port, never
a read of a seed, step or coordinate. Verify with
`node scripts/verify.mjs --fn <fn>` (uses the committed scoreboard; if
the row was queued at an older SHA pass `--base <sha>`). Refill from
`node scripts/hidden-proxy.mjs queue --limit 30` — rows 13+ of that
output are the next candidates; `PORT-GAP-TOP30.md` rows the corpus
reaches come after; map singletons only at ≥ 90 % corpus PASS.

- [ ] `end.c` savelife — RESTORED to Open: finish-iteration checked this row off by bare iteration-id mention although scen-wish-Valkyrie-92014 is still blocked on touch_artifact@49 (see the quest-arm commit's D-log Next bullet, 0b6f3f56; the DONE entry is kept for audit, not as proof of fix — lesson: never cite the current D-id inside a NEW queue row). Lifesave-decline More-state: step 49/66 kind=screen at artifact.c:966: C «The bow named the Longbow of Diana evades your grasp!» vs JS same + `--More--`. RNG matched through step 48 incl. C `rn2(1)=0 @ readobjnam(objnam.c:5374)` (Longbow non-quest, both sides roll) + `d(4,10)=32` blast + death + Die?-decline; step-49 C `rn2(100)=85 @ makewish(zap.c:6421)` (= JS ublesscnt `rn1(100,50)`, js/zap.js:6728, C order kept) then evade on a clean topline. JS evade text+position correct, only stale More pending → writer is the Die?-decline message state (end.c savelife / display More), NOT touch_artifact/readobjnam/makewish (all C-faithful here; cf. D-2051 family). Probe: `node scripts/hidden-proxy.mjs verify touch_artifact` (expect Valkyrie-92014 → PASS or a later owner after a savelife/More-state port; never re-pop touch_artifact for this More). No seed/step/coordinate gates.
- [ ] `dungeon.c` save_dungeon — blocks 11/553 corpus sessions (first at step 113): C «The Dungeons of Doom:» vs JS «The Dungeons of Doom:». Probe: `node scripts/hidden-proxy.mjs verify save_dungeon` (scen-genesis-Barbarian-91118, scen-genesis-Samurai-92080, scen-normal-Caveman-92140).
- [ ] `wield.c` ready_weapon — blocks 8/553 corpus sessions (first at step 20): C «You cannot wield a two-handed weapon while wearing a shield.» vs JS «e - a dwarvish mattock (weapon in hands).». Probe: `node scripts/hidden-proxy.mjs verify ready_weapon` (scen-genesis-Valkyrie-92166, scen-wish-Knight-92204, scen-wish-Monk-92063).
- [ ] `insight.c` show_conduct — blocks 6/553 corpus sessions (first at step 859): C «Ebenezum walked before me along the closest thing we could» vs JS «More info about "human wizard"? [yn] (n)». Probe: `node scripts/hidden-proxy.mjs verify show_conduct` (random-seed0360-wizard-world-tour-e115a25b, scen-genesis-Knight-92002, scen-genesis-Knight-92112).
- [ ] `teleport.c` collect_coords — blocks 3/553 corpus sessions (first at step 77): C draws `rn2(8)=4` in collect_coords, JS `rn2(3)=2` from mhitm_knockback(mhitm.js:2029). Probe: `node scripts/hidden-proxy.mjs verify collect_coords` (scen-genesis-Knight-92185, scen-poly-Wizard-92169, scen-tour-Tourist-92100).
- [ ] `mcastu.c` mcast_disappear — blocks 1/553 corpus sessions (first at step 121): C «The lich suddenly becomes transparent!» vs JS «The lich suddenly becomes transparent!». Probe: `node scripts/hidden-proxy.mjs verify mcast_disappear` (scen-wish-Monk-92013).
- [ ] `attrib.c` from_what — blocks 1/553 corpus sessions (first at step 69): C «You were acid resistant from your creature form.» vs JS «You were acid resistant from your creature form.». Probe: `node scripts/hidden-proxy.mjs verify from_what` (scen-death-Monk-92000).
- [ ] `pager.c` look_at_monster — blocks 1/553 corpus sessions (first at step 148): C «ca cockatrice (chickatrice) [seen: normal vision, infravisio» vs JS «ca chickatrice [seen: normal vision, infravision]». Probe: `node scripts/hidden-proxy.mjs verify look_at_monster` (scen-genesis-Archeologist-91135).
- [ ] `trap.c` erode_obj — blocks 1/553 corpus sessions (first at step 86): C «Your robe smoulders!» vs JS «Your cloak smoulders!». Probe: `node scripts/hidden-proxy.mjs verify erode_obj` (scen-normal-Priest-92020).
- [ ] `eat.c` start_tin — blocks 1/553 corpus sessions (first at step 118): C «Using your 6 orcish daggers you try to open the tin.--More--» vs JS «Using your orcish daggers you try to open the tin.--More--». Probe: `node scripts/hidden-proxy.mjs verify start_tin` (scen-normal-Rogue-92115).
- [ ] `dothrow.c` hurtle_step — blocks 1/553 corpus sessions (first at step 110): C «You bump into a little dog. You stagger... The baluchitheriu» vs JS «You bump into the little dog. You stagger...--More--». Probe: `node scripts/hidden-proxy.mjs verify hurtle_step` (scen-poly-Archeologist-92226).
- [ ] `topten.c` formatkiller — blocks 1/553 corpus sessions (first at step 69): C «» vs JS «». Probe: `node scripts/hidden-proxy.mjs verify formatkiller` (scen-wish-Wizard-92060).

## Deferred (map-driven singletons — do not pop while any corpus family is < 90 % PASS)

Plain bullets on purpose (not popped, not counted). Re-enable as `- [ ]`
Open rows only when `hidden-proxy status` shows every family ≥ 90 %.

- `monmove.c` dochug demon/caster retaliation — MS_BRIBE mux skipped by D-1798; live `demon_talk`/`cuss` unwired at monmove.c:823/985 (sounds.c:1143/1150 wired).
- `artifact.c` artiname/discover_artifact/artidisco[] save-rest — discovery announce + artidisco bit (D-1107 live; save/rest artidisco named; c-js-map data.md).
- `artifact.c` restore_artifacts save-rest — artilist restore on load (named in init_artifacts D-1201 row; c-js-map data.md).
- `artifact.c` arti_invoke on drop / questart artitouch / zap-poly addinv_core1 — invoke-touch family (named in cspfx W_ART D-1539 row; c-js-map data.md).
- `vision.c` howmonsseen — artifact-warn see-monsters helper (named in SPFX_WARN D-1514 row; c-js-map data.md).
- `dogmove.c` dog_move beg/dog_hunger caller wiring — dogmove.c:383 `beg(mtmp)` unwired in live `js/dogmove.js` dog_move (named in `beg` D-1763 + js/sounds.js:518; c-js-map turns.md).
- `wintty.c` core cliparound call sites — allmain.c:546 moveloop, dungeon.c:1580 u_on_newpos, muse.c:2637, restore.c:629 (named D-1974/D-1982; tty_cliparound live, unwired; c-js-map turns.md).
- `display.c` get_othersym base + assign_graphics showsyms copy (named D-1983; SYM_OFF_X/SYM_MAX live; c-js-map turns.md).
- `display.c` docrt_flags maponly/redrawonly/nocls + post_map botlx/update_inventory (named D-1974/D-1981; c-js-map turns.md).

## Parked (do not pop)

- `mcastu.c` mcast_death_touch — SYMPTOM OWNER, writer unidentified (parked 2026-09-09, no D-log, no js/). scen-tour-Tourist-92134 step 71/84 kind=screen: C «Oh no, he's using the touch of death! You feel drained...--More--» vs JS «Oh no, she's using the touch of death! You feel drained...--More--». Baseline `verify mcast_death_touch` on a clean tree: 0 PASS, 0 moved, 1 unchanged → NO MOVEMENT (green 2/2 + strict + cohort 7/7 held). JS body is arm-for-arm C-faithful (`js/mcastu.js:442–462` vs `mcastu.c:389–408`: pline/mhe, nonliving/demon, `!Antimagic && rn2(m_lev)>12`, Hallucination, touch_of_death, monstunseesu, else shieldeff+monstseesu+Lucky) and `mhe`/`pronoun_gender` match C verbatim (`js/mondata.js` vs `mondata.c:1188–1207` + `you.h:322`, hallu `rn2(4)` first). C-side proof the body is innocent: step-71 C dice are exactly `rn2(27)=16 @ mcast_death_touch:394` (caster m_lev 27) + `d(8,6)=35 @ touch_of_death:326` — zero draws in any pronoun path (no `rn2(4)` hallucination arm); flat RNG matches 17006/17006 through C's last draw (JS extras are tail-only). Deeper, both sides agree the caster is the unique m_lev-27 arch-lich (adj_lev 25+2), born step 47 as 16th of 34 `newmonhp` (`d(27,8)=129`, idx 3643; sequences identical both sides, all 34 values+order) with gender draw `rn2(2)=1 @ makemon:1279` immediately after (idx 3644) → C female=1; JS identical (gender draws 17/17 at step 26, 26/26 at step 47, same order+values; JS lich female=1 from birth through step 71 per prefix-state probes; extractor regenerates `monsters_data.js` byte-identical; no MM_FEMALE/MM_MALE/leader/nemesis arm involved, mmflags=0). Yet C prints "he", contradicting upstream `pronoun_gender` for a spotted humanoid non-neuter with female=1 and no hallu draw — so the writer is upstream C state (caster identity/form), unidentified with available oracles. Live candidates, all unproven: mplayer/doppelganger `newcham` into lich form (step-68 `decide_to_shapeshift`/`pickvampshape`/`mgender rn2(10)=4` draws belong to an unidentified shifter; a JS doppelganger sits at (44,11)), `mon_arrive` migration (step-69 «suddenly appears next to you» both sides), or cross-step gender assignment. Needs a C per-turn mon dump (caster `female`/`cham`/`mnum` at step 71) or a JS/C fmon-order audit. Falsifier: such a dump or a writer port (`mplayer`/`newcham`/`mon_arrive`) moving `verify mcast_death_touch` (expect Tourist-92134 → PASS or a later owner); re-queue under the writer it names — never re-pop mcast_death_touch (a body port is PROVEN no-movement). No seed/step/coordinate gates.
- `do.c` u_stuck_cannot_go — MISATTRIBUTED + STALE OWNER (parked 2026-09-09, no D-log, no js/). Queue row claimed scen-poly-Valkyrie-92195 step 172/313 kind=screen at do.c:1123 (C «You release the iron golem. The iron golem hits!--More--» vs JS «The iron golem hits! The iron golem hits!»). Owner string is the topline-literal tie-break across 3 cMsgOwners (`u_stuck_cannot_go(do.c:1123)`, `savelife(end.c:751)`, `escape_from_sticky_mon(hack.c:2653)`). C-side proof u_stuck_cannot_go is innocent: the recorded step-172 key is `n` (movement family, not a stair key), and u_stuck_cannot_go's only callers are the stair commands (`dodown do.c:1221`, `doup do.c:1321`, keys `>`/`<`) — a stair-gate port cannot produce movement-key behavior. True writer is the domove-path printer `escape_from_sticky_mon` (same «release» literal via `y_monnam`; non-tame golem so `y_monnam`≡`mon_nam`≡«the iron golem»), already shipped (D-2158 cb8412fa, «1 session PASS»). The session moved past with zero new code: clean-tree live replay at HEAD prints the identical release line at the identical step (JS probe across the window: `u.ustuck`=iron golem, `sticks(youmonst.data)`=true), and `verify u_stuck_cannot_go` → 0 PASS, 1 moved past (172→200 `do_statusline2`@botl.c:130, identical toplines «You turn into a brown mold!--More--» both sides), 0 worse; working scoreboard (scrM 310→312) committed in the park. Live residual is `do_statusline2`@200 — left for the next rescore to queue under its own attribution, not re-queued here undiagnosed. Falsifier: a rescore or fresh `verify u_stuck_cannot_go` showing a session blocked at a stair-key step under owner u_stuck_cannot_go; re-queue under the writer it names — never re-pop u_stuck_cannot_go while a move-key step blocks. No seed/step/coordinate gates.
- `attrib.c` adjattrib — STALE OWNER (parked 2026-09-09, no D-log, no js/). Queue row claimed scen-normal-Samurai-92071 step 16 (C dat-text «By that time, Narahara had already slipped his arm from the» vs JS «More info about "human samurai"? [yn] (n)», scoreboard fae41579). Clean-tree live replay at HEAD passes step 16: `verify adjattrib` → 0 PASS, 1 moved past → pick_lock@175; `verify pick_lock` confirms the live first divergence is step 175/205 «You feel no door there.» vs «You see no door there.» (all three C sites lock.c:591/:851/:1015 print `Blind ? "feel" : "see"`). C-side proof adjattrib is innocent: recorded C steps 0–16 contain no adjattrib message (every prompt step nrng=0); the step-16 text is dat/data.base:4795 (samurai entry) displayed by pager.c checkfile with without_asking (getpos ':' → LOOK_VERBOSE → do_look chkfilDontAsk; JS pager.js:2087–2089 + getpos.js:1293 already pass the flag — a later port fixed the step-16 look path, no adjattrib arm involved). The owner string was the "already" literal tie-break across adjattrib:179/youhiding:2075/doquiver_core:635 — same class as parked doname_base/zapyourself/spoteffects. An adjattrib verbose-arm port guarantees NO MOVEMENT. Falsifier: a rescore or fresh `verify adjattrib` showing the session blocked on adjattrib again; re-queue under the writer it names — never re-pop adjattrib. Re-queued as Open `lock.c` pick_lock feel/see (step 175; distinct arm from archived D-2002 direction-with-monster). No seed/step/coordinate gates.
- `eat.c` lesshungry — STALE OWNER (parked 2026-09-08, no D-log, no js/). Queue row claimed scen-wish-Tourist-91125 step 162 (C «You're having a hard time getting all of it down.--More--» vs JS «Your movements are now unencumbered.--More--», scoreboard stamped bb24a32f/18:37). Clean-tree live replay at HEAD (0bed4afc) diverges at step 82–83 (RNG 2745/3232, Screen 83/189; `verify lesshungry` → REGRESSION, owner do_statusline2@82) — the session never reaches the lesshungry step, so a lesshungry port guarantees NO MOVEMENT. JS lesshungry already C-faithful (D-1356; brief shows arm-for-arm match of eat.c:3289–3333 incl. iseating/choke/fullwarn/paranoid). Proven stale, not HEAD-caused: identical FAIL with bb24a32f's js/end.js swapped in (D-2145 touched js/end.js death-disclosure only, not live at step 82); js/ has no Date/Math.random, the C recording is unchanged (rngT 3232 both sides), and 3 live replays agree exactly — so the 18:37 rescore ran on a working tree that never became the committed bb24a32f (D-1831 class). Live owner do_statusline2@82 is the already-parked lembas pair (same session+step; falsifier: botl-parity iteration keeping 44/44 green). Falsifier: a rescore or fresh `verify lesshungry` showing the session reaching step 162 under owner lesshungry; re-queue under the writer it names — never re-pop lesshungry while step 82 blocks. No seed/step/coordinate gates.
- `apply.c` magic_whistled — FIRED + MISATTRIBUTED owner (2026-09-08, no D-log, no js/). Clean-tree `verify magic_whistled` (scoreboard at b567c868, tree e13647e8): scen-genesis-Priest-91110 step 78 → minliquid_core@130 with no js/ — the tree already contains D-2096's enexto_core/appear-msg fix (213658d3, same «next to you» vs «close by» genesis family). Owner `apply.c:523` is the `sizeof "appears"` declaration literal, first of 3 cMsgOwners (`magic_whistled(apply.c:523)`, `makemon(makemon.c:1497)`, `summonmu(mhitu.c:1019)`); step-78 C dice are collect_coords/goodpos/next_ident/newmonhp/makemon/m_initinv with ZERO whistle draws (recipe is `^G` genesis, no whistle use); true printer `makemon.c:1492–1500` (`next2u(x,y) ? " next to you" : distu ≤ 64 ? " close by"`), whose JS predicate matches C exactly (`js/makemon.js:3148` `du <= 2`; `next2u ≡ distu ≤ 2` per `you.h:558`). Live replay at HEAD: step-78 topline + map identical both sides (@ (56,3), `;` (58,6)). Re-queued as Open `mon.c` minliquid_core@130 (rng-first: C `rn2(15)=2` vs JS no draw after mhitm_knockback; lifesave topline vs empty). Falsifier: a rescore or fresh `verify magic_whistled` naming a live step-≤78 block; re-queue under the writer it names — never under magic_whistled. Do not re-pop. No seed/step/coordinate gates.
- `weapon.c` dmgval — FIRED owner (2026-09-08, no D-log, no js/). Clean-tree `verify dmgval` (scoreboard at 1602fc13, tree eb7ec1cb): Priest-91137 dmgval@123 → do_statusline2@214 with no js/ — the only js/ delta since the scoreboard is D-2139's `dothrow.js` coin arm. JS dmgval already faithful (D-1793/D-0189/D-1354) — no port to ship. Re-queued as Open do_statusline2@214 (HP 44 vs 43; symptom owner). Do not re-pop. No seed/step/coordinate gates.
- `objnam.c` doname_base — MISATTRIBUTED owner (parked 2026-09-08, no D-log, no js/). scen-wish-Rogue-92037 step 54/233 kind=screen: identical text («k - a long sword named Sunsword (weapon in right hand).» both sides), first cell diff row 0 col 55 C `--More--` vs JS ``. Owner `objnam.c:1590` is the topline-literal tie-break (a `/* ... "(weapon in hand)" ... */` comment line) across 3 cMsgOwners (`doname_base(objnam.c:1590)`, `weldmsg(wield.c:1069)`, `ready_weapon(wield.c:215)`); the true printer is `ready_weapon`'s `prinv` + artifact-light shine queue (`wield.c:232–251`). C-side proof doname is innocent: step-54 C dice are a single `rn2(4)=1 @ touch_artifact(artifact.c:945)` (badalign `!rn2(4)` → no blast, proceed to wield) with ZERO dice in `doname_base` (pure formatter for this path: xname → `(weapon in right hand)` suffix `:1591–1595`, glow gate `:1599` false since `lamplit` is unset pre-`begin_burn`), and step-55 C topline is the queued shine pline («The long sword named Sunsword begins to shine brightly!») — hence the More at 54. JS predicts the observation exactly: `js/wield.js:432` defers `arti_speak / artifact_light / unpaid shop` and `:314–315` names `setuwep-path Sunsword begin_burn / ready_weapon shine` as omitted, while `js/timeout.js:1237` `artifact_light` already returns true for Sunsword — so JS prints the wield `prinv` (`js/wield.js:423` via `xprname`) with no second message queued, hence no More and no shine. A `doname_base` body port (526 lines) guarantees NO MOVEMENT (baseline `verify doname_base` on a clean tree, scoreboard at 1bb96408: 0 PASS, 0 moved, 1 worse → same-step re-report noise, cf. distfleeck park). Falsifier: a `wield.c ready_weapon` shine-arm port (`artifact_light && !lamplit` → `begin_burn` + `pline("%s to shine %s!")`) moving `verify doname_base` (expect Rogue-92037 → PASS or a later owner); re-queue as Open `wield.c ready_weapon` shine when the `ready_weapon` park lifts (Knight-92204 hang profiled). Do not "fix" with seed/step/coordinate gates. Probe: NOTES.md Active `doname_base park`.
- `zap.c` zapyourself — MISATTRIBUTED owner (parked 2026-09-08, no D-log, no js/). scen-tour-Priest-92235 step 60/111 kind=screen: identical toplines («The sleep ray hits the little dog. The sleep ray hits you!--More--» both sides), first cell diff row 6 col 8 C `@` vs JS `│`. Owner `zap.c:2860` is the topline-literal tie-break (`pline_The("sleep ray hits you!")`) across 3 cMsgOwners (`zapyourself(zap.c:2860)`, `toss_up(dothrow.c:1269)`, `peffect_polymorph(potion.c:1320)`); the true printer on this turn is `dobuzz` `:4962–4964` (`pline_dir(xytodir(-dx,-dy), "%s hits you!", The(flash_str(...)))`). C-side proof zapyourself is absent: the turn is a MONSTER zap (step 59 «The salamander zaps a curved wand!», beam hit dog via `zhitm`/`resist` dice step 59, then hero step 60); `zapyourself` runs only on hero self-zap (`dozap` no-direction branch `:2658`, `spell.c` nondirectional, `music.c` horns) — never on a monster-zap `buzz` path. Step-60 recorded dice are a single `rn2(20)=8 @ zap_hit(zap.c:4709)` (dobuzz to-hit); a zapyourself sleep hit would draw `rnd(50)` for `fall_asleep` — zero dice in zapyourself's range. JS `zapyourself` sleep arm is already C-faithful (`js/zap.js:4287–4299`, exact order + `rnd(50)`), and the JS `dobuzz` hero arm already matches C order (`js/zap.js:2201–2236`, D-2133/D-1216) — so the residual is beam/tmp_at display state at the More capture, not zap logic. A zapyourself body port is PROVEN no-movement (baseline `verify zapyourself` on a clean tree, scoreboard at 4d859c92: 0 PASS, 0 moved, 1 unchanged → NO MOVEMENT). Falsifier: a `dobuzz`/display beam-paint port (why C shows `@` where JS shows `│` at row 6 col 8 at the «hits you!» More prompt) moving `verify zapyourself` (expect Priest-92235 → PASS or a later owner); re-queue under that writer. Do not "fix" with seed/step/coordinate gates.
- `artifact.c` dump_artifact_info — MISATTRIBUTED owner (parked 2026-09-08, no D-log, no js/). scen-wish-Priest-92136 step 126/170 kind=screen: identical toplines («The credit card named the Platinum Yendorian Express Card evades your grasp!» both sides), first cell diff row 1 C `` vs JS `--More--`. Owner `artifact.c:1203` is the topline-literal tie-break across 3 cMsgOwners (`dump_artifact_info(artifact.c:1203)`, `the(objnam.c:2222)`, `touch_artifact(artifact.c:966)`) — the dump match is the C *comment* `/* "The Platinum Yendorian Express Card" is 35 characters */`, not a print; the true printer is `touch_artifact :966–970` (`pline("%s your grasp!", Tobjnam(obj, "evade"))`), present in stepFns with `exercise`+`makewish` (dump absent, zero draws). C-side proof dump cannot run here: sole caller is `o_init.c:1066–1070` behind `wizard && y_n("Dump information about all artifacts?")`, and the recipe has no discovery dump; JS body (`js/artifact.js:475`, brief `sync`) already matches C order/bits/`%-36.36s` padding. A dump body port is PROVEN no-movement (baseline `verify dump_artifact_info` on a clean tree, scoreboard at 8c0e9448: 0 PASS, 0 moved, 1 unchanged → NO MOVEMENT). JS `touch_artifact` evade arm is already live (`js/artifact.js:1163–1169`), so the residual is --More-- timing on the wish/touch path, not dump content. Falsifier: a `touch_artifact`/makewish-port moving `verify dump_artifact_info` (expect Priest-92136 → PASS or a later owner); re-queue under that writer — cf. Open `artifact.c touch_artifact` (genesis-Valkyrie-92094, same «evades yo» family). **FIRED 2026-09-08 by D-2134** (`verify dump_artifact_info`: Priest-92136 dump@126 → dog_goal@136); re-queue as Open `dog.c dog_goal`. Do not "fix" with seed/step/coordinate gates.
- `hack.c` spoteffects — MISATTRIBUTED owner (parked 2026-09-08, no D-log, no js/). scen-tour-Samurai-92161 step 35/88 kind=screen: identical toplines («You are hit by a shuriken! You are hit by a shuriken!--More--» both sides), first cell diff row 8 col 53 C `·)·` vs JS `···` (floor shuriken below hero `@`(53,7), dog `d`@(52,7)). Owner `hack.c:3434` is the topline-literal tie-break (`You("are hit by %s!"` piercer arm); the true printer is `thitu(mthrowu.c:121)` (`You("are hit by %s%s"`), one of three `cMsgOwners` sharing the literal — `hidden-worker.mjs:182-183` picks `cMsgOwners[0]` whenever the topline matches, even with identical toplines and a map-cell diff. Proven: step-35 C draws (22: exercise/mulch/obj_resists/monmulti/next_ident/m_throw×5/u_catch/dmgval/thitu × two volleys) contain ZERO spoteffects dice (no `rn2(2)` lev-timeout, no `rnd(20)`/`d(4,6)` piercer); JS RNG matched positionally through the step (first mismatch index 4084, ~34 draws later); JS step-34 screen MATCHED incl. the `)` (placed at arrival step 34 both sides) and JS loses it at 35 while C keeps it through 35 (C clears it at 36 with «You catch the shuriken! g - a shuriken.»); no spoteffects arm touches (53,8) (`m_at` surprise needs a mon on the hero square, `pickup` takes the hero square only, no trap/ice/pool evidence). A spoteffects body port is PROVEN no-movement. D-2111 already moved this session `next_ident`@34 → `spoteffects`@35 (weapon.js PM_NINJA volley); this is the residual. True writer is the draw-free `mthrowu.c` flight/catch path — two concrete C-wrongs in `js/mthrowu.js`: (a) the flight loop draws `forcehit = !rn2(5)` then discards it (`mthrowu.js:1070-1071`, C `:798` stop condition via `MT_FLIGHTCHECK`), so JS landing squares diverge with identical dice; (b) `u_catch_thrown_obj` success returns true silently (`mthrowu.js:1036-1039`) without C's `hold_another_object` («You catch the %s!» + inventory), predicting JS step 36 exactly (no catch message, no `g - a shuriken`, dog later standing on (53,8) `·d·` vs C `···`). Falsifier: an `mthrowu.c m_throw`/`u_catch_thrown_obj` port (forcehit stop + catch inventory/pline) moving `verify spoteffects` (expect Samurai-92161 → PASS or a later owner); re-queue as Open `mthrowu.c m_throw` when a second session needs it. Do not "fix" with seed/step/coordinate gates. Probe: NOTES.md Active `spoteffects park`.
- `mon.c` can_carry — IMPORT-ONLY PARK (re-measured 2026-09-08, no D-log, no js/). Step-13 writer for Knight-92182 (C `rnd(20)=6 @ mattackm` vs JS `rn2(1)=0 @ dog_move:976`; stub 0 on 640-wt chest vs C=1). Import (`monmove.js:248`, SAFE) reproduces the parked hang: `verify mattackm` REGRESSION (→ worker ETIMEDOUT). Ship only as trio with the mattackm park below. Do not pop.
- `mhitm.c` mattackm — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/ — can_carry import fixes step 13 but hangs the session, reverted). mattackm body faithful; a body port is PROVEN no-movement. At stream 2572: JS goal=(33,13) hero/UNDEF (can_carry=0: chest 640 > stub max 448) vs C goal=(38,13) CHEST/APPORT (C=1: 200+640 ≤ 1000 strong-flat-cap) — JS draws two extra `rn2(++chcnt)` first, shifting dieroll 6→3 and all after. mfndpos arms measured dead. Falsifier (ship together; never alone): can_carry import + #2 writer (stream 3037: C obj_resists vs JS distfleeck) + move-99 spin (N=98 ok, N=99 sync-hangs in `F b`; CRL cleared). No seed/step/coordinate gates.

- `were.c` were_change residual scen-intrinsic-Healer-92124 — MISATTRIBUTED owner (parked 2026-09-08, D-2113, no js/ for the residual). D-2113 shipped the genuine howl arm (scen-tour-Ranger-92177 fully PASS, 30739/30739 RNG + 135/135 screens). Healer step 69/145 is a screen-only diff with identical toplines («The jackal bites!» both sides) and row 3 C `$`@col 57 vs JS ``; the `were_change` owner string comes from the topline word «jackal» matching the `were.c:28` literal (likewise `were_summon :168` in cMsgOwners). C-side proof were_change is inactive at that step: the recorded 15 step dice are `distfleeck`×3, `m_move rn2(32)`, `mattacku rnd(20)`, `hitmu d(1,2)`, `mhitm_knockback rn2(3)=2` then `rn2(6)=4`→`return FALSE` (`uhitm.c:5258–5271`, no knockback), `mcalcmove`×2, `maybe_generate_rnd_mon`, `regen_hp`, `gethungry`, `moveloop_core`×2 — ZERO dice in `were.c`, positional RNG 2729/2729 matched. A were_change body port is PROVEN no-movement here. Falsifier: a JS prefix-state probe of map cell (57,0) at step 69 (monster covering gold that C's `m_move` step moved away vs a gold-paint gap in `newsym`/`show_glyph`), then re-queue under the writer that probe names — never under `were_change`. Do not "fix" with seed/step/coordinate gates.
- `zap.c` lightdamage — MISATTRIBUTED + transient-artifact park (parked 2026-09-08, no D-log, no js/). Both blocked sessions diverge for exactly ONE mid-arm `--More--` frame inside `muse.c use_misc` MUSE_WAN_MAKE_INVISIBLE (monster self-zap with make-invisible): C shows the under-cell (`%` bee-corpse at genesis-Barbarian-92062 step 123 / `·` floor at tour-Wizard-91112 step 73) where JS shows the still-visible mon (`h` mind flayer / `L` master lich); the very next frame both print "Suddenly you cannot see..." + `map_invisible` → `I`, with full positional RNG sync (4241/4241; 91112's zap turn draws nothing at all) and every later screen matching. Proven: the printing function is `muse.c mzapwand` (`monverbself`, muse.c:182-185), not `zap.c lightdamage` (hero-gremlin-only, already faithful per D-1366 — a body port guarantees NO MOVEMENT); the muse arm and `map_invisible` behave identically on both sides (same `vismon && minvis && !canseemon` branch, same follow-up, convergence). The C under-cell paint has no game-code producer in the span: session 2 shows the lich visible `L` at step 72 → bare floor at 73 with zero intervening RNG draws and zero messages before "zaps himself" — but `newsym`/`map_object` can only reveal the under-cell for a hidden/absent mon while `minvis` flips only after the blocking pline, so no correct C path paints it there. Verdict: recorder/shadow-capture artifact at the MORE prompt, not game state. Falsifier: a live-C per-turn dump (mon minvis/pos, levl glyph, shadow writes) across the zap turn showing a real repaint, or a session where the transient does NOT converge on the next step. Do not "fix" by painting `%`/`·` in JS display (D-1831 trace-tailoring ban).
- `teleport.c` rloc — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/). Both blocked sessions diverge INSIDE losedogs→mon_arrive(After_you, MIGR_RANDOM)→rloc (teleport.c:1850 50-try `rnd(COLNO-1)`), but rloc's body is faithful (D-0686) so a body port is PROVEN no-movement. Healer-92042 step 73 fully diagnosed: C pick place_lregion(45,8) (whole-level dndest, no monster) → u_on_newpos(45,8) → losedogs → mon_arrive migrant → rloc rnd(79)=69,rn2(21)=16 try-1 success → turn ends; JS identical through place_lregion(45,8) (screens byte-identical @45,8, all prior RNG matched) but holds NO migrant for (2,8) (mydogs empty; 6 orcs →(2,4),(2,5),(3,0) only), draws nothing, proceeds to movemon (Healer JS: mcalcmove rn2(12)). True writer is the draw-free migration CREATION (migrate_orc/muse-stairs/dig-hole/dokick/mon.c RANDOM creators), not rloc/put_lregion_here/mon_arrive. Drive-by (not this iter, no session reaches it): js/mklev.js:530 calls async rloc without await (floating rloc_to + dead limbo fallback). Falsifier: C migrating_mons dump at arrival naming the →(2,8) migrant + its creation step, or a creator port moving `verify rloc`. Ranger-92033 (same owner, JS next_ident@makemon) UNPROBED — may be same writer or shop-stock; needs own prefix probe. Do not "fix" with seed/step/coordinate gates.

- `monmove.c` m_move — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/). Both blocked sessions diverge INSIDE m_move's selection loop (recorder monmove.c:2003 = upstream :1963 `rn2(4*(cnt-j))` track-avoid; :2011 = upstream :1970 `rn2(++chcnt)`; +40 patch shift; JS site monmove.js:1816, scoreboard :1798 stale by 18 lines), but the loop body is faithful (branch order/short-circuit verified vs C) — both splits are draw-free INPUT state, so a body port is PROVEN no-movement. (A) scen-poly-Wizard-92076 step 94: C `rn2(1..4)` chcnt ladder (appr==0; no appr-block draws, no track draws) vs JS `rn2(24)` track check (appr!=0). Same step C topline «You are now hiding on the ceiling.» (polyself.c dohide → u.uundetected=1 → youhiding(FALSE,0)) vs JS «Any special ability...» (named omission js/polyself.js:1527/1562 `return tail(); // dohide deferred`; D-1898 shipped the other five arms). Re-queued as Open `polyself.c` dohide (head row). Falsifier: dohide port moving `verify m_move` for Wizard-92076. (B) scen-poly-Caveman-92202 step 103: C `rn2(20)=5` vs JS `rn2(16)=13` — first draw of that m_move call, both nonzero (both skip), appr!=0 both sides; cnt-j differs by exactly 1. D-1868 mfndpos arms already shipped (30f0e9b7, tour-Healer passes), so the +1 is not those arms. Candidates: mfndpos extra non-track-matching candidate in C, one-slot mtrack history shift (C clears at mon.c:4394/monmove.c:529/makemon.c:896/vault.c:226/dog.c:458/teleport.c:1683 vs JS live call only monmove.js:820), or draw-free m_search_items goal shift (D-2047 open). Needs C per-turn state dump (cnt/mtrack/goal of the moving mon). Falsifier: per-session owner row moving under that writer's port. Do not re-pop m_move. Do not "fix" with seed/step/coordinate gates.
- `mon.c` mcalcmove — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/). All 3 blocked sessions diverge AT C's unconditional `rn2(NORMAL_SPEED)` rounding draw (mon.c:1164) with JS drawing something else or nothing; the mcalcmove body is faithful (MSLOW/MFAST/rounding match; gallop arm inactive in all three — C divergent draw is rn2(12) not rn2(2), JS usteed=false — so a body port is PROVEN no-movement). Two true writers. (A) scen-wish-Archeologist-92216 step 180: rest-safety flip — C topline "" vs JS "Are you waiting to get hit? ...": C monster_nearby=0 ('.' rest allowed, ECMD_TIME, block runs; C step 179 values [7,9,14,1,62,14,62] exactly equal JS J[3112..3118], proving sync through block179, then C runs a second full block at step 180 [0,4,51,1,86,17,15]) vs JS =1 (adjacent Large Mimic, rest prevented ECMD_OK, block skipped, next key draws impaired rn2(5)). Of monster_nearby's predicates only M_AP_TYPE differs: C masks (monst.h `M_AP_TYPE & M_AP_TYPMASK` → OBJECT → skip) while JS returns raw m_ap_type=10 (OBJECT|F_DKNOWN, const.js:3184) → no skip. Falsified same-call: onscary live-false both sides (no Elbereth/scare/altar at hero (12,9)); terrain identical (geom-probe 0 cells); monster data identical (mimic mmove 3, zombie mmove 6, 2 mons both sides); helpless/canspotmon/noattacks/hider match. Re-queued as Open `const.js` M_AP_TYPE (head row). Falsifier: mask port moving `verify mcalcmove` for Archeologist. (B) scen-wish-Rogue-92137 step 26 + scen-death-Knight-92188 step 20: C lifesave toplines (cMsgOwners savelife end.c:727, unmul hack.c:4187, slimed_to_death timeout.c:508) vs JS "" with NO JS draws after exercise — JS never runs the slime-death/lifesave sequence (possible missing lifesave or slime-application gap). UNPROBED beyond proxy attribution — needs its own prefix-state (Slimed/amulet/HP) + C-step-rng diagnosis. Falsifier: per-session owner rows moving under a savelife/slimed_to_death port. Do not re-pop mcalcmove. Do not "fix" with seed/step/coordinate gates.
- `botl.c` do_statusline2 lembas pair — PARKED 2026-09-07, no D-log (was Open: 6 sessions; ports reverted, tree green). scen-wish-Healer-92092 step 58 + scen-wish-Tourist-91125 step 82: identical trigger (wished lembas eaten beside a hostile monster), identical two-part symptom — (a) JS paints `Satiated` on the `delicious!--More--` step where C keeps the stale bare line; (b) JS aborts the meal `You stop eating...` where C finishes `You're finally finished.`. Diagnosed end to end with byte-exact local probes: (a) C repaints status only on botl/botlx/time_botl (allmain.c:473–479) while JS `bot()` runs unconditionally every iteration (allmain.js:1158) — gating it fixes step 58 byte-exact on both sessions; (b) JS `stop_occupation` (hack.js:1014) lacks C's `maybe_finished_meal(TRUE)` gate (allmain.c:684–695 + eat.c:3877–3890): with the genesis'd chickatrice adjacent-hostile in both (mpeaceful=0, maps identical, RNG fully matched), JS kills a meal at usedtime>=reqtime while C completes it via done_eating — porting the gate fixes step 60 byte-exact on both. BUT the botl gate regresses 7 fortress sessions, so both ports were reverted: menu-close blank ×4 (process_menu_window row 22 — Options/fullscreen teardown needs a C docrt-equivalent repaint; JS _statusSuppressed persists with flags cleared), stale T ×1 (seed0007 T:350 vs 351), stale HP ×1 (seed0002 sick 11 vs 5), stale gold ×1 (seed0399 wish $:0 vs 30) — JS is missing botl/time_botl sets the C paint relies on. Residual 4 sessions are other writers (uac/HP/order per D-1990/D-2024-Next). Resume notes: falsified, do not re-chase — nutrition double-count (JS bite counts match C exactly: start bite + one eatfood bite per turn), lembas/cram race adjustment (both heroes unpoly'd human/gnome, adj is identity), turn-count drift (full-session RNG 3078/3078 positional match forces identical turn counts), peaceful genesis (C defaults hostile too; mpeaceful=0 both sides). Probe recipe: prefix-truncation replay (moves.slice(0,k)) + temporary pre-nhgetch capture-hook logging (screen index, moves, uhunger/uhs, occupation) + bite/lesshungry entry logging; beware module-level eat.js save_hs/saved_hs leaking across prefix runs in one process (use fresh processes for state reads). NOT yet localized — this is the real remaining work, do not claim otherwise: the exact C botl-set sites behind the seed0002 HP, seed0399 gold and seed0007 time repaints, and the fullscreen-menu-close redraw point. Falsifier: a botl-parity iteration (menu-close redraw + the three missing sets) that keeps 44/44 green with the gate on; then re-apply gate + maybe_finished_meal and `verify do_statusline2` (expect the lembas pair PASS). Do not pop until that measurement exists. Do not "fix" with seed/step/coordinate gates.
- `dungeon.c` save_dungeon — MISATTRIBUTED owner (parked 2026-09-07, no D-log, no js/). C `save_dungeon` (`dungeon.c:148–206`) performs only `Sfo_*` NHFILE writes + `free_data` frees — zero display calls, so it cannot paint the overview screen; none of the 4 blocked recipes contains save/restore (`S`/`^S` absent in all four moves), so neither arm can affect these runs. All 4 diffs are `print_mapseen` (`dungeon.c:3516`) live-state content: 92080 row 3 epitaph `you, killed by Demogorgon.` vs JS `a Demogorgon` = `end.c done_in_by` G_UNIQ arm (`:198–207`, `KILLED_BY` + `the ` unless `type_is_pname`), named omitted in `js/end.js:1112–1114`; 92140 (`fire elemental of Ishtar`) / 92050 (`, while paralyzed`) = `done_in_by` mgivenname + `topten.c formatkiller` helpless suffix, both omitted in JS (`js/end.js:463` `void incl_helpless`); 92075 `Sokoban: levels 6 up to 4` vs `up to 5` = `print_mapseen :3544` `dunlev_ureached` topology state. A save_dungeon port guarantees NO MOVEMENT on all 4. Falsifier: true-owner rows re-queued — `hidden-proxy verify done_in_by` moving 92080/92140/92050, and a `dunlev_ureached`-writer port moving 92075. Do not pop until then.
- `teleport.c` collect_coords — SYMPTOM OWNER (parked 2026-09-07, no D-log, port reverted: none needed — body already faithful). JS `collect_coords` matches C verbatim for all live shapes: flags/dims (`CC_*`, `COLNO`/`ROWNO`), `rowrange`/`colrange`/`maxradius` (incl. `cy=10<10.5`≡10, `cx=40`≡40), ring loop bounds/edge predicate, `CC_NO_FLAGS` (no `m_at`/`ZAP_POS`/`filter`), per-ring Fisher-Yates (`rn2(nn)` swap-with-front), `RING_PAIRS` newpass/passend accumulation, rloc call site passes int center (`(COLNO/2)|0`). All 4 blocked sessions show C `rn2(8)`@`collect_coords` with zero tactics draws in stepFns vs JS `tactics`/`distfleeck` at the same index — a body port cannot move them (NO MOVEMENT guaranteed). True owner re-queued as Open `wizard.c tactics/target_on`. Falsifier: that port moving `verify collect_coords`. Do not pop until then.
- `wield.c` ready_weapon — PARKED 2026-09-07: full-arm port (bimanual+shield `:187–191`, weld pline `:198–215`, aklys tether `:232–233`, unpaid `:264–271`; corpse/arti_speak/artifact_light stay named-deferred) moves 5/6 blocked sessions (scen-genesis-Valkyrie-92166 + scen-wish-Valkyrie-92027 + scen-wish-Tourist-92181 PASS; scen-wish-Priest-92041 → use_grapple@146; scen-wish-Samurai-92088 → dodown@88) but scen-wish-Knight-92204 (blessed horn + shield, step 25) spins at ~99% CPU (worker 45 s timeout, no row → verify REGRESSION). New arms proven innocent: byte-identical bimanual pline PASSES on Genesis/Valkyrie mattock; weld pline advances Priest/Samurai/Tourist — so the hang is downstream of the corrected step-25 fail (horn unwielded, no turn), a pre-existing sync loop on that path, not the port. Green fortress 44/44 held throughout. Falsifier: stack/profile of the Knight worker past step 25 (suspects: multi-turn/search lifecycle — recipe carries `20s` keys — or input-exhaustion microtask spin). Do not pop until that measurement exists. Port reverted (no D-log); retry from this note.
- `insight.c` show_conduct — STALE premise + misattributed owner (parked 2026-09-05, HEAD c209ccc7). Row was queued at baf24c95 as "859 conduct-text vs yn prompt"; at HEAD the session diverges at **824 x_monnam** (do_name.c:967): C row 0 `@ a human or elf (human wizard called wizard)--More--` (message-more, map intact, 35 frozen steps) vs JS row 0 Ebenezum `wizard` data.base entry menu (offx 18). Owner `insight.c:2122` is a C **comment** line — C screen is the pager.c checkfile `* wizard` entry, never conduct text. True C chain (all read at HEAD): getpos ':' → LOOK_VERBOSE (hack.h:544-546 "show more info w/o asking") → do_look `:1942-1952` `checkfile(temp_buf=firstmatch, pm, chkfilDontAsk, …)` → pass 1 alt="wizard" displays Ebenezum entry, pass 0 dbase="human wizard" same-offset-skipped; the message-more pending from putmixed is serviced (blocking, 34 bell keys + space at 859) before the menu paints. Proven: `(ans==LOOK_VERBOSE)?chkfilDontAsk:0` alone REGRESSES 859→824 (verify show_conduct), because JS reaches checkfile during turn 824 — its look pline saw `_toplin=NON_EMPTY` (traced) where C had NEED_MORE — so the load-bearing gap is topline-more/window-display timing (does tty_display_nhwindow flush a pending message-more first? cmdq REPEAT interplay from yn_function:1607?), not the flags. Do not pop until: re-baselined `hidden-proxy verify` shows a post-824 owner, or a display-timing iteration takes the do_look+topline envelope together. Do not "fix" with a yn-gate or coordinate seed-gate (D-1831/D-1849).
- `steal.c` mdrop_obj — capture-point divergence, not game logic (parked iter ~2279). explore-seed1500-rogue-explore-move-d7877f7d step 30 (key H): C frame is a MID-TURN --More-- pause (kitten glyph @x70 pre-move, dart pre-place, cursor parked at topline col 32, then 58 rng=0 repeats); JS frame is post-turn (kitten@x69, dart@x70 = JS end-state). C vs JS RNG identical through the session incl. all 48 drop-turn draws site-by-site (drop gates, 2× rn2(8) APPORT arms failed → appr=0 both, loop rn2(1..7) first-accepted/rest-rejected both). Falsified: flooreffects, stay-square accept, appr≠0, ALLOW_U, region veto, digweapon, stale-glyph, sound/pickup/curse extra line. Full mdrop_obj port is a proven no-op here → NO MOVEMENT on verify. Falsifier: C post-turn state (rebuilt-recorder dog_move dump) contradicting JS, or a re-record. Do not contort display (D-1831). See NOTES.md Active `mdrop_obj park`. Do not pop until that measurement exists.
- `worn.c` mon_adjust_speed — MISATTRIBUTED owner (parked 2026-09-08, no D-log, no js/). scen-tour-Barbarian-92079 step 62/119 kind=screen: identical toplines («The titan is suddenly moving faster. The titan casts a spell!--More--» both sides), first cell diff row 5 C `·@·` vs JS `Y@'`; geom-probe 4 cells differ ((6,4)·→Y, (8,4)·→', (6,5)·→D, (7,5)`→H), extends identical, eligible 0, no terrain diff. mon_adjust_speed draws zero RNG and is absent from the step's 161 C draws (exercise/distfleeck/m_move/decide_to_shapeshift/mcalcmove/maybe_generate_rnd_mon/u_calc_moveamt/gethungry/moveloop_core/mdig_tunnel/choose_monster_spell `rn2(17)=16`/castmu `rn2(170)=37`/nasty/pick_nasty/collect_coords×~40/next_ident/newmonhp `d(12,8)=47`/makemon/m_initweap/m_initinv); the owner string is the topline-literal tie-break across 3 cMsgOwners (`mon_adjust_speed` worn.c:554, `castmu` mcastu.c:217, `there_cmd_menu_self`) with identical toplines — same class as parked spoteffects/were_change. Step 62's spell is MCAST_SUMMON_MONS (choose/castmu/nasty/pick_nasty/one makemon; step 63 «Monsters appear from nowhere!»); the «faster» text is step 61's purple-red-potion quaff effect («The titan drinks a purple-red potion!--More--» at 61, muse.c quaff → mon_adjust_speed(mtmp,1,otmp)) surfacing through the MORE queue ahead of the new turn's «casts a spell» — i.e. mon_adjust_speed already ran correctly on both sides a turn earlier; full positional RNG sync through the step means same monsters at same positions both sides, so the writer is monster-glyph visibility/paint timing in the summon path, not the speed printer. JS body (`js/muse.js:2765`, D-0871) verified arm-for-arm vs C worn.c:487–564 (switch/permspeed/boots/message gate/howmuch/learnwand); messages match including no-"much", so mspeed/oldspeed agreed; only residual gap is plain `pline` vs C `pline_mon` (set_msg_xy only — no map effect with accessiblemsg off, proven no-movement, not shipped). A mon_adjust_speed body port is PROVEN no-movement here. Falsifier: a C per-turn dump (mons pos/minvis + levl glyphs at step 62) naming the glyph writer, or a summon/display port moving `verify mon_adjust_speed` (expect Barbarian-92079 → PASS or a later owner); re-queue under that writer. Do not "fix" with seed/step/coordinate gates. Probe: NOTES.md Active `mon_adjust_speed park`.

- `hack.c` dopush — misattributed owner: the step-127 cell is a giant mimic's memory/viz, not the push. explore-seed0116-wizard-wear-shop-cfabc006 step 127/175, single cell r13c32 (map 33,12): C `` ` `` vs JS `·`, RNG 12853/12853, screens re-match at 128. Falsifier: C-side viz at step 127 (`cansee(33,12)` / IN_SIGHT bit) or JS `view_from` boundary audit around wall gap (32,11). See NOTES.md Active `dopush park`. Do not pop until that measurement exists.
- `dogmove.c` dog_invent — misattributed corpus owner (shared `"%s picks up %s."`; both hits are `mon.c mpickstuff`). Iter 2278. Do not pop. Falsifier: `node scripts/hidden-proxy.mjs verify dog_invent` (NO MOVEMENT until proxy rescore). Needs C `movement[]`/`mtrack` on tour-Priest-70006 step 44–45 (also Barbarian step 34: 0 dogmove draws, RNG match).
- D-0006 seed1800 pet movement — needs C state/candidate capture.
- `zap.c` obj_resists — SYMPTOM OWNER, 3 sessions / 3 different writers (parked 2026-09-08, no D-log, no js/). S1 scen-genesis-Valkyrie-92074 step 75 (stoning death, screens 78/78): C savebones `remove_mon_from_bones` → `mongone(Medusa)` → `mdrop_special_objs` (2 ordinary items) → 2× `rn2(100)`@obj_resists, then LEAVESTATUE statue (`mk_named_object`→`mksobj`→`next_ident`+`rndmonst_adj` pass+`rn2(2)` gender) + `drop_upon_death` `rn2(5)`s; JS savebones omits BOTH `remove_mon_from_bones` and the statue arm (named omissions js/end.js:1222-1226) so JS skips exactly the 2 draws — re-queued as Open `bones.c savebones` (all callees live: `mongone`/`mdrop_special_objs`/`discard_minvent`/`dmonsfree`/`mk_named_object`/`drop_upon_death`; `fixuporacle` oracle-only). S2 scen-tour-Samurai-92032 step 59 (fire-trap + `burnarmor` + `destroy_items` + `m_search_items` + `rloc` + vamp-shapeshift in step): C 3× `rn2(100)`@obj_resists mid-m_move vs JS `rn2(5),rn2(1),rn2(2)` then TRUE reconvergence — hypothesis C fire-trap event (burn 3 scrolls?) vs JS silent trap-state skip; needs JS trap/pile state probe. S3 scen-tour-Wizard-92219 step 115: C [stalker D + `m_move:1882` `rn2(3)` + recalc-D] then 3× `rn2(100)`@obj_resists with NO preceding dochug-D, then D… vs JS [same stalker triple] then HORSE(D,silent-move,recalc-D), APE(D,silent-move,recalc-D), [gel cube SKIPPED despite `movement=12`, silently displaced 37,19→37,18]; falsified: pet-food/`dog_move` (no pet in fmon), `gelcube_digests` (draw-free), `meatcorpse` (draw-free), `monstone`, `burn_floor_objects` (`rn2(3)`s absent), `polyuse` (`rn2(minwt+1)` absent), `migrate_mon` (no stair use), uhitm-leprechaun displace, `mcalcmove` (JS matches C verbatim; cube `movement` accrual identical), `minliquid` (ROOM floor), move-gate skips (all eligible per prefix-state dump). Cube-turn paradox stands: dochug-D is unconditional, so OOO-without-D fits no movemon path; needs C per-turn monpos dump or JS fmon-order audit. Falsifier: per-session owner rows moving under `verify obj_resists`. Probe tips: prefix-moves replay + live `ns.game` binding (`resetGame()` rebinds — read AFTER run); `__NH_RNG_TRACE` for JS draw provenance. Do not "fix" with seed/step/coordinate gates.
- `detect.c` dosearch residual — PARKED 2026-09-07, no D-log (was Open: strangulation pair PASS per D-2035; residual 3 sessions). Grid-bug `x` (mnum 116 = PM_GRID_BUG, speed 12, S_XAN class) at different map cells with fully matched RNG (scen-normal-Archeologist-92012 step 11 `s`: C x @(39,7)-decode vs JS id=23 (40,4)→(40,12) in 10 turns; scen-normal-Wizard-92127 step 32 `s`: C x @(54,8)-decode vs JS id=26 (48,7)→(58,6) in 13 turns; first RNG mismatch is strictly after each search step — 92127 idx 2975 = kick_dumb `exercise(A_DEX,FALSE)`+`rn2(3)`, a path consequence since JS kicks toward the adjacent bug) + gremlin short round (scen-tour-Archeologist-92023 step 65, diagnosed `mattacku` per D-2035-Next, own brief when re-queued). Falsifier: C per-turn mon positions during the multi-turn search (geom-probe invalid: appended playmode:debug diverges the run — different drop item, whole-map unknown, RNG totals differ; recorder has no mon-dump). See NOTES.md Active `dosearch grid-bug park`. Do not pop until that measurement exists.
- `timeout.c` slimed_to_death — MISATTRIBUTED + DUAL-WRITER park (2026-09-08, no D-log, no js/). scen-death-Valkyrie-92229 step 37/63 kind=screen: post-`Die?`-decline Demogorgon disease hit prints C «You feel even worse.» vs JS «much worse.». Owner `timeout.c:508` is a C **comment** (`/* follows "OK, so you don't die." ... */`) winning the topline-literal tie-break across 3 cMsgOwners (`slimed_to_death`, `done` end.c:1113, `do_class_genocide`); the differing text is printed ONLY by `potion.c make_sick:158` (`xtime <= Sick/2 ? "much" : "even"`), reached via `mhitu.c diseasemu:1039` (`Sick ? Sick/3+1 : rn1`) with ZERO draws (already sick) — flat RNG matches 3094/3094, 61/63 screens. C history (recorded toplines): first sickness step 24 (`rn2(16)=6` → Sick 26), re-sicks at 27/34/38/43, HP-deaths→declines at 33/37. JS state probe (runSegment + `_preNhgetchHook` logging `u.Sick`/`uprops[SICK].intrinsic`/topline per boundary) proves TWO defects: (A) COMBAT — JS applies an extra already-sick diseasemu hit at step 26 (`u.Sick` 25→9 via /3+1; message scrolled under the `--More--` tail) while C has NO hit there (proven: C-step-26 draws `gethungry/exercise/hitum/passive/tactics/distfleeck/summonmu/choose_spell/mattacku rnd(21)=7/hitmu d(1,4)=4` are die-for-die identical to JS, yet C stays sick-25→24 with a contiguous no-worse topline, and any C reset would force an illness death by step 30 that never comes). Same dice + different application ⇒ draw-free attack-selection gap; lead theory: D-2151's `mhitm_ad_dise_u` runs hitmsg+diseasemu unconditionally — C gates AD_DISE on the attack landing (read `mattacku`/`hitmu` caller gating vs C `mhitu.c`). (B) STORES — C keeps ONE Sick store (`u.uprops[SICK].intrinsic`, youprop.h:108) for diseasemu/make_sick/decrement/expiry/dialogue; JS splits flat `u.Sick` (make_sick/diseasemu/dialogue) vs `uprops[SICK].intrinsic` (generic nh_timeout loop + unconditional writeback `u[flat] = intr`), with flat→uprops sync gated on intr-timeout==0 (`sync_timeout_flats`, timeout.js:306-323). Observed: hit-reset 9 clobbered back to 24 by the writeback; intr stuck at 25 while flat=9; JS Sick rides the stale countdown (→ «much») while C follows resets (→ «even»). Neither half ships alone: store-mirror-alone PREDICTED REGRESSION (JS trajectory 9→3→2→1→silent-clear at ~step 31 — JS has NO `You die from your illness` expiry, timeout.c:703 unported — then next hit draws rn1 + «deathly», RNG-first ~step 34, earlier than baseline); combat-gate-alone PREDICTED NO MOVEMENT (clobber keeps JS high → «much» persists). Ship vehicle (precedent D-2151 cross-file cluster): ONE future iteration porting (A) mattacku AD_DISE landing gate + (B) make_sick single-store mirror (`set_itimeout` on the uprops slot at every `u.Sick` write: make_sick set/partial/full-clear potion.js:960/969/973, holy-water potion.js:1472, zap.js healup curesick :2360; live `set_itimeout` helper already exported from potion.js) + optionally the savelife `(Sick&TIMEOUT)==1` cure (end.c:724-727, PROVEN ABSENT in js/end.js savelife but PROVEN IRRELEVANT here — both declines followed by non-deathly worse with no rn1 draw — so name-only unless its own session appears). Falsifier: mhitu.c caller read confirming the landing gate + a combined port moving `verify slimed_to_death` (expect Valkyrie-92229 → PASS or a later owner). Do not "fix" with seed/step/coordinate gates. Probe: NOTES.md Active `slimed park`.
- `uhitm.c` hmonas — MISATTRIBUTED owner + More-transient (parked 2026-09-08, no D-log, no js/). scen-wish-Priest-92163 step 193/320 kind=screen: identical toplines («You hit the gray ooze! The gray ooze bites!--More--» both sides), 4-cell diff (row 13 col 52 C `1` vs JS blank; r15c51 C `r` vs JS `S`; r16c51 C floor vs JS `r`; HP C 89 vs JS 79). Owner `uhitm.c:5544` is a C *comment* (`/* Do not print "You hit" message; known_hitum already did it. */`), sole cMsgOwner via the topline-literal tie-break (`hidden-worker.mjs:182-183`) — same class as parked doname_base/zapyourself/dump_artifact_info/spoteffects. C-side proof hmonas never runs here: step-193 C dice (13 draws) are gethungry/exercise/hitum(`uhitm.c:780`)/dmgval/knockback×2/known_hitum/passive/distfleeck/mattacku/hitmu `d(2,8)=11`/erode_armor — ZERO dice in hmonas (Upolyd false; stepFns lists `hitum`, not `hmonas`). Positional RNG fully matched through the step (all 13 OK; first mismatch index 3744 at step 248). Transient More-capture artifact, not game state: C step 194 == JS step 193 on all 4 cells (`S`/`r`/no-`1`, HP 79) — JS paints the post-More map/HP one frame early while C holds pre-retaliation paint + stale HP 89 at the More prompt (cf. mdrop_obj park, lightdamage park). A hmonas body port (436 lines, already substantially live `js/uhitm.js:2458–2715`) is PROVEN no-movement (baseline `verify hmonas` on a clean tree: 0 PASS, 0 moved, 1 unchanged → NO MOVEMENT). Falsifier: a hitum/mattacku More-paint-ordering port (why C defers newsym/botl past the More prompt) moving `verify hmonas` (expect Priest-92163 → PASS or a later owner); re-queue under that writer — never under hmonas. Do not "fix" with seed/step/coordinate gates or grid snapshot/restore (D-1831). Probe: NOTES.md Active `hmonas park`.
- `mon.c` minliquid_core — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/). scen-genesis-Priest-91110 step 130/151 kind=rng: C `rn2(15)=2 @ minliquid_core(mon.c:1116)` + `rn2(8)=3` (eel-out-of-water `rn2(mhp)>rn2(8)` with mhp=15) vs JS no draw after `rn2(6)=0 @ mhitm_knockback`; C topline lifesave vs JS empty. Re-queued from fired `apply.c` magic_whistled row (D-2096 tree moved it past step 78; see Parked magic_whistled entry). Baseline `verify minliquid_core` on a clean tree: 0 PASS, 0 moved, 1 unchanged → NO MOVEMENT. JS-side probes (throwaway /tmp scripts, reverted; no js/ diff): lifesave itself works (final umort=1, killer cleared; knockback-time uhp=74 = min(74,120) correct); single knockback pass is rat-vs-player S_RODENT→S_HUMAN ad=31/2 (AD_DRCO/AT_BITE) correctly returning FALSE both sides (adtyp≠PHYS, zero message dice both sides); final JS state 130/151 screens, RNG 3236/3557 ending at the knockback pair, S_EEL mhp=15/24 at 59,6 move=12 unmoved (matches C pre-check mhp=15), HP 68 vs C 69 — i.e. JS stops before the eel's minliquid, after the rat bite (74→68 vs C 74→69) with zero C player-damage dice in step 130. JS eel arm is already C-faithful (`js/mon.js:2442-2449`: S_EEL + !Is_waterlevel + !breathless + `rn2(mhp)>rn2(8)` + monflee). A minliquid eel-arm port guarantees NO MOVEMENT (eel never reached; count-only fix would be D-1849 location-blind). Falsifier: an `mhitu.c` rat-bite AD_DRCO (Con-drain/damage) port that makes step-130 HP/dice match and moves `verify minliquid_core` (expect Priest-91110 → PASS or a later owner); re-queue under that writer — never re-pop minliquid_core while the step-130 rat attack diverges. Do not fix with seed/step/coordinate/RNG-index gates. Probe: NOTES.md Active `minliquid_core park`.
- `monmove.c` distfleeck — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/). All 3 blocked sessions diverge AT C's unconditional `bravegremlin = (rn2(5) == 0)` (monmove.c:549) while JS draws a different die at that index — i.e. JS did not invoke distfleeck there. JS already mirrors all three C call sites (`:791/:834/:915` ↔ monmove.js:2208/2248/2321) with the same unconditional rn2(5); the only unported distfleeck code (seescary coords, `onscary`, `flees_light && !bravegremlin`, sanctuary, `monflee(rnd(rn2(7) ? 10 : 100))`) executes strictly AFTER the rn2(5), so a body port is a PROVEN no-movement on all three (positional argument — do not re-pop distfleeck for these sessions). Per-session true writers: S1 scen-normal-Healer-92218 step 43 — C «Pardon me, little dog.» (`hack.c domove_bump_mon` m-prefix branch) vs JS «You swap places with your little dog.» + spurious `rn2(7)`@do_attack (uhitm.c:474 safemon `foo`); «Pardon me» is textually absent from js/ (named omission js/cmd.js:3145), which predicts the observed jsEntry exactly. True writer `hack.c domove_bump_mon`, itself a Deferred singleton. S2 scen-normal-Tourist-92061 step 3 — matching toplines, C order [distfleeck … trapeffect_fire_trap → corpse_chance] vs JS [corpse_chance …]; same trap+death, flipped interleaving (fmon-order or move-phase flip; both sides draw corpse_chance, so not a clone-predicate bug). Needs C-side per-turn fmon/move dump. S3 scen-tour-Rogue-92030 step 76 — matched distfleeck, then C distfleeck-next-mon vs JS `rn2(3)`@m_move appr-condition (monmove.js:1718 stalker/bat/light arm, C :1871): same-monster short-circuit divergence (C hit an earlier no-draw arm — `!mcansee`/mappear/uundetected/peaceful?) or order flip; plus a JS-only «blast of fire hits you» (C topline empty, C still draws breamm later in the step). Needs JS prefix-state probe (mcansee/appearance/uundetected of the moving mon at step 76). Falsifier: per-session owner rows moving under `verify distfleeck` — Healer via a domove_bump_mon port (re-queue as Open `hack.c domove_bump_mon` when singletons re-enable at ≥ 90 % or a second session needs it), Tourist/Rogue via their writers. Baseline 2026-09-08: `verify distfleeck` still 3 blocked at the same steps (Healer «WORSE now distfleeck at step 43 (was 43)» is re-report noise on a clean tree). Re-queued 2026-09-09 as Open (7 blocked, first Rogue-92172@103); clean-tree baseline `verify distfleeck` (scoreboard ae833302, HEAD f4fc75e9): 1 PASS with zero code change (Rogue-92172 — stale row, fixer predates the scoreboard), 5 unchanged same-step, 1 same-step re-report (Tourist-92061 3→3, vacuous REGRESSION verdict, no js/ diff) → re-parked 2026-09-09, no D-log, no js/. Residuals: Samurai-92239@96 (post-D-2084; C distfleeck vs JS mattacku — undiagnosed, do not invent), Priest-91120@108 (jackal kill-vs-bite combat outcome — undiagnosed, do not invent), Caveman-92053@32 re-queued as Open `hack.c domove_bump_mon` (2nd «Pardon me» session; falsifier fired — C hack.c:1942 `pline("Pardon me, %s.")`, zero hits in scored js/). Do not "fix" with seed/step/coordinate gates.
- `sit.c` rndcurse — STALE OWNER (parked 2026-09-08, no D-log, no js/). Queue row claimed scen-death-Valkyrie-92229 step 22/63 kind=screen at sit.c:574 (C «You feel a malignant aura surround you. Demogorgon stings!--More--» vs JS «...stings!»). Clean-tree live replay at HEAD (144563ef) passes step 22 fully (RNG 2832/3094, screens 23/63) and diverges at step 23 kind=rng owner diseasemu@mhitu.c:1039 (C `rn2(16)=6` inside `rn1(ACURR(A_CON),20)` vs JS `rn2(3)=2 @ mhitm_knockback(mhitm.js:2060)`; C «Demogorgon hits! You feel deathly sick. Demogorgon hits!» vs JS «Demogorgon hits!»). No js/sit.js delta exists between the scoreboard stamp (3c229022) and HEAD (D-2150 touched js/read.js + js/insight.js only), and JS rndcurse is already C-faithful (D-1087/D-1089; brief arm-for-arm: Magicbane absorb, Antimagic shieldeff, COIN skip, SPFX_INTEL resist, bless→unbless else curse, saddle 1/4). Baseline `verify rndcurse` on the clean tree: 0 PASS, 1 moved past (22→23), 0 worse — the row never reproduced on a clean committed tree (stale rescore or dirty-tree capture, D-1831 class). Re-queued as Open `mhitu.c` diseasemu. Suspect for the next iter: the AD_DISE dispatch, not the diseasemu body — C caller mhitu.c:1534, JS clone already C-faithful but local/unwired (js/mhitu.js:2465), AD_DISE named omitted in the turns.md mhitu.c map line. Falsifier: a rescore or fresh `verify rndcurse` showing the session blocked at step ≤22 under owner rndcurse; re-queue under the writer it names — never re-pop rndcurse. Do not fix with seed/step/coordinate gates.
- `uhitm.c` mhitm_ad_famn — STALE OWNER (parked 2026-09-08, no D-log, no js/). Queue row claimed scen-wish-Tourist-92067 step 140/234 kind=screen at uhitm.c:3791 (C «You hit Famine. Famine reaches out, and your body shrivels.--More--» vs JS «...shrivels.»). Clean-tree live replay at HEAD moves past it with zero code change: `verify mhitm_ad_famn` → 0 PASS, 1 moved past (140→196 do_statusline2@botl.c:130, status cond-order C `Hallu Stun` vs JS `Stun Hallu`), 0 worse; fresh `hidden-proxy queue` no longer lists mhitm_ad_famn. Both arms already live and C-faithful (D-2033, review 1003 ACCEPT): mhitu arm `mhitm_ad_famn_u` (js/mhitu.js:2436 — pline_mon reach-out, exercise(A_CON), !fainted morehungry(rn1(40,40)), leftover kept) + mhitm arm `mhitm_ad_famn` (js/mhitm.js:827 — non-eater zero) + dispatches. A body port guarantees NO MOVEMENT (already shipped). Falsifier: a rescore or fresh `verify mhitm_ad_famn` showing the session blocked at step ≤140 under owner mhitm_ad_famn; re-queue under the writer it names — never re-pop mhitm_ad_famn while step 196 blocks. No seed/step/coordinate gates.
- `allmain.c` regen_hp — SYMPTOM OWNER (parked 2026-09-08, no D-log, no js/). Queue row claimed scen-genesis-Archeologist-92084 step 64/103 kind=rng (C `rn2(100)=82 @ regen_hp(allmain.c:659)` vs JS `rn2(400)=82 @ dosounds(sounds.js:336)`). Live replay at HEAD passes the session fully (RNG 3227/3227, screens 103/103); `verify regen_hp` → 1 PASS, 0 moved, 0 worse (PROGRESS) with zero code change in this iter. C-side proof regen_hp is downstream: step-64 C dice show the hero whip attack drawing `mattacku rnd(20)=13` + `hitmu d(2,6)=7` (nurse counterattack «The nurse hits!») before EOT; JS at HEAD draws the identical combat dice (`mattacku(mhitu.js:3799)`, `hitmu(mhitu.js:2977)`) then `rn2(100)=82 @ regen_hp(allmain.js:449)` + `rn2(400)=88 @ dosounds` — the earlier JS miss of the counterattack (HP stayed full → EOT gate skipped regen_hp → dosounds drew the shared raw value) is already fixed by landed combat ports. JS regen_hp body + EOT entry gate already C-faithful (D-0035/D-1003; gate matches C verbatim incl. the Upolyd-eel arm). A regen_hp body port guarantees NO MOVEMENT. Falsifier: a rescore or fresh `verify regen_hp` showing a session blocked at a regen_hp step; re-queue under the writer it names — never re-pop regen_hp for a gate-skip. No seed/step/coordinate gates.
- `uhitm.c` hmon_hitmon_barehands — STALE OWNER (parked 2026-09-08, no D-log, no js/). Queue row claimed scen-poly-Monk-92213 step 87/119 kind=rng at uhitm.c:847 (C `rnd(4)=2 @ hmon_hitmon_barehands` vs JS `rn2(3)=2 @ mhitm_knockback(mhitm.js:2015)`; C «You hit the master mind flayer! You hit...» vs JS «You miss...»). Clean-tree live replay at HEAD (756b0fe9) never reproduces it: RNG 3318/3318 fully matched, screens 116/119, diverges at step 108 kind=screen owner enlightenment@insight.c:398 (C « You were actually a Candidate, a level 1 male human Monk.» vs JS «...female...»). Baseline `verify hmon_hitmon_barehands` (scoreboard at 173d06a4): 0 PASS, 1 moved past (87→108), 0 worse; `--base 6f87bee1` (the queue's scoreboard era): 0 blocked — the row postdates it, a vacuous verify, NOT a PASS. Fresh `hidden-proxy queue` (limit 60) no longer lists hmon_hitmon_barehands. JS barehands core already live and C-faithful on this path (`js/uhitm.js:1008–1017`: shade-0, `rnd(martial_bonus()?4:2)`, use/train_weapon_skill); the only omission is the `special_dmgval` gloves/silver-rings tail + ring switch (`:852–881`), which draws nothing here — positional RNG sync through step 108 proves it. Mover unidentified (no js/uhitm.js or js/weapon.js delta between 173d06a4 and HEAD; only D-2157 light/vision) — stale rescore or dirty-tree capture (D-1831 class). Falsifier: a rescore or fresh `verify hmon_hitmon_barehands` showing a session blocked at a barehands step; re-queue under the writer it names — never re-pop barehands while step 108 blocks. Residual enlightenment@108 gender is screen-only with zero blocked RNG; re-queue as Open only if a queue row names it. Do not fix with seed/step/coordinate gates.
- `detect.c` do_mapping — STALE OWNER (parked 2026-09-08, no D-log, no js/). Queue row claimed scen-normal-Priest-91120 step 95/154 kind=screen at detect.c:1436 (C «Move cursor to anything of interest:» vs JS identical topline; cursor region, rowDiff null). Clean-tree live replay at HEAD (344fe348) never reproduces it: RNG 2840/2900, screens 108/154, diverges at step 108 kind=rng owner distfleeck@monmove.c:549 (C `rn2(5)=2 @ distfleeck` vs JS `rn2(20)=17 @ gethungry(eat.js:709)`; C «The jackal bites!» vs JS «You kill the jackal!»). Baseline `verify do_mapping` (scoreboard at 19f52d95): 0 PASS, 1 moved past (95→108), 0 worse; fresh `hidden-proxy queue` (limit 30) no longer lists do_mapping. Mover unidentified (only js/ delta between 19f52d95 and HEAD is D-2160's timeout.c SICK-expiry arm) — stale rescore or dirty-tree capture (D-1831 class). JS do_mapping's `!hero_memory || unconstrained` browse_map/map_redisplay arm is still deferred (`js/detect.js:989-991`; live local clones `browse_map` :1297 + `map_redisplay` :1314 are module-private and wired only to do_vicinity_map/monster_detect), but no corpus session blocks on it — a body port now guarantees a vacuous verify, NOT a PASS. Falsifier: a rescore or fresh `verify do_mapping` showing a session blocked at a do_mapping step; re-queue under the writer it names — never re-pop do_mapping while step 108 blocks (session now belongs to the distfleeck@108 group). No seed/step/coordinate gates.
- `timeout.c` vomiting_dialogue — MISATTRIBUTED + STALE OWNER (parked 2026-09-09, no D-log, no js/). Queue row claimed scen-wish-Rogue-92221 step 57/107 kind=screen at timeout.c:249 (C «Your feet are frozen to the stairs! You can move again.» vs JS «Your feet are frozen to the floor! You can move again.»). Owner `timeout.c:249` is the topline-literal tie-break: that line is inside `vomiting_dialogue`'s case-0 comment containing «You can move again.» (from `vomit()`'s `nomul(-2)`), first of 3 cMsgOwners (`vomiting_dialogue(timeout.c:249)`, `use_mirror(apply.c:1055)`, `peffect_paralysis(potion.c:891)`); the true printer is `peffect_paralysis` (`potion.c:884–905`: `Your("%s are frozen to the %s!", makeplural(body_part(FOOT)), surface(u.ux,u.uy))`), present in stepFns with `exercise` (vomiting_dialogue absent, zero draws). C-side proof vomiting_dialogue is innocent: pinned C `timeout.c:196–265` contains no frozen-feet print (only vomiting countdown + `vomit()` at case 0), and the step-57 turn is a paralysis-potion quaff (`qj` in the recipe; stepFns `peffect_paralysis`). JS `vomiting_dialogue` already C-faithful (`js/timeout.js:412–464`, arm-for-arm incl. FALLTHROUGH 6→9, `d(2,4)` pair) and JS `peffect_paralysis` already C-faithful (`js/potion.js:498–515`, exact branch order + `rn1(10,25-12*bcsign)` + `surface` from `js/sit.js:475` which ports `dungeon.c surface :1750–1788` in exact order incl. the `On_stairs` → «stairs» arm, D-2008 — STAIRS≥ROOM so the pre-D-2008 `IS_ROOM`-only read explained «floor»). Clean-tree live replay at HEAD (b46928ff) never reproduces step 57: working scoreboard shows RNG 3308/3349, screens 107/107 fully matched, diverges at step 92 kind=rng owner `touch_artifact@artifact.c:953` (C `d(4,10)=22` vs JS `rn2(1)=0 @ readobjnam`); `verify vomiting_dialogue` (scoreboard at 109f4444) → 0 PASS, 1 moved past (57→92 touch_artifact), 0 worse. A vomiting_dialogue body port guarantees NO MOVEMENT (already shipped via D-1792/D-2008). Falsifier: a rescore or fresh `verify vomiting_dialogue` showing the session blocked at step ≤57 under owner vomiting_dialogue; re-queue under the writer it names (live residual is `artifact.c touch_artifact` step-92 blast RNG) — never re-pop vomiting_dialogue while step 92 blocks. No seed/step/coordinate gates.
- `pray.c` doturn — STALE OWNER (parked 2026-09-09, no D-log, no js/). Queue row claimed scen-intrinsic-Priest-92096 step 149/172 kind=screen at pray.c:2434 (C «You are not able to call upon Raijin to turn aside evilness.--More--» vs JS «You are not able to call upon Raijin to turn aside evilness.»). Clean-tree live replay at HEAD (f5d7adfc) never reaches step 149: diverges at step 76 (worker step 75, key `s` search) kind=screen owner `find_trap(detect.c:1956)` — toplines identical both sides («You find an anti-magic field.--More--»), first cell diff row 2 col 34 region map/menu (C room walls `lqqqk`/`x<~~~`, JS rows 2–7 blank except `@^`): the owner is a topline-literal tie-break over a map-cell diff (cMsgOwners find_trap/mfind0; stepFns dosearch0/exercise/distfleeck/dochug/obj_resists/dog_move/m_move/dog_goal/mcalcmove/maybe_generate_rnd_mon/dosounds/gethungry). Proven pre-existing, not HEAD drift: identical worker result on clean HEAD (fix stashed) AND on pure-b46928ff js/ (the scoreboard commit) — the committed doturn@149 row reproduces from no committed tree (D-1831 class: that rescore ran dirty). GENUINE doturn C-wrong found underneath (fix reverted with this park; re-apply after the map writer ships): `js/pray.js:2301` `if (!(u.uconduct.gnostic++))` runs on undefined → NaN, so the can_chant-fail arm returns ECMD_OK where C `!gnostic++` (0→1) returns ECMD_TIME — a whole lost monster turn (C step-150 draws distfleeck/mattacku/hitmu; JS printed the message with no More and ate the dismissal space as «Unknown command ' '»). Measured via temp DOTURN tracing (before=undefined, after=NaN, ret=OK). Fix is the house `|0` idiom (cf. pray.js:1719–1724): `if (!(u.uconduct.gnostic | 0)) { u.uconduct.gnostic = 1; livelog_printf(...); } else { u.uconduct.gnostic = (u.uconduct.gnostic | 0) + 1; }`. Direct C-recorded replay proof with fix: step-150 screen byte-matches incl `--More--`, steps 151/152/154 match, RNG first-mismatch 3185→3257 (C step 169 mattacku); green 2/2 + cohort 7/7 held. Falsifier: a rescore or fresh `verify doturn` showing the session reaching step 149+ with map rows intact at step 76 (re-queue under the map writer — likely dosearch/display repaint — if it names one); then re-apply the gnostic fix above and expect PASS or move to step 169+. No seed/step/coordinate gates.
- `polyself.c` break_armor — MORE-TRANSIENT park (parked 2026-09-09, no D-log, no js/). scen-poly-Tourist-92171 step 88/145 kind=screen: identical toplines («You drop your gloves!--More--» both sides), 1-cell map diff row 5 C `+@d` vs JS `+qd` (hero glyph; status already «Wizard the Rothe» both sides). Owner `polyself.c:1253` is the topline-literal tie-break across 3 cMsgOwners (`break_armor`, `light_cocktail(apply.c:1759)`, `use_whip(apply.c:3230)`); stepFns empty, RNG fully matched 3637/3637, screens 144/145 — every later screen matches, so game state (incl. floor piles) is identical. C-side proof it is capture timing, not logic: session file step-88 map `+@d` vs step-89 `+q` — C paints the rothe glyph one frame later. JS probe (temporary display_self log, reverted, no js/ diff): the first q is painted by the C-faithful gloves `dropp→dropx→dropy→dropz→newsym` (js/do.js dropz newsym) with «You drop your gloves!» still pending — i.e. the JS per-key frame lands post-dropp under the retained More topline while the C recorder kept the mid-More pre-dropp frame (same class as parked hmonas/mdrop_obj/lightdamage). JS `break_armor` is already C-faithful (`js/polyself.js:903–1047`, exact arm order incl. breakarm destroy vs sliparm shed, horns pierce/drop, nohands gloves+drop_weapon/shield/helm, boots, eyewear); baseline `verify break_armor` on a clean tree: 0 PASS, 0 moved, 1 unchanged → NO MOVEMENT, so a body port is PROVEN no-movement (0 RNG blocked; D-1991's destroyed-vs-dropped grouping does not cover this session — no pre-gloves armor drop exists here, only the shirt useup). Falsifier: a More-paint-ordering port (why C holds the pre-newsym map at the More prompt) moving `verify break_armor` (expect Tourist-92171 → PASS or a later owner); re-queue under that writer — never under break_armor. Do not "fix" with seed/step/coordinate gates or grid snapshot/restore (D-1831). Probe: NOTES.md Active `break_armor park`.
- `mondata.c` name_to_monplus — STALE OWNER (parked 2026-09-09, no D-log, no js/). Queue row claimed scen-genesis-Archeologist-92175 step 81/84 kind=screen at mondata.c:1001 (C "|     elf-lord     |" vs JS "|    elf-noble     |" row 10, both toplines empty, RNG 3050/3050 fully matched). Clean-tree live replay at HEAD (ae833302) fully PASSES (RNG 3050/3050, Screen 84/84); `verify name_to_monplus` (scoreboard at 48fb0115) → 1 PASS, 0 moved, 0 worse (PROGRESS) with zero code change — the only commit since the scoreboard is the no-js/ u_stuck park, so the fixer predates it: d9e7079e (D-2001) "read.c create_particular ^G gendered-name MM_MALE/MM_FEMALE flags (elf-lord/Elvenking RNG)" — JS create_particular_creation now passes MM_MALE for the male-named request and creates a male elf-lord like C instead of falling through to neuter elf-noble. C-side proof the owner is innocent here: mondata.c:1001 is the { "elf lord", PM_ELF_NOBLE, MALE } alt_spl row (the literal tie-break for the centered name); for input "elf-lord" both sides take the identical pmnames exact-match arm (PM_ELF_NOBLE, gender MALE — alt "elf lord" misses on '-' vs ' ' both sides and JS ALT_NAMES holds no elf entry), so a name_to_monplus body port guarantees NO MOVEMENT. Remaining genuine debt (full ~60-entry alt_spl table — JS ALT_NAMES has 2; vortices/ies/ves singularization; title_to_mon fallback — NOT FOUND in js/) is reached by no corpus session → singleton class, Deferred while any corpus family is below 90 % PASS. Falsifier: a rescore or fresh `verify name_to_monplus` showing a session blocked on it again; re-queue under the writer it names — never re-pop name_to_monplus for the elf-lord box. No seed/step/coordinate gates.
- `pager.c` lookat — MISATTRIBUTED + STALE OWNER (parked 2026-09-09, no D-log, no js/). Queue row claimed scen-genesis-Knight-92224 step 20/80 kind=screen at pager.c:672 (C «You miss the gnomish wizard.--More--» vs JS «You miss the gnomish wizard.»). Owner pager.c:672 is a C comment inside lookat (`/* file lookup can't distinguish between "gnomish wizard" monster ...`, pager.c:673-675), not a print — literal-heuristic tie-break across 3 cMsgOwners (`lookat(pager.c:672)`, `use_pole(apply.c:3531)`, `readobjnam_postparse1(objnam.c:4400)`), same class as parked doname_base/zapyourself/dump_artifact_info/hmonas and D-2013 wishymatch. C-side proof lookat is innocent: step-20 turn is combat (stepFns gethungry/exercise/hitum/passive/distfleeck/choose_monster_spell/castmu; recipe moves `Fh` fight, no `/`/`;`/`:` look keys), and lookat only fills buf for farlook/getpos autodescribe — it never prints the «You miss» topline (true printer is the hitum combat path). JS lookat already C-faithful (`js/pager.js:1312-1457` vs pager.c:656-802, D-1843/D-1848 shipped). Clean-tree live replay at HEAD moves past with zero js/ diff (`git status` clean): `verify lookat` (scoreboard 58d11e0f) → 0 PASS, 1 moved past (20→77 next_ident@mkobj.c), 0 worse — the row never reproduces on a committed tree (stale rescore, D-1831 class). Live residual is `mkobj.c next_ident`@77, already Open (do not re-queue here). Falsifier: a rescore or fresh `verify lookat` showing a session blocked at a look/farlook step under owner lookat; re-queue under the writer it names — never re-pop lookat for a combat More. No seed/step/coordinate gates.
