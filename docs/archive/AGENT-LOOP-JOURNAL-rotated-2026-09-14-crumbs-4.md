# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-14 — Audit 9ac4640d..7a2ef83e (reviews 1232–1238: 7 ACCEPT, 0 Must-fix) + cadence 44/44 + rescore 463/540

**Scope:** all 7 `js/` commits since audit 5aafc8bc (D-2266 pager DEF_INVISIBLE, D-2267 Cloak_on, D-2268 s_suffix retire, D-2269 enlightenment Sunsword chain, D-2270 stinky_nemesis, D-2271 stealarm/unstolenarm, D-2272 shkgone/has_shop). One SHA at a time, file written at end of each SHA; single grouped commit.
**Method notes:** every new/changed JS arm walked against pinned C via `csym.mjs` (range cited) + `sym.mjs` + `--can` (outputs pasted in reviews); corpus claims re-measured with `verify <fn> --base <sha>~1` — 3 PROGRESS (there_cmd_menu_next2u 77→78, Cloak_on PASS Rogue-92137, enlightenment 226→227) + 4 vacuous-zero confirmed, 0 WORSE. Two D-log wordings corrected in review (mhitm→questpgr/sounds edges are ALREADY, not new; D-2266 "js-throw" token is the null-owner fallback, error:null verified). Two micro-observations recorded, not queued: s_suffix canonical adds trailing-'S'→"'" vs C lowercase-only; hallu-remapped 'I' ttychar edge in 1232.
**Cadence:** full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.29/turn` (R² 0.79). Corpus rescore `score --jobs 8`: **463/540 (85.7%)** excl env (463/553; RNG 98.9%, screens 98.4%) — the CURRENT 262/540 figure was stale since 09-06; rewritten with fresh top owners.
**Queue:** no Must-fix (all ACCEPT) so no prepend, Next cluster advanced past shipped shkgone → vamprises (first Open row). REFILL outstanding: Open 7/12; all 28 fresh-queue rows are live-Open or parked-with-proof, map singletons barred < 90% — documented in NOTES, not forced. Filled archive short hash D-2272 `7a2ef83e`.
**Next:** port loop pops vamprises; consider `scenario-gen.mjs` growth (corpus 85.7%, near the ≥85% growth point). Unowned `Unavailable command '!'` ×2 in score tail is a candidate for diagnosis, not an invented row.

## 2026-09-14 — D-2272 `shk.c` `shkgone` damage + `has_shop` arms (queue row `shk.c` shkgone damage + has_shop, mondead-tail D-2231 named omit)

**C locus:** `shk.c:234–269` (`shkgone`: `discard_damage_owned_by` `:246`, `resident = 0` `:247`, `!search_special(ANY_SHOP)` → `has_shop = 0` `:248–249`, bbox `no_charge` loop `:252–257`, `setpaid`/bill/`ushops` splice `:261–267`, all inside the `on_level` `:245` gate); `:4529–4552` (`discard_damage_owned_by`: `prevdam` walk, `strchr(in_rooms(x,y,SHOPBASE), shoproom)` match, unlink + `memset`/`free`; no-RNG, no-pline); `mkroom.c:764–780` (`search_special`: rooms then subrooms, `hx < 0` sentinel, `ANY_TYPE`/`ANY_SHOP`/`rtype ==` triple); `rm.h:435` (`has_shop` 1-bit bitfield); caller `mon.c:2785` (`m_detach` `isshk` arm, C order discard→resident→… matching the JS `m_detach` call site).
**JS:** 3 files, +44/−9 (`js/shk.js` +26 new function, `js/sounds.js` +5/−3 export + canonical note, `js/mhitm.js` +13/−6 imports + two arms + doc), under the 600/10 caps. Density note: the 34-line C function is only correct with its `staticfn` callee — the callee is the unit.
**Change:** new `export function discard_damage_owned_by(shkp)` in `js/shk.js` next to `discard_damage_struct`, in C order (`prevdam` walk, unlink owned, drop; GC frees — no `memset`/`free` in JS), ownership via the existing `shop_owns_cell` (the exact `strchr(in_rooms(x,y,SHOPBASE), shoproom)` test, `shk.js:1029`). `shkgone` (mhitm.js) now runs `discard_damage_owned_by(mtmp)` first, then resident, then `if (!search_special(ANY_SHOP) && game.level?.flags) game.level.flags.has_shop = 0` (C `= 0` verbatim; every JS reader is falsy-gated), all in C order inside the existing `on_level` gate. `search_special` canonicalized by exporting the `sounds.js` clone — it matches C arm-for-arm (triple condition + both room lists + sentinel); the `teleport.js` twin lacks the `ANY_TYPE` arm (pre-existing drift, named below, untouched).
**Verify:** `node scripts/verify.mjs --fn shkgone` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (ran `--full` by hand: new cross-module edge, fortress check) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probe `/tmp/probe_shkgone.mjs` (kept out of tree, deleted) through the real modules, 18/18: null-guard no-throw; owned head+tail dropped with kept survivor + cleared `next`s; full `shkgone` drops owned damage / clears resident / de-charges bbox stock only / skips bill when absent from `ushops`; no-shop-type state clears `has_shop` to 0 + pays bill + strikes `ushops` + zeroes billct/credit/debit/loan; remaining-shop state keeps `has_shop`; off-level shk untouched.
**Named:** `teleport.js` `search_special` ANY_TYPE arm (clone drift; canonical export is `sounds.js` — named in data.md, separate row on rescore). The rest of data.md:373 stays as-was — `minimal_monnam` format, `mongone` FALSE caller, `vamprises` door-trap kill, xkilled disinteg/readers, sync `kill_genocided_monsters` nuance (separate Open rows).
**Next:** do not re-pop `shkgone`/`discard_damage_owned_by`/`search_special` for this pair. Falsifier: a rescore or fresh `verify shkgone` / `verify discard_damage_owned_by` showing a session blocked with either as owner (e.g. a shop-damage billing topline after a shopkeeper death, or a shop-jingle `rn2(200)` RNG row on a shop-less level).

## 2026-09-14 — D-2271 `steal.c` `thiefdead` stealarm→unstolenarm swap + `stealarm`/`unstolenarm` completion (queue row `steal.c` thiefdead stealarm arm, mondead-tail D-2231 named omit)

**C locus:** `steal.c:119–128` (`thiefdead`: `stealmid = 0`, then `afternmv == stealarm` → `unstolenarm` + `nomovemsg = 0`); `:144–162` (`unstolenarm`: find `stealoid` in invent before clearing it, `You("finish taking off your %s.")` if present); `:165–211` (`stealarm`: stealoid/stealmid gate → find stealoid in invent → find stealmid on fmon with the `DEADMONSTER` impossible arm → `dmgtype(AD_SITM)` + `distu ≤ 2` gates → shop `subfrombill` → `freeinv` → pline → `mpickobj` → `monflee` → `rloc`, `botm` clears both ids on every path); `:551–560` (`steal()` sets `stealoid`/`stealmid` + `afternmv = stealarm`, returns 0); `hack.h:1531` (`distu` ≡ `dist2`), `monattk.h:63` (`AD_SITM` 21).
**JS:** 2 files, +98/−13 (`js/steal.js` +85/−7 incl. header + imports + two functions + one-line wiring, `js/mhitm.js` +13/−6 incl. comment retirements), under the 600/10 caps. Density note: the 8-line C `thiefdead` is only correct with its two `staticfn` callees and the one-line `steal()` producer — the cluster is the unit.
**Change:** new `export async function stealarm()` / `unstolenarm()` in `js/steal.js` in C order (async because `unmul` awaits `afternmv` and the bodies pline/rloc/monflee; `impossible` awaited on the dead-monster arm). `stealarm` gates on both ids, scans `game.invent` by `o_id` then `game.fmon` by `m_id`, `DEADMONSTER` ≡ `(mhp|0) <= 0` (mon.js idiom), `distu` via the already-imported-module `dist2` (`mon.js` export), `dmgtype` via `monsters.js` (same-module addition), `armor_simple_name` via `do_wear.js` (same-module addition), shop path via the stealamulet idiom `shop_keeper((u.ushops||'')[0])`, C pline-before-`mpickobj` order with `doname` captured first, `monflee`-then-`rloc` (C stealarm order, not stealgold's), no `mavenge` (C: seduction implies it), `botm` clears both ids on every return. `steal()` now sets `game.afternmv = stealarm`.
**Verify:** `node scripts/verify.mjs --fn thiefdead` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full `sessions` skipped: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probe `/tmp/probe_thiefdead.mjs` (kept out of tree) through the real modules: swap arm sets stealmid 0 + afternmv→unstolenarm + nomovemsg null (OK), foreign-function and null afternmv left untouched with stealmid still cleared (OK ×2).
**Named:** none new. The rest of data.md:373 stays as-was — `shkgone` damage/`has_shop` arms, `mongone` FALSE caller, `vamprises` door-trap kill, xkilled disinteg/readers, sync `kill_genocided_monsters` nuance (separate Open rows).
**Next:** do not re-pop `thiefdead`/`stealarm`/`unstolenarm`. Falsifier: a rescore or fresh `verify thiefdead` / `verify stealarm` showing a session blocked with either as owner (e.g. a nymph multi-turn armor-steal topline or RNG row), or a "finish taking off" vs "steals" topline divergence after a thief death.

## 2026-09-14 — D-2270 `questpgr.c` `stinky_nemesis` gas arm + all-13-role `killed_nemesis` texts (queue row `questpgr.c` stinky_nemesis, mondead-tail D-2231 named omit)

**C locus:** `questpgr.c:148–194` (`stinky_nemesis`): `#else` arm (the `#if 0` role-scan is compiled out) — `com_pager_core(gu.urole.filecode, "killed_nemesis", FALSE, &mesg)` (rawtext arm `:544–547` returns the text with no display), `strNsubst(mesg, "\n", " ", 0)` newlines→spaces, then case-insensitive `strstri` for `noxious`/`poisonous`/`toxic` with a later `" gas"`/`" fumes"`; `mon.c:2768–2774` (`m_detach` MS_NEMESIS arm: `nemdead()`, then `stinky_nemesis` → `nemesis_stinks(mx, my)`); `dat/quest.lua` `killed_nemesis` ×13 — Arc «noxious fumes», Cav «noxious fumes», Pri «noxious gas»; the other ten carry no trigger word, exactly the `mon.c:2770–2771` comment's Arc/Cav/Pri list (independently re-derived by running the predicate over all 13 texts).
**JS:** 2 files, +167/−10 (`js/questpgr.js` +~160, `js/mhitm.js` +7/−5 incl. comment retirements), under the 600/10 caps. Density note: the 44-line C function needs its 13-role text data to execute — the table is the function's data dependency, one cluster.
**Change:** `js/questpgr.js` — new `QUEST_KILLED_NEMESIS` (all 13 raw lua texts, byte-checked against `quest.lua`), wired into `QUEST_ROLE_TEXT`, plus `QUEST_MSG_META.killed_nemesis` synopsis/output for the 11 roles lua annotates (Arc/Cav have none, matching C's no-synopsis/default-output path); new exported `stinky_nemesis(mtmp)` following C order (hero filecode single lookup, no common retry; `\n`→space flatten; short-circuit `strstri` chain on the canonical `hacklib.js` export, already imported — no clone; miss → 0 like C's NULL `mesg`); `js/mhitm.js` `m_detach` runs `if (await stinky_nemesis(mtmp)) await nemesis_stinks(mx, my)` inside the live MS_NEMESIS arm, with `nemesis_stinks` added to the existing `quest.js` import and `stinky_nemesis` on a new `questpgr.js` edge (`imports.mjs --can` → same 90-module SCC, callee used at call time only, no TDZ risk). Both stale `stinky_nemesis`-omit comments retired (`mhitm.js` m_detach/mondead docs, `com_pager_core` + `qt_pager` rawtext notes). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn stinky_nemesis` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · full `frozen/ps_test_runner.mjs sessions` → 44/44 (run by hand: shared-adjacent `m_detach` edit, RNG 792,838-equivalent totals, speed `47+0.29/turn`) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probes (kept out of tree, deleted): a table checker diffing all 13 embedded texts + META presence byte-for-byte against `quest.lua` (ALL OK), and a live-module probe (`initRng` harness init only) calling `stinky_nemesis` per hero role through the real `com_pager_core` rawtext path — Arc/Cav/Pri → 1, other ten + unknown filecode → 0, matching the C predicate on every lua text (ALL OK).
**Named:** none new. `thiefdead` stealarm arm, `shkgone` damage/`has_shop` arms, `minimal_monnam` format, `mongone` FALSE caller, `vamprises` door-trap kill (the rest of data.md:373) stay as-was — separate Open rows.
**Next:** do not re-pop `stinky_nemesis`. Falsifier: a rescore or fresh `verify stinky_nemesis` showing a session blocked with `stinky_nemesis` as owner (e.g. an Arc/Cav/Pri nemesis-death screen/RNG row), or a nemesis-death topline missing its gas cloud.

## 2026-09-14 — D-2269 `insight.c:1564–1570` Vision-head arms + Sunsword `EBlnd_resist` conferral/`what_gives`/`from_what` chain (queue row `insight.c` enlightenment, scen-wish-Rogue-92037)

**C locus:** `insight.c:1567–1570` (`Blnd_resist && !Blind` → `you_are("not subject to light-induced blindness", from_what(BLND_RES))`); `:1564–1566` (`(HBlinded||EBlinded)&&BBlinded` → `you_can("see", from_what(-BLINDED))`, Eyes of the Overworld); `youprop.h:103` (`Blind ≡ (HBlinded||EBlinded) && !BBlinded`, no roleplay) and `:161` (`Blnd_resist ≡ H||E`); `artifact.c:886–891` (wielded Sunsword `EBlnd_resist |= W_WEP`, exact-`W_WEP` match); `artifact.c:2411–2417` (`what_gives`: `obj==uwep && abil==&EBlnd_resist && *abil&W_WEP` → Sunsword); `attrib.c` `from_what` negative arm (`-BLINDED`: `BBlinded && is_art(ublindf, ART_EYES_OF_THE_OVERWORLD)` → «because of …»); `wintty.c:1914` + `:2720` + `:1426` + `:1545` (H2344 `offx = min(40, 80-maxcol-1)`, per-row `cl_end` + one `putchar(' ')` + raw item, `(end)` morestr row) — geometry/paint code needed no change once content matched.
**JS:** 3 files, +75/−8 (`artifact.js` +19/−3, `attrib.js` +16/−5, `invent.js` +40/−0; plus `startup.md` map row + D-2269 clause), under the 600/10 caps. Density note: one C locus family (`insight.c` Vision-head + its `artifact.c`/`attrib.c` conferral chain) across the two builders that share it.
**Change:** `js/artifact.js` — `set_artifact_intrinsic` gains the exact-`W_WEP` `is_art(otmp, ART_SUNSWORD)` → `set_spfx_extrinsic(BLND_RES, 'EBlnd_resist', …)` arm (symmetric on unwield via the existing `W_WEP` off-paths); `what_gives` gains the `propidx===BLND_RES && obj===uwep && bits&W_WEP` return after the spfx check, in C order (C relies on the same only-Sunsword-sets-it invariant, so no extra `is_art` gate). `js/attrib.js` — `from_what` handles `-BLINDED` with the Eyes/`ublindf`/`bare_artifactname` arm (same-module `is_art` + `ART_EYES_OF_THE_OVERWORLD` additions; `attrib↔artifact` edge already exists). `js/invent.js` — both builders gain the two arms in C order with C tenses (`you_can` «see» via inline `enlght_line_txt`; final path uses the `you_are` helper for «were/weren't», `doattributes` fixed «can/are»); `Blind`/`Blnd_resist` read macro-exact (`(HBlinded||EBlinded)&&BBlinded` / H+E flats + uprops, no `uroleplay.blind` gate — `invent.js` `Blind()` adds roleplay, C's macro does not).
**Verify:** `node scripts/verify.mjs --fn enlightenment` → PASS syntax (3 changed js files) · PASS rule2 · PASS hidden (`verify enlightenment`: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Rogue-92037 step 226→227, owner enlightenment@`insight.c:398` → drinkfountain@`fountain.c:292`, a strictly later step — left for its own row) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full `sessions` skipped: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Scratch probe `/tmp/probe_enl226.mjs` (prefix replay, kept out of tree) showed the JS menu byte-identical to C rows 0–23 incl. the 66-char Sunsword line, `(end)` row, and cursor (17,22) before the formal run.
**Named:** none new. `from_what` negative INVIS (mummy wrapping) / CLAIRVOYANT (cornuthaum), birth blind/deaf, `Blindfolded_only`/cream, strangulation trim stay as-was; `what_gives` cspfx/adtyp arms stay deferred (D-1539); `HBlnd_resist` FROMFORM beyond the H-field read stays as-was.
**Next:** do not re-pop `enlightenment` for the Sunsword-blindness content. Falsifier: a rescore or fresh `verify enlightenment` showing a session blocked with `enlightenment` as owner at a Vision-head step (blinded-blocked or Blnd_resist content).

## 2026-09-14 — D-2268 `hacklib.c` `s_suffix` uhitm clone retired onto the canonical `do_name.js` import (queue row `hacklib.c` s_suffix, D-2261 Next)

**C locus:** `hacklib.c:345–359` (`s_suffix`): `it`→`its`, `you`→`your` (both `strcmpi`, case-insensitive); trailing lowercase `s` → `'`; else `’s`. Only `s` — C has no z/x/ch/sh arm.
**JS:** 1 file, +1/−13 (`js/uhitm.js`), under the 600/10 caps. Density note: portable C is 14 lines and the canonical export already existed — a clone retirement is small by nature.
**Change:** deleted the local clone; added `s_suffix` to the existing `do_name.js` import (`imports.mjs --can uhitm.js do_name.js s_suffix` → ALREADY, no new edge, no TDZ risk). All 8 callers now resolve to the canonical `js/do_name.js:383` export, which is C-arm-for-arm (`toLowerCase` it/you, trailing-`s` → `'`, else `'s`). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn s_suffix` → PASS syntax (1 changed js file: js/uhitm.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full `sessions` skipped: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); the z/x/ch/sh possessive arm fires on no fortress path (durable-test-collateral disclosure).
**Named:** none new. The remaining `s_suffix` clones named by `brief.mjs` (`explode.js`, `minion.js`, `mthrowu.js`, `questpgr.js`, `shk.js`, `trap.js`) stay as-was — different owners, separate rows on rescore; do not widen this row to them.
**Next:** do not re-pop `s_suffix` for uhitm. Falsifier: a session blocked with `s_suffix` as owner, or a uhitm possessive topline diverging on a z/x/ch/sh-final name.
