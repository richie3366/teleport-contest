# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-15 — Audit 4581ce02..ef75880a (reviews 1270–1274: 5 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration, no `js/` edits. One SHA at a time, file written as each finished: 1270 mktrap dispatcher + maze trap loop (traptype_roguelvl verbatim; victim-tail delegation verified; tm pool/lava `IS_POOL` idiom noted latent-unreachable, not Must-fix), 1271 squeaky-board pair + sobj_at retire + door unblock_point (C :6078), 1272 `-`→fight (corpus session re-run PASS 72/72 by this review; every `'F'` site carries `-`), 1273 bury five arms (two genuine C-wrongs fixed: bare-lamplit leak, deltrap→delfloortrap; async ripple fully awaited), 1274 rile_shk surcharge (`(price+2)/3` bit-exact; dig `unblock_point`-vs-recalc drive-by adjudicated outcome-equivalent, not queued). Corpus re-runs all vacuous-honest except 1272's real move. Cadence full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed 46+0.29/turn. Queue 9 Open (in-band, no refill), Must-fix empty, D-2308/D-2306 archive hashes filled.

## 2026-09-15 — D-2308 `shk.c` shopdig anger arms: rile_shk bill surcharge + digactualhole make_angry_shk

**C locus:** `shk.c` `shopdig` `:5018–5110` (curse/growl arm calls `rile_shk`); `rile_shk` `:1362–1377` (NOTANGRY=FALSE + `(price+2)/3` bill walk); `dig.c` `digactualhole` `:821–822` (`if (mtmp->isshk) make_angry_shk(mtmp, 0, 0)` before `migrate_to_level`).
**JS:** 2 files (`shk.js` +8/−2, `dig.js` +7/−3 incl. doc-ledger updates), far under the 600/10 caps.
**Change:** `js/shk.js` only: `rile_shk` ports the walk verbatim in C order over the live `bill_p`/`bill` shape (same `||` fallback as `addupbill`; `| 0` integer idiom). `js/dig.js` only: the `teleport_pet` success arm calls the already-live `make_angry_shk` via the file's existing dynamic `shk.js` import (no new module edge, no `imports.mjs --can` needed); header envelope + Named-omit ledger updated. Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `verify --fn shopdig` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool-gated) · VERIFY: PASS. No hand probe: `rile_shk` is module-private (no production export added for testability) and both arms are straight-line C-order ports over live callees (`make_angry_shk` already exercised via its dokick/dothrow/teleport/trap callers); surcharge arithmetic spot-checked (price 10→14, 1→2, 0→0 both sides).
**Named:** `SetVoice` in `shopdig` warn (pre-existing); `#if 0` nolimbs curse/rile early-return (C-disabled, both sides); remaining `digactualhole` ledger (`buried_ball_to_punishment`, `ship_object`, shop `add_damage`, `liquid_flow` — other rows). Seen while reading, untouched: `dig()` `:520` `unblock_point` vs JS `recalc_block_point`, `:528–533` earth-debris elemental still deferred.
**Next:** do not re-pop shopdig. Falsifier: a rescore or fresh `verify` showing a session blocked with a shopdig/rile/make_angry owner.

## 2026-09-15 — D-2307 `dig.c` bury-family deferred arms: end_burn/maybe_unhide_at/buried-ball/delfloortrap/damage-chains/pooleffects

**C locus:** `dig.c` `bury_an_obj` `:1982–2047` (`end_burn` `:2011–2012`); `bury_objs` `:2049–2081` (`maybe_unhide_at` `:2074`); `unearth_objs` `:2084–2112` (`buried_ball` `:2091`, punish arm `:2096–2099`); `liquid_flow` `:837–879` (`u_at` `:843`, `delfloortrap` `:857`, damage chains `:868–872`, `pooleffects` `:875`). `buried_ball{,_to_punishment,_to_freedom}` + `rot_organic` re-read; bodies already faithful, untouched.
**JS:** 2 files (`dig.js` +56/−27 incl. doc-ledger updates, `zap.js` +1/−1 `await unearth_objs` in async `melt_ice`), far under the 600/10 caps.
**Change:** `js/dig.js` only (+ `js/zap.js` 1-line `await`): `end_burn` via dynamic `timeout.js` import (sync export; `stop_timer` runs `cleanup_burn` — C `timeout.c` order); `maybe_unhide_at` joins the existing `monmove.js` static import; `unearth_objs` now async with the `bball`/`TT_BURIEDBALL` arm verbatim (same-module `buried_ball_to_punishment`); `liquid_flow` in C order — `u_spot` hoisted pre-return, `delfloortrap` (already imported), `await unearth_objs`, `fire_damage_chain(objchain, TRUE, TRUE, x, y)` / `water_damage_chain(objchain, TRUE)` join the existing `trap.js` import, `pooleffects(FALSE)` via dynamic `pickup.js` import (file's convention for not-yet-imported modules). `imports.mjs --can` for the three cross-module names: monmove ALREADY, trap/pickup/timeout same 90-module SCC, call-time use only. Sanity-check `impossible` kept soft per file convention (`:1397` precedent).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_bury.mjs` PROBE PASS 16/16 (arms no corpus session reaches; deleted after run): bury/unearth round-trip with coords+where; ROCK merge; candle (burn timer + light) and magic lamp (light, no timer) bury with zero light-source leak; trapped ball within C's dist2≤8 search radius re-punished (`uball` set, not floor/buried); `liquid_flow` ROOM early-return + POOL path complete. Probe detours worth recording: `const { game } = await import(gstate)` snapshots `export let game` (took `{}`); static `import { game }` stays live — and C `buried_ball_to_punishment` searches from the *hero*, so a far-away test ball is correctly ignored (verified vs C before adjusting).
**Named:** sanity-check `impossible` text (file-convention soft); CORPSE-under-ice (C TODO `:2026–2028`, copied); RUST_METAL (`#if 0` both sides); `trap_ice_effects` in `melt_ice` (other row); `spoteffects`/`boulder_hits_pool` in `melt_ice` (pre-existing); `liquid_flow` caller in `digactualhole` (D-0954 row owns it).
**Next:** do not re-pop the bury family. Falsifier: a rescore or fresh `verify` showing a session blocked with a bury/unearth/liquid_flow owner.

## 2026-09-15 — D-2306 `cmd.c` `-`→fight binding missing from rhack dispatch (commands_init bind_key)

**C locus:** `cmd.c:2772` `(void) bind_key('-', "fight", FALSE)` in `commands_init` (unconditional — always installed, not num_pad-gated); extcmdlist `F` fight PREFIXCMD `:1722–1723`; `do_fight` `:1621–1634`; rhack `:3679` `cmdbind_get` dispatch; `cmd_from_func` `:3049–3052` skips `-` only for key display when `!num_pad` (behavior unaffected).
**JS:** 1 file (`cmd.js`, +9/−6), far under the 600/10 caps. Density note: the locus is the 1-line C bind over the already-ported `do_fight` arm — C is that small; value is the corpus PASS below.
**Change:** `js/cmd.js` only, no new module edge (`do_fight` same-module; no `imports.mjs --can` needed): `-` shares the `F` arm (`ch === 'F' || ch === '-'` → `do_fight`, `prefix_seen` = fight, C `:1621–1634` + `:2772` cited); the forcefight follow-up guard, the g/G rush-prefix guard, and the `menu_requested`-preserve gate all exempt `-` exactly like `F` (C: `-` IS the fight prefix, so F-then-`-` reaches `do_fight`'s "Double fight prefix, canceled." instead of the F-follow-up pline, and m-F-h keeps nopick); `rhack_repeat_command`/`rhack_repeat_txt` map `-`→`do_fight`/`fight` (Ctrl-A repeat parity with `F`). A user BIND= overlay on `-` still wins (overlay arm precedes the if/else, same as `F`). No RNG/message/control-flow change beyond C order; Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Direct replay `node frozen/ps_test_runner.mjs .cache/hidden/sessions/random-seed0015-valk-level2-pit-dog-wait-eb7e90ad.session.json`: before FAIL (RNG 8547/8547, Screen 70/72) → after PASS (8547/8547, 72/72). `node scripts/verify.mjs --fn rhack` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: ownerless step, 0 blocked at HEAD — NOT claimed as a corpus PASS; the row cited the session explicitly, proved by the direct replay) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (cmd.js not shared-gated) · VERIFY: PASS.
**Named:** `!`→doshell binding (2 corpus sessions, own future row); other number_pad/phone/swap_yz/pcHack dir layouts (pre-existing dokeylist omits); full CMD_gGF table (pre-existing turns.md:256).
**Next:** do not re-pop `-`→fight. Falsifier: a rescore or fresh `verify` showing a session blocked with a `-` Unknown-command step.

## 2026-09-15 — D-2305 `trap.c` disarm_squeaky_board + unsqueak_ok; try_disarm sobj_at; untrap door unblock_point

**C locus:** `trap.c` `unsqueak_ok` `:5606–5626`; `disarm_squeaky_board` `:5630–5660`; `try_disarm` boulder gate `:5456` (`sobj_at(BOULDER, …)`); untrap door set-off `:6079` (`unblock_point(x, y)`; C `vision.c:899` = `dig_point` + full-recalc flag). Audit basis: `disarm_holdingtrap` `:5553` / landmine `:5594` / shooting `:5664` / `help_monster_out` `:5700` / `disarm_box` `:5794` / `untrap_box` `:5821` vs the `js/trap.js` clones.
**JS:** 1 file (`trap.js`, +52/−14), far under the 600/10 caps. No committed probes (reads + `node --check` + `/tmp` probe only).
**Change:** `js/trap.js` only, no new module edge (`getobj`/`useup`/`consume_obj_charge` join the existing `invent.js` import; `GETOBJ_*` join `const.js`; `unblock_point` joins `vision.js`; `POT_OIL` follows the file's `objectNames.indexOf` convention; every callee same-module or already imported): `unsqueak_ok` verbatim (null→EXCLUDE; grease / dknown+`oc_name_known` oil→SUGGEST via `game.objects` table; other potions→DOWNPLAY; else EXCLUDE); `disarm_squeaky_board` verbatim (`getobj("untrap with", …)`; `bad_tool = cursed || ((not-oil || lamplit) && (not-grease || !spe))`; `try_disarm(ttmp, bad_tool)`; grease→`consume_obj_charge` else `useup`+`makeknown(POT_OIL)`; repair pline; `deltrap`; `newsym(ux+dx,uy+dy)`; `more_experienced(1, 5)`; `newexplevel`); `case SQKY_BOARD` wired in the `untrap` switch; boulder loop → `sobj_at(BOULDER, …)` (identical loop, canonical export); door set-off → `unblock_point` (identical outcome post-`D_NODOOR`, C-exact call). No RNG/message/control-flow change beyond C order; Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `verify --fn disarm_squeaky_board` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (trap.js not shared-gated) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `move_into_trap` (adjacent-Whoops; needs `test_move` export, D-1813); `stumble_on_door_mimic`. `disarm_squeaky_board` retired from the untrap doc ledger and data.md:176.
**Next:** do not re-pop untrap floor disarm_*/box. Falsifier: a rescore or fresh `verify` showing a session blocked with an untrap-envelope owner.

## 2026-09-15 — D-2304 `mklev.c` mktrap dispatcher + `mkmaze.c` populate_maze trap loop wired

**C locus:** `mklev.c:2036-2150` `mktrap` (invalid-args once-guard + paniclog; `m.x=m.y=0`; tm pool/lava abort; specified/rogue/hell-bias/`traptype_rnd`-loop kind select; hole→ROCKTRAP; tm-copy vs MAZEFLAG-mazexy/croom-somexyspace 200-try occupied/boulder loop; `maketrap`; post-tail) + static `traptype_roguelvl :2002-2030` (`rn2(7)` table); caller `mkmaze.c:1122-1123` (`mktrap(0, MKTRAP_MAZEFLAG, NULL, NULL)`).
**JS:** 1 file (`mklev.js`, +110/−7), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (reads only + `node --check`).
**Change:** `js/mklev.js` only, no new cross-module edge (every callee same-module or already imported — `MKTRAP_NOFLAGS/SEEN/MAZEFLAG` join the existing `const.js` flag import; `--can` correctly skipped): new `traptype_roguelvl` verbatim; new `mktrap(num, flags, croom, tm)` in C order (once-guard via module-level `mktrap_err`; tm pool/lava abort via the module `IS_POOL/IS_LAVA` idiom; specified → rogue (`Is_rogue_level` live import) → hellish `!rn2(5)` FIRE_TRAP → `traptype_rnd` loop; `is_hole`/`Can_fall_thru` ROCKTRAP; tm-copy vs mazexy/somexyspace 200-try `occupied`/`sobj_at(BOULDER)` loop; `maketrap`; tail delegated to live `mktrap_seen_victim` with `seen` derived from `MKTRAP_SEEN`); `populate_maze` gains the C 6th loop verbatim and the omit comment retires.
**Verify:** `node scripts/verify.mjs --fn mktrap` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` PASS on a clean tree before edits. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `mktrap` invalid-args `paniclog` write (file-only, no scored-JS equivalent under Rule #2; observable behavior — no trap — kept; C `mktrap_err` once-semantics mirrored); `traptype_rnd` LEVEL_TELEP `single_level_branch` (pre-existing Knox deferral, untouched); `makemaz` load_special-path `dmonsfree` (mon.c, own future row) and wizard SPLEVTYPE/`getenv` override (wizard-only env read, no plain-ESM equivalent; both already named at `js/mklev.js:1478/1511/1525`). Other `mktrap` call sites (`mklev.c:985` room path, `sp_lev.c:1845`) keep their existing live inlines — rewiring them is separate rows.
**Next:** do not re-pop populate_maze/mktrap (dispatcher + all six stock loops live). Future maze-level divergences attribute to their true writer.
