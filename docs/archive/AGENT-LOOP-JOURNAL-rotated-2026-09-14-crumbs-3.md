# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-14 — D-2267 `do_wear.c` `Cloak_on` full otyp switch: OILSKIN fit-tightly pline + MUMMY/INVISIBILITY/ALCHEMY arms + known/`update_inventory` tail (queue row `do_wear.c` Cloak_on, oilskin arm)

**C locus:** `do_wear.c:326–380` (`Cloak_on`); oilskin arm `:364–366` (`pline("%s very tightly.", Tobjnam(uarmc, "fit"))`); MUMMY `:348–355` (`(HInvis || EInvis) && !Blind`, newsym, `You("can %s!")`); INVISIBILITY `:356–363` (`!oldprop && !HInvis && !Blind`, `makeknown`, newsym, `pline("Suddenly you can%s yourself.")`); ALCHEMY_SMOCK `:367–370` (`EAcid_resistance |= WORN_CLOAK`, `youprop.h:60` = `uprops[ACID_RES].extrinsic`); tail `:371–375` (`known = 1` + `update_inventory()`, no `find_ac` per D-0810). ALCHEMY `oc_oprop` is POISON_RES (already conferred by `setworn`), so the arm adds the second bit.
**JS:** 1 file, +60/−10 (`js/do_wear.js`), under the 600/10 caps. Density note: the whole 54-line C function is now arm-for-arm; the remainder beyond the oilskin arm is the same function's switch, shipped together per the same-`file.c:function` rule.
**Change:** `js/do_wear.js` `Cloak_on` is now the C switch in C order: plain-cloak breaks; PROTECTION `makeknown`; ELVEN `toggle_stealth`; DISPLACEMENT `toggle_displacement`; MUMMY checks the `(HInvis|EInvis)` flats (not full `Invis`, per the C "already worn, cheat" comment) + `Blind()` with `newsym` and `You can no longer see through yourself!` / `You can see yourself!` via the hoisted `See_invisible_dw()`; INVISIBILITY checks `!oldprop && !HInvis && !Blind` with `makeknown` + newsym + `Suddenly you can see through/not see yourself.` (C `"can%s"` spacing preserved: `not see` concatenates to `cannot`); OILSKIN `await pline(\`${Tobjnam(o, 'fit')} very tightly.\`)` (`otense` gives `fits`; unidentified `xname` gives the `slippery cloak` descr, matching the recorded topline); ALCHEMY ORs `WORN_CLOAK` into `uprops[ACID_RES].extrinsic` (Gloves_on FUMBLING uprops-ensure idiom); default `await impossible(...)` like C; tail sets known + `update_inventory()` (Helmet_on idiom). Six local `objectNames.indexOf` consts added; `ACID_RES` rides the existing `const.js` edge. `See_invisible_dw`/`Blind` are hoisted function declarations; `Tobjnam`/`newsym`/`pline`/`makeknown`/`update_inventory` were already imported — no new cross-module edge, no TDZ risk (`node --check` + dynamic import OK).
**Verify:** `node scripts/verify.mjs --fn Cloak_on` → PASS syntax (1 changed js file: js/do_wear.js) · PASS rule2 · PASS hidden (`verify Cloak_on`: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Rogue-92137 PASS; working scoreboard committed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full `sessions` skipped: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); durable coverage is the corpus session itself via `hidden-proxy verify` (durable-test-collateral disclosure).
**Named:** none new. `Cloak_off` MUMMY/INVISIBILITY/ALCHEMY arms stay deferred (turns.md:1017 — takeoff side, untouched); Boots_off SPEED/water/levitation unchanged.
**Next:** do not re-pop `Cloak_on`. Falsifier: a rescore or fresh `verify Cloak_on` showing a session blocked with `Cloak_on` as owner (not mere stepFns presence).

## 2026-09-14 — D-2266 `pager.c` `do_screen_description` DEF_INVISIBLE arm keyed on the shown glyph, not `mon_at` (queue row `cmd.c` `there_cmd_menu_next2u`, literal-heuristic misattribution)

**C locus:** `pager.c:1406–1417` (DEF_INVISIBLE arm of `do_screen_description`); `:1246–1273` (looked → `glyph = glyph_at`, `sym` = shown ttychar, prefix = encglyph + 8 spaces); check_monsters skips `S_invisible`; didlook `:1607–1640` not taken (found == 1, need_to_look clear → no lookat parenthetical, firstmatch = invisexplain); `getpos.c:777–779` + `hack.h:544–546` (`.` → LOOK_ONCE, so `do_look :1941` skips checkfile). `cmd.c:4524–4621` (`there_cmd_menu_next2u`) never runs on this recipe (no mouse/here-menu key; stepFns empty, zero RNG at the step) — the owner string is the topline-literal tie-break across 3 cMsgOwners (`there_cmd_menu_next2u(cmd.c:4613)` «unseen creature» vs the true printer `do_screen_description(pager.c:1407)` invisexplain), same class as parked spoteffects/were_change/doname_base.
**JS:** 1 file, +13/−0 (`js/pager.js`), under the 600/10 caps. Density note: the C arm is 12 lines and the `describe_looked` envelope (D-2045 etc.) already exists — a dispatch fix is small by nature.
**Change:** `js/pager.js` `describe_looked` — `glyph_is_invisible_id(glyph)` (already imported) tested on the shown `glyph_at` before `mon_at`, returning `I` + `an(unseen)` with the C `:1408–1410` EDetect-I_SPECIAL/Blind select (`unseen creature` vs `remembered, unseen, creature`), first = unseen, found = 1. The `mon_glyph`-keyed sub-arm is kept as fallback. No new imports; no DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn there_cmd_menu_next2u` → PASS syntax (1 changed js file: js/pager.js) · PASS rule2 · PASS hidden (0 PASS, 1 moved past, 0 worse → PROGRESS: Wizard-91112 step-77 screen now PASS, first divergence step 78 wizmap `^F` map row 10 C `@I` vs JS `@·`, owner null on an empty topline; the «js-throw» token in the verify line is the null-owner display fallback (`hidden-proxy.mjs:388` `${r.owner || 'js-throw'}`) — the working scoreboard row has `error: null`, scrM 78/137) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. Preflight `--no-cohort` green on a clean tree before edits. Throwaway probe /tmp/probe_look77.mjs (kept out of tree) confirmed JS step 77 «Ia remembered, unseen, creature» and a throw-free full run.
**Named:** none new. `there_cmd_menu_next2u`/far arms stay deferred (turns.md:1956); full `do_screen_description` showsyms scan stays deferred (turns.md:904).
**Next:** do not re-pop `there_cmd_menu_next2u` (misattribution; its body never runs on this recipe). The session's live residual is a new owner: wizmap `do_mapping` drops the `I` marker in the mapped view (C keeps `@I`, JS shows floor) — re-queue under `detect.c` `do_mapping`/`dowizmap` on rescore, never under `there_cmd_menu_next2u`. Falsifier: `hidden-proxy verify do_mapping` moving Wizard-91112 past step 78.

## 2026-09-14 — Audit e277f895..8c1c209d (reviews 1225–1231: 6 ACCEPT, 1 ACCEPT-WITH-DEBT) + cadence 44/44

Review iter, no js/ edits. Seven JS SHAs since review 1224, oldest first, each file written as its SHA finished: 1225 D-2259 (review-1217 Must-fix, five `mndx` gates — C-confirmed), 1226 D-2260 (four mhitm arms arm-for-arm + monkilled null gate), 1227 D-2261 (four uhitm arms; DEBT = pre-existing local `s_suffix` clone, already a live Open row, unreachable from the new CURS arm in practice), 1228 D-2262 (polyself goto-flattening incl. tryct/RNG-trace equivalence + skinback), 1229 D-2263 (litroom tail + snuff_light_source; 4 new module edges LOAD-OK), 1230 D-2264 (3-line CHECKSCARY drift, C-confirmed), 1231 D-2265 (TIN gate + canonical set_tin_variety + BoH arm, all C-confirmed). Every corpus verify re-measured at the parent baseline — all vacuous-0 exactly as labeled, no false PASS anywhere. No Must-fix (no QUALITY-RISK/REJECT), so LOOP-QUEUE Must-fix stays empty and Next cluster unchanged (mhitu passiveum). Cadence full `sessions`: 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed 48+0.30/turn (R² 0.78) — fortress unchanged. Open rows 12/12, no refill owed.

## 2026-09-14 — D-2265 `mkobj.c` `mksobj_init` envelope completion: TIN cnutrit gate + canonical `set_tin_variety`, scalar inits, CHEST `tknown`, samurai `In_quest`; `mkbox_cnts` BoH arm (queue row `mkobj.c` mksobj_init, trace reach only)

**C locus:** `mkobj.c:868–1175` (`mksobj_init`): FOOD `otmp->oeaten = 0`; TIN (`corpsenm = NON_PM`, `!rn2(6)` → `set_tin_variety(SPINACH_TIN)`, else 200-try `undead_to_corpse(rndmonnum())` accepting only `mons[mndx].cnutrit && !(mvflags & G_NOCORPSE)` then `set_tin_variety(RANDOM_TIN)` + `blessorcurse(10)`); GEM `corpsenm = 0` (LOADSTONE hack); SPBOOK `spestudied = 0`; TOOL CHEST `tknown = otrapped && !rn2(100)` (assigned); ARMOR samurai `moves <= 1 || In_quest(&u.uz)`; `mkbox_cnts :371–379` (`Is_mbag(otmp)` → `SACK`+`spe = 0`+`weight`, else re-roll `WAN_CANCELLATION` via `rnd_class(WAN_LIGHT, WAN_LIGHTNING)`); `eat.c:1461–1486` (`set_tin_variety` incl. the rotten remap); `obj.h:339` (`Is_mbag` = `BAG_OF_HOLDING || BAG_OF_TRICKS`).
**JS:** 1 file, +41/−15 (`js/mkobj.js`), under the 600/10 caps. Density note: the clone already covered ~95 % of the 306-line C function (D-0588/D-1690/D-1732 and the D-00xx class arms); this is the complete genuine remainder — the function is now arm-for-arm.
**Change:** `js/mkobj.js` — FOOD sets `oeaten = 0`; TIN sets `corpsenm = NON_PM` up front, calls imported `set_tin_variety(otmp, SPINACH_TIN | RANDOM_TIN)` (canonical `eat.js` export, hoisted so the new `mkobj→eat` edge is cycle-safe per `imports.mjs --can`; `SPINACH_TIN`/`RANDOM_TIN`/`In_quest` ride the existing `const.js` edge), loop gate `(mons(mndx)?.cnutrit | 0) && !(mv & G_NOCORPSE)`; GEM `corpsenm = 0`; SPBOOK `spestudied = 0`; CHEST exact `tknown` assign (short-circuit preserved); samurai `|| In_quest(game.u?.uz)`; `mkbox_cnts` BoH arm with local `WAN_LIGHT`/`WAN_LIGHTNING` indices and same-module `rnd_class` (draw-for-draw with C, loop included). Verified faithful and untouched: WEAPON/AMULET/POTION/SCROLL/RING/WAND/STATUE/EGG/CORPSE/SLIME/KELP/CANDY arms, `blessorcurse`/`bcsign` order (C `:1841–1860`), `may_generate_eroded`/`mkobj_erosions` (arm-for-arm vs C `:177–222`), ICE_BOX corpse path, COIN/ROCK box arms, candy `1+rn2(12)` (C `SIZE(candy_wrappers)-1` = 12, counted). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn mksobj_init` → PASS syntax (1 changed js file: js/mkobj.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited trace reach, re-ran `--base f7aec9b3` → same vacuous note, tail pasted) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `--full` → PASS full 44/44 · VERIFY: PASS. Preflight `--no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); the TIN zero-`cnutrit` and BoH-nesting arms fire only when level-gen draws those paths, which no fortress session reaches (durable-test-collateral disclosure).
**Named:** none new. Still deferred (pre-existing): `mksobj_init` default-arm `panic` (C panics on unknown class; sync JS cannot await async `impossible`, unreachable for valid otyp); ICE_BOX `obj_stop_timers` breadth (≡ C's three stops for fresh corpses); bless-family COIN/luck/bag-weight tails (D-2244); BoH weight factor (map line below); VENOM/CHAIN/BALL no-op (matches C `break`).
**Next:** do not re-pop `mksobj_init`. Falsifier for this family: a session blocked with `mksobj_init` as owner, or `mksobj_init`-attributed draws (`rndmonnum`/`blessorcurse`/`rn2(6)`) at its first-divergence step. The two cited sessions diverge outside it — Healer-92042 step 73 is C `rnd(79) @ rloc(teleport.c:1850)` vs JS `rn2(12) @ mcalcmove` (missing migrant, parked `rloc` writer = migration creation), Tourist-92100 step 131 is C `rn2(24)` vs JS `rn2(17)` at the same `collect_coords` shuffle (geometric candidate count, parked `collect_coords`→tactics writer) — with every pre-divergence draw matched (rngM 18880 / 25076), so a port here guarantees no movement on them.

## 2026-09-14 — D-2264 `makemon.c` `makemon_rnd_goodpos` fallback clears `GP_CHECKSCARY` in `gpflags` itself (queue row `makemon.c` makemon_rnd_goodpos, trace reach only)

**C locus:** `makemon.c:1076–1137` (`makemon_rnd_goodpos`); `:1085` (`gpflags |= GP_AVOID_MONPOS`); `:1100–1104` (`bl` init + `for (; bl < 2; bl++) { if (!bl) gpflags &= ~GP_CHECKSCARY;` — the clear mutates the loop-carried parameter, so bl==1 and the stairway `goodpos(nx, ny, mon, gpflags)` run without it); `:1163` caller `gpflags` (`MM_IGNOREWATER | GP_CHECKSCARY | GP_AVOID_MONPOS`); `teleport.c:167–168` (`checkscary` → `onscary`/`goodpos_onscary` reject).
**JS:** 1 file, +5/−3 (`js/makemon.js`), under the 600/10 caps. Density note: portable C is 58 lines and the clone (D-0034) already existed — a drift fix is small by nature.
**Change:** `js/makemon.js` — mutate the parameter in place (`if (!bl) gpflags &= ~GP_CHECKSCARY;`, no per-pass copy) and pass `gpflags` to all three fallback `goodpos` calls: exact C control flow (the C parameter is pass-by-value, so the caller is unaffected in both). Verified faithful and untouched: `Blind` term (`game.u?.ublind || game.u?.Blind`, house youprop-mirror idiom per end.js:337/hack.js:283), null-mon caller (`fakemon = ptr ? {data:ptr} : null` ≡ C `ptr ? &fakemon : NULL`), `rn1(COLNO-3,2)`/`rn2(ROWNO)` tries, `((dx+xofs)%(COLNO-1))+1` wrap (`coordxy` is int16, no truncation), stairway `rn2(2)` walk + `break`. No new imports; no DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn makemon_rnd_goodpos --base f7aec9b3` → PASS syntax (1 changed js file: js/makemon.js) · PASS rule2 · note hidden (vacuous: 0 blocked at f7aec9b3 and HEAD — NOT a corpus PASS; the row cited trace reach, not N blocks, so no further --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `--no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); the fallback arm needs 50 consecutive `goodpos` failures on a sighted level, which no fortress/corpus session reaches (durable-test-collateral disclosure).
**Named:** none new. `goodpos` onscary fidelity (`goodpos_onscary` approximation vs live `onscary` for `m_id`) stays with its owners (D-1101/D-1102 family).
**Next:** do not re-pop `makemon_rnd_goodpos`. A future random-placement divergence attributes to callees (`goodpos`, `cansee`, `enexto_core`) or the stairs list, not this loop.

## 2026-09-14 — D-2263 `read.c` litroom tail: Punished `move_bc` pick-up/re-place + gremlin queue/drain + engulfer-lit plines; `light.c` `snuff_light_source` (queue row `read.c` litroom gremlin light hits + Punished `move_bc`, no corpus block)

**C locus:** `read.c:2473–2488` (`set_lit`: `levl[x][y].lit = 1` + `m_at` gremlin queue via `data == &mons[PM_GREMLIN]`; else `lit = 0` + `snuff_light_source(x,y)`); `read.c:2543–2562` (blessed invent loop + swallowed `on` plines: `digests(ustuck->data)` → `s_suffix(Monnam) + mbodypart(STOMACH) + "is lit"`, `is_whirly` → "shines briefly", else "glistens"; Blind silent); `read.c:2559–2564` (`Punished && !on && !Blind` → `move_bc(1, 0, uball->ox, …)` after the `no_op` return); `read.c:2605–2633` (`!Blind` → `vision_recalc(2)` + `Punished && !on` → `move_bc(0, 0, …)`; `vision_full_recalc = 1`; `if (gremlins)` → `vision_recalc(0)` + LIFO drain `light_hits_gremlin(gremlin->mon, rnd(5))` + free); `light.c:729–758` (`snuff_light_source`: first LS_OBJECT entry at (x,y) wins, `obj_is_burning` gate, artifact lights skipped, `end_burn(obj, otyp != MAGIC_LAMP)`, return after since the entry is removed); `uhitm.c:6425–6449` (`light_hits_gremlin` body — already live in JS); `ball.c:437–556` (`move_bc` body — already live in JS).
**JS:** 3 files, +92/−15 (`js/read.js` +63/−12; `js/light.js` +28/−2; `js/uhitm.js` +1/−1), under the 600/10 caps. Density note: portable C is ~60 lines (set_lit + snuff_light_source + two move_bc sites + drain + engulfer arms); the balance is per-arm C-order cites plus the LIFO/timer notes the next iter needs to not "simplify" away.
**Change:** `js/light.js` — new `export function snuff_light_source(x, y)` in C order (index loop over `game.light_base`, `| 0` coords, first LS_OBJECT match, `obj_is_burning` gate, artifact skip, `end_burn(obj, otyp !== MAGIC_LAMP)`, immediate return); new `end_burn` name on the existing timeout edge + `objectNames` edge (`imports.mjs --can light.js objects.js` → no cycle) with a top-level `MAGIC_LAMP` const (timeout.js:1314 pattern). `js/uhitm.js` — one-word `export` on `light_hits_gremlin` (no clone #2; already C-faithful incl. Deaf/`mdistu` cry, `canseemon` recoil, `wake_nearto`, `monkilled`/`killed`, `map_invisible`).
**Verify:** `node scripts/verify.mjs --fn litroom` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: row cited 0 corpus blocks, no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; then forced `--full` → PASS full 44/44 · VERIFY: PASS. Preflight `--no-cohort` green on a clean tree before edits. /tmp probe (kept out of tree, deleted): 6/6 — timed floor lamp snuffed via the C `end_burn(TRUE)`→`stop_timer`→cleanup chain (entry removed, lamplit 0), wrong-cell untouched, unlit entry kept, empty base no-throw. First probe run failed 2/6 on a timerless fake lamp and proved the failure was the probe's (a real burning lamp always holds a BURN_OBJECT timer via begin_burn; C `stop_timer` on a timerless obj is likewise a no-op) — re-ran with a live `start_timer` registration, 6/6.
**Named:** Underwater beyond the `no_op` gate (as before); `end_burn`/`cleanup_burn` `update_inventory` (C-live, JS-named elsewhere); Rogue whole-room + Sunsword radius-0 stay as-was (already live, map-stale wording untouched beyond this row).
**Next:** do not re-pop the litroom row (all D-2250 deferrals in this envelope are live). A future scroll-of-light divergence attributes to the callee owners (`snuff_lit`/`impact_arti_light`/`obj_resists`, `do_clear_area` radius, `move_bc` paint, `light_hits_gremlin` RNG), not this wiring. Do not "fix" with seed/step/coordinate/RNG-index gates.
