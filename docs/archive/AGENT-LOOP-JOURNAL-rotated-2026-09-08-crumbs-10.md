# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-08 — D-2094 trap.c float_down come-down used a floor/ground stand-in, printing «to the floor» on stairs (queue owner float_down, 1 session)

**C locus:** `trap.c:4144` `You("float gently to the %s.", surface(u.ux, u.uy))` — the shared `dungeon.c:1750–1788` `surface()` (D-2008), whose stairs arm (`On_stairs` → 'stairs', ahead of the `IS_ROOM` → 'floor' arm) fires because STAIRS >= ROOM.
**JS:** 1 file (trap.js +9/−3), under the 600/10 caps.
**Change:** `js/trap.js` `float_down` only — the come-down arm now `await import('./sit.js')` for `surface` (the dynamic-import idiom this file already uses for `./sit.js` `split_mon`; sit.js statically imports trap.js, so a static edge would add a trap↔sit cycle — imports.mjs verdict for the static shape was SAFE/hoisted, but the dynamic call-time import avoids the question entirely, no TDZ) and prints `surface(u.ux | 0, u.uy | 0)`. Doc comment drops `surface() exact` from Named omissions. No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn float_down` → PASS syntax (1 changed file: js/trap.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-intrinsic-Samurai-92017 step 43 → `dosearch` at step 54, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree; final verify after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** file-local `surface_fd` stays for the `fall_through` `The … opens up` pline (`js/trap.js:3598`, different C function); other `float_down` omissions stand (Punished ball drag, ustuck release wording, selftouch/dismount Sokoban fell, underwater vision, assign_level trapdoor skip).
**Next:** do not re-pop `float_down` (0 blocked). scen-intrinsic-Samurai-92017's `dosearch`@54 flows through the normal queue.

## 2026-09-08 — D-2093 mon.c unstuck never ran on hero kill: lichen-holder death skipped `rnd(2)` mspec_used, xkilled treasure drew one slot early (queue owner unstuck, 1 session)

**C locus:** (1) `mon.c:3438–3467` `unstuck` — `u.ustuck==mtmp`, `!mspec_used`, holder gate `dmgtype(AD_STCK)||AT_ENGL||AT_HUGS` → `mspec_used=rnd(2)`. Lichen qualifies (`monsters.h:1614` `ATTK(AT_TUCH,AD_STCK,0,0)`). (2) `mon.c:2696–2703` `mon_leaving_level` — `mtrapped=0` + `unstuck(mon)` on every death/migration. (3) `mon.c:3175` `m_detach` → `mon_leaving_level`, called from `mondead :3175`, called from `xkilled :3535` *before* the `!rn2(6)` treasure drop `:3595`. So a stuck-holder kill draws `rnd(2)` then `rn2(6)`.
**JS:** 1 file (uhitm.js +6/−0), under the 600/10 caps.
**Change:** `js/uhitm.js` `xkilled` only — after the lifesaved early-return (matching C `mondead` lifesave-before-`m_detach`), `mtmp.mtrapped = 0` + `await (await import('./mhitu.js')).unstuck(mtmp)` before treasure. Dynamic import = the `mon.js:1714` unstuck cycle idiom already used in this file (`mdamageu`); call-time use only, no TDZ (imports.mjs verdict for the static shape was already SAFE — hoisted function). No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn unstuck` → PASS syntax (1 changed file: js/uhitm.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Rogue-92210 step 120 → `prisoner_speaks` at step 146, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree; final verify after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** `mhitm.js`/`trap.js` `mondead` still skip `mon_leaving_level` unstuck (same one-line gap on mon-kill/trap-kill paths; no corpus session blocks on them here); full `mon_leaving_level` take-off-map (`remove_monster`/`mundetected`/`seemimic`/`fill_pit`/polearm) stays as-is — JS death map handling untouched; `lifesaved_monster`/`vamprises`/`m_unleash`-slack and other `m_detach` arms stay deferred per existing `mondead` comments.
**Next:** do not re-pop `unstuck` (0 blocked). scen-wish-Rogue-92210's `prisoner_speaks`@146 flows through the normal queue.

## 2026-09-08 — D-2092 pager.c do_look + checkfile: `;`-glance `:`-pick on nonhuman-race self never showed the data.base entry (found never reset, asked instead of DontAsk, no named/called alt pass, menu painted over pending More) (queue owner x_monnam, 2 sessions)

**C locus:** (1) `pager.c:1346–1353` check_monsters "or you" tack-on (`found += append_str` → 1→2) + didlook `:1591–1616` (lookat parenthetical appended, `found = 1` "we have something to look up"). (2) `do_look :1941–1948` (`found == 1` gate; `(ans == LOOK_VERBOSE) ? chkfilDontAsk : chkfilNone`). (3) `checkfile :948–976` (named/called split, alt article/paren strip), `:996–1053` (pass 1 alt first, pass 0 same-offset skip), `:1056–1072` (user-typed/no-ask display); flags `:57–60` (`chkfilNone=0/UsrTyped=1/DontAsk=2/IaCheck=4`). (4) `wintty.c:1929–1932` menu display flushes NEED_MORE WIN_MESSAGE blocking first.
**JS:** 1 file (pager.js +96/−21), under the 600/10 caps.
**Change:** `js/pager.js` only — (1) u_at arm returns `found: 1` with didlook cite; (2) `checkfile(first, ans === LOOK_VERBOSE ? CHK_DONT_ASK : 0)`; (3) new `split_db_query` (split the lowered full input first, incl. the named-beats-called truncation rule; article/paren alt cleanup) + two-pass loop with same-`index` skip + userTyped-display arm; `lookup_data_base_entry` returns `{body, index}`, `lookup_data_base` kept as wrapper for `ia_checkfile`; (4) `show_nhw_menu_text` opens with `await flush_topl_more()` (pre-existing import; the `paint_corner_nhw_menu` NEED_MORE-flush idiom). No new module edge; no DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn x_monnam` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Knight-91128 PASS; Barbarian-92024 step 36 → `done_in_by` at step 47, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS, plus full fortress `node frozen/ps_test_runner.mjs sessions` → 44/44 (extra guard for the menu-sequencing change). Preflight `verify --no-cohort` green on a clean tree; final verify after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** pm-derived dbase (`:862–864`); makesingular/fruit alt (`:990–996`); supplemental_name (`:956–958`); `sk` skip-set + `#ifdef DEBUG` debugcore (pre-existing D-1954); C «You don't have any information on those things.» vs JS «I don't recognize that.» user-typed-miss text (pre-existing, untouched). Retires the D-1954 pass-1 named/called-alt omit; corrects the D-2020 self found-count.
**Next:** do not re-pop `x_monnam` (0 blocked). Barbarian-92024's `done_in_by`@47 and the `pager.c checkfile` figurine row are queued as Open this commit (probes: `verify done_in_by`, `verify checkfile`).

## 2026-09-08 — D-2091 uhitm.c mhitm_ad_drli + mhitm_ad_plys uhitm arms never ported: poly-hero drain/paralyze attacks skipped their `!rn2(3)`, so knockback dice drew one slot early (queue owner mhitm_knockback, 2 sessions)

**C locus:** (1) `uhitm.c:2450–2477` `mhitm_ad_drli` uhitm arm — `if (!rn2(3) && !(resists_drli(mdef) || defended(mdef, AD_DRLI)) && !mhitm_mgc_atk_negated(magr, mdef, TRUE))` then `d(2,6)` drain off mhpmax (floored at m_lev+1) and mhp, level-0 `xkilled` else m_lev--, leftover damage zeroed. (2) `uhitm.c:3434–3442` `mhitm_ad_plys` uhitm arm — `if (!rn2(3) && mhm->damage < mdef->mhp && !mhitm_mgc_atk_negated(magr, mdef, TRUE))` then !Blind «frozen by you!» + `paralyze_monst(mdef, rnd(10))`, leftover dice kept.
**JS:** 2 files (uhitm.js +62/−3, mhitm.js +2/−2), under the 600/10 caps. Two arms of the damageum dispatch reached by the row's two sessions; neither alone closes the row (drli-only verify left Ranger unchanged).
**Change:** `js/uhitm.js` — new `damageum_ad_drli` (C order verbatim: `!rn2(3)`, `resists_drli`, mgc_negated(TRUE); drain math with `|0` ints; `Monnam` «becomes weaker!»; mhpmax floor; mhp; level-0 `xkilled(XKILL_NOMSG)` with nonliving expires/dies else m_lev--; damage zeroed) wired as the AD_DRLI arm; new `damageum_ad_plys` (C order: `!rn2(3)`, damage<mhp, mgc_negated(TRUE); `Blind_that()`-gated «frozen by you!»; `paralyze_monst(rnd(10))`) wired as the AD_PLYS arm; adtyping doc lists both live. `resists_drli` joins from `./zap.js` (imports.mjs SAFE — same 90-module SCC, hoisted function decl, call-time use only; mhitu.js already holds this edge shape); `paralyze_monst` is a new `export` on the existing mhitm.js edge (same-file sync fn, no new edge); `AD_DRLI` joins the mhitm.js export block (uhitm.js already had local `AD_PLYS=14`, so no second declaration). No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn mhitm_knockback --full` → PASS syntax (2 changed files) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Wizard-92076 PASS; Ranger-92090 step 236 → `ghitm` at step 238, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after; no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** `defended(mdef, AD_DRLI)` worn-item walk (named on every defended site; zap.js `resists_drli` carries the same omit); `mhitm_ad_drli` mhitm (mon→mon) arm + `mhitm_ad_dren` (map turns.md:3379); `mhitm_ad_plys` mhitu/mhitm arms. Retires the uhitm halves of both map rows.
**Next:** do not re-pop `mhitm_knockback` (0 blocked). Ranger-92090's `ghitm`@238 flows through the normal queue.

## 2026-09-08 — D-2090 monmove.c dochug MS_CUSS `!rn2(5)` cuss arm was a named omit, and mcastu.c `Antimagic()` ignored uprops so cloak-MR took the losestr arm (queue owner dochug, 2 sessions)

**C locus:** (1) `monmove.c` dochug tail — `if (inrange && mdat->msound == MS_CUSS && !mpeaceful && couldsee(mx,my) && !minvis && !rn2(5)) cuss(mtmp);` the only direct `rn2(5)` in dochug (distfleeck's bravegremlin is tagged `distfleeck`). (2) `mcastu.c` `mcast_weaken_you` — Antimagic arm is draw-free (`You_feel("momentarily weakened.")`), else arm draws `rnd(dmg)` via `losestr`; `youprop.h:55–57` `HAntimagic ≡ uprops[ANTIMAGIC].intrinsic`, `EAntimagic ≡ uprops[ANTIMAGIC].extrinsic`, `Antimagic ≡ H || E`.
**JS:** 2 files (monmove.js +9/−2, mcastu.js +16/−2), under the 600/10 caps. Two causally-linked arms, one queue row: the cuss arm moves Priest, the Antimagic-reader arm moves Wizard; neither alone closes the row.
**Change:** `js/monmove.js` — `cuss` joins the pre-existing `./wizard.js` import (imports.mjs ALREADY, no new edge; call-time use only, no TDZ); local `MS_CUSS = 34` beside `MS_BRIBE` (monflag.h); gate in verbatim C order/position after `quest_talk`. `js/mcastu.js` — `ANTIMAGIC` joins the `./const.js` import; file-local `Antimagic()` and the death-touch inline now read flats + `uprops[ANTIMAGIC]` (D-1089 `hero_Antimagic` pattern, flats kept for eat/poly paths). No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn dochug` → PASS syntax (2 changed files) · PASS rule2 · PASS hidden `0 PASS, 2 moved past (1 re-attributed at the same step), 0 unchanged, 0 worse → PROGRESS` (Priest-92235 step 59 → `zap_hit` at step 59, same-step re-attribution, toplines match; Wizard-92103 step 100 → `really_done` at step 110, 10 steps later) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after; no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** none new — retires `cuss !rn2(5)` on dochug (map turns.md:2623) and the mcastu-reader half of the cloak-MR gap (map data.md:1267) for all 9 file-local `Antimagic()` sites + death-touch uselessness. Still named: `demon_talk` paid-off path (mux==hero); the other files' flat-only `Antimagic` clones (mhitm/muse/potion/pray/spell/teleport/explode/artifact — same cloak-MR blind spot, own future rows if the corpus reaches them); the confer mirror itself is not touched.
**Next:** do not re-pop `dochug` (0 blocked). Priest-92235's `zap_hit`@59 and Wizard-92103's `really_done`@110 flow through the normal queue.

## 2026-09-08 — D-2089 objnam.c readobjnam gold-block fall-through: wish `tin of gold piece` returned GOLD_PIECE where C (`return 2` → typfnd) makes a tin (review 1054 C-wrong 1)

**C locus:** `objnam.c` `readobjnam_postparse1` — the tin arm (`d->typ = TIN; return 2;`, ~:4390) sits textually above the gold block (`if (!BSTRCMPI(d->bp, d->p - 10, "gold piece") … return 3;`), so no tin-typed bp ever reaches the gold check.
**JS:** 1 file (readobjnam.js +8/−4), under the 600/10 caps. Small because the envelope is live (D-2084); density exception applies (Must-fix review C-wrong with a recorded falsifier).
**Change:** `js/readobjnam.js` — gold condition is now `if (!d.typ && isGold && GOLD_PIECE >= 0)` with a C cite (`return 2` skips this block); corrected the stale postparse1 comment that claimed every later block was already guarded. Same-file edit, no new import or edge, no TDZ.
**Verify:** `node scripts/verify.mjs --fn readobjnam` → PASS syntax (1 changed file) · PASS rule2 · note hidden (no corpus session blocked on readobjnam at HEAD — vacuous, NOT a corpus PASS; row cited 0 blocks so no `--base` re-run owed; the hand probe is the falsifier for this arm) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after; no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** none new — retires the `isGold` `!d.typ` guard (review 1054 Actionable 1). Still named: `of spinach` strip + `ishistoric`/`wetness`/`ftype`/`zombify` wish-prefix parsing; CORPSE zombify `start_timer` (`obj_to_any` missing).
**Next:** do not re-pop readobjnam for this arm. Review 1054's single C-wrong is fully addressed.
