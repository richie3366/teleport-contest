# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-08 — D-2128 `uhitm.c` mhitm_ad_plys mhitm (mon→mon) arm: ghoul-vs-monster freeze drew knockback dice where C draws `!rn2(3)` first (queue row `uhitm.c` mhitm_ad_plys, 1 session)

**C locus:** `uhitm.c:3430–3476` `mhitm_ad_plys`, mhitm arm `:3464–3475` (`mdef->mcanmove && !rn2(3) && !mhitm_mgc_atk_negated(magr, mdef, TRUE)` → `gv.vis && canspotmon(mdef)` → plain `pline("%s is frozen by %s.")` → `paralyze_monst(mdef, rnd(10))`; leftover kept). Reached via `mhitm_adtyping :4815` from mon→mon `mdamagem` (`mhitm.c:1059`), which runs adtyping → knockback (`:1061–1065`) → HP.
**JS:** 1 file (`js/mhitm.js` +42/−0 — a 12-line C arm plus doc + dispatch; the small diff is the whole envelope), under the 600/10 caps.
**Change:** `js/mhitm.js` only — new exported `mhitm_ad_plys(magr, mattk, mdef, mhm)` in exact C short-circuit order (`mcanmove`, `!rn2(3)`, mgc-negated(TRUE) via the same-module live helper, `_mm_vis && canspotmon` + C-plain `pline`, `paralyze_monst(rnd(10))` same-module live); `mdamagem` AD_PLYS dispatch in the AD_PHYS fall-through shape (arm keeps leftover/done, shared knockback + HP tail applies it). No new imports (all live in-module, no TDZ risk — no `imports.mjs --can` needed). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** hand probe (mhitm arm unreachable by corpus; throwaway, deleted after use): PROBE-PASS — frozen defender draws nothing; cancelled attacker draws exactly `[rn2(3)]` and never paralyzes; taken trials draw exactly `rn2(3)=0 @ mhitm_ad_plys, rn2(10) @ mhitm_mgc_atk_negated, rnd(10)=M @ mhitm_ad_plys` with `mfrozen==M`, leftover/done untouched; gate-fail trials draw exactly `[rn2(3)!=0]`. `node scripts/verify.mjs --fn mhitm_ad_plys` → PASS syntax (1 changed: js/mhitm.js) · PASS rule2 · PASS hidden (0 PASS, 1 moved past → PROGRESS: Monk-92005 RNG now 2735/2735, screens 104/106, residual screen-first save_dungeon@101, was rng-first mhitm_ad_plys@78) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (forced `--full`; shared combat file) · VERIFY: PASS. Preflight green before edits.
**Named:** mhitu you-as-def arm stays named → open D-2005 (`hitmsg` + `nomul` + `dynamic_multi_reason` + `exercise`, 4-session unit; not poached). `defended()` untouched.
**Next:** Monk-92005 now diverges screen-first at step 101 under `save_dungeon` (dungeon overview screen) — leave to the corpus queue; do not re-pop `mhitm_ad_plys` for it.

## 2026-09-08 — D-2127 `zap.c` zhitm ZT_LIGHTNING blind arm: `rnd(50)` draws before the `rn2(3)` destroy gate (queue row `zap.c` zhitm, 1 session)

**C locus:** `zap.c:4342–4365` ZT_LIGHTNING (`tmp = d(nd,6)`; spellcaster → `spell_damage_bonus`; `resists_elec||defended` → shield + `tmp = 0` with «can still blind»; `!resists_blnd && !(type > 0 && engulfing_u) && nd > 2` → `rnd(50)`, `mcansee = 0`, `mblinded += rnd` clamp 127; then `!rn2(3)` → `destroy_items`). Helper `spell_damage_bonus :3479–3502` (ACURR Int/level arithmetic, RNG-free). Gate `resists_blnd mondata.c:248–272` (monster arm: `mblinded || !mcansee || !haseyes || msleeping` + AD_BLND expl/gaze; youmonst Blind/Unaware arm unreachable here — every zhitm caller passes a `monst*`, never `&youmonst`).
**JS:** 2 files (`js/zap.js` +21/−4, `js/mhitm.js` +1/−1 — 22 insertions; the C locus is a 24-line arm so the small diff is the whole envelope), under the 600/10 caps.
**Change:** `js/zap.js` only, exact C order — `if (spellcaster) tmp = spell_damage_bonus(tmp)` (same-module live helper), shield + `tmp = 0`, the blind gate with `rnd(50)` / `mcansee = 0` / 127-clamped `mblinded`, then the `rn2(3)` destroy gate; import-the-export `resists_blnd_mm` (C-matched monster arm, `mhitm.js:717` + one `export`; `zap.js` already statically imports `mhitm.js` — `imports.mjs --can` ALREADY, no new edge; `engulfing_u`/`rnd` already imported). `defended()` stays commented (no JS implementation), `shieldeff` stays void (deferred). Header envelope + `turns.md` zhitm rows updated.
**Verify:** `node scripts/verify.mjs --fn zhitm` → PASS syntax (2 changed js file(s): js/mhitm.js js/zap.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify zhitm: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Samurai-92087 moved → dobuzz at step 65, was 64) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new. MM/FIRE/COLD-arm `spell_damage_bonus`, `defended()`, `shieldeff`, and `resists_blnd_by_arti` stay named (map + `zap.js` header; no corpus session reaches them this iter).
**Next:** Samurai-92087 now diverges at step 65 under `dobuzz` — leave to the corpus queue; do not re-pop `zhitm` for it.

## 2026-09-08 — D-2126 `display.h` Invis/See_invisible uprops fallback: worn ring of invisibility hides @ via newsym canspotself (writer behind queue row `potion.c` self_invis_message, 1 session)

**C locus:** `youprop.h:198` `Invis ≡ (HInvis || EInvis) && !BInvis` (each arm is `uprops[INVIS].intrinsic/extrinsic/blocked`), `:152` `See_invisible ≡ HSee_invisible || ESee_invisible` (`uprops[SEE_INVIS]`), `display.h:174` `canseeself ≡ Blind || uswallow || (!Invisible && !uundetected)` with `Invisible ≡ Invis && !See_invisible`, reached via `do_wear.c:2410–2411` `setworn(obj,mask)` then `:1300–1304` `Ring_on` RIN_INVISIBILITY (`learnring` → `newsym(ux,uy)` → `self_invis_message`). C `setworn` confers the extrinsic into `uprops` before `newsym`, so `canspotself` is already false at repaint.
**JS:** 1 file (`js/display.js` +16/−4 — the C locus is 3 macros so the small diff is the whole envelope), under the 600/10 caps.
**Change:** `js/display.js` only — `hero_Invis()` now ORs flats + `uprops[INVIS]` intrinsic/extrinsic/blocked, `hero_See_invisible()` ORs flats + sticky + `uprops[SEE_INVIS]` intrinsic/extrinsic, in the established `Detect_monsters`/`hero_ETelepat` read-through pattern; `INVIS`/`SEE_INVIS` added to the same-edge `./const.js` import (no new module edge, no TDZ risk — no `imports.mjs --can` needed). Cites `youprop.h:152,198` + `display.h:174`. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn self_invis_message` → PASS syntax (1 changed js file(s): js/display.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify self_invis_message: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Healer-92010 moved → next_ident at step 120, was 62) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new. `hero_Blind` stays flat-only (EBlinded is mirrored by `confer_oc_oprop`; no corpus session proves an uprops-only blind path this iter).
**Next:** Healer-92010 now diverges at step 120 under `next_ident` — leave to the corpus queue; do not re-pop `self_invis_message` for it.

## 2026-09-08 — D-2125 `weapon.c` mon_wield_item artifact_light arm: monster-wielded Sunsword ignites + shine pline (queue row `light.c` arti_light_description, 1 session)

**C locus:** `weapon.c:918–928` in `mon_wield_item` (`artifact_light(obj) && !obj->lamplit` → `begin_burn(obj, FALSE)` → canseemon `pline("%s %s in %s %s!", Tobjnam(obj, "shine"), arti_light_description(obj), s_suffix(mon_nam(mon)), mbodypart(mon, HAND))`, else cansee → «Light begins shining nearby/in the distance» via `mdistu<=25`). The queue owner `light.c:922` is the «brilliantly» adverb literal inside `arti_light_description` (`light.c:916–931`, radius from `arti_light_radius :881–911` — already sync in `js/timeout.js`); the message writer is `mon_wield_item`, ported here.
**JS:** 2 files (`js/weapon.js` +19/−4, `js/objnam.js` +3/−1 — 22 insertions; the C locus is an 11-line arm so the small diff is the whole envelope), under the 600/10 caps.
**Change:** `js/weapon.js` only — the `:918–928` arm in exact C order (`begin_burn(obj, false)` before the visibility branch so lamplit/radius are set first, matching C where the adverb reads the lit radius); import-the-export `arti_light_description` + `Tobjnam` over the pre-existing weapon→objnam edge (one `export` added in `js/objnam.js`, no second copy), `begin_burn` via same-edge `timeout.js` extension, `dist2` via a new weapon→hacklib edge (`imports.mjs` SAFE, no cycle; `mdistu≡dist2(mx,my,ux,uy)` per `js/mon.js:133`). Header retires artifact_light from the deferred list. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn arti_light_description` → PASS syntax (2 changed js file(s): js/objnam.js js/weapon.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify arti_light_description: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-poly-Tourist-92047 moved → gazemu at step 163, was 161) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new. mwelded refuse-wield plines + weld-on-wield + autoreturn tether stay deferred (map + `weapon.js` header; no corpus session reaches them this iter).
**Next:** Tourist-92047 now diverges at step 163 under `gazemu` (the «blinded by the Archon's radiance» frame; `mhitu.c:1806` hero-side port already live at `mhitu.js:3365`, so the residual is the monster-gaze arm) — leave to the corpus queue; do not re-pop `arti_light_description` for it.

## 2026-09-08 — D-2124 `mon.c` setmangry victim growl else-arm: peaceful non-humanoid growls on attack (writer behind queue row `uhitm.c` missum, 1 session)

**C locus:** `mon.c:4265–4318` `setmangry` (reached via missum → `wakeup(mon, TRUE)` → `setmangry`), `:4304–4309`: humanoid/isshk/isgd → couldsee «gets angry!» pline, `else growl(mtmp)`. `growl` (`sounds.c:402–427`) → `growl_sound` default `:395` «scream» → «It screams!» (blind hero → `Monnam` «It») + `wake_nearto(mlevel*18)`. The freeze is a red herring: step-118 C RNG shows `passiveum(mhitu.c:2523)` `rn2(3)=2` on the second kick, so C freezes too — behind `--More--`; JS's missing scream shifted the queue one slot and pulled «It is frozen by you.» into the topline.
**JS:** 1 file (`js/mon.js` +9/−4 — the C delta is exactly this 3-line arm), under the 600/10 caps.
**Change:** `js/mon.js` only — `else { await growl(mtmp); }` in exact C position with C cite; `growl` was already imported from pre-existing `./sounds.js` edge (no new import, no TDZ risk — no `imports.mjs --can` needed); header retires growl from the named omissions. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn missum` → PASS syntax (1 changed js file(s): js/mon.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify missum: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-poly-Knight-92220 moved → step 136, was 118) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits. Note: verify prints the new row as «js-throw at step 136» because its owner is null, but there is no throw — `error` is null and raw `ps_test_runner` is deterministic over 3 runs (RNG 6847/6857, screens 203/206, error null); the step-136 row is an unattributed screen diff (C «The ki-rin kicks!
**Named:** none new. Elbereth hypocrite/`rnd(5)`/`del_engr` + `qst_guardians_respond` stay named (map + `mon.js` header; quest-leader only, no corpus reach).
**Next:** Knight-92220 now diverges at step 136 (ki-rin kick/kick/butt `--More--` tail; stepFns `mattacku`/`hitmu`/`choose_monster_spell`; no C-literal owner) — leave to the corpus queue; do not re-pop `missum` for it.

## 2026-09-08 — D-2123 `do_wear.c` canwearobj polyform/weld/trap gates: poly-unwearable armor DOWNPLAY makes wear prompt `[*]` (queue row `invent.c` getobj, 1 session)

**C locus:** `do_wear.c:2029–2206` `canwearobj` (verysmall/nohands `:2036–2042`, cantweararm cloak-exception + racial `:2043–2057`, worn `:2058–2063`, welded-bimanual suit/shirt `:2065–2072`, helm horns `:2074–2084`, shield bimanual/twoweap `:2085–2103`, boots slithy/centaur/utrap `:2104–2134`, gloves welded/Glib `:2135–2153`, shirt/cloak/suit occupancy `:2154–2188`, else silly_thing `:2189–2194`); `equip_ok :3403–3447` armor-unwearable → DOWNPLAY; `wear_ok :3464–3468`; `invent.c` getobj filter `:1832–1917` + prompt `:1914–1919` (`!buf[0]` → ` [*]`, else ` [%s or ?*]`).
**JS:** 2 files (`js/do_wear.js` +133/−17, `js/worn.js` +4/−3 — 137 insertions), under the 600/10 caps.
**Change:** `js/worn.js` — `cantweararm` exported (import-the-export, no second copy; `breakarm`/`sliparm` stay private). `js/do_wear.js` — same-edge import extensions only (no new module edge): `worn.js` +`cantweararm`/`racial_exception`/`WrappingAllowed`/`is_flimsy`/`has_horns`/`num_horns`, `wield.js` +`bimanual`, `monsters.js` +`slithy`/`MZ_SMALL`, `const.js` +`Upolyd`/`TT_LAVA`/`TT_BURIEDBALL`; full C-order ports in both `canwearobj` (noisy, with C messages) and `canwearobj_silent` (equip_ok DOWNPLAY, silent) — verysmall/nohands, cantweararm with the MUMMY_WRAPPING/`WrappingAllowed`/small-cloak exception + `racial_exception`, worn, welded-bimanual, helm horns (`Upolyd` + `has_horns` + `!is_flimsy`), shield bimanual/`twoweap`, boots slithy/centaur/4-trap, gloves welded/`hero_glib`, occupancy, else `silly_thing`. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn getobj` → PASS syntax (2 changed js file(s): js/do_wear.js js/worn.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify getobj: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-poly-Wizard-92169 moved → `collect_coords` at step 164, was 61) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** none new. Noisy utrap infloor/lava message omits the `surface()` terrain name and buried-ball uses FOOT (C LEG) — display-only approximation, functional return exact; helm-occupied keeps the pre-existing generic `already_wearing('that')` (C `an(helm_simple_name)`).
**Next:** Wizard-92169 now diverges at `collect_coords` step 164 (parked symptom owner — true writer is `wizard.c` tactics per the Parked row; do not re-pop `getobj` for it); the corpus queue will surface the tactics row.
