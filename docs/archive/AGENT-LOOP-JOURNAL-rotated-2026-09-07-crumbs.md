# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-06 — human: scenario corpus replaces the saturated proxy (no D-id)

**Why:** leaderboard held-out is **7/44** (RNG 22.8 %, screens 45.4 %) while the local proxy said 96 %; the last ~40 iterations shipped display/getpos singletons (`pmatch`, `On_ladder`, `map_frame_color`…) that no session can see. The public 44 and the mutant corpus are scripted-scenario vs random-key genres: the mutants never wish, `^G`, `#polyself`, `#wizintrinsic`, die, or `^V ?` to a named level.
**Change:** new `scripts/scenario-gen.mjs` (drives the C recorder key by key from the screen; 8 families) → 275 `scen-*` recipes in `hidden-corpus/recipes/`; `record-session.mjs` exports its marker parser. Re-scored: **262/540 PASS**, scen-* **7/275**. `LOOP-QUEUE.md` Must-fix = 4 `ReferenceError` imports (`is_pit`, `FORCEBUNGLE`, `otense`, `STARVED`; 8 sessions dead at step 0); Open = 14 corpus owners (`calendar.c getlt` DST ×51, `do_statusline2` ×11, `break_armor` ×9, `exercise` ×8, `enlightenment` family ×15, `wiz_intrinsic` ×7, …); the 9 map singletons moved to **Deferred**. Constitution §10.13 (saturation rule) + §10.14 (throw = Must-fix); playbook §2a, port/cadence/review prompts, runbook §3, `HIDDEN-PROXY.md` §4–5, `PORT-GAP-TOP30.md` refreshed.
**Verify:** `node scripts/hidden-proxy.mjs score --jobs 8` (553 in 199 s); old families unchanged (255/278); public 44/44 untouched (no `js/` edit).
**Next:** pop Must-fix; on the audit iter, if every family ≥ 85 %: `node scripts/scenario-gen.mjs --n 120 --seed <iter×100>`.

## 2026-09-06 — D-1987 getpos.c map_frame_color store + HiliteBackground wiring

**C locus:** `nethack-c/upstream/src/getpos.c` — `getpos_sethilite` `:41–64` (old frame color `:46`, defaultHiliteState recompute `:49`, getvalid-change reset `:50–51`, HI_ZAP/NO_COLOR store `:57–58`, old∪new newsym force `:60–62`) + `getpos_refresh` `:753–765` (Goodpos→default reset `:757`, Background re-sethilite `:762–765`) + `getpos_toggle_hilite_state` `:72–91`; `decl.c:820` store init NO_COLOR; `wintype.h:258–261` struct; C `bgcolors` default On (`optlist.h:196`).
**JS:** `js/getpos.js` (+49/−14), `js/display.js` (+9/−9 comments); 2 js files, under 600 cap.
**Change:** `js/getpos.js` — new `HiliteBackground = 2` + `defaultHiliteState` module state (C `:30–38`); `getpos_sethilite` in exact C order (old store read, default recompute from live `game.iflags?.bgcolors`, conditional reset, callback install, HI_ZAP/NO_COLOR store write with lazy `game.gw.wsettings` init, old∪new force on getvalid-or-color change via the established `force_getvalid_newsyms` union loop); `getpos_refresh` resets to `defaultHiliteState` + Background re-sethilite arm; toggle docstring corrected (3-state cycle was already live). `HI_ZAP` extends the ALREADY `./const.js` edge; `NO_COLOR` from `./terminal.js` (imports.mjs SAFE, no cycle). Behavior-inert under defaults (bgcolors unset → Normal → NO_COLOR, as C pre-getpos); JS `bgcolors` default (unset/Off vs C On) left untouched as options-init territory. display.js named-omission comments (3 spots) marked live. /tmp 11-arm probe forced bgcolors open (store/frame on valid vs non-valid cells, exit-path store, reinstall under toggled option — all ok), deleted after.
**Verify:** `node scripts/verify.mjs --fn getpos_sethilite` → VERIFY: PASS — `PASS syntax 2 changed js file(s)`; `PASS rule2`; `note hidden verify getpos_sethilite: no corpus session is blocked on it at HEAD` (vacuous — row cited 0 blocks, so no `--base` re-run owed and no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** `wdmode` field of `gw.wsettings` unset (tiled mode unsupported); C `bgcolors` On default (JS unset/Off — options-init territory, separate row); selvar identity (union loop is the established clone); `selection_free` (no selvar to free).
**Next:** pop the next Open row (`monmove.c` dochug demon/caster retaliation).

## 2026-09-06 — D-1986 display.c flush_screen span-gated grid paint + show_glyph pre-store dirty gate

**C locus:** `nethack-c/upstream/src/display.c` — `flush_screen` `:2241–2257` (per-row `gbuf_start[y]..gbuf_stop[y]` loop, `gnew || framecolor` gate, no clear) + `:2259` unconditional `reset_glyph_bbox()`; `show_glyph` `:2031–2056` (compare the BUFFERED glyphinfo against the new one before storing).
**JS:** `js/display.js` (+135/−69); 1 js file, under 600 cap.
**Change:** `js/display.js` — grid paint is span-gated per C (`gnew ||` live framecolor arm via `get_bkglyph_and_framecolor`; `gnew` cleared only when painted, `:2255`; unexplored-with-gnew paints blank, D-0931 precedent); no blanket clear. Message rows (WIN_MESSAGE: row 0 always, row 1 when wrapped) blank+paint with wrap arbitration keeping map row 0; status rows blank+paint from the bot cache (`bot_disabled`/`statusSuppressed` still skip, no snapshot/restore — D-1831); cursor logic unchanged. Overlay full-resync (JS-only: overlays share this grid, C windows are separate): flushes early-returning under `_menu_overlay` set `_overlay_resync`, and the next real flush repaints the full map including explicit unexplored blanks.
**Verify:** `node scripts/verify.mjs --fn flush_screen` → VERIFY: PASS — `PASS syntax 1 changed js file(s)`; `PASS rule2`; `note hidden verify flush_screen: no corpus session is blocked on it at HEAD` (vacuous — row cited 0 blocks, so no `--base` re-run owed and no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`. Mid-iteration the first gated version regressed seed0383 (6/7 cohort, 43/44 full; single group: stale map cells, owner make_hallucinated, RNG tied) — fixed once via the pre-store snapshot, re-ran once → green.
**Named:** `map_glyphinfo` glyphmap[]-base re-derive at `:2250` (tty transform already applied at store time; background arm shut on tty); `flushing` reentrancy guard (paint path is sync setCell-only, no impossible→pline recursion); `suppress_map_output` early gate (save/restore); `curs()`/`display_nhwindow(WIN_MAP)` mapping (cursor logic already parks on hero; the grid IS the window).
**Next:** `display.c`/`getpos.c` map_frame_color store (next Open row).

## 2026-09-06 — D-1985 display.c flush_screen dirty-span loop: conditional mark + unconditional reset

**C locus:** `nethack-c/upstream/src/display.c` — `show_glyph` change gate `:2031–2056` (store + `gnew = 1` + span expand only when the buffered glyphinfo differs: glyph id, ttychar, gm color/flags/tile, or `use_background_glyph`); `flush_screen` `:2259` unconditional `reset_glyph_bbox()`.
**JS:** `js/display.js` (+28/−12); 1 js file, under 600 cap.
**Change:** `js/display.js` — `show_glyph_cell` resolves the glyph id first (two ids can share one ttychar, e.g. altar/fountain `{`) and gates `gnew = 1` + `mark_gbuf_dirty` on id/ch/color/dec/attr difference; the disp store itself stays unconditional (rewriting identical values is a no-op, so the D-1767 "always stamp" map rows stand). `reset_glyph_bbox()` moved after the grid block so every rebuild empties spans per C `:2259`. Safe by construction: paint still rebuilds all (no incremental-paint overlay hazard — D-1984 regression mode untouched); the only `gnew`-gated paint paths (`bones.js` leave-level, `flush_screen_getpos_dirty`) keep working because real changes still set `gnew`, and the getpos path's `newsym_force` mark stays unconditional per C `:1867–1870`.
**Verify:** `node scripts/verify.mjs --fn flush_screen` → VERIFY: PASS — `PASS syntax 1 changed js file(s)`; `PASS rule2`; `note hidden verify flush_screen: no corpus session is blocked on it at HEAD` (vacuous — row cited 0 blocks, so no `--base` re-run owed and no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** span-loop shape + `:2241–2257` `gnew || framecolor` gate + no blanket clear (next Open row, needs overlay full-resync); `map_glyphinfo` re-derive at `:2250`; `gw.wsettings.map_frame_color` store wiring; `clear_glyph_buffer` `nul_gbuf.gnew` dynamic computation (C derives 0-or-1 from the unexplored rendering; JS keeps `gnew = 1` — revisit with the gate row); `use_background_glyph` arm (shut on tty, D-1984).
**Next:** pop the next Open row (`display.c` flush_screen span-gated grid paint — `:2241–2257` gate + no blanket clear).

## 2026-09-06 — D-1983 display.c map_glyphinfo ov_* accessibility override tables

**C locus:** `nethack-c/upstream/src/display.c` — `map_glyphinfo` `:2594–2656` (hero override `:2637–2644`, `GMAP_ROGUELEVEL` `:2590–2591` set from `Is_rogue_level` `:2759–2761`); `symbols.c` `init_ov_rogue_symbols` `:112–119` + `init_ov_primary_symbols` `:122–128` + `update_ov_primary_symset` `:295–298` + `update_ov_rogue_symset` `:301–304`; `decl.h` `:716–717` `go.ov_*_syms[SYM_MAX]`; `global.h:108` `nhsym` uchar; `hack.h` `:1080–1085` offsets; `sym.h` `:111–119` `SYM_HERO_OVERRIDE = 5`, `MAXOTHER = 6`.
**JS:** `js/display.js` (+66/−6); 1 js file, under 600 cap. Density note: C is ~8 init + ~8 update + ~8 hero-gate lines; the JS is the full ov-table cluster (consts + 2 getters + 4 exports) plus the live hero wire (one C `file.c:function` family, no stubs in a live arm).
**Change:** `js/display.js` — new exported `SYM_OFF_X = 190`/`SYM_MAX = 196` (`105 + 18 + 61 + 6 + 6`: MAXPCHARS 105 from `S_expl_br` 104, MAXOCLASSES 18 from `objects.js`, MAXMCLASSES 61 from `defsym.h` MONSYM 1..60, WARNCOUNT/MAXOTHER 6) + module-local `ov_primary_table()`/`ov_rogue_table()` (C `go` struct → `game.go`, lazily zero-filled length `SYM_MAX`) + exported `init_ov_primary_symbols`/`init_ov_rogue_symbols` in C order + exported `update_ov_primary_symset(idx, val)`/`update_ov_rogue_symset(idx, val)` (C takes `(symp, val)` with `symp->idx`; JS takes `idx` directly — no symparse struct — normalizing `nhsym` uchar to `0`/single char, string or char-code both accepted) + hero arm live (`heroOff = SYM_HERO_OVERRIDE + SYM_OFF_X`, table picked by live `Is_rogue_level(game.u?.uz)` — C caches it in `glyphmap_perlevel_flags`, named — `out.ch = heroOverride` only on `accessibility == 1 && !(mg & MG_FLAG_NOOVERRIDE) && heroOverride`, C order kept). `SYM_HERO_OVERRIDE` added to the existing `./const.js` edge (ALREADY, no new cycle surface). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). /tmp probe of the arms no suite reaches (tables zeroed → shut; primary override `X` paints; NOOVERRIDE keeps `@`; a11y-off keeps `@`; re-init shuts again; integer code 89 paints `Y`) → 6/6 PASS (deleted before finishing). `node scripts/verify.mjs --fn map_glyphinfo` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify map_glyphinfo: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — map-driven Open row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** `get_othersym` base + `assign_graphics` showsyms copy (ov tables live here; hero arm reads them directly — full symset machinery still deferred); `glyphmap_perlevel_flags` cache (live `Is_rogue_level` read instead); `reset_glyphmap` pet-override `SYM_PET_OVERRIDE` arm (`:3067–3076`, separate row); `pager.c` lookat override loop (`:1543–1580`, separate row); options `symbols.c:841–843`/`savedsym` wiring (no JS symbols-option path — tables stay zero until wired).
**Next:** pop the next Open row (`display.c` flush_screen dirty-span loop).

## 2026-09-06 — D-1982 wintty.c CLIPPING cliparound pan offsets + tty consumer gates

**C locus:** `nethack-c/upstream/win/tty/wintty.c` — `setclipped` `:3806–3814` (clipping on, pan at origin, `clipxmax = CO`, `clipymax = LI - 1 - wc2_statuslines`) + `newclipping` `:471–485` (small screen → setclipped + `tty_cliparound` when x nonzero; full screen → clipping off, offsets zeroed) + `tty_cliparound` `:3817–3842` (x hysteresis 5-margin/shift-20 clamped to COLNO; y hysteresis 2-margin/shift-half-viewport clamped to ROWNO; origin moved → `redraw_map(TRUE)` `:3840`) + `tty_print_glyph` clip gate `:3869–3873` (`x <= clipx || y < clipy || x >= clipxmax || y >= clipymax` → return) + `tty_curs` WIN_MAP offset `:2119–2124` (`x -= clipx, y -= clipy`) + `tty_display_nhwindow` board loop `:3696–3705` (skip + `row_refresh(xmin + clipx - offx, …, y + clipy - offy)`). Dispatch: `include/winprocs.h:141–142` `#define cliparound (*windowprocs.win_cliparound)` → tty procs. `CLIPPING` always compiled in (`config.h:537–538`); HUPSKIP empty without HANGUPHANDLING (`:93–94`, no arm); `wc2_statuslines` defaults 2 (`options.c:7263`). Core callers (still unwired): `allmain.c:546`, `dungeon.c:1580`, `getpos.c:851/1146`, `muse.c:2637`, `restore.c:629`.
**JS:** `js/display.js` (+153/−11); 1 js file, under 600 cap. Density note: C is ~7 + ~12 + ~24 + gate/offset arms (~15 lines); the JS is the four full C-ordered functions with `:line` citations plus the three live consumer gates (single queue row, every callee live — no stubs in a live arm).
**Change:** `js/display.js` — module-local `clipping`/`clipx`/`clipxmax`/`clipy`/`clipymax` (C file-statics) + `clip_screen_size()` (`CO`/`LI` ≡ `game.nhDisplay.cols/rows`, 80x24 fallback = the layout JS always renders) + exported `setclipped()` in exact C order (`?? 2` reproduces the C `wc2_statuslines` default) + exported async `newclipping(x, y)` (`CO < COLNO || LI < 1 + ROWNO + statuslines` → setclipped + `if (xi)` pan, else clipping off; async only for the `tty_cliparound`→`redraw_map` nhgetch reach, same shape as `redraw_map` D-1974) + exported async `tty_cliparound(x, y)` (early `!clipping` return; x/y hysteresis in C order with `Math.max/min` and `| 0` truncation matching C `/ 2`; origin moved → `await redraw_map(1)`) + exported async `cliparound(x, y)` dispatch alias (the `winprocs.h` macro shape). Sync consumer gates, all shut at 80x24: `_paint_gbuf_cell` early return (the `tty_print_glyph` gate), `docorner` skip (`y + clipy > ROWNO`) + offset `row_refresh` segment (the `:3696–3705` arms), hero cursor minus clips when clipping (the `tty_curs` arm). No new cross-module import (all same-module), so no `imports.mjs --can` owed.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. /tmp small-screen probe of the arms no suite reaches (40x20: base paints map-x 1..39 only; `tty_cliparound(70,10)` pans to 21..59; `tty_cliparound(70,19)` pans to 41..79 with y = 3 gated by clipy = 4; back at 80x24 `newclipping` switches clipping off and all 79 cells paint) → 4/4 C-arithmetic arms exact (deleted before finishing). `node scripts/verify.mjs --fn cliparound` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify cliparound: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — map-driven Open row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing` (auto: shared file changed).
**Named:** core `cliparound` call sites (`allmain.c:546` moveloop, `dungeon.c:1580` u_on_newpos, `getpos.c:851/1146`, `muse.c:2637`, `restore.c:629`) — functions live, unwired; resize/preference_update `newclipping` callers (no JS resize path; display fixed 80x24); `flush_screen` dirty-span loop stays its own Open row.
**Next:** pop the next Open row (`display.c` map_glyphinfo ov_* override tables).
