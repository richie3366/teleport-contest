# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-18 — D-2495 `cmd.c` help_dir whole-body port (live-binding grid; prefixhandling/self via spkeys)

**C locus:** `nethack-c/upstream/src/cmd.c:4171–4296` (`help_dir`, staticfn; sole code call site `cmd.c:4101` in `getdir`) + staticfn `show_direction_keys` (`cmd.c:4122–4165`). Callees: `create_nhwindow`/`putstr`/`display_nhwindow`/`destroy_nhwindow` (`show_text_pages` idiom, D-1806), `dowhatdoes_core` (`pager.js:2915` sync), `visctrl` (`dokeylist.js:42` sync), `cmd_from_func(do_move_*)` (live `cmd_from_dir(dir, MV_WALK)` — same move_funcs row-0 table), `letter`/`highc` (hacklib), `NODIAG(u.umonnum)` (hack.h PM_GRID_BUG-only).
**JS:** `js/lock.js` `show_direction_keys` (`:203`), `help_dir` (`:231`), 2 import lines (`:21`, `:59`).
**Change:** `js/lock.js` only — `help_dir` restarted in C order with `:line` cites plus module-local `show_direction_keys(lines, centerchar, nodiag)` with verbatim Sprintf layouts (`:4129–4130` falsy-center fallback; `:4133–4146` cardinal / `:4147–4164` 8-way) reading live `cmd_from_dir(dir, MV_WALK)` through `visctrl` (bottom row index 7,6,5 = SW,S,SE per move_funcs rows); `:4184–4185` prefixhandling vs live `getdir_spkey(NHKF_ESC)`; `:4277–4281` self via `getdir_spkey(numPad ? SELF2 : SELF)` with `%4s` padStart; `:4193–4229` `#if 0` buf arms cited as compiled-out so `*buf` at `:4239` is always empty. `cmd_from_dir` joins the dokeylist import block and `MV_WALK` the const block (`imports.mjs --can`: lock.js→dokeylist.js ALREADY, no new edge).
**Verify:** `node scripts/verify.mjs --fn help_dir` → first run FAIL on reach (smoke 23/24: explore-seed0116-wizard-wear-shop row 7 C `b  j  n` vs JS `n  j  b` — bottom-row dir indices read as SE,S,SW instead of C arg order SW,S,SE); fixed to `key(7),key(6),key(5)`. Re-run → PASS syntax (1 file: js/lock.js) · rule2 · hidden note (no session blocked) · reach REACH-OK (no RNG tags, smoke 24/24, 0 regressed) · green 2/2 · strict ×2 · cohort 7/7. VERIFY: PASS.
**Named:** `:4193–4229` `#if 0` bad-prefix buf arms (compiled out in C); `:4239` `*buf` branch (dead — always empty); `create_nhwindow` NULL → `!game.nhDisplay`; `dokeylist.js:378` `show_direction_keys(lines)` keylist clone untouched (different owner, `keylist_putcmds` family).
**Next:** next coverage row.

## 2026-09-18 — D-2494 `mon.c` setmangry whole-body port (Elbereth hypocrite + qst_guardians_respond; vault.c:526 dead-Croesus caller wired)

**C locus:** `nethack-c/upstream/src/mon.c:4265–4318` (`setmangry`); 18 code call sites (`dokick.c:152,318`, `dothrow.c:869`, `explode.c:584`, `mon.c:3075,4355`, `mthrowu.c:492`, `polyself.c:1698`, `priest.c:886`, `quest.c:479`, `read.c:1050`, `region.c:1145`, `trap.c:3823`, `uhitm.c:1293,1302,6398`, `vault.c:270,526`). Callees: `sengr_at` (`engrave.c:250–261`), `onscary` (`monmove.c:240–303`), `You_feel`, `adjalign` (`attrib.js:742` sync), `rnd` (`rng.js:97` sync), `pline`, `del_engr_at` (`engrave.js:150` sync), `pline_mon`, `Monnam`, `growl` (`sounds.js:830` async), `quest_info` (`questpgr.c:30–46`), `qst_guardians_respond` (`mon.c:4134–4159` staticfn), `peacefuls_respond` (`mon.c:4162–4257` staticfn, D-1772).
**JS:** `js/mon.js` `Blind`/`Hallucination` + `qst_guardians_respond` + `setmangry` (restarted), 4 import words; `js/vault.js` `invault` Croesus split + `PM_CROESUS` + 4 import words.
**Change:** `js/mon.js` — `setmangry` restarted in C order with `:line` cites: Elbereth arm first (`via_attack` + strict `sengr_at` inlined via live `engr_at`: exact case-insensitive `actual_text` match, `HEADSTONE` skip, `engr_time <= game.moves`, then live same-module `onscary(u.ux, u.uy, mtmp) || mpeaceful` short-circuit preserved) → `await You_feel('like a hypocrite.')` + `adjalign(record > 5 ? -5 : -rnd(5))` + `!Blind()` pline + `del_engr_at`; waitmask clear; `!mpeaceful`/tame returns; priest coaligned `-5`/`+2` else `-1`; humanoid/shk/gd `couldsee` `pline_mon` else `growl`; quest-leader mndx-vs-`urole.ldrnum` gate → new module-local `qst_guardians_respond` (C `:4134–4159` in C order: fmon sweep with DEADMONSTER skip, guardian-mndx + mpeaceful → clear, `canseemon` → `got_mad`, then Hallucination-gated `pline_The` with `makeplural` past one); `peacefuls_respond` when `!mon_moving`. New module-local `Blind()`/`Hallucination()` follow the do.js youprop idiom. Imports added to existing edges only (`del_engr_at`, `pline_The`, `makeplural`, `HEADSTONE` — `imports.mjs` new-edge check not needed, no new module pair).
**Verify:** `node scripts/verify.mjs --fn setmangry` → PASS syntax (2 files: js/mon.js js/vault.js) · rule2 · hidden note (no session blocked) · reach REACH-OK (no RNG-tagged reach; smoke 24/24, 0 regressed) · green 2/2 · strict ×2 · cohort 7/7. Re-run with `--reach-all` → identical PASS tail. VERIFY: PASS ×2.
**Named:** `quest.c:479` `quest_chat` pissed_off → `setmangry` (pre-existing deferral, `js/quest.js:432` comment); vault greeting Croesus-alive `waves goodbye` / `Sorry to have disturbed you` dialogue (`vault.c:514–523`, JS leaves via `mongone_guard`); `sengr_at` has no live ESM export (teleport.js:175 module-local clone stays the only copy — mon.js inlines strict to avoid clone #2); `quest_info` has no live export (read.js:2261 local — mon.js reads `game.urole.ldrnum/guardnum` directly per the peacefuls_respond idiom); `onscary`'s own pre-existing omissions (is_lminion, unique_corpstat, own-shop/temple, Inhell — `js/mon.js` doc); no maintained unit test (repo has no `tests/` harness — sessions are the suite; focused evidence is `verify --fn` + cohort above).
**Next:** next coverage row.

## 2026-09-19 — Audit 1444–1452 (D-2485…D-2493) + cadence: 7 ACCEPT, 2 ACCEPT-WITH-DEBT

1444 closes 1438 Must-fix (`has_egd` bound; import/use/export re-grepped). 1445 misc_obj whole-body exact (9 arms, RNG order, GEM/potion keep-dmgval OMIT w/ `:1415–1429` cite). 1446 parse_id whole family exact (hash/cache/G_/S_ vs C; LOADSYMS 196 verified; debt: one-hole piletop divergence from pre-existing wide `glyph_is_piletop_generic_obj`, nil blast radius). 1447 quickmimic exact (qm[] verbatim, all 5 call sites await, `!== something` proven safe). 1448 bc_sanity_check exact (debt: `safe_typename` guard reads enum names not oc_name — cosmetic, corrupt-state-only). 1449/1450/1451/1452 ACCEPT (legs envelope diffed identical; corpse_xname block-exact + TDZ revert honest; makemaz 4 C-wrongs fixed + hell gate in `makelevel`; see_monsters/flush_screen order-exact). Every SHA re-measured (`verify --base HASH~1 --reach-all`, 0 regressed; makemaz 77/77, misc_obj 12/12, legs 13/13). Rule #2 clean. No Must-fix prepend (debts unqueued). Cadence: public 44/44 (Scr 11,405, RNG 792,838), held-out 11/44 (unchanged), corpus 495/540 (91.7 %, identical). Refill: makemaz/see_monsters tool rows skipped as just-shipped; 9 verbatim appended (newcham/getobj/checkfile/yn_function/getdir/mon_arrive/make_blinded/really_done/rloc_to_core) to 12 coverage rows. No `js/` edits, no REJECT.

## 2026-09-18 — D-2493 `display.c` see_monsters + flush_screen whole-body ports (invented guards removed; suppress/reentrancy/hangup arms added)

**C locus:** `nethack-c/upstream/src/display.c:1487–1529` (`see_monsters`; 35 code call sites); `display.c:2208–2267` (`flush_screen`; 38 code call sites + 2 `win/tty/wintty.c`). Callees: `newsym` (`js/display.js:4980` sync), `see_wsegs` (`js/worm.js:472` sync), `Warn_of_mon` + `warntype.obj`/`mflags2` count, `Sting_effects` (`js/artifact.js:800` async — fire-and-forget from sync `see_monsters`, sync prefix computes old/new strength before any await); `bot`/`timebot` (`:7121`/`:7138` async), span-gated map paint + `reset_glyph_bbox` + curs-on-hero + `display_nhwindow(WIN_MAP)` (all inside `_buildScreenOutput`, `:6205–6374` terminal-grid adaptation of `print_glyph`).
**JS:** `js/display.js` `see_monsters` (`:5271`), `flush_screen` (`:6995`), `let _flushing` (`:6382`).
**Change:** `js/display.js` only, no new imports (`suppress_map_output` is same-module `:4748`; `_flushing` module-level beside `_delay_flushing`). `see_monsters`: both guards deleted with `:line` cites (`:1505–1518` no position guard — `newsym` no-ops off-map via `level.at`; `:1527–1528` hero cell always newsym'd when unmounted). `flush_screen` in C order: `suppress_map_output()` first (`:2220`, same gate as newsym/show_glyph/feel_location); `flushing` check+set synchronously before the first await so a reentrant call during `await bot()` returns early like C (JS reentry path is `pline_after_consume` `:7802`); `done_hup` after the set, returning with the guard held exactly as C does; remainder in try/finally (C has no exceptions; success-path semantics identical).
**Verify:** `node scripts/verify.mjs --fn see_monsters` → PASS syntax (1 file) · rule2 · hidden note (no session blocked) · reach REACH-OK (no RNG tags, smoke 24/24) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44. `node scripts/verify.mjs --fn flush_screen` → identical PASS tail (smoke 24/24, full 44/44). VERIFY: PASS ×2.
**Named:** `allmain.c:94` moveloop_preamble defer arm (`game.defer_see_monsters` is never set in JS — the `restore.c:682` setter has no JS counterpart; `newsym`-level `suppress_map_output` covers the restore hazard); `attrib.c:785` postadjabil (pre-existing deferral, `js/attrib.js:833`); `do_wear.c:546` Helmet_off (JS `Helmet_off` is a 9-line stub `:749–757` without the C `:518–546` takeoff/ability flow; cf D-2192 residual); `mon.c:3662` xkilled murderer arm (no `murderer` string in `js/` — HTelepat-strip/luck/message/Blind-gate arm absent from JS xkilled; mon.c home); `timeout.c:770` SEE_INVIS expiry body (JS has the dedicated SEE_INVIS arm, `js/timeout.js:915`, expiry body not diffed); `potion.c:872` peffect_see_invisible (pre-existing deferral, `js/potion.js:415`); pre-existing `js/uhitm.js:835` call without C counterpart (untouched); overlay/stale-map/`_paintToplineOnly`/per-option/per-site flush supersets and `flush_screen_getpos_dirty` (terminal-grid adaptations, D-1831 side-effect rule).
**Next:** next coverage row.

## 2026-09-18 — D-2492 `mkmaze.c` makemaz whole-body port (C-order restart; ransacked-assign, dmonsfree, await-impossible, hell/medusa 5th caller wired)

**C locus:** `nethack-c/upstream/src/mkmaze.c:1127–1223` (`makemaz`); 5 code call sites `mklev.c:1270,1272,1274,1285,1289`. Callees: `Is_special`, `rnd`, `check_ransacked` (staticfn `:707`), `load_special` (`sp_lev.c:6454`), `dmonsfree`, `impossible`, `rn2`, `Invocation_lev`, `create_maze`, `wallification`, `mazexy`, `mkstairs`, `pick_vibrasquare_location`, `maketrap`, `Is_branchlev`, `place_branch`, `populate_maze`; helpers `dunlev` (`dungeon.c:1325` returns dlevel), `dunlevs_in_dungeon` (`:1332` returns num_dunlevs), `LEV_EXT` (`global.h:34` ".lua"), wizard `getenv("SPLEVTYPE")` (`:1160–1182`).
**JS:** `js/mklev.js` `makemaz` (`:1480`), `makemaz_maze_fallback` (`:1549`), `makelevel` gate (`:24193`), `makelevel_ordinary` cleanup (`:24214`), 2 import lines (`:114`, `:139`).
**Change:** `js/mklev.js` only, no new static edges (`imports.mjs --can` reports the dungeon.js/mon.js edges already static) — 4 import words (`Is_special`, `Invocation_lev` in the dungeon.js block `:139`; `dmonsfree` in the mon.js block `:114`). `makemaz` restarted in C order (`:1480`): `:1133–1139` `*s` → `%s-%d` with `rnd(rndlevs)`; `:1140–1154` dungeon-proto + `dunlev` with `rnd` suffix when `dunlevs_in_dungeon > 1` (direct field reads — both C helpers are trivial returns); `:1160–1182` SPLEVTYPE kept as a named-omit comment (no environment in scored ESM, Rule #2); `:1184–1195` guarded block with `check_ransacked` ASSIGN, `.lua` extension kept for the message while dispatch takes the bare stem, `gi.in_mk_themerooms = FALSE`, `await load_special_proto` + live `dmonsfree()` on success, `await impossible` WITH the extension on failure, then fall-through to `makemaz_maze_fallback` (no early return, like C). Fallback (`:1549`) refreshed with `:line` cites and the live `Invocation_lev` (`!rn2(3)` ≡ `rn2(3) === 0`, `!rn2(5)` ≡ `rn2(5) === 0`, `mkstairs(x, y, up, 0)` ≡ NULL room + FALSE default).
**Verify:** `node scripts/verify.mjs --fn makemaz` → PASS syntax (1 file: js/mklev.js) · rule2 · hidden note (no session blocked) · reach REACH-OK (77 baseline-PASS sessions reach it, 77 run, 0 regressed) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed). VERIFY: PASS.
**Named:** SPLEVTYPE `getenv` endpoint (wizard-debug only; no environment in scored ESM, Rule #2); `Is_branchlev` (no live export — same-file local, only `end.js:575` clone elsewhere); `load_special` file IO (bare-stem `load_special_proto` dispatch); `In_hell` (no live export — inline hellish-flag read matching the do.js/trap.js clones); `Invocation_lev_mk` retained for `invocation_pos_mk`/other users while the makemaz arms use the live export.
**Next:** next coverage row.

## 2026-09-18 — D-2491 `objnam.c` corpse_xname whole-body port (C-order restart + food_xname wired; TDZ revert)

**C locus:** `nethack-c/upstream/src/objnam.c:1824–1920` (`corpse_xname`); 23 code call sites (dig/do/do_wear/dokick×2/dothrow/eat×2/invent×2/objnam×4/pickup/pray/trap/uhitm×2/wield/zap×3). Callees: `nextobuf`, `obj_pmname` (do_name.c:1321), `the_unique_pm`, `type_is_pname`, `s_suffix`, `an`, `eos`, `mungspaces`, `makeplural` (unused — plural is a literal `s`), `releaseobuf`.
**JS:** `js/objnam.js` `corpse_xname` (`:1215`); `js/eat.js` `food_xname` (`:1011`) + 2 import words. No new static edges.
**Change:** `js/objnam.js` — `corpse_xname` restarted in C order with `:line` cites: `:1830–1841` flag decode; `:1841` glob as `(otyp|0) !== CORPSE && globby`; `:1843` OBJ_NAME glob name; `:1844–1845` NON_PM paranoia → `thing` (null/negative guard is JS null-safety); `:1847` `obj_pmname` valid arm via `obj_pmname_corpse` (gender-aware + aligned-cleric remap); `:1848–1857` unique/pname possessive (`s_suffix`, pname → no_prefix, non-personal unique → the_prefix); `:1860–1863` prefix exclusion; `:1865–1872` forced `the ` (never `the()`); `:1874–1887` adjective placement (possessive after, else before) + `mungspaces` + ASCII `digit` (`hacklib.c:62–65`, charCode 48–57); `:1890–1897` ` corpse` suffix + literal-`s` plural with `quan > 1` clearing `any_prefix`; `:1902–1908` `an()` tail. Nextobuf/PREFIX + eos/Sprintf + releaseobuf are by-design plain strings (D-2483 idiom). New static `do_name.js`/`getline.js` edges for the live helpers were written first, then **reverted**: they reorder cycle eval past `shk.js:832` top-level `set_shk_owns_prefix` and TDZ-fault `let _shk_owns_prefix` (`js/objnam.js:2375`) at cohort startup — 0/7 cohort throw, reproduced and reverted in-commit; the file-local copies stay (bodies identical to the live exports, cycle-avoidance convention already documented in-file).
**Verify:** `node scripts/verify.mjs --fn corpse_xname` → PASS syntax (2 files) · rule2 · hidden note (no session blocked) · reach REACH-OK (no RNG tags, smoke 24/24) · green 2/2 · strict ×2 · cohort 7/7. VERIFY: PASS. (Mid-iteration TDZ cohort FAIL `Cannot access '_shk_owns_prefix' before initialization` reproduced the cycle-order fault and forced the clone-convention revert above; final tree re-verified green.)
**Named:** `releaseobuf`/`nextobuf`/`eos` buffer machinery (by-design strings); `obj_pmname` impossible-log + `two-legged glorkum-seeker` fallback (unreachable for CORPSE, map keeps it); dothrow.c:142 bare-hand throw arm (pre-existing D-1346 named omit, `js/dothrow.js:917`); eat.c:1913 eatcorpse `rotted` make_sick name (tainted-path make_sick deferred, `js/eat.js:2371`); wield.c:148 `cant_wield_corpse` (host unported, `js/wield.js:425,445` named); zap.c:971,1045 revive + :1177 unturn_dead corpse-name arms (JS `revive :3024` / `unturn_dead :3267` arms unchecked this iteration); trap.c:3925 hero `selftouch` corpse-name arm (JS `selftouch :3408` arm unchecked; `mselftouch :1209` inlines neutral pmname — follow-up candidate); pickup quan>1 petrify-name edge (`killer_xname` vs `cxname_singular`).
**Next:** next coverage row.
