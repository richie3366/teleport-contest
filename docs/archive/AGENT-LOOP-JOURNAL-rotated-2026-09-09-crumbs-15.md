# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-09 — D-2243 `steed.c` poly_steed steed-polymorph body + `newcham` wiring (queue row `steed.c` poly_steed, no corpus block)

**C locus:** `steed.c:851–873` (`poly_steed`: `if (!can_saddle(steed) || !can_ride(steed)) dismount_steed(DISMOUNT_FELL)` — with the can't-get-here comment crediting `newcham → mon_break_armor → m_lose_armor` — else `Strcpy(buf, x_monnam(steed, ARTICLE_YOUR, NULL, SUPPRESS_SADDLE, FALSE))`, `if (oldshape != steed->data) strsubst(buf, "your ", "your new ")`, `You("adjust yourself in the saddle on %s.", buf)`, `steed_vs_stealth()`); `mon.c:5517–5518` (`if (mtmp == u.usteed) poly_steed(mtmp, olddata)` — after the ex-giant boulder drop, before the Elbereth `mon_moving` re-check at `:5522–5532`); `hacklib.c:534–551` (`strsubst` first-occurrence in-place replace).
**JS:** 2 files (`js/steed.js` +27/−2: 2 import names + 24-line export; `js/makemon.js` +14/−8: 1 import name + `after_steed` + doc/marker updates), under the 600/10 caps. Density note: C is 19 lines + a 2-line call site; the diff is the complete portable behavior (all 10 C callees live, no stubs).
**Change:** `js/steed.js` — new `export async function poly_steed(steed, oldshape)` in C branch/short-circuit order (`!can_saddle || !can_ride` → `await dismount_steed(DISMOUNT_FELL)`; else `x_monnam(steed, ARTICLE_YOUR, null, SUPPRESS_SADDLE, false)` + `oldshape !== steed?.data` → `buf = strsubst(buf, 'your ', 'your new ')` (JS returns the new string; C edits in place) + `await pline('You adjust yourself in the saddle on ...')` (C `You()` idiom, same as the neighboring dismount plines) + `steed_vs_stealth()`). Async because both `dismount_steed` and `pline` await. Import names join pre-existing static edges only (`x_monnam`→do_name, `strsubst`→hacklib, `ARTICLE_YOUR`/`SUPPRESS_SADDLE`→const; `imports.mjs --can makemon.js steed.js poly_steed` → ALREADY, no new edge, no top-level reads).
**Verify:** `node scripts/verify.mjs --fn poly_steed` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit. Preflight `verify --no-cohort` was green before any edit on a clean tree. /tmp light probe (kept out of tree, deleted): `strsubst('your pony')` → `'your new pony'` OK, first-occurrence-only OK, no-match passthrough OK; static C-order checks (short-circuit, FELL arm, ARTICLE_YOUR/SUPPRESS_SADDLE call, `!==` shape gate, adjust pline, stealth tail, newcham gate + order) all OK. A full-game import probe was attempted and removed: direct `/tmp` import of `steed.js` hits a probe-order-only `_body_part` TDZ via the `objnam↔polyself` cycle that production boot order never takes (fortress 44/44 loads `steed.js` fine) — recorded here so the next iter does not re-try it.
**Named:** none new. `newcham`'s remaining omits stand (W_ARMG `mselftouch`, ustuck expels/unstuck with Hallu `l_oldname`, `set_apparxy` byyou arm).
**Next:** do not re-pop `poly_steed` (body live, sole C caller wired). A future steed-polymorph divergence attributes to the callee owners (`can_saddle` steeds[] gate, `x_monnam` saddle suppression, `dismount_steed` landing) or the `mon_break_armor` saddle-strip order, not this wiring. Do not invent a FAIL peel.

## 2026-09-09 — D-2242 `detect.c` object_detect spurious `--More--` before browse_map (queue row `getpos.c` getpos, scen-normal-Wizard-92127 step 72)

**C locus:** `detect.c:780–785` (`object_detect`: `You("detect the %s of %s.", ct ? "presence" : "absence", stuff);` then `if (!ct) display_nhwindow(WIN_MAP, TRUE); else browse_map(ter_typ, "object");` — no flush/`more()` between the sense pline and `browse_map`); `detect.c:106–118` (`browse_map`: save autodescribe, `iflags.autodescribe = TRUE`, `terrainmode = ter_typ`, `getpos(&dummy_pos, FALSE, ter_explain)`); `getpos.c:843–846` (`if (flags.verbose) { pline("(For instructions type a '%s')", visctrl(...)); msg_given = TRUE; }`). The verbose tip appends on the same topline via the `topl.c` NEED_MORE + room two-space join. Same shape as the shipped `monster_detect` arm (D-2081, e55af311); C `gold_detect` (`:471` + `browse_map`) and trap `display_trap_map` (`:1000` + `browse_map`) likewise have no flush between `You_feel` and `browse_map`.
**JS:** 1 file (`js/detect.js` +4/−1: 4 comment lines, 1 flush call), under the 600/10 caps. Density note: the C locus is the absence of one call between two live lines; the diff is the complete portable behavior (same size as the D-2081 precedent).
**Change:** `js/detect.js` — deleted the `flush_topl_more()` call between the detect pline and the `!ct`/`browse_map` branch and replaced it with the C cite (`detect.c:780` → `getpos.c:843–846`, two-space join, same-class-as-D-2081 note). No import change (`flush_topl_more` still used at :1400/:1980/:2357 — shared top-level edge untouched, no new module edge, no TDZ, no `imports.mjs --can` owed). Draw-free by construction (a display flush draws nothing), so no fortress RNG movement is possible.
**Verify:** `node scripts/verify.mjs --fn getpos` → PASS syntax (1 changed js file: js/detect.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-normal-Wizard-92127: getpos@72 → distfleeck@101, RNG 3042→3282, scr 72→104, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file per runner) · VERIFY: PASS, final verify after the last js/ edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** none new. Sibling `gold_detect` (:1980) and `display_trap_map` (:2357) `flush_topl_more()` calls keep the same spurious pattern but have no corpus block — left untouched for future rows, not silently fixed.
**Next:** do not re-pop `getpos` for this topline — the `getpos` verbose arm is innocent and already faithful (C `:843–846` vs `js/getpos.js:1214–1217`); a `getpos` body port guarantees NO MOVEMENT here. Live residual is `distfleeck(monmove.c:549)`@101 (C `rn2(5)=3` vs JS `rnl(20)=3 @ doopen_indir(lock.js:733)`, identical toplines) — a parked symptom owner (NOTES Active) — do not re-pop it and do not re-queue this row under `object_detect`. Do not "fix" with seed/step/coordinate/RNG-index gates.

## 2026-09-09 — D-2241 `invent.c` fully_identify_obj EGG `learn_egg_type` arm (queue row `timeout.c` learn_egg_type, no corpus block)

**C locus:** `invent.c:2636–2647` (`fully_identify_obj`: `makeknown`, oartifact `discover_artifact`, `observe_object`, known/bknown/rknown = 1, `set_cknown_lknown`, then `if (otmp->otyp == EGG && otmp->corpsenm != NON_PM) learn_egg_type(otmp->corpsenm)`); `timeout.c:1193–1200` (`learn_egg_type` body, already live `js/timeout.js:1893`, D-1036). The sibling C mentions are comments only: `uhitm.c:1205` and `zap.c:2270` are commented-out / question-form, nothing to port. The other two live C callers were already wired (sit `#sit` FALSE `js/sit.js:1128–1129`, D-1075; polyself TRUE pair `js/polyself.js:1193–1195`, D-2239; `hatch_egg` gate `js/timeout.js:2041`, D-1036).
**JS:** 1 file (`js/invent.js` +9/−3: 1 import line, arm + doc), under the 600/10 caps. Density note: C is 10 lines and the missing arm is 2; the diff is the complete portable behavior (callee + all sibling caller arms already live per D-1036/D-1075/D-2239).
**Change:** `js/invent.js` — `fully_identify_obj` gains the C tail arm in C position (after `set_cknown_lknown`, last in the body): `(otmp.otyp | 0) === EGG && (otmp.corpsenm | 0) !== NON_PM` (same `| 0` idiom as the neighboring `STATUE` arm; `EGG` const at :2728, `NON_PM` already imported at :232) then `learn_egg_type(otmp.corpsenm | 0)`. New static edge `invent.js → timeout.js` (`imports.mjs --can invent.js timeout.js learn_egg_type` → hoisted function, cycle-safe; mirrors the pre-existing reverse edge `timeout.js:48` `update_inventory`). Sync callee in a sync function — no missing await.
**Verify:** `node scripts/verify.mjs --fn learn_egg_type` → PASS syntax (1 changed js file: js/invent.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file per runner) · VERIFY: PASS. /tmp hand probe (kept out of tree): EGG/corpsenm=12 → `known=1`, `mvitals[12]` gains `MV_KNOWS_EGG`, no throw; ROCK/corpsenm=12 control → flags unchanged. Static cycle smoke: `import('./js/invent.js')` + call → ok (no TDZ).
**Named:** none new. turns.md:392 retires the `learn_egg_type` mention (pray-gift `discover_artifact` / save-rest artidisco stay named); `uhitm.c`/`zap.c` comment-only mentions need no map line.
**Next:** do not re-pop `learn_egg_type` caller wiring (all four C call sites live: hatch_egg, sit FALSE, polyself TRUE, fully_identify_obj). A future egg-knowledge divergence attributes to the `mvitals` save/restore or `doname` owners, not this wiring. Do not invent a FAIL peel.

## 2026-09-09 — D-2240 `hack.c` pooleffects full port: leave-water / steed / ceiling-hider / Wwalking arms (queue row `hack.c` pooleffects, no corpus block)

**C locus:** `hack.c:3233–3309` (`pooleffects`: leave-water `:3236–3265` — non-pool + Waterlevel air-bubble + `last_msg`, lava-leave via `hliquid("water")`, else `back_on_ground(FALSE)`; in-pool Waterlevel/Levitation/Flying/Wwalking pop-outs, else stay; `set_uinwater(0)` + `docrt()`/`vision_full_recalc` when `was_underwater`; enter `:3267–3307` — `!ustuck && !Levitation && !Flying && is_pool_or_lava`, floating-steed safe, `dismount_steed(Underwater ? DISMOUNT_FELL : DISMOUNT_GENERIC)` + air/water-level early FALSE + `check_special_room(FALSE)` TRUE, `Upolyd && ceiling_hider(&mons[umonnum]) && uundetected` stay-out, `is_lava ? lava_effects : (!Wwalking || is_waterwall) && (newspot || !uinwater || !(Swimming || Amphibious || Breathless)) ? drown`); `dbridge.c:37–43` (`is_waterwall`: `isok && IS_WATERWALL(typ)`); `dbridge.c:76–83` (`is_pool_or_lava` ≡ `is_pool || is_lava`); `mondata.h:43–45` (`ceiling_hider` macro); `youprop.h:260/266/272/276/279` (Wwalking / Swimming / Amphibious / Breathless / Underwater≡`u.uinwater`).
**JS:** 3 files (`js/pickup.js` +99/−21: 6 import lines + full body; `js/dbridge.js` +9/−1: import name + 6-line export; `js/mon.js` +3/−1: export + canonical note), under the 600/10 caps. Density note: C is 75 + 7 lines; the diff is the complete portable behavior (every callee live: `drown`/`lava_effects` D-0357, `back_on_ground`, `set_uinwater` D-1267, `dismount_steed`, `check_special_room`).
**Change:** `js/pickup.js` — full C-order port: Wwalking/Swimming/Amphibious/Breathless computed once from the same-file `pickup_checks` uprops+H/E-flat idiom (+ `is_swimmer(usteed.data)` steed disjunct per C `:266`); leave-water block in C branch order (`You pop into an air bubble.` + `last_msg` set, lava-leave, `back_on_ground(false)`, pop-outs, `set_uinwater(0)` + `docrt()` + `vision_full_recalc = 1`); enter block with floating-steed FALSE, `dismount_steed(uinwater ? FELL : GENERIC)` + air/water early FALSE + `check_special_room(false)` TRUE, `Upolyd(u) && ceiling_hider(mons(u.umonnum)) && u.uundetected` stay-out, and the full C drown gate. `is_pool_or_lava` kept inline as `is_pool||is_lava` (proven identical C body — no 4th clone).
**Verify:** `node scripts/verify.mjs --fn pooleffects` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; plus `node frozen/ps_test_runner.mjs sessions` → **44/44** (RNG + screens exact, speed `43+0.30/turn` R² 0.83). No maintained test added: the repo has no unit harness for `js/` logic (sessions are the suite) and no public session stages a leave-water/steed-dismount/ceiling-hider step, so those arms are covered by the C-body match plus live-callee evidence, not a probe.
**Named:** none new. The turns.md:1094 `wizterrainwish` drawbridge/lava terrain arms (the `objnam.c:3660` `pooleffects(FALSE)` caller) stay named — different C function, future row.
**Next:** do not re-pop `pooleffects` (every portable arm live). A future pool/lava-entry divergence attributes to the callee owners (`drown`, `lava_effects`, `dismount_steed`), not this wiring. Do not invent a FAIL peel.

## 2026-09-09 — D-2239 `polyself.c` newman egg-knowledge arms: `learn_egg_type` + `egg_type_from_parent(u.umonnum, TRUE)` (queue row `mon.c` egg_type_from_parent, no corpus block)

**C locus:** `polyself.c:905–911` (`newman`: after `newsym(u.ux,u.uy)`, `if (lays_eggs(gy.youmonst.data)) { learn_egg_type(u.umonnum); learn_egg_type(egg_type_from_parent(u.umonnum, TRUE)); }` — deliberately placed before the `u.uswallow`/`expels` block in case `expels()` → `spoteffects()` drops the hero onto eggs); `mon.c:5569–5580` (`egg_type_from_parent`: `force_ordinary || !BREEDER_EGG` with `BREEDER_EGG (!rn2(77))`, queen bee → killer bee, winged gargoyle → gargoyle); `timeout.c:1193–1200` (`learn_egg_type`: `little_to_big`, `mvitals[mnum].mvflags |= MV_KNOWS_EGG`, `update_inventory()`); `sit.c:383–388` (`lay_an_egg` FALSE arm, already live).
**JS:** 1 file (`js/polyself.js` +11/−2: 2 import names, 9-line arm + doc), under the 600/10 caps. Density note: C is 7 lines; the diff is the complete portable behavior (both callees and the sibling sit arm were already live).
**Change:** `js/polyself.js` — `polymon` gains the C block in C position (immediately after `newsym`, before the deferred `u.uswallow`/`u.ustuck`/`u.usteed` arms): `lays_eggs(game.youmonst?.data)` gate (same `uptr` idiom as the live `#sit` verbose tip at :1248), `learn_egg_type(u.umonnum | 0)` then `learn_egg_type(egg_type_from_parent(u.umonnum | 0, true))` with the C queen-bee comment. Both names join pre-existing static edges (`imports.mjs --can polyself.js mon.js egg_type_from_parent` → ALREADY; `--can polyself.js timeout.js learn_egg_type` → ALREADY — no new edges, no top-level reads). Draw-free by construction: `TRUE` short-circuits the `rn2(77)` roll (`force_ordinary || rn2(77)`), and `learn_egg_type` draws nothing — so no fortress RNG movement is possible.
**Verify:** `node scripts/verify.mjs --fn egg_type_from_parent` → PASS syntax (1 changed js file: js/polyself.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file per runner) · VERIFY: PASS. /tmp hand probe (kept out of tree): seeded `initRng(12345)` — TRUE arm maps queen→killer-bee and winged-gargoyle→gargoyle 400/400 with 0 RNG draws; FALSE arm keeps the queen 9/770 (≈1/77, roll provably runs); `learn_egg_type(KILLER_BEE)` sets `mvflags=8` (MV_KNOWS_EGG). No maintained test added: the repo has no unit harness for `js/` logic (sessions are the suite) and no session stages a poly-into-egg-layer step, so the probe plus the draw-free argument is the honest coverage.
**Named:** none new. `newman`'s `u.uswallow`/`expels`, `u.ustuck`/`sticking`, `u.usteed`, `spoteffects(TRUE)`, `Passes_walls`, `amorphous`/whirly/unsolid arms stay deferred per the existing in-code comments.
**Next:** do not re-pop `egg_type_from_parent` / `learn_egg_type` caller wiring (both C callers live: sit FALSE, polyself TRUE). A future egg-knowledge divergence attributes to the `mvitals` save/restore or `hatch_egg` owners, not this wiring. Do not invent a FAIL peel.

## 2026-09-09 — D-2238 `pray.c` prayer_done p_type −2/−1 arms + pray_revive (queue row `pray.c` pray_revive, no corpus block)

**C locus:** `pray.c:2177–2195` (`pray_revive`: scan `svl.level.objects[u.ux][u.uy]` for the first CORPSE/STATUE with `has_omonst` + `OMONST->mtame && !isminion`; CORPSE → `revive(otmp, TRUE)`, STATUE → `animate_statue(otmp, u.ux, u.uy, ANIMATE_SPELL, NULL)`); `pray.c:2283–2295` (p_type −2: `You("%s diabolical laughter…", !Deaf ? "hear" : "intuit")`, `wake_nearby(FALSE)`, `adjalign(-2)`, `exercise(A_WIS, FALSE)`, non-Inhell `pline("Nothing else happens.")` + return 1, else fall through to the Gehennom gate); `pray.c:2296–2305` (p_type −1: lawful/neutral `godvoice`, `You_feel("like you are falling apart.")`, `rehumanize()`, `losehp(rnd(20), "residual undead turning effect", KILLED_BY_AN)`, `exercise(A_CON, FALSE)`, return 1); `pray.c:2336–2340` (coaligned: `on_altar()` → `pray_revive()` + `water_prayer(TRUE)`, then `pleased(alignment)`).
**JS:** 1 file (`js/pray.js` +70/−13: 6 import lines incl. the zap edge, `STATUE` const, 19-line `pray_revive`, −2/−1 arms, coaligned wiring, header + `prayer_done` doc updates), under the 600/10 caps. Density note: C is 18 + 23 + 5 lines across three sites; the diff is the complete portable behavior.
**Change:** `js/pray.js` — new module-local `async function pray_revive()` (C staticfn: same-file caller only) in C order: `objects_at(u.ux,u.uy)` `nexthere` walk (same idiom as the live `water_prayer`), CORPSE/STATUE + `has_omonst` + `OMONST?.mtame && !isminion` first-match break, `revive(found, true)` / `animate_statue(found, u.ux, u.uy, ANIMATE_SPELL, null)` with `!== null` → boolean. `prayer_done` gains the −2 arm in C position (Deaf-aware `pline('You hear/intuit …')` via the `altar_wrath` Deaf idiom, `await wake_nearby(false)`, `adjalign(-2)`, `exercise(A_WIS, false)`, `!Inhell()` → `pline('Nothing else happens.')` + return 1, else fall-through to the live Gehennom gate) and the −1 arm (`godvoice` lawful/neutral strings, `You_feel`, `await rehumanize()`, `losehp(rnd(20), …, KILLED_BY_AN)` + `await finish_maybe_wail()` per the house losehp contract, `exercise(A_CON, false)`); coaligned arm awaits `pray_revive()` before `water_prayer(true)` in C order. Imports extend pre-existing edges only (`wake_nearby`→mon, `animate_statue`→trap, `A_CON`→attrib, `losehp`/`finish_maybe_wail`→hack, `OMONST`/`ANIMATE_SPELL`/`KILLED_BY_AN`→const, `STATUE` objectNames const) plus one new static edge `revive`→zap (`imports.mjs --can pray.js zap.js revive` → IN-SCC cycle-safe, hoisted function, runtime-only use — no top-level reads).
**Verify:** `node scripts/verify.mjs --fn pray_revive` → PASS syntax (1 changed js file: js/pray.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file per runner) · VERIFY: PASS. /tmp hand probe (kept out of tree): seeded `initRng(1234)` + p_type −2 non-Inhell `prayer_done()` → returned 1 with `uinvulnerable` false, no throw (PROBE-PASS). The −1 arm (rehumanize→polyman needs full hero state) and `pray_revive` (module-local; reachable only via altar + tame-corpse state no session stages) are covered by the C-body match plus live-callee evidence, not a probe.
**Named:** none new. p_type 1/2 arms were already live (verified against C, untouched); `Deaf` keeps the house `u.Deaf ||` disjunct alongside C's `HDeaf || EDeaf || uroleplay.deaf` (same idiom as `altar_wrath`).
**Next:** do not re-pop `pray_revive`/`prayer_done` p_types (every portable arm live). A future prayer-outcome divergence attributes to the callee owners (`revive`, `animate_statue`, `pleased`, `water_prayer`), not this wiring. Do not invent a FAIL peel.
