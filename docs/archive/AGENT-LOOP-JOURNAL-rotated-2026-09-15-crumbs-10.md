# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-15 — D-2344 `priest.c` angry_priest temple-priest anger + `free_epri` (turns.md:399 dosacrifice family)

**C locus:** `nethack-c/upstream/src/priest.c` `angry_priest` `:876–911` (`findpriest(temple_occupied(u.urooms))`, `wakeup(priest, FALSE)` + `setmangry(priest, FALSE)`, shrpos-cell `!IS_ALTAR || Amask2align(altarmask & AM_MASK) != shralign` → `newemin`-if-missing, `ispriest=0`/`isminion=1`, `min_align=shralign`, `renegade=FALSE`, `free_epri`) + `free_epri` `:28–37` (free epri slot, `ispriest=0`); callers `dig.c:427–430` (pickaxe altar dig, after `altar_wrath`), `pray.c:1683–1686` (conversion-glow priest check), `:1722` (stain arm), `:1736` (vanish arm).
**JS:** 3 js files (`priest.js` +56/−3 incl. C-cited docs, `pray.js` +8/−9 wiring + comment retirements, `dig.js` +6/−3 altar arm; +70/−15 total), far under the 600/10 caps.
**Change:** `js/priest.js` — exported `free_epri` (GC replaces `free()`: null the `mextra.epri` slot, then `ispriest=0` like C) and exported async `angry_priest` in C order (`findpriest(temple_occupied(...))` early-return, `await wakeup(priest, false)` + `await setmangry(priest, false)`, shrpos-cell read, `!IS_ALTAR || Amask2align(mask & AM_MASK) != shralign` conversion arm with `newemin`-if-missing (object identity kept when emin pre-exists), `isminion=1`, `min_align` copied from the pre-`free_epri` epri ref (C order: fields before free), `renegade=false` (C FALSE; all JS readers are truthiness checks), `free_epri`). The altar check deliberately does NOT strip `AM_SHRINE` (C masks only `AM_MASK` here, unlike `has_shrine`'s `~AM_SHRINE`). `js/pray.js` — all three sites wired (`await angry_priest()` in stain arm, vanish arm, and the `pri && !p_coaligned(pri)` conversion check).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_angry.mjs` (no corpus session reaches any arm, so no corpus falsifier exists; kept in /tmp, out of tree) → 23/23 PASS: silent no-priest return; intact shrine stays tended (epri kept, no emin, hostile+woken); destroyed altar releases roaming minion (ispriest 0, isminion 1, epri freed, emin min_align=old shralign, renegade false); converted altar same + pre-existing emin object reused with fields reset; `free_epri` unit incl. no-mextra safety. `node scripts/verify.mjs --fn angry_priest` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (forced `--full`) · VERIFY: PASS.
**Named:** none new — `altar_wrath` was already live (`pray.js`, used by dokick/eat); remaining `dig()` omits (earth-elemental debris, drawbridge wall string, steed fumble) stay named in `dig.js`.
**Next:** next Open row `pray.c` offer_real_amulet/offer_fake_amulet amulet offering arms.

## 2026-09-15 — D-2343 `pray.c` offer_different_alignment_altar altar-wrath offering + `attrib.c` uchangealign (turns.md:399 dosacrifice family)

**C locus:** `nethack-c/upstream/src/pray.c` `offer_different_alignment_altar` `:1630–1695` (conversion test `ugod_is_angry() || (altaralign == A_NONE && Inhell)`, convert arm `:1640–1650`, reject arm `:1651–1660`, consume+conflict `:1662–1694` with `rn2(8+ulevel)>5` glow vs decrease), dispatched from `offer_corpse` `:2025–2028` (`u.ualign.type != altaralign`); callee `attrib.c` `uchangealign` `:1319–1362` (CONVERT `:1331–1342`, helm on/off `:1343–1359`, record-wipe tail `:1360–1362`).
**JS:** 2 js files (`attrib.js` +78/−1, `pray.js` +112/−9; +190/−10 total), far under the 600/10 caps. One new cross-module edge (pray→priest); rest join existing edges.
**Change:** `js/attrib.js` — exported `uchangealign` (1:1 with C `attrib.c`): ublessed=0 + `game.flags.botl` (pray.js-guarded idiom), CONVERT livelog `permanently converted to <adj>` via `aligns[1-newalign]` (roles.js order matches C role.c:697–700) + `ualignbase.current` write (JS `{current,original}` object, u_init.js:1963 shape) + helm-block check + sudden/new-direction pline; helm-on `adjalign(-7)` + oscillate pline + `make_confused(rn1(2,3))` + astral/abuse gate + helm livelog; helm-off pline; record-wipe tail. `js/pray.js` — local `offer_different_alignment_altar` in C order (convert: strong-feeling + consume + allegiance-accepted + `uchangealign(CONVERT)` + luck−3 + blesscnt+300; reject: ugangr+3/adjalign−5/rejects-pline/`godvoice(altaralign,'Suffer, infidel!')`/luck−5/`adjattrib(WIS,−2)`/`angrygods`-if-!Inhell; else consume + conflict-sense + `rn2(8+ulevel)>5` glow arm — `You_feel` increase, luck+1, `Align2amask` + shrine-bit + `newsym`, Blind-gated `The altar glows <hcolor white/black/gray>` (decl.h NH_WHITE/NH_BLACK are c_color_names "white"/"black", artifact.js:147 precedent; C `type?black:gray` gives neutral gray, matched), `rnl(ulevel)>6 && record>0 && rnd(record)>3*ALIGNLIM()/4` summon + priest check — vs decrease arm with luck−1 and the 7/8 summon gate), wired into `offer_corpse` replacing the bare return. Imports: `uchangealign` + `Align2amask`/`A_CG_CONVERT` join existing attrib/const edges (ALREADY); `findpriest`/`temple_occupied`/`p_coaligned` new pray→priest edge (`imports.mjs --can`: SAFE, hoisted declarations); `aligns`/`make_confused`/`livelog_printf`/`Hallucination` join attrib's roles/potion/pline/display edges (all SAFE, no cycles); no `You`/`pline_The` clones (pline literals per pray.js idiom + D-1678 review).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_altar.mjs` (uchangealign: convert type/base/record/ublessed/RNG-silence, helm-block keeps type+record but sets base, helm-on abuse+7 + wipe, helm-off wipe — the pray.js local is unreached by any session so no corpus falsifier exists) → 13/13 PASS after (kept in /tmp, out of tree). `node scripts/verify.mjs --fn offer_different_alignment_altar` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 · VERIFY: PASS.
**Named:** `angry_priest` (C priest.c:876–911) at the conversion-glow priest check — own next Open queue row, not this cluster (same precedent as D-1678's sacrifice_your_race arms); `summon_furies` (C makemon.c:2605, helm-on astral/abuse arm) + `retouch_equipment` (C artifact.c:2639, align-change tail) inside `uchangealign` — no live JS counterparts; helm caller wiring in do_wear stays deferred (turns.md:1070 notes `uchangealign` live); `bestow_artifact` / amulet-offer rows untouched.
**Next:** do not re-pop offer_different_alignment_altar/uchangealign for these arms. Falsifier: a fresh `verify offer_different_alignment_altar` showing a session blocked with it as owner, or a foreign-altar sacrifice trace contradicting the C order above. No seed/step/coordinate gates.

## 2026-09-15 — D-2342 `sp_lev.c` spo_end_moninvent m_dowear for Kni-strt King Arthur + Mon-strt Grand Master (D-2335 follow-up)

**C locus:** `nethack-c/upstream/src/sp_lev.c` `spo_end_moninvent` `:3031–3036` (`m_dowear(invent_carrying_monster, TRUE)`, then clears to NULL), dispatched from `lspo_monster` `:3390–3393` after the lua invent function runs (mechanism verified in review 1301: `create_monster` sets `invent_carrying_monster` on CUSTOM_INVENT `:2183–2185`); `m_dowear` body `worn.c:757–796` already live in `js/worn.js:978`, no re-port.
**JS:** 1 file (`mklev.js`, +11/−5: 2×3-line call sites + 2 docstring lines), far under the 600/10 caps. Small because the C delta is two one-line `spo_end_moninvent` applications of an already-ported 39-line locus (density note, said plainly — same class as D-2335/review-1301-ACCEPT).
**Change:** `js/mklev.js` only, zero new module edges (`imports.mjs --can mklev.js worn.js m_dowear` → ALREADY: file already statically imports worn.js). One `m_dowear(mtmp, true)` after each invent loop, in C position (after invent, before chest/abbot spawns), C-cited. Sync-through without await: the creation path takes no awaits (all pline/autocurse/artifact-light arms are gated on `!creation`), is RNG-free and message-free, and nothing later in the same sync stacks reads the leaders' worn state — same shape as the D-2335 sites and the `makemon.js` precedent.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn m_dowear` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** other loaders' `spo_end_moninvent m_dowear` Named-omission lines (Kni-loca/fila/filb, Mon-loca/goal/fila/filb, etc.) stay: those levels spawn no CUSTOM_INVENT monster, so `spo_end_moninvent` is a no-op there. humidity-aware `get_location`, light-region wall expansion, `ensure_way_out`/`map_cleanup` stay named per loader.
**Next:** do not re-pop m_dowear / spo_end_moninvent for Kni-strt King Arthur or Mon-strt Grand Master (data.md:786 retires to this D-id alongside D-2335). Falsifier: a quest-start spawn with leader armor loose in minvent (wornmask unset) where C wears it, or a fresh `verify m_dowear` showing a session blocked with m_dowear/spo_end_moninvent as owner. No seed/step/coordinate gates.

## 2026-09-15 — D-2341 death-disclosure overview epitaph writers behind misattributed `save_dungeon` (imitator / priest-minion / helpless / Sokoban ureached)

**C locus:** `end.c` `done_in_by` `:184-344` — imitator arm `:230-255` (`imitator = mptr != champtr || mimicker`; realnm=champtr, shape article rules pname-bare / unique-"the" / else `an()`, `"%s imitating %s"` / vampshifter `"in %s form"` / mimicker `"disguised as %s"`), ispriest/isminion `m_monnam` arm `:262-267`, killer multi trim `:288-316` (`multireasonbuf` "m_id:reason", truncate at first space on m_id match); `topten.c` `formatkiller` `:89-162` — helpless arm `:145-155` (`incl_helpless && multi<0`: `", while <reason>"` else `", while helpless"`, NUL-counted siz guards); `uhitm.c` `dynamic_multi_reason` `:104-124` (`"m_id:verb by <mon>"`, `x_monnam ARTICLE_A` + IT/INVISIBLE/HALLUCINATION/SADDLE/NAME, gaze `s_suffix+" gaze"`) + call sites `:3459` (mhitu paralyze), `:6045` (gaze frozen), `:6062` (cube frozen); `do.c` `goto_level` `:1678-1684` (non-builds_up max-track, builds_up `==0 || dl<reached` min-track). `monhealthdescr` (`pager.c:139`) is exact-omit: body `#if 0`'d to `*outbuf='\0'`.
**JS:** 4 js files (`end.js` +72/−21, `uhitm.js` +22/−6, `mhitu.js` +5/−5, `do.js` +8/−5; ≈+107/−37), far under the 600/10 caps. No new modules; zero new cross-module edges (all ALREADY).
**Change:** `js/end.js` — 5 names join existing imports (no new edges: `m_monnam`, `is_vampshifter`, `strstri`, `the_unique_pm`, `BUFSZ`; all `imports.mjs --can` ALREADY); imitator arm in C order incl. mimicker `mons(mappearance)` + vampshifter bat-form + `mptr=mtmp->data` equivalence noted (JS arise already reads `mtmp.data`); ispriest/isminion `m_monnam` arm; m_id trim with tail-identity check (string analogue of C's pointer-range check) truncating both `multireasonbuf` and `multi_reason`; helpless arm with BUFSZ-remaining guards (8/17 with NUL). `js/uhitm.js` — exported `dynamic_multi_reason` (x_monnam+s_suffix already imported; SUPPRESS_HALLUCINATION joins existing const edge) + gaze/cube call sites in C position. `js/mhitu.js` — `dynamic_multi_reason` joins existing uhitm edge; static string replaced.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Diagnosis probes `/tmp/probe_overview.mjs` (grids), `/tmp/probe_all8.mjs` (all 8 diffs — grouped 4 writers), `/tmp/probe_killer.mjs` (post-death cham/isminion state — deleted before finishing). `node scripts/verify.mjs --fn save_dungeon` → PASS syntax (4 changed js files) · PASS rule2 · PASS hidden (7 PASS: 91118/92140/92119/92050/92005/92063/92098; 92075 moved 78→80 owner-null single-cell `^F`-map glyph, error null — the verify line's "js-throw" label is its null-owner print, NOT a throw: direct replay runs clean, 141 screens, and `hidden-proxy show` reports error null with a 1-cell row-10 screen diff) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) · VERIFY: PASS.
**Named:** `done_in_by` ghost arms (no corpus reach); vampshifter-alt + mimicker sub-arms (ported, unreached); gaze/frozen `dynamic_multi_reason` sites (ported, unreached); `uhitm` gaze-site `nomovemsg=0` (display-only, unreached); turns.md:1745 ghost clause stays, imitator/priest/trim clauses retire to this D-id; `goto_level` min-track noted at turns.md:307.
**Next:** do not re-pop save_dungeon/done_in_by/formatkiller/dynamic_multi_reason/dunlev for these arms. Rescore notes (all fail→pass or fail→later-fail; zero was-PASS→fail): baseline pre-edit rescore on the clean tree already flipped 2 unrelated sessions to PASS (random-seed0015-eb7e90ad `-`-command flake, scen-genesis-Tourist-92044 do_statusline2@106 — environment flakes, not this port); post-fix rescore surfaced 2 pre-existing RNG diffs from behind cleared transients (Priest-92163 hmonas@193 More-transient → live queue row mhitm_knockback@248 whose index-3744 mismatch the hmonas park already documented; Valkyrie-92162 None@70 → m_move@72, already in that row's session list) — neither is reachable from this port (zero new RNG draws; death-text/overview-listing only). 92075's step-80 owner-null single cell is its own future row on rescore. Queue refill re-measured exhausted (all 24 fresh tops parked/archived; TOP30 remainder likewise) — queue drops to 6 Open, no filler invented (D-2337 precedent). No seed/step/coordinate gates.

## 2026-09-15 — Audit ce42f088..09146ec5 (reviews 1298–1306: 9 ACCEPT, 0 Must-fix) + cadence 44/44

Review-only iteration, no `js/` edits. Every JS-touching SHA since audit 1292–1297 re-audited against pinned C with `csym.mjs` + `--callers`, `sym.mjs` on every deleted/re-pointed symbol, `--can`/`--rulecheck`, added-lines banned grep, and an independent `hidden-proxy verify <fn> --base HASH~1` re-run (all 9 confirm 0 blocked; all rows cited 0 so no deeper `--base` owed). Notable depth: 1301 proves D-2335's un-awaited `m_dowear` converges (all 7 `m_dowear_type` awaits sit under `!creation`; creation path RNG/message-free; same shape as `makemon.js:3472`); 1302 proves D-2336's `delarea −1` ≡ C `l_get_lregion` default and placement now rides C's own fixup slot (`mkmaze.c:585-646`); 1303 proves D-2337's split eligible/fallback arrays ≡ C shared-array `n`/`altn` and `oname` subsumes the dropped `artifact_exists`; 1306 traces the `Stone_resistance` triple-OR to C's H||E (flat field has zero writes). Prior QUALITY-RISKs closed: 1289 surface nouns by D-2327, 1291 cloak-90 by D-2326 — Must-fix drained, 0 outstanding. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.28/turn` (R² 0.78). CURRENT Next cluster advanced past shipped D-2340 to queue head `worn.c` mon_adjust_speed — flag: NOTES.md symptom-owner-parks line still lists it while the Open row says never-parked; port iter re-measures via `verify mon_adjust_speed` before trusting either.

## 2026-09-15 — D-2340 `mon.c` hideunder concealer arms: can_hide_under_obj coins + pet cursed + cockatrice skip (data.md:462, D-0630 residual)

**C locus:** `nethack-c/upstream/src/mon.c` `hideunder` `:4725–4802`, concealer arm `:4752–4772` (`hides_under && objects && can_hide_under_obj && (!mtame || !cursed_object_at) && !is_pool_or_lava`, `seenobj` from the pre-walk pile head `:4763–4764`, cockatrice walk gated on `is_u ? !Stone_resistance : !resists_ston` `:4767–4770`, `undetected` only if post-walk `otmp` non-null `:4771–4772`); `monmove.c` `can_hide_under_obj` `:2121–2167` (10-coin threshold over the `nexthere` chain); `dogmove.c` `cursed_object_at` `:145–153` (any `cursed` on the pile); `makemon.c` `:1307–1313` (S_SPIDER/S_SNAKE births call the real `hideunder`, not an inline).
**JS:** 4 js files (`dogmove.js` +1/−1, `makemon.js` +19/−7, `mon.js` +23/−7, `monmove.js` +18/−8; +61/−23 total), far under the 600/10 caps. C arms are ~12 lines × 3 sites, so the residual is dense by construction.
**Change:** 4 js files (+61/−23), no new modules. `dogmove.js`: 1-word `export` on `cursed_object_at` (body already exact vs C). `monmove.js`: 1-word `export` on `can_hide_under_obj`; `cursed_object_at` joins the existing dogmove import (`imports.mjs --can` ALREADY — no new edge); local `hideunder` gains the pet gate + cockatrice walk in C order (seenobj pre-walk, `!resists_ston` walk, post-walk null check).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_hideunder.mjs` (10 cases over the primary `hideunder`: plain-hide, pet-cursed-refuse, wild-cursed-hide, cockatrice-only-refuse, cockatrice-mixed-hide, stoneres-hide, 9-coin-refuse, 10-coin-hide, hero-refuse, hero-stoneres-hide) → 4 FAIL before (pet-cursed, cockatrice-only, 9-coin, hero-cockatrice all wrongly hid), 10/10 after; deleted before finishing. `node scripts/verify.mjs --fn hideunder` → PASS syntax (4 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) · VERIFY: PASS.
**Named:** `You_see "%s %s under %s"` pline + `set_msg_xy` / `PLNMSG_HIDE_UNDER` / `last_hider` (async boundary; both `hideunder` locals stay silent — own future row); data.md:462 retires to this D-id; turns.md:2777/2821/3683 pet/cockatrice/`can_hide_under_obj` clauses retire with it (their `You_see` clauses stay).
**Next:** do not re-pop can_hide_under_obj/hideunder/cursed_object_at for coins/pet-cursed/cockatrice. Falsifier: a fresh `verify hideunder` showing a session blocked with hideunder as owner, or a hide-decision trace contradicting the C order above. No seed/step/coordinate gates.
