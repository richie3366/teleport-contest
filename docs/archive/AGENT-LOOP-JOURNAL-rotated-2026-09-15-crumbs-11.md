# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-15 — D-2349 `apply.c` use_grease envelope: shk_owns shop prefix + suffix-clone retirements (review 617 named debts)

**C locus:** `nethack-c/upstream/src/shk.c` `shk_your` `:5862–5877` (`!shk_owns && !mon_owns` order) + `shk_owns` `:5885–5898` (`get_obj_location(obj,&x,&y,0)`, `unpaid || (OBJ_FLOOR && !no_charge && costly_spot)`, `shop_keeper(inside_shop)` → `s_suffix(shkname)` else `the_your[0]`); `objnam.c` `Tobjnam` `:2289–2299` (`The(xname)` + `otense`), `otense` (`is_plural` → verb else `vtense`, incl. Eyes-of-the-Overworld arm), `yname` `:2359` (shk_your prefix); `do_wear.c` `fingers_or_gloves` `:59–65`; `apply.c` `use_grease` `:2603–2654` (verified live, untouched).
**JS:** 3 js files (+41/−35), no new modules, no new module edges.
**Change:** `js/shk.js` — exported `shk_owns_prefix` (C `shk_owns` order, canonical `timeout.js` `get_obj_location(obj, 0)` via existing import, local `costly_spot`/`inside_shop`/`shop_keeper`/`s_suffix`/`shkname`; the file-local pricing `get_obj_location` subset left untouched). `js/objnam.js` — `shk_your` tries the helper before mon_owns, like C; registration is late-bound (`set_shk_owns_prefix`, the `_y_monnam` idiom) — a first attempt with a static objnam→shk import crashed the cohort 0/7 (`_body_part` TDZ via polyself's top-level `set_body_part`), so the edge was reverted. `js/apply.js` — all 14 clone call sites repointed to the canonical `fingers_or_gloves` (do_wear.js, joins existing import), `Tobjnam`, `otense` (objnam.js, already imported); the four defs deleted.
**Verify:** first `node scripts/verify.mjs --fn use_grease` → cohort 0/7 TDZ throw (reported, fixed via late-bind, re-ran once) → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; the row cited 0 corpus blocks so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. `/tmp/probe_owns.mjs` smoke (plain invent → null, unpaid invent shopless → "the " per C `the_your[0]`, floor goods → null, `shk_your` end-to-end → "your ") PROBE PASS; probe deleted. No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite; no public or corpus session names an unpaid item.
**Named:** `Yname2_oil`/`s_suffix_apply`/`plur_quan`/`otense_stone` suffix families (use_stone/use_lamp arms — own rows); `timeout.js` local `Shk_Your` subset (no shk_owns arm); `shk.js` local `get_obj_location` pricing subset kept as-is. Refill vetting notes (not rows): `use_royal_jelly` proved shipped (D-2331, turns.md:2016); `getobj_apply`/`passtune` have no C definition in this tree — never enqueue.
**Next:** do not re-pop `use_grease`. Falsifier: a fresh `verify use_grease` blocking, or an unpaid-shop naming trace contradicting the `shk.c:5885-5898` order above. No seed/step/coordinate gates.

## 2026-09-15 — D-2348 `end.c` done_in_by imitator predicate: permonst index compare (review 1307 Must-fix)

**C locus:** `nethack-c/upstream/src/end.c` `done_in_by` `:184-190` (`mptr = mtmp->data`, `champtr = ismnum(cham) ? &mons[cham] : mptr`, `imitator = (mptr != champtr || mimicker)` — permonst pointer compare); birth state `makemon.c:1355-1359` (`cham = pm_to_cham(mndx)`) with `pm_to_cham` returning `mndx` itself (`mon.c:535-546`), so C reads `mptr == champtr`, imitator false.
**JS:** 1 js file (+6/−1), no new modules, no new module edges.
**Change:** `js/end.js` only: `mptrNdx = mptr?.mndx ?? mnum`, `chamNdx = ismnum(cham) ? cham : mptrNdx`, `imitator = mptrNdx !== chamNdx || mimicker` with C citations (`end.c:184-190`, `makemon.c:1355-1359`, `mon.c:535-546`). Null-safe (both-undefined reads equal, matching old `null !== null` false); data-`mndx` preferred over `mnum` (C compares the data pointer); `champtr` object still feeds `realnm`. Matches the existing `makemon.js:1061`/`1196` index-compare idiom.
**Verify:** `/tmp/probe-imitator.mjs` → pre-fix FAIL observed (`mptr !== champtr` present; true-form old=true/new=false, shifted old=true/new=true), post-fix PASS (true-form non-imitator, shifted imitator, source uses `mptrNdx !== chamNdx`); probe deleted. `node scripts/verify.mjs --fn done_in_by` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited a review, not corpus blocks, so no `--base` owed; `brief done_in_by` likewise lists none) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS.
**Named:** none new — ghost arms stay named per D-2341. No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite (disclosed per durable-test-collateral); no public or corpus session reaches a true-form-shifter killer epitaph.
**Next:** do not re-pop `done_in_by` imitator predicate. Falsifier: a true-form-shifter killer epitaph trace contradicting the C order above. No seed/step/coordinate gates.

## 2026-09-15 — Audit c22b911d..e2d16801 (reviews 1307-1313: 6 ACCEPT, 1 QUALITY-RISK + Must-fix) + cadence 44/44 + proxy 478/540

Each JS-touching SHA re-audited against pinned C (csym body+callers, sym on deleted/re-pointed symbols, imports --can/--rulecheck, banned grep, independent hidden-proxy verify --base re-run). 1307 QUALITY-RISK: D-2341's new `done_in_by` imitator arm gates on `mptr !== champtr` (object identity) where C compares permonst pointers — `mons()` is a fresh-object factory and birth-state shifters carry `cham == mndx` (`makemon.c:1355-1359`), so true-form shifters print "X imitating a X" and lose G_UNIQ "the"; one-line Must-fix (compare `mndx`), Next cluster. 1308-1313 ACCEPT (m_dowear sync-through proven: zero awaits fire on creation path; uchangealign/angry_priest/amulet arms branch-exact; do_break_wand 7 residuals C-verbatim; magic_negation re-run PROGRESS Priest-92163 PASS). D-log imprecisions noted in the safe direction only (--can ALREADY ×4, "8 with NUL"). Cadence: full sessions 44/44, Scr 11405/11405, RNG 792838/792838, speed 48+0.30/turn. Proxy rescore 478/553 (88.5% excl env): +1 vs HEAD board, scen-wish-Knight-92072 (owner-null 1-screen at HEAD, 4/4 PASS incl. 3 direct replays — stale capture, fixer unidentified in the D-2341..D-2347 window); board committed. Queue at 13 unchecked (1 Must-fix + 12 Open) — no refill. Note for next port iter: `offer_too_soon` Open row is now stale (live since D-2345, park candidate per its D-log).

## 2026-09-15 — D-2347 `mhitu.c` magic_negation hero arm: amulet-of-guarding / extrinsic Protection / intrinsic floor (queue owner `mhitm_knockback` was the symptom)

**C locus:** `nethack-c/upstream/src/mhitu.c` `magic_negation` hero arm (`:1087–1137`): `gotprot = (EProtection != 0L)`; `mc` = max worn `objects[].a_can` (`objclass.h:103` `#define a_can oc_oc2`); `W_AMUL` → `via_amul = (otyp == AMULET_OF_GUARDING)`; hero `if (is_you || gotprot) continue` skips the `protects()` scan; `gotprot` → `mc += via_amul ? 2 : 1`, cap 3; else `mc < 1` with `(HProtection && u.ublessed > 0) || u.uspellprot` → `mc = 1`. With `rn2(10)=6`, C negated=TRUE ⇒ armpro 3 (cloak of MR a_can=1 + ring-of-protection EProtection + worn amulet of guarding +2); `negated = !(rn2(10) >= 3*armpro)` (`uhitm.c:75–99` `mhitm_mgc_atk_negated`, already exact in JS).
**JS:** 2 js files, no new modules; one new edge invent→mhitm inside the existing 90-module SCC (clone count −1).
**Change:** `js/mhitm.js` `magic_negation_you` exported with the full C hero arm in C order (gotprot from `u.EProtection` flat mirror or `uprops[PROTECTION].extrinsic` — eat.js:746 idiom; `AMULET_OF_GUARDING` via the `objectNames.indexOf` idiom; `oc_level` ≡ a_can per checked-in `scripts/extract-objects.py:297-298`; `HProtection`/intrinsic + `ublessed`/`uspellprot` floor; cap 3; `PROTECTION` joins the existing const.js import; monster clause omitted — hero-only function, mon arm stays D-1405). `js/invent.js` local clone deleted; its two enlightenment-display callers (`insight.c` uses the same C function) now import from `mhitm.js` (`imports.mjs --can` → IN-SCC, hoisted function declaration, runtime use only — no top-level TDZ read). No DIAG/FORCE/seed gates; short-circuit, list, ownership, mutation and integer semantics preserved (pure read-only scan, `|0`/`!==0` on masks).
**Verify:** `node scripts/verify.mjs --fn mhitm_knockback` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden (1 PASS: scen-wish-Priest-92163 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite (disclosed per durable-test-collateral); the blocked corpus session is the regression test.
**Named:** `magic_negation_mon` monster arms (amulet of guarding / `protects()` / innate high-cleric·minion — D-1405 stands); `mhitm_knockback` body untouched (exact since D-1932).
**Next:** do not re-pop `mhitm_knockback` for this session. Falsifier: a fresh `verify mhitm_knockback` showing a session blocked with it as owner, or an MC trace (EProtection/amulet/a_can state) contradicting the armpro math above. No seed/step/coordinate gates.

## 2026-09-15 — D-2346 `apply.c` do_break_wand residual: check_unpaid + freeinv + striking Soundeffect + ICE timers + bot + bhitpos

**C locus:** `nethack-c/upstream/src/apply.c` `do_break_wand` `:3909–4146` — unpaid `check_unpaid(obj)` then `costly_alteration(obj, COST_DSTROY)`; `gc.current_wand = obj; freeinv(obj); setnotworn(obj)`; WAN_STRIKING `Soundeffect(se_wall_of_force, 65)` before the wall-of-force pline; loop `gb.bhitpos.x/y`; dig `if (levl[x][y].typ == ICE) spot_stop_timers(x, y, MELT_ICE_AWAY)`; `if (disp.botl) bot()` after each `bhitpile` + trailing blindness one (the post-`bhitm` one stays commented out, as in C); `Sprintf(buf, "killed %sself by breaking a wand", uhim())`. `invent.c:1403 freeinv` (extract_nobj + pickup_prev + freeinv_core + update_inventory); `shk.c:5739 check_unpaid` (normal-use wrapper).
**JS:** same file, C-ordered lines with C comments; docstring Named-omit now only HOLE fall + revive polish.
**Change:** `js/apply.js` only (+22/−15, no new modules): `await check_unpaid(obj)` before `costly_alteration`; real `freeinv(obj)` (already imported); `Soundeffect(se_wall_of_force, 65)` in C order (`imports.mjs --can apply.js sndprocs.js Soundeffect` → SAFE; `se_wall_of_force` from checked-in `js/generated/seffects_data.js`); `spot_stop_timers(x, y, MELT_ICE_AWAY)` on ICE (`--can` ALREADY for display/shk/mkobj; MELT_ICE_AWAY joins the existing dynamic const import); `game.bhitpos = game._bhitpos` alias each loop cell (zap.js `bhitpile` unifies the slots the same way); `if (game.disp?.botl || game.flags?.botl) await bot()` at the three C sites (zap.js cancel_item precedent); `uhim()` killer (already imported). Small because the envelope shipped across D-0949/D-0950/D-0952/D-0979/D-0999 — this retires their named residual.
**Verify:** `node scripts/verify.mjs --fn do_break_wand` → PASS syntax (1 changed file) · PASS rule2 · note hidden (vacuous, 0 blocked at HEAD — not claimed as corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. /tmp wiring probe (bot/check_unpaid/spot_stop_timers/Soundeffect/se_wall_of_force/MELT_ICE_AWAY/uhim resolve + apply.js loads) PROBE PASS; probe deleted.
**Named:** HOLE `goto_level` fall (+ migrate/spoteffects — dig.c own row); revive container/buried polish; non-PHYS explode mon/hero damage (explode.c D-0968/D-0971/D-0973 rows); `furniture_handled`/full `boulder_hits_pool`/`flooreffects` (D-0950 deferrals, dig.c rows).
**Next:** queue head moves to `eat.c` cpostfx row; break-wand envelope needs a live session (no corpus/public wand-break) before HOLE-fall can be measured.

## 2026-09-15 — D-2345 `pray.c` offer_real_amulet / offer_fake_amulet / offer_too_soon amulet offering arms (turns.md:399 dosacrifice family)

**C locus:** `nethack-c/upstream/src/pray.c` `offer_real_amulet` `:1529–1589` (Amulet_off-if-worn, carried/useupf consume, `You offer…`, A_NONE Moloch death with lifesave fall-through to `fry_by_god(A_NONE,TRUE)` + ESCAPED, wrong-align `adjalign(-99)` + ESCAPED, own-align ascended + `adjalign(10)` + ASCENDED), `offer_fake_amulet` `:1601–1627` (low-altar unknown → `offer_too_soon`; `Soundeffect(se_thunderclap)` + `You_hear`; unknown → mistake + known + luck−1; known → Deaf "Oh, no." + luck−3 + `adjalign(-1)` + ugangr+3 + `offer_negative_valued`), `offer_too_soon` `:1478–1498` (A_NONE+Inhell → `gods_upset(A_NONE)`; else Hallu homesick / on-track surface-urge / ashamed `You_feel`), dispatched from `dosacrifice` `:1874–1889` (real: `!highaltar` → too_soon/TIME else real/NOTREACHED; fake → fake/TIME).
**JS:** 1 js file (`pray.js` +145/−11 incl. C-cited docs + dosacrifice wiring + header retire), far under the 600/10 caps. No new modules, no new module edges.
**Change:** `js/pray.js` only — file-local async `offer_too_soon` / `offer_real_amulet` / `offer_fake_amulet` in C order (C `staticfn` ⇒ file-local, matching `offer_corpse`/`offer_negative_valued`), wired into `dosacrifice` replacing the bare returns. Real: `u.uamul === otmp` identity → `Amulet_off()`; `carried`/`useup`/`useupf` sync idiom (consume_offering precedent); pline literals for `You`/`You_hear`-adjacent messages (no new `You` clone — zap.js drift noted by brief); Moloch killer `${s_suffix(MOLOCH)} indifference` + KILLED_BY (fry_by_god precedent); each `done`/`fry_by_god` arm gates continuation on `program_state.gameover` (god_zaps_you `:1102–1104` idiom — C NOTREACHED but `done()` returns on lifesave/wizard-decline); cloud colors via `hcolor('black'/'orange')` string prefs (file precedent); `u_gname(game.urole, u.ualign.type)` / local `a_gname()`; `uevent.ascended = 1` (`{}` guard, gcrownu precedent); `game.flags?.female` Demigod-dess (end.js:906 precedent). Fake: `Soundeffect(se_thunderclap, 100)` live no-op call (ball.js/dbridge.js precedent) + `await You_hear`; `otmp.known = 1` (mkobj numeric idiom); file Deaf idiom.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Probe `/tmp/probe_amulet.mjs` (module-load smoke: new sndprocs/seffects imports evaluate, no cycle/TDZ, `dosacrifice` exported; the ascension arms are reached by no public or corpus session so no behavioral falsifier exists; kept in /tmp, out of tree) → PASS. `node scripts/verify.mjs --fn offer_real_amulet` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed; `brief offer_fake_amulet` likewise 0 blocked) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (no shared-file full forced by config).
**Named:** `bestow_artifact` (corpse-gift path, own row) untouched; `display_nhwindow(WIN_MESSAGE, FALSE)` on the ascension arm is a no-op in JS (topline paints via pline; cf. allmain `init_sound_disp_gamewindows`) — control flow ported, no grid snapshot/restore (D-1831); ascension `SetVoice` pitch deferred (pray.js file convention); the sibling Open row `pray.c` offer_too_soon altar-too-soon refusal is now live as this cluster's required helper (its body + the dosacrifice low-altar real-Amulet arm are wired) — it will verify 0-blocked/vacuous, park candidate, not re-popped here.
**Next:** do not re-pop offer_real_amulet/offer_fake_amulet for these arms. Falsifier: a fresh `verify offer_real_amulet`/`verify offer_fake_amulet` showing a session blocked with either as owner, or an altar-amulet trace contradicting the C order above. No seed/step/coordinate gates.
