# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-14 — D-2298 `pager.c` look_traps: glyph-first `glyph_to_trap` + `trap_description` listing replaces the `trap at (x,y)` stub

**C locus:** `nethack-c/upstream/src/pager.c:2077–2141` `look_traps` (glyph branch `:2092–2096`: `glyph = glyph_at`, `glyph_is_trap` → `tnum = glyph_to_trap(glyph)` → `trap_description(lookbuf, tnum, x, y)`, `++count`; obscured branch `:2097–2104`: `t_at` + `tseen` + water/air-unless-`couldsee` gate → `trapname(t->ttyp, FALSE)` + `", obscured by encglyph(glyph)"`, glyph re-pointed at `trap_to_glyph(t)`; line `:2113–2130`: `coord_desc` per `getpos_coords` cmode (`%s`/`%8s`/`%12s`), `encglyph(glyph)`, BUFSZ truncation guard, `putmixed`; header `:2108–2112` `upstart` + `"    "` separator; footer `:2136–2140` `display_nhwindow` vs `pline("No traps seen or remembered%s.")`).
**JS:** `js/pager.js` only (+67/−27 with doc; `trap_description` doc note refreshed to name `look_traps` live), under the 600/10 caps. Density note: C is 64 lines; the port is the whole residual of the D-1779 named omit.
**Change:** port the C body in C order over live callees only — `glyph_at`/`glyph_is_trap`/`glyph_to_trap`/`trap_to_glyph` (display.js), `trap_description` (local, C `staticfn`), `t_at`/`trapname` (trap.js), `couldsee` (vision.js), `Is_waterlevel`/`Is_airlevel`/`BUFSZ` (const.js), `look_region`/`look_coord_prefix`/`look_getpos_cmode`/`look_shown_at`/`mon_glyph`/`obj_glyph` (local/display.js). Three new import names on three already-imported modules (`imports.mjs --can`: all ALREADY, no new edge; same-SCC call-time use, no top-level TDZ read). `encglyph` has no JS table (glyphmap[] deferred), so the covering char follows the `look_engrs` idiom (hero `@`, mon/obj constructor char, else the cell's shown `disp_ch`) and the trap char comes from `trap_to_glyph` (the same defsym table C's `encglyph` reads: `^`, `"` for web, `~` for vibrating square — probed).
**Verify:** `node scripts/verify.mjs --fn look_traps` → PASS syntax (1 file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; the row cited no N blocks so no `--base` re-run owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_looktraps.mjs` (out of tree, deleted after — no corpus session reaches `/t`/`/T`): pager import loads (no cycle/TDZ from the three extended imports), trap chars `^`/`^`/`^`/`"` for bear/chest/door/web, `glyph_to_trap(GLYPH_TRAP_OFF)` = ARROW_TRAP (1, matches C trap.h numbering), `t_at` null on empty level, `couldsee` OOB false, BUFSZ 256 cap arithmetic.
**Named:** `doidtrap` (the `^` single-cell command, C `:2335+` — its own C function, own future row); C TODO recursive/buried containers in `trapped_chest_at` (unchanged); glyphmap[]-based `encglyph` (covering char via `look_engrs` idiom, named here).
**Next:** do not re-pop `look_traps`/`trapped_chest_at` for the glyph-caller omit. Falsifier: a rescore or fresh `verify look_traps` showing a session blocked with it as owner, or a `/t`/`/T` listing session diverging on a trapped-chest/door name.

## 2026-09-14 — D-2297 `region.c` create_region box/nrects + add_rect_to_reg: stored bounding_box/nrects/C defaults wired at both gas-cloud constructors

**C locus:** `nethack-c/upstream/src/region.c:79–127` create_region (box seeded from rects[0] then min/max-expanded per rect; empty → lx=COLNO, ly=ROWNO, hx=hy=0; rects copied; nrects stored; ttl=-1, NO_CALLBACK ×6, clear_hero_inside + clear_heros_fault, zero monster list, arg=cg.zeroany) + `:133–157` add_rect_to_reg (append copy, nrects++, expand box). Live callers both do create_region(NULL,0) + add_rect loop: create_gas_cloud `:1297`, create_gas_cloud_selection `:1325`; clone_region/msg/force-field callers are `#if 0`. Struct `region.h:33–66`.
**JS:** `js/region.js` (+91/−20 with the two rewires and header ref).
**Change:** new exported `create_region(rects, nrect)` + `add_rect_to_reg(reg, rect)` in `js/region.js` (C order, `| 0` idiom, rects copied not aliased); both constructors now `create_region(null, 0)` + `add_rect_to_reg` per cell (identical resulting flags: REG_NOT_HEROS set, INSIDE clear; `make_gas_cloud` still overwrites inside_f/expire_f/arg/glyph/visible in C order); `inside_region` loops `reg.nrects` per C `:62–73` with `?? rects.length` fallback for pre-stored-field saves; stored-box recompute fallback kept for the same.
**Verify:** 16-case hand probe (`/tmp/probe_create_region.mjs`, deleted after run) PROBE PASS — empty box/defaults/flags, seeded-box expansion, copy independence, add grow + interior no-op, stored-box early-out, null reg, empty-region containment, legacy no-field fallback. `node scripts/verify.mjs --fn create_region` → PASS syntax (1 file) / rule2 / green 2/2 / strict ×2 / cohort 7/7; hidden is the explicit vacuous note (0 blocked at HEAD, NOT a corpus PASS; row cited 0 blocks so no `--base` re-run owed). VERIFY: PASS.
**Named:** none new (`clone_region`/`create_msg_region`/`create_force_field` are `#if 0` in C too; binary save_regions/rest_regions + free_region teardown stay deferred per the module header).
**Next:** `detect.c` trapped_chest_at/trapped_door_at (next Open row).

## 2026-09-14 — D-2296 `dog.c` mon_leave worm arm: count/truncate/wormgone/head-back wired at keepdogs + migrate_to_level (queue row `worm.c` wormgone mondead/dog callers)

**C locus:** `dog.c:728–763` `mon_leave` (worm arm `:748–760`: `count_wsegs`, `min(cnt, MAX_NUM_WORMS-1)` per `monst.h:160` = 32, `wormgone` `:755`, `place_monster` head-back `if (mx)`); callers keepdogs `:861/:865` (mydogs) and `migrate_to_level` `:904/:916` (migrating_mons). `m_detach` `:2786–2787` already live (D-2231).
**JS:** 3 files (`js/dog.js` +~40 incl. docs, `js/teleport.js` +~8, `js/worm.js` doc-only; + map + queue + this log), under the 600/10 caps. Density note: the residual is one small C staticfn plus two one-line wirings — the mon.c half of the row shipped under D-2231.
**Change:** new exported `mon_leave(mtmp)` in `js/dog.js` (C home; worm arm verbatim in C order, sync — `wormgone`/`place_monster` are sync, their `impossible` is fire-and-forget); keepdogs follower arm calls it before the fmon splice and stores the count in `wormno` (C `:861/:865`); `migrate_to_level` calls it after mx/my capture and stores the count before `mlstmv` (C `:904/:916`). Imports extend existing edges only (`MAX_NUM_WORMS` joins the `const.js` import, `place_monster` the `steed.js` import, `count_wsegs`/`wormgone` the `worm.js` import); the one new edge (`teleport.js` ← `dog.js`) is same-SCC (`imports.mjs`: IN-SCC, cycle not a defect) and `mon_leave` is a hoisted function declaration used at call time only — no top-level TDZ read. No DIAG/FORCE/seed-gate/coordinate/RNG-index reads (arm is RNG-free on the success path); Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn wormgone` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited no N blocks) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (forced `--full`: migration-adjacent change) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_monleave.mjs` (out of tree, deleted after — no corpus session reaches a migrating worm): via the real `jsmain.js` entry — non-worm `mon_leave` returns 0 unmutated; wormno'd off-map mon runs the `wormgone` arm (wormno 7→0, returns 0); `migrate_to_level` end-to-end sets `wormno`, zeroes mx/my, lands on `migrating_mons`, no throw. (A direct `dog.js`-first import trips a pre-existing objnam/do_name TDZ, reproduced identical at clean HEAD in a scratch worktree — harness-order artifact, not this change; worktree removed.) No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); the full-44 green is the regression proof for the shared-file change.
**Named:** `mon_leave`'s minvent `no_charge`/`picked_container` loop; isshk `set_residency` (C says `mon_arrive` sets it back); `migrate_to_level` leash + light-source arms (pre-existing names, untouched); `place_wsegs` restore/replmon callers (own Open row, untouched).
**Next:** do not re-pop `wormgone`/`mon_leave` for these arms. Falsifier: a rescore or fresh `verify wormgone` showing a session blocked with it as owner, or a migrating-worm session diverging on stale segs/wormno.

## 2026-09-14 — D-2295 `mon.c` kill_genocided_monsters chameleon newcham arm (queue row `cmd.c` makemap_prepost kill_genocided_monsters, D-1097/D-1190 residual)

**C locus:** `mon.c:5639–5677`, arm `:5663–5667` (`if (ismnum(mtmp->cham) && !kill_cham) (void) newcham(mtmp, NULL, NC_SHOW_MSG); else mondead(mtmp)`); `hack.h:1462–1464` (`NC_SHOW_MSG` 0x01); callers `cmd.c:1048` (makemap_prepost post), `do.c:1817` (goto_level), `read.c:2749` (do_class_genocide), `read.c:2993` (do_genocide).
**JS:** 4 files (`js/mon.js` +13/−7 incl. doc, 3 files one-word awaits each; +18/−12 total with the CURRENT Next-cluster line), under the 600/10 caps. Density note: the C locus is 40 lines and ~90 % shipped by D-1097/D-1190/D-1288 — this closes the last named arm, so the small diff is the whole residual.
**Change:** `js/mon.js` — the arm awaits the already-imported live `newcham(mtmp, null, NC_SHOW_MSG)` (C `(void)` return = result unused; the await only completes C's inline message before the loop continues, scored runs stay sync per D-1648); function is now `async`; `await` added at all four call sites (`js/wizcmds.js:595` makemap_prepost post, `js/do.js:1854` goto_level, `js/read.js:2405` do_class_genocide, `js/read.js:2599` do_genocide — nearest-fn scan confirms all four are already `async`); `kill_eggs` exported to match C linkage (kills the sym.mjs clone-drift flag; no second copy). No new module edge (`imports.mjs --can mon.js makemon.js newcham`: ALREADY). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn kill_genocided_monsters` → PASS syntax (4 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited no N blocks) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_kgm.mjs` (out of tree, deleted after — no corpus session reaches genocide+cham-imitator): cham imitator of genocided grid bug reshaped 116 → 111 (non-genocided), survives with mhp intact, non-genocided bystander untouched, async return confirmed, no hangs or unhandled rejections.
**Named:** mondead fire-and-forget (review-1197 debt — amulet+More corner suspends detach past later loop iterations, display-order only; the await-ripple is a bigger iter); makemap_prepost pre discard/savelev package (review-250 debt); kill_eggs TIN/CORPSE arms (`#if 0` in C, correctly absent).
**Next:** do not re-pop `kill_genocided_monsters` / `makemap_prepost` for this arm. Falsifier: a rescore or fresh `verify kill_genocided_monsters` showing a session blocked with it as owner.

## 2026-09-14 — Audit 48180ad1..e6f16d72 (reviews 1255–1260: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Six JS SHAs vs pinned C (`csym` + `sym.mjs` each): D-2289 acid
boom/blank_novel; D-2290 steedintrap ×4 (Antimagic_prop = pre-existing
D-1089 CLONE); D-2291 DFLAG2 truth table ≡ C; D-2292 elements + full
DRLI (sync/async call shapes checked); D-2293 wiz_intrinsic count +
wintty machine re-read; D-2294 MM_ANGRY ternary (skips 2 tail draws).
All `verify --base SHA~1` re-runs 0-blocked both sides; rulecheck clean.
Nits (no Must-fix): D-2290 `:1211/:1276` prose swap, landmine map row
stale omit, keep_saddle codelog-only. Cadence 44/44, Scr
11,405/11,405, RNG 792,838/792,838, `47+0.29/turn` — identical to
standing score. Queue 11 open, no refill. No js/ edits.

## 2026-09-14 — D-2294 `makemon.c` birth knowledge residuals: mpeaceful MM_ANGRY arm + mwandexp zeromonst/save field (queue row `makemon.c` birth knowledge residuals, D-2107-named)

**C locus:** `makemon.c:1297` (`mtmp->mpeaceful = (mmflags & MM_ANGRY) ? FALSE : peace_minded(ptr)`); `monst.h:166` (`mwandexp` bitfield, zeroed by `*mtmp = cg.zeromonst`) + `save.c:826–834`/`restore.c:307` (`savemon`/`restmon` write the whole struct via `Sfo_monst`/`Sfi_monst`, so `mwandexp` persists across save/restore by construction).
**JS:** 1 file (`js/makemon.js`, +4/−1), under the 600/10 caps. Density exception: the C locus is one line plus one struct field (same class as D-2107's +24/−1).
**Change:** `js/makemon.js` only — `mpeaceful` ports the C ternary verbatim (`(mmflags & MM_ANGRY) ? 0 : (peace_minded(ptr) ? 1 : 0)`); template gains `mwandexp: false` beside `mtame` so the saved blob always carries the field like C's whole-struct write. No new import (same module), no DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean. `mcastu.js:624` already zeroes `mpeaceful` post-call per C `:667`; `dokick.js:724` killer bees relied solely on this arm.
**Verify:** `node scripts/verify.mjs --fn makemon` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe (out of tree, deleted after): `/tmp/probe_mwandexp.mjs` through the real `serMon`→JSON→`deserMonList` path (via `jsmain.js` import order) — `mwandexp=true→true`, `false→false`, both OK, matching C `savemon`/`restmon` whole-struct persistence.
**Named:** none new (D-2107's pair is now fully live; `place_monster` cutworm/makemon callers belong to the next Open row, not this one).
**Next:** do not re-pop `makemon` birth knowledge. Falsifier: a rescore or fresh `verify makemon` showing a session blocked with it as owner, or a save-oracle probe showing `mwandexp` lost across a save/restore round-trip.
