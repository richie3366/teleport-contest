# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-26 — D-2861 `reset_commands` rebinds movement for number_pad, swap_yz, phone, and pcHack

**C locus:** `nethack-c/upstream/src/cmd.c:3344–3476` `reset_commands`. Callees `cmdbind_add` (`cmd.c:2125`, live `js/cmd.js:1264`), `cmdbind_swapkeys` (`cmd.c:2194`, `js/cmd.js:1521`), `cmdbind_remove` (`cmd.c:2161`, `js/cmd.js:1315`), `ext_func_tab_from_func` (`cmd.c:3015`, `js/cmd.js:1553`), `bind_key_fn` (`cmd.c:2731`, `js/cmd.js:1569`), `commands_init` (`cmd.c:2749`, `js/cmd.js:1584`), `bind_key` (`cmd.c:2661`, live `js/cmd.js:1345`), `update_rest_on_space` (`cmd.c:3483`, `js/cmd.js:1643`), `cmd_from_func` (`cmd.c:3035`, live `js/dokeylist.js`). `spkeys_binds` is `cmd.c:3161–3191`. `move_funcs` is `cmd.c:2070–2083` (`N_DIRS` 8, `N_MOVEMODES` 3). Calls: `options.c:2618` `optfn_number_pad` do_set; `options.c:5944` `handler_number_pad` after a menu pick; `options.c:7158` `initoptions_init` `reset_commands(TRUE)`. `cmd.c:2006` and `:3478` are comments. `number_pad()` at `:2619` and `:5945` is the `winprocs.h:161` tty no-op. `update_rest_on_space` is also called from `options.c:5426` (`optfn_boolean` `rest_on_space`) and `options.c:7364` (`initoptions_finish`).
**JS:** `js/cmd.js` `reset_commands` `:1667`, boot stand-in `:1671`, ylist swap `:1712`, pcHack `:1724`, phone `:1735`, dirchars `:1746`, backup/remove `:1752`, `bind_key_fn` `:1766`, `update_rest_on_space` call `:1775`, `extcmd_char` `:1777`. `update_rest_on_space` `:1643`, `commands_init` `:1584`, `bind_key_fn` `:1569`, `ext_func_tab_from_func` `:1553`, `cmdbind_swapkeys` `:1521`, `cmdbind_add` `:1264`. `js/dokeylist.js` `cmdbinds_live` `:272`. `js/options.js` `optfn_number_pad` `:2025`, `get_val` `:2040`, `handler_number_pad` `:2119`, `optfn_boolean_do_set` `:7336`.
**Change:** One `reset_commands` in that C order. Slots stay unset until the function runs, so a session that never calls it still uses `build_default_cmdbinds` (0 txt diffs against that table after `reset_commands(true)`). The first non-initial call, and `update_rest_on_space` when slots are missing, run the TRUE boot once: that is `options.c:7158`, which has no JS function, and the static `backed_dir_cmd` starts false.
**Verify:** `node scripts/verify.mjs --fn reset_commands` → PASS syntax (3 changed js files: js/cmd.js js/dokeylist.js js/options.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** `bind_mousebtn` (`cmd.c:2624`, called from `commands_init` `:2752–2753`); `click_to_cmd` still reads `game.Cmd.mousebtn` and stays inert. `initoptions_finish` `:7364`.
**Next:** `sp_lev.c` `get_coord` (next Open — coverage row). Nine Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.

## 2026-09-26 — D-2860 `peffect_oil` burns lit oil, then clears slime

**C locus:** `nethack-c/upstream/src/potion.c:1260–1294` `peffect_oil`. Callees `pline` (`pline.c`, live `js/display.js:7927`), `You` (`pline.c:355`, live `js/display.js:7671`), `likes_fire` (`mondata.h:196`, live `js/monsters.js:432`), `body_part` (`polyself.c:626`, live `js/polyself.js:626`), `losehp` (`hack.c:4255`, live `js/hack.js:1779`), `d` (`rnd.c`, live `js/rng.js:126`), `burn_away_slime` (`timeout.c`, live `js/timeout.js:1623`), `exercise` (`attrib.c:199`, live `js/attrib.js:199`). `Fire_resistance` / `Cold_resistance` are `youprop.h:28` and `:32` (`uprops[].intrinsic || extrinsic`). Calls: `potion.c:1412` inside `peffects`. `potion.c:34` is the static declaration.
**JS:** `js/potion.js` `peffect_oil` `:375`, `likes_fire` `:379`, `You` `:384`, `hero_element_resistance` `:387`, `losehp` `:391`, `finish_losehp_done` `:399`, `finish_maybe_wail` `:402`, `burn_away_slime` `:405`, `exercise` `:411`. Import `burn_away_slime` `:156`.
**Change:** One `peffect_oil` in that C order. Lit and `likes_fire(youmonst.data)`: "Ahh, a refreshing drink." and `good_for_you`. Otherwise `You("burn your %s.", body_part(FACE))`, then `losehp(d((!Fire_resistance || Cold_resistance) ? 4 : 2, 4), "quaffing a burning potion of oil", KILLED_BY)`.
**Verify:** `node scripts/verify.mjs --fn peffect_oil` → PASS syntax (1 changed js file: js/potion.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (potion.js is outside the shared-file set) · VERIFY: PASS.
**Named:** The green-slime extra damage in the comment at `potion.c:1269–1273` is not an arm. `losehp` still treats polymorphed `mh < 1` as fatal (`rehumanize` deferred), so a lit quaff that drops poly HP calls `finish_losehp_done`.
**Next:** `cmd.c` `reset_commands` (next Open — coverage row). Ten Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.

## 2026-09-26 — D-2859 `fall_asleep` stops the occupation, then sleeps

**C locus:** `nethack-c/upstream/src/timeout.c:951–974` `fall_asleep`. Callees `stop_occupation` (`allmain.c:683`, live `js/hack.js:1608`), `nomul` (`hack.c:4160`, live `js/hack.js:1582`). `You_can_move_again` is `decl.c:47` "You can move again." The `#if 0` block (`You_hear`, `incr_itimeout(&HDeaf)`, `Hear_again`) is not compiled. Calls: `eat.c:2595`, `hack.c:3045`, `mhitu.c:1862` (`#ifdef PM_BEHOLDER`, not defined), `potion.c:909`, `spell.c:491`, `timeout.c:790` (`nh_timeout` `SLEEPY`), `trap.c:1575` (`trapeffect_slp_gas_trap`), `uhitm.c:3502` (`mhitm_ad_slee` mhitu arm), `zap.c:2864` (`zapyourself`), `zap.c:4461` (`zhitu` sleep ray).
**JS:** `js/hack.js` `fall_asleep` `:1681`, `stop_occupation` `:1682`, `nomul` `:1683`, `usleep` `:1687`, `nomovemsg` `:1688`. `js/timeout.js` `incr_itimeout_HSleepy` `:398`, `Sleepy` `:408`, expiry arm `:1334`, `You` `:1344`, `fall_asleep` `:1346`. `js/trap.js` `trapeffect_slp_gas_trap` hero arm `:5133`, `fall_asleep` `:5141`, `steedintrap` `:5144`.
**Change:** One `fall_asleep` in that C order. `await stop_occupation()`, then `nomul(how_long)`, `multi_reason = "sleeping"`, `u.usleep = moves`, `nomovemsg` "You wake up." or "You can move again." `nh_timeout` on `SLEEPY` expiry: `unconscious()` or `Sleep_resistance` extends `HSleepy` by `rnd(100)`; else if `Sleepy`, `You("fall asleep.")`, `sleeptime = rnd(20)`, `fall_asleep(-sleeptime, TRUE)`, then `incr_itimeout(sleeptime + rnd(100))`. The hero sleep-gas arm `seetrap`s, then resistance or `breathless(youmonst.data)` says the cloud and `monstseesu(M_SEEN_SLEEP)`, else the gas line, `fall_asleep(-rnd(25), TRUE)`, `monstunseesu`, then `steedintrap`.
**Verify:** `node scripts/verify.mjs --fn fall_asleep` → PASS syntax (8 changed js files: js/eat.js js/hack.js js/mhitu.js js/potion.js js/spell.js js/timeout.js js/trap.js js/zap.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** The `#if 0` deafness / `Hear_again` block in `fall_asleep` is not in this build. `gazemu`'s beholder `AD_SLEE` (`mhitu.c:1856–1867`) stays out for the same `#ifdef`.
**Next:** `potion.c` `peffect_oil` (next Open — coverage row). Eleven Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.

## 2026-09-26 — D-2858 `status_initialize` enables every status field in C order

**C locus:** `nethack-c/upstream/src/botl.c:1683–1720` `status_initialize`. Callees `impossible` (`pline.c`, live `js/display.js:8116`), `init_blstats` (`botl.c:1758`, live `js/botl.js:167`), `(*windowprocs.win_status_init)()` (tty: `tty_status_init` `wintty.c:4336`, now `js/botl.js:315`, which calls `genl_status_init` `windows.c:893`), `status_enablefield` (`winprocs.h:185` → `tty_status_enablefield` `wintty.c:4364` → `genl_status_enablefield` `windows.c:922`), `panic` on reassess-before-init. Field order is `wintty.c:4278–4299`. Calls: `allmain.c:721` `status_initialize(FALSE)`; `options.c:5350` and `:5389` `REASSESS_ONLY`; `polyself.c:123` `REASSESS_ONLY`; `wintty.c:505` inside `new_status_window`. `winprocs.h:182` is a comment.
**JS:** `js/botl.js` `status_initialize` `:342`, `tty_status_init` `:315`, `genl_status_init` `:280`, `genl_status_enablefield` `:300`, `tty_status_enablefield` `:307`, `new_status_window` `:392`, reassess call `:405`.
**Change:** One `status_initialize` in that C order. A second full init calls `impossible` and continues: `init_blstats` returns, `tty_status_init` runs again, `blinit` stays true, then the field loop. Reassess throws `status 'reassess' before init` when `blinit` is false.
**Verify:** `node scripts/verify.mjs --fn status_initialize` → PASS syntax (4 changed js files: js/allmain.js js/botl.js js/options.js js/polyself.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** `display_nhwindow(WIN_STATUS, FALSE)` in `genl_status_init` (`windows.c:905`). `tty_clear_nhwindow` (`wintty.c:1034`) and `tty_destroy_nhwindow` (`wintty.c:2009`) in `new_status_window`; the second clear would also set `disp.botlx` (`wintty.c:1072`), which the reassess call sets on the next line.
**Next:** `timeout.c` `fall_asleep` (next Open — coverage row). Five coverage rows appended (`mhitm_ad_deth`, `obj_timer_checks`, `mhitm_ad_dcay`, `walk_path`, `get_level_extends`) so the band stays at 12 after archive. `--rows 8` is the never-re-pop Stale head.

## 2026-09-26 — D-2857 `unplacebc_core` lifts the ball and chain before the map update

**C locus:** `nethack-c/upstream/src/ball.c:147–177` `unplacebc_core`. Callees `Is_waterlevel` (`dungeon.h`, live `js/const.js`), `carried` (`obj.h:332` `where == OBJ_INVENT`), `obj_extract_self` (`mkobj.c:2556`, live `js/mkobj.js:3476`), `Blind` (`youprop.h:103`, file-local `Blind_bc`), `maybe_unhide_at` (`mon.c:4698`, live `js/monmove.js:1347`), `newsym` (`display.c`, live `js/display.js:5104`). Calls: `unplacebc` `ball.c:218`, `unplacebc_and_covet_placebc` `ball.c:230`. `ball.c:14` is the static declaration. `ball.c:302` and `:321` are the `BREADCRUMBS` wrappers; `config.h:644` leaves that macro undefined.
**JS:** `js/ball.js` `unplacebc_core` `:449`, `maybe_unhide_at` `:471` and `:477`, `newsym` `:472` and `:478`, `unplacebc` `:486`, core call `:491`, covet core call `:521`. Import `js/ball.js:51`.
**Change:** One `unplacebc_core` in that C order. Swallowed: the water level still extracts a ball that is not `OBJ_INVENT` and always extracts the chain, then returns before vision. Otherwise the same `where` test extracts the ball, a felt `BC_BALL` restores `u.bglyph` through `set_levl_glyph`, then `maybe_unhide_at` and `newsym`.
**Verify:** `node scripts/verify.mjs --fn unplacebc_core` → PASS syntax (7 changed js files: js/ball.js js/do.js js/mhitu.js js/shk.js js/teleport.js js/trap.js js/wizcmds.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** A null ball or chain returns; C would dereference it. `maybe_unhide_at` still skips the hero `u_at` / `u.uundetected` arm (`mon.c:4706–4709`).
**Next:** `botl.c` `status_initialize` (next Open — coverage row). Eight Open — coverage rows remain after archive, at the floor of 8, so nothing was refilled.

## 2026-09-26 — D-2856 `pre_mm_attack` unhides both monsters before the blow

**C locus:** `nethack-c/upstream/src/mhitm.c:41–72` `pre_mm_attack`. Callees `seemimic` (`mon.c`, live `js/mon.js:1178`), `M_AP_TYPE` (`monst.h:73`, live `js/const.js:3218`), `canspotmon` (`display.c`, live `js/display.js:1366`), `map_invisible` (`display.c`, live `js/display.js:1374`), `newsym` (`display.c`, live `js/display.js:5104`). `gv.vis` is the file-local `_mm_vis` set in `mattackm`. Calls: `missmm` `mhitm.c:81`, `hitmm` `mhitm.c:657`. `mhitm.c:13` is the static declaration. `mhitm.c:745` is a comment inside `gazemm`, not a call.
**JS:** `js/mhitm.js` `pre_mm_attack` `:4022`, `seemimic` `:4029` and `:4036`, `mundetected = 0` `:4032` and `:4039`, `map_invisible` `:4045` and `:4049`, `newsym` `:4047` and `:4051`.
**Change:** One `pre_mm_attack` in that C order. Defender then attacker: `seemimic` when `M_AP_TYPE` is set, otherwise clear `mundetected`. `showit` becomes true only when a reveal happens while `_mm_vis` is set.
**Verify:** `node scripts/verify.mjs --fn pre_mm_attack` → PASS syntax (1 changed js file: js/mhitm.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS.
**Named:** None inside `pre_mm_attack`. `gazemm` (`mhitm.c:746–748`) still does its own mimic `seemimic` and unconditional `mundetected = 0`.
**Next:** `ball.c` `unplacebc_core` (next Open — coverage row). Nine Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
