# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-15 — D-2375 `do_name.c` minimal_monnam body + m_detach/place_monster wires

**C locus:** `nethack-c/upstream/src/do_name.c:1254–1285` (null mon / null data / two wild-pointer range arms with `fmt_ptr` / `ckloc` long-worm tail arm reading `svl.level.monsters[mx][my]` / tame→peaceful prefix + `mon_pmname` + `<mx,my>` + `{cham}` suffix). Callers: `mon.c:2793` (`m_detach` already-detached, FALSE), `steed.c:910` (bounds, TRUE) + `:924–925` (overlap, FALSE/TRUE), `wizcmds.c:1587` (migr list, FALSE — not ported).
**JS:** 3 js files (+58/−16 per `git diff --numstat`: `js/do_name.js` +48/−1, `js/mhitm.js` +6/−6, `js/steed.js` +4/−9; plus the 1-line `CURRENT.md` Next-cluster copy + map touches), under the 600/10 caps. Above the 40-insertion density floor (C is 31 lines).
**Change:** new exported sync `minimal_monnam(mon, ckloc)` in `js/do_name.js` (C home) in C order/conjuncts, one `nextmbuf` slot per call (`x_monnam` idiom, so the steed overlap double-call keeps both tags). JS adaptations (C has real pointers, JS does not): the range arms key on numeric `data.mndx` (`?? mon.mnum`; `< 0` for `ptr < &mons[0]`, `>= NUMMONS` for `ptr >= &mons[NUMMONS]`) with `0x` hex where C prints `fmt_ptr` and the table ends named symbolically; a data object without a numeric index falls through to the normal arm (`mon_pmname` already handles that shape); grid read is `game._level_monsters` (C `svl.level.monsters`). Wired with C's exact `ckloc` flags: `m_detach` already-detached arm (`minimal_monnam(mtmp, false)`), `place_monster` bounds (`true`) + overlap (`false`/`true`); `place_mon_nam` clone deleted (`mon_plain` kept — still used by mount/dismount plines).
**Verify:** `node scripts/verify.mjs --fn minimal_monnam` → PASS syntax (3 files) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 → VERIFY: PASS. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Hand probe (arms no corpus/public session reaches; out of tree at `/tmp/probe-minimal-monnam.mjs`, kept for re-run): null mon/data, both Invalid shapes, peaceful/tame prefixes, cham suffix + undefined-cham skip, worm tail/head/ckloc-FALSE/mx-0 arms, nextmbuf rotation — 16/16 ok.
**Named:** wiz `migr` list caller (`wizcmds.c:1587–1599` `strsubst <0,0>` strip + MGIVENNAME + mux/muy + MIGR_EXACT_XY — no JS migr UI; own row when ported); `mongone` FALSE-caller body stays D-1149 (this iter wires only the `m_detach` impossible arm it shares).
**Next:** do not re-pop `minimal_monnam` for this body. Sibling `shrink_glob` stays its own Open row (different C function, already queued next). Falsifier: a rescore or fresh `verify minimal_monnam` showing a session blocked with `minimal_monnam` as owner (not mere m_detach/place_monster caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2374 `shk.c` globby_bill_fixup full 4-scenario bill port + globby get_pricing_units

**C locus:** `nethack-c/upstream/src/shk.c:5976–6097` (floor x/y; unpaid-absorber `next_shkp(fmon,TRUE)` + `onbill(,TRUE)` walk; absorbed-unpaid `OBJ_FREE` + `costly_spot` → `shop_keeper(*in_rooms)`; `shop_keeper(*u.ushops)` fallback; `onbill(,FALSE)` ×2; `set_cost(absorbed)`; scen 1 `bp && (!no_charge || billable(&shkp,,eshkp->shoproom,FALSE))` → drop entry via `*bp = bill_p[--billct]`, `clear_unpaid_obj`, fold into `bp_absorber->price`; scen 2 no-records `get_pricing_units*per_unit_cost` → `saleable` → debit/loan/`pline_The donated` vs credit/`Your debt is paid off`/`pline_The established/added`; scen-2-else fold into `bp_absorber->price`; scen 3 `bp && (no_charge || (floor && !costly_spot))` → `bill_dummy_object` + `SetVoice` + `verbalize You owe me`; scen 4 no-op) + `shk.c:2846–2858` `get_pricing_units` globby arm (`(wt + unit_weight - 1)/unit_weight`, `wt = owt || weight(obj)`) + `shk.c:214–231` `next_shkp` rile_shk side effect (JS clone already has it — C-faithful, kept) + `shk.c:307–315` `clear_unpaid_obj` silent-TRUE (entry already removed in scen 1, so absorbed.unpaid stays set — C order, kept).
**JS:** 2 js files (+167/−12 per `git diff --numstat`: `js/shk.js` +161/−2, `js/mkobj.js` +6/−10; plus the 1-line `CURRENT.md` Next-cluster copy + `data.md:287` map touch), under the 600/10 caps. Above the 40-insertion density floor (C is 121 lines + 12-line pricing arm).
**Change:** new exported async `globby_bill_fixup(obj_absorber, obj_absorbed)` in `js/shk.js` (C home; all 16 other callees already local/imported there — `next_shkp`/`onbill`/`set_cost`/`clear_unpaid_obj`/`get_pricing_units` locals, `costly_spot`/`shop_keeper`/`billable`/`bill_dummy_object` + `saleable`/`currency`/`in_rooms`/`obj_typename`(added to the existing objnam edge)/`impossible`/`pline`/`verbalize`/`SetVoice`/`ANGRY` — no new module edges, `imports.mjs` ALREADY both directions). C order/conjuncts throughout: `{shkp}` holder for C `&shkp` (writeback only fires on null entry; shkp non-null here so it round-trips); bill-drop mirrors the obfree `indexOf` precedent; `pline_The` → `` `The ...` `` (donate_gold `Your debt is paid off.` precedent for `Your`); scenario numbers as C banner comments. Async only because scen 2–3 reach pline/verbalize (--More--); scen 1 is fully synchronous.
**Verify:** `node scripts/verify.mjs --fn globby_bill_fixup` → PASS syntax (2 files) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 → VERIFY: PASS. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Hand probe (arms no corpus/public session reaches; out of tree at `/tmp/probe-globby.mjs`, kept for re-run): scen 1 billct 2→1 + absorber price 50+30=80 + bill[0] swapped + absorbed.unpaid stays set per C order; scen 4 empty-bill no-op; no-shkp early return — 7/7 ok.
**Named:** `onbill` impossible() arms (pre-existing clone takes `_silent`; shared helper, own row); scen-2 `saleable`-FALSE arm stays silent per C (no message); unhandled-rejection exposure of the fire-and-forget call matches the existing `void pudding_merge_message` in `merged` (same cluster).
**Next:** do not re-pop `globby_bill_fixup` for this body. Sibling `shrink_glob` stays its own Open row (different C function, already queued next). Falsifier: a rescore or fresh `verify globby_bill_fixup` showing a session blocked with `globby_bill_fixup` as owner (not mere `obj_absorb`/`obj_meld` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2373 `lock.c` stumble_on_door_mimic shared door-mimic + 3 call sites

**C locus:** `nethack-c/upstream/src/lock.c:758–769` (`m_at` + `is_door_mappear` + `!Protection_from_shape_changers` → `stumble_onto_mimic`, TRUE/FALSE); `monst.h:240` (`M_AP_FURNITURE` + `S_hcdoor`/`S_vcdoor` only); `youprop.h:355–360` (H || E). Callers: `lock.c:820` (doopen_indir → ECMD_TIME, before Confusion/Stunned cost), `lock.c:987` (doclose → ECMD_TIME, between !isok nodoor and Confusion/Stunned), `trap.c:6026` (untrap doorway → 1, after box loop, before IS_DOOR check). C `trap.c:5962` floor-trap inline (FURNITURE||OBJECT, ungated) is a different arm — already live in JS, untouched.
**JS:** `js/lock.js` (+helper, 2 wires, doc touch-ups), `js/trap.js` (doorway wire + `untrap` doc omit retired). No new modules, no new static edges. `js/` insertions ~50, well under 600.
**Change:** new exported async `stumble_on_door_mimic(x, y)` in `js/lock.js:636` (C home; predicate draws no RNG; async only because JS `stumble_onto_mimic` reaches pline --More--) with local `Protection_from_shape_changers()` (`js/lock.js:618`, H||E+flat per-file idiom). Wired in exact C order: `doopen_indir` (`:691`, before Confusion/Stunned res), `doclose` (`:899`, between !isok and Confusion/Stunned), untrap doorway (`js/trap.js:7027-7028`, after box loop) via lazy `await import('./lock.js')` — lock.js statically imports `b_trapped`/`t_at` from trap.js, so no new static edge and no top-level TDZ read (`imports.mjs` IN-SCC, runtime-only use).
**Verify:** `node scripts/verify.mjs --fn stumble_on_door_mimic` → PASS syntax (2 files) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 → VERIFY: PASS. Focused /tmp probes (deleted before finish): 5 false arms (no-monster, object-mimic, furniture-non-door, H-protected hcdoor, E-protected vcdoor → all false) + true arm (unprotected hcdoor → true, mimic revealed `m_ap_type=0`).
**Named:** `pick_lock` `:571` keeps its own inline (same predicate but C-ungated + `maybe_absorb_item` tail); doopen pit dirprompt/pit-reach; doclose portcullis/drawbridge/steed; untrap `move_into_trap` residuals (D-2372).
**Next:** pop next Open row (`shk.c` globby_bill_fixup probe).

## 2026-09-15 — Audit reviews 1324–1338 (D-2358…D-2372) + cadence 44/44

Review iteration, no `js/` edits. Audited every JS-touching SHA since
1323 oldest-first against pinned C (`csym` body + callers + guarding
`if`, `sym.mjs` on re-pointed symbols, added-line banned grep,
`imports.mjs --rulecheck` clean, `hidden-proxy verify --base HASH~1`
re-measured per SHA): 15/15 **ACCEPT**, zero Actionable C-wrongs, zero
Must-fix (LOOP-QUEUE untouched, CURRENT Next cluster untouched). Every
D-log Verify bullet honestly marked its corpus check vacuous (all rows
cited 0 blocks — confirmed 0-at-baseline/0-working on each re-run).
Residuals noted in prose, not queued: 1335 Esc-on-swap menu-flow delta
(doc-named `:2159–2163`); 1338 unmoved-jerk spoteffects nuance lives in
the pre-existing ball.js caller-side vehicle shared with domove.
Cadence: full `sessions` at HEAD `12cae556` → **44/44**, Scr
11,405/11,405, RNG 792,838/792,838, `46+0.29/turn` (R² 0.80) — fortress
holds, CURRENT Score refreshed. Queue at 8 Open (band floor, no refill;
no `- [x]` leftovers). Filled archive `D-2372` short hash `12cae556`.

## 2026-09-15 — D-2372 `trap.c` move_into_trap failed-untrap stumble onto trap

**C locus:** `nethack-c/upstream/src/trap.c` `move_into_trap` `:5393–5437` (`test_move(u.ux,u.uy,sgn(x-ux),sgn(y-uy),TEST_MOVE)` short-circuit + `!Punished || drag_ball(x,y,&bc,&bx,&by,&cx,&cy,&unused,TRUE)`; then `ux0/uy0`, `u_on_newpos(x,y)`, `umoved`, `newsym(old)`, `vision_recalc(1)`, `check_leash(old)`, `move_bc(0,...)` when punished, `tseen=0` check_here hack, `failing_untrap++`, `spoteffects(TRUE)`, `failing_untrap--`, re-`tseen=1`, `exercise(A_WIS)`; else `Fortunately... into/onto` via `into_vs_onto`) + `nethack-c/upstream/src/hack.c` `test_move` `:1000` door_opened clear / `:1140–1147` + `:1205–1213` doorway-diagonal bans + `nethack-c/upstream/src/ball.c` `drag_ball` TRUE/FALSE contract (jerk-back moves hero + `spoteffects(TRUE)` inside before FALSE; encumber moves nothing) + `nethack-c/upstream/src/dungeon.c` `u_on_newpos` `:1568–1595`.
**JS:** 1 js file (+92/−6 per `git diff --numstat`: `js/trap.js` only; plus the 1-line `CURRENT.md` Next-cluster copy + `turns.md:2867` map touch), under the 600/10 caps. Above the 40-insertion density floor (C is 44 lines; gate + both spoteffects paths + C citations).
**Change:** `js/trap.js` only, no new modules, no new module edges (`imports.mjs --can` ALREADY on all three static extends: `trap.js → ball.js` for `drag_ball`/`move_bc`, `trap.js → apply.js` for `check_leash`, `trap.js → steed.js` for `test_move_ok`; `u_on_newpos` via `await import('./mklev.js')` and both `spoteffects` calls via `await import('./pickup.js')` at runtime only — same dynamic pattern trap.js already uses — no top-level TDZ read): new async `move_into_trap(ttmp)` next to `try_disarm` in C order/conjuncts — `door_opened=false` (C `:1000`), inline `uball` Punished (youprop.h:77, D-1786 convention), `test_move_ok` doorway-diagonal gate (steed.js subset of TEST_MOVE; C comment says the sgn calls are redundant since adjacent), `drag_ball(x,y,TRUE)` only when punished with the JS `{ok,...}` shape mapped to C boolean (ok → save bc/bx/by/cx/cy; !ok + hero jerked → runtime `spoteffects(true)` to match C-inside-drag_ball, then fall to Fortunately; !ok unmoved encumber → Fortunately with no spot), then `ux0/uy0`, `u_on_newpos` + steed share, `umoved=true`, `newsym`/`vision_recalc(1)`/`check_leash`, conditional `move_bc(0,...)`, `tseen=0`, `failing_untrap++/spoteffects(TRUE)/--`, re-`tseen=1`, `exercise(A_WIS,false)`, else `Fortunately...` via same-file `into_vs_onto`/`sgn`; `try_disarm` adjacent arm now `await move_into_trap(ttmp)` and its doc retires the omit. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn move_into_trap` → `PASS syntax 1 changed js file(s): js/trap.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify move_into_trap: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session fails an adjacent disarm onto a still-blocked vs enterable trap cell (same disclosure as D-2367–D-2371); both `move_into_trap` and `try_disarm` are file-local (C `staticfn`, like the caller), so no export probe applies.
**Named:** full `test_move` rock/closed-door/boulder/worm/travel arms (try_disarm already gates boulder/tight-diagonal/reach; trap cells are accessible so the doorway subset is the live arm — block_door/block_entry shopkeeper, may_passwall, underwater, tunnels, autodig ride along, doc-named on the function); `u_on_newpos` cliparound/uundetected/see_nearby/earth_sense/map_location (thin mklev.js + steed sync only); drag jerk hmon/miss damage (ball.js burns the rnd(20) roll, same deferral as the domove caller).
**Next:** do not re-pop `move_into_trap` for this body. Sibling `stumble_on_door_mimic` stays its own Open row (different C function). Falsifier: a rescore or fresh `verify move_into_trap` showing a session blocked with `move_into_trap` as owner (not mere `try_disarm`/`untrap` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2371 `zap.c` poly_obj invent side effects via addinv_core1/2

**C locus:** `nethack-c/upstream/src/zap.c` `poly_obj` `:1905–1914` (`get_obj_location` + `old_wornmask` mask-out `W_ART|W_ARTI`, `replace_object(obj, otmp)`, `OBJ_INVENT` → `freeinv_core(obj)` + `addinv_core1(otmp)` + `addinv_core2(otmp)`, then the worn remap) + `nethack-c/upstream/src/invent.c` `addinv_core1` `:960–1004` / `addinv_core2` `:1025–1050` (both live in `js/u_init.js` since D-1539/D-2370, incl. the questart `artitouch` arm).
**JS:** 2 js files (+12/−5 per `git diff --numstat`: `js/u_init.js` +2/−2, `js/zap.js` +10/−3; plus the 1-line `CURRENT.md` Next-cluster copy + `data.md:149` map touch), under the 600/10 caps. Below the usual 40-insertion density floor because the C block is 5 lines and both callees were already live (same disclosure as D-2370).
**Change:** 2 js files, no new modules, no new module edges (`imports.mjs --can` ALREADY on `zap.js → u_init.js`; `u_init.js` imports nothing from `zap.js` — no cycle; both names used only at runtime inside async `poly_obj` — no top-level TDZ read): `export` on both cores in `js/u_init.js`; `js/zap.js` extends the existing `./u_init.js` import and awaits both in C order/conjuncts right after `freeinv_core(obj)`, with the C `:1910–1914` citation; `poly_obj` doc retires the `addinv_core1/2` named omit. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn poly_obj` → `PASS syntax 2 changed js file(s): js/u_init.js js/zap.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify poly_obj: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session polymorphs an invent item with a side-effecting result (same disclosure as D-2367/D-2368/D-2370).
**Named:** `arti_invoke` on W_ART drop (C `artifact.c:880–885` in `set_artifact_intrinsic`; the locus is sync with 25 `freeinv` call sites incl. local shadows in `dothrow.js`/`steal.js`/`steed.js`, while `arti_invoke` is async — needs an async-propagation vehicle, queued as its own Open row this same commit); the old-obj invoked-toggle reversal inside `freeinv_core` on this poly path (same vehicle); GEM mineral backfire / spestudied / shop bill / boulder block (pre-existing `poly_obj` named omits, untouched).
**Next:** do not re-pop `poly_obj` for core1/2. Falsifier: a rescore or fresh `verify poly_obj` showing a session blocked with `poly_obj` as owner (not mere `dozap`/`polyself` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
