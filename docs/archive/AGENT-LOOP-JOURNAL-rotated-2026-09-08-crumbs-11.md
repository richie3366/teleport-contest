# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-08 — D-2101 `sp_lev.c:5241-5251` `ensure_way_out` rescan order: inner `break` (x-scan continues) must be both-loops exit per C `goto outhere` (review 1065 Must-fix)

**C locus:** `sp_lev.c:5241-5251` `ensure_way_out` driver: the match arm ends with `goto outhere` whose label sits outside both loops — one join exits the x-scan entirely and the do-while rescans from `x = 1` (`sp_lev.c:5217-5255` body read in `brief ensure_way_out`).
**JS:** 1 file (mklev.js +5/-4 incl. comment), under the 600/10 caps. Must-fix stays one item, alone.
**Change:** `js/mklev.js` only — outer x-loop labeled (`outer:`) with `break outer` in the match arm (exact C both-loops exit + rescan-from-x=1 via the existing do-while); doc comment corrected to cite `sp_lev.c:5241-5251` (exits both loops). No new import or edge, no TDZ (label is intra-function), no RNG/mutation change on the observed path.
**Verify:** `node scripts/verify.mjs --fn selection_rndcoord` → PASS syntax (1 changed file: js/mklev.js) · PASS rule2 · note hidden (no corpus session blocked on `selection_rndcoord` at HEAD — vacuous, NOT a corpus PASS; row cited 0 blocks so no `--base` re-run owed; the review's `65152c55~1` measurement already showed the D-2095 move Ranger-92033 → rloc@same-step with full positional draw match) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after).
**Named:** none new. Review 1065's non-queued note (map_cleanup / count_level_features / link_doors_rooms geometry-invariance argument) stays as noted, not queued.
**Next:** do not re-pop `ensure_way_out` (0 blocked; latent arm now faithful). Next rescan-order divergence on another minetn-6 seed re-opens those three extras first per review 1065.

## 2026-09-08 — Audit reviews 1059–1066 (fd3f5f38…213658d3) + cadence 44/44

**Reviews:** 8 files, one per JS SHA, each with `--base` re-measured corpus verify (all matched D-logs). 6 ACCEPT (1059–1064), 1 QUALITY-RISK (1065 ensure_way_out inner-`break` ≠ C `goto outhere`, latent join-order C-wrong → Must-fix prepended, Next cluster set), 1 ACCEPT-WITH-DEBT (1066, pre-existing demonpet appear-msg coords debt pointer at uhitm.js:1453). Rule #2 clean; no REJECT; no STOP.
**Score:** full `sessions` 44/44 on the D-2098 tree (Scr 11,405/RNG 792,838 unchanged; speed 83+0.49/turn). check-hot-docs --fix: all ok, journal rotated, queue mf=1/open=11 in band, no refill owed.
**Next:** next port pops Must-fix 1065 first, alone.

## 2026-09-08 — D-2098 objnam.c xname FIGURINE "of <monster>" suffix (queue owner checkfile, 3 sessions) + retire stale dowield (D-2099) / find_trap (D-2100); operator-ordered Open-head ship (head was stale)

**C locus:** `objnam.c:709–714` `xname_flags` TOOL arm — FIGURINE + corpsenm appends `" of just_an+pm"` (`obj_pmname`, `do_name.c:1321–1356`). Creation faithful (`mkobj.js:1575`); STATUE twin live (`objnam.js:666`). Stale heads measured first: `verify dowield` → 1 PASS (credit D-2044 simpleonames plural); `verify find_trap` → moved to `dolook`@108 (inferred credit D-2092); `verify checkfile` → 3 live (newt/Archon/leocrotta figurines).
**JS:** 1 file (objnam.js +11/−2; same-file `obj_pmname_corpse` + `just_an`, STATUE-guard shape, no new edge). Retires D-0418 figurine half; wet-towel/`permapoisoned` stay named.
**Change:** figurine suffix live; queue: archived dowield/find_trap/checkfile, added Open `insight.c` one_characteristic@168 (mattock vs pick-axe) + `invent.c` dolook@108 (can't-see-in-here); Open=11, Must-fix=1 (review 1065 rescan).
**Verify:** hand probe real `xname` 3/3 + bare-ok (/tmp only) · `verify --fn checkfile` → hidden 0 PASS/3 moved past → PROGRESS · green 2/2 · strict ×2 · cohort 7/7 · VERIFY: PASS.
**Named:** wet-towel moist/wet; `permapoisoned`; corpse creation untouched.
**Next:** next port pops Must-fix 1065 (ensure_way_out `break outer`) first — Next cluster already set.

## 2026-09-08 — D-2097 potion.c peffect_sleeping queue row is stale: the step-62 yawn already ships via D-2070 sleep_dialogue (literal-match misattribution); session moved to distfleeck@103, no js/

**C locus:** the yawn is `timeout.c:267–274` `sleep_dialogue` (`i == 4 → You("yawn.")`) under `nh_timeout :639–640`, NOT `potion.c:905` `peffect_sleeping` (quaff arm). Proof: step-62 key is `s` (search — no quaff, no vapor event; the step's RNG is monster-movement only); only three `yawn` sites exist in C (`potion.c:905,2061`, `timeout.c:273`); `#wizintrinsic` granted SLEEPY=30 at step 51 («Timeout for sleepy set to 30.») and steps 53–58 plus the step-61 `20s` run exactly 26 `nh_timeout` ticks → 30−26=4 at step 62's pre-decrement check. `peffect_sleeping` JS body (D-1437, `js/potion.js:1774`) re-read against C and faithful — nothing to port.
**JS:** 0 files — under the 600/10 caps trivially. Rule #2 untouched; no DIAG/FORCE/seed gates; probes lived in /tmp only (kept, not committed).
**Change:** none — no js/ changes (tree clean apart from docs). Queue row retired as stale; `**Addressed:** D-2097` (this entry records the retirement), code credit D-2070.
**Verify:** `node scripts/verify.mjs --fn peffect_sleeping` → PASS syntax (0 changed js files) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Rogue-92172 62→distfleeck@103, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2068 precedent).
**Named:** SLEEPY-expiry `fall_asleep`+incr (`timeout.c` case SLEEPY — JS generic arm silently clears; D-2070 named) — left for the session's future owner, not this envelope.
**Next:** do not re-pop `peffect_sleeping` (0 blocked). Rogue-92172's `distfleeck`@103 flows through the normal queue on rescore — but `distfleeck` stays a parked symptom owner: its screen delta («You stop searching.» is the `allmain.c:688` `stop_occupation` arm — JS ends a search occupation where C continues) plus the JS-first `rnd_otyp_by_namedesc` draw need a true-writer row, not a distfleeck pop.

## 2026-09-08 — D-2096 teleport.c enexto_core far loop evaluated a shifted candy slice (append vs C overwrite) + genesis appear-msg used requested coords: kraken placed off-C-dice, «next to you» instead of «close by» (queue owner goodpos, 1 session)

**C locus:** (1) `teleport.c:256–268` `enexto_core` — the second `collect_coords(candy,…,0,…)` overwrites the SAME buffer; the far loop evaluates `candy[nearcandyct..allcandyct)`. (2) `makemon.c:1491–1499` — the appear Norep uses post-enexto `x,y` (`x = cc.x` at :1182–1185); `read.c:3315` `create_particular_creation` prints nothing itself. C `next_ident` legitimately draws `rnd(2)` (mkobj.c anti-`#adjust` exploit comment), so the JS-side draw is correct — the divergence is the missing goodpos call before it.
**JS:** 3 files (teleport.js +9/−8, read.js +5/−1, makemon.js comment +4/−3), under the 600/10 caps.
**Change:** `js/teleport.js` `enexto_core` only — `candy.length = 0` before the second collect (exact C buffer-reuse semantics: same indices, shuffle slices and draws) with a C cite; temp probe fully reverted in the same edit. `js/read.js` `create_particular_creation` passes final `(mtmp.mx|0, mtmp.my|0)` to `makemon_appear_msg` with a C cite; `js/makemon.js` doc comment corrected (final placement, not requested). No new imports/edges (all helpers module-local or already imported); no DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn goodpos` → PASS syntax (3 changed files: js/makemon.js js/read.js js/teleport.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Rogue-91119 step 67 → `givit` at step 70, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree; final verify after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** `collect_coords` callers at teleport.js:1304/1666 pass fresh arrays (unaffected; no helper-signature change needed); `makemon_appear_msg` `set_msg_xy` + occupation `dochugw` stays named (pre-existing); second `enexto_core` attempt without GP_CHECKSCARY unchanged (C-identical retry).
**Next:** do not re-pop `goodpos` (0 blocked). scen-wish-Rogue-91119's `givit`@70 flows through the normal queue.

## 2026-09-08 — D-2095 sp_lev.c ensure_way_out never ran: minetn-6 skipped the 670-cell secret-door drain + hole, pairing flip_level_rnd against C's selection_rndcoord (queue owner selection_rndcoord, 1 session)

**C locus:** `sp_lev.c:6467–6469` `load_special` — `if (gc.coder->check_inaccessibles) ensure_way_out();` before `map_cleanup`/wallification/`flip_level_rnd :6482`. The flag comes from des `level_flags("inaccessibles")` (`sp_lev.c:3798–3799`); only `dat/minetn-6.lua` sets it (repo-wide search). `ensure_way_out :5217–5255` seeds ov from same-dnum stairs + hole/undestroyable traps, then joins each ACCESSIBLE cell outside ov via `generate_way_out_method :5146–5214`, whose secret-door loop (`while (selection_rndcoord(ov3, &x, &y, TRUE)) :5162`) drains the whole 670-cell mines floodfill (C draws `rn2(670)`…`rn2(1)`, 670 draws, no wall found), then the `Can_fall_thru` loop (`:5197`) places a HOLE on its first pick (`rn2(670)=601`, `rn2(2)=1` → HOLE, `hole_destination` `rn2(4)=1`), then C draws the two `flip_level_rnd` `rn2(2)`s. Full step-70 draw audit: 671 selection draws (4189–4859) + `rn2(2)`@sp_lev.c:5198 + `rn2(4)`@hole_destination + `rn2(2)`×2@flip_level_rnd + place_lregion run.
**JS:** 2 files (mklev.js +187/−6, trap.js +1/−1 export), under the 600/10 caps.
**Change:** `js/mklev.js` + one-word `export` on `js/trap.js` `undestroyable_trap` — new `floodfillchk_match_accessible` (`ACCESSIBLE||SDOOR||SCORR`, sp_lev.c:4600–4605), `selection_floodfill_accessible` (selvar.c:395–452 stack shape with the accessible predicate: seed pushed unchecked, neighbours need isok + predicate + not-visited-this-call, diagonals param), `generate_way_out_method` (`:5146–5214` verbatim incl. +x/−x/+y/−y secret-door order, `Can_fall_thru` hole/trapdoor loop, `ROLL_FROM(escapeitems)` third arm), `ensure_way_out` (`:5217–5255` verbatim incl. x=1 start, inner-`break` = C `goto outhere`, do-while rescan; C `selection_free` = GC), called in `load_minetn_6` pre-wallification (C `load_special` position). Reuses live imports (`ACCESSIBLE`/`IS_WALL`/`maketrap`/`mksobj_at`/`Can_fall_thru`/`isok`) + `undestroyable_trap` added to the existing trap.js import edge (imports.mjs: ALREADY, no new edge); 4 escape-item otyps via the file's `objectNames.indexOf` idiom (PICK_AXE 259, DWARVISH_MATTOCK 71, WAN_TELEPORTATION 424, RIN_TELEPORTATION 194 — all resolve). No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn selection_rndcoord` → PASS syntax (2 changed files: js/mklev.js js/trap.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past (1 re-attributed at the same step), 0 unchanged, 0 worse → PROGRESS` (scen-tour-Ranger-92033 step 70 → `rloc`@teleport.c:1850 at step 70; `hidden-proxy show` confirms JS now matches C positionally through all 671 drain draws + `rn2(2)` hole + `rn2(4)` hole_destination + both flip dice + the place_lregion run — rngM 15152→15701, prevEntry `rn2(17)=12 @ place_lregion` is a matched draw) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree; final verify after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** `map_cleanup` + `count_level_features` + `link_doors_rooms` extras stay named on `load_minetn_6` (draw-free on this path — C shows no draws between the hole arm and the flip dice); `ensure_way_out` stays named-omitted on every other special loader (only minetn-6 sets the des flag).
**Next:** do not re-pop `selection_rndcoord` (0 blocked). scen-tour-Ranger-92033's `rloc`@70 flows through the normal queue.
