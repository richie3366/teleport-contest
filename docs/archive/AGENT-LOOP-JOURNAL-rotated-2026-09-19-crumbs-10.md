# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-19 — Audit e900cb58..1c1e08cc (reviews 1489–1497: 9 ACCEPT, no Must-fix) + cadence 44/44, proxy 497/540, held-out 11/44

One SHA at a time against pinned C, each re-run with hidden-proxy verify `--base HASH~1 --reach-all` (all 0-blocked + smoke 24/24 REACH-OK, matching the D-logs): misc_stats template/plur/region-two-sizeof exact (SIZEOF_* on the D-log gcc claim, wizard-display only); breakobj shop-billing conjuncts + 6 wired callers (fracture_rock named) + potion clone→import re-point verified; dofiretrap melt_ice unconditional fix + dual botl; merged 3 clone rewirings + sync-callee audit (impossible async fire-and-forget is the file pattern); getpos_menu both C callers byte-matched (OK-vs-CANCEL); build_options opt_out_words re-derived C-exact + zero-import TDZ revert proven; draft_message dridx ranges walked 0..3 + both zap else-if slots; thrwmu mswings_verb two-level check (positional rn2 kept); set_savefile_name signature-change audit (no other-file callers) + RELEASED/SAVESIZE/regularize verified. No C-wrongs: no queue/CURRENT-Next change except the shipped-pointer advance (Next: mhitm_ad_slim). Cadence: full sessions 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, 69+0.47/turn R² 0.82); held-out 11/44 (5,753 pts, RNG 26.6 %, screens 51.1 % — judge scored 07:08Z, through ~D-2534); corpus 497/540 (92.0 %) flat (+0/−0 in-window). Refill @1c1e08cc: 5 → 12 (elec/init_CapMons/check_room/all_options_strbuf/readobjnam_preparse/polyman/fill_special_room; reset_glyphmap guard + docompress infra + parked/done dupes skipped).

## 2026-09-19 — D-2538 `files.c` set_savefile_name whole body in C order (coverage THIN → live, UNIX arm ported, both JS save sites wired)

**C locus:** `nethack-c/upstream/src/files.c:1020–1123` (`set_savefile_name`). Callees: `regularize` (`:1087`, UNIX body `sys/unix/unixunix.c:297` ported file-local as `regularize_save_suffix`); `fname_encode` (`:1043`, WIN32-only — named, not cloned); `impossible` (`:1119`, inside `#if NH_DEVEL_STATUS != NH_STATUS_RELEASED`, contest pins RELEASED `patchlevel.h:33` — compiled out, named, never called). Constants: `SAVESIZE` (UNIX `fnamesiz.h:74` = PL_NSIZ 32 + sizeof SAVEX 12 + sizeof ext 1 + INDSIZE 8 = 53, derived in JS from the same parts); `SAVE_EXTENSION` (UNIX `fnamesiz.h:46` = `""` — `#ifdef` block live, length guard false, no-op); `SAVEX` (`save/99999.e`, shape only).
**JS:** `js/save.js` (+~85/−6: SAVESIZE parts, `regularize_save_suffix`, restarted body, `PL_NSIZ` import word, 2 caller arms) + CURRENT.md cluster line + map lines — under caps (1500 ins / 15 files).
**Change:** `js/save.js` — restarted `set_savefile_name(regularize_it)` in C order with `:line` cites: `:1030–1034` VMS arm named compiled out; `:1036–1053` WIN32 arm named compiled out; `:1054–1057` UNIX arm live (`save/` + `game.plname`, regoffset 5, spot 2); `:1059–1064` MSDOS / `:1065–1085` MICRO+AMIGA arms named compiled out; `:1086–1087` `regularize` applied to the post-`regoffset` suffix only (the `save/` slash survives); `:1088–1093`/`:1094–1104`/`:1105–1108`/`:1109–1115` indicator-1 / SAVE_EXTENSION / indicator-2 / postappend arms live with C short-circuit (`sfindicator`/`postappend` null, extension `""` — no-ops, `overflow` stays 0); `:1116–1122` `impossible` named compiled out. Writes `game.SAVEF` (the `gs.SAVEF` analogue), returns it as a JS convenience (C returns void). Same-file callers rewired to C order: `dosave0` presets `set_savefile_name(1)` then uses `game.SAVEF`; `try_restore_save` (the `restore_saved_game :1276` analogue) presets `set_savefile_name(1)` then VFS-reads `game.SAVEF`.
**Verify:** `node scripts/verify.mjs --fn set_savefile_name` → PASS syntax (1 changed: js/save.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full skipped (no shared file changed) → VERIFY: PASS. Tail pasted verbatim:
**Named:** `getuid()` uid digits (`:1055` — Rule #2, no POSIX identity in dual-runtime ESM; single-user VFS keeps `save/<plname>`); `fname_encode` (WIN32-only, never cloned); VMS/WIN32/MSDOS/MICRO/AMIGA arms; SYSV 14-char truncation inside `regularize`; RELEASED `impossible`; `check_panic_save`/`recover_savefile`/`get_saved_games` caller counterparts (unported recover/selectsaved paths).
**Next:** Open head after set_savefile_name (`uhitm.c` mhitm_ad_slim).

## 2026-09-19 — D-2537 `mthrowu.c` thrwmu whole body in C order (coverage THIN → live, polearm arm ported, the one C caller wired)

**C locus:** `nethack-c/upstream/src/mthrowu.c:1173–1264` (`thrwmu`). Callees/macros: `mon_wield_item` (`:1190`, live weapon.js edge, async → awaited); `select_rwep` (`:1194`, live weapon.js edge); `is_pole` (`:1198`, live wield.js edge, already imported); `MON_WEP` (live weapon.js edge); `dist2`/`distmin` (live mon.js edge, established); `MON_POLE_DIST` = 5 (hack.h:1435 = js/const.js:1942 — added to the existing specifier); `couldsee` (live vision.js edge); `canseemon`/`lined_up` (file-local `js/mthrowu.js:278`/:369); `xname`/`the`/`an`/`obj_is_pname` (live objnam.js edge); `Monnam` (live do_name.js edge); `mswings_verb` (live mhitu.js:354 — new static edge, `imports.mjs --can` SAFE hoisted-function in the existing 96-module SCC); `pline_mon` (live display.js:7492 — on the existing static display edge, specifier extended); `dmgval` (live weapon.js edge); `bigmonst` (live monsters.js edge); `Maybe_Half_Phys` = `maybe_half_phys` (live hack.js edge); `thitu` (same-file `js/mthrowu.js:545`, async → awaited, fresh `{ obj }` box — C passes `&local`, discarded); `stop_occupation` (live hack.js edge, async → awaited); `autoreturn_weapon` (live weapon.js edge); `mwelded` (live wield.js edge); `rn2` (live rng edge); `monshoot` (C staticfn, same-file `js/mthrowu.js:1355`); `nomul` (live hack.js edge); `URETREATING` (mthrowu.c:18–19 — pre-existing JS formula matches with `??` null-safety).
**JS:** `js/mthrowu.js` (55 changed lines per diff --stat: 3 import words, doc-comment update, body restart) + CURRENT.md cluster line + map lines — under caps (1500 ins / 15 files).
**Change:** `js/mthrowu.js` — restarted `thrwmu_body` in C order with `:line` cites: `:1186–1191` wield-gate; `:1194–1196` `select_rwep`; `:1198–1240` polearm arm (must-be-wielded return, `dist2` rang vs `MON_POLE_DIST`/`couldsee` return, canseemon-gated `pline_mon '%s %s %s.'` with `mswings_verb(otmp, rang <= 2)` — bash iff adjacent, matching C `(rang <= 2) ? TRUE : FALSE` — then `dmgval(otmp, game.youmonst)`, `hitv = 3 - distmin`, `-4` clamp, `bigmonst` bump, `+ 8 + spe`, `dam < 1 → 1`, `thitu(hitv, Maybe_Half_Phys(dam))`, `stop_occupation`, return); `:1241–1247` autoreturn arm as the C `else if` (short-circuit `autoreturn_weapon` → `!mwelded`, range/`couldsee` return, `always_toss = true` set once in the body — no recompute); `:1249–1259` lined_up/URETREATING gate (`!always_toss` short-circuits before `rn2`, unchanged); `:1261–1263` `monshoot` + `nomul(0)`. Clang left-to-right arg order kept at every site (`Monnam`, `mswings_verb` maybe-`rn2(2)`, article).
**Verify:** `node scripts/verify.mjs --fn thrwmu` (+ `--reach-all` re-run, identical) → PASS syntax (1 changed: js/mthrowu.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full skipped (no shared file changed) → VERIFY: PASS. Tail pasted verbatim:
**Named:** none in this body — every arm and callee is live, file-local, or imported above.
**Next:** Open head after thrwmu (`files.c` set_savefile_name).

## 2026-09-19 — D-2536 `dig.c` draft_message whole body in C order (coverage THIN → live, both `zap.c` callers wired)

**C locus:** `nethack-c/upstream/src/dig.c:1502–1544` (`draft_message`). Callees/macros: `You_feel` (`:1514`/`:1523`/`:1527`/`:1542`, live display edge, async → awaited); `Hallucination` (youprop.h:120 — module-local `js/dig.js` helper, see omissions); `ACURR(x)` = `acurr(x)` (attrib.h:24 — live attrib.js edge; `A_INT`/`A_DEX`/`A_CON`/`A_CHA` consts added to the existing specifier, `A_STR`/`A_WIS` already there); `rn1` (live rng edge, already imported — one `rn2` draw each, order preserved); `sgn` (hacklib.c — module-local `js/dig.js` helper); `STRIDENT` = 4 (`:1499`, from pray.c — file-local const); `u.ualign.type`/`record` (`game.u` idiom, `:1872` pattern).
**JS:** `js/dig.js` (+~40/−6: STRIDENT const, full body, export, 4 import words, 1 doc line) + `js/zap.js` (+5/−7: import word, two caller arms, 3 comment lines) + CURRENT.md cluster line — 2 files, under caps (1500 ins / 15 files).
**Change:** `js/dig.js` — restarted + exported `draft_message` in C order with `:line` cites: `:1513–1514` plain «an unexpected draft»; `:1515–1523` hallu «like you are %s» (4-F when any of the six ACURR attrs < 6, else 1-A); `:1526–1528` plain «a draft»; `:1529–1542` hallu `draft_reaction[dridx]`, `dridx = rn1(2, 1 - sgn(type))` widened by `rn1(3, sgn(type) - 1)` when `record < STRIDENT` (Lawful 0..1, Neutral 1..2, Chaotic 2..3 — all 0..3, index always in-bounds). Same commit wires `zap.c:5408` (`zap_over_floor` inline → call) and `zap.c:3746` (`zap_map` named → `else if (Is_rogue_level)` call) via the existing static dig.js edge (`imports.mjs --can` ALREADY — specifier extended, no new edge); zap header + `zap_map` doc comments updated.
**Verify:** `node scripts/verify.mjs --fn draft_message` → PASS syntax (2 changed: js/dig.js js/zap.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full skipped (no shared file changed) → VERIFY: PASS. Tail pasted verbatim:
**Named:** none in this body — every arm and callee is live or file-local. Note: the module-local `Hallucination()` helper ORs H/HH/EH without the youprop.h:120 `!Halluc_resistance` conjunct — a pre-existing module pattern (used at `js/dig.js:1897/1911`), not introduced here; out of scope for this row.
**Next:** Open head after draft_message (`mthrowu.c` thrwmu).

## 2026-09-19 — D-2535 `mdlib.c` build_options whole body in C order (coverage MISSING → live, the one C caller wired)

**C locus:** `nethack-c/upstream/src/mdlib.c:669–830` (`build_options`). Callees: `build_savebones_compat_string` (`:681`, file-local port `js/version.js:184`); `datamodel(0)` (`:699`, C home hacklib.c:981 — ported as file-local export `js/version.js:150`, see Fix); `opt_out_words` (`:700`, file-local port `js/version.js:291`); `count_and_validate_winopts` (`:716`, file-local port `js/version.js:256`); `count_and_validate_soundlibopts` (`:754`, file-local port `js/version.js:271`); `eos` (hacklib.c:192 — folded into `+=` appends, JS strings are values, named below); `Sprintf/Strcpy/Strcat` scaffolding (assignments/concatenation at each site); `STOREOPTTEXT` (`:98–101`, file-local `storeOpttext`); tables `build_opts` (`:417–599`), `window_opts` (`:112–164`), `soundlib_opts` (`:184–232`), `lua_info` (`:800–817`).
**JS:** `js/version.js` (+366/−0, 1 file) + CURRENT.md cluster line — under caps (1500 ins / 15 files). `js/hacklib.js` untouched (datamodel reverted out of it).
**Change:** `js/version.js` (+366) — full family in C order with `:line` cites: opttext state (`:95–104`); `build_savebones_compat_string` (`:392–415`, VERSION_COMPATIBILITY arm compiled out → "5.0.0 only"); 26 contest `build_opts` entries in C order (every compiled-out arm named: TIMED_DELAY off via contest `-DNO_TIMED_DELAY` — recorder Makefile + 006 patch, which resolves the MACOS auto-enable paradox; WIN32/GUILaunched, DLB, DUMPLOG, SCORE_ON_BOTL, USE_XPM and the rest listed per C line); `window_opts` tty-only (`:113–128`) + fencepost; `soundlib_opts` nosound-only + fencepost (USER_SOUNDS off); both count fns (WIN32 block compiled out); `opt_out_words` with C-exact word splitting (empty words from double spaces still append; trailing space yields no phantom word); `build_options` (`:681–828`: STATUS_ARG "" RELEASED, defwinsys "tty", `","`/`"."` punctuation by C index via the `:597` live savebones slot, winsys Oxford list, soundlib list, Lua block with `:TAG:` deferred, trailing `""`); `runtime_info_init`/`do_runtime_info`/`release_runtime_info` (`:834–872`, `{ i }` holder for `int *`). INVERSION FOUND AND REVERTED: first cut imported COLNO from const.js + datamodel from hacklib.js — direct version-first import crashed (`const.js:33` reads this module's COMMIT_NUMBER at top level → TDZ), so the module stays import-free per D-1881 (local OPT_COLNO=80, datamodel file-local; `imports.mjs --can` had flagged the cycle).
**Verify:** `node scripts/verify.mjs --fn build_options --full` → PASS syntax (1 changed: js/version.js) · rule2 (a seed id in a comment tripped the first run — reworded, no seed names in scored code) · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 PASS → VERIFY: PASS. Tail pasted verbatim. Independent oracle (/tmp/probe_build_options.mjs, not committed): live opttext (38 raw lines) + a probe-only doextversion filter == the hardcoded pager.js array AND all 3 recorded C `#version` screens, up to 2 trailing spaces C keeps in lua_info (`:808/:811`) that every recorded screen strips (display-layer trim, unanimous).
**Named:** `eos` (pointer-arithmetic helper — folded into `+=`, C hacklib.c:192); `make_version`/`populate_nomakedefs`/`free_nomakedefs` (save-compat version struct, no JS reader — future row); callers-of-caller `early_init`/`dump_version_info` (different C functions — future rows); doextversion-loop rewire onto live `do_runtime_info` (pager.js keeps its passing hardcoded lines; Next); hardcoded array drops the 2 C trailing spaces (screen-invisible, sessions PASS — not "fixed" here).
**Next:** Open head after build_options (`dig.c` draft_message).

## 2026-09-19 — D-2534 `getpos.c` getpos_menu whole body in C order (coverage MISSING → live, both C callers wired)

**C locus:** `nethack-c/upstream/src/getpos.c:665–725` (`getpos_menu`). Callees: `gather_locs` (`:677`, file-local `js/getpos.js:909` — same-file call, no second clone); `You` (`:682`, live display edge, async → awaited); `create_nhwindow`/`start_menu`/`add_menu`/`end_menu`/`select_menu`/`destroy_nhwindow` + `nul_glyphinfo`/`any.a_int`/ATR_NONE/clr/MENU_ITEMFLAGS_NONE (no JS window layer — one `select_menu_pick_one` call, the there_cmd_menu/doextlist mapping); `do_screen_description` (`:699`, live pager edge, `{s}/{v}` box convention); `coord_desc` (`:701`, live display edge — returns string vs C outbuf, firstmatch never aliases tmpbuf either side); `an` (`:712`, live objnam edge); `gloc_descr`/`gloc_filtertxt` (file-local GLOC_DESCR/GLOC_FILTERTXT, verified identical to C `:117–133`); GFILTER_VIEW/PICK_ONE/MENU_BEHAVE_STANDARD/NHW_MENU (live const edge — behavior via the helper); `free()` (GC no-op).
**JS:** `js/getpos.js` (+~85 new fn + 2 import lines + 10 caller lines) + `js/cmd.js` (+16/−6 dotravel branch + 1 import word); ~100 js/ insertions, 2 files + CURRENT.md cluster line — under caps (1500 ins / 15 files).
**Change:** `js/getpos.js` — exported async `getpos_menu` (`js/getpos.js:944`) in C order with `:line` cites: `:677` same-file gather_locs; `:679–685` count<2 → `You('cannot %s %s.')` see/detect + descr[0], FALSE; `:687–692` item list (array[0] hero skipped); `:693–706` per-spot firstmatch 'unknown' + do_screen_description gate, coord_desc suffix, `a_int = i + 1` carried on the item; `:710–716` "Pick …" title + select_menu_pick_one (title ATR_INVERSE + blank header rows); `:717–722` `garr[a_int - 1]` → ccp, boolean return. New imports join existing edges only (`You`/`coord_desc` extend the display specifier; `ATR_INVERSE` extends terminal; `an` objnam + `select_menu_pick_one` options are same 96-module SCC, runtime-only reads — `imports.mjs --can` verdicts recorded).
**Verify:** `node scripts/verify.mjs --fn getpos_menu` → PASS syntax (2 changed: js/cmd.js js/getpos.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full skipped (no shared file changed) → VERIFY: PASS. Tail pasted verbatim.
**Named:** C window layer has no JS counterpart (select_menu_pick_one is the established PICK_ONE mapping — selectors a..z assigned tty_end_menu-style by the helper); `free()` calls are GC no-ops.
**Next:** Open head after getpos_menu (`mdlib.c` build_options).
