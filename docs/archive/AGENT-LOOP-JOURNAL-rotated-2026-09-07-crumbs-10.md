# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-07 — D-2035 timeout.c STRANGLED expiry: done_timeout(DIED, …) + amulet-vanishes (2 PASS, dosearch row residual)

**C locus:** `timeout.c` `case STRANGLED :890–900` (killer.format=KILLED_BY, name buried?`suffocation`:`strangulation`, `done_timeout(DIED, STRANGLED)`, then a worn AMULET_OF_STRANGULATION vanishes via `Your`+`useup`), reached from the `nh_timeout` uprops-expiry loop after `choke_dialogue`'s `urgent_pline`+`stop_occupation` (`:629–630`/`:294–314`).
**JS:** 1 file (`timeout.js` +21/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; only `/tmp` probes (replay-screen + mon-dump), uncommitted and outside the repo.
**Change:** `js/timeout.js` — new `!(next & TIMEOUT) && p === STRANGLED` arm after SLIMED in C order (killer init mirrors the STONED arm; `done_timeout(DIED, STRANGLED)` + `gameover` early-return; amulet arm via `u.uamul` + `objectNames` otyp idiom (do_wear.js:108 precedent) + `pline('Your amulet vanishes!')` + invent `useup`, the C `useup :1320–1333` port). Imports: `DIED` extends the pre-existing `./const.js` edge; `useup` extends the pre-existing `./invent.js` edge (hoisted function decl, no TDZ risk; `imports.mjs --can timeout.js end.js useup_amulet` reports the end.js edge ALREADY exists — the file-local `useup_amulet` was not needed since C calls plain `useup`). Expiry-switch summary comment lists the new arm.
**Verify:** `node scripts/verify.mjs --fn dosearch` → `PASS syntax 1 changed js file(s): js/timeout.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `hidden verify dosearch: 2 PASS, 0 moved past, 2 unchanged, 1 worse` (92121 FULL 3808/3808 RNG + 83/83 screens; 92191 FULL 3280/3280 + 70/70; 92127 + 92023 rows byte-identical before/after) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · full `frozen/ps_test_runner.mjs sessions` 44/44 PASS (extra: timeout.js is turn-central). The `WORSE` on 92012 is stale-scoreboard drift, proven not-a-regression: the pre-change baseline `verify dosearch` on the clean tree prints the identical `WORSE: now dosearch at step 10 (was 10)` (committed scoreboard at 525cbba9 says rngM 3084, HEAD code measures 3039 — D-2034-era drift), and a stashed A/B (`hidden-worker` on HEAD `timeout.js` vs fixed file) gives identical `dosearch@10 rngM 3039/3443 scrM 10/179` both ways. Final verify ran after the last edit (no D-1831 gap).
**Named:** none new — `done`/`Die?`/`savelife` (end.js), `useup` (invent.js), `choke_dialogue` all live. Map `turns.md:45` drops STRANGLED from the silent-expiry list.
**Next:** residual dosearch row (3 sessions, still first-diverge under dosearch): 92023@65 gremlin round is one attack short in JS (JS topline misses the last «hits!» every round — steps 65/77/89/92 pattern; the key stream self-corrects via «Unknown command ' '» so RNG still matches — suspect `mhitu.c mattacku` attack-count/loop, needs its own brief, not this cluster); 92012@10 + 92127@31 grid-bug `x` at different map cells with matched RNG (C `·········│` vs JS `x········│` at 92127 row 7 col 57; movement-logic vs stale-glyph undetermined — needs a per-step mon dump; geom-probe N/A, not level-gen).

## 2026-09-07 — D-2034 bones.c give_to_nearby_mon: death-drop reservoir port (4 PASS + 1 moved past)

**C locus:** `bones.c` `give_to_nearby_mon :226–255` (static; sole caller `drop_upon_death :297` `!rn2(8)` arm): xx-outer/yy-inner 3×3 scan, `isok` → `u_at` hero-square skip → `m_at` → `likes_gold/gems/objs/magic` on `mtmp->data` (intentionally no otmp-match check per the C comment), reservoir `if (!rn2(nmon)) selected = mtmp`; tail `can_carry → add_to_minv` else `place_object`.
**JS:** 1 file (`end.js` +40/−5), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (corpus sessions are the falsifier).
**Change:** `js/end.js` — new `give_to_nearby_mon` verbatim from C (loop/guard order, `!rn2(nmon)` reservoir, `can_carry`→`add_to_minv` else `place_object`; the else arm keeps this file's pre-existing RNG-free `stackobj` floor convention so the no-neighbour path is state-identical to the stub it replaces). The `!rn2(8)` arm now calls it. Imports: `m_at` (`./mon.js`) + `can_carry` (`./monmove.js`) are new edges (`imports.mjs --can`: both SAFE — hoisted function decls, runtime-only reads); `likes_gold/gems/objs/magic` extend `./monsters.js`, `add_to_minv` extends `./mkobj.js`, `u_at` extends `./const.js` (all pre-existing edges); `isok`/`rn2`/`place_object`/`stackobj` already imported.
**Verify:** `node scripts/verify.mjs --fn give_to_nearby_mon` → `PASS syntax 1 changed js file(s): js/end.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify give_to_nearby_mon: 4 PASS, 1 moved past (1 re-attributed at the same step), 0 unchanged, 0 worse → PROGRESS` (Barbarian-92201, Caveman-91109, Ranger-91139, Ranger-92151 PASS; Knight-92072 moved RNG→screen at same step 150) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Manual replay of Knight-92072: RNG 3422/3422 FULL, screens 152/153; `hidden-proxy show` kind=screen step 150/153 owner-null, row 10 `| doppelganger |` vs `| healer |`, stepFns `[drop_upon_death, give_to_nearby_mon, next_ident, newmonhp, makemon, m_initinv]` — JS now reaches the ported function and every RNG draw matches; the residual is one unattributed menu/map row. Post-port `hidden-proxy queue` no longer lists `give_to_nearby_mon` (all 5 sessions left the owner).
**Named:** `drop_upon_death` mtmp/cont arms (`add_to_minv(mtmp)` / `add_to_container`) + `artifact_light`/`end_burn` stay named (pre-existing, untouched); map `turns.md:1644` drops `give_to_nearby_mon body` from the deferred list, live entry at :1610.
**Next:** Knight-92072 step-150 screen residual (`doppelganger` vs `healer` row, owner null — possibly a `can_carry` accept/reject fork, which draws no RNG, or downstream `m_initinv` content; needs a bones-inventory dump comparison, not more RNG work) — queue material only if it re-attributes to a named owner. Note the live queue's new `bones.c drop_upon_death` row (3 sessions, `rn2(5)` curse vs `start_corpse_timeout`) is a different divergence (corpse-timeout envelope), not this cluster.

## 2026-09-07 — Audit reviews 998–1003 (D-2028…D-2033) + cadence score 44/44 (review iteration, no js/ edits)

**Scope:** every js/-touching SHA since audit 990–997: `92eedfe1` (D-2028 use_container), `4bda180d` (D-2029 STONED), `525cbba9` (D-2030 tactics), `ee5d6d71` (D-2031 dodrink), `40b275ef` (D-2032 seffect_fire), `7d22c4f3` (D-2033 famn). Docs-only `5c95a5b6` skipped. One SHA at a time, file written before opening the next.
**Verdicts:** 5 ACCEPT (999, 1000, 1001, 1002, 1003), 1 ACCEPT-WITH-DEBT (998: `use_container` drops the C `:2985–2989` lknown pre-branch — old JS set lknown only in the locked arm, new JS nowhere; messages match, state-only debt, no blocked session → listed in review, no Must-fix). 1000 notes a harmless D-log slip ("nothing imports wizard.js" — 9 importers at that SHA; edges still TDZ-safe, full suite green). Each corpus claim re-measured `--base HASH~1`: use_container 2/3 PROGRESS, trapmove 1/3 PROGRESS, collect_coords 0/4 PROGRESS, dodrink 0/3 PROGRESS, seffect_fire vacuous (0 blocked, as D-log says; replay re-run at HEAD: Screen 73/152, confirms 66→73), mhitm_ad_famn NO MOVEMENT (0 at baseline, screen---More-- residual, as D-log says).
**Score:** full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `55+0.44/turn` (R² 0.84). CURRENT refreshed (D-2033, audits 998–1003).
**Next:** queue untouched (10 Open, in 8–12 band, no Must-fix owed); first Open remains bones.c give_to_nearby_mon.

## 2026-09-07 — D-2033 uhitm.c mhitm_ad_famn: Famine mhitu + mhitm arms (RNG past the arm to mhitm_ad_stun; continue-iteration)

**C locus:** `uhitm.c` `mhitm_ad_famn :3777–3805` (uhitm arm `:3780–3783` dead per C comment — hero never polymorphs into a FAMN attacker; mhitu arm `:3784–3796` `pline_mon` + `exercise(A_CON,FALSE)` + `!is_fainted()` `morehungry(rn1(40,40))` + normal damage; mhitm arm `:3797–3804` non-eater `damage = 0` else normal damage); dispatch `case AD_FAMN :4826`; `mhitu.c` `hitmu :1187` base `d()` then `mhitm_adtyping` (no hitmsg); consecutive-DISE/PEST/FAMN→STUN substitution `mhitu.c:340–348` (already live in JS `get_mattk`).
**JS:** 2 files (`mhitm.js` +52/−2, `mhitu.js` +24/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (corpus session is the falsifier).
**Change:** `js/mhitu.js` — new `mhitm_ad_famn_u` + `case AD_FAMN` in `mhitm_adtyping_u`. `js/mhitm.js` — new `mhitm_ad_famn` + `AD_FAMN` block in `mdamagem` with the standard knockback/damage epilogue; `AD_FAMN` joins the export block. Imports extend pre-existing edges (`monsters.js` diet trio, `mhitm.js` `AD_FAMN`); `morehungry`/`is_fainted` are a new top-level `mhitu.js`→`eat.js` edge (`imports.mjs --can`: SAFE, hoisted decls).
**Resume:** previous agent (iter-2488) wrote docs + D-2033 + popped/archived the row but died before commit with verify + rescore in flight — and its `vacuous` verify claim came from a pre-edit run (it never re-ran post-edit). This iteration re-measured everything: post-port hidden deterministically reports NO MOVEMENT (verify's own rescore re-attributes Tourist-92067 to `mhitm_ad_famn` every run — same step 140/owner, screen-only `--More--`); replay + stashed before/after rng-diff confirm the arm is faithful and the RNG is past it. Shipped as a faithful strict-improvement with the taxonomy verdict recorded honestly (reverting would strand the session at `exercise`@140 and guarantee a repeat port).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_famn` → `PASS syntax` · `PASS rule2` · `FAIL hidden 0 PASS, 0 moved past, 1 unchanged, 0 worse → NO MOVEMENT` · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · full skipped (no shared file changed). Replay Tourist-92067 → RNG 5401/5854 Screen 140/234 (topline byte-exact sans `--More--`); rng-diff 5380 (exercise + FAMN draws missing) → 5386 (`mhitm_ad_stun` rn2(4) vs rn2(3)) — strictly later owner, 0 worse. Working-tree scoreboard churn excluded from the commit (verify regenerates it; D-2032 precedent).
**Named:** none — uhitm arm dead per C comment (documented in both callee docs); every live callee is live.
**Next:** later owner `mhitm_ad_stun` mhitu arm (`rn2(4)` gate at `uhitm.c:4406` — Famine's STUN-substituted second attack) should unblock Tourist-92067 (its port makes JS print/more where C mores); queue material for that owner, not this cluster. Screen-only `--More--` is display putstr more-timing/turn-flow (lich precedent), never a gate-port target (D-1831).

## 2026-09-07 — D-2032 read.c seffect_fire: scroll-of-fire port + doread allowlist/nodisappear (1 moved to later owner)

**C locus:** `read.c` `seffect_fire :1850–1916` (bcsign dam `:1864`, useup+learnscrolltyp `:1865–1868`, confused `:1869–1891`, underwater `:1892–1893`, blessed getpos `:1895–1905`, tower+`burn_away_slime` `:1907–1910`, `explode(ZT_SPELL_O_FIRE=11, SCROLL_CLASS, EXPL_FIERY) :1915`); `seffects` exercise head `:2199–2200` + `SCR_FIRE` dispatch `:2270–2271`; `doread` `nodisappear` `:617–618` (SCR_FIRE first).
**JS:** 1 file (`read.js` +102/−10 total incl. header docs), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (corpus session is the falsifier).
**Change:** `js/read.js` — new `seffect_fire` in C order (already_known before useup; dam `Math.trunc((2*(rn1(3,3)+2*cval)+1)/3)`; useup + `learnscrolltyp(SCR_FIRE)` up front, returns null on every arm since C does `*sobjp = 0`; confused → underwater trickle / Fire_resistance `shieldeff`+`monstseesu(M_SEEN_FIRE)`+pretty-flames vs `monstunseesu`+1hp `losehp`; underwater → violent vaporize; blessed → self-ID pline + dam×5 + `getpos` centering where a failed `can_center_cloud` falls back to the hero (C ignores the getpos return — no Never_mind); hero-centered → tower pline + `iflags.last_msg = PLNMSG_TOWER_OF_FLAME` + `burn_away_slime`; always `explode(cc, 11, dam, SCROLL_CLASS, EXPL_FIERY)`); `seffects` `SCR_FIRE` arm returns 1 when consumed so `doread` skips its second useup (identify/charging pattern); `SCR_FIRE` joins the allowlist; `nodisappear` gains `otyp === SCR_FIRE`. Imports: `explode` (`./explode.js`) + `burn_away_slime` (`./timeout.js`) are new edges, both `imports.mjs --can` SAFE (hoisted function decls); `shieldeff`/`hliquid`/`Fire_resistance`/`monstseesu`/`monstunseesu`/`u_at`/`EXPL_FIERY`/`PLNMSG_TOWER_OF_FLAME`/`M_SEEN_FIRE` extend pre-existing edges (ALREADY, no new edge). Reuses the local `useup`/`learnscrolltyp`/`can_center_cloud`/`display_stinking_cloud_positions` (no clone #2).
**Verify:** `node scripts/verify.mjs --fn seffect_fire` → `PASS syntax 1 changed js file(s): js/read.js` · `PASS rule2` · `note hidden vacuous` (as row predicts) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · full skipped (no shared file changed) · `VERIFY: PASS`. Manual replay `node frozen/ps_test_runner.mjs .cache/hidden/sessions/scen-wish-Ranger-92212.session.json` → screens 66→73/152, `rng-diff` first mismatch moves from the step-66 fire draws to index 3285 (`C rn2(106)=4 @ resist(zap.c:6141)` vs `JS rn2(109)=46`): the fire-scroll draws now match and the session first-diverges inside explode's monster-resist/burnarmor — a strictly later owner. Ranges factor cleanly (C `100+9−3`: SCROLL alev 9, victim dlev 3; JS `100+12−3`: local clone alev = Ranger ulevel 12, same dlev 3) so the blast center and victim match C — the remainder is pre-existing explode-envelope drift, not this locus.
**Named:** none — every arm's callee is live (`explode`, `burn_away_slime`, `shieldeff`, `getpos` family, `hliquid`, `Fire_resistance`, `monstseesu/unseesu`, `losehp`). Fire has no SPE/spell path, so nothing deferred there. (Pre-existing, untouched: `SCR_TAMING` is live in `seffects` but still absent from the `doread` allowlist — separate row if the corpus reaches it.)
**Next:** later owner `explode.c` resist — `js/explode.js:216` local `resist` clone ignores oclass (`alev = ulevel`, cf. C `zap.c:6113–6130` oclass switch with SCROLL→9) plus `js/trap.js burnarmor` range drift (`rn2(5)` vs `rn2(6)/rn2(3)`) — rescore should re-attribute Ranger-92212 there; queue material for that owner, not this cluster.

## 2026-09-07 — D-2031 potion.c dodrink: Strangled gate + underwater + worn split + milky/smoky occupant rolls (3 moved)

**C locus:** `potion.c` `dodrink :526–615` (Strangled `:530–533`, fountain/sink/underwater `:535–572`, worn split `:591–598`, milky/smoky occupant `:601–613`); `ghost_from_bottle :481–500`; `objdescr_is` (`o_init.c:352–365`); `POTION_OCCUPANT_CHANCE` (hack.h:1409 `13 + 2 * born`); `Strangled` (youprop.h:110 `u.uprops[STRANGLED].intrinsic`); `Underwater` (youprop.h:279 `u.uinwater`).
**JS:** 2 files (`potion.js` +86/−12, `apply.js` +1/−1 export keyword), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (corpus sessions are the falsifier).
**Change:** `js/potion.js` — Strangled gate first (uprops intrinsic per the C macro, plus flat `u.Strangled` for the same C value per the `do.js` danger_uprops dual-store note); underwater `u.uinwater && !u.uswallow` yn prompt with C's «Do you know what lives in this water?» + TIME; worn-stack split (`splitobj(otmp,1)` + `owornmask=0`) or `await remove_worn_item(otmp,false)`; new `ghost_from_bottle` (makemon GHOST, empty-bottle / Blind-`something` / enormous-ghost incl. hallu `rndmonnam`, verbose fright + `nomul(-3)` + multi_reason/nomovemsg); milky-then-smoky occupant arms in C order with the `13 + 2*born` chance and `G_GONE` gates, `useup` + TIME. Imports: `STRANGLED`/`G_GONE` extend the pre-existing `./const.js` edge; `rndmonnam` extends the pre-existing `./do_name.js` edge (ALREADY, no new edge); `remove_worn_item` from `./steal.js` (`imports.mjs --can`: SAFE, hoisted function); `objdescr_is` imported from `./apply.js`, exported there this commit (was a local in 4 files — no clone #5). `PM_GHOST` joins the top-level `monsterNames` consts beside `PM_DJINNI`.
**Verify:** `node scripts/verify.mjs --fn dodrink` → `PASS syntax 2 changed js file(s): js/apply.js js/potion.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify dodrink: 0 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (Priest-92096 → `doturn`@149 was 118; Knight-91128 → `x_monnam`@82 was 79; Rogue-92137 → `mcalcmove`@26 was 23) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · full skipped (no shared file changed) · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap). Note: Knight/Rogue rngM ticked down slightly (6612→6609, 2657→2635) while their steps advanced under new owners — script verdict is PROGRESS with 0 worse; the occupant `rn2(13)` now draws where C draws it.
**Named:** none new — every arm's callee is live (`djinni_from_bottle` D-1144, `drinkfountain` D-0237, `drinksink` D-0434, `getobj` D-1834, `splitobj`, `remove_worn_item`, `dopotion`); `SetVoice` inside djinni stays named (D-1144).
**Next:** moved owners `doturn`@149, `x_monnam`@82, `mcalcmove`@26 join future queue rows for those owners (`x_monnam` refilled this commit).
