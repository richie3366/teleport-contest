# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-06 — D-1966 nhlsel.c l_selection_iterate selection-iterate Lua-C singleton

**C locus:** `nethack-c/upstream/src/nhlsel.c` — `l_selection_iterate` `:924–957` (`"iterate"` method table `:1002`, prototyped `:35`): argc==2 + LUA_TFUNCTION guard else `nhl_error("wrong parameters")`; `l_selection_check` + `selection_getbounds` (selvar.c); y-outer loop, x from `max(1,rect.lx)`, `selection_getpoint` guard; per-cell `cvt_to_relcoord` (sp_lev.c `:4793–4803`) before `nhl_pcall_handle(..., NHLpa_impossible)` with `goto out` abort; per-row `lua_gc`. Lua callers: bigrm-11.lua `:22/25`, knox.lua `:70`, nhlib.lua `:125`, themerms.lua `:56/84/97/112/260`.
**JS:** `js/mklev.js` (+17/−3; 1 js file, under 600 cap). Density note: C is 32 lines; the loop body already shipped under D-1821/D-1840/D-1853 — this iteration makes it the canonical exported port with the C parameter guard (small-C exception).
**Change:** `js/mklev.js` — exported `selection_iterate_lua` with the full C-contract header (`:924–957` + `:1002` + sp_lev.c `:4793–4803` citations; relcoord round-trip skipped per reviews 791/810 since des.* adds the origin back; pcall-abort/lua_gc named as Lua-VM omissions) and the C argc/TFUNCTION arm as `typeof fn !== 'function'` → `throw new Error('l_selection_iterate: wrong parameters')` (fatal like nhl_error, cf. find_objtype); loop body byte-identical (y-outer, `Math.max(1, sel.lx)`, pts-membership ≡ getpoint over in-range stored cells; empty early return ≡ C's full-map all-false walk). Same-module change, no new import edge; no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn l_selection_iterate` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mklev.js`; `PASS rule2`; `note hidden verify l_selection_iterate: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). Probe (/tmp, deleted): y-outer order with max(1,lx) floor, empty/null no-op, non-function throws 'wrong parameters', singleton rect visits exactly its cell — PROBE PASS.
**Named:** `l_selection_check` userdata/metatable validation (no Lua VM — JS callers pass Set-backed selections directly); `cvt_to_relcoord` live call (round-trip folded: match/getpoint/write all absolute; reviews 791/810 bless the skip); `nhl_pcall_handle` NHLpa_impossible error-abort (callbacks are sync; a throw propagates instead of aborting both loops); per-row `lua_gc(LUA_GCCOLLECT)` (no manual GC in JS); `selection_getbounds` dirty-recalc (JS bounds maintained live by setpoint; `selection_recalc_bounds` covers the sub case).
**Next:** pop the next Open row (`dbridge.c` do_entity).

## 2026-09-06 — D-1965 allmain.c init_sound_disp_gamewindows window-system init singleton

**C locus:** `nethack-c/upstream/src/allmain.c` — `init_sound_disp_gamewindows` `:699–763` (soundlib `:703`, splash/nosplash `SoundAchievement` `:705–710`, `CHANGE_COLOR` palette `:712–717`, `WIN_MESSAGE = create_nhwindow(NHW_MESSAGE)` `:719`, `VIA_WINDOWPORT() ? status_initialize(FALSE) : WIN_STATUS = create_nhwindow(NHW_STATUS)` `:720–724`, `WIN_MAP` `:725`, `WIN_INVEN = create_nhwindow(NHW_MENU)` `:726`, `adjust_menu_promptstyle` `:727–728`, `TTY_PERM_INVENT` perminv prepare `:730–735`, empty `start_menu`/`end_menu` Qt pacify `:738`, `display_nhwindow(WIN_STATUS)` `:753–755`, `display_nhwindow(WIN_MESSAGE)` `:756`, `clear_glyph_buffer()` `:757`, `display_nhwindow(WIN_MAP)` `:758`, `check_perm_invent_again` `:759–762`). Sole caller: `sys/unix/unixmain.c:217` after `vision_init`, before `attempt_restore` (comment at `win/tty/wintty.c:648` confirms askname runs between `init_nhwindows` and this function).
**JS:** `js/allmain.js` (+111/−4 incl. imports), `js/jsmain.js` (+6/−1); 2 js files, under 600 cap. Density note: C is 64 lines; the JS is the export plus the caller wire (one function cluster, Must-fix stays alone — this is Open so the wire rides).
**Change:** `js/allmain.js` — exported sync `init_sound_disp_gamewindows()` in C order (`| 0` int idiom; splash condition `iflags.wc_splash_screen && !flags.randomall` read but both `SoundAchievement` arms no-op; `WIN_MESSAGE = 10` / `WIN_STATUS = 11` else-arm (contest sets no `wincap2`, so `VIA_WINDOWPORT()` false like the tty build; true-arm keeps the `flags.botlx` tail) / `WIN_MAP = 12` / `WIN_INVEN = 20` sentinel ids matching the `invent.js WIN_INVEN_ID` stand-in; `opt_need_promptstyle = false` for the promptstyle relay; `WINDOWPORT(tty)`-as-tty + live `prepare_perminvent(WIN_INVEN)`; empty-menu pacify as consumed `menu_behavior` with no grid touch per D-1831; live `clear_glyph_buffer()` between the two named display no-ops) with the `:699–763` + per-arm citations; `js/jsmain.js` — wired once after `askname` defaults, before `try_restore_save` (C `unixmain.c:217` relative order). Existing edges only (`invent.js`/`display.js`/`const.js` already imported — `imports.mjs --can`: ALREADY); no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn init_sound_disp_gamewindows` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/allmain.js js/jsmain.js`; `PASS rule2`; `note hidden verify init_sound_disp_gamewindows: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed).
**Named:** `activate_chosen_soundlib` (`sounds.c:1779–1796`, no SND_LIB — cf. `exper.js`/`insight.js` debt); both `SoundAchievement` arms (`sndprocs.h:232–241`); `#ifdef CHANGE_COLOR` `change_palette` (`coloratt.c:1098–1108`, compiled out); `adjust_menu_promptstyle` ctrl relay (`windows.c:1769–1778`, headings read directly); `start_menu`/`end_menu` Qt pacify (no Qt window); three `display_nhwindow(..., FALSE)` paints (no pending More / no level yet; later `docrt`+`bot` paints); `#ifndef STATUS_HILITES` STATUS paint (compiled out — `STATUS_HILITES` defined); `check_perm_invent_again` (`options.c:5532–5542`, pending falsy at init).
**Next:** pop the next Open row (`nhlsel.c` l_selection_iterate).

## 2026-09-06 — D-1964 display.c row_refresh glyph-row repaint singleton

**C locus:** `nethack-c/upstream/src/display.c` — `row_refresh` `:2147–2186` (`force` from `map_glyphinfo(0, 0, GLYPH_UNEXPLORED, 0)` vs `nul_gbuf.glyphinfo` `:2163–2173`; loop `x = start..stop` reads `gg.gbuf[y][x].glyphinfo.glyph`, calls `get_bkglyph_and_framecolor(x, y, &bkglyph, &framecolor)` `:2178–2179`, calls `print_glyph(WIN_MAP, x, y, Glyphinfo_at(x, y, glyph), &bkglyphinfo)` iff `force || glyph != GLYPH_UNEXPLORED || framecolor != NO_COLOR` `:2180–2184`). Callers: `wintty.c docorner` `:3700`/`3704`/`3709` (`row_refresh(xmin ± clip - offx, COLNO - 1, y ± clip - offy)` after `tty_curs` + `cl_end`).
**JS:** `js/display.js` (+52/−6; 1 js file, under 600 cap). Density note: C is 39 lines; the JS is the export plus the caller rewire (small-C exception).
**Change:** `js/display.js` — exported `row_refresh(start, stop, y)` in C order (`| 0` int idiom; `force` false since JS nul (`clear_glyph_buffer` `' '`/`NO_COLOR`) renders identically to UNEXPLORED so the `map_glyphinfo`-vs-`nul_gbuf` comparison can never flip; `framecolor` always `NO_COLOR`; gate `force || glyph !== GLYPH_UNEXPLORED || framecolor !== NO_COLOR` kept in C shape; paint delegates to `_paint_gbuf_cell(xi, yy, xi - 1, yy + 1)` for C `print_glyph`/`Glyphinfo_at` at WIN_MAP offx 0 / offy 1) with the `:2147–2186` + `:2163–2173` + `:2178–2184` citations; `docorner` board loop rewired to `row_refresh(x0 + 1, COLNO - 1, y - 1)` (C `:3704` shape; `x0 + 1` is `xmin` at offx 0; `setCell` clips OOB like the tty). Same-module change, no new import edge (no TDZ read); no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn row_refresh` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2`; `note hidden verify row_refresh: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). Probe (/tmp, deleted): map (5,3) `@` paints screen (4,4) only, UNEXPLORED neighbors skipped, all-UNEXPLORED segment paints nothing, OOB/empty-range safe — PROBE PASS.
**Named:** `map_glyphinfo` tile/rogue/hero/accessibility arms (force source); `get_bkglyph_and_framecolor` background-glyph (SCORR/STONE/ROOM/CORR seenv) + `map_frame_color` frame arms; CLIPPING `clipx`/`clipy` start adjustment (caller passes map coords); TTY_PERM_INVENT / paging arms stay as named on `docorner`.
**Next:** pop the next Open row (`allmain.c` init_sound_disp_gamewindows).

## 2026-09-06 — D-1963 weapon.c give_may_advance_msg skill-advance message singleton

**C locus:** `nethack-c/upstream/src/weapon.c` — `give_may_advance_msg` `:76–84` (`You_feel("more confident in your %sskills.", P_NONE→"" / ≤P_LAST_WEAPON→"weapon " / ≤P_LAST_SPELL→"spell casting " / else "fighting ")` then `(void) handle_tip(TIP_ENHANCE)`). Callers: `use_skill` `:1432` (before/after `can_advance` around `P_ADVANCE += degree`) + `add_weapon_skill` `:1449` (`before < after` around `weapon_slots += n`). Callee arm completed in-cluster: `hack.c handle_tip` `:1858–1862` (`TIP_ENHANCE` → `pline("(Tip: use the #enhance command to advance them.)")`).
**JS:** `js/weapon.js` (+38/−8), `js/hack.js` (+9/−2), `js/pray.js` (+1/−1); 3 js files, under 600 cap. Density note: C is 8 lines; the JS is the export plus the two in-file wiring points and the callee arm the observable needs (small-C exception).
**Change:** `js/weapon.js` — exported `async give_may_advance_msg(skill)` in C ternary order (`| 0` int idiom; `await You_feel(...)` + `await handle_tip(TIP_ENHANCE)` since both callees can reach nhgetch; C's `(void)` only discards the boolean) with the `:76–84` citation; `add_weapon_skill` is now async and awaits it on `before < after` (sole caller `gcrownu` is async — one-word `await` in `js/pray.js`); `TIP_ENHANCE` added to the existing `./const.js` edge, `handle_tip` to the existing `./hack.js` edge (`imports.mjs --can`: both ALREADY). `js/hack.js` — added the missing TIP_ENHANCE arm first in C switch order with the `:1852–1882` citation (zero risk to SWIM/UNTRAP_MON arms; no prior caller passed TIP_ENHANCE). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn give_may_advance_msg` → VERIFY: PASS — `PASS syntax 3 changed js file(s)`; `PASS rule2`; `note hidden … no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). Probes (/tmp, deleted): 4 kind arms exact (`"You feel more confident in your skills."` P_NONE at --More-- capture; weapon / spell casting / fighting pendings), once-per-bit tips=1, `add_weapon_skill` fires 0→1 slot (`(Tip: …)` emitted) and stays silent with no crossing (pending=null) — PROBE PASS.
**Named:** `use_skill` may-advance arm stays unwired (weapon.js export + spell.js local clone) — both are sync and callers include sync hot paths (`hmon_hitmon_dmg_recalc`, `exercise_steed` via cmd move), so awaiting the async export needs an async cascade of its own; `handle_tip` TIP_GETPOS stays (lua NHCORE `l_nhcore_call`, Rule #2-hostile).
**Next:** pop the next Open row (`display.c` row_refresh).

## 2026-09-06 — D-1962 region.c inside_rect/inside_region rect-containment predicates singleton

**C locus:** `nethack-c/upstream/src/region.c` — `inside_rect` `:53–57` (`x >= lx && x <= hx && y >= ly && y <= hy`, "Should be inlined") + `inside_region` `:62–73` (`reg == 0 || !inside_rect(&bounding_box)` → FALSE, then per-rect `inside_rect` → TRUE, else FALSE). Callees: none (pure predicate). Callers (same file): `inside_region` at `:311`, `:334`, `:373`, `:488`, `:503`, `:517`, `:541`, `:556`, `:568`, `:588`, `:603`, `:725`, `:1069`.
**JS:** `js/region.js` (+22/−4; 1 js file, under 600 cap). Density note: C is 3 + 11 lines; the JS is the one predicate plus the caller-shape rewire of its sole live use (small-C exception).
**Change:** `js/region.js` — exported `inside_rect(r, x, y)` in C order (inclusive comparisons, `| 0` int idiom) with the `:53–57` citation; promoted `inside_region` to exported and rewired it to C shape (`!reg || !inside_rect(box)` short-circuit so the box is never read on null reg, then indexed per-rect `inside_rect` loop) with the `:62–73` citation. Box source is `reg.bounding_box` when stored, else the existing `region_bounding_box(reg)` recompute (C stores it at `create_region`; hoisted function declaration, no new import edge). Same-module change; no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn inside_rect` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/region.js`; `PASS rule2`; `note hidden verify inside_rect: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full` (tool: no shared file changed). Probe (inline `-e`, nothing to delete): inclusive edges true/true, outside false/false, null-reg→false (C FALSE; old JS threw), empty-rects→false, two-rect hit→true, outside-box miss→false, stored narrow box respected→false — PROBE PASS.
**Named:** `create_region` stored `bounding_box`/`nrects` not yet ported (JS regs carry `rects` only; `region_bounding_box` recompute stays — `inside_region` prefers a stored box when a future port provides one). No caller rewiring owed (all live callers are same-module `inside_region` users).
**Next:** pop the next Open row (`weapon.c` give_may_advance_msg).

## 2026-09-06 — Audit D-1953…D-1961 against pinned C (reviews 923–931) + cadence score

**Scope:** 9 JS-touching SHAs since audit 914–922 (`b4ae907a`), oldest-first, one file per SHA written as each finished. All verdicts **ACCEPT**, 0 Must-fix — LOOP-QUEUE Must-fix stays empty, Open stays 10 rows (no refill owed).
**Method per SHA:** `git show` hunks vs subject promise; `csym.mjs` body + `--callers` (ranges cited from tool output); `sym.mjs` on every new/re-pointed symbol; diff-body banned grep (FORCE/DIAG/getRngLog/fastforward/coords); `imports.mjs --rulecheck` clean across scored `js/`; `hidden-proxy verify <fn> --base HASH~1` re-measured all 9 → 0 blocked at baseline and HEAD (all D-log vacuous notes honest, no corpus-PASS claims made).
**Findings:** 928 verified the live `readmail` arm selection against build config (MAIL+DEF_MAILREADER defined, SIMPLE_MAIL off; fake-mail `:486` and VMS `:762` arms compiled out) — old JS printed the MAIL-undefined `#else` text, real wrong-arm fix. 924 caught a comment-rationale error (D-log says `A.S*` matches "un-lowered pass-1 alt"; read at HEAD, `alt` derives from lowered `dbase_str`, so the key is dead in C too — code outcome identical, noted not queued). 927's D-log discloses a measured adjacent defect NOT fixed (const.js `MELT_ICE_AWAY` string-vs-enum → `start_timer` stores action 0, ice-melt timers never fire) — pre-existing, needs its own Open row; not Must-fix from this audit. 929/930/931 `--can` edges all ALREADY-or-safe (body-use only, no TDZ read); 929 imports the real `has_head` export despite the pre-existing `shk.js:3856` clone.
**Cadence:** full `sessions` at `100914d5` → **44/44**, Scr 11,405/11,405, RNG 792,838/792,838 (identical to D-1952 audit), speed `72+0.48/turn` (R² 0.80). CURRENT Score refreshed.
**Next:** pop Open `region.c` inside_rect.
