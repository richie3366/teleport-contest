# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-16 — D-2423 `insight.c:1834–1856` + `:1892–1893` attributes_enlightenment Unchanging shape-change arms (Monk disclosure row 17)

**C locus:** `nethack-c/upstream/src/insight.c` attributes_enlightenment — `:1834–1836` Protection_from_shape_changers heads the block; `:1837–1856` `if (Unchanging)` (`!Upolyd` you_can + blocked-shape `if (Polymorph)…else if (ismnum(ulycn))` → `enl_msg(You_, buf, buf, " if not locked…")`, `else if (Polymorph)` you_are); `:1892–1893` `if (Unchanging && Upolyd)` after were-form. Display half: `win/tty/wintty.c` tty_putstr NHW_MENU — `compress_str` (len ≥ CO or `\n`: strip leading spaces, collapse runs, strip trailing) then word-wrap at CO (`n0 > CO`: break at last space before col 79, break space consumed, recurse; no space → stored whole). The 81-char Unchanging line is the first disclosure line long enough to reach this path (stored 68 + `unchanging.` fragment).
**JS:** `js/invent.js` (3 const imports; Protection + Unchanging blocks before `Polymorph_control`, `:1892` block after were-form/foreign-shape, both builders; doc-header + deferred-list comments updated); `js/pager.js` (+11-line storage-time expansion in `show_nhw_menu_text`). Map `docs/c-js-map/startup.md:24` (three deferred lists retired/landed).
**Change:** arms ported in C order on both builders — final `enlightenment()` (past tense via `final`, `you_are`/`enlght_line_txt` directly) and `doattributes()` ^X (in-progress tense via `o()` wrapper; C `!final` arms read `polymorph`/`change shape` since final == 0 there; `:1892` sits after foreign-shape, before Fast — C order among live arms). Predicates: dynamic `await import` of canonical `Protection_from_shape_changers` (`./were.js`) and `Unchanging` (`./polyself.js`) (`imports.mjs --can` → SAFE, hoisted functions, same per-arm dynamic shape as neighbours — no new static edge); Polymorph has no canonical export, so flat H/E + uprops inline, same predicate as `allmain.js` Polymorph. `UNCHANGING`/`POLYMORPH`/`PROT_FROM_SHAPE_CHANGERS` join the existing static `./const.js` import (no new edge).
**Verify:** `node scripts/verify.mjs --fn one_characteristic` tail: `PASS syntax (js/invent.js, js/pager.js)` · `PASS rule2` · `PASS hidden verify one_characteristic: 1 PASS, 0 moved past, 1 unchanged, 0 worse → PROGRESS` (scen-wish-Monk-92013 → **PASS**; scen-wish-Caveman-92148 still `one_characteristic` at step 245 — its first-diff row is D-2422's documented row-12 Grimtooth residual, untouched by this change) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Mid-iteration probe (deleted `/tmp/js135.mjs`, `/tmp/monk135.mjs`, `/tmp/csteps.mjs`): JS step-135 rows 0–23 byte-match the C capture incl. the 68 + `unchanging.` wrap. Full fortress `node frozen/ps_test_runner.mjs sessions` (shared `pager.js` changed): **44/44**.
**Named:** `lays_eggs` `:1879` (both builders), were-form on the ^X path (live on final path), Free_action/Fixed_abil, Slow_digestion/combat-inc/defense/half-damage (pre-existing, kept); `one_characteristic` not re-ported (parked MISATTRIBUTED); `turns.md:2593`'s overlapping Unchanging/Poly/Upolyd deferral is retired by this D — do not requeue from it.
**Next:** Caveman-92148 row-12 «You were poison resistant because of Grimtooth.» vs «You were poison resistant.» is a new `from_what`/artifact-attribution writer under parked `one_characteristic` (not added here — needs its own brief + measurement per refill rules).

## 2026-09-16 — D-2422 `mkobj.c:1932–1934` weight() Bag-of-Holding bless/curse factor (Caveman encumbrance Δ37)

**C locus:** `nethack-c/upstream/src/mkobj.c` weight() `:1932–1934` — `cwt = obj->cursed ? (cwt*2) : obj->blessed ? ((cwt+3)/4) : ((cwt+1)/2)` (uncursed), inside the `Is_container || STATUE` branch after the recursive cwt sum; the C comment names `pickup.c` DELTA_CWT/`delta_cwt` as the twin implementation.
**JS:** `js/mkobj.js` only (const + 6-line arm; no new import — `objectNames` already imported, so no `imports.mjs --can` edge). New durable test `scripts/weight-boh.test.mjs` (5 its: blessed 28 = the corpus case, uncursed 40, cursed 115 + cursed-beats-blessed order, SACK sums unmodified 65, empty bag + cwt=4 rounding 16/17).
**Change:** the three divisor arms in C order (cursed first, C ternary short-circuit; `Math.trunc` for the round-up divisions) + a module-level `BAG_OF_HOLDING` const via `objectNames.indexOf` (same shape as `STATUE`); doc header now cites `:1914–1939` / D-2422.
**Verify:** `node --test scripts/weight-boh.test.mjs` 5/5 PASS; pre-fix stash check 1 pass/4 fail (the test pins the arms). `node scripts/verify.mjs --fn one_characteristic` tail: `FAIL hidden verify one_characteristic: 0 PASS, 0 moved past, 2 unchanged, 0 worse → NO MOVEMENT` (owner+step granularity) + `PASS green 2/2`, `PASS strict` ×2, `PASS cohort 7/7`. Row-level movement past the cited divergence: Caveman-92148 first-diff row 5 (encumbrance <-247>/<-210>) → row 12 (C «You were poison resistant because of Grimtooth.» vs JS «You were poison resistant.» — a different disclosure writer under the same parked symptom owner); rows 0–11 incl. the encumbrance line now match with only this `js/` change.
**Named:** STATUE corpsenm/msize/minwt arms (C `:1919–1931`) still absent from JS `weight()`; HEAVY_IRON_BALL `owt != 0` kludge still deferred (in-code comment); C `impossible()` quan<1 diagnostic stays silent-0 (pre-existing). `one_characteristic` not re-ported (parked MISATTRIBUTED); `inv_weight` untouched (Δ fully accounted by the bag).
**Next:** Monk-92013 → the already-queued `attributes_enlightenment` Unchanging row; Caveman row-12 Grimtooth-attribution disclosure line is a new writer candidate under parked `one_characteristic` (not added here — needs its own brief + measurement per refill rules).

## 2026-09-16 — D-2421 `makemon.c:1502–1504` makemon occupation `dochugw` arm (Barbarian search split)

**C locus:** `makemon.c:1476–1504` (`!gi.in_mklev`: `newsym`, then `!MM_NOMSG`-gated appear `Norep`, then — outside the NOMSG guard — `if (go.occupation) (void) dochugw(mtmp, FALSE)`) via `wizard.c:599` (`nasty`: `mmflags = summoner ? MM_NOMSG : NO_MM_FLAGS`) via `mcastu.c:422–446` (`mcast_summon_mons` prints «Monsters appear from nowhere!» after `nasty`). C `monmove.c` `dochugw(mtmp, FALSE)`: chug FALSE skips the move, only the newly-spotted threat check can `stop_occupation`.
**JS:** `js/makemon.js:181` (import), `:3485–3545` (tail restructure + arm); `js/wizard.js:7` (import), `:123–143` (both `nasty` arms); `scripts/makemon-dochugw.test.mjs` (new); map `docs/c-js-map/data.md:484–486`.
**Change:** `js/makemon.js` only for the arm — `dochugw` added to the existing static `./monmove.js` import (`imports.mjs --can` → ALREADY, hoisted `async function`, no new edge); `makemon_appear_msg` restructured so the appear `Norep` stays `!MM_NOMSG`-gated while the occupation arm runs whenever `!in_mklev` and an occupation is set (`typeof game.occupation === 'function'` gate + `await dochugw(mtmp, false)`, same shape as `rloc_maybe_occupation`), in C order after the appear block. `js/wizard.js` `nasty` awaits the tail after each successful `makemon` (primary + geno-substitute arms) BEFORE zeroing `msleeping/mpeaceful/mtame`, so the threat check sees birth values exactly as C's in-body call does. New `scripts/makemon-dochugw.test.mjs` (3 its: hostile MM_NOMSG birth stops searching — failed pre-fix; peaceful birth keeps occupation; no occupation no-ops).
**Verify:** `node --test scripts/makemon-dochugw.test.mjs` → 3/3 (stop-searching it failed pre-fix, proven above). `node scripts/verify.mjs --fn mon_adjust_speed` → PASS syntax (2 files: makemon.js, wizard.js) · rule2 · hidden PROGRESS (scen-tour-Barbarian-92079 → PASS; 0 moved past/unchanged/worse) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed).
**Named:** `set_msg_xy` in the appear block (pre-existing, kept); `unmakemon` geno-substitute kill (pre-existing JS null-mark, kept — the arm runs before it, as C's in-body call runs before `unmakemon`); sync-only callers (above); no seed/step/coordinate logic read.
**Next:** hidden re-score on an audit iter should hold Barbarian-92079 at PASS; the Open `mktrap` Ranger row and D-2420 W1–W6 rows are untouched by this change (all PASS/verify clean here).

## 2026-09-16 — [measure] distfleeck residual writers delivered as D-2420 (no js/)

Queue `[measure]` head (7 distfleeck-owned sessions): recorder RNG-tag step dumps (no temp instrumentation — tags ARE the dump) + JS flat first-mismatch/slice probe (`/tmp/distfleeck-probe.mjs`, deleted). 5 clean single-draw shifts + 2 branch divergences → 6 writer Open rows W1–W6 (D-2420); `[measure]` row marked done + archived; commit+push, no finish-iteration. `verify distfleeck` 7 unchanged (expected); green gate PASS. Queue band 8−1+6=13 (one over; pop in order).

## 2026-09-16 — D-2419 `dungeon.c` `ledger_to_dnum` bottom-level spill via `migrate_orc` ORC_LEADER (Healer orc-captain arrival)

**C locus:** `dungeon.c:1403–1414` (`ledger_to_dnum`: `ledger_start < ledgerno && ledgerno <= ledger_start + num_dunlevs`; `ledger_no` `:1376–1378` is `dlevel + ledger_start`, so valid ledgers run start+1..start+num) via `dog.c:887–926` `migrate_to_level` (`:909–910` decode → `:924–925` `mux=new_lev.dnum, muy=new_lev.dlevel`) via `mkmaze.c:717–745` `migrate_orc` leader arm (`nlev=max_depth`, `!rn2(40)` rare −1, `MIGR_LEFTOVERS`; sole leader caller `:851`, gang caller `:885`).
**JS:** `js/teleport.js:2733–2747`; `scripts/ledger-to-dnum.test.mjs` (new); map `docs/c-js-map/turns.md:3257` (new line).
**Change:** `js/teleport.js` only — local condition → `start < want && want <= start + n` with a `|0` coercion (same shape as `js/dungeon.js:718–729`), C-order comment citing `:1408–1411`. No new import (teleport↔dungeon stay decoupled); `ledger_to_dlev` local already matches C. New `scripts/ledger-to-dnum.test.mjs` (3 its: bottom-ledger→own bottom, interior unchanged, next-dungeon head unchanged).
**Verify:** `node --test scripts/ledger-to-dnum.test.mjs` → 3/3 (bottom-boundary it failed pre-fix: mux=next-dungeon). `node scripts/verify.mjs --fn migrate_orc` → PASS syntax (1 file: teleport.js) · rule2 · green 2/2 · strict ×2 · cohort 7/7; hidden vacuous for `migrate_orc` (nothing blocked ON it at HEAD — recorded owner is `rloc`). Live single-session replay `node frozen/ps_test_runner.mjs .cache/hidden/sessions/scen-tour-Healer-92042.session.json` → PASS RNG 18888/18888 Screen 95/95 (was step-73 rloc-vs-mcalcmove).
**Named:** leash `mtame--`/`m_unleash` + light-source `vision_recalc` inside `migrate_to_level` (pre-existing, doc-kept); no new omission.
**Next:** hidden re-score on an audit iter should take Healer-92042 to PASS (rloc ×2 → ×1); Ranger-92033 already sits at mktrap@98 (queued writer row, untouched here).

## 2026-09-16 — Audit reviews 1386–1390 (bbe672c4..c2935846) + cadence 44/44, proxy 495/540
**Reviews:** 5 JS-touching SHAs since 1385, oldest-first, each re-measured at its parent baseline — all **ACCEPT**, 0 Must-fix (no `LOOP-QUEUE.md` prepend, `CURRENT.md` Next cluster unchanged: W5 `doopen_indir`). 1386 mk_bubble ini boing (C `mv_bubble:2088–2106` switch verbatim; Tourist-92100 → level_tele@132). 1387 can_fog→mfndpos door (export + disjunct; both Valkyries moved past; clone deferral now 2-site, named). 1388 mktrap victim-gate burn (2 tail calls; Ranger-92033 PASS; `maketrap` returns ttmp confirmed). 1389 getdir trailing confdir centralized (4-caller draw parity verified exit-by-exit; Samurai-92239 PASS; Tourist-92061 extra move is D-2431's). 1390 fire-trap xtradmg guard (nested under mhp>0 = !DEADMONSTER; Tourist-92061 → doread@17). check-hot-docs --fix: 5× ok (89–104 L), no cap edit.
**Cadence:** full `sessions` 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, `48+0.29/turn` R² 0.79). Proxy rescore `score --jobs 8`: 495/540 PASS (91.7 %) excl. 13 env — up 2 (m_move → 0 blocked, mktrap + 3 distfleeck cleared); top owners distfleeck ×4 / do_statusline2 ×4, rest singles.
**Refill:** queue --limit 30 → 0 untagged-eligible (22 shown: open/parked/archived); parks' writers/[measure] already queued (W2/W3 rows Open, all 4 distfleeck + 3 do_statusline2 sessions covered); no [campaign] row exists; no new top-owner [measure] warranted. Queue sits at 7 Open — append nothing per eligibility rules.
