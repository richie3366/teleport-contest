# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-09 — D-2231 `mon.c` mondead tail: lifesaved/vamprises/grddead/logdeadmon/full m_detach (queue row `mon.c` mondead tail, no corpus block)

**C locus:** `mon.c` `lifesaved_monster` `:2838–2884`, `vamprises` `:2888–2987`, `logdeadmon` `:2995–3068` (+`livelog_mon_nam` macro `:2991`, `set_mon_min_mhpmax` `:2806–2823`), `mondead` `:3081–3177`, `m_detach` `:2734–2803`, `monstone` lifesave `:3301–3303`, `monkilled` disintegested `:3398`; `vault.c` `grddead` `:174–189`; `steal.c` `thiefdead` `:119–128` + `relobj` `:874–898`; `shk.c` `shkgone` `:234–269`.
**JS:** 8 files (`js/mhitm.js` +~400/−~40, `js/mon.js` +14/−4, `js/mhitu.js` +2/−2, `js/uhitm.js` +3/−2, `js/trap.js` +1/−1, `js/muse.js` +1/−1, `js/vault.js` +2/−2, `js/shk.js` +1/−1), under the 600/10 caps.
**Change:** `js/mhitm.js` — new `set_mon_min_mhpmax` (m_lev+1 floor then caller minimum), async `lifesaved_monster` (cansee-gated plines, `mlifesaver`/`m_useup_mm`/`attacktype_mm` locals, `makeknown`, `check_gear_next_turn`, `wary_dog`, genocided arm), async `vamprises` (G_GENOD gate, Unaware-dream action via house `u.Unaware`, `expels`/`uunstick`, `newcham`, rise pline + `vamp_rise_msg`, closed-door smash with C-ordered You_hear/You_see/pline_The + D_NODOOR + recalc), async `grddead` (vault `clear_fcorr`/`parkguard` exports, isgd gold-vanish + drop-rest, park+retry, isgd clear), sync `logdeadmon` (Medusa achievement, G_UNIQ/shopkeeper livelog cadence, `shtypes`/`ESHK`/SHOPBASE detail, `ordin` xtra), sync `thiefdead` (stealmid reset), sync `shkgone` (resident, fobj no_charge loop, `setpaid`+bill+ushops via `shk.js` export), async `m_detach` in exact C order (unleash, `emits_light`/`del_light_source`, `mon_leaving_level`, wizdead, nemesis/leader/relobj+gated newsym, stealmid-guard, shk, wormgone, endgame, MON_DETACH+purge, usteed dismount), async `mondead` in exact C order (lifesave→vamp→be_sad→vortex `rn2(10)+5`→grddead→mptr save→restore→mvitals→quest→mail→Kops→logdeadmon→unmap→m_detach); `monstone` lifesave + `monkilled` `game.disintegested` wired; all callers await (`muse/mhitm/mhitu/uhitm/trap/mon`). `mon.js` exports `mon_leaving_level`; `vault.js` exports `clear_fcorr`/`parkguard`; `shk.js` exports `setpaid`. MS_NEMESIS/MS_LEADER are file-local consts (artifact.js:127 precedent).
**Verify:** `node scripts/verify.mjs --fn mondead --full` → PASS syntax (8 changed js files) · PASS rule2 · note hidden (vacuous: nothing blocked at HEAD; row cited no blocks so no --base re-run; ship on public gates) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44. VERIFY: PASS. First full run regressed 42/44 (seed0360@368 ghost `1`, seed4500@1048 ghost `I`, both screen-only, RNG fully matched): `m_at`'s liveness filter made `mon_leaving_level`'s onmap false on the death path so grid removal + unconditional newsym never ran while the relobj newsym went cansee-gated.
**Named:** `stinky_nemesis` gas + `nemesis_stinks` call (quest-text `com_pager_core` dependency); `minimal_monnam` format in the impossible arm (`mon_nam` stands in); `thiefdead` stealarm arm (occupation never set; steal.js:379); `shkgone` `discard_damage_owned_by` + `has_shop`/`search_special` arms; `mongone`'s `m_detach(FALSE)` caller (D-1149 body kept); `vamprises` trapped-door `mb_trapped` + destroy pline (monmove.js:1035 clone omits mondead); xkilled-side `disintegested=nocorpse` writer + Maybe-not/vamp_rise readers (uhitm header names them); `kill_genocided_monsters` stays sync (fire-and-forget; genocided mons cannot lifesave/vamprise; minvent-egg pass runs pre-drop — equivalent kill set via the fobj pass).
**Next:** row archived; `mon.c` mondead family fully live except the named arms above (re-queue per arm only on a falsifier).

## 2026-09-09 — D-2230 `eat.c` maybe_finished_meal eatfood-completion predicate + `steal.c:371` call-site wiring (queue row, 0 blocked)

**C locus:** `eat.c:3877–3889` `maybe_finished_meal(stopping)` — `if (go.occupation == eatfood && svc.context.victual.usedtime >= svc.context.victual.reqtime) { if (stopping) go.occupation = 0; /* for do_reset_eat */ (void) eatfood(); return TRUE; } return FALSE;` (eatfood() calls done_eating() to use up victual.piece). Callers: `allmain.c:687` `stop_occupation` (`if (!maybe_finished_meal(TRUE)) You("stop %s.")`) and `steal.c:370–371` (`if (go.occupation) (void) maybe_finished_meal(FALSE)` with the "food already used up but not yet removed" comment).
**JS:** 2 files (eat.js +29, steal.js +5/−2: 1 import line + 3-line gate replacing the 1-line deferred comment), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (one /tmp-free inline `node -e` import check only: both exports load as functions, false-path `maybe_finished_meal(false/true)` → `false false` with no occupation).
**Change:** `js/eat.js` — new `export async function maybe_finished_meal(stopping)` after `cant_finish_meal`, C branch/short-circuit order (occupation → usedtime>=reqtime → stopping-clear → eatfood → TRUE/FALSE); must live here — the gate compares against the module-local `eatfood` identity (same constraint as `cant_finish_meal`, D-2223, and `eating_dangerous_corpse`, D-2229). C `go.occupation = 0` → JS `null` idiom (as in done_eating / hack.js stop_occupation); `|0` on usedtime/reqtime (as in eatfood); `await eatfood()` for C `(void) eatfood()`; no RNG in the gate itself. `js/steal.js` — `steal.c:367–371` wired in C position (after `Some_Monnam`, before `inv_cnt`): `if (game.occupation) await maybe_finished_meal(false)` with C's comment rationale; import joins the pre-existing `eat.js` edge (`imports.mjs --can steal.js eat.js maybe_finished_meal` → ALREADY, function binding read lazily inside `steal()` only — no top-level TDZ read).
**Verify:** `node scripts/verify.mjs --fn maybe_finished_meal` → PASS syntax (2 changed js files: js/eat.js js/steal.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap). Density note: C is 12 lines; the 34-line js/ diff is the complete missing behavior (predicate + the one non-deferred caller), not padding.
**Named:** `allmain.c:687` `stop_occupation(TRUE)` gate stays deferred per the lembas park (`botl.c` do_statusline2 pair — JS `hack.js` `stop_occupation` keeps its `You stop …` message path; turns.md + hack.js header already name the deferral, untouched). `do_reset_eat` 0-occupation path untouched.
**Next:** do not re-pop maybe_finished_meal (now live with its non-deferred caller wired). A future first-diff at meal completion attributes to `stop_occupation`/`do_reset_eat` or the `eatfood`/`bite`/`done_eating` arms, not this predicate. Do not invent a FAIL peel.

## 2026-09-09 — D-2229 `timeout.c` nh_timeout ACID_RES/STONE_RES expiry: meal-extension + "no longer feel safe/secure" (queue row, 1 blocked)

**C locus:** `timeout.c` nh_timeout ACID_RES + STONE_RES expiry arms — `if (!Acid_resistance) { if (eating_dangerous_corpse(ACID_RES)) { set_itimeout(&intrinsic, 1L); break; } if (!Unaware) You("no longer feel safe from acid."); }` and the same shape for STONE_RES with `You("no longer feel secure from petrification.")` + the `wielding_corpse(uwep/uswapwep, NULL, FALSE)` pair (`do_wear.c:606–643`: petrify when bare-wielding a touch-petrifying corpse as resistance lapses). Callee `eating_dangerous_corpse` (`eat.c:472–493`): `occupation == eatfood` && piece is a carried/floor CORPSE with `corpsenm >= LOW_PM` && (`acidic` for ACID_RES / `flesh_petrifies` — Medusa included — for STONE_RES).
**JS:** 2 files (eat.js +24, timeout.js +52/−2: flats, header, two arms), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (session replayed via the frozen runner only).
**Change:** `js/eat.js` — new `export function eating_dangerous_corpse(res)` after `givit`, C branch/short-circuit order (occupation → piece → CORPSE → LOW_PM → carried/obj_here → res-specific `acidic(mons)/flesh_petrifies(mons)`); must live here — the gate compares against the module-local `eatfood` identity (same constraint as `cant_finish_meal`, D-2223). `js/timeout.js` — `[ACID_RES]/[STONE_RES]` join TIMEOUT_FLAT (single-storage mirror, FROMFORM/FROMOUTSIDE bits disjoint from the TIMEOUT mask so form resistances never tick down); both expiry arms in the generic loop in C order (resistance-idiom gate → extension via potion `set_itimeout` slot + flat mirror → `Unaware()`-gated `pline`). Edges extend pre-existing static imports only (`eat.js`: Unaware + eating_dangerous_corpse; `potion.js`: set_itimeout — all `imports.mjs --can` ALREADY, no new edge).
**Verify:** `node scripts/verify.mjs --fn nh_timeout` → PASS syntax (2 changed js files: js/eat.js js/timeout.js) · PASS rule2 · PASS hidden (1 PASS: scen-wish-Rogue-91119 fully PASS, RNG 6704/6704 + Screen 275/275) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** STONE_RES `wielding_corpse(uwep/uswapwep, NULL, FALSE)` pair stays deferred (`do_wear.c:606–643` needs instapetrify/killer_xname/gloves-name surface; fires only when bare-wielding a petrifying corpse exactly as resistance lapses — no corpus session reaches it; turns.md allmain section + timeout.js header). FIRE_RES/WWALKING/DISPLACED/WARN_OF_MON/PASSES_WALLS/MAGICAL_BREATHING/PROT_FROM_SHAPE_CHANGERS expiry messages stay silent (pre-existing deferral, untouched).
**Next:** do not re-pop nh_timeout for 91119 (PASS). A future first-diff at these arms attributes to `wielding_corpse` (cockatrice-wield lapse) or the Unaware/multi path, not the message/extension. Do not invent a FAIL peel.

## 2026-09-09 — D-2228 `dig.c`/`zap.c` falling-rock lifesave resume: rock still lands after done() returns (queue row, 1 blocked)

**C locus:** `dig.c:1596–1604` zap_dig u.dz arm — `dmg = rnd(hard_helmet(uarmh) ? 2 : 6)`; `losehp(Maybe_Half_Phys(dmg), "falling rock", KILLED_BY_AN)`; then UNCONDITIONALLY `otmp = mksobj_at(ROCK, u.ux, u.uy, FALSE, FALSE)` + `xname`/`stackobj`/`newsym`. Death inside losehp runs done()→savelife, and done() RETURNS after lifesave, so zap_dig resumes and the rock lands (its o_id is the next_ident draw). Twin: `zap.c:3310–3320` striking-up arm («A rock is dislodged...», same dice/killer, same unconditional rock after losehp; C comments it "similar to zap_dig()").
**JS:** 2 files (dig.js +9/−2 incl. comment correction, zap.js +5/−1), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. Diagnostic probes lived under /tmp only, deleted before handoff.
**Change:** `js/dig.js` + `js/zap.js` only, control-flow, no new imports/edges (mksobj_at/xname/stackobj/newsym already imported at both sites): after `finish_losehp_done()`, return only if `game.program_state?.gameover` (true death via really_done); on lifesave (done() cleared gameover, end.js:1732) fall through to the existing rock creation in C order. The other two zap.js losehp sites already resume correctly (1672 falls through to `return dmg`; 2055 is function-end) — untouched.
**Verify:** `node scripts/verify.mjs --fn next_ident` → PASS syntax (2 changed js files: js/dig.js js/zap.js) · PASS rule2 · PASS hidden (1 PASS: scen-death-Wizard-92187 fully PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. No-regression argument: the changed lines execute only post-fatal-rockfall; true-death path is byte-identical (`return` preserved); a lifesave-after-rockfall previously skipped C-mandated creation, so no passing session could contain one. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** none new — u.dz swallowed-pierce + pitdig conjoined/adj_pit_checks/pit_flow stay named (turns.md:1793, untouched); zap striking drawbridge/closeholdingtrap arms untouched.
**Next:** do not re-pop next_ident for 92187 (PASS). A future first-diff at next_ident attributes to the actual creating caller (makemon/clone_mon/mkobj/splitobj/shk-bill/restore), not the ident counter. The zap striking-up lifesave path has no corpus coverage (fixed by C-symmetry with the measured dig arm) — a later divergence there attributes to zap.c:3310–3320, not this D. Do not invent a FAIL peel.

## 2026-09-09 — D-2227 `worn.c` m_dowear_type !creation wear plines + artifact_light/invis arms (queue row, 1 blocked)

**C locus:** `worn.c:798–1002` `m_dowear_type` — `:809` sawmon/sawloc capture at entry; `:920–951` the `!creation` message block (`distant_name(old/best, doname)`, strcmpi-identical → `strsubst "a|an " → "another "`, `pline_mon "%s%s puts on %s."`, autocurse `pline "%s %s %s %s for a moment."` via `s_suffix`/`simpleonames`/`otense("glow")`/`hcolor(NH_BLACK)`); `:955–961` old-arm `artifact_light` → `end_burn`; `:966–984` best-arm `artifact_light && !lamplit` → `begin_burn` + `vision_recalc(1)` + four-way shine pline (`sawmon`/`canseemon`/`sawloc`/else); `:987–996` invis flip `!creation && (sawmon ^ canseemon)` → `cannot see` + `makeknown` (else-branch commented out in C).
**JS:** 4 files (`worn.js` +92/−32, `mon.js`/`end.js`/`minion.js` +1/−1 each), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/worn.js` — `m_dowear_type`/`m_dowear`/`maybe_m_dowear_special` async (awaits only on the !creation message path; creation callers run sync-through, no await reached); sawmon/sawloc at entry in C order; puts-on + autocurse-glow plines with the `another` substitution (strcmpi/strncmpi inline — no exported home, local clones only — `strsubst` canonical import); old/best `artifact_light` end/begin_burn arms; four-way shine pline; invis `cannot see` + `makeknown`. New edges all `imports.mjs --can` checked: objnam (`distant_name/doname/simpleonames/otense/Yname2/arti_light_description` → ALREADY), timeout (`artifact_light/begin_burn/end_burn` → SAFE hoisted-function), invent (`makeknown` → SAFE), vision (`vision_recalc` extends existing edge), do_name (`hcolor`), hacklib (`strsubst`), const (`Something`); local `NH_BLACK = 'black'` (cf. potion.js/read.js). Async callers updated: `mon.js:2792` + `end.js:1477` + `minion.js:535` await; sync creation callers (`makemon.js:3092`, `mplayer.js:307`) unchanged.
**Verify:** `node scripts/verify.mjs --fn m_dowear_type` → PASS syntax (4 changed js files) · PASS rule2 · PASS hidden (1 PASS: scen-intrinsic-Samurai-92017 fully PASS, RNG 7803/7803 + Screen 143/143) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** W_ARMC `!See_invisible` selection guard still deferred (JS treats as !See_invisible — untouched, gloves path unaffected); dragon-scale altprop beyond alchemy smock; `extract_from_minvent` artifact_light/obj_no_longer_held (pre-existing header omits, untouched).
**Next:** do not re-pop m_dowear_type wear arms (all four envelopes live). A future monster-wear divergence attributes to selection (`extra_pref`/ARM_BONUS), the ARMC See_invisible guard, or `update_mon_extrinsics`, not these arms. Do not invent a FAIL peel.

## 2026-09-09 — D-2226 `extralev.c` roguecorr impossible wall checks + `sp_lev.c` LVLINIT_ROGUE dispatch (queue row, 0 blocked)

**C locus:** `extralev.c:62` (`down: no wall`), `:68` (`down door ... going nowhere`), `:84` (`up: no wall`), `:104` (`down: no wall` verbatim in the XL_RIGHT arm), `:110` (`right door ... going nowhere`), `:126` (`left: no wall`), `:134` (`corridor in direction %d?`), `:270–273` (XL_LEFT/XL_UP never-connected); `sp_lev.c:2986–3003` (`default → impossible`, `LVLINIT_NONE → break`, `LVLINIT_ROGUE → makeroguerooms()`); `sp_lev.h:38–46` enum order; `do_name.c:1428` ROGUEOPTS env read (not portable, stays named).
**JS:** 2 files (`extralev.js` +33/−6, `mklev.js` +13/−5 net), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/extralev.js` — `IS_WALL` joins the existing `const.js` edge; `impossible` joins a new static `display.js` edge (`imports.mjs --can extralev.js display.js impossible` → SAFE, hoisted-function; fire-and-forget without await in the sync level-gen path, same shape as `mklev.js:1525`). All nine guards in C position/order with verbatim formats (RIGHT from-door keeps C's `down: no wall` label); second `if` → `else if` + `else impossible('corridor in direction %d?')`. `js/mklev.js` — LVLINIT_NONE/LVLINIT_ROGUE imports, `default → await impossible` (draw-free, in the async function), `NONE → break`, `ROGUE → makeroguerooms()` between MAZE and MINES in C order (`makeroguerooms` already imported).
**Verify:** `node scripts/verify.mjs --fn makeroguerooms` → PASS syntax (2 changed js files: js/extralev.js js/mklev.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Guards are draw-free and unreachable on valid levels (rogue callers pass only DOWN/RIGHT; miniwalk never strands a LEFT/UP end), so zero fortress movement is the expected C-faithful result. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** ROGUEOPTS `name=` env parse (`do_name.c:1428`) stays deferred — no environment in scored `js/` (Rule #2; same class as the `mail.js`/`files.js` getenv omits); the `rn2(3)` designer-name fallback arm was already live. `dodoor` flag/dualmask shape untouched (out of this row).
**Next:** do not re-pop LVLINIT_ROGUE / roguecorr guards (dispatch + all nine guards live). A future rogue-level divergence attributes to room-gen RNG or `dodoor`, not these arms. Do not invent a FAIL peel.
