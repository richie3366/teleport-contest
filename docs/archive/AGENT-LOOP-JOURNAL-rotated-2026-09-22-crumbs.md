# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-21 — D-2741 `read.c` doread FORTUNE_COOKIE arm consume via live `useup` (review 1688 Must-fix)

**C locus:** `nethack-c/upstream/src/read.c:365–377` (cookie arm; `:377 useup(scroll)`) + live `useup` `invent.c:1320–1333` (quan>1 → `update_inventory()`, else `useupall` = `setnotworn` + `freeinv` + `obfree`).
**JS:** `js/read.js:2204–2208`.
**Change:** the cookie arm now calls `useup_live(scroll)` (live `useup` `js/invent.js:4596`, already imported at `js/read.js:107`) with a C-cite comment; no new cross-module edge.
**Verify:** `node scripts/verify.mjs --fn outrumor` → VERIFY: PASS (syntax, Rule #2, hidden note 0 blocked, smoke 24/24 REACH-OK, green 2/2, strict 2/2, cohort 7/7). `node scripts/verify.mjs --fn doread` → syntax PASS, Rule #2 PASS, green 2/2, strict 2/2, cohort 7/7, smoke 24/24 REACH-OK; hidden NO MOVEMENT on 1 pre-existing block (scen-normal-Tourist-92061 step 17/169 screen-first at read.c:622: C «As you pronounce the formula…» vs J «As you read the scroll…» — C's Blind arm vs JS non-Blind, i.e. a Blind-state writer upstream of doread, symptom-owner class, phase 2; identical at baseline, 0 worse). No shared file changed → full skipped.
**Named:** remaining local-clone `useup` call sites in `js/read.js` (seffects tails) stay named clone debt; other `doread` deferred arms per the map section (shirt/credit/marker/coin/orb/candy reads, Braille arms).
**Next:** pop next Open — coverage row.

## 2026-09-21 — D-2740 `apply.c` jump whole body (known_spell fallback, guard chain, utrap switch, hurtle_jump path)

**C locus:** `nethack-c/upstream/src/apply.c:1988–2164` (`:1993–1995` known_spell #jump fallback; `:1997–2006` nolimbs/slithy + !Jumping; `:2007–2010` stucksteed; `:2011–2038` uswallow/uinwater/ustuck; `:2039–2045` Levitation/air/water; `:2046–2057` encumbrance, hunger/strength, Wounded_legs, steed trap; `:2059–2069` getpos prompt + is_valid_jump_pos + steed in-place; `:2075–2112` utrap escape switch + reset_utrap; `:2116–2136` same-spot arms; `:2138–2163` range + walk_path hurtle_jump + teleds/nomul/morehungry) + callee `dothrow.c:742–752` hurtle_jump (EWwalking I_SPECIAL around hurtle_step). C callers: `:1850` dojump, `spell.c:1585` spelleffects SPE_JUMPING (`dothrow.c:756` comment-only, `dbridge.c:549` debugpline — no call).
**JS:** `jump` js/apply.js:5488; `hurtle_jump` + `walk_path_async` js/dothrow.js:2950/2891; `dojump` unchanged js/apply.js:5476.
**Change:** restarted `jump` in C order with per-arm `:line` cites; new same-SCC runtime edges (imports.mjs SAFE) `known_spell`/`spe_Fresh`/`spelleffects` (+ exported `SPE_JUMPING` js/spell.js:282), `stucksteed`, `slithy`, `Flying`, `hliquid`/`YMonnam`, `deltrap`/`set_wounded_legs`/`legs_in_no_shape`, `walk_path_async`/`hurtle_jump`; const adds TT_BEARTRAP/TT_WEB/TT_LAVA/TT_INFLOOR/FORCETRAP/TOOKPLUNGE/LEFT_SIDE/RIGHT_SIDE/UNENCUMBERED/LEG. `hurtle_jump` ported in this commit (js/dothrow.js:2950, C :742–752 shape: save/set I_SPECIAL, await hurtle_step, restore); `walk_path_async` (js/dothrow.js:2891) is a JS-side async twin of the sync Bresenham walk (same order + early-stop dest mutation) since C's sync callback is async in JS. Levitation uses the youprop.h shape (artifact.js:386 precedent); Wounded_legs reuses the file's 4554 shape.
**Verify:** `node scripts/verify.mjs --fn jump` → PASS: syntax (3 files), Rule #2, hidden note (0 blocked — coverage row), REACH-OK 12/12 baseline-PASS sessions reach it, green 2/2, strict 2/2, cohort 7/7.
**Named:** none new — whole C body live; every callee live (29/29 brief list + nolimbs/slithy/Flying/hliquid/YMonnam/deltrap/set_wounded_legs/legs_in_no_shape/stucksteed/known_spell/spelleffects) or ported here (hurtle_jump). C staticfns check_jump/is_valid_jump_pos/get_valid_jump_position/display_jump_positions stay file-local (js/apply.js:5322–5449), matching their C staticfn shape.
**Next:** pop next Open — coverage row (queue back to 6 after archive; `--rows 300` @ 289e5ec2c re-emits only Stale-parked/archived/done/parked/dupes + phase-2 `do_statusline2` — no eligible refill, next iter sweeps deeper).

## 2026-09-21 — D-2739 `hack.c` moverock_core whole body (trap switch, shop costly, verysmall, revive_nasty, pool, fobj relink)

**C locus:** `nethack-c/upstream/src/hack.c:348–638` (`:358–363` Blind feel; `:365–372` next_boulder; `:376` movobj top; `:382–413` nopick; `:415–424` Levitation; `:426–431` verysmall; `:432–436` clear gate; `:438` costly; `:442` Sokoban diagonal; `:450` revive_nasty; `:461–479` monster-behind; `:485` closed_door; `:494` disturb; `:496–504` trap intro; `:505–528` LANDMINE; `:530–542` PIT; `:544–565` HOLE; `:567–576` LEVEL_TELEP; `:578–593` TELEP_TRAP; `:595–617` ROLLING_BOULDER; `:620` pool; `:627–630` fobj relink; `:631` dopush; `:633` else cannot_push) + callees `dopush :166–244`, `cannot_push_msg :247`, `cannot_push :262`, `rock_disappear_msg :315–324`, `moverock_done :327`, `moverock :336`. Only C caller is `:342` moverock.
**JS:** `js/hack.js` (+~260: imports, Deaf_mr, rock_disappear_msg, dopush costly/shop arms, full core restart), `js/trap.js` (+2 export keywords).
**Change:** `js/trap.js` — exported `blow_up_landmine` + `launch_obj` (same-SCC edge, no new module edge: hack.js already imports trap.js). `js/hack.js` — new names on safe edges (`imports.mjs` SAFE/ALREADY throughout): shk.js costly_spot/shop_keeper/addtobill/subfrombill/onshopbill/find_objowner/stolen_value (new edge, hoisted-function SAFE) + dungeon.js get_level (new edge, hoisted SAFE) + generated seffects ids; There/unmap_object (display), cansee (vision), YMonnam (do_name), Soundeffect (sndprocs), Tobjnam/otense (objnam), rloco/random_teleport_level (teleport), flooreffects/boulder_hits_pool (do.js), bury_objs/fill_pit (dig.js), useupf (invent.js), remove_object/add_to_migration (mkobj.js), seetrap (trap.js), depth (hacklib.js), ESHK + LANDMINE/PIT/SPIKED_PIT/HOLE/TRAPDOOR/TELEP_TRAP/LEVEL_TELEP/ROLLING_BOULDER_TRAP/ROLL/LAUNCH_KNOWN/MIGR_RANDOM/IN_SIGHT (const.js). Local `Deaf_mr` (HDeaf||EDeaf||uroleplay.deaf||u.Deaf idiom) + local `rock_disappear_msg` (C staticfn shape, async for JS pline).
**Verify:** `node scripts/verify.mjs --fn moverock_core` → PASS syntax (2 changed: js/hack.js js/trap.js) · PASS rule2 · hidden note (0 blocked — normal for a coverage row) · PASS reach (no RNG-tagged reach; smoke 24/24, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** unmap_object trap/engr arms in dopush (remembered-glyph clear stands in); squeezeablylightinvent pack-weight arm lives in could_move_onto_boulder, not here; u.Levitation field + Is_airlevel topology flag (pre-existing renderings, untouched); launch_obj's own omissions stay with D-2318.
**Next:** queue holds 7 Open after archive — pop jump next.

## 2026-09-21 — D-2738 `polyself.c` newman missing arms (Sick/Stoned, PolyControl uhp, livelog, Slimed, selftouch)

**C locus:** `nethack-c/upstream/src/polyself.c:414–466` (`:415–416` Sick→make_sick; `:417–418` Stoned→make_stoned; `:419–422` uhp≤0 PolyControl clamp else dead; `:423–432` dead label — urgent_pline/killer/done(DIED)/newuhs/encumber; `:436` newuhs; `:438–443` newform + polyman; `:445` newgend; `:449–451` level-change livelog_printf; `:452–453` livelog_newform; `:455–458` Slimed Your + make_slimed; `:460–462` botl/see/encumber; `:464` retouch_equipment(2); `:465–466` gloveless selftouch). Callers `:692` werecritter, `:714` polymon force.
**JS:** `js/polyself.js` (+~60: 1 local + 5 arms + 2 import names; 36 importers).
**Change:** `js/polyself.js` only (no new cross-module edge — `Your` joins the existing display.js import, runtime-called like `You`/`pline` per the imports check; `LL_MINORAC` joins the const.js import): module-local `newman_dead_end()` (`:423–432` single definition, C goto shape; both gates call it then return); `:360` oldgend captured; `:415–418` Sick/Stoned clears via live make_sick/make_stoned; `:419–422` PolyControl arm via local `Polymorph_control(u)` else dead; `:445` newgend captured; `:449–451` livelog_printf on level change; `:455–458` Slimed Your + make_slimed; `:465–466` gloveless selftouch via live selftouch + file `no_longer_petrify_resistant`. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn newman` → PASS syntax (1 changed) · PASS rule2 · hidden note (0 blocked — normal for a coverage row) · PASS reach (3 reach it, 3 PASS, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 → VERIFY: PASS; `--full` → PASS full 44/44 → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** retouch_equipment(2) (`:464` — not in js/**, own coverage row, rehumanize precedent); livelog_newform (`:307` non-static C fn, own row) on the no-level-change arm.
**Next:** queue holds 8 Open after archive — pop moverock_core next.

## 2026-09-21 — D-2737 `sp_lev.c` lspo_map + lspo_replace_terrain + lspo_region whole bodies (des.map/replace_terrain/region bindings)

**C locus:** `lspo_map :6075–6319` (`:6099` coder; `:6101–6102` themeroom-fail return; `:6105–6130` string/table dispatch; `:6132–6136` Map-data error; `:6147–6223` x,y-vs-align placement incl. `:6148–6168` themeroom somex/somey, `:6173–6186` croom clamp, `:6197–6223` align switch + odd-force; `:6225–6235` ystart clamp; `:6237–6238` 1×1 reset; `:6244–6283` themeroom guard + redo; `:6286–6305` cell load; `:6311–6324` skipmap/contents/selection return) + `lspo_replace_terrain :5051–5143` (`:5068–5069` MAX_TYPE return; `:5073–5082` mf arm + mapfrag_error; `:5092–5101` region/selection fallback; `:5104–5119` fresh selection; `:5123–5134` per-cell match/chance arms) + `lspo_region :5584–5715` (`:5597–5618` table form; `:5619–5640` selection-lit pair; `:5648` litstate_rnd; `:5650–5651` ANY_LOC; `:5660–5674` light-only path; `:5682–5698` irregular/rect rooms; `:5700–5712` subroom push + contents + endroom + doors) + ported staticfns `sel_set_lit :5535–5540`, `get_table_coords_or_region :5561–5580`, `mapfrag_error :281–295`, `l_push_wid_hei_table :3050` ({width,height}), `set_levltyp_lit` (mkmaze.c:125). Only C references are the prototypes + Lua des dispatch entries (map `:6392` verified).
**JS:** `js/mklev.js` (+461 after `lspo_room`: 3 file-locals + 3 exports).
**Change:** `js/mklev.js` only, after `lspo_room` (same-file locals throughout — no new cross-module edge): file-local `sel_set_lit` (C staticfn shape, numeric lit), `mapfrag_error` (NULL/odd/center arms, truncating center index), `set_levltyp_lit_tail` (C lit half: NOCHANGE keep, RANDOM→rn2(2), lava forces 1); exported `lspo_replace_terrain(opts)` in C order (required 1-char toterrain/fromterrain via live `splev_chr2typ`, INVALID→throw; mf arm with mapfrag_error throw; splev_opt_int defaults; optional-TRUE region via live `get_table_region_unpacked`; selection-field shape check; fresh whole-level/ANY_LOC-rect selection; recalc + empty→full bounds per C selvar.c:77/region.js:1146; max(1,lx)/ly loop bounds; match-then-rn2 short-circuits; sel_set_ter + lit tail; freesel-only free); exported `lspo_region(a, b)` in C order (function-2nd-arg = unpacked contents, else C argc dispatch; table form via `splev_opt_*` + `splev_roomtype`/impossible like `lspo_room`, required-FALSE region, "region needs region" throw, live `litstate_rnd`, NULL-croom ANY_LOC endpoints, light-only vs irregular-flood vs rect+`topologize`, needfill/needjoining post-add_room to the stored room, coder push + room-object contents + `spo_endroom` + `add_doors_to_room`; selection pair via live `selection_clone`/`selection_do_grow`(W_ANY)/`selection_iterate` + `sel_set_lit`, C TODOs kept); exported `lspo_map(a, contentsFn)` in C order (string=centered vs table dispatch; align tables with TOP=1/BOTTOM=5; -1 side keeps its start like C's unmatched switch arm; truncating division; themeroom somex/somey + croom clamp; isok-else frees selection + throws; ystart clamp; keep-SpLev_Map reset variant — C `reset_xystart_size` never clears SpLev_Map; guard with `tryct++ < 100` redo vs fail flags for the gotos; load with SpLev_Map mark + setpoint + `sel_set_ter` + explicit lit-FALSE clear; contents gets `{width, height}`; live selection returned). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn lspo_map` → PASS syntax (1 changed: js/mklev.js) · PASS rule2 · hidden note (0 blocked — normal for a coverage row) · PASS reach (136 reach it, 136 run with --reach-all, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 → VERIFY: PASS. `--fn lspo_replace_terrain` → same shape, reach 64/64 REACH-OK, full 44/44 → PASS. `--fn lspo_region` → same shape, smoke spread 24/24 REACH-OK, full 44/44 → PASS.
**Named:** `mapfrag_free`/dupstr/`free` (GC no-ops — `lspo_map_themeroom` precedent); `lcheck_param_table` (table-or-empty + object check); `l_selection_check` (selection-shape check); `get_table_mapchr` family (inline 1-char + `splev_chr2typ`); `l_push_wid_hei_table` (`{width,height}` object); `l_push_mkroom_table` (the room object — `lspo_room` precedent); `nhl_pcall_handle` (direct contents call); `l_selection_push_copy` + post-copy `selection_free` (the live selection is returned); string-"true"→0 C boolean quirk (sibling `splev_opt_boolean` mapping stands — des inputs are booleans/ints where both agree); C needfill-string + lit=random TODOs (C's own notes, kept in comments).
**Next:** queue holds 0 Open after archive (below band 8) — refill below.

## 2026-09-21 — D-2736 `sp_lev.c` lspo_trap des.trap binding + create_trap whole body

**C locus:** `nethack-c/upstream/src/sp_lev.c:4397–4470` (`:4402` create_des_coder; `:4404–4406` defaults; `:4408–4427` string/coord-pair/triple dispatch; `:4428–4461` table form — xy, traptype_opt, boolean opts, launchfrom + teledest both → gl.launchplace; `:4463–4464` NO_TRAP error; `:4466–4467` coord pack; `:4469` create_trap(croom); `:4470` launchplace reset) + `create_trap` `:1812–1846` (`:1816` MAZEFLAG init; `:1819–1823` vib arm; `:1824–1825` croom arm; `:1826–1835` DRY retry loop; `:1837–1842` flags; `:1844` mktrap) + `get_table_traptype_opt` `:4350–4364` + `get_traptype_byname` `:4379–4389` (staticfn). Only C caller is the Lua dispatch table (`:162` decl).
**JS:** `js/mklev.js` (+~130 before the lspo_feature block: table + 2 locals + 2 exports).
**Change:** `js/mklev.js` only (same-file locals — no new cross-module edge; `maketrap` already imported from `./trap.js`): `LSPO_TRAPTYPES` table (C `:4322–4347`); file-local `lspo_traptype_byname` (C staticfn shape, strcmpi → lowercase compare, NO_TRAP default) + `lspo_traptype_opt` (empty/unmatched → defval, C `:4355–4362`); exported `create_trap(tmp, croom)` in C order (vib arm via live `pick_vibrasquare_location` + `game.svi.inv_pos` idiom; croom arm via `get_free_room_loc_coord`; DRY retry loop with C `(A || B) && ++trycnt <= 100` precedence; flags; `mktrap(type, flags, null, {x, y})`); exported `lspo_trap(a, b, c)` with `arguments.length` dispatch like `lspo_gold` (string / string+coord via live `get_coord_unpacked` / triple with checkstring-equivalent throw / table-or-empty with live `get_table_xy_or_coord` + `splev_opt_boolean` + launchfrom/teledest both → `game.launchplace` + reset). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn lspo_trap` → PASS syntax (1 changed: js/mklev.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; fixed smoke spread 24 run, 24 PASS, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** Lua-stack callback (lspo_trap takes no function arg — contentsFn pattern N/A); Lua argc dispatch itself (unpacked `arguments.length` form per file precedent); `lcheck_param_table` (≡ table-or-empty guard); `nhl_push_obj` return count (unpacked `return 0` per siblings).
**Next:** queue holds 4 Open after archive (map/replace_terrain/region + residuals) — refill top-up next iter.
