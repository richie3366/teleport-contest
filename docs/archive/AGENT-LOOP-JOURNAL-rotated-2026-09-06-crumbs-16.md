# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-06 — D-1971 hack.c findtravelpath TEST_TRAV edge (doors pass, boulder leaves, diagonal doorways banned)

**C locus:** `nethack-c/upstream/src/hack.c` — `test_move` `:989–1255` TEST_TRAV arms (obstructed/ironbars `:1011–1073`, doors `:1074–1148`, `testdiag` `:1139–1147`, squeeze `:1153`, worm `:1172` deferred, run==8 trap/liquid `:1181–1200`, diagonal-out `:1205–1213`, boulder `:1216–1252`), called per direction by `findtravelpath` (`:1400` TRAVEL/VALID, `:1427` GUESS).
**JS:** `js/cmd.js` (+96/−13); 1 js file, under 600 cap. Density note: C is ~90 TEST_TRAV arm lines; the JS is the full C-ordered edge envelope both BFS loops share (one C `file.c:function` family, every callee live or named below — no stubs in a live arm).
**Change:** `js/cmd.js` — new `travel_test_move(ux, uy, dx, dy)` in exact C arm order (obstructed/ironbars with `test_move_hero_passes_bars` + tunnels/needspick rock-eat; closed-door pass with Passes_walls/ooze/underwater/tunnel gates and testdiag diagonal; open-door testdiag diagonal; squeeze via `travel_blocks_tight_diag`; run==8-gated `travel_avoids_cell`; diagonal-out with `doorless_door` + deferred `block_entry`; boulder Sokoban-never + two-in-a-row gate with `could_move_onto_boulder`/tunnels/carried pick/mattock), wired into both BFS loops in place of the 4-line stand-in chain (delay arm, NODIAG, visited-deferral, and `seenv||couldsee` gate untouched). New imports on ALREADY edges (`tunnels`/`needspick`, `carrying`, `In_sokoban`; `--can` clean, no new cycle surface). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn findtravelpath` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/cmd.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify findtravelpath: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — TOP30 row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`. Plus `node scripts/verify.mjs --full` (BFS reachability changed) → `PASS full 44/44 passing`.
**Named:** Passes_walls+may_passwall rock (`:1014`, no JS export — brief clone rule); Underwater rock (outcome identical — blocked); `worm_cross` (`:1172`); `block_entry` (matches domove `:3217` deferral); WAN_DIGGING-unknown arm of the two-in-row gate (no JS `objects[].oc_name_known` — treated as not carrying); Known_wwalking/Known_lwalking/WATERWALL/LAVAWALL (ride inside `travel_avoids_cell`, pre-existing); travelmap visited stop + `selection_setpoint` (`:1440`, needs mklev selection exports + async `You` — BFS stays sync); TEST_MOVE arms (adjacent + GUESS no-guess) and full `test_move` export.
**Next:** pop the next Open row (`display.c` map_glyphinfo).

## 2026-09-06 — Audit reviews 932–940 (D-1962…D-1970) + cadence score

**Scope:** 9 JS-touching SHAs since audit 923–931 (`5e47ff6a`…`de4686d7`), one SHA at a time, each file written before the next SHA opened. All 9 **ACCEPT**, 0 Must-fix, no LOOP-QUEUE/CURRENT-Next change.
**Method per SHA:** `git show` js hunks, `csym.mjs` body + `--callers` (ranges cited), `sym.mjs` on new/deleted/re-pointed symbols, `imports.mjs --rulecheck` + `--can`, diff banned-grep, `hidden-proxy verify <fn> --base HASH~1` re-measured (all 9: 0 blocked at baseline and working — vacuous-but-honest, no corpus-PASS claims in D-logs confirmed).
**Findings worth keeping:** 937's stale-`other` recursion loop is literal C (`:667–668`); 939's `wake_nearto` is a pre-existing C-cited cycle clone reused, not new; 940's `!otmp→return 0` vs C `panic` is the house-wide softening, `wall_info|flags` OR is D-0865, `PM_VLAD=indexOf('PM_VLAD_THE_IMPALER')` correct; 940's only probe was an import smoke (probe-depth note, not verdict-changer). 934: `:3700` call site is `#if TILES+MSDOS` compiled out.
**Score:** full `sessions` 44/44 at `de4686d7` — Scr 11,405/11,405, RNG 792,838/792,838 (identical to D-1952 audit). Speed `61+0.62/turn` (R² 0.87). Hidden proxy: 255/265 (96.2%), RNG 99.61%, screens 99.7% — identical to recorded baseline.
**Next:** pop the next Open row (`hack.c` findtravelpath).

## 2026-09-06 — D-1970 muse.c use_defensive hurt-monster horn/bugle/wand depth

**C locus:** `nethack-c/upstream/src/muse.c` — `use_defensive` `:796–1219`, this D ports `MUSE_UNICORN_HORN` `:820–837` (optional otmp; `!mcansee` → `mcureblindness`, else conf/stun clear + `seems steadier`, else `impossible`), `MUSE_BUGLE` `:838–848` (`Monnam plays doname` vs Deaf-gated `Soundeffect(se_bugle_playing_reveille)` + `You_hear reveille` + `awaken_soldiers`), `MUSE_WAN_TELEPORTATION_SELF` `:849–857` (shk/gd/priest early `return 2`, `m_flee`, self `mzapwand`, `m_tele` WAN_TELEPORTATION), `MUSE_WAN_TELEPORTATION` `:858–869` (`zap_oseen`, `mzapwand`, `m_using` + `mbhit(rn1(8,6))`, noteleport `mon_learns_traps`), `MUSE_WAN_DIGGING` `:917–971` (`m_flee`, `mzapwand`, oseen `makeknown`, furniture/drawbridge/stairs ineffective, pit-vs-hole `Can_dig_down`/`candig` fork with `maketrap`/`seetrap`/`fill_pit`/`recalc_block_point` + `mintrap(FORCEBUNGLE)` kill check, hole `migrate_to_level` +1), `MUSE_WAN_UNDEAD_TURNING` `:972–980` (same beam shape, no learn step), `MUSE_WAN_CREATE_MONSTER` `:981–997` (pool→eel/croc `pm`, `enexto` fail → 0, `makemon` + `canspotmon && oseen` `makeknown`). `find_defensive` `:441–750` selection shipped in the same envelope (same-row family rule): unicorn-horn `:474–488` (uncursed scan / `is_unicorn` / KI_RIN), undead-turning `:525–541` (corpse-wielding petrifying hero, `lined_up`), bugle `:636–643` (`is_mercenary` + `m_carrying(BUGLE)` + `m_sees_sleepy_soldier`), digging/tele/create inventory loop `:654–748` (pit/WEB/BEAR_TRAP kludge, `W_NONDIGGABLE`/Sokoban/`Is_botlevel`/`In_endgame`/ice-pool-lava/Vlad-tower gates, amulet→TELE-vs-SELF, Pestilence sickness+create branch). `awaken_soldiers` `music.c:161–192` (bugler==youmonst ? ulevel/`mdistu` : bugler mlevel/`dist2`).
**JS:** `js/muse.js` (+278/−25), `js/music.js` (+10/−5); 2 js files, under 600 cap. Density note: C is ~230 use-arm lines + ~120 selection lines; the JS is the full 7-arm + selection envelope the TOP30 row cites (one C `file.c:function` family, every callee live or imported export — no new clones).
**Change:** `js/muse.js` — 7 use arms in C order (horn before SCR_TELE; digging/undead/create-monster between SCR_TELE and SCR_CREATE) with `:line` citations: horn (`pline` `The tip of mon_nam's horn glows!` for the `pline_The` arm — no JS `pline_The` symbol, cf. dig.js `The ...` style; `await impossible` else-arm), bugle (file deaf pattern, no-op `Soundeffect` id from generated, `await awaken_soldiers(mtmp)`), tele-self (shk+`inhishop`/gd/priest gate, `m_flee`, self `mzapwand`, `m_tele`), tele beam (`game._zap_oseen`/`game.m_using` + local `mbhit(rn1(8,6))` like the use_offensive TELE arm, no `_buzzer`), digging (`IS_FURNITURE`/`IS_DRAWBRIDGE`/`is_drawbridge_wall`/`stairway_at` ineffective; `Can_dig_down`+`candig` pit fork with `t_at || !maketrap(PIT)` too-hard arm, `seetrap`+`fill_pit`+`recalc_block_point`, `await mintrap(FORCEBUNGLE) === Trap_Killed_Mon ? 1 : 2`; HOLE fork with flyer dives/falls vs Deaf-gated crash+`You_hear(something…)`, `migrate_to_level(ledger_no+1, MIGR_RANDOM)`), undead beam, create-monster (`is_pool`→eel/croc `mons()`, `enexto` fail → 0, `canspotmon && oseen` `makeknown` per C — the older SCR_CREATE arm's `canseemon||sensemon` stays as D-1809 shipped it). `find_defensive` — horn/undead/bugle arms + pit kludge + `is_Vlad` (`monst.h:222` data-or-cham) + digging/tele-self/tele/create selection in exact C `nomore` order (digging `break`, rest `continue`). New imports: `is_unicorn`/`poly_when_stoned`/`is_flyer` (monsters, ALREADY edge), `is_lava` (hack, ALREADY), `is_ice` (zap, ALREADY), `is_drawbridge_wall` (dbridge), `maketrap`/`Trap_Killed_Mon` (trap), `recalc_block_point` (vision, ALREADY), `Can_dig_down`/`IS_FURNITURE`/`IS_DRAWBRIDGE`/`W_NONDIGGABLE`/`PIT`/`HOLE`/`WEB`/`BEAR_TRAP`/`something` (const), `fill_pit` (dig, SAFE), `surface` (sit, SAFE), `Soundeffect` (sndprocs, SAFE no-cycle), `se_bugle_playing_reveille`/`se_crash_through_floor` (generated), `awaken_soldiers` (music); `BUGLE`/`PM_VLAD` consts.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn use_defensive` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/muse.js js/music.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify use_defensive: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — TOP30 row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `skip full (no shared file changed; pass --full to force)`. Probe (deleted): `import('./js/muse.js')` → `load ok function function`.
**Named:** tryescape Knox `m_next2m` gate (`find_defensive`, pre-existing); `munstone` other-consume caller (D-1809 debt); `use_offensive` `mreadmsg` arm; `flash_mon` canonical home `mon.js` (local muse.js clone stands); `resists_acid`/`mon_has_amulet`/`surface`/`seetrap`/`inhishop`/`m_useup`/`unbless` clones elsewhere (import-the-export rule; `js/muse.js` imports the exports); `default:` C `impossible` → JS `return 2` (dead while find stays honest).
**Next:** pop the next Open row (`hack.c` findtravelpath).

## 2026-09-06 — D-1969 sounds.c domonnoise FULL_MOON howl + animal MS_MEW..MS_ORC

**C locus:** `nethack-c/upstream/src/sounds.c` — `domonnoise` `:678–1242`, this D ports `MS_WERE` `:824–841` (`flags.moonphase == FULL_MOON && (night() ^ !rn2(13))` → immediate howl/shriek `pline` + `wake_nearto(11*11)`, else whisper `pline_msg`), `MS_BARK` `:842–860` (`FULL_MOON && night()` howls; peaceful whines/yips/barks with `ptr != &mons[PM_DINGO]`, else growls), animal `MS_MEW` `:861–883` (tame yowl/meow/purr/mew, untame FALLTHROUGH) through `MS_ORC` `:1000–1003` (`GROWL :884–888`, `ROAR :889–893`, `SQEEK :894–897`, `SQAWK :898–905` hostile-raven `Nevermore!`, `HISS :906–913` peaceful `return ECMD_OK`, `BUZZ :914–917`, `GRUNT :918–921`, `NEIGH :922–933`, `CHIRP :941–944`, `WAIL :945–948`, `GROAN :949–954` silent unless `!rn2(3)`, `GURGLE :955–958`, `BURBLE :959–962`, `TRUMPET :963–968` + `wake_nearto`, `SHRIEK :969–974` + `aggravate()`, `IMITATE :975–977`, `BONES :978–988` immediate rattle + `You freeze` + `nomul(-2)` + `multi_reason`, `LAUGH :989–996` `laugh_msg[rn2(4)]`, `MUMBLE :997–999`, `ORC :1000–1003`). Callers (`brief`): `dogmove.c:965`, `sounds.c:527/1302/1408/1456/1526`. `Soundeffect` on every arm is compiled out without `SND_LIB` (`sndprocs.h:272` empty define) — omitted with citation like the live `MOO`/`BELLOW` arms.
**JS:** `js/sounds.js` (+145/−8); 1 js file, under 600 cap. Density note: C is ~180 lines across the 21-arm envelope; the JS is the full envelope (TOP30 row cites the envelope, not one arm).
**Change:** `js/sounds.js` — added `night` (`./calendar.js`: NEW-CYCLE, SAFE), `aggravate` (`./wizard.js`: same 89-module SCC, hoisted, SAFE), `FULL_MOON` (existing `./const.js` edge) imports; added `PM_HUMAN_WERERAT`/`PM_DINGO`/`PM_RAVEN` (`monsterNames.indexOf` pattern) and `MS_GURGLE=15`/`MS_BURBLE=16`/`MS_TRUMPET=17`/`MS_SHRIEK=18`/`MS_BONES=19` constants (`MS_ANIMAL=17` already correct per `monflag.h`); ported `MS_WERE` (short-circuit `moonphase` → `night() ^ !rn2(13)` preserves RNG; immediate `await pline` + `await wake_nearto`), fixed `MS_BARK` (`FULL_MOON && night()` first, `mndx`-vs-`PM_DINGO`), split `MS_MEW` tame vs `MS_GROWL || MS_MEW` fallthrough, then `ROAR`/`SQEEK`/`SQAWK`/`HISS` (peaceful `return ECMD_OK`) /`BUZZ`/`GRUNT`/`NEIGH` and `CHIRP`/`WAIL`/`GROAN` (`!rn2(3)`-gated)/`GURGLE`/`BURBLE`/`TRUMPET` (`pline_msg` + `await wake_nearto` before epilogue print, like C) /`SHRIEK` (`pline_msg` + sync `aggravate()` before print)/`IMITATE`/`BONES` (immediate double `pline` + `nomul(-2)` + `game.multi_reason`/`game.nomovemsg` + `return ECMD_TIME`)/`LAUGH` (`rn2(4)`)/`MUMBLE`/`ORC` in C order with `:line` citations. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn domonnoise` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/sounds.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify domonnoise: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — TOP30 row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `skip full (no shared file changed; pass --full to force)`. Probes (deleted): static 36-string C-vs-JS check PROBE PASS; `import('./js/sounds.js')` → `ok import domonnoise`.
**Named:** `MS_VAMPIRE` (`:744–803` night/midnight/kindred + `rn2` + `body_part`/`pmname`/`an`); `MS_DJINNI` (`:1004–1019`); `MS_ARREST` (`:1138–1151`); `MS_BRIBE`+`MS_CUSS` (`:1152–1169`, `demon_talk`/`cuss` have no JS symbol — brief NOT FOUND); `MS_SPELL` (`:1170–1173`); `MS_NURSE` (`:1174–1188` uwep/armor gates); `MS_GUARD` (`:1189–1194` `money_cnt`); `MS_SOLDIER` (`:1195–1210` `rn2(3)` tables); `verbl_msg_mcan` cancelled-speech epilogue arm (`:1227–1229`); save-rest `oracle_loc`.
**Next:** pop the next Open row (`muse.c` use_defensive).

## 2026-09-06 — D-1968 mkmaze.c maybe_adjust_hero_bubble water-level hero-bubble adjust

**C locus:** `nethack-c/upstream/src/mkmaze.c` — `maybe_adjust_hero_bubble` `:1929–1941` (Is_waterlevel guard, u.dx/u.dy guard, `hero_bubble && !rn2(2)` → `hero_bubble->dx/dy = u.dx/u.dy`). Sole caller: `hack.c domove` `:2702–2705` on the `(succeeded & (RUSH|WALK))` gate, after `domove_core` (which runs `spoteffects(TRUE)` `:2979–2980` first). State owner: static `hero_bubble` `:1530`, reset NULL + set to the bubble containing the hero in the `movebubbles` water cons-pickup loop `:1559/:1622–1631` (`!u.uswallow && u_at(x,y)`, last overlapping match wins, pre-toggle traversal).
**JS:** `js/mklev.js` (+39/−2 incl. hero tracking + export + comment), `js/cmd.js` (+8/−1 incl. import + gate); 2 js files, under 600 cap. Density note: C is 12 lines; the JS is the export plus the hero_bubble state tracking and the caller wire the observable needs (small-C exception).
**Change:** `js/mklev.js` — exported `maybe_adjust_hero_bubble()` in C short-circuit order (Is_waterlevel → u.dx/u.dy → hero_bubble-gated `rn2(2)` → steer dx/dy) with the `:1929–1941` citation; `movebubbles()` now resets `game.hero_bubble = null` and, on the water level with `!uswallow`, scans the chain in C pre-toggle order (`upOld = !!movebubbles_up`, forward `bbubbles` or reverse `ebubbles`) for bitmap containment of (u.ux,u.uy) (`i = ux-b.x`, `j = uy-b.y`, `bm[j+2] & (1<<i)` ≡ C's per-cell loop condition; last match wins like C's overwrite) with the `:1559/:1622–1631` citation. `js/cmd.js` — added to the existing `./mklev.js` edge (`imports.mjs --can`: ALREADY) and called in the domove finally block on the same RUSH|WALK gate, independent of smudgeCoords, after spoteffects like C (`domove_core :2979–2980` precedes `domove :2704`). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn maybe_adjust_hero_bubble` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/cmd.js js/mklev.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify maybe_adjust_hero_bubble: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`. Probe (/tmp, deleted): non-waterlevel / zero-dir / null-bubble arms draw nothing; set bubble draws exactly one rn2(2) with dx/dy kept-or-steered only; movebubbles sets hero_bubble when the hero stands in the bitmap and resets it otherwise — PROBE PASS.
**Named:** water obj/mon/trap cons pickup+deposit in `movebubbles` (objects/monsters/traps still not carried by drifting bubbles; Punished ball `unplacebc`/`lift_covet` arms); `vision_recalc(2)` at `movebubbles` head (JS sets `vision_full_recalc` at tail only — pre-existing); `set_wportal` `impossible` pline (pre-existing).
**Next:** pop the next Open row (`sounds.c` domonnoise).

## 2026-09-06 — D-1967 dbridge.c do_entity drawbridge crush/jump/relocate driver

**C locus:** `nethack-c/upstream/src/dbridge.c` — `do_entity` `:554–759` (automiss ride `:570–578`; e_missed `:579–593` with portcullis-miss pline vs drawbridge debugpline; DRAWBRIDGE_DOWN crush `:595–609` with NO_KILLER_PREFIX "crushed to death underneath a drawbridge"; must_jump portcullis e_jumps `:610–634` with "crushed by the falling portcullis" + se_crushing_sound; find_drawbridge/get_wall_for_db relocate `:643–695` with e_at standoff recursion; db_wall chamber `:700–722` with "tumble towards the closed portcullis"/"closing drawbridge"; bridge-square `:723–758` with se_splash, "fall from the bridge", lava/moat drink/fall, "fell from a drawbridge" DROWNING/BURNING/CRUSHING). Helpers in-cluster: `e_survives_at` `:380–399`, `e_died` `:402–480`, `automiss` `:486–490`, `e_missed` `:496–525`, `e_jumps` `:531–551`, `nokiller` `:763–769`. Callers: `close_drawbridge` `:811–815`, `open_drawbridge` `:863–867`, `destroy_drawbridge` `:967–1013`.
**JS:** `js/dbridge.js` (+456/−12 incl. imports/header per `git diff --stat`; 1 js file, under 600 cap). Density note: C is ~366 lines across the 7-symbol cluster (do_entity 205 + e_died 78 + rest); the JS is the full cluster in one handoff (Open-row family rule: every callee live, C-matched clone, or named below).
**Change:** `js/dbridge.js` — exported `automiss` (`:486–490`), `e_survives_at` (`:380–399` noncorporeal→pool→lava→db_wall→TRUE with hero Wwalking/Amphibious/Breathless/Swimming/Flying/Levitation + is_swimmer/is_flyer/is_floater/likes_lava arms), `e_died` (`:402–480` hero DROWNING/BURNING killer-clear + drown/lava_effects else "falling drawbridge" + done + enexto/teleds rescue, monster monkilled-vs-xkilled by `game.context.mon_moving` with AD_DGST-on-NOCORPSE + NOMSG|NOCONDUCT re-kill + worm-tail edata clear), `e_missed` (`:496–525` 5/3/2/0 then db_wall −3 then `>= rnd(8)`), `e_jumps` (`:531–551` Unaware/Fumbling vs helpless/!mmove/wormno gate, Confusion −2, Stunned −3, db_wall −2, `>= rnd(10)`), `do_entity` (`:554–759` in C order, single function; pline_The as `The ...`, You as `You ...`, find_drawbridge/get_wall_for_db via `{x,y}` pt, `while (e_at(newx,newy) && !== etmp) await do_entity(other)` recursion), `nokiller` (`:763–769` killer clear + both occupants via m_to_e). Hero props are file-local youprop.h readers (Passes_walls/Wwalking/Swimming/Amphibious/Breathless/Flying/Levitation/Deaf/Confusion/Stunned; helpless inline `msleeping||!mcanmove` per monst.h:251 — no new clones of exported Unaware/Fumbling, both imported). Imports `--can` SAFE (same 89-module SCC, hoisted functions, no top-level TDZ); debugpline D_DEBUG-only omitted; Soundeffect is no-op without SND_LIB but se_crushing_sound/se_splash ids imported from generated; no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn do_entity` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/dbridge.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify do_entity: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `skip full (no shared file changed; pass --full to force)`.
**Named:** `revive_nasty` open/close/destroy arms (still deferred in all three callers); destroy `rn2(6)` IRON_CHAIN scatter + `scatter()` (no partial RNG); destroy `flooreffects` boulder-dunk body (still delobj); `minliquid` destroy-debris water arm (no JS symbol — destroy path still unwired); close/open `set_entity`+`do_entity` caller wiring + `block_point`/`unblock_point` vision ops (terrain-only port stands); `You_see` Blind/Unaware polish stays; `wait_synch` D_DEBUG-only.
**Next:** pop the next Open row (`mkmaze.c` maybe_adjust_hero_bubble).
