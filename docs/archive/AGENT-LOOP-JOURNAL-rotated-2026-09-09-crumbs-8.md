# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-09 — D-2201 `botl.c` status line 2: tty field-path condition order (rank sort, 2 corpus PASS + 2 moved)

**C locus:** NOT the do_statusline2 append block (`botl.c:170–206`). Live tty path: `bot()` takes `bot_via_windowport()` whenever `VIA_WINDOWPORT()` (`botl.h:213`; `windows.c` forces `WC2_FLUSH_STATUS`) pushing `BL_HUNGER`, `BL_CAP`, `BL_CONDITION` (`windows.c` fieldorder); `wintty.c:5073–5104` draws `BL_CONDITION` words walking `cond_idx[]` — the `conditions[]` ranking table (`botl.c:781–813`) sorted by `cond_cmp` (`botl.c:1332–1342`: rank asc, useroption-alpha tiebreak; init-sorted at `options.c:5002`). Net order: hunger, cap, then Strngl(4); FoodPois/Slime/Stone/TermIll (6, alpha); Blind/Conf/Deaf/Fly/Hallu/Lev/Ride/Stun (10, alpha). C-measured: today's recorder binary re-run on the 92035 prefix prints row 23 `Xp:20 Conf Deaf` (geom-probe `.cache/probe/scen-wish-Priest-92035-s209.c.session.json` step 210) — no pinned-source table orders Conf-before-Deaf outside the rank sort, which fits all four pairs exactly.
**JS:** 1 file (`js/display.js`, cond-block reorder + comments), under the 600/10 caps.
**Change:** `js/display.js` `_statusLine2` emits `BL_HUNGER`, `BL_CAP`, then rank-sorted `BL_CONDITION`, every predicate verbatim (Sick split into separate FoodPois/TermIll gates so Slime/Stone sort between; Conf before Deaf, Hallu before Stun, hunger/cap before all conds). Doc comment revised with the field-path citation. No new imports (all consts already in scope).
**Verify:** `node scripts/verify.mjs --fn do_statusline2` → PASS syntax (1 file) · PASS rule2 · PASS hidden (2 PASS: 92035, 92091; 2 moved past: 92138 66→do_statusline1@72, 92067 196→list_vanquished@224; 9 unchanged same-step/same-owner; 0 worse → PROGRESS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed). Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** `cond_shrinklvl` abbreviations (`text[1]`/`text[2]`) — no corpus session needs them (all four show full words); opt_in conditions (Bare/Busy/Glow/…) — disabled by default via `condtests[].enabled`, no JS state; MAXCO panic / showvers (pre-existing D-1990 defers, untouched).
**Next:** do NOT re-pop do_statusline2 for the 4 order sessions (92035/92091 PASS at HEAD; 92138→`botl.c` do_statusline1@72 and 92067→`insight.c` list_vanquished@224, both re-queued as Open rows this commit). do_statusline2 stays OPEN for 9 residuals: AC-state — 92095 shirt-destroy (blockedRng 5233, stepFns empty; writer upstream of the rip turn, undiagnosed) / 92021+92195 polymon-AC (blockedRng 151/10, stepFns exercise+polymon → `polyself.c` polymon writer) / 92090 poly-AC (blockedRng 0, stale-display class); HP-state — 92107 cold-shatter (441) / 92044 pudding-bite (0, stale-display class); Pw-state — 92194 gulpmu (0, stale-display class); lembas-repaint — 92092/91125 (the parked lembas pair, same sessions+steps; falsifier stands). The three 0-blocked-RNG value diffs (92044 HP 20vs3, 92090 AC 7vs10, 92194 Pw 2vs3) draw identical dice both sides — same-dice stale/fresh paint, botl-timing class, cf. parked lembas falsifier (botl-parity iteration keeping 44/44); do not chase as content bugs. Head rows `pager.c` do_screen_description + `detect.c` reveal_terrain park STALE in this commit (zero-diff moves to trapeffect_sqky_board@72 / prinv@67; entries in Parked).

## 2026-09-09 — D-2200 `potion.c` make_hallucinated set/clear: docrt-entry --More-- flush + timeout.c HALLUC expiry arm (1 corpus PASS)

**C locus:** `potion.c:368–438` `make_hallucinated` (changed gate `:394`, see_* before pline `:424–426`); `display.c:1709–1778` `docrt_flags` (vision_recalc(2) → cls → memory → recalc(0) → see_monsters) + `display.c` `cls` (`display_nhwindow(WIN_MESSAGE,FALSE)` flushes messages before `clear_nhwindow(WIN_MAP)`); `timeout.c:777–783` HALLUC expiry (`set_itimeout(&HHallucination,1L); make_hallucinated(0L,TRUE,0L); if (!Hallucination) stop_occupation()`); `youprop.h:116–120` HHallucination/EHalluc_resistance/Hallucination macros.
**JS:** 2 files (`js/display.js` +9, `js/timeout.js` +13/−4 incl. comments), under the 600/10 caps.
**Change:** `js/display.js` — `await flush_topl_more()` first inside `docrt()`'s guarded body (after the in_docrt latch, before uswallow/vision arms): a pending --More-- now waits on the intact see_* paint (C order), then docrt repaints (second-round picks) for later steps. No-op when nothing pends; zero RNG/display-RNG delta (same waits and burns, wait moved earlier). `js/timeout.js` — HALLUC expiry arm in the generic loop mirroring the CONFUSION-arm shape: re-arm flat `HHallucination` TIMEOUT bits to 1 (the -- above already zeroed it; make_hallucinated needs old≠0; uprops already reads 0 so the arm cannot re-fire), `await make_hallucinated(0,true,0)`, `if (!Hallucination()) await stop_occupation()`.
**Verify:** `node scripts/verify.mjs --fn make_hallucinated` → PASS syntax (2 files) · PASS rule2 · PASS hidden (1 PASS, 0 moved past, 0 unchanged, 0 worse → scen-intrinsic-Caveman-92052: PASS; docrt fix alone moved it 17→75, expiry arm closed it) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed). Final verify ran after the last js/ edit (map/docs/queue/archive edits only after; no D-1831 gap).
**Named:** make_hallucinated body still names (js/potion.js doc): EHalluc_resistance mask polish beyond |=/&=~, eatmupdate (mimic-orange arm; skipped on turn-ON since Hallucination true), update_inventory, disp.botl (JS sets flags.botl only; status repaint verified identical), itch/flatten clear-messages (turn-off-without-change path). uprops[HALLUC].intrinsic is not written by make_hallucinated (flat only); timeout sync fills it (sync_timeout_flats) — save/restore of HALLUC intrinsic stays named.
**Next:** do not re-pop `make_hallucinated` for Caveman-92052 (PASS at HEAD; map turns.md:2003 updated). 7 Open rows remain after this done → refilled +1 (`dungeon.c` save_dungeon, topmost unparked/unarchived corpus owner) back to the 8-row floor.

## 2026-09-09 — D-2199 `trap.c:1029–1042` set_utrap Lev/Fly block: wire float_vs_flight (map-driven, vacuous corpus verify)

**C locus:** `trap.c:1029–1042` `set_utrap` (botl on `!u.utrap ^ !tim`, utrap/utraptype store, `float_vs_flight()`); `polyself.c:130–154` `float_vs_flight` (re-audited, no change); `youprop.h:235–253` H/E/B Levitation/Flying macros (JS flat fields match: full-value truthiness, blocked-field I_SPECIAL).
**JS:** 1 file (`js/trap.js` +6/−4 incl. comments), under the 600/10 caps. The C locus is 36 lines total (23 + 13), so the small insertion is the whole C unit, not a thin slice.
**Change:** `js/trap.js` — `float_vs_flight` added to the existing static `./polyself.js` import (same edge as `polymon`/`body_part`/`mbodypart`; `imports.mjs --can trap.js polyself.js float_vs_flight` → ALREADY, no new edge; both modules expose only hoisted function declarations at top level so no TDZ read; live `import('./js/trap.js')` smoke OK); `set_utrap` calls `float_vs_flight()` in C position (after the store, with C's `/* maybe block Lev and/or Fly */` comment). Bear-trap/web/pit paths inherit it through `set_utrap`, matching C. No DIAG/FORCE/seed/coordinate gates.
**Verify:** `node scripts/verify.mjs --fn float_vs_flight` → PASS syntax (1 file) · PASS rule2 · note hidden (vacuous, 0 blocked — row cited 0 blocks, shipped on public gates as the note directs) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed). Truth-table probe (throwaway `node -e`, no js/ diff): plain+trap BF/BLev 0/0; lev+trap 1/1; lev+freed BF 1/BLev 0 + utraptype TT_NONE; fly+trap 1/0; fly+pit 0/0; lev+pit 1/0 — all six match C arm-for-arm. Final verify ran after the last js/ edit (map/docs/queue/archive edits only after; no D-1831 gap).
**Named:** none new. (`reset_utrap(msg)` Lev/Fly restore arms — `float_up()` + `You("can fly.")` — stay deferred: `float_up` is async while `reset_utrap`'s ~15 call sites invoke it synchronously; own future cluster. `Amulet_off` FLYING land/spoteffects stays named in `js/do_wear.js:2352`.)
**Next:** do not re-pop `float_vs_flight`/`set_utrap` for the toggle (body + unconditional site live; map data.md updated). 7 Open rows remain after this done → refilled +1 (`stairs.c` stairs_description, topmost unparked/unarchived corpus owner) back to the 8-row floor.

## 2026-09-09 — D-2198 `do.c` heal_legs call-site wiring: royal-jelly fpostfx + mount_steed wizard-force gate (map-driven; 1 corpus PASS)

**C locus:** `do.c:2449–2486` `heal_legs(int how)` (body already live, re-audited arm-for-arm: `Wounded_legs` gate, `disp.botl`, `ATEMP(A_DEX)++`, `!u.usteed && how != 2` pline with `BOTH_SIDES` plural, `HWounded = EWounded = 0`, `how == 0 → encumber_msg()`); `eat.c:2540–2562` fpostfx `LUMP_OF_ROYAL_JELLY` (queen-bee morph break, `gainstr(otmp,1,TRUE)`, `rnd(20)` HP + `disp.botl`, `!rn2(17)` `setuhpmax`, rotten-death killer + `done(POISONING)`, `!cursed → heal_legs(0)`); `steed.c:228–238` mount_steed `Wounded_legs` gate (`legs_in_no_shape("riding", FALSE)`, `Heal your leg%s?` with the `(HWounded_legs & BOTH_SIDES)` plural quirk ported verbatim, `force && wizard && y_n == 'y' → heal_legs(0)` else `return FALSE`); `youprop.h:136–138` (`Wounded_legs = HWounded || EWounded`).
**JS:** 2 files (`js/eat.js` +44/−4, `js/steed.js` +13/−4), under the 600/10 caps.
**Change:** `js/eat.js` — full royal-jelly fpostfx arm in C order (killer-bee `hero_form_data()?.mndx` + `Unchanging` morph break via live `polymon`; `gainstr(piece,1,true)`; Upolyd `mh` vs hero `uhp` `rnd(20)` + dual `botl`; `rn2(17)` `setuhpmax(+1,FALSE)`; `mh<=0 → rehumanize()`, `uhp<=0 → killer "rotten lump of royal jelly"/KILLED_BY_AN + done(POISONING)`; `!cursed → heal_legs(0)`), single-draw `cursed ? -rnd(20) : rnd(20)` preserving C short-circuit. `js/steed.js` — canonical wounded gate (`Wounded_legs || (HWounded&TIMEOUT) || EWounded`), `legs_in_no_shape('riding', false)`, C-verbatim `Heal your leg${s}?`, wizard check in the file's own `(debug||wizard)` idiom, `heal_legs(0)` on `y` else refuse. Same-edge static imports only (`heal_legs` via trap.js, `setuhpmax` via exper.js, `legs_in_no_shape` via trap.js, `TIMEOUT`/`POISONING` via const.js, `PM_QUEEN_BEE` via monsterNames — all `imports.mjs ALREADY`).
**Verify:** `node scripts/verify.mjs --fn heal_legs` → PASS syntax (2 files) · PASS rule2 · note hidden (vacuous, 0 blocked — row cited 0 blocks, shipped on public gates as the note directs) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7; `node scripts/hidden-proxy.mjs verify legs_in_no_shape` → 1 PASS (Healer-91123) PROGRESS; `node frozen/ps_test_runner.mjs sessions` → **44/44** full RNG+screen PASS. Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** none new. (done_eating envelope still names carrot blindness + EGG/other fpostfx otyps; mount_steed doc still defers its pre-existing list.)
**Next:** do not re-pop `heal_legs` (body + all `heal_legs(0)` sites live; map data.md:1053 updated) or `legs_in_no_shape` for the Healer-91123 riding refusal (PASS at HEAD). 7 Open rows remained after the two dones → refilled +1 (`potion.c` make_stunned, the only unparked/unarchived corpus owner) back to the 8-row floor; all other queue owners are live, archived, or parked.

## 2026-09-09 — D-2197 `mon.c` minliquid_core steed + engulfed-hero arms + `steal.c` mdrop_obj full body (map-driven; corpus PASS held)

**C locus:** `mon.c:975–981` steed gate (`mtmp == u.usteed && (Flying || Levitation) && !waterwall`); `mon.c:1088–1093` engulfing flush (`pline("%s sinks as %s rushes in and flushes you out.")`); `steal.c:808–849` `mdrop_obj` + `:852–872` `mdrop_special_objs`; `youprop.h:240,253` Flying/Levitation macros (`(H||E)&&!B`).
**JS:** 3 files (`js/mon.js` +75/−22, `js/dog.js` +1/−1, `js/shk.js` +1/−1), under the 600/10 caps.
**Change:** steed gate in C position (after inpool/inlava/infountain, before gremlin) with the youprop shape (flat cache or `(H||E)&&!B`, as in do.js); engulfing flush `pline` in C order (after drown pline, before mondied/xkilled) via live `hliquid` (display-RNG only under hallu, same as C); full `mdrop_obj` body in C order — distant_name observe, extract_from_minvent(FALSE,TRUE) + unlink fallback for untagged objs, tame-saddle no_charge via costly_spot + in_rooms SHOPBASE room match, verbosely pline, flooreffects "fall" gate before place/stack, update_mon_extrinsics when alive+worn — with dynamic shk.js/do.js imports (zero new static edges; imports.mjs hoisted-SAFE); specials on-map → `await mdrop_obj(FALSE)`, off-map → extract(TRUE,TRUE) + rloco; all four call sites await (mon.js migrate_mon/mongone, dog.js steed-Amulet, shk.js shopkeeper). Same-edge static imports only (W_SADDLE via const.js, hliquid via do_name.js, extract/update via worn.js). No DIAG/FORCE/seed/coordinate gates.
**Verify:** `node scripts/verify.mjs --fn minliquid_core` → PASS syntax (3 files) · PASS rule2 · PASS hidden 1 PASS (scen-genesis-Priest-91110; already PASS on the clean tree pre-edit — stale scoreboard row, still PASS post-edit) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7; then `node scripts/verify.mjs --full` → PASS full 44/44. Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** none new. (`mdrop_obj` verbosely-TRUE pline arm implemented though unreached from specials — same body, no extra debt.)
**Next:** do not re-pop `gush`/`minliquid_core` for these arms (0 blocked). Note: the `mon.c minliquid_core` symptom-owner park (eel/rat-bite step-130 premise) is stale at HEAD — its session replays PASS with zero js/ diff; park text left untouched for its owner to retire on rescore. Refill +2 (do_statusline2, next_ident) → 9 Open; all other hidden-proxy owners are parked/live, singletons stay Deferred while scen families < 90 %.

## 2026-09-09 — D-2196 `sounds.c` dosounds deferred room/guard arms: swamp You1, barracks/court You_hear, temple_priest, oracle canseemon, beehive/morgue bodies (map-driven, vacuous corpus verify)

**C locus:** `sounds.c:201–339` `dosounds` (swamp `You1(swamp_msg[rn2(2)+hallu])` + return `:234–243`; barracks `You_hear1(barracks_msg[rn2(3)+hallu])` on mercenary msleeping/count>5 `:277–299`); `throne_mon_sound :33–62` (which=rn2(3)+hallu: 0/1 You_hear1, 2 pline off-with-`uhis()`); `beehive_mon_sound :65–91` (rn2(2)+hallu switch incl. uarmh bonnet); `morgue_mon_sound :94–119` (undead||vampshifter, `body_part(HAIR)` + NECK/HEAD + `vtense` arms); `temple_priest_sound :137–185` (ispriest+inhistemple+!helpless+outside-temple gate, do-loop rn2(3+hallu) with speechless/in_sight retries, flag-strip, halu_gname %s arms); `oracle_sound :188–208` (always TRUE; print gated on Hallu||!canseemon, ora_msg[rn2(3)+hallu*2]); `hack.h:1028` You1≡You("%s"); `monst.h:251` helpless≡msleeping||!mcanmove.
**JS:** 1 file (`js/sounds.js` +~130/−~25), under the 600/10 caps.
**Change:** port all seven arms in C order with identical draw sequences (same rn2 calls, same gates). Swamp → `pline('You '+msg)` (You1 idiom); barracks/throne/beehive/oracle → live `You_hear`; throne-2 → `pline(... uhis() ...)` (`uhis` from roles.js); morgue → `pline` You/pline_The idioms with live `body_part`+`vtense` + `HAIR/NECK/HEAD` consts; temple → live `inhistemple`/`temple_occupied` (priest.js, existing edge), `EPRI` (const.js, existing edge), same-file `helpless` (matches monst.h macro), `cansee` (vision.js; imports.mjs hoisted-safe), `align_gname` (roles.js; imports.mjs no-cycle SAFE) for the %s arms, raw `data.msound` read for speechless (C reads `mtmp->data->msound` directly); oracle print gated on `Hallucination() || !canseemon(mtmp)` (both live imports), removing the visible-oracle over-burn. New static edges: roles.js (uhis/align_gname, SAFE), vision.js (cansee, hoisted-safe); priest/monsters/const/display/objnam/polyself edges already existed.
**Verify:** `node scripts/verify.mjs --fn dosounds` → PASS syntax · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — map-driven row, ships on the public gates; row cited no N blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7; then `node scripts/verify.mjs --full` → PASS full 44/44. Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** Soundeffect on throne/beehive arms (no audio backend); `Is_sanctum` in the temple gate (pre-existing); `findgd` migrating_mons (pre-existing); temple Hallu pantheon RNG (pray.js `halu_gname` defers it too — same debt).
**Next:** do not re-pop `dosounds` (0 blocked). No refill owed (queue stays ≥ 8 Open after archive; singletons still Deferred while scen families < 90 %).
