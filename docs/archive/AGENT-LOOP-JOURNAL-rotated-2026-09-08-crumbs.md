# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-07 — D-2046 music.c do_play_instrument ynq default 'q' + wind can_blow gate + Mastermind Soundeffect (queue owner do_play_instrument)

**C locus:** `music.c do_play_instrument :759–899` (read whole body + apply.c:4383 caller) + `include/hack.h:1330` `#define ynq(query) yn_function(query, ynqchars, 'q', TRUE)` — the default is `'q'` at both call sites (`:778` Improvise, `:788` passtune). Same-function arms read for parity: wind gate `:766–773` (`can_blow(&gy.youmonst)`, `mondata.c:566–576`), tune abort `:792` (`if (*buf == '\033')` — ESC only), Mastermind `Soundeffect(se_tumbler_click/se_gear_turn, 50)` `:851–863` (both-sound arm, click-only arm, and gears-only arm with NO sound call). `obj_to_instr` (`music.c:902`, `#if SND_LIB_INTEGRATED` mapping) read and deliberately not ported: contest build has no SND_LIB_* so C always returns `ins_no_instrument`, and JS `Hero_playnotes` is a no-op stub taking `_instr` — the argument value is unobservable.
**JS:** 1 file (`music.js` +25/−5), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + `hidden-proxy show` only). Small diff noted honestly: the 140-line function body already landed in D-0977 — this iteration completes its four remaining unfaithful arms, nothing else in the function diverges.
**Change:** `js/music.js` — both `yn_function` defaults `'y'`→`'q'` with hack.h:1330 citation; `if/else-if` gate mirroring C `:763–773` (`can_blow(game.youmonst)` — same call shape as the sibling apply.js whistle arms D-1007; `thesimpleoname` extends the existing objnam.js edge; text `You are incapable of playing …` is C `You("are incapable…")` verbatim); nevermind condition back to ESC-only per `:792`; `Soundeffect(se_tumbler_click, 50)` + `Soundeffect(se_gear_turn, 50)` in C order before `You_hear` (ids from `js/generated/seffects_data.js`, dbridge import pattern; no-op without SND_LIB per sndprocs.js, zero screen/RNG risk; gears-only arm keeps C's no-sound shape). New edges: music.js→mondata.js (`can_blow`, hoisted decl, `imports.mjs --can` cycle-safe — same 90-module SCC, runtime call only) and music.js→sndprocs.js/generated (no cycle). Underwater `You_cant` and `You(extract…)` arms left as `pline`: text-identical to C, no screen delta.
**Verify:** `node scripts/verify.mjs --fn do_play_instrument` → `PASS syntax 1 changed js file(s): js/music.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify do_play_instrument: 3 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-intrinsic-Healer-92168: PASS; scen-wish-Caveman-92174: PASS; scen-wish-Ranger-92156: PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `Hero_playnotes` audio + `obj_to_instr` mapping (unobservable while the stub takes `_instr`; file-header note stands); `can_blow` full is_silent table (mondata.js named omit — poly'd-silent-form edge stays approximate); no corpus session reaches the can_blow-blocked arm, the empty-tune arm, the passtune `'q'` arm, or the Mastermind sound arms — those four are C-cited but corpus-unverified, stated honestly.
**Next:** pop the next Open row (`monmove.c` m_search_items, 2 corpus blocks).

## 2026-09-07 — D-2045 pager.c do_screen_description monster arm: class-explain + lookat parenthetical (queue owner mondata.c name_to_monclass)

**C locus:** `pager.c do_screen_description` check_monsters (looked: `sym == gs.showsyms[i + SYM_OFF_M]`, `S_invisible` skipped, `an(def_monsyms[i].explain)` under an `encglyph`-plus-8-space prefix) + didlook `:1607–1640` (`lookat` fills `look_buf`, `*firstmatch = look_buf`, append `" (lookat)"`, found back to 1 for checkfile) + DEF_INVISIBLE arm `:1406–1417` (`EDetect_monsters & I_SPECIAL` or `Blind` → "unseen creature", else "remembered, unseen, creature"; no didlook: found stays 1, need_to_look clear) + `do_look` checkfile gate (`found == 1 && ans != LOOK_QUICK && ans != LOOK_ONCE && (VERBOSE || (help && !quick))`). Queue owner `name_to_monclass` (`mondata.c:1090–1176`) is a misattribution: it never prints (pure classifier; its live use was D-1098) — the matched literal is the `def_monsyms[].explain` table and the printing writer is do_screen_description via do_look/getpos. Proven by keys: `m` (getpos auto_describe → lookat buf "tame kitten"/"fox", matched both sides) then `,`/`.` (do_look → putmixed out_str, diverged).
**JS:** 2 files (`pager.js` monster arm + invisible arm + QUICK conjunct, `mondata.js` +11 helper), ~45 insertions total, under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (all diagnosis via recorded C frames + `hidden-proxy show` key/screen reads).
**Change:** `js/pager.js` — monster arm now: prefix `mon_glyph(mtmp).ch` (shown char, C encglyph; same source `look_all` uses); body `an(mlet_class_explain(mlet))` + ` (look)` with `first = look` unstripped (C didlook; empty-look guard keeps `first = explain` per C's `look_buf[0] != '\0'` gate); shown-`'I'` takes the C DEF_INVISIBLE arm (`u.EDetect_monsters & I_SPECIAL` game.u-field idiom + `u.Blind`, no parenthetical, no `[seen:]`); `[seen:]` suffix kept. `do_look` gate gains the missing `ans !== LOOK_QUICK` conjunct (same C function, C-literal; no blocked session reaches the `/`-then-`,` path, stated honestly). `js/mondata.js` — new exported `mlet_class_explain(mlet)` over the existing `DEF_MONSYM_MLET/EXPLAIN` tables (null when no row, C `explain && *explain` guard).
**Verify:** `node scripts/verify.mjs --fn name_to_monclass` → `PASS syntax 2 changed js file(s): js/mondata.js js/pager.js` · `PASS rule2` · `PASS hidden verify name_to_monclass: 1 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (kit-Monk-92007: PASS; genesis-Wizard-92223 → find_trap@81 was 59; normal-Knight-91100 → def_char_is_furniture@104 was 8) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. The QUICK conjunct was added after that run for the same C function; final verify re-ran after the last edit (no D-1831 gap) — tail pasted from the final run.
**Named:** mimic-disguised-as-furniture/object dispatch (C prints the furniture/object line with no monster-explain; JS `describe_looked` still takes the `mon_at`-first monster arm — pre-existing structure, out of unit); multi-class same-sym `append_str "or …"` (monster showsyms are unique letters in both symsets, so the single-row lookup is exact); `add_quoted_engraving` inside didlook (no-op unless the look target IS an engraving/grave); `coord_desc` suffixes (already deferred: `auto_describe_suffix` covers travel/valid only).
**Next:** (1) later owners find_trap (genesis-Wizard-92223@81) and def_char_is_furniture (normal-Knight-91100@104 — added to the refill below) are each their own future rows. (2) `do_look` `/`-then-`,` checkfile path (QUICK gate) has no corpus coverage — named here, not verified.

## 2026-09-07 — D-2044 objnam.c simpleonames quan!=1 makeplural + invent.c addinv_core0 thrown-autoquiver fill (queue owner wield.c doquiver_core)

**C locus:** (a) `objnam.c:2428–2442 simpleonames` — `minimal_xname`, then `if (obj->quan != 1L) makeplural`. The doquiver_core uswapwep prompt (`wield.c:616`) passes `simpleonames(uswapwep)` with quan 6/12, so C's plural comes from inside simpleonames. (b) `invent.c addinv_core0` thrown-autoquiver arm — `obj_was_thrown && flags.pickup_thrown && !uquiver && oartifact != ART_MJOLLNIR && otyp != AKLYS && (throwing_weapon(obj) || is_ammo(obj))` → `setuqwep(obj)` before the `added:` label, so the subsequent `pickup_prinv`→`prinv` prints the W_QUIVER suffix. The Valkyrie dagger qualifies on every conjunct (LOST_THROWN from the step-11 throw, default pickup_thrown, empty quiver, dagger is throwing_weapon via pierce-blade). Queue owner doquiver_core is the symptom owner here (string-literal match on "(at the ready)"); doquiver_core itself needed no change.
**JS:** 5 files (`objnam.js` +9/−7 incl. comment, `u_init.js` +22/−4 incl. imports/comment, `dothrow.js` +3/−2 export+comment, `uhitm.js` +3/−3, `iactions.js` +3/−3 comment), 51 insertions total, under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (one temporary console.error probe added and reverted mid-iteration after it proved the `ready` arm never runs on the Valkyrie path).
**Change:** `js/objnam.js` — `simpleonames` pluralizes via `makeplural(base)` when `((obj.quan ?? 1) | 0) !== 1` (missing quan reads as 1 — C always sets quan; same guard as the pre-existing iactions clone). `js/u_init.js` — `addinv` captures `objWasThrown` at entry and applies the C arm verbatim (flag default-on idiom `pickup_thrown !== false` per invent.js:4264; `throwing_weapon` newly exported from `js/dothrow.js` — same 90-module SCC, hoisted decl, runtime call, no TDZ risk; `ART_MJOLLNIR` extends the generated-data edge; AKLYS via house `objectNames.indexOf`) on both the merge-survivor and fresh-insert paths, before `addinv_core2` like C's pre-`added:` order. `js/uhitm.js` — dropped the `makeplural(simpleonames(otmp))` compensation wrapper at the mimic-appearance site to match C `uhitm.c:6234` exactly (required: `makeplural("orcish daggers")` is "orcish daggerses", not idempotent — measured).
**Verify:** `node scripts/verify.mjs --fn doquiver_core` → `PASS syntax 5 changed js file(s)` · `PASS rule2` · `PASS hidden verify doquiver_core: 0 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (Rogue-92115 → start_tin@118 was 18; Rogue-92030 → dofire@63 was 61; Valkyrie-92200 → step 82 was 46) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Follow-up `hidden-proxy show scen-normal-Valkyrie-92200`: step 82/148 screen-first menu/border diff (C row 1 `***…`, JS empty; owner unattributed, cMsgOwners empty), RNG 3278/3278 fully matched, error null — no throw in tree. Extra: full public `frozen/ps_test_runner.mjs sessions` → **44/44** (fortress holds under the global simpleonames change).
**Named:** `addinv_core0` quiver-prefer merge (merge into uquiver before general merge), `addinv_before`/`other_obj` reinsert, oname absorb, worn-slot merge, `how_lost` clearing to LOST_NONE (JS still reads stale how_lost — the merge compare-learn message depends on it); Eyes-of-the-Overworld `is_plural`/`Those` asymmetry at the uhitm mimic site (pre-existing, quan-1 path, untouched). The Mjollnir/aklys exclusions are live (ported as part of the arm).
**Next:** (1) Valkyrie-92200 step-82 menu-border diff → its own future row (unattributed tty/menu drawing, full RNG match). (2) later owners start_tin (Rogue-92115@118), dofire (Rogue-92030@63) are each their own future rows. (3) `name_to_monclass`, the next Open row, is untouched.

## 2026-09-07 — D-2043 uhitm.c mhitm_ad_slow mhitu + mhitm_ad_drst uhitm arms: the missing gates behind mhitm_mgc_atk_negated

**C locus:** `mhitm_ad_slow :3652–3689` (gate FALSE always `:3658`, `defended(mdef, AD_SLOW)` early return `:3660–3661`, mhitu branch `:3672–3676`: `hitmsg`, then `!negated && HFast && !rn2(4)` → `u_slow_down()`; damage untouched); `mhitm_ad_drst :3122–3142` (gate FALSE always `:3126`, uhitm branch `:3130–3142`: `!negated && !rn2(8)` → `Your("%s was poisoned!", mpoisons_subj)` with the youmonst→uwep arm (`mhitu.c:150`), `resists_poison` → "doesn't seem to affect", else `!rn2(10)` deadly (`damage = mhp`) or `damage += rn1(10, 6)`).
**JS:** 3 files (`mhitu.js` +26/−1, `uhitm.js` +58/−3, `mhitm.js` 1-word export), 82 insertions, under the 600/10 caps. No new module edges, no DIAG/FORCE/seed gates (Rule #2 clean); no committed probes.
**Change:** `js/mhitu.js` — `mhitm_ad_slow_u` in exact C mhitu-branch order (gate first, then hitmsg, then HFast+`rn2(4)` → awaited `u_slow_down`; leftover `d()` kept like FAMN) + `case AD_SLOW` + file-local `AD_SLOW = 13` (`monattk.h:55`, AD_STCK precedent); HFast tested C-literally (`youprop.h:374`: `u.HFast` flat mirror OR `uprops[FAST].intrinsic`, attrib.js Fast idiom minus extrinsic). `js/uhitm.js` — `damageum_ad_drst` in exact C uhitm-branch order + case + file-local `Your_u` (pline.c Your, zap/mhitu idiom) + `mpoisons_subj_u` with the C-exact youmonst→`u.uwep` arm (the mhitm.js mm-variant reads `MON_WEP(mtmp)`, which is not the wielded weapon for a poly'd hero). Same-edge import extensions only (`mhitm.js`: `mhitm_mgc_atk_negated`, newly-exported `resists_poison_mm`, `AD_DRST`; `weapon.js`: `MON_WEP`) — no `imports.mjs --can` call: pre-existing edges, hoisted function declarations, runtime calls only, no top-level TDZ read.
**Verify:** `node scripts/verify.mjs --fn mhitm_mgc_atk_negated` → `PASS syntax 3 changed js file(s)` · `PASS rule2` · `PASS hidden verify mhitm_mgc_atk_negated: 1 PASS, 2 moved past, 1 unchanged, 0 worse → PROGRESS` (genesis-Caveman-92118: PASS; poly-Valkyrie-92195 → u_stuck_cannot_go@172 was 169; wish-Archeologist-92038 → do_statusline1@185 was 143; wish-Monk-92013 still mhitm_mgc_atk_negated@75 screen-only `You avoid harm.--More--` vs `You avoid harm.` — the D-2005-Next residual: gate path byte-identical both sides incl. RNG, needs its own display iteration, not another gate port) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `mhitm_ad_slow` mhitm + uhitm branches (their own future rows; `mhitm_ad_were`-style mon→mon queue covers the family pattern); `mhitm_ad_drst` mhitm dispatch arm (`mhitm_really_poison` live, row named per mhitm_ad_phys D-1447); full `defended()` port (`mondata.c:91` + artifact `defends` blue-scales arm — RNG-free, deferred alongside the fire/cold `defended` omits); uhitm AD_SLOW arm.
**Next:** (1) wish-Monk-92013 display `--More--` residual → display iteration per D-2005-Next. (2) later owners `u_stuck_cannot_go`, `do_statusline1` are each their own future rows.

## 2026-09-07 — D-2042 dokick.c kickdmg: await tenth check_caitiff site (review 1006 C-wrong 3)

**C locus:** `nethack-c/upstream/src/dokick.c:68` `check_caitiff(mon);` inside `kickdmg :33–123`, synchronous, immediately before the `if (mon->mtame)` abuse block `:70–76`.
**JS:** 1 file (`dokick.js` +3/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes.
**Change:** `js/dokick.js` — `await check_caitiff(mon);` with a C citation comment (`dokick.c:68`, sync in C / async in JS for the awaited pline, must await to keep topline order). No new import (`check_caitiff` already imported from `./uhitm.js:42`); enclosing `kickdmg` already async, so no signature or edge change.
**Verify:** `node scripts/verify.mjs --fn kickdmg` → `PASS syntax 1 changed js file(s): js/dokick.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify kickdmg: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cites zero corpus blocks — Must-fix from written review, so public gates carry the ship) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Probe `node frozen/ps_test_runner.mjs sessions/seed4500-knight-coverage.session.json` → `PASS (RNG 108275/108275, Screen 1814/1814)`. Final verify ran after the last edit (no D-1831 gap).
**Named:** none new — every `kickdmg` callee is live; all ten `check_caitiff` sites now awaited.
**Next:** Must-fix queue is empty — pop the first Open row (`uhitm.c` mhitm_mgc_atk_negated, 4 corpus blocks).

## 2026-09-07 — D-2041 uhitm.c ranged silver predicate: mon_hates_silver disjunct (review 1006 C-wrong 2)

**C locus:** `nethack-c/upstream/src/uhitm.c:896` `if (hmd->material == SILVER && mon_hates_silver(mon))` inside `hmon_hitmon_weapon_ranged :884–917`; `mon_hates_silver` = `nethack-c/upstream/src/mondata.c:516–520` `return (boolean) (is_vampshifter(mon) || hates_silver(mon->data))`. The exact callee was already live in JS (`js/monsters.js:832–834`, D-1254) — the D-2036 port reached past it for the narrower predicate (D-1849 class).
**JS:** 1 file (`uhitm.js` +4/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; probe lives only in `/tmp` (uncommitted, outside the repo).
**Change:** `js/uhitm.js` — `mon_hates_silver` extends the pre-existing `./monsters.js` import (no new module edge per `imports.mjs --can`: already statically imported; sync callee, same 82-module SCC, no top-level TDZ read) and the ranged predicate is now `mon_hates_silver(mon)` with a C citation comment (`uhitm.c:896`, `mondata.c:516–520`). `hates_silver` stays imported (still used by the `Hate_silver()` hero wrapper at `:774`). Sibling silver sites already correct: `mhitm.js:3650`, `weapon.js:330/:587/:595/:602/:685` all call `mon_hates_silver`; the two remaining `hates_silver(data)` uses (`uhitm.js:774`, `artifact.js:319`, `dothrow.js:1063`) are the `youprop.h Hate_silver` hero macro, which is C-correct as `hates_silver(youmonst.data)`.
**Verify:** `node scripts/verify.mjs --fn mon_hates_silver` → `PASS syntax 1 changed js file(s): js/uhitm.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify mon_hates_silver: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cites zero corpus blocks — Must-fix from written review, so public gates carry the ship) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Pre-verify `/tmp/silver-vampshifter-probe.mjs`: vampire-shifted doppelganger (`cham=PM_VAMPIRE`, data `S_HUMAN`) → `is_vampshifter true`, `hates_silver(data) false` (old predicate missed), `mon_hates_silver(mon) true` (new predicate sears); plain-doppelganger control still false. Final verify ran after the last edit (no D-1831 gap).
**Named:** none new — the silver-sear *message* (`msg_silver` plumbing, absent on every hmon weapon path) stays named in turns.md. The sibling Must-fix row stays queued: `dokick.js:860` caitiff await (review C-wrong 3).
**Next:** pop the dokick caitiff-await row (add await at `js/dokick.js:860`, C `dokick.c:68`).
