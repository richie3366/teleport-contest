# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-06 — D-1955 vision.c new_angle vision-angle singleton

**C locus:** `nethack-c/upstream/src/vision.c` — the live locus is the `#else` macro `:461` (`#define new_angle(lev, sv, row, col) (*sv)`, "The other parameters are not used"). The `#ifdef EXTEND_SPINE` staticfn `:413–451` (CROSSWALL..TRWALL spine extension via `viz_clear`) is compiled out (`:366` keeps `/*#define EXTEND_SPINE*/` commented) — read in full, deliberately not ported. Live callers, all in the main update loop: `:749` (night/xray IN_SIGHT arm), `:776` (door/wall adjacent-lit arm), `:790` (lit else-arm); the xray/nv/pit `seenv = SVALL` arms never call it (direct assignment).
**JS:** `js/vision.js` (+13/−3; 1 js file, under 600 cap). Density note: C live locus is 1 macro line (the 37-line `#ifdef` body is compiled out); the JS is the export plus the three live caller wirings that retire the inline clones.
**Change:** `js/vision.js` — exported `new_angle(lev, sv, row, col)` (`sv | 0` int idiom; unused params kept for C shape per the C comment), placed after `seenv_matrix` with the `:461`/`:366` citation; all three sites now `| new_angle(loc, sv, row, col)` (`loc` ≡ C `lev` cell). File header + `docs/c-js-map/data.md` vision.c section updated. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn new_angle` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/vision.js`; `PASS rule2`; `note hidden verify new_angle: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). No hand probe: the arm runs every turn in every session, so the full suite is the probe.
**Named:** `#ifdef EXTEND_SPINE` staticfn body (`:413–451`) — compiled out in C, display-cosmetic T-wall spine extension, intentionally not ported (re-enabling it would DIVERGE from compiled C).
**Next:** pop the next Open row (`light.c` obj_adjust_light_radius).

## 2026-09-06 — D-1954 strutil.c pmatch glob-match predicate singleton

**C locus:** `nethack-c/upstream/src/strutil.c` — `pmatch` `:144–148` (`return pmatch_internal(patrn, strng, FALSE, (const char *) 0)`); guts `pmatch_internal` `:104–141` (`*` matches 0+ via `!*patrn || recurse(strng - 1)`, else recurse `(patrn - 1, strng)` on `s`; `?` matches any single char but NUL (`p != '?' || !s`); `ci` folds via `lowc`, A–Z only (`hacklib.c:83–86`); tail recursion is `goto pmatch_top`; `sk` skip-set arm `:119–127`). Live callers: `pager.c:1024–1025` checkfile (`pmatch(&buf[chk_skip], dbase_str)` pass 0, alt pass 1; `dbase_str` lcase'd `:866`); `cmd.c:664–665` doextlist search via `pmatchi`; `files.c:3154` debugcore (`#ifdef DEBUG`).
**JS:** 2 js files, 34 insertions/23 deletions, under 600 cap and 10-file cap. Density note: C locus is 3 lines (guts already live in JS since D-1625/D-1646); the JS is the export plus the live checkfile caller wiring that retires the regex clone.
**Change:** `js/cmd.js` — exported `pmatch(patrn, strng)` in C order (thin wrapper, `ci=false`), placed before `pmatchi` in C declaration order with both wrappers' C ranges cited; `pmatch_internal` header now cites `:104–141` plus the `goto`-to-loop shape. `js/pager.js` — `lookup_data_base` imports `pmatch` from `./cmd.js` (no new edge: `imports.mjs --can` ALREADY, pre-existing import) and calls `pmatch(key, q)` for the positive and the `~`-exclusion passes (C `:1024` + `:1023–1029` chk_skip arms), retiring both RegExp clones; keys keep file case (trim only) since C matches case-sensitively over `lcase(dbase_str)` — measured `dat/data.base`: 951 keys, 326 wildcard keys, exactly one uppercase key (`A.S*`, Arne Saknussemm engraving entry). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). Probe (/tmp, deleted, 19 lines): C truth table (`*`/empty/`?`/`A`-vs-`a`) all hold; 951 keys × 903 queries (858k comparisons) old-regex vs `pmatch` differ ONLY on the `A.S*` key (2 queries) — exactly the C case-sensitive behavior — PROBE PASS. `node scripts/verify.mjs --fn pmatch` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/cmd.js js/pager.js`; `PASS rule2`; `note hidden verify pmatch: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (forced `--full`: the lookup change is user-facing).
**Named:** `sk` skip-set arm (`:119–127` fuzzy variant) — no C caller passes non-null sk (`pmatchz` declared `extern.h:1265` but never defined in pinned C); checkfile pass-1 `alt` second pass (JS `lookup_data_base` is single-pass — pre-existing structure, not this predicate); `files.c` debugcore (`#ifdef DEBUG`, no JS counterpart).
**Next:** pop the next Open row (`vision.c` new_angle).

## 2026-09-06 — D-1953 eat.c tin_variety_txt tin-variety text singleton

**C locus:** `nethack-c/upstream/src/eat.c` — `tin_variety_txt` `:1405–1421` (`*tinvariety = -1`; loop `k < TTSZ - 1` skipping the `""` sentinel; `l = strlen(tintxts[k].txt)`; `!strncmpi(s, txt, l) && strlen(s) > l && s[l] == ' '` → `*tinvariety = k`, return `l + 1`; else return 0, null args return 0 with no write). `strncmpi` (`hacklib.c:717–739`) returns 0 over the first `n` chars (short `s` → nonzero, no match). Sole C caller: `objnam.c:4386` `d->tmp = tin_variety_txt(d->p + 7, &d->tinv)` (after the `strcmpi(spinach)` contents arm; result feeds `name_to_mon(d->p + 7 + d->tmp)` + `typ = TIN`).
**JS:** `js/eat.js` (+33/−1; 1 js file, under 600 cap). Density note: C is 16 lines; the JS is the one body plus file-idiom citation and the named-omission note.
**Change:** `js/eat.js` — exported `tin_variety_txt(s, out)` in C order (`s != null && out` guard, no write on null; `-1` default; `TTSZ - 1` sentinel skip; `str_start_is` + explicit `s.length > l && s[l] === ' '` so bare "rotten"/"rottenx …" miss; `out.tinvariety = k`, return `l + 1`; else 0). `{tinvariety}` out-idiom per the `artifact_name` convention in `js/readobjnam.js`. Placed after `tin_variety`, header tin list updated.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn tin_variety_txt` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/eat.js`; `PASS rule2`; `note hidden verify tin_variety_txt: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C singleton, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full` (tool: no shared file changed). Probe (/tmp, deleted): 13 prefix arms (rotten/ROTTEN/homemade/soup-made-from/french-fried/deep-fried/pureed/sauteed + spinach/rot/bare-rotten/rottenx/empty misses) plus both null guards — PROBE PASS.
**Named:** caller wiring (function live, unwired): `objnam.c:4386` readobjnam "tin of " arm (spinach `strcmpi`→`TIN_SPINACH` contents, `tvariety`/`contents`/`mntmp` fields, `name_to_mon` offset parse, `typ = TIN`, downstream `TIN_EMPTY`/`TIN_SPINACH` spe/corpsenm + `set_tin_variety` postparse) has no JS counterpart — `js/readobjnam.js` has no tin arm at all (own-row material, not this singleton).
**Next:** pop the next Open row (`strutil.c` pmatch).

## 2026-09-06 — Audit D-1944…D-1952 against pinned C (reviews 914–922) + cadence score

Review iter (no `js/` edits): 9 JS-touching SHAs since `82cffe79`, one SHA at a time, file written per SHA. 914 selection_sub (ACCEPT — Set-membership ≡ union cell loop, recalc endpoints verified vs selvar.c:98–165). 915 stealamulet (ACCEPT — branch/RNG call-for-call vs steal.c:688–767, all callees LIVE). 916 move_gold (ACCEPT — 13-line C ≡ JS, ox/oy pre-save). 917 ucatchgem (ACCEPT — call site verified at mthrowu.c:687–692). 918 mon_hates_light (ACCEPT — 5-line wrapper). 919 exercise_steed (ACCEPT — call site verified at hack.c:2879–2883). 920 On_ladder (ACCEPT — 7-line predicate). 921 doclassdisco (ACCEPT-WITH-DEBT — full #knownclass envelope ≡ o_init.c:888–1127 branch-by-branch; debt review-listed: choose_disco_sort items lack C's letter selectors, auto a–d instead; function unwired, no Must-fix). 922 mazexy+is_solid (ACCEPT — isSolidTile NOT FOUND post-delete, consts ≡ decl.c:827/840 defaults). Every hidden-proxy re-run `--base <SHA>~1` showed 0 blocks at baseline and now, matching each D-log's vacuous note — no false PASS anywhere. Rule #2 clean. No Must-fix; queue untouched (10 Open rows, in the 8–12 band).
Cadence: full `sessions` 44/44 at `615d3ea7` — Scr 11,405/11,405, RNG 792,838/792,838 (identical to D-1943 audit), speed `54+0.53/turn` (R² 0.90).
Next: pop the next Open row (`eat.c` tin_variety_txt).

## 2026-09-06 — D-1952 mkmaze.c mazexy + is_solid maze solidity predicates

**C locus:** `nethack-c/upstream/src/mkmaze.c` — `is_solid` `:69–73` (`return (boolean) (!isok(x, y) || IS_STWALL(levl[x][y].typ))`); `mazexy` `:1316–1350` (`allowedtyp = corrmaze ? CORR : ROOM`; `rnd(x_maze_max)`/`rnd(y_maze_max)` loop with `++cpt < 100`, then systematic x-outer/y-inner scan, then `panic("mazexy: can't find a place!")`). `is_solid` sole live caller: `wall_cleanup` `:218–221` (8-neighbor gate turning surrounded walls to STONE). `mazexy` callers: `populate_maze` ×5 (`:1103–1120`), `makemaz` stairs ×2 (`:1209,1212`), `mklev.c` branch/stair sites (`:1665,1710,2092`), `restore.c:1251`. `gx.x_maze_max`/`gy.y_maze_max` init `(COLNO-1)&~1` / `(ROWNO-1)&~1` (`decl.c:827,840`).
**JS:** `js/mklev.js` (+34/−6; 1 js file, under 600 cap). Density note: C is 37 lines (3 + 34); the JS is the same two bodies plus file-idiom citations.
**Change:** `js/mklev.js` — exported `is_solid(x, y)` in C order (`!isok(x | 0, y | 0) || IS_STWALL(...)`, `||` short-circuit so `at()` never reads out of bounds; `| 0` int idiom); removed the `isSolidTile` clone and pointed `wall_cleanup` at `is_solid` (live, behavior-identical). Exported `mazexy(cc)` in C order (`allowedtyp` from `game.level?.flags?.corrmaze`; `rnd(X_MAZE_MAX)`/`rnd(Y_MAZE_MAX)` with the C `1+rn2(N)` comment; systematic x-outer/y-inner scan; loud `throw` ≡ C panic per the `lev_json.js` idiom). Same-module callees only — no new import edge, no `imports.mjs --can` needed.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn mazexy` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mklev.js`; `PASS rule2`; `note hidden verify mazexy: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none for both — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed).
**Named:** caller wiring (functions live, unwired): `populate_maze` / `makemaz` stair arms / `create_maze` / `maze0xy` / `walkfrom` / `maze_remove_deadends` stay deferred under the map-named `D-0906 create_maze` omit (so `mazexy` uses the restored `X/Y_MAZE_MAX` consts; the `create_maze` scaled-bounds mutation has no JS counterpart yet); `iswall`/`iswall_or_stone` remain local `isWallTile`/`isWallOrStone` clones (pre-existing, not this cluster).
**Next:** pop the next Open row (`eat.c` tin_variety_txt).

## 2026-09-06 — D-1951 o_init.c doclassdisco class-discovery singleton

**C locus:** `nethack-c/upstream/src/o_init.c` — `doclassdisco` `:888–1127` (discosort normalize; `menu_requested → choose_disco_sort(2)`; unique `'u'` scan; artifact `'a'` count; packorder+VENOM class collection; traditional `yn_function` vs menu class pick incl. `MENU_PARTIAL` single-class skip; `'u'/'r'`, `'a'`, default display arms; text window iff `ct`). Helpers in the cluster: `choose_disco_sort` `:611–657`, `disco_fmt_uniq` `:725–741`, `disco_output_sorted` `:743–766`, `discovered_cmp` `:553–565`, `oclass_to_name` `:877–886`; `artifact.c` `disp_artifact_discoveries` `:1147–1175`, `dump_artifact_info` `:1177–1215`. Sole C caller: extcmd `` ` `` `knownclass` (`cmd.c:1752`) + `cmd.c:60` extern decl.
**JS:** `js/o_init.js` (+309/−4), `js/artifact.js` (+61/−1; 2 js files, 365 insertions, under 600 cap). Density note: C is ~420 lines across the cluster; the JS carries the full envelope plus file-idiom JSDoc/imports.
**Change:** `js/artifact.js` — exported `disp_artifact_discoveries(lines)` (null lines ≡ WIN_ERR count-only; A_NONE prints "non-aligned" since JS `align_str` has no unaligned arm) and `dump_artifact_info(lines)` (live `"  %-36.36s%s"` shape, `#if 0` tab arm dead in C; `game.artiexist.rnd` ≡ C `.rndm`), ATR headers per the `dodiscovered` convention. `js/o_init.js` — `DISCO_ORDER_LET`/`DISCO_ORDERS_DESCR`/`UNIQ_OBJS` consts; file-local `discovered_cmp` (strcmpi past the mark; stable `Array.sort` per Constitution §4), `disco_output_sorted` (lootsort rewrite kept for shape), `disco_fmt_uniq` (C outbuf → return), `oclass_to_name`; exported `choose_disco_sort(mode)` (mode-2 note, title-as-first-item per `rename_disco`; 1 pick / −1 dismiss; PICK_ONE kills the C n>1 arm; preselect highlight has no pick_one equivalent); exported `async doclassdisco()` in C order (`'o'` normalize writes back `game.flags.discosort`; menu_requested gate; `lootsort=false` named; traditional `yn_function(prompt, discosyms, NUL)` with unseen-syms+ESC envelope and `!c‖NUL → clear + return`; `MENU_PARTIAL` single-class skip; class menu with menulet-order selectors and C group accelerators as `gselector`, incl. `'r'` for unique; `'u'/'r'` upstart header + `disco_fmt_uniq` loop; `'a'` wizard `y_n` dump vs discoveries; default `def_char_to_objclass` + `impossible`-then-fall-through + sortname header + bases-range loop; `show_text_pages` iff `ct`; `ECMD_OK`).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn doclassdisco` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/artifact.js js/o_init.js`; `PASS rule2`; `note hidden verify doclassdisco: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full` (tool: no shared file changed). Probes (/tmp, deleted): import smoke (no TDZ/cycle); count-mode 0 empty / 2 seeded with `  Excalibur [lawful …]` lines; dump 34 lines with `%-36s` padding; both exports live. (Bare-node `init_objects` RNG-seed failure is a probe-harness limit — ISAAC needs game boot, not a port fact.)
**Named:** discosort `'s'` sortloot order (`sortloot_descr` needs `loot_classify`, `invent.c:149–305` — own-row material; falls back to discovery order under a `'sortloot' order` header); caller wiring (function live, unwired): extcmd `` ` `` `knownclass` (`cmd.c:1752`; the `extcmdlist_data.js` row exists, no `cmd.js` dispatch arm); `choose_disco_sort` current-sort preselect highlight (no pick_one equivalent); `dodiscovered` `menu_requested`/`choose_disco_sort(1)` arm stays as named in `js/invent.js` (function now live for future wiring).
**Next:** pop the next Open row (`mkmaze.c` mazexy + is_solid).
