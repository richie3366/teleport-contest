# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-08 — Audit D-2080…D-2088 against pinned C (reviews 1050–1058) + cadence score 44/44.

9 JS-touching SHAs since 251f209e, each re-verified with hidden-proxy verify --base HASH~1: 7 reproduce the D-log corpus claim exactly (later moves strictly forward, 0 worse); 1051's 2-vs-3 session delta is the stale 84dc0e34 baseline (3rd session corroborated moved off-owner at HEAD); 1052's disclosed REGRESSION exonerated (Healer-92092 chest wished/applied steps 121+, cannot paint step-58 lembas/do_statusline2 — pre-existing parked pair). 8 ACCEPT, 1 QUALITY-RISK (1054: readobjnam gold-block fires on tin-typed bp, C return-2 skips Case 3 — Must-fix prepended, Next cluster set). Watch item, no queue row: Healer-92092 drift 130→58 entered under D-2080/D-2081 (D-2081 display change candidate) without appearing in either verify — stale-baseline blind spot for screen-first sessions outside the replay set. Fortress 44/44 (Scr 11405/11405, RNG 792838/792838, 64+0.38/turn). Rule #2 clean. check-hot-docs --review 1050-1058 all ok. Queue 11 (1 Must-fix + 10 Open), no refill.

## 2026-09-08 — D-2088 monmove.c disturb woke the Rogue-level ghost silently and display.js lacked the ROGUESET object-symbol swap, so "wakes up!" never printed and floor armor drew `[` not `]` (queue owner roguename, a string-match symptom owner)

**C locus:** `monmove.c:327–360` `disturb` — `:355` `wake_msg(mtmp, !mtmp->mpeaceful)` before `msleeping = 0`; `mon.c:4321–4331` `wake_msg` — prints only `if (msleeping && canseemon)`, `'!'` when interesting, plus flesh-golem suffix. Unmasked second half in the same turn: `drawing.c:72` `def_r_oc_syms` (rogue armor `]`, amulet `,`, food `:`, gold `*`) installed by `symbols.c:201` `init_rogue_symbols` and swapped into showsyms by `assign_graphics(ROGUESET)` (do.c goto_level).
**JS:** 2 files (monmove.js +5/−5, display.js +20/−4), under the 600/10 caps. Small because both envelopes were already live (disturb predicates D-0278, wake_msg body + wakeup wiring D-0922, assign_graphics/goldsym D-0805); density exception applies (corpus-driven Open row with a recorded expectation — D-2085/D-2086/D-2087 precedent).
**Change:** `js/monmove.js` — `wake_msg` joins the pre-existing `./mon.js` import (imports.mjs ALREADY, no new edge; hoisted function decl, call-time use only, no TDZ); `disturb` is now async and awaits `wake_msg(mtmp, !mtmp.mpeaceful)` in verbatim C position (pre-clear, so the `msleeping` gate sees the sleeper); sole caller mcalcmove `:2167` awaits it. `js/display.js` — new `DEF_R_OC_SYM` (armor `]`, amulet `,`, food `:`) + `oc_display_sym(oclass)` gated on `currentgraphics === ROGUESET` (exactly C's showsyms-swap state, same goto_level driver); all four object-char sites (`objnum_to_display_glyph`, Hallu-statue memory, Hallu-random, `obj_glyph` map path) read through it; COIN keeps the existing `_goldsym` line (same value). No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn roguename` → PASS syntax (2 changed files) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Tourist-91101 step 89 → `maybe_destroy_item` at step 164, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after; no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Intermediate `verify --fn roguename` after the wake_msg half alone showed the C-identical topline with NO MOVEMENT (same step, same owner) — the rogue-glyph half is what advanced the recorded expectation, not a scope add.
**Named:** none new — retires `wake_msg` on `disturb` (map turns.md:2596/2619) and the rogue object-symbol swap (map turns.md:2085). Still named: `hack.js` themed-room enter wake (`rn2(3)`, COURT/SWAMP/MORGUE/ZOO) still defers wake_msg — level-gen path, canseemon-false so C-silent, own future row; `detect.js` object_detect stays primary per `detect.c:628` (C reads `def_oc_syms` directly, not showsyms); RogueIBM/`load_symset`/full showsyms (walls/monsters/colors) still deferred.
**Next:** do not re-pop `roguename` (0 blocked). Tourist-91101's `maybe_destroy_item`@164 flows through the normal queue.

## 2026-09-08 — D-2087 trap.c resists_elem dropped species mresists, so a fire-resistant master lich on a fire trap drew thitm/rn2(6) instead of burnarmor's rn2(5) (queue owner burnarmor)

**C locus:** `monst.h:270` `mon_resistancebits(mon)` = `data->mresists | mextrinsics | mintrinsics` + `mondata.c:129–199` `Resists_Elem` (hero uses `u.uprops`, monsters use the bits, then wielded/worn/carried checks); `trap.c:1744–1797` `trapeffect_fire_trap` — `resists_fire` true skips the whole `thitm`/`rn2(num+1)` block and falls into `if (burnarmor(mtmp) || rn2(3))`.
**JS:** 1 file (trap.js +9/−4), under the 600/10 caps. Small because the whole fire-trap envelope was already live (D-0254/D-0491); density exception applies (corpus-driven Open row with a recorded expectation — D-2082/D-2085/D-2086 precedent).
**Change:** `js/trap.js` `resists_elem` now ORs `mtmp.data.mresists` for monsters only — `is_youmonst` gate preserves hero behavior (C ignores species bits for the hero; hero resists ride intrinsics/extrinsics). Same-module edit, no new import or cycle (`is_youmonst` is local at trap.js:1365, hoisted; `mr_bit` already equals the MR_* mask, monflag.h:62–69).
**Verify:** `node scripts/verify.mjs --fn burnarmor` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Wizard-91112 step 72 → `lightdamage` at step 73, a later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after; no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** none new — this retires the `data->mresists` species-bits omission on `resists_elem` (map data.md:1025) for `resists_fire` AND `resists_sleep` (same C `Resists_Elem`). Still named: wielded-artifact `defends`, worn/carried `oc_oprop` + alchemy-smock pair, carried-artifact `defends_when_carried` (mondata.c:175–196); burnarmor materialnm helm descr; grease_protect polish.
**Next:** do not re-pop `burnarmor` (0 blocked). Wizard-91112's `lightdamage`@73 flows through the normal queue.

## 2026-09-08 — D-2086 muse.c rnd_misc_item treated See_invisible as false, so a peaceful watchman never drew the case-1 rn2(6) (queue owner rnd_misc_item)

**C locus:** `muse.c:2654–2686` `rnd_misc_item` — switch `rn2(3)` case 1 `:2677–2680`: `if (mtmp->mpeaceful && !See_invisible) return 0; return rn2(6) ? POT_INVISIBILITY : WAN_MAKE_INVISIBLE;` with `See_invisible (HSee_invisible || ESee_invisible)` (`youprop.h:152`).
**JS:** 1 file (makemon.js +8/−3), under the 600/10 caps. Small because C is 32 lines and the rest of the function was already live (D-0749); density exception applies (corpus-driven Open row with a recorded expectation — D-2081/D-2082/D-2084/D-2085 precedent). No DIAG/FORCE/seed gates; no frozen files; /tmp driver kept out of the repo.
**Change:** `js/makemon.js` — file-local `See_invisible_misc()` (`youprop.h:152` cite; H||E+sticky flat, the muse.js:2147/trap.js/mhitm.js file idiom; `game` already imported, no new module edge, no TDZ risk) and case 1 is now `if (mtmp.mpeaceful && !See_invisible_misc()) return 0;` in verbatim C position; doc-comment omission retired (map `turns.md:2976` updated).
**Verify:** `node scripts/verify.mjs --fn rnd_misc_item` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Ranger-92033 step 70 → `selection_rndcoord` selvar.c:302, re-attributed same step; matched RNG 15034→15152, +118; the rnd_misc_item draws now match and the new owner is a later, unrelated subsystem) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit (map/docs edits only after; no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** none new — this retires the last one on `rnd_misc_item` (See_invisible treat-as-false). Untouched/still live: `mplayer.js:320` mk_mplayer `rnd(3)` loop shares the fixed function and benefits with no edit.
**Next:** do not re-pop `rnd_misc_item` (0 blocked). Ranger-92033's `selection_rndcoord`@70 flows through the normal queue.

## 2026-09-08 — D-2085 mon.c mpickstuff dropped the shop gates, prize skip and corpse thru-arms, so a non-tame monster in a shop never drew rn2(25) (queue owner mpickstuff)

**C locus:** `mon.c:1847–1910` `mpickstuff` — `:1852–1853` `if (mtmp->isshk && inhishop(mtmp)) return FALSE`; `:1856–1857` `if (!mtmp->mtame && *in_rooms(mtmp->mx, mtmp->my, SHOPBASE) && rn2(25)) return FALSE`; `could_reach_item` pool gate; `is_mines_prize/is_soko_prize` skip; `mon_would_take_item`; `CORPSE && mlet != S_NYMPH && !touch_petrifies(&mons[corpsenm]) && corpsenm != PM_LIZARD && !acidic(...)` → continue (a handful of corpse types thru to `can_carry()`).
**JS:** 1 file (monmove.js +20/−10), under the 600/10 caps. Small because the whole pickup envelope (could_reach_item, mon_would_take_item, can_carry, splitobj, distant_name, mpickobj, check_gear_next_turn) was already live; density exception applies (corpus-driven Open row with a recorded expectation — D-2081/D-2082/D-2084 precedent). No DIAG/FORCE/seed gates; no frozen files.
**Change:** `js/monmove.js` — ported the missing C guards verbatim in C order (isshk+inhishop, then !mtame → in_rooms(SHOPBASE) → rn2(25), so the draw stays shop-gated by short-circuit; empty-string in_rooms is falsy like C `*p == 0`): `inhishop` joins the pre-existing `./shk.js` import, `touch_petrifies`+`acidic` join the pre-existing `./monsters.js` import (same-SCC edges only, no new module, no TDZ — call-time use), `PM_LIZARD` const via `monsterNames.indexOf` (file + dogmove.js idiom); `in_rooms`/`SHOPBASE`/`rn2`/`is_mines_prize`/`is_soko_prize`/`mons()` were already imported. Corpse arm uses `mons(otmp.corpsenm)` (null-safe: out-of-range → null → both predicates false). Floor objects always carry nonzero `o_id` (`mkobj.js:352,1772`; the `tin.o_id = 0` at `:2632` is the dealloc'd context record, never a floor obj), so the prize skip cannot false-positive while prizes are undesignated.
**Verify:** `node scripts/verify.mjs --fn mpickstuff` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Tourist-92100 step 100 → `level_tele`@108, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2082/D-2084 precedent).
**Named:** none new — this retires four from the mpickstuff doc comment: shopkeeper inhishop gate, in_rooms shop rn2(25), prize skip, corpse petrify/lizard/acidic thru-arms (map `turns.md:2665` updated). Untouched/still named: `pline` vs C `pline_mon` message routing (D-0840 hitmsg shape stands; no blocked session reaches it), m_search_items-envelope shop arms (different function, `turns.md:2674` line untouched).
**Next:** do not re-pop `mpickstuff` (0 blocked). Tourist-92100's `level_tele`@108 flows through the normal queue.

## 2026-09-08 — D-2084 objnam.c postparse1 dropped the " of <monster>" corpse-type arm, so "figurine of a newt" never truncated to "figurine" and srch drew nothing where C draws rn2(26) (queue owner rnd_otyp_by_namedesc)

**C locus:** `objnam.c:4371–4397` `readobjnam_postparse1` corpse-type block — the wand/spellbook/gauntlets/gloves/finger guards, then `tin of` → spinach/tvariety/`name_to_mon`/typ=TIN/`return 2`, else `strstri(bp," of ")` + `name_to_mon(p+4) >= LOW_PM` → `*d->p = 0` (bp «figurine of a newt» becomes «figurine», mntmp=newt). srch (`:4749`, `rnd_otyp_by_namedesc(actualn, oclass, 1)`) then matches OBJ_NAME «figurine» with maxprob 26.
**JS:** 1 file (readobjnam.js +60/−4), under the 600/10 caps. Small because srch, the FIGURINE/CORPSE spe + corpsenm hunks, and the TIN contents arms were already live; density exception applies (corpus-driven Open row with a recorded expectation — D-2081/D-2082 precedent). No DIAG/FORCE/seed gates; no frozen files.
**Change:** `js/readobjnam.js` — ported the full conditional verbatim in C position (ahead of the no-`of` scan; glob intercept above it stays map-named): guards via live `strstri`, `tin of` → spinach exact / `tin_variety_txt` offset + `name_to_mon` `{gender}` box + `d.typ = TIN` (fall-through is typfnd-equivalent — every later block is `!d.typ`-guarded and the no-`of` scan is a proven no-op on no match), else `of`-truncate via slice (C `*d->p = 0`) with the `mgend` box; `d` gains C-init defaults `contents: TIN_UNDEFINED`, `tvariety: RANDOM_TIN` (live `const.js` export), `mgend: -1` (`:3946–3949`); `:5342–5344` tail wired (`tvariety >= 0 && (rn2(4) || wizardMode())` keeps C `||` draw order) with `set_tin_variety`. `name_to_mon` joins the pre-existing `./mondata.js` import; `tin_variety_txt`/`set_tin_variety` from `./eat.js` (`imports.mjs --can`: same 90-module SCC, both hoisted functions — cycle-safe; import smoke passes).
**Verify:** `node scripts/verify.mjs --fn rnd_otyp_by_namedesc` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden `0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Samurai-92239 → checkfile@92, screens 107/130; Priest-92179 → checkfile@31 with RNG now FULLY matched 3097/3097, screens 43→100/108 — same-step re-attribution to a different owner, which §3 counts as a later owner; row diff read, not assumed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last js/ edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Probe `/tmp/figprobe.mjs` (prefix replay, deleted): wished figurine carries corpsenm=125 (Archon) — the parse/mntmp chain is proven live; the residual «figurine.» vs «figurine of an Archon» is display-only (xname, see Next).
**Named:** none new — this retires `tin of`/`of`-split + `mgend`/`contents`/`tvariety` parsing + `set_tin_variety` wiring (map `turns.md` readobjnam + eat sections updated). Untouched/still named: glob intercept, `of spinach` strip, `ishistoric`/`wetness`/`ftype`/`zombify` wish-prefix parsing.
**Next:** do not re-pop `rnd_otyp_by_namedesc` (both sessions left it). Residual is C `objnam.c:709` xname TOOL_CLASS `if (typ == FIGURINE && omndx != NON_PM)` « of %s%s» arm — JS xname renders CORPSE/STATUE corpsenm but has no FIGURINE arm (`js/objnam.js:663–683`); own xname row on rescore.
