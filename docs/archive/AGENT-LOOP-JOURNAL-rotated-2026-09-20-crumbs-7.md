# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-20 — D-2630 `dungeon.c` insert_branch whole-body port (extract panic, prev_val sort guard, callers wired)

**C locus:** ``nethack-c/upstream/src/dungeon.c:462–508`` (insert_branch, decl ``extern.h:869``); callers ``:534`` (add_branch ``insert_branch(new_branch, FALSE)``), ``:1156`` (fixup Knox ``insert_branch(br, TRUE)``), ``mklev.c:2655`` (mk_knox_portal ``insert_branch(br, TRUE)``).
**JS:** ``js/dungeon.js`` ``export function insert_branch`` (same name/signature, callers untouched). No other files touched.
**Change:** restarted the export in C order with per-arm ``:line`` cites — extract identity scan (``:469–472`` ⇔ ``indexOf``); missing → ``throw new Error('insert_branch: not found')`` (``:474–475`` panic; throw ≡ C panic per botl.js compare_blstats precedent); unlink (``:476–479`` ⇔ ``splice``); ``next = null`` (``:481``); ``branch_val`` macro (``:484–487``, existing helper untouched); ``prev_val = -1`` (``:493``), ``new_val`` (``:494``), guarded scan + ``insertAt`` (``:495–500``), link (``:501–507`` ⇔ ``splice``). ``.next`` stays null by design: no JS reader traverses branch.next and save.js JSON-copies branches (a linked chain would duplicate on save) — noted in code. First handoff dropped the ``insertAt = i`` assignment (bare ``break``), so every insert appended at end: verify caught it as 4 corpus REACH regressions + cohort 6/7, all print_dungeon branch-order diffs; a stash control proved the old code passed seed0383; restored the assignment and all pass.
**Verify:** ``node scripts/verify.mjs --fn insert_branch`` → VERIFY: PASS (syntax 1 file js/dungeon.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate; same under ``--reach-all``). Previously-regressed sessions replayed directly after the fix, all full PASS: scen-tour-Healer-92042 (95/95 scr, 18888/18888 RNG), explore-seed0360-db38e7fa (881/881, 121124/121124), random-seed0367-c32340b1 (352/352, 50086/50086), random-seed4500-1d7125fb (1842/1842, 108275/108275). Throwaway probes ``/tmp/ib_probe.mjs`` + ``/tmp/ib_probe2.mjs`` (sorted/extract-reinsert/panic 3/3, not committed).
**Named:** none — every callee is live or file-local (``branch_val``); C next-pointer surgery ⇔ array ``splice`` (save-safe, map-named here).
**Next:** pop the next Open — coverage row.

## 2026-09-20 — D-2629 `end.c` build_english_list whole-body port (SYSCF WIZARDS English list + wordcount/bel_copy1)

**C locus:** ``nethack-c/upstream/src/end.c:1823–1859`` (build_english_list, extern ``extern.h:1003``); static callees ``wordcount :1793–1806``, ``bel_copy1 :1809–1820``; callers ``cfgfiles.c:806`` (cnf_line_WIZARDS) + ``sys/unix/unixmain.c:659`` (platform main).
**JS:** ``js/end.js`` ``export async function build_english_list`` (same name/signature, returns the string) + file-local ``wordcount``/``bel_copy1``/``isEndSpace`` (staticfns stay file-local); ``impossible`` joins the existing display.js import (``imports.mjs --can``: no new edge). No other files touched.
**Change:** ported the whole C body in C order with per-arm ``:line`` cites — ``wordcount`` blank/word run counting (``:1797–1803``); ``bel_copy1`` skip-blanks + copy-word + advance-cursor (``:1813–1818``; cursor object stands in for ``char **inp``, return accumulation for ``out += strlen(out)``); ``build_english_list`` case 0 ``impossible`` (``:1835``), case 1 single (``:1839``), 2-word ``"first or second"`` (``:1842–1844``), N-word ``"first, second, or third"`` do/while (``:1846–1849``), trailing ``"or "`` + last word (``:1852–1853``). ``alloc``/sizing arithmetic (``:1827–1832``) has no representable effect on growing JS strings (map-named). ``isspace((uchar))`` is the file-local six-blank C-locale set (options.js ``isOptSpace`` precedent).
**Verify:** ``node scripts/verify.mjs --fn build_english_list`` → VERIFY: PASS (syntax 1 file js/end.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). Throwaway probe ``/tmp/bel-probe.mjs`` (not committed): 1/2/3/4-word, padded/multi-blank/tab/newline, ``*`` wildcard, and blank→``''`` arms all match C-derived expectations 9/9.
**Named:** ``alloc`` length precompute (GC strings); ``cnf_line_WIZARDS`` caller + ``fmtd_wizard_list`` consumers (no JS SYSCF layer); ``unixmain.c`` caller (platform main).
**Next:** pop the next Open — coverage row.

## 2026-09-20 — D-2628 `pickup.c` pick_obj whole-body restart (get_obj_location engulfer path, fromfloor/ushops fidelity)

**C locus:** ``nethack-c/upstream/src/pickup.c:1897–1942`` (pick_obj); caller ``:1879`` (pickup_object ``obj = pick_obj(obj)``); ``shk.c:3008`` is a comment naming the function, not a call; decl ``extern.h:2451``.
**JS:** ``js/pickup.js`` ``export async function pick_obj`` (``:1007``, same name/signature; caller untouched) + ``get_obj_location`` joining the existing ``timeout.js`` import (``imports.mjs --can``: no new edge). No other files touched.
**Change:** restarted the export in C order with per-arm ``:line`` cites — ``fromfloor`` sampled before extract mutates ``where`` (``:1900``, ``|0`` int compare); ``(void) get_obj_location(otmp, &ox, &oy, 0)`` via the live ``timeout.js`` export with ``null → 0,0`` matching C's ``*xp = *yp = 0; return FALSE`` (``:1901–1905``); ``robshop`` gate without the invented guards (``:1907``; ``u.uball`` is the C global ``uball`` per module precedent ``:3102``); extract + ``newsym``-when-fromfloor (``:1908–1910``); the ``:1912–1920`` ordering comment preserved; shop arm with fake-ushops round-trip and recompute off restored ushops (``:1924–1934``; ``''`` models ``'\0'`` — ``strchr(s,'\0')`` is always non-null so both read false); ``addinv`` then ``remote_burglary``-when-robbed-outside (``:1937–1940``). ``addtobill``/``addinv``/``remote_burglary`` awaited (async engine, sync in C).
**Verify:** ``node scripts/verify.mjs --fn pick_obj`` → VERIFY: PASS (syntax 1 file js/pickup.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). No committed unit test: repo has no tests/ layout (D-2606 precedent); fortress gates cover.
**Named:** none new — every true callee is live (``get_obj_location`` timeout.js, ``costly_spot``/``addtobill``/``remote_burglary`` shk.js, ``obj_extract_self`` mkobj.js, ``newsym`` display.js, ``in_rooms`` hack.js, ``addinv`` u_init.js). ``rob_shop``/``setpaid``/``clear_unpaid``/``impossible`` in the brief callee list appear only in the C ``:1912–1920`` comment, not the body — not calls, nothing to wire; the shk.js ``get_obj_location`` local clone is pre-existing clone drift, untouched (own row if emitted).
**Next:** pop the next Open — coverage row.

## 2026-09-20 — Audit 1578–1586 (reviews 84e0e295..f6b591c3: D-2619…D-2627, 9 ACCEPT, 0 Must-fix) + cadence 44/44

9 JS-touching SHAs since audit 564ba152, each reviewed one-at-a-time against pinned C with per-arm cites: get_hilite (rule engine exact; LONG_MAX→MAX_SAFE_INTEGER cited), act_on_act (36 arms exact; doidtrap/auto_describe OMIT, map-cited, currently unreachable — bare-TypeError hardening note, no Must-fix), loot_xname (80 L exact; D-2626 closes the sortloot_cmp wiring note), obj_meld (4 boundary fixes + 2 awaited callers), chat_with_leader (Rules 0–3 + re-gated banished + livelogs), trapeffect_hole (5 arms; wormno>5→count_wsegs fix), readchar_core (all 4 ifdefs verified live in unix headers), sortloot_cmp (full tail; greatest_erosion macro-exact clone, no export exists), init_objects (repair loop present; shuffle_tiles compiled out — TTY_TILES_ESCCODES off). Every per-SHA `hidden-proxy verify --base HASH~1 --reach-all` re-run ends REACH-OK, no REGRESSED session. Cadence: public 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, 57+0.33/turn), held-out 11/44 unchanged (5,972 pts, RNG 26.7 %, screens 53.0 %), corpus 497/540 (92.0 %, RNG 99.31 %, screens 99.1 %) +0/−0. Refill @f6b591c3: 5 fresh rows verbatim (role_menu_extra/m_use_undead_turning/mon_givit/gather_locs/cond_menu), queue back to 12 coverage.
**Next:** pop `pickup.c` pick_obj (Open PARTIAL, queue head).

## 2026-09-20 — D-2627 `o_init.c` init_objects whole-body restart (generic-class panic, oc_name_known repair, callee sanity arms, tiles named)

**C locus:** ``nethack-c/upstream/src/o_init.c:151–234`` (init_objects); callees ``setgemprobs :54–83``, ``init_oclass_probs :240–267``; callers ``allmain.c:783`` (newgame, must precede u_init), ``hack.c:4430`` (dump_weights), ``options.c:7281`` (comment only).
**JS:** ``js/o_init.js`` ``export function init_objects`` (``:265``, sync — callers unchanged) + ``raw_printf``/``tty_wait_synch`` joining the existing display.js import (no new edge). No other files touched.
**Change:** restarted the export in C order with per-arm ``:line`` cites — bases zero + generic-class panic as throw with the C message (``:156–162``; botl.js compare_blstats precedent — JS has no sync abort); name/descr identity init (``:163–165``); ascending-class range walk + GEM ``setgemprobs(null)``/``randomize_gem_colors`` (``:166–192``); NUM_OBJECTS extra entries incl. doclassdisco guard (``:202``); gap-fill inherit-next (``:207–209``); ``oc_name_known`` loop (``:211–226`` — OBJ_DESCR ≡ ``objectDescrs[oc_descr_idx]``, OBJ_NAME ≡ ``objectNameStrs[oc_name_idx]``, ``iflags.sanity_check`` ≡ ``game.iflags?.sanity_check``, ``impossible`` floating per in-file undiscover_object precedent, repair ``nmkn?0:1``); ``init_oclass_probs`` (``:227``); ``shuffle_all`` (``:230``); WAN_NOTHING oc_dir (``:234``). Callee arms restored in the same commit: ``setgemprobs`` corrupt-gems guard (``:70–75`` — ``raw_printf`` live sync, ``tty_wait_synch`` floating like ``impossible``); ``init_oclass_probs`` zero/negative ``impossible`` (``:255–256``). The ``setgemprobs`` dlev ``lev`` line is untouched (D-0893 owns it; identical to C whenever dungeons exist, i.e. every reachable state).
**Verify:** ``node scripts/verify.mjs --fn init_objects`` → VERIFY: PASS (syntax 1 file js/o_init.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 80/80 PASS 0 regressed → REACH-OK, same under ``--reach-all`` 497/497 PASS 0 regressed; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). No committed unit test: repo has no tests/ layout (D-2606 precedent); new arms are unreachable-on-real-data sanity guards, fortress-covered.
**Named:** ``shuffle_tiles`` (both the ``:232`` init site and the ``restnames :434`` restore site are ``#ifdef TILES_IN_GLYPHMAP``-only; JS has no glyphmap[]/tileidx machinery — display.js named omit — so there is no live arm to wire).
**Next:** pop the next Open — coverage row.

## 2026-09-20 — D-2626 `invent.c` sortloot_cmp whole-body port (class/subclass/disco, invlet, loot-name, BUCX/grease/erosion/erodeproof/enchant tail)

**C locus:** ``nethack-c/upstream/src/invent.c:403–547`` (sortloot_cmp, staticfn); caller ``:634`` (sortloot qsort) + dead ``#if 0`` 3.6.0-revamp direct call ``:671``.
**JS:** ``js/invent.js`` ``export function sortloot_cmp`` (``:2275``) + file-local ``greatest_erosion`` (``:2257``, obj.h macro mirror — dig/lock/u_init/weapon precedent, no new import edge) + ``let sortlootmode`` (``:2265``); ``sortloot`` rewired (``:2442``), ~50 inline lines deleted. No new cross-module import (all callees in-file: inuse_classify/loot_classify/invletter_value/loot_xname).
**Change:** exported ``sortloot_cmp(sli1, sli2)`` in C order with per-arm ``:line`` cites — INUSE classify-once + bigger-first + indx tiebreak (``:412–428``); PACK|INVLET class gate (``:430–432``), loot_classify-once + orderclass/subclass/disco asc (``:435–468``); invlet letter (``:471–476``); LOOT==0 → tiebreak (``:478–479``); format-once name cache + A-Z-fold compare (``:481–499``); BUCX 3/2/1/0 bigger-first (``:501–504``); grease desc (``:506–510``); greatest_erosion asc, bigger-is-WORSE (``:512–516``); erodeproof 1/0 desc (``:517–523``); enchant unknown→-1000 desc under oc_uses_known && !=FOOD_CLASS (``:525–539``, obj1-gated per C); indx tiebreak (``:544–546``). ``sortloot`` sets module ``sortlootmode`` (= C gs.sortlootmode ``decl.h:873``) around ``items.sort`` and resets to 0 after (``:634–636``); post-sort str free is GC (``:638–640``).
**Verify:** ``node scripts/verify.mjs --fn sortloot_cmp`` → VERIFY: PASS (syntax 1 file js/invent.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK, same under --reach-all; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). No committed unit test: repo has no tests/ layout (D-2606 precedent); fortress gates cover.
**Named:** ``dupstr`` (identity — JS strings are immutable values, the loot_xname result is already an owned copy); ``maybereleaseobuf`` (no-op — no static obuf; loot_xname builds fresh per call); ``unsortloot`` (separate C fn — GC, own coverage row if emitted).
**Next:** pop the next Open — coverage row.
