# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-15 — D-2354 `makemon.c` bagotricks: bad-bag impossible, perm_invent refresh, tipcontainer BoT-target arm

**C locus:** `nethack-c/upstream/src/makemon.c` `bagotricks` `:2554–2601` (`:2562–2563` bad-bag `impossible("bad bag o' tricks")`, moncount stays 0; `:2568–2571` empty `cknown = 1` + `update_inventory()`; `:2591–2594` seen `makeknown(BAG_OF_TRICKS)` + `update_inventory()`); caller `pickup.c` `tipcontainer_checks` `:3961–3966` (`targetbox->otyp == BAG_OF_TRICKS` → `bagotricks(targetbox, FALSE, &seencount)`, `TIPCHECK_CANNOT`); gettarget exclusion `:3912–3916` (known BoT is skipped from the target menu, so the checks arm only ever fires for undiscovered BoT).
**JS:** 2 js files (+25/−5: `js/apply.js` +20/−4, `js/pickup.js` +10/−1). Under the 600-insertion / 10-file caps. Small insertion matches small C (44 lines).
**Change:** `js/apply.js` — `impossible` joins the existing `display.js` import (`imports.mjs --can` ALREADY, no new edge); bad-bag arm awaits `impossible("bad bag o' tricks")` then returns 0; both tails call sync `update_inventory()` (already imported) with C line citations. `js/pickup.js` `tipcontainer` — first check after gettarget/cancelled (C order: the targetbox arm leads `tipcontainer_checks`): undiscovered-BoT target awaits dynamic `bagotricks(targetbox, false, { n: 0 })` (existing dynamic-import pattern, no new static edge; local seencount discarded as in C) and returns (C `TIPCHECK_CANNOT`); dropped "BoT-target apply" from the Named-omissions header. No new modules, no new module edges.
**Verify:** `node scripts/verify.mjs --fn bagotricks` → `PASS syntax 2 changed js file(s): js/apply.js js/pickup.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify bagotricks: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. `/tmp/probe-bagotricks.mjs` (deleted after run): null-bag 0, wrong-otyp 0, empty-undiscovered 0 + cknown untouched, empty-known-tipping 0 + cknown 1 (exercises `update_inventory`), tipcontainer BoT-target wiring present — all ok.
**Named:** `tipcontainer_checks` otrapped `chest_trap`/`nomul`, recursive `tipcontainer_checks(targetbox)` for non-BoT targets, `get_obj_location` floor-coordinate sync, `subfrombill` after floor shop BoT/horn emptying, targetbox shop-bill per-item `addtobill` (pre-existing `tipcontainer` header names, unchanged); `bagotricks` charged-path `makemon`/`canseemon`/`sensemon` behavior unchanged (D-1023 core).
**Next:** do not re-pop `bagotricks` for the debt.md D-1023 line. Falsifier: a rescore or fresh `verify bagotricks` showing a session blocked with bagotricks as owner (not mere doapply/tipcontainer caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2353 invent.c getobj: drop/apply/rub prompt clones retire to live getobj

**C locus:** `nethack-c/upstream/src/invent.c` `getobj` `:1751–2089`; call sites `do.c:35-36` (`getobj("drop", any_obj_ok, GETOBJ_PROMPT|GETOBJ_ALLOWCNT)`), `apply.c:4226` (`getobj("use or apply", apply_ok, GETOBJ_NOFLAGS)`), `apply.c:1793` (`getobj("rub", rub_ok, GETOBJ_NOFLAGS)`); NULL arms `invent.c:1710-1716` any_obj_ok, `apply.c:4153-4154` apply_ok, `apply.c:1772-1773` rub_ok (all GETOBJ_EXCLUDE → no hands arm, so the swap keeps the null return shape).
**JS:** 2 js files (+24/−251). Under the 600-insertion / 10-file caps.
**Change:** `dodrop` (shop + non-shop arms), `doapply`, `dorub` call live `getobj` with the C word/ok/flags; deleted the three clones + six single-use letter helpers (`drop_raw_lets`, `drop_suggest_lets`, `apply_lets`, `apply_prompt_lets`, `apply_has_downplay`, `rub_suggest_lets`); `do.js` imports `getobj`+`GETOBJ_PROMPT`+`GETOBJ_ALLOWCNT` (existing edges) and drops five now-unused helper imports + `mark_topline_prompt`; `apply.js` adds `GETOBJ_NOFLAGS` (existing const edge). No new modules, no new module edges (`imports.mjs --can` not owed: both files already import `invent.js`/`const.js`).
**Verify:** `node scripts/verify.mjs --fn getobj` → `PASS syntax 2 changed js file(s): js/apply.js js/do.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify getobj: no corpus session is blocked on it at HEAD` (vacuous, NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `PASS full 44/44 passing (auto: shared file changed)` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite (D-1834/D-1927 precedent); drop/apply/rub prompts are exercised across the public 44 and the full suite stayed 44/44 across the swap.
**Named:** remaining `getobj_*` clones with verb-specific shape (wield/ready hands-return mapping `wield.js`, dip/dip_into/dip_ok/drink_ok `potion.js`, stash `pickup.js`, charge `artifact.js`, write_on `write.js`, zap `zap.js`, rub_on_stone dynamic-word `apply.js`) — own rows when a falsifier fires; `display_pickinv` body + `readchar_core` fuzzer/queue/ALTMETA (pre-existing map names).
**Next:** next Open queue row in order (`cmd.c` getdir direction machinery). Refill refused: all 23 live `hidden-proxy queue` tops are parked symptom/misattributed owners or archived/shipped (do_statusline2/obj_resists/distfleeck/m_move/one_characteristic/rloc/spoteffects/mon_adjust_speed/mdrop_obj/break_armor/dodown/drinkfountain/disclose/list_vanquished/dog_invent parks verified this iter; collect_coords/doturn/were_change/mcast_death_touch likewise archived/parked), and every unqueued TOP30 row is already an Open row or parked — queue sits at 7 Open until a rescore surfaces a new owner; do not invent filler (Constitution §10.13, mon_adjust_speed-park precedent).

## 2026-09-15 — D-2352 `pickup.c` count-N PICK_ONE arm drops PICK_ANY-only FEEL_COCKATRICE abort + SORTLOOT_PETRIFY (review 1316 Must-fix)

**C locus:** `nethack-c/upstream/src/pickup.c` `pickup` `:761–772` (count-N arm: `"Pick %d of what?"` + PICK_ONE `n_or_more`, no FEEL_COCKATRICE) vs `:774–776` (PICK_ANY arm: `traverse_how | FEEL_COCKATRICE`); `query_objlist` gates both the `SORTLOOT_PETRIFY` augment and the CORPSE `will_feel_cockatrice` menu-destroy/`look_here(0)` abort on `qflags & FEEL_COCKATRICE`.
**JS:** 1 js file (+11/−4). No new modules, no new module edges.
**Change:** `js/pickup.js` only (+11/−4, no new modules, no new imports — `PICK_ANY` already imported): `sortflags` starts `(how === PICK_ANY) ? SORTLOOT_PETRIFY : 0`, and the CORPSE `will_feel_cockatrice` abort is gated on `how === PICK_ANY`; C-cited comments on both gates plus the doc header. PICK_ANY path byte-identical behavior.
**Verify:** `node scripts/verify.mjs --fn pickup` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; the row cited 0 corpus blocks and review 1316 re-measured 0 blocked at `b214fb72~1`, so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS (green 2/2 + strict ×2). No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite (D-2350 precedent); the count-N arm is reached by no public or corpus session (review 1316 states full-suite green is the backstop) and `query_objlist_pickup` is file-local async over display/input, infeasible to unit-probe headless.
**Named:** none new.
**Next:** do not re-pop `query_objlist_pickup`/`pickup` for this arm. Falsifier: count-N pickup over a feelable-cockatrice-corpse pile, C menu vs JS abort. No seed/step/coordinate gates.

## 2026-09-15 — D-2351 `end.c` done_in_by vampire-bat arm polarity (`!==` → `===`)

**C locus:** `nethack-c/upstream/src/end.c` imitator arm (`else if (alt && strstri(realnm, "vampire") && !strcmp(fakenm, "vampire bat"))` → `fakenm = "bat"`, comment: prefer "vampire in bat form" over "vampire in vampire bat form"); reachable for vampshifter killers (`cham` ∈ vampire/leader/Vlad, `monst.h:217-219`, fog-or-bat forms).
**JS:** 1 js file (+3/−2). No new modules, no new module edges.
**Change:** `js/end.js` only (+3/−2, no new modules, no new imports): predicate `!==` → `===` with a C-cited comment (`end.c` imitator arm `!strcmp(fakenm, "vampire bat")` → `"bat"`).
**Verify:** `node scripts/verify.mjs --fn done_in_by` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; the row cited 0 corpus blocks so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. `/tmp/probe-vampbat.mjs` truth table (bat/fog/vampire vs C `!strcmp`) + shape consequence (bat→"bat", fog→"fog cloud") PROBE PASS; probe deleted. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS (green 2/2 + strict ×2).
**Named:** none new — ghost arms stay named per D-2341.
**Next:** do not re-pop `done_in_by` for this arm. Falsifier: a shifted-vampire killer epitaph trace contradicting the `===` arm above. No seed/step/coordinate gates.

## 2026-09-15 — Audit aa08fdb3..b214fb72 (reviews 1314-1316: 1 ACCEPT, 2 QUALITY-RISK + Must-fix) + cadence 44/44

Each JS-touching SHA re-audited against pinned C (csym body+callers, sym on re-point targets, added-line banned grep, rulecheck, independent hidden-proxy verify --base re-run). 1314 QUALITY-RISK: aa08fdb3 fixed its 1307 Must-fix (imitator index compare verified arm-for-arm incl. null/birth cases), but walking the function surfaced the D-2341 vampire-bat arm firing on `!==` where C fires on equality (`!strcmp(fakenm,"vampire bat")`) — shifted-vampire epitaphs contradict C both ways; Must-fix prepended (`!==` → `===`), Next cluster set. 1315 ACCEPT: 18f6e38f shk_owns prefix arm-for-arm (order/trailing-space), all 14 clone re-points land on live C-faithful canonicals (Eyes delta toward C), TDZ late-bind disclosed; one stale `shk_your` jsdoc omit line noted, not queued. 1316 QUALITY-RISK: b214fb72 gate/FOLLOW/count/autoselect all verified vs C, but the new PICK_ONE arm inherits the ungated FEEL_COCKATRICE abort + PETRIFY augment (C sets both only at pickup.c:774-776 PICK_ANY); Must-fix prepended (gate on `how === PICK_ANY`). Cadence: full sessions 44/44, Scr 11405/11405, RNG 792838/792838, speed 47+0.29/turn. Queue at 10 unchecked (2 Must-fix + 8 Open) — no refill.

## 2026-09-15 — D-2350 `pickup.c` pickup floor-pickup body: dbridge pool/lava gate, count-N PICK_ONE, engulfer chain, all_but_uchain

**C locus:** `nethack-c/upstream/src/pickup.c` `pickup` `:672–910` (objchain `:741–747` BY_NEXTHERE vs nobj FOLLOW; autopick `:755` + menu_pickup `:779–791`; AUTOSELECT_SINGLE `:761`; count-N `:766–772` "Pick %d of what?" + PICK_ONE n_or_more + force-to-N; PICK_ANY all_but_uchain `:774–776`; pool/lava gate `:700–706`); `n_or_more` `:458–465`, `all_but_uchain` `:507–512`; `query_objlist` n==0 `:1069–1070`, autoselect `:1072–1077`, count fix-up `:1189–1190`; `dbridge.c` `is_pool` `:46–59`, `is_lava` `:62–73`.
**JS:** 1 js file (+136/−50), no new modules, no new module edges.
**Change:** `js/pickup.js` only, no new modules, no new module edges (`PICK_ANY` joins the existing const.js import; `is_pool`/`is_lava` were already imported from hack.js — no `imports.mjs --can` owed). Autopickup gate now calls live `is_pool(u.ux,u.uy)`/`is_lava(u.ux,u.uy)` (D-1077/D-1090). File-local `n_or_more` (uchain reject + quan>=N via module `val_for_n_or_more`, set/reset around the query with try/finally) and `all_but_uchain` (C staticfn ⇒ file-local, D-2345 idiom).
**Verify:** `node scripts/verify.mjs --fn pickup` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; the row cited presence "reached by 374 corpus traces", not blocks, and `brief pickup` lists none blocked — so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. `node scripts/verify.mjs --full` → PASS full 44/44. No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite; the count-N and swallowed arms are reached by no public or corpus session (hand UI probes infeasible headless; the autoselect/no-menu arms are covered by the 44/44 fortress).
**Named:** query_objlist `:1060–1062` single-worn-engulfer-item autoselect suppress (unreachable: manual `,` while swallowed routes to loot_mon via dopickup; autopickup never takes the query arm; pickup_object already prints the worn refusal); clone INCLUDE_VENOM/menu_head_objsym/page-invert (pre-existing named); `count_unpaid` nobj-walk on floor query_classes heads (pre-existing, outside this locus); `u.Underwater` vs `u.uinwater` mirror (pre-existing, untouched).
**Next:** do not re-pop `pickup`. Falsifier: a fresh `verify pickup` blocking, or a count-prefix `,` session trace / swallowed-autopickup trace contradicting the arms above. No seed/step/coordinate gates.
