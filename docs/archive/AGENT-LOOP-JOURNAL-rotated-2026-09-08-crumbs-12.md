# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-08 — D-2107 `makemon.c` birth knowledge: wand experience + trap learning (queue row `zap.c` zap_hit, 1 session)

**C locus:** `makemon.c:1283–1294` (birth knowledge: `In_sokoban && !mindless` → `mon_learns_traps(PIT)` + `(HOLE)`; `Is_stronghold && !mindless` → `(TRAPDOOR)`; `MS_LEADER/MS_NEMESIS` → `(ALL_TRAPS)`; `Is_stronghold||Is_knox||In_endgame||In_hell||In_V_tower||In_quest` → `mwandexp=TRUE`) + `muse.c:1834` (`mwandexp ? buzz : buzz_force_miss` — first wand always misses) + `dungeon.c:1942` (`In_hell` = `dungeons[dnum].flags.hellish`) + `mondata.h:64` (`mindless` = `M1_MINDLESS`).
**JS:** 1 file (`makemon.js`, +24/−1), under the 600/10 caps. Small diff because the C paragraph is ~12 lines (density exception: C is that small). No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — the corpus session reaches the changed arm (first wand shot after a quest birth).
**Change:** `js/makemon.js` only — birth-knowledge block in exact C order between `female` and `mpeaceful` (C order is female → traps → mwandexp → place_monster → mpeaceful; the JS `place_monster`/fmon-link shape is untouched). Same-module import extensions only (`Is_stronghold/Is_knox/In_V_tower/PIT/HOLE/TRAPDOOR/ALL_TRAPS` join the existing const.js edge, `mon_learns_traps` joins the existing monsters.js edge — both `imports.mjs --can` ALREADY, no new edge, no TDZ); `In_hell` via this file's existing hellish-flag idiom (cf. S_BAT `:2929`, D-0928 #1119).
**Verify:** `node scripts/verify.mjs --fn zap_hit` → `PASS syntax 1 changed js file(s): js/makemon.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify zap_hit: 0 PASS, 1 moved past (1 re-attributed at the same step), 0 unchanged, 0 worse → PROGRESS` (scen-tour-Priest-92235 → `resist`@59 was `zap_hit`@59; row diff: C `rn2(119)=42 @ resist(zap.c:6141)` vs JS `rn2(20)=16 @ zap_hit(zap.js:1335)`, prev `d(6,25)=57 @ zhitm` — C's `zap_hit` draw is now positionally matched, rngM 5019→5402 (+383), and the beam now reaches zhitm damage + the resist check) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `mpeaceful` `MM_ANGRY` force-hostile arm (`makemon.c:1297`, adjacent line, different theory — peace outcome, not birth knowledge; left for its own row); `mwandexp` save/restore persistence (no `save.js` field — no corpus recipe saves mid-run; stays as mapped).
**Next:** do not re-pop `zap_hit` (0 blocked; body faithful). The moved session rests at `resist`@59 (same step, later owner) — leave it to the queue refill; do not invent rows.

## 2026-09-08 — D-2106 `hack.c` domove_fight_empty statue arm via live gbuf: F at a displayed statue prints "harmlessly attack a statue" (queue row `dokick.c` ghitm, 2 sessions)

**C locus:** `hack.c` `domove_fight_empty` `:2258–2267` (`boulder = sobj_at(BOULDER, x, y)`; `if (glyph_is_statue(glyph) || (Hallucination && glyph_is_monster(glyph))) boulder = sobj_at(STATUE, x, y)` — full-pile scan, unconditional overwrite) + `:2296–2315` buf (`ansimpleoname(boulder)`) + `:2319–2322` (`!(boulder||solid) ? "" : "harmlessly "` + `attack` + buf). `glyph` is `glyph_at(x, y)` (`display.c:2478` = on-screen `gbuf`, live or remembered), `glyph_is_statue` is `display.h:835`, `glyph_to_obj` maps it to `STATUE` (`display.h:904`).
**JS:** 1 file (`cmd.js`, +15/−10), under the 600/10 caps. Small diff because the C arm is 5 lines (density exception: C is that small). No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — both corpus sessions reach the changed arm.
**Change:** `js/cmd.js` only, exact C order: full-pile `sobj_at(BOULDER)` scan first, then `glyph_to_obj_at(x, y) === STATUE_OTYP` (pre-existing `display.js` gbuf export joining the existing display edge — no new module, no TDZ, called at runtime) → unconditional overwrite with a full-pile `STATUE` scan (null when shown-but-absent, e.g. unsensed mimic-as-statue, exactly like C's `sobj_at` miss); target `a statue` vs `a boulder` (what C `ansimpleoname` yields for the common case).
**Verify:** `node scripts/verify.mjs --fn ghitm` → `PASS syntax 1 changed js file(s): js/cmd.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify ghitm: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-genesis-Caveman-92199 → `use_pole`@54 was 16; scen-poly-Ranger-92090 → `do_statusline2`@273 was 238, both strictly later steps with different owners) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** forcefight dig-with-pick arm (`use_pick_axe2`, `:2270–2279`), `Underwater` gate + underwater buf arms (`:2256`, `:2300–2306`), `Hallucination && glyph_is_monster` statue arm (`:2262–2264`), full `ansimpleoname` (named/inscribed statues) — all map-named (turns.md fight_empty line).
**Next:** do not re-pop `ghitm` (0 blocked; body faithful). The two moved sessions rest at `use_pole`@54 and `do_statusline2`@273 — leave both recorded owners to the queue refill; do not invent FAIL peels.

## 2026-09-08 — D-2105 invent.c addinv_core1 uhave/ACH arms: wished Bell/Candelabrum now record achievements (queue row end.c disclose, 3 sessions)

**C locus:** `invent.c` `addinv_core1` `:960–1004` (uhave/ACH arms). The disclose suffix itself (`end.c:664–680` via `count_achievements`, `insight.c:2493–2501`) is live since D-2015; the three recipes wish quest regalia into invent (Bell of Opening ×1, Candelabrum of Invocation ×2) via `hold_another_object`→`addinv`, where C records ACH_BELL/ACH_CNDL (acnt > 0) and JS recorded nothing (acnt = 0).
**JS:** 1 file (`u_init.js`, +57/−9 incl. docs), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — corpus sessions reach every changed arm (Bell ×1, Candelabrum ×2; prize arms share the verified body, no corpus session reaches them yet).
**Change:** `js/u_init.js` — new sync `addinv_core1(obj)` in exact C order, called once before the merge loop (C `addinv_core0` `:1082`): COIN → disp.botl (`flags.botl`+`disp.botl`); AMULET_OF_YENDOR → `uhave.amulet=1`+ACH_AMUL; CANDELABRUM_OF_INVOCATION → `menorah=1`+ACH_CNDL; BELL_OF_OPENING → `bell=1`+ACH_BELL; SPE_BOOK_OF_THE_DEAD → `book=1`+ACH_BOOK; `oartifact` → `set_artifact_intrinsic` W_ART (existing line moved in); then the separate mines/soko-prize chain (`record_achievement` + achieveo oid clear + `nomerge=0`). Imports: `is_mines_prize`/`is_soko_prize` join the existing mkobj edge, ACH_* join the existing const edge, `record_achievement` is a new static insight.js edge (`imports.mjs --can` SAFE — hoisted function, same 90-module SCC, no TDZ). Already-have `impossible()` arms omitted (async-pline precedent, cf. artifact.js:496).
**Verify:** `node scripts/verify.mjs --fn disclose` → `PASS syntax 1 changed js file(s): js/u_init.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify disclose: 1 PASS, 2 moved past, 1 unchanged, 0 worse → PROGRESS` (wish-Monk-92031 PASS; genesis-Barbarian-91118 →save_dungeon@113 was 110; wish-Priest-92098 →save_dungeon@88 was 85; wish-Priest-92179 still disclose@100 with matching toplines — map-cell diff, different mechanism) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `addinv_core1` questart/`artitouch` arm (no live `artitouch`/`is_quest_artifact` export — clone-only); `addinv` quiver-prefer/worn-slot/oname/globby/`#adjust`/lamplit-timer defers stay as mapped (turns.md).
**Next:** do not re-pop `disclose` for these three (0 blocked on the conduct prompt). The two moved sessions rest at `save_dungeon` (Parked misattributed owner — true writers `done_in_by`/`print_mapseen` per that row; leave to queue refill, do not pop `save_dungeon`). Priest-92179@100 is a map-cell diff under a matching topline — separate writer, separate row.

## 2026-09-08 — D-2104 `insight.c` enlightenment final-disclosure family: Upolyd background/handed/XP/hit-dice/transformed/hide + wallet hidden_gold + Invisible + Jump/Teleport (queue row enlightenment, 4 sessions)

**C locus:** `insight.c` `background_enlightenment` Upolyd form arm (`:491–511`: current-gender adj when the form is genderless, `vampshifted` cham `just_an` phrasing, `!final ? "currently " : ""`) + role-line `"actually "` prefix (`:529`) + handed `"normally "` when `body_part(HANDED) != "handed"` (`:565–569`); `basics_enlightenment` Upolyd hit-dice (`:753–766`), wallet `hidden_gold(final)` continuation (`:787–808`: `!hmoney ? "." : !umoney ? ", but" : ", and"` + `you have/had <hmoney> [more|<currency>] stashed…`); XP line suppressed while poly'd (`:687–709` `if (!Upolyd)`); `status_enlightenment` Upolyd `transformed [+ and felt/feel <dead/condemned/empty> inside]` (`:964–973` via `polyself.c` `ugenocided`/`udeadinside`); `attributes_enlightenment` Invisible trio (`:1628–1636`) + Jumping/Teleportation/Teleport_control (`:1675–1685`); `one_characteristic` Upolyd `hide_innate_value` (`:845–936`: plain value while poly'd).
**JS:** 1 file (invent.js +160/-26), under the 600/10 caps. Open row ships alone (one cluster: enlightenment + its tight helpers; the live `one_characteristic` Open row scen-wish-Priest-92180 is a different symptom — mattock vs pick-axe wielding line — and is untouched).
**Change:** `js/invent.js` only, same-module import extensions only (`const.js` INVIS/G_GENOD/TELEPORT/HANDED, `roles.js` genders, `objnam.js` just_an, `monsters.js` is_male/is_female/is_neuter/vampshifted/nonliving/weirdnonliving — all ALREADY edges, no new module, no TDZ): background Upolyd form line + `actually ` role prefix; handed `normally ` iff `Upolyd(u)` and `body_part_latebound(HANDED) !== 'handed'` (non-poly keeps `""` even under the objnam fallback, so the fortress is untouched); Basics hit-dice (`0 hit dice (actually 1/2)` / `1 hit die` / `N hit dice`) + wallet `hidden_gold(!!final)` continuation with C-exact terminators and lowercase `you have/had` stash line; XP block under `if (!Upolyd(u))`; shared `status_core_lines` Upolyd `transformed` with `mvitals` G_GENOD + nonliving/weirdnonliving `inside` suffix; final + overlay Invisible trio (`Invisible ≡ (H||E)&&!B` incl. uprops, `from_what(-INVIS)` → `""` for the blocked-visible arm); final-path Jumping/Teleportation (flats + uprops) + `hero_Teleport_control`; `one_characteristic_line_final` Upolyd early-plain-return. Doc comment omissions updated (c-js-map startup.md:24 carries the D-2104 delta).
**Verify:** `node scripts/verify.mjs --fn enlightenment` → PASS syntax (1 changed js file: js/invent.js) · PASS rule2 (no fs/path/url/node:, no DIAG/FORCE/seed gates) · PASS hidden `2 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Healer-92147 PASS; Knight-92045 PASS; Monk-92000 step 67 → chwepon@68; Wizard-92120 step 48 → chwepon@49, both strictly later steps with a different owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS, final verify after the last js/ edit (map/D-log/queue edits only after).
**Named:** Adornment/Aggravate/Conflict/lev-fly-blocked/Wwalking/Swimming/Breathless/Passes_walls/Slowdig/combat-inc/defense/half-damage/Unchanging/Polymorph/Upolyd-foreign-shape/lays_eggs/Free_action/Fixed_abil + `one_characteristic` Fixed_abil/stuck-ring/cursed-item hide arms + non-final (`doattributes`) Upolyd-hide stay deferred (map startup.md:24); overlay wallet hidden_gold + overlay background Upolyd/handed/XP/hit-dice arms stay deferred (final path only — no corpus session measures ^X-while-poly'd or ^X-with-container-gold); no new stub in any live arm.
**Next:** do not re-pop `enlightenment` (0 blocked). Monk-92000/Wizard-92120 now rest at `chwepon`@68/49 (row 9 content is the Blind `from_what` suffix — C «innately blind» vs JS «innately blind from your creature form» — leave the recorded owner to the queue refill; do not invent a FAIL peel). The pre-existing Open `insight.c` one_characteristic row (Priest-92180) is unaffected.

## 2026-09-08 — D-2103 `objnam.c` wishymatch queue row stale: step-70 `--More--` no longer reproduces at HEAD (session to m_move@113); no js/

**C locus:** none ported. Recorded owner `wishymatch(objnam.c:3280)` is the literal-heuristic misattribution D-2013 already diagnosed (the `"dwarven "` special-case literal at :3280; `wishymatch` prints nothing and plays no role in farlook output). True writer of the step-70 text is `pager.c` `do_screen_description` '@'-as-you arm (`:1346–1353`, shipped D-2013); the residual `--More--` was the didlook found-count path (found 1→2 fires `:1595–1624`), later covered by D-2092's checkfile u_at arm (inferred credit, not bisected).
**JS:** no js/ files changed (0 insertions; 600/10 caps untouched). No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — the corpus session reaches the arm.
**Change:** none — the tree is already past the recorded divergence with zero edits this iteration (`git status --short` clean before and after diagnosis).
**Verify:** `node scripts/hidden-proxy.mjs verify wishymatch` → baseline HEAD (scoreboard at c8d25c72): 1 session blocked (92040@70); fresh re-run: `scen-tour-Valkyrie-92040: moved → m_move at step 113 (was 70)`; `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS`. Direct replay confirms: `FAIL: scen-tour-Valkyrie-92040.session.json (RNG 30346/37526, Screen 120/148)` — first divergence strictly past step 70. Preflight `node scripts/verify.mjs --no-cohort` was green before any change (syntax/rule2/green 2/2/strict ×2 PASS); final state equals preflight state (no code edits after), so no D-1831 gap.
**Named:** `wishymatch` special-case arms (dwarvish/dwarven, elven/elvish/elfin, helmet→helm, gloves→gauntlets, detect↔detection, ability/abilities, aluminum/aluminium) stay map-named (wish-subset row `turns.md:1054`, via D-2013); no corpus session is blocked on them — porting now would be a vacuous verify (D-1831 false-PASS territory) plus wish-path regression risk for zero recorded gain.
**Next:** do not re-queue `wishymatch` for 92040 (0 blocked). 92040 now rests at m_move@113 — `m_move` is a Parked symptom owner (do not re-pop), so no new Open row; queue stays at 9 Open (≥8, no refill).

## 2026-09-08 — D-2102 `monmove.c` dochugw threat check read the door-feedback visibility stub, stopping the search before the bat's bite (queue row `end.c` done_in_by)

**C locus:** `monmove.c` dochugw threat check + `display.h` `_canseemon` (`:117-120`: `cansee||see_with_infrared` + `mon_visible`) via `canspotmon`. The biting bat starts at (64,10), d2=10 from the searching hero: `cansee` false but `see_with_infrared` true (orc infravision vs infravisible bat) with `couldsee` true, so C `already_saw_mon` is true and the `!already_saw` disjunct is false — no early stop. The bat's approach move returns `rd=0` without attacking (melee has no post-move attack arm), then its next move bites via `mattacku`→`hitmu` (`hitmsg` bite, then `hitmu`'s `stop_occupation`): bite-then-stop.
**JS:** 1 file (monmove.js +8/-4 incl. comments), under the 600/10 caps. Open row stays one item, alone.
**Change:** `js/monmove.js` dochugw only — both visibility reads now use the pre-existing `./display.js` `canspotmon` import (`display_canspotmon`; call-time use, no new edge, no TDZ); doc comment updated to cite the stub-vs-macro distinction and this D. No other stub callers touched.
**Verify:** `node scripts/verify.mjs --fn done_in_by` → PASS syntax (1 changed js file: js/monmove.js) · PASS rule2 · PASS hidden `0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Barbarian-92024 step 47 → step 97; Ranger-92177 step 81 → `were_change` step 108, both strictly later) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit (map/docs/queue edits only after).
**Named:** none new. `onscary` stays stubbed-false as map-named (`turns.md:2623`); the remaining local-stub readers (flee flinch, web spin, door open/smash) stay as-is under the D-1548-adjacent clone debt (`data.md:574-578`).
**Next:** do not re-pop `done_in_by` for these two sessions (0 blocked). Barbarian-92024 now diverges at step 97 (verify worker labels it `js-throw`; `show` reads a map/menu row-11 `R`-glyph diff with no error — re-measure next iter; a confirmed `js-throw` is Must-fix per §10.14). Ranger-92177 sits at `were_change`@108 for the normal queue.
