# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-18 — Audit ef8abf5a..1363fc19 (reviews 1408–1416: 8 ACCEPT, 1 ACCEPT-WITH-DEBT) + cadence 44/44, proxy 495/540

All 9 JS-touching SHAs since review 1407 audited against pinned C, one file per SHA written as each finished. Every per-SHA `hidden-proxy verify --base HASH~1 --reach-all` re-run matches the D-log (0 blocked/vacuous as stated; reach 12–325/325, 0 regressed — destroy_items 55/55 and dog_move 325/325 are RNG-tagged reach, the rest smoke). No Must-fix prepended (0 pending): the three prior Must-fix rows shipped as D-2449/2450/2451, all ACCEPT. Sole debt: 1412 domove_core `if (!displaceu)` middle-skip (C `hack.c:2802–2858`) — map-named in D-2453, rare trigger, 0 reach. Nits logged in-review (1408–1412 comment cites). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `71+0.49/turn` (R² 0.74); held-out 11/44 unchanged (5,637/11,265, RNG 26.6 %, screens 50.0 %); corpus 495/540 (91.7 %) excl. 13 env-only, RNG 99.29 %, screens 99.0 %. Open — coverage holds 9 rows (≥8, no refill).

## 2026-09-18 — D-2457 `timeout.c` slip_or_trip whole-body port (coverage PARTIAL → all arms live) + `rloc_to_core` STALE park

**C locus:** `nethack-c/upstream/src/timeout.c:1222–1341` (`slip_or_trip`, staticfn); caller `:906` nh_timeout FUMBLING arm; callees `vobj_at`/`objects_at`, `is_pool`, `doname`, `sobj_at`, `highc`, `body_part`, `touch_petrifies`, `an`/`pmname`, `instapetrify`, `is_ice`, `rn2`, `x_monnam`/`upstart`/`vtense`, `which_armor`, `dismount_steed`, `acurr` (ACURR), `NODIAG`, `confdir`, `hurtle`, `makeplural`; consts RLOC none; PLNMSG_ONE_ITEM_HERE, DISMOUNT_FELL, W_SADDLE, ARTICLE_THE, SUPPRESS_SADDLE, NEUTRAL.
**JS:** `js/timeout.js:176–307` (doc+def); imports `:34–40` (consts), `:38–47` (trap/hack/mkobj/worn/steed/dothrow), `:56–79` (monsters/hacklib/do_name/mon); `js/mon.js:554` (NODIAG export); map `docs/c-js-map/turns.md` hack.c fumbling line.
**Change:** `js/timeout.js:176–307` restart in C order — pronoun/doname/sobj_at-rock/something chain; highc capitalize + bite/bites; uarmf+CORPSE+touch_petrifies+Stone_resistance-flat instapetrify (`tripping over <an pmname NEUTRAL> corpse`); ice_only from EFumbling flat+uprops extrinsic; upstart(x_monnam SUPPRESS_SADDLE)/vtense steed-you pline with on/off ice; unseat arm in C short-circuit shape (which_armor hoisted: pure read, no RNG/state effect); rn2(10+acurr(A_DEX)) + NODIAG-gated confdir(true) + start-square hurtle guard; mounted rn2(4) four-message switch with Your()-as-"Your " prefix + DISMOUNT_FELL unless cursed. Imports: live `sobj_at`/`instapetrify`/`touch_petrifies`/`highc`/`pmname`/`confdir` join existing edges (no new edge); new edges worn.js `which_armor`, steed.js `dismount_steed`, dothrow.js `hurtle` (all IN-SCC, runtime-called only — `imports.mjs --can` clean); `js/mon.js` NODIAG gains `export` (import, not clone #2). `Your` built as "Your " pline (no clone #5); local `is_ice` clone reused, none added.
**Verify:** `node scripts/verify.mjs --fn slip_or_trip` → PASS syntax (2 files: js/timeout.js, js/mon.js) · rule2 · hidden note (0 blocked at baseline) · **reach 12/12 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS.
**Named:** none new, none remaining in this body (all six prior deferred arms now live; the doc records that).
**Next:** Open — coverage head after slip_or_trip (`potion.c` make_hallucinated PARTIAL).

## 2026-09-18 — D-2456 `mkmaze.c` mv_bubble whole-body port (coverage MISSING → same-named live)

**C locus:** `nethack-c/upstream/src/mkmaze.c:1952–2107` (`mv_bubble`, staticfn); callers `:1677` movebubbles, `:1777` restore_waterlevel, `:1924` mk_bubble; callees `rn2`/`sgn`, `pline`, `unblock_point`/`block_point`, `place_object`/`stackobj`, `mnearto`/`elemental_clog`, `m_at`/`u_on_newpos`/`newsym`/`mnexto`, `impossible`; consts CONS_OBJ/MON/HERO/TRAP, RLOC_NOMSG.
**JS:** `js/mklev.js:145` (import), `:16001–16009` (mk_bubble doc), `:16056–16073` (ini paint), `:16207–16215` (mv_bubble doc+def), `:16234–16249` (clamp arms), `:16278–16314` (deposit+default); map `docs/c-js-map/data.md:927–928`.
**Change:** `js/mklev.js` only — renamed `mv_bubble_move` → `mv_bubble` (module-local like C staticfn; bounds ride as params for C's file-scope gbxmin statics); added the four `:1981–1999` pline+clamp arms in C order (template-literal messages, awaited — every live path is async); deposit now runs whenever waterlevel over `b.cons || []` with `b.cons = null` unconditional and the default `await impossible('mv_bubble: unknown bubble contents')`; `mk_bubble` ini paint gains the C unblock/block_point arms. `pline` joins the existing display.js import (`imports.mjs --can` ALREADY, no new edge). No DIAG/FORCE/seed logic; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn mv_bubble` → PASS syntax (1 file: js/mklev.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed) → VERIFY: PASS.
**Named:** none new (pre-existing D-2171 omits stand: Punished ball carry, `vision_recalc(2)`, `earth_sense`/`see_nearby_objects` at hero deposit; `mk_bubble`'s own n-guard arms belong to a future mk_bubble row).
**Next:** Open — coverage head after mv_bubble (`teleport.c` rloc_to_core — note Parked Stale claims its body is complete split-named; brief decides).

## 2026-09-18 — D-2455 `dogmove.c` dog_move missing arms (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/dogmove.c:977–1358` (`dog_move`); `:348–360` `dog_starve`; `:362–397` `dog_hunger`; `:651–685` `find_targ`; `:736–835` `score_targ`; `:889–945` `pet_ranged_attk`; `monmove.c:1070–1104` `should_displace`; `:2277–2312` `undesirable_disp`; caller `monmove.c:1773` postmov wrap.
**JS:** `js/dogmove.js:14–60` (imports), `:63–72` (consts), `:703–752` (starve/hunger), `:821–845` (find_targ), `:873–940` (score_targ), `:974–1020` (pet_ranged_attk), `:1042–1400` (dog_move); `js/monmove.js:770–830` (displace pair); `scripts/dogmove-displace.test.mjs` (new, 4 its); map `docs/c-js-map/turns.md:3219` dogmove section.
**Change:** `js/dogmove.js` — DOG_HUNGRY/WEAK/STARVE 300/500/750 (`:10–12`) + AT_NONE 0; local `dog_starve` (leash-slack net-identical pline / starves / Hallu feel + `mondied`) + `dog_hunger` (non-eater push, weak/confuse/cansee-beg/feel + `stop_occupation`, starve past DOG_STARVE); `find_targ` perceives gate in C break shape; `score_targ` restart in C order (confuse/`rn2(3)`/`Is_qstart` gate first, angelic align, AT_NONE −1000, lichen −25, vampshifter `rn2`, stronger-foe penalty); `pet_ranged_attk` full body (hungry `rn2(5)`, youmonst `mattacku`, bhitpos/notonhead + `mcansee`/`haseyes` retaliation, MISS→NOTHING else DONE, forced `domonnoise`); `dog_move` in C order — non-pet `impossible`, hunger pre-empt, j==2/offmap + j==1 `invent_ate` flag (skips to newdogpos tail incl kludge), leashed-drag + guardian-range gates, gaze/petrify/`mon_reflects` melee guards with `best_target` RNG call, bhitpos/notonhead, ALLOW_MDISP/`mdisplacem` arm, leashed `whimper` (`!hero_Deaf`), `m_in_out_region` + `m_digweapon_check`, memory-glyph/vtense-locomotion/flyer-over cursemsg, goodpos kludge (additive dirtocoord verbatim) + `newsym`/`set_apparxy`. `js/monmove.js` — exported `should_displace` + `undesirable_disp` (C-locus module, no new imports needed) + one-word exports `m_digweapon_check`, `hero_Deaf`; one-word exports `perceives` (`mon.js`), `mon_reflects` (`mhitu.js` — C `muse.c` locus, de-facto live clone), `Is_qstart` (`quest.js`). New edges all `imports.mjs --can` SAFE/ALREADY (sounds/quest-CHECK-lazy/teleport/region; quest call sits in function body).
**Verify:** `node --test scripts/dogmove-displace.test.mjs` → 4/4 (HEAD stash: load-fail — exports absent). `node scripts/verify.mjs --fn dog_move` → PASS syntax (5 files) · rule2 · hidden note (0 blocked at baseline) · **REACH 80/80 sample + 325/325 `--reach-all` PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 · VERIFY: PASS.
**Named:** remove/place_monster keep direct mx/my set (pre-existing shape — `m_at` scans fmon, grid tolerates mx/my-only moves); `dog_goal`/`dog_invent` local clones stay (own rows; D-2149 dog_goal open); `mhis` stays inline her/his; `stuff_prevents_passage` inside displace path stays deferred (can_fog map note).
**Next:** Open — coverage is empty after this ship — refill `port-coverage.mjs --rows N` verbatim per the breadth-phase rule.

## 2026-09-18 — D-2454 `zap.c` destroy_items whole-body port (coverage THIN → bypass traversal + deferral second pass)

**C locus:** `nethack-c/upstream/src/zap.c:5965–6097` (destroy_items); callees `worn.c` bypass_objlist/nxt_unbypassed_obj, `zap.c` destroyable + maybe_destroy_item (both live in-file clones), `display.c` impossible; consts `DMG_DESTROY_SCALE=5`/`MAX_ITEMS_DESTROYED=20` (`:5957–5959`), `NOBJ_STATES`, `LEVITATION`/`FLYING` oc_oprop, `POT_WATER` + `ismnum(u.ulycn)` + `Upolyd` water arm.
**JS:** `js/zap.js` (+~50 net: restart + 2 import words); `scripts/destroy-items-defer.test.mjs` (new, node:test — worn levitation ring + fire wand, seed 1: wand `rnd(10)=5` at log[1] proves pass-0-before-pass-1, ring survives, bypass cleared, `dmg_out=5`; dmg_in=0 early-return after the single C gate draw; verified FAIL on the pre-fix body via stash).
**Change:** `js/zap.js:1711` restart in C order — limit + unconditional `rn2(DMG_DESTROY_SCALE)` gate (`:1712–1716`), live-chain `objchn()` getter (C `:5984 obj**`), `{oid,otmp,deferred}` array (`:1722–1726`), `bypass_objlist(clear)` + `nxt_unbypassed_obj` reservoir walk (`:1730–1732`), `where` multi-chain `impossible` (`:1740–1744`, awaited — async in JS), u_carry worn-LEVITATION/FLYING + `POT_WATER && ismnum(game.u.ulycn) && (Upolyd(game.u)?blessed:cursed)` deferral (`:1749–1758`), `elig_stacks` cap + `defer 0..1` sweep with `o_id`/`where`/`deferred` identity (`:1760–1778`), final bypass clear (`:1779`), `dmg_out` return. New imports extend existing edges only (`worn.js` bypass pair — `--can` ALREADY; `const.js` LEVITATION/FLYING/NOBJ_STATES — same module already imported). Gameover break kept as the JS rendering of C losehp→done noreturn.
**Verify:** `node scripts/verify.mjs --fn destroy_items` → PASS syntax (1 file: js/zap.js) · rule2 · hidden note (0 blocked at baseline) · **reach 55/55 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. Focused suite: `node --test scripts/destroy-items-defer.test.mjs` 2/2 (fails pre-fix, passes post-fix).
**Named:** none new (in-file destroyable/maybe_destroy_item clones reused, not duplicated; uhitm hero arms stay with their owners as above).
**Next:** Open — coverage head after destroy_items (`mon.c` newcham).

## 2026-09-18 — D-2453 `hack.c` domove_core missing arms (coverage MISSING → wired into live `domove`)

**C locus:** `nethack-c/upstream/src/hack.c:2712–2991` (domove_core); callees `hack.c:2342–2360` air_turbulence, `:2396–2415` slippery_ice_fumbling, `:1955–1990` domove_attackmon_at, `:2900–2927` displaceu swap, `:2775` bhitpos, `:2789–2791` attack nomul, `:2817–2820` stucksteed arm, `:2937` u_on_newpos-final, `:2939` reset_occupations; `steed.c:878–895` stucksteed (`monst.h:251` helpless macro); `dungeon.c:1548–1565` earth_sense; `cmd.c:195–200` reset_occupations; caller `hack.c:2695–2710` domove wrapper.
**JS:** `js/cmd.js` (+167), `js/hack.js` (+59: two callees), `js/steed.js` (+26: stucksteed + export word), `js/do.js` (export word); 4 files, ~240 insertions, under caps.
**Change:** `js/hack.js` — new `air_turbulence` (`:2273`, Is_airlevel+rn2(4)+Levitation/Flying gate, rn2(3) tumble/You_cant/thin-air + DEX exercise) and `slippery_ice_fumbling` (`:2300`, snow-boots objdescr / resists_cold / Flying / floater-clinger-whirly exemptions, Cold_resistance rn2(2|3) → HFumbling FROMOUTSIDE+1, off-ice clear), both wired at the else-branch head in C order (`js/cmd.js:3332–3333`, bare return like C). `js/steed.js` — new `stucksteed` (`:452`, helpless→"won't move!", checkfeeding meating→"still eating"); `helpless_steed` exported (`:1029`, C helpless macro, reused by the displaceu gate). `js/do.js` — `reset_occupations` exported (`:2470`, cmd→do edge already static).
**Verify:** `node scripts/verify.mjs --fn domove_core` → PASS syntax (4 files) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach; `--reach-all` identical) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared files changed) → VERIFY: PASS. Durable collateral per skill: repo has no unit harness — corpus sessions are the maintained suite (untouched); no new framework added.
**Named:** displaceu middle-skip (C skips ironbars/test_move/swim when displaceu; JS runs the middle then swaps — converges next turn); travel block (handled upstream in `continue_run`, `findtravelpath_travel/guess` + travel1=0, same order vs the carrying check); final CLIPPING (wintty cliparound singleton, Deferred list); pre-existing arrangements untouched (test_move inline vs live D-2439, fight_empty pre-branch, u_rooted air/water omits, mimic "Wait!" in do_attack); mtele_trap RLOC_MSG (rloc_to_core stale park, code-named at `js/teleport.js:1347`, future teleport.c companion).
**Next:** Open — coverage head after domove_core (`zap.c` destroy_items).
