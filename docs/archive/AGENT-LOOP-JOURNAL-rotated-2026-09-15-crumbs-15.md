# Rotated from AGENT-LOOP-JOURNAL.md (6 crumbs; live kept 10)

## 2026-09-15 — D-2370 `quest.c` artitouch quest-artifact touch arms + addinv_core1 wiring

**C locus:** `nethack-c/upstream/src/quest.c` `artitouch` `:125–136` (`!Qstat(touched_artifact)` → `observe_object(obj)` → flag TRUE → `qt_pager("gotit")` → `exercise(A_WIS, TRUE)`; `Qstat(x) = svq.quest_status.x` per `quest.c:12`; flag is a 1-bit `Bitfield(touched_artifact)` per `include/quest.h:26`) + caller `nethack-c/upstream/src/invent.c` `addinv_core1` `:984–992` (`is_quest_artifact` → `uhave.questart = 1` + `artitouch(obj)`, then `set_artifact_intrinsic(obj, 1, W_ART)`).
**JS:** 2 js files (+34/−8 per `git diff --numstat`: `js/quest.js` +19/−2, `js/u_init.js` +15/−6; plus the 1-line `CURRENT.md` Next-cluster copy + `turns.md:1270` map touch), under the 600/10 caps. Below the usual 40-insertion density floor because the C function is 11 lines and every callee was already live.
**Change:** 2 js files, no new modules (`imports.mjs --can` SAFE on `is_quest_artifact`; `quest.js → invent.js` ALREADY; new `u_init → quest` edge reads both names only at runtime inside `addinv_core1` — no top-level TDZ read): new exported async `artitouch(obj)` in `js/quest.js` next to `is_quest_artifact` (C home; `game.quest_status.touched_artifact` for `Qstat`; C order incl. flag-before-pager; `observe_object` joins the existing `./invent.js` import; `qt_pager`/`exercise`/`A_WIS` ride pre-existing imports); `addinv_core1` goes async and gains the C `:984–990` sub-arm (`uhave.questart = 1`, `await artitouch(obj)`) before `set_artifact_intrinsic`, with the sole caller (`addinv`, same file, already async) awaiting it. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn artitouch` → `PASS syntax 2 changed js file(s): js/quest.js js/u_init.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify artitouch: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Hand probe (arms no corpus/public session reaches; out of tree at `/tmp/artitouch-probe.mjs`, kept for re-run): module graph loads with the new cycle (no TDZ throw), set-flag path no-ops keeping the flag, `is_quest_artifact` gates on `urole.questarti` (7→true, 3→false, zero-role→false), `addinv` still async — 5/5 ok.
**Named:** already-have `impossible("already have quest artifact?")` arm stays named (async pline, same convention as the four sibling uhave arms); save/restore of `quest_status.touched_artifact` rides the existing `quest_status` object (no new field plumbing in this unit).
**Next:** do not re-pop `artitouch` for the touch arms. Falsifier: a rescore or fresh `verify artitouch` showing a session blocked with `artitouch` as owner (not mere `addinv` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2369 `spell.c` dovspell VIEW swap/sort + sort cluster

**C locus:** `nethack-c/upstream/src/spell.c` `dovspell` `:2021–2053` (VIEW loop; `SPELLMENU_SORT` → `spellsortmenu`→`sortspells`; else `qbuf` second call with `splnum` as splaction → swap `spl_book[splnum]`↔`spl_book[othnum]`; free `orderindx`; `sortmode = SORTBY_LETTER`; `ECMD_OK`) + `dospellmenu` `:2072–2168` (`:2119` orderindx display mapping; `:2134–2143` VIEW `+` SORT entry / PICK_NONE single-spell; `:2150–2158` preselect-decline `*spell_no == splaction` → FALSE; `:2159–2163` explicit-de-select self-swap) + `spellsortmenu` `:1978–2017` (9 choices, `z` + blank separator for RETAINORDER, preselected `spl_sortmode`, `n > 1` second-pick) + `sortspells` `:1926–1972` (CURRENT early-out; `n < 2` early-out; lazy identity alloc except LETTER/RETAIN; RETAIN permutes the book + resets index/mode; qsort first `n` of the index) + `spell_cmp` `:1867–1921` (LETTER/ALPHA/LVL_LO/LVL_HI/SKL_AL/SKL_LO/SKL_HI/CURRENT arms + `strcmpi(OBJ_NAME)` tie-break) + sort enum/choices `:1842–1865`.
**JS:** 1 js file (+202/−14 per `git diff --numstat`: `js/spell.js` only; plus the 1-line `CURRENT.md` Next-cluster copy + `turns.md` map touch), under the 600/10 caps.
**Change:** `js/spell.js` only, no new modules, no new module edges (all names — `ATR_INVERSE`, `paint_corner_nhw_menu`, `dismiss_nhw_menu`, `flush_screen`, `nhgetch`, `objectNameStrs`, `game.objects` oc_level/oc_skill, `ECMD_OK` — ride pre-existing imports/edges, used only at runtime inside function bodies — no top-level TDZ read): new `SORTBY_*`/`SORTRETAINORDER`/`NUM_SPELL_SORTBY` consts + `spl_sortchoices` (C strings verbatim) + 3-way `strcmpi3` (existing vault.js/write.js clones are boolean-eq only) + `spell_cmp` (C arm order; CURRENT/default → 0 since JS `sort` is stable per Constitution §4.5, matching the contest stable-sort patch; tie-break via `objectNameStrs[otyp]`) + `sortspells` (C order/conjuncts incl. two-pass RETAIN permute) + `spellsortmenu` (C letters `a`–`h` + `z`, blank separator, prompt `View known spells list sorted`; Return/Space accepts the preselected = current mode → TRUE; Esc → FALSE) next to `dospellmenu` (C home); `dospellmenu` displays `spl_orderindx ? orderindx[i] : i` (wizard `spellknow(i)` already used the loop index, per C `:2127`), VIEW first-call letter/`+` → ok:true, swap-call preselected letter → decline (ok:false → break), other letter → ok:true; `dovspell` runs the C loop (SORT dispatch; `Reordering spells; swap 'x' with` qbuf via `spellet`; struct swap; decline/ESC break) with the C tail (conditional free → null, mode 0, `ECMD_OK` — callers `cmd.js`/`getline.js` ignore the value, as before). No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn dovspell` → `PASS syntax 1 changed js file(s): js/spell.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify dovspell: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Hand probe (arms no corpus/public session reaches; out of tree at `/tmp/probe_dovspell.mjs`, kept for re-run): drove the real `dovspell` via `pushKeys` — swap `a`↔`c` exchanges book entries + ECMD_OK + tail reset; same-letter decline keeps order; level-hi-lo sort then swap maps letters to internal indices; alpha sort + retain permutes book to name order; CURRENT keeps the index (retain still alpha); empty-book path clean — 11/11 ok.
**Named:** tty explicit-de-select self-swap no-op (C `:2159–2163` `splaction >= 0` arm; single-key menu has no deselect gesture — doc-named on `dovspell`); sort-menu preselected visual marker (behavior ported: Return/Space accepts current mode); `n > 1` second-pick arms in both menus (need multi-select; unreachable single-key — doc-named); `SPELLMENU_DUMP` path (no `dump_spells` symbol in `js/`, untouched); row remainder `spelleffects` non-standard cast otyps/oc_charged stays `turns.md:2961` and re-queues as its own Open row (different C function, over the single-cluster cap).
**Next:** do not re-pop `dovspell` for swap/sort. Falsifier: a rescore or fresh `verify dovspell` showing a session blocked with `dovspell` as owner (not mere `dospellmenu` CAST caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2368 `sp_lev.c` is_ok_location Is_waterlevel + is_pool/is_lava arms

**C locus:** `nethack-c/upstream/src/sp_lev.c` `is_ok_location` `:1280–1308` (`:1284–1285` `Is_waterlevel` accept-any; `:1291` ANY_LOC; `:1293` SOLID `IS_OBSTRUCTED`; `:1295–1300` DRY|SPACELOC `SPACE_POS` with `bould && SOLID`; `:1303` WET `is_pool(x, y)`; `:1305` HOT `is_lava(x, y)`) + `nethack-c/upstream/src/dbridge.c` `is_pool` `:46–59` (POOL/MOAT/WATER + `is_moat`, Juiblex MOAT note) / `is_lava` `:62–74` (LAVAPOOL/LAVAWALL + DRAWBRIDGE_UP DB_LAVA) + `include/dungeon.h:115` `Is_waterlevel` (`Lcheck(&water_level)`).
**JS:** 1 js file (+14/−9 per `git diff --stat`: `js/mklev.js` only), under the 600/10 caps. Below the usual 40-insertion density floor because the C function is 28 lines and SOLID/DRY/ANY_LOC already shipped in D-0552.
**Change:** `js/mklev.js` only, no new modules, no new module edges (`imports.mjs --can` ALREADY on both: `is_pool`/`is_lava` join the existing `./hack.js` import, `Is_waterlevel` rides the pre-existing `./const.js` edge — all used only at runtime inside the function body, no top-level TDZ read): body in C order/conjuncts — `Is_waterlevel(game.u?.uz)` accept-any head with the C `/* accept any spot */` comment, `/* TODO: diggable/passwall? */` restored, boulder as `!= null` boolean with the C `(bould && (humidity & SOLID))` conjunct, WET via `is_pool(x, y)`, HOT via `is_lava(x, y)`; both callees are sync and RNG-free (pure typ/drawbridge checks) so no draw moves on levels without drawbridges. Doc retires the `Is_waterlevel` omission and cites `:1280–1308`.
**Verify:** `node scripts/verify.mjs --fn is_ok_location` → `PASS syntax 1 changed js file(s): js/mklev.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify is_ok_location: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared file changed) · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session places a special-level feature on a drawbridge-under cell or runs special placement while on the water level with a visible delta (same disclosure as D-2365/D-2366); both newly-wired callees are already live in `js/hack.js` with identical C-order use on adjacent water/lava paths.
**Named:** `is_ok_location_func` global stays emulated via the `ok_fn` params at `get_location_random`/`get_location_in_room` (sole C setter is `l_create_stairway` `good_stair_loc`; pre-existing, untouched); the `isok` OOB guard is retained (C callers guarantee in-bounds; `is_pool`/`is_lava` carry their own `isok` gates — never fires on C-reachable paths).
**Next:** do not re-pop `is_ok_location` for the humidity arms. Falsifier: a rescore or fresh `verify is_ok_location` showing a session blocked with `is_ok_location` as owner (not mere `get_location`/`maze1xy` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2367 `pray.c` bestow_artifact body + caller wiring

**C locus:** `nethack-c/upstream/src/pray.c` `bestow_artifact` `:1781–1836` (raw `u.ulevel > 2 && u.uluck >= 0` gate; wizard `y_n("Gift an artifact?")` vs `!rn2(6 + 2*ugifts*nartifacts)`; `mk_artifact(NULL, a_align(ux,uy), max_giftvalue, TRUE)` NULL-able; `artifact_origin(GIFT|KNOW_ARTI)`, spe<0 clamp, uncurse, oerodeproof; Hallu/Blind buf + `" named <bare>"` when seen; `at_your_feet(upstart)` + `dropy` + godvoice; `ugifts++`, `ublesscnt = rnz(300+50*nartifacts)`, `exercise(WIS)`, livelog gift-by-god, `unrestrict_weapon_skill(weapon_type)`; seen observe + makeknown + discover) + caller `offer_corpse` `:2088–2092` (`if (bestow_artifact(value)) return;` before the luck block).
**JS:** 1 js file (+77/−~6 per `git diff --stat` slice: `js/pray.js` only; plus the 1-line `CURRENT.md` Next-cluster copy + 1-line `turns.md:400` map touch), under the 600/10 caps.
**Change:** `js/pray.js` only, no new modules, no new module edges (all four artifact names + `y_n` ride pre-existing edges — `imports.mjs --can` ALREADY on both; used only at runtime inside the function body — no top-level TDZ read): new async `bestow_artifact(max_giftvalue)` in C order/conjuncts next to `offer_corpse` (C home; raw `uluck` not `Luck()` per C; wizard flag matches `dopray` idiom; `mk_artifact(null, a_align, value, true)` sync NULL-able; `eos(buf)` append as `+=`; local `godvoice`/`at_your_feet` reused, pre-existing `upstart`/`weapon_type` imports reused — no clones); `offer_corpse` else arm awaits it and returns on TRUE before the luck block, in C position. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn bestow_artifact` → `PASS syntax 1 changed js file(s): js/pray.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify bestow_artifact: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a satisfied-god high-value sacrifice gift arm with a visible gift delta (RNG-gated gift path; same disclosure as D-2366/D-2363).
**Named:** none new. `mk_artifact` fallback-pool semantics stay D-2337's (untouched); `y_n` wizard-gift prompt uses the async `getline.js` idiom (C is sync); `Soundeffect`/pitch stays file-convention deferred.
**Next:** do not re-pop `bestow_artifact` for this body. Falsifier: a rescore or fresh `verify bestow_artifact` showing a session blocked with `bestow_artifact` as owner (not mere `offer_corpse`/`dosacrifice` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2366 `zap.c` resist tell-shield `shieldeff_mon` arm

**C locus:** `nethack-c/upstream/src/zap.c` `resist` `:6100–6158` (`:6141` `resisted = rn2(100 + alev - dlev) < mr`; `:6143–6144` `if (tell) shieldeff_mon(mtmp)`; `:6145` `damage = (damage + 1) / 2`) + `nethack-c/upstream/src/mon.c` `shieldeff_mon` `:6056–6064` (`shieldeff(mx, my)` then `cansee` → `pline_mon "%s resists!"`).
**JS:** 1 js file (+7/−3 per `git diff --stat`: `js/zap.js` only; plus the 1-line `CURRENT.md` Next-cluster copy), under the 600/10 caps. Below the usual 40-insertion density floor because the C residual is genuinely two lines and the other 56 shipped in D-2116.
**Change:** `js/zap.js` only, no new modules, no new module edges (callee is a hoisted same-file `async function` declaration — no import, no TDZ read): the `if (resisted)` arm awaits `shieldeff_mon(mtmp)` when `tell`, before the halve, in C order/conjuncts; doc comment cites `:6100–6158`/`:6143–6144`. `shieldeff_mon` is display-only and the halve is unchanged, so zero RNG draws move. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn resist` → `PASS syntax 1 changed js file(s): js/zap.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify resist: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a tell+resisted `resist` arm with a visible shield delta (display-only arm; same disclosure as D-2363/D-2364).
**Named:** none new. Sleep `Resists_Elem` wielded/worn/artifact scan + `defended(mon, AD_SLEE)` stay deferred (D-2116 named, untouched); `music.js`/`pray.js` fixed-oclass `resist` clones stay exact-for-caller (untouched); R-1082 `sleep_monst` mimic-reveal debt stands (turns.md:563, untouched).
**Next:** do not re-pop `resist` for the tell-shield arm. Falsifier: a rescore or fresh `verify resist` showing a session blocked with `resist` as owner (not mere caller presence); re-queue under the writer it names. No seed/step/coordinate gates.

## 2026-09-15 — D-2365 `monmove.c` mon_would_consume_item body (corpse_eater corpse + pet dogfood)

**C locus:** `nethack-c/upstream/src/monmove.c` `mon_would_consume_item` `:1036–1050` (`CORPSE && !touch_petrifies(&mons[corpsenm]) && corpse_eater(mtmp->data)`; `mtame && has_edog(mtmp)` (guardian-angel exclusion) `&& (ftyp = dogfood(mtmp, otmp)) < MANFOOD && (ftyp < ACCFOOD || EDOG(mtmp)->hungrytime <= svm.moves)`) + `include/mondata.h:200` `touch_petrifies` (cockatrice/chickatrice) + `:243` `corpse_eater` (purple worm/baby, ghoul, piranha) + `include/mextra.h:162–165` `DOGFOOD=0/CADAVER=1/ACCFOOD=2/MANFOOD=3`. Sole C caller `m_search_items` `:1425` (`((take && carry>0) || consume) && touch` short-circuit, already live D-0183/D-0223).
**JS:** 2 js files (+~30/−8 per `git diff --stat`: `js/monmove.js` body + imports + 2 doc lines; `js/mon.js` 1 doc line), under the 600/10 caps. Below the usual 40-insertion density floor because the 14-line C function is that small and its caller wiring shipped in D-0183/D-0223.
**Change:** `js/monmove.js` only, no new modules, no new module edges (`imports.mjs --can` ALREADY on both: `dogfood` joins the existing `dogmove.js` import, `EDOG`/`has_edog`/`ACCFOOD`/`MANFOOD` join the existing `const.js` import — runtime-only reads inside the function body, no top-level TDZ read; `dogfood` is a hoisted export and the monmove↔dogmove cycle pre-exists): body in C order/conjuncts — `otmp.otyp === CORPSE && !touch_petrifies(mons(otmp.corpsenm)) && corpse_eater(mtmp.data)`, then `mtmp.mtame && has_edog(mtmp)` with `dogfood(mtmp, otmp) < MANFOOD && (ftyp < ACCFOOD || hungrytime <= moves)` using the repo dual-edog idiom (`EDOG(mtmp)?.hungrytime || mtmp.edog?.hungrytime || 0`, cf. `js/dog.js:614`) and `game.moves || 1` (cf. `js/dog.js:615`).
**Verify:** `node scripts/verify.mjs --fn mon_would_consume_item` → `PASS syntax 2 changed js file(s): js/mon.js js/monmove.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify mon_would_consume_item: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared file changed) · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a corpse-eater-corpse or hungry-pet-food loot arm (same disclosure as D-2363/D-2364); the edited function is file-local, reachable only through level-state `m_search_items` dispatch, so a standalone probe would need full game state (disproportionate setup).
**Named:** none new. `can_touch_safely` silver/artifact/petrify arms stay stub-true (own row, untouched); `m_search_items` onscary inhishop/inhistemple + lminion/unique arms stay deferred in live `mon.js` arms (untouched).
**Next:** do not re-pop `mon_would_consume_item` for the corpse/pet-food arms. Falsifier: a rescore or fresh `verify mon_would_consume_item` showing a session blocked with `mon_would_consume_item` as owner (not mere `m_search_items` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
