# C→JS map — Known constitutional debt

Parent index: `docs/C-JS-MAP.md`. Do not load other map sections
unless this subsystem is in scope.

## Known constitutional debt

These are not protected merely because the two green sessions exercise them:

| JS area | Debt to replace from C |
|---|---|
| `js/jsmain.js` capture hook | Detects Count/`--More--` text and repairs cursor at capture time; cursor semantics belong in input/display code |
| `js/display.js` message paths | Contains scenario-derived cursor/layout special cases rather than complete window/message policy |
| `js/eat.js` | Cookie + reqtime-1 `touchfood`/`fprefx` (D-0155); **CORPSE `eatcorpse`/`eatfood`** (D-0193); **`floorfood` feeding + poison_strdmg** (D-0221); **floor `useupf`/`delobj`** (D-0222); **`eye_of_newt_buzz` via `cpostfx`** (D-0492); **`start_tin`/`opentin`/`consume_tin` + multi-turn rations** (D-0935); **`is_edible` poly diets + `doeat_nonfood`/`eatspecial`/`foodword` + floor gold** (D-0936); **metallivore floorfood beartrap/IRONBARS + `doeat` `hands_obj` + `still_chewing`/`dissolve_bars`** (D-0937); **`b_trapped` + `make_stunned` (tin/door/chew/kick)** (D-0938); **`cprefx` + `maybe_cannibal`/`fix_petrification` + `flesh_petrifies`/`slimeproof`/`same_race`/`were_beastie` + `make_stoned`/`make_slimed`/`delayed_killer`** (D-0939); **`costly_tin` + `use_tin_opener` + shop `costly_alteration`/`bill_dummy_object`** (D-0940); **`still_chewing` shop `add_damage` + `watch_dig`/`angry_guards`** (D-0941); **`pay_for_damage`/`getcad`/`hot_pursuit` + chew/`zap_dig`/`wakeup`** (D-0942); **`cpostfx` specials + AD_STUN/AD_HALU hallu** (D-0943); **`mconveys` extract + `corpse_intrinsic`/`givit`/`should_givit`/`temp_givit`/`intrinsic_possible`** (D-0944); **`cpostfx` were* `set_ulycn` + mimic gold `eatmdone` + `attrcurse`** (D-0945); **`eatspecial` PAPER/potion/`eataccessory`/leash/trident/flint/`uwepgone`/`unpunish`** (D-0946); **floorfood pool/lava reach + `vault_gd_watching(GD_EATGOLD)` + gd_move witness** (D-0953); **`Ring_gone`/`float_up`/`rescham`/`choke`(strangle)/`set_mimic_blocking`/`perceives`** (D-0956); **`choke` `killer_xname` D-1344**; **`lesshungry`/`bite` choke+fullwarn D-1356**; **`eatspecial` MAIL_STRUCTURES `SCR_MAIL` + `uwepgone` `artifact_light` `end_burn`** (D-1204); **`Ring_on`/`learnring`/`adjust_attrib`/`float_down` via do_wear** (D-0966); **`toggle_stealth` RIN/ELVEN cloak+boots on/off + `EStealth`** (D-0970); **`dosinkfall` + `spoteffects` sink+Lev + `ELevitation` mirror + `stop_donning`** (D-0976); **floor remote `stolen_value` via `costly_alteration`** (D-0983); **wolfsbane `you_unwere(TRUE)` + ParanoidWerechange/Hit** (D-1001); **cprefx rider `revive_corpse` after lifesave** (D-1081; tin skip / `zero_victual`; **MINVENT/CONTAINED + Adjmonnam D-1212**; **BURIED !is_zomb FALLTHROUGH impossible D-1220**; Soundeffect se_scratching still named on `do.js` `revive_corpse`); unpaid `splitbill`; other fpostfx |
| `js/zap.js` | **`zap_over_floor` closed-door/SDOOR/IRONBARS shopdamage + `dobuzz` `pay_for_damage`** (D-0948); **`explode` floor shop bill** (D-0949); **`bhitm`/`cancel_item`/`cancel_monst` strike/cancel/poly/tele/undead + `bhito` cancel/striking/tele + zapyourself arms** (D-0952); **`cancel_monst` self-cancel walks hero `invent` Array (not `nobj` on the Array) + clay hallu/`s_suffix`/`AD_SPEL`** (D-1017); **`unturn_dead`/`revive` invent+floor + `hero_breaks`/`breaks` + worn ABON cancel** (D-0955); **`revive` container/buried + `cant_revive` + `zombie_can_dig` + OBJ_BURIED extract** (D-0964); **`melt_ice`/`start_melt_ice_timeout`/`melt_ice_away` + `burn_floor_objects` + `zap_over_floor` WEB/POOL→PIT/fountain/cold freeze + fireball `dobuzz`/`explode`** (D-0965); **`bury_objs`/`unearth_objs`/`obj_ice_effects`/`obj_timer_checks` + melt/freeze/`liquid_flow` wire** (D-0967); **`explode` AD_FIRE mon/hero combat + `explosionmask` Fire_resistance/`resists_fire` + `mon_explodes` AD_FIRE** (D-0968); **`explode` AD_COLD/ELEC mon/hero combat + Cold/Shock mask + `mon_explodes` COLD/ELEC** (D-0971); **`explode` AD_MAGM/DISN/DRST/ACID mon/hero combat + Antimagic/Disint/Poison/Acid masks + `mon_explodes` MAGM..SPC2** (D-0973); **`zapyourself` WAN_FIRE/FIRE_HORN + WAN_COLD/SPE_CONE_OF_COLD/FROST_HORN + exported `ubuzz`/`flash_str`** (D-0974); **lavawall→wall `fix_wall_spines` + `burn_floor_objects` give_feedback + fire-trap wire** (D-0975); **`ignite_items`/`catch_lit`/`begin_burn`/`burn_away_slime` + LS_OBJECT/BURN_OBJECT** (D-0978); **`release_hold` + `bhitm`/`zapyourself` WAN_OPENING + `flash_hits_mon` WAN_LIGHT** (D-0979); **`openholdingtrap`/`openfallingtrap` + Punished/`boxlock_invent` + SPE_KNOCK `mhurtle`/saddle** (D-0981); **`save_mtraits`/`get_mtraits`/`montraits` + revive omonst/ghost + KEEPTRAITS/`wary_dog`** (D-0982); **`stolen_value` revive shop bill** (D-0983); **`break_statue` STATUE_TRAP activate** (D-0997); **`dozap` self-zap `losehp` `killer_xname` D-1345**; **`dozap` cursed `backfire` D-1416**; **`zapyourself` WAN_LIGHTNING + `flashburn` D-1355**; **`zapyourself` WAN/SPE_MAGIC_MISSILE D-1364 / Antimagic uprops D-1367**; **`zapyourself` SPE_FIREBALL D-1365**; **spell.c skilled FIREBALL scatter + `spell_damage_bonus` D-1378**; **`lightdamage` WAN_LIGHT/CAMERA D-1366**; **`maybe_destroy_item` AD_ELEC D-1368 / Shock uprops D-1371**; **`zapyourself` WAN_MAKE_INVISIBLE D-1369**; **`zapyourself` WAN_SPEED_MONSTER D-1410**; **`zapyourself` WAN/SPE_SLOW_MONSTER D-1433**; **`zapyourself` WAN_LOCKING/`boxlock_invent` D-1434**; **`zapyourself` WAN_PROBING `probe_objchain`+`ustatusline` D-1435**; **`bhitm` WAN_MAKE_INVISIBLE D-1414 / conferral See_invisible D-1423**; **`bhitm` WAN_LOCKING/`closeholdingtrap` D-1425**; **`bhitm` WAN_PROBING/`probe_monster` D-1426**; cant_finish_meal |
| `js/apply.js` | **`do_break_wand` explode-type + inert** (D-0949); **`do_break_wand` dig/`fillholetyp`/`digactualhole` + create-monster + dig `pay_for_damage`** (D-0950); **`use_pick_axe`/`use_pick_axe2` via `doapply` is_pick|is_axe** (D-0951); **`do_break_wand` strike/cancel/poly/tele/undead adjacent bhit + `WAN_LIGHT` litroom** (D-0952); **`DRUM_OF_EARTHQUAKE` `do_earthquake`/`do_pit`** (D-0972 via `music.js`); **flute/harp/horn charm·sleep + FIRE/FROST `ubuzz` + BUGLE `awaken_soldiers`** (D-0974 via `music.js`); **passtune getlin + Castle `open_drawbridge`/`close_drawbridge` + Mastermind hints** (D-0977 via `music.js`/`dbridge.js`); **`catch_lit` fire ignition** (D-0978); **`release_hold` WAN_OPENING + `flash_hits_mon`** (D-0979); **`paranoid_query` ParanoidBreakwand + `see_monster_closeup` camera/makedog** (D-0999); **ParanoidPray Confirm + `see_nearby_monsters`** (D-1000); **ParanoidWerechange/Hit + `you_were`/`you_unwere`** (D-1001 via were/uhitm/timeout/eat); **`use_towel` + wet/dry_a_towel + burnarmor towel dry** (D-1009); **`use_crystal_ball` + thin object/trap/furniture detect** (D-1010); **BLINDFOLD/LENSES Blindf_on/off** (D-1013); **`getobj_apply` CMDQ_KEY after wield/`itemactions` (D-1018)**; **`use_royal_jelly` + dorub/doapply (D-1021)**; **`use_whip`/`use_grapple`/`use_pole` + Snickersnee `is_pole` (D-1022)**; **pole `glyph_at` targeting (D-1040; not live `m_at`)**; **`thitmonst` weapon/weptool/GEM hit-vs-miss (D-1041)**; **shared `getdir` + `hurtle`/`hurtle_step` (D-1038; no `teleds` yank)**; **whip `yname`/`Amonnam`/`mbodypart` not apply clones (D-1045)**; **`u_wipe_engr` + pole/grapple/jump `tmp_at` S_goodpos (D-1051)**; **`use_lamp`/`light_cocktail`/`use_trap`/`bagotricks` (D-1023)**; **`use_lamp` cursed-spill `Glib` TIMEOUT HGlib|EGlib (D-1052)**; **`light_cocktail` `struct obj **` (D-1046)**; **`consume_obj_charge` unpaid/`check_unpaid` (D-1047)**; **`flip_through_book`/`flip_coin` (D-1024)**; **`use_candle`/`use_candelabrum` (D-1025)**; **`use_grease` CAN_OF_GREASE (D-1026)**; **`use_tinning_kit` TINNING_KIT (D-1027)**; **`use_bell` BELL/BELL_OF_OPENING (D-1028 via detect `openit`/`openone` + mkroom `mkundead`)**; **`use_figurine` FIGURINE (D-1029 via dog `make_familiar`/`pick_familiar_pm`)**; **`use_unicorn_horn` UNICORN_HORN (D-1030)**; **`hornoplenty` HORN_OF_PLENTY (D-1031 via mkobj + pickup floor tip BoT/horn)**; **`fig_transform` / `attach_fig_transform_timeout` (D-1032)**; pickup invent getobj tip |
| `js/dig.js` | **`watch_dig` + `zap_dig` shop** (D-0941/D-0942); **`dig_check`/`fillholetyp`/`digactualhole`/`liquid_flow`/`fill_pit`/`maybe_dunk_boulders`** (D-0950); **`dig_typ`/`pick_can_reach`/`is_digging`/`holetime`/`dig` occupation/`dighole` thin/`fracture_rock`/`break_statue`/`use_pick_axe`** (D-0951); **`furniture_handled` fountain/sink + HOLE hero `goto_level` + mon migrate** (D-0954); **`dig_up_grave` + `dighole` IS_GRAVE** (D-0957); **`shopdig` warn/snatch** (D-0958); **`shopdig(1)` um_dist snatch polarity + do.js setnotworn** (D-1016); **`use_pick_axe` cmdq wield re-apply queues `doapply`+invlet CMDQ_KEY (D-1018)**; **`destroy_drawbridge` + find/is_wall + dig wires** (D-0959); **`open_drawbridge`/`close_drawbridge` + music passtune** (D-0977); **`mkcavearea`/`mkcavepos`/`rm_waslit` earth dig** (D-0960); **`impact_drop`/`down_gate`/`drop_to` dig HOLE wire** (D-0961); **`conjoined_pits`/`xytodir` + autodig quiet + `dighole` boulder-fill/`delfloortrap`** (D-0962); **`desecrate_altar`/`god_zaps_you`/`fry_by_god` + dig wire** (D-0963); **`bury_an_obj`/`bury_objs`/`unearth_objs`/`rot_organic` + `liquid_flow` ice/unearth** (D-0967); **`bury_objs` shop `stolen_value` merchandise owe** (D-0983); **`digactualhole` `switch_terrain` D-1269**; **maketrap PIT/HOLE `set_levltyp` D-1280**; DRAWBRIDGE_UP ice / shop add_damage / liquid_flow / dothrow hurtle / `u_on_rndspot` / objnam wish / magical-trap explode / zap_dig pitdig still named |
| `js/dokick.js` | **`kick_door` shop `in_rooms`/`add_damage`/`pay_for_damage` + town `watchman_thief_arrest`/`watchman_door_damage`** (D-0947); **`impact_drop`/`down_gate`/`drop_to`** (D-0961); **`impact_drop` shop `stolen_value`/`picked_container` + debit/robbed chase** (D-0983); **`ship_object`/`otransit_msg` + dropx/throwit/drop_throw** (D-0984); **`kick_nondoor` SDOOR/SCORR + altar/fountain/grave/IRONBARS/sink + `altar_wrath`/`disturb_grave`/`sink_backs_up`** (D-0985); **throne destroy/loot/`fall_through` + tree scatter/swarm** (D-0986); **`kick_object` + `bhit` KICKED_WEAPON** (D-0988); **Is_box `container_impact_dmg`/lock/lid/`chest_trap` + `ghitm` + `make_angry_shk`** (D-0989); **`dropz`/`throwit` `container_impact_dmg`** (D-1249); **`hits_bars`/`hit_bars` via bhit/throwit/launch_obj** (D-0990); **`costly_gold`/`donate_gold` + addtobill coin/container gold + kick out/refund** (D-0991); **`sellobj`/`check_shop_obj` + drop/throw bill** (D-0994); **`sellobj` BSS `'\0'` not `'a'` + C `robbed -= (offer<0)` then clear** (D-1019); **`instapetrify` + barefoot kick petrify + `bhit` DISP_FLASH** (D-0995); **Blind `feel_location`/`feel_newsym`/`wake_nearto` + `STATUE_TRAP` `activate_statue_trap`** (D-0997); **`mon_yells` D-1248**; giant doorbuster poly |
| `js/invent.js` | Corner invent + disco `*`/encounter + `obj_typename` (D-0040); ^X autopickup/limits/`weapon_descr` (D-0041); **Samurai disco + invent `observe_object`** (D-0079); **^X gender omit + Attributes MC warded** (D-0097); **^X moon/friday13 + 23-row continuous page** (D-0158); **`consume_obj_charge` unpaid/`check_unpaid` (D-1047)**; fullscreen invent and full magic enlightenment deferred |
| `js/u_init.js` / `js/roles.js` | Rogue/Tourist/Wizard/Priest/Knight/Samurai/Healer/Valkyrie/Ranger/Monk/Archeologist/Barbarian/**Caveman** + human/orc(/elf/dwarf/gnome) race kits (D-0027/D-0042…/52); pantheon gods + C roles[] order; **race `hpadv`/`enadv` table** (D-0036); **roles `xlev` copied to `game.urole`** (D-0061); helm/gloves/boots/shield wear + Barbarian/Knight/Samurai/Valkyrie/Ranger/Monk `knows_class`/`HJumping`/`Japanese_item_name`/`is_ammo`/`is_launcher`/`is_spear`; Caveman FLINT/ROCK quiver; **`oc_skill`/`a_ac`/`oc_level` extracted**; dagger `knows_class` can migrate; **`skill_init` via `u_init_skills_discoveries`** (D-0122; Skill_T/R + all roles); **roles `spel*` + `initialspell`/`age_spells`/`dovspell` VIEW** (D-0129); **`skill_based_spellbook_id` + spelspec unrestrict** (D-0132); **`docast` SPE_HEALING** (D-0135); omit swap/sort/other cast otyps/`oc_charged` |
| `js/allmain.js` | Welcome/HP/align no longer Tourist-literal; **`regen_hp` once-per-turn** (D-0035); **`see_nearby_monsters`** after seer_turn (D-1000); **mtimedone `you_unwere`** via timeout (D-1001); **Teleportation / Polymorph / ulycn once-per-turn** (D-1002); **warnreveal / overexert_hp / Upolyd eel** (D-1003); **`moveloop` EOT `movebubbles`/`fumaroles` (D-1168)**; **once-per-input `m_everyturn_effect(&youmonst)` fog at `u.ux` (D-1175)**; tutorial, hunger, sound, and attribute checks still have deferred branches |
| `js/mon.js` / `js/monmove.js` / `js/dogmove.js` | Monster flags, movement predicates, targeting, carrying, and combat have named stubs/defaults; **`you_were`/`you_unwere` + ParanoidHit peaceful confirm** (D-1001 via were/uhitm); **`mon_poly` youmonst + monster-defender + mhitu/mhitm AD_POLY** (D-1004/D-1006; uhitm `damageum` troll_baned D-1233; hmonas AT_HUGS D-1250; `special_dmgval` `hates_silver` D-1254; hmonas AT_EXPL explum D-1251; **`gelcube_digests` D-1257**; **`passes_bars` ALLOW_BARS D-1258**; remaining `mhitm_ad_*` / **hmonas AT_ENGL gulpum D-1264**; **fight_empty explum D-1265**; **hmonas altwep D-1266**; **hmonas skipdrin / pit kick D-1298**; **eat_brains D-1306**; **helmet / m_slips_free D-1307**; **mhitu AD_DRIN D-1329**; **mhitm AD_DRIN D-1330**; **mhitu AD_WRAP D-1331**; **uhitm AD_WRAP D-1348**; **mhitm AD_WRAP brush D-1406**; **mhitm gazemm D-1338**; **mhitm explmm D-1339**; **mhitm AT_HUGS D-1340**; **mhitm hitmm shade_miss D-1341**; **mhitm hitmm silver sear D-1351**; **mdamagem AD_STON leftover D-1352**; **mdamagem AD_CONF leftover D-1385**; **mdamagem AD_STUN leftover D-1396**; **mdamagem AD_FIRE leftover D-1405**; **mhitm AD_WRAP brush D-1406**;  **hero `test_move` IRONBARS D-1270**; Underwater / rock Passes_walls still named) |
| `js/pray.js` / `js/potion.js` | **TROUBLE_HIT** (D-0920) + **TROUBLE_LYCANTHROPE → you_unwere** (D-1004); **majors Stoned…Region** (D-1011); **collapsing…cursed_blindfold + all minors** (D-1012: `stuck_ring`/`unchanger`/`make_deaf`/`buried_ball_to_freedom`/`confers_luck`); **`peffect_water` + potionbreathe POT_WATER lycan** (D-1004); **`make_glib` `set_itimeout(&Glib)` + `Glib()` HGlib|EGlib (D-1052)**; **`peffect_levitation` POT/SPE_LEVITATION** (D-1419); **`peffect_restore_ability` POT/SPE_RESTORE_ABILITY** (D-1420); **`peffect_invisibility` POT/SPE_INVISIBILITY** (D-1421); **`peffect_polymorph`** (D-1428); **`peffect_gain_energy`** (D-1429); **`peffect_acid`** (D-1430); **`peffect_gain_level`** (D-1431); **`peffect_blindness`** (D-1432); **`peffect_sleeping`** (D-1437); remaining peffect_* (gain ability/hallucination) / pleased gifts deferred |
| `js/mklev.js` / `js/mkobj.js` / `js/makemon.js` | Many terrain/object/monster-type branches remain scenario-limited |

When editing one of these areas, replace the narrow behavior with its C
semantic unit and run a non-target cohort. Do not preserve a trace-derived path
solely to keep a Tourist seed green.

