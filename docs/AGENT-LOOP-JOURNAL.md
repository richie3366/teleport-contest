# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
## 2026-09-09 — Audit 32f374ab..5cd7c741 (reviews 1176–1178: 3 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 3 JS-touching SHAs since 32f374ab (D-2210..D-2212), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: ustatusline PASS (Archeologist-92004, stronger than the D-log's 141→145 PROGRESS — later commits closed the tail); use_misc PASS (Archeologist-92004, same session); dosearch0 PASS (Knight-92130) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (ustatusline branch-for-branch insight.c:3402–3489 + macro reads youprop.h:81/93/96/138/376–377, monst.h:71, you.h:554; C-locus sticks engrave.js:346 over divergent monmove.js:1629; digests mondata.h:71–72; passive_obj AD_FIRE uhitm.c:6156–6162 gate-for-gate incl. steam-vortex carve-out, weapon≡C-resolved obj per preamble :6135–6143; dosearch0 Norep :2022–2024 + SDOOR :2050 + SCORR :2056/:2060, SDOOR correctly keeps recalc). Notes, not C-wrongs: Strangled/flat-OR broadening is the house dual-store idiom (mirrors display.js); AD_CORR passes raw obj vs AD_FIRE's weapon (equivalent — callers never pass Null); passive_obj carried→update_inventory tail still unported (pre-existing, shared with ACCEPTED AD_CORR arm). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.28/turn` (R² 0.79). No Must-fix prepended (3 ACCEPT). Next cluster unchanged (Open head use_grapple); Score refreshed from this run. Also backfilled D-2212 `5cd7c741` Addressed hash.
## 2026-09-09 — Audit 2b917b52..38bd5d4f (reviews 1170–1175: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 2b917b52 (D-2204..D-2209), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: stairs_description PROGRESS (Healer-92109 208→do_statusline2@241); getpos PASS (Healer-91124); trapeffect_sqky_board PASS (Barbarian-92064); prinv PASS (Caveman-92059); enlightenment PROGRESS (Monk-92213 108→chwepon@109); steal_it PROGRESS (Priest-92097 152→do_statusline2@180) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (wasinside arm mon.c:3632–3640: outside the accessible gate, link-zeroing N/A under GC; getpos exitgetpos :1151–1157 clear gate; sqky both arms trap.c:1402–1476 byte-exact formats + wake_nearby file-local clone; mergable body :4378–4499 holds zero owornmask reads + merged fixup obj-worn-only :877–913; innategend :474–479 + title :396–401, form arm keeps flags.female; steal_it call-for-call :2173–2278 incl. SITM→SEDU fallthrough :4798–4799). Notes, not C-wrongs: `sym.mjs` misses `export {}` lists (could_seduce false "NOT EXPORTED", resolved by direct read of mhitm.js:685–694); D-2206/D-2209 messages call extended imports "new edges" but `--can` reports both ALREADY (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `64+0.40/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (Open head chwepon); Score refreshed from this run.
## 2026-09-09 — Audit ab784680..2b917b52 (reviews 1164–1169: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since ab784680 (D-2198..D-2203), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: legs_in_no_shape PROGRESS (Healer-91123 PASS); float_vs_flight vacuous-0-confirmed as labeled; make_hallucinated PROGRESS (Caveman-92052 PASS); do_statusline2 PROGRESS (2 PASS 92035/92091 + 2 moved, 9 unchanged, 0 worse — byte-identical to the D-log); next_ident PROGRESS (4 PASS + 92187 residual, 0 worse); make_stunned PROGRESS (Archeologist-92226 PASS). C spot-checks held throughout (fpostfx royal-jelly arm-for-arm incl. single-draw rnd(20); mount_steed gate + plural quirk; set_utrap/reset_utrap routing via C trap.c:1044–1057; HALLUC/STUNNED re-arm shapes; Stunned≡HStun youprop.h:81; statusline2 rank table botl.c:781–813 + cond_cmp + windows.c fieldorder read from pinned C; arise arm bones.c:457–478 + tail :505–511 + res_to_mr prop.h:25–27). Notes, not C-wrongs: TIMEOUT-masked wounded gate (house canonical, shared trap.js:2977), debug||wizard idiom, dual botl mirrors, end.js-local give_u_to_m clone (no mondata export exists). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `83+0.59/turn` (R² 0.76). No Must-fix prepended (6 ACCEPT). Next cluster repointed to Open head stairs_description (make_stunned shipped D-2203, PASS at HEAD); Score refreshed from this run.
## 2026-09-09 — Audit 74944545..ab784680 (reviews 1156–1163: 8 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 8 JS-touching SHAs since d22f6c29 (D-2190..D-2197), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: burnarmor/thitm/m_harmless_trap/dosounds vacuous-0-confirmed as labeled; unstuck PROGRESS (Knight-92002 52→one_characteristic@81); accessory_or_armor_on PASS (Tourist-92144); look_here PROGRESS (Wizard-92223 121→130, residual null-owner label as disclosed); minliquid_core PASS (stale row, PASS pre/post as disclosed). C spot-checks held throughout (burnarmor literal, savelife tail, Helmet_on 7 arms + set_bknown/adj_abon, expels tail + um_dist, defended/resists_magm + sqky/rocktrap arms, thitm full body + AD_RBRE=242, dosounds 7 arms draw-for-draw, mdrop_obj/steed/flush). Minor message framings noted (ALREADY-edges called "new", D-2196/D-2194) — substance safe, no C-wrongs. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `92+0.57/turn` (R² 0.77). No Must-fix prepended; Next cluster unchanged (fountain gush/engulfed map-driven).

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-09 — D-2214 `eat.c` givit ACID/STONE arms dropped C's `"%s."` period, «petrified» lost its full stop (queue row givit, 1 session PROGRESS 70→108)

**C locus:** `eat.c:1078–1095` ACID_RES/STONE_RES arms — `You_feel("%s.", Hallucination ? "secure from flashbacks" : "less concerned about being harmed by acid")` and `You_feel("%s.", Hallucination ? "unusually limber" : "less concerned about becoming petrified")`. The period comes from the `"%s."` format, not the argument.
**JS:** 1 file (`eat.js`, 2 lines, 4 chars), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/eat.js` only — appended `.` to all four ACID/STONE literals to match C `"%s."`. Density note: +2/−2 lines — the 97-line C function was already ~95 % ported, so completing the remaining C-cited punctuation is the whole unit (C-is-that-small clause).
**Verify:** `node scripts/verify.mjs --fn givit` → PASS syntax (1 changed js file: js/eat.js) · PASS rule2 · PASS hidden PROGRESS (scen-wish-Rogue-91119 step 70 → nh_timeout@108, strictly later step, different owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** unchanged (debugpline only; `should_givit`/`temp_givit`/`incr_itimeout` wiring already live).
**Next:** do not re-pop `givit` for Rogue-91119 (0 blocked there). It now rests at `nh_timeout`@108 — leave to the rescore queue; do not invent a FAIL peel.
## 2026-09-09 — D-2213 `apply.c` use_grapple/use_whip `surface_apply` stub said «furniture», C `surface()` says «stairs» (queue row use_grapple, 1 session PROGRESS 146→158)

**C locus:** `apply.c:3857` `You("are yanked toward the %s!", surface(cc.x, cc.y))`; `dungeon.c:1749–1788` `surface()` — `On_stairs` arm returns "stairs" ahead of IS_WALL/IS_DOOR/IS_ROOM; the word "furniture" appears nowhere in C `surface()`. C `grep surface(` on `apply.c` shows all 7 whip/grapple message sites (`:3051/:3183/:3788/:3795/:3819/:3855/:3857`) call `surface()`.
**JS:** 1 file (`apply.js`, ~+11/−15 incl. import/doc/7 reworded calls/stub deletion), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/apply.js` only — `import { surface } from './sit.js'` (canonical `dungeon.c:1750` port, D-2008; `imports.mjs --can apply.js sit.js surface` → IN-SCC hoisted-function SAFE, and `sit.js` holds no static `apply.js` edge); all 7 sites call `surface()`; stub deleted; `use_grapple` doc cites the removal.
**Verify:** `node scripts/verify.mjs --fn use_grapple` → PASS syntax (1 changed js file: js/apply.js) · PASS rule2 · PASS hidden PROGRESS (scen-wish-Priest-92041 step 146 → weldmsg@158, strictly later step, different owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** unchanged (use_grapple untrap non-adjacent FIXME, S_goodpos tmp_at D-1051; `surface()` swallow maw/husk arm stays named in `sit.js`; dig/dokick/engrave per-context `surface` clones untouched).
**Next:** do not re-pop `use_grapple` for Priest-92041 (0 blocked there). It now rests at `weldmsg`@158 — leave to the rescore queue; do not invent a FAIL peel.
## 2026-09-09 — D-2212 `detect.c` dosearch0 uswallow arm used pline, counted search re-queued «The exit?» behind --More-- (queue row dosearch0, 1 session PASS)

**C locus:** `detect.c:2022–2024` uswallow arm — `Norep("What are you looking for?  The exit?")` (PLINE_NOREPEAT), not `pline`. Same function, same commit: `:2050` SDOOR `set_msg_xy`, `:2056` SCORR `unblock_point` (vision), `:2060` SCORR `set_msg_xy`.
**JS:** 1 file (`detect.js` +11/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/detect.js` only — uswallow arm now `await Norep('What are you looking for? The exit?')`; SDOOR/SCORR arms now `set_msg_xy(x, y)` before the find pline; SCORR arm now `unblock_point(x, y)`. All three names were already imported (no new edge, no cycle risk).
**Verify:** `node scripts/verify.mjs --fn dosearch0` → PASS syntax (1 changed js file: js/detect.js) · PASS rule2 · PASS hidden PROGRESS (scen-wish-Knight-92130 → PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** C `nomul(0)` stays the local `nomul_clear()` subset (JS clears multi + `_repeat_search`/context run but does not mirror C's botl/uinvulnerable/usleep/end_running/cmdq_clear — map `turns.md` dosearch0 row); `mfind0` set_msg_xy / via_warning flush still deferred; Hallucination/cls trap-wait still deferred.
**Next:** do not re-pop `dosearch0` for Knight-92130 (0 blocked, session fully PASS). Do not invent a FAIL peel.
## 2026-09-09 — D-2211 `uhitm.c` passive_obj AD_FIRE burn was deferred, «Your bullwhip smoulders!» lost (queue row use_misc, 1 session PASS)

**C locus:** queue owner `use_misc(muse.c:2552)` is a literal tie-break, not the printer: line 2552 is the `{` under `case MUSE_BULLWHIP` (monster-disarms-hero, `The_whip = "The bullwhip"` at :2553) — no `use_misc` pline produces either message, and step-145 C dice contain zero muse draws (`gethungry/exercise/hitum:780/dmgval/knockback×2/known_hitum/passive:5888/passive_obj:6158/passive:6019`). True writer is `uhitm.c` `passive()` → `passive_obj()` → `trap.c` `erode_obj()` (the other cMsgOwner, `trap.c:178` action table): recorded `rn2(6)=0 @ passive_obj(uhitm.c:6158)` fires the AD_FIRE burn (`!rn2(6) && !mcan`, non-steam) → `erode_obj(obj, NULL, ERODE_BURN, EF_NONE)` → «Your bullwhip smoulders!» (`trap.c:283–288` damaged arm, prints for uvictim with no EF_VERBOSE gate). Knockback ruled out (`rn2(6)=5 @ mhitm_knockback:5269` → `rn2(chance)` nonzero → FALSE, no movement); `monnear` identical both sides (`mon.c:2476–2482` vs `js/mon.js:871–876`). More-timing proven from the recording, not theory: C step 146 (` ` dismiss) topline is «You are suddenly very hot! You are burning to a crisp!» with HP 114→74 (`mdamageu` 32 + `gulpmu d(1,10)=8`), and C step 148 lashes again with `rn2(6)=3` (no erode) → «You lash the fire vortex. You are suddenly very hot!» — byte-identical to the JS step-145 text. So C prints hot too, one frame later: lash+smoulders fills the 80-col topline first, hot falls behind the More; JS, missing smoulders, accumulates lash+hot in the same frame.
**JS:** 1 file (`uhitm.js` +8/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/uhitm.js` only — AD_FIRE arm now `await erode_obj(weapon, null, ERODE_BURN, EF_NONE)` via dynamic `./trap.js` import (same-function AD_CORR convention, no new static edge; `weapon` is the C-resolved `obj`); `ERODE_BURN`/`EF_NONE` added on the existing `./const.js` edge. Cites `uhitm.c:6156–6162`.
**Verify:** `node scripts/verify.mjs --fn use_misc` → PASS syntax (1 changed js file: js/uhitm.js) · PASS rule2 · PASS hidden PROGRESS (scen-wish-Archeologist-92004 → PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `passive_obj` AD_ACID/RUST/ENCH erode arms stay deferred (map `turns.md:3433`); `passive_obj` trailing `if (carried(obj)) update_inventory()` not mirrored (shared across arms, no evidence at this step — inventory screen never shown at 145/146).
**Next:** do not re-pop `use_misc` for Archeologist-92004 (0 blocked, session fully PASS — it previously rested here from D-2210). Do not invent a FAIL peel.
## 2026-09-09 — D-2210 `insight.c` ustatusline: info suffix was hardcoded empty, «, fast» lost (queue row ustatusline, 1 session PROGRESS 141→145)

**C locus:** `insight.c:3402–3489` `ustatusline` — full info chain in order (Sick dying-from with VOMITABLE/NONVOMITABLE « and» join; Stoned; Slimed; Strangled; Vomiting «nauseated»; Confusion; Blind + ucreamed goop `ucreamed < BlindedTimeout || Blindfolded || !haseyes`; Stunned; Wounded_legs `EWounded_legs & BOTH_SIDES` plural, side never named; Glib `fingers_or_gloves(TRUE)`; utrap; Fast Very_fast split; uundetected/U_AP_TYPE; Invis; ustuck uswallow-digests/sticks + `a_monnam`; visible-region cloud poison-gas/vapor) + Upolyd mh/mhmax/mlevel arms in the final pline.
**JS:** 1 file (`insight.js` +~100/−10), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/insight.js` only — full info chain in C order with house predicate idioms (`u.Sick/Stoned/Slimed` + uprops-intrinsic mirrors per `display.js:5700–5703`; Strangled H/E flats + intrinsic/extrinsic; Vomiting flat-or-intrinsic per `wizcmds.js:273`; `(u.HConfusion|0) || u.Confusion`, `(u.HStun|0) || u.Stunned` per display.js; `Blind()`; BlindedTimeout inline `(u.HBlinded|0) & TIMEOUT`, Blindfolded as `u.EBlinded` per `potion.js:2051`; Wounded_legs flats per `invent.js:5076–5078`; `Glib()`; `Fast()`/`Very_fast()`; masked `youmonst.m_ap_type & M_AP_TYPMASK` per `monst.h:71`; `Invis()`; `digests(u.ustuck.data)` per `polyself.js:1704`; C-locus `sticks` from `engrave.js`; `Upolyd(u)` + `mons(umonnum).mlevel`). Imports extended on existing module edges + 8 new same-SCC edges (`attrib.js` Fast/Very_fast, `invent.js` Blind, `timeout.js` Invis, `potion.js` Glib, `do_wear.js` fingers_or_gloves, `polyself.js` body_part, `mhitu.js` digests, `engrave.js` sticks; `imports.mjs --can` → all IN-SCC hoisted-function SAFE) and const.js names (SICK/STONED/SLIMED/STRANGLED/VOMITING, SICK_VOMITABLE/NONVOMITABLE, BOTH_SIDES, M_AP_NOTHING/M_AP_TYPMASK, LEG, TIMEOUT, Upolyd) + `reg_damg`/`haseyes`/`a_monnam` on already-imported modules. C `Strcat/Sprintf/eos/BUFSZ` bounds N/A in JS (unbounded strings).
**Verify:** `node scripts/verify.mjs --fn ustatusline` → PASS syntax (1 changed js file: js/insight.js) · PASS rule2 · PASS hidden PROGRESS (scen-wish-Archeologist-92004 step 141 → use_misc@145, strictly later step, different owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** none new — full 87-line C function now live (ailment deferral retired).
**Next:** do not re-pop `ustatusline` for Archeologist-92004 (0 blocked). It now rests at `use_misc`@145 — leave to the rescore queue; do not invent a FAIL peel.
## 2026-09-09 — D-2209 `uhitm.c` steal_it: poly hero SEDU/SSEX/SITM never stole (queue row steal_it, 1 session PROGRESS 152→180)

**C locus:** `uhitm.c:2173–2278` staticfn `steal_it` (worn-suit-to-end, charm/seduce pline, gold shuffle, hold_another_object "You steal: " loop, theft_petrifies, W_WEP/W_ARMG fixups) + `:2147–2165` staticfn `theft_petrifies` + `:4629–4632` `mhitm_ad_sedu` uhitm arm (`steal_it(mdef, mattk); damage = 0`), reached for AD_SEDU/AD_SSEX (via `:4754–4758`)/AD_SITM via `mhitm_adtyping :4797–4799`.
**JS:** 1 file (`uhitm.js` +~150), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/uhitm.js` only — file-local `theft_petrifies` (uarmg/corpsenm/Stone_resistance gates; C-disabled `#if 0` arm stays omitted) + `steal_it` in C order (minvent pointer-surgery via prev/cur; charm/seduce pline via inline `You …` prefix to avoid a second file-local `You` clone beside `zap.js:847`; C panic on a second worn suit is `impossible()`); `damageum_adtyping` gains the AD_SEDU/AD_SSEX/AD_SITM arm (`steal_it` + leftover `d()` zeroed). Imports extended on existing edges (`findgold`, `mhe`/`upstart`/`doname`/`corpse_xname`, `obj_extract_self`, `mpickobj`, `hold_another_object`, `find_artifact`, `possibly_unwield`, `W_WEP`/`CXN_ARTICLE`/`COIN_CLASS`/`OBJ_INVENT`/`is_neuter`) plus two new same-SCC edges (`steal.js`, `trap.js` for `mselftouch`/`instapetrify`; `imports.mjs --can` IN-SCC, deferred use only) plus `impossible` on the existing display.js edge (also fixes the latent `:2856` throw).
**Verify:** `node scripts/verify.mjs --fn steal_it` → PASS syntax (1 changed js file: js/uhitm.js) · PASS rule2 · PASS hidden PROGRESS (scen-poly-Priest-92097 step 152 → do_statusline2@180, strictly later step, different owner; direct replay RNG 5241/5241, screens 320/321) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Mid-iter self-caught `OBJ_INVENT is not defined` (step-0 REGRESSION on first verify) fixed by adding the const.js import; final verify ran after the last js/ edit.
**Named:** `mhitm_ad_sedu` mhitm (mon→mon) arm (`:4694–4747`, mon-side steal + nymph rloc-vanish); `mhitm_ad_ssex` mhitm arm (`:4773–4778`); mhitu arms stay live in `js/mhitu.js`.
**Next:** do not re-pop `steal_it` for Priest-92097 (0 blocked). It now rests at `do_statusline2`@180 (parked paint-timing owner) — leave to the rescore queue; do not invent a FAIL peel.
## 2026-09-09 — D-2208 `insight.c` background_enlightenment innategend: role/rank/gender read saved u.mfemale when poly'd (queue row enlightenment, 1 session PROGRESS 108→109)

**C locus:** `insight.c:474–479` (`innategend = (Upolyd ? u.mfemale : flags.female) ? 1 : 0`; role_titl/rank_of from innategend) + `:512–515` (gender tmpbuf from `genders[innategend]`) + title `:392–397` (same saved-gender expression); the Upolyd form arm `:496–506` deliberately keeps current `flags.female` («here we always use current gender, not saved role gender»).
**JS:** 1 file (`invent.js` +20/−9), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/invent.js` only — `innateFemale = Upolyd(u) ? !!u.mfemale : female` (the house Ugender idiom, cf. `display.js:314` / `polyself.js:794` / `do_name.js:609`; `u.mfemale` maintained by `polyself.js:654,1099,1103`); role/rank/gender/innategend read it in both builders; form-line adj, cham/form pmname and were-form pmname keep `flags.female` per C. No new import or edge (`Upolyd` already file-live), no TDZ (defined before first use in both builders).
**Verify:** `node scripts/verify.mjs --fn enlightenment` → PASS syntax (1 changed js file: js/invent.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-poly-Monk-92213 step 108 → chwepon@109, strictly later step, different owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS, final verify after the last js/ edit (map/D-log/queue edits only after).
**Named:** `background_enlightenment` difgend/difalgn «started out» + «actually <align>» temp-align arms (`:574–589`; need `ualignbase`, untracked in JS) — fire on neither side here (no started-out line either side); overlay background Upolyd/handed/XP/hit-dice arms stay deferred (D-2104).
**Next:** do not re-pop `enlightenment` for Monk-92213 (0 blocked). It now rests at `wield.c` chwepon@109 — leave to the queue refill; do not invent a FAIL peel.
