# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
## 2026-09-15 — Audit 3f602af2..a1b96e3f (reviews 1346–1347) + cadence 44/44

1346 D-2380 → **ACCEPT** (1339 Must-fix retired as ordered; `were.js:57`
verified vs `youprop.h:355–360`). 1347 D-2381 float_up → **ACCEPT**
(buried_ball/Lev_at_will/surface/WEB/Flying arm-exact; fixes latent
`TT_WEB`-vs-`WEB` + `Flying_fu`-sans-steed-flyer C-wrongs). Per-SHA verify
0/0, rulecheck/banned-grep clean. Cadence 44/44 (Scr 11,405, RNG 792,838,
`65+0.43/turn`). No Must-fix; 9 Open in band, no refill.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — D-2383 `region.c` region_danger/region_safety use the REG_HERO_INSIDE bit + safety blind tail

**C locus:** `nethack-c/upstream/src/region.c:1341–1363` (`region_danger`: `!hero_inside` skip, gas_cloud-only, nonliving/Breathless + Poison_resistance skips) and `:1368–1405` (`region_safety`: same membership; `n>1||(n==1&&!r)` → `safe_teleds` + danger recheck → `set_itimeout(&HMagical_breathing, d(4,4)+4)`; single expiring → `remove_region`; gone message; `BlindedTimeout==1 → make_blinded(0L,TRUE)`); `include/region.h:17` (hero_inside bit); `include/youprop.h:270` (HMagical_breathing ≡ uprops slot), `:93` (BlindedTimeout ≡ HBlinded&TIMEOUT). Flag writers verified live in JS: `in_out_region` (C `:480–527`), `update_player_regions` (C `:582–592`), `add_region` (C `:334–337`) — all C-cited.
**JS:** `js/region.js` only, 1 js file, no new modules, no new static edges (one const name joins an existing edge; two runtime dynamic imports).
**Change:** both loops use the file-local `hero_inside(reg)` (no new edge, no TDZ — same-file function declaration); safety dual-writes flat + `uprops[MAGICAL_BREATHING].intrinsic` TIMEOUT bits (HBlinded precedent in `do.js`; `MAGICAL_BREATHING` joins the existing `const.js` edge); blind tail reads `(u.HBlinded & TIMEOUT) === 1` (`BlindedTimeout` is local to `do.js`) and dynamic-imports `make_blinded` from `./do.js` — same pattern as the existing `safe_teleds` dynamic imports, no new static edge. Docs + file header updated; `pline('The …')` kept (`pline_The` has no JS export; text-identical).
**Verify:** `node scripts/verify.mjs --fn region_safety` → VERIFY: PASS — `PASS syntax 1 changed js file(s)` · `PASS rule2` · `note hidden verify region_safety: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing`. Same tail for `--fn region_danger`. /tmp hand probe (deleted before handoff): flag-set/geo-miss → danger true, flag-clear/geo-hit → false, single-expiring safety removes region, empty-regions no-throw, HBlinded&TIMEOUT==1 cured to 0 — 5/5.
**Named:** none new — geometric residual, blind tail and breathing dual-write all retired. `pline_The` wrapper shape (pre-existing, text-identical); `safe_teleds` dynamic-import shape (pre-existing).
**Next:** pop the next Open row (`trap.c` back_on_ground full surface matrix).
## 2026-09-15 — D-2382 `hack.c` findtravelpath travelmap visited set + VALID mark-only + Underwater rock gate

**C locus:** `nethack-c/upstream/src/hack.c:1266–1523` (`findtravelpath`: entry alloc `:1268–1269`, TRAVEL/VALID success `:1400–1418`, GUESS no-guess mark `:1484–1487`) + `test_move` obstructed Underwater `:1016–1023`; `cmd.c` `reset_cmd_vars` `:3616–3618` and `hack.c` `end_running` `:4151–4153` (both free travelmap; end_running unconditionally).
**JS:** `js/cmd.js` (+~110/−40: helpers, BFS arm, tri-state returns, two wrapper mappings, VALID param, underwater gate, 3 import lines) · `js/hack.js` (+4/−1) · `js/mklev.js` (3 `export` words). 3 js files, no new modules, no new static edges (`--can` ALREADY on all four names).
**Change:** `js/cmd.js` — `travelmap_ensure()` (C `:1268–1269`; per-game heap on `game.travelmap`, never saved, like C) + `TRAVEL_NOPATH/STEP/STEP_UNSURE` tri-state (C returns boolean, but `You` is async-only in JS, so the sync BFS reports UNSURE and the async TRAVEL wrappers `continue_run`/`dotravel_target` print `You('stop, unsure which way to go.')` in C order); BFS success arm in exact C shape (visited read off current cell, dx/dy set either way, TRAVEL-only stop on dest-or-visited with travelcc-clear on fresh dest, `selection_setpoint(u.ux,u.uy)` + return on all three success shapes); `findtravelpath_bfs` takes `mode` (`TRAVP_TRAVEL`/`TRAVP_VALID` join the existing `const.js` edge; default TRAVEL); `is_valid_travelpt` passes `TRAVP_VALID` and maps to boolean (getpos stays sync); GUESS no-guess success marks travelmap (C `:1484–1487`); `reset_cmd_vars` clears `game.travelmap` (C `cmd.c:3616`); obstructed arm gains the all-modes `uinwater → return false` gate (review-941 debt). `js/mklev.js` — `export` on `selection_new`/`selection_getpoint`/`selection_setpoint` (file-local clones; `--can` ALREADY, no new edge, still hoisted). `js/hack.js` — `end_running` clears `game.travelmap` unconditionally + doc updated (no import needed; game-object write).
**Verify:** `node scripts/verify.mjs --fn findtravelpath` → VERIFY: PASS — `PASS syntax 3 changed js file(s)` · `PASS rule2` · `note hidden verify findtravelpath: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` · `PASS full 44/44 passing (auto: shared file changed)`. No maintained unit harness exists for game logic (only `scripts/*.test.mjs` for loop scripts) — the sessions + corpus gates are the suite, so no new test file per the no-new-framework rule; the revisit arm fires only on travel loops (unreached by any suite, same as C) and its callees (`You`, `nomul`, `selection_*`) are live elsewhere.
**Named:** full `test_move` TEST_MOVE/DO_MOVE modes (adjacent + GUESS no-guess arms keep the `blocksMove`/`boulder_at` stand-ins); `may_passwall` rock gate, `worm_cross`, `block_entry` (diagonal-out hardcoded `false`), WAN_DIGGING-unknown two-in-row arm, Known_wwalking/Known_lwalking/WATERWALL/LAVAWALL (all pre-existing, unchanged); `found:` dx/dy-zero shape at the travel/guess callers (pre-existing); menu-getpos `#retravel` family (own row).
**Next:** pop the next Open row (`region.c` region_danger/region_safety geometric residual).
## 2026-09-15 — D-2381 `trap.c` float_up retires D-0956 residuals + dead-WEB C-wrong

**C locus:** `nethack-c/upstream/src/trap.c:3937–4006` (`float_up`); `dig.c:1884–1932` (`buried_ball`); `dungeon.c:1749–1759` (`surface` maw/husk first branch); `include/youprop.h:242–245` (`Lev_at_will`), `:253–255` (`Flying`); `include/trap.h:77` (WEB=18) vs `include/you.h:347–353` (TT_WEB=3).
**JS:** `js/trap.js` `float_up` (+doc) · `js/dig.js` export `buried_ball` · `js/mhitu.js` export `enfolds`. 3 files, +51/−19, no new modules or static edges.
**Change:** BURIEDBALL arm calls `buried_ball(cc)` (newly exported from `js/dig.js:514`, verified arm-for-arm vs C: `!u.utrap || TT_BURIEDBALL` gate, exact-spot return, dist2≤8 nearest + cc mutation) and reads `IS_ROOM` at the ball cell; steed arm ports `Lev_at_will` inline over the `HLevitation`/`ELevitation` flats (`I_SPECIAL`/`W_ARTI`/`TIMEOUT` value-matched to `prop.h`; bits maintained live at `js/artifact.js:1587`, `js/hack.js:3193–3194`, `js/potion.js:1267/1294`) with `Monnam` magically-floats-up vs dismount; uswallow-animal arm inlines the only reachable `surface()` branch (maw/husk/nonesuch via `mhitu.js` `digests` + newly exported `enfolds` — shared `sit.js` `surface` still names that arm); WEB arm kept literal-dead per C so TT_WEB falls through; tail uses canonical `mhitu.js` `Flying`. All cross-module names via runtime `await import` (no new static edges; `W_ARTI` joins the existing `const.js` edge).
**Verify:** `node scripts/verify.mjs --fn float_up` → PASS syntax (3 changed files) · rule2 clean · hidden vacuous note (0 blocked at HEAD; row cited 0 blocks) · green 2/2 · strict ×2 · cohort 7/7. Runtime shape probe (plain-node dynamic imports): `float_up`/`buried_ball`/`Flying`/`digests`/`enfolds` all `function`. No corpus PASS claimed.
**Named:** none new in `float_up` (D-0956 float_up residuals fully retired). Pre-existing surroundings untouched: file-local `Flying_fu` stays for `float_down` and below (own row when a falsifier fires); `flags.botl` extra set left as-shipped.
**Next:** Open `hack.c` still_chewing body (next queue head after this row archives).
## 2026-09-15 — D-2380 `lock.c` stumble_on_door_mimic PfSC gate uses canonical `were.js` export (review 1339 Must-fix)

**C locus:** `nethack-c/upstream/include/youprop.h:355–360` (`Protection_from_shape_changers` ≡ `u.uprops[PROT_FROM_SHAPE_CHANGERS].intrinsic || .extrinsic`); `worn.c:96–123` (`setworn` sets `uprops[p].extrinsic |= mask` generically); `objects.h:826` (ring `RIN_PROTECTION_FROM_SHAPE_CHAN` has `oc_oprop = PROT_FROM_SHAPE_CHANGERS`); `lock.c:758–769` (`stumble_on_door_mimic` — `m_at` + `is_door_mappear` + `!Protection_from_shape_changers`).
**JS:** 1 js file (+6/−11 per `git diff --numstat`: `js/lock.js`; plus the `turns.md` map touch + queue mark + review stamp), under the 600/10 caps. Below the 40-insertion floor because the `stumble_on_door_mimic` body + wires shipped in D-2373 — this iteration retires only the review-named residual (same disclosure as D-2379).
**Change:** deleted the `js/lock.js:618` local, added `import { Protection_from_shape_changers } from './were.js'` (the `:57` export reads flats + `uprops[].intrinsic/extrinsic`, same shape as the `display.js`/`mon.js` clones). `node scripts/imports.mjs --can lock.js were.js Protection_from_shape_changers` → SAFE (hoisted function declaration; in-SCC runtime-only use). Doc comment on `stumble_on_door_mimic` notes the canonical gate.
**Verify:** pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Post-change `node scripts/verify.mjs --fn stumble_on_door_mimic` → PASS syntax (1 file) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped by the tool: no shared file changed) → VERIFY: PASS. Hand probe (arm no corpus session reaches; out of tree at `/tmp/probe-pfsc.mjs`, deleted before finish): `were.js` export with ring-extrinsic-only uprops → true PASS; old flats-only clone shape → false CONFIRMS-DIVERGENCE; no-protection baseline → false PASS.
**Named:** none new. (`pick_lock :571` keeps its own inline predicate + `maybe_absorb_item` tail per D-2373 — different C arm, untouched.)
**Next:** do not re-pop `stumble_on_door_mimic`/PfSC for this arm. Falsifier: a rescore or fresh `verify stumble_on_door_mimic` showing a session blocked with it as owner (not mere caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — Audit ae5c5da2..ff517412 (reviews 1339–1345) + cadence 44/44

1339 D-2373 stumble_on_door_mimic → **QUALITY-RISK** (flats-only PfSC clone drops uprops extrinsic; worn ring stumbles where C is FALSE; fix = import were.js:57, --can SAFE; Must-fix + Next cluster set). 1340 D-2374 globby_bill_fixup → **ACCEPT-WITH-DEBT** (4 scenarios + pricing exact; scen 2–3 msg order fire-and-forget, named). 1341 D-2375 minimal_monnam → **ACCEPT**. 1342 D-2376 shrink_glob → **ACCEPT**. 1343 D-2377 peffect_water → **ACCEPT**. 1344 D-2378 set_artifact_intrinsic → **ACCEPT**. 1345 D-2379 ceiling → **ACCEPT**.
Verify: per-SHA `hidden-proxy verify --base ~1` → 0/0 every time (rows cited 0 blocks); rulecheck clean; banned-grep hits prose-only. Cadence 44/44 (Scr 11,405, RNG 792,838). Next: 1339 Must-fix first, alone (10 Open, in band).
## 2026-09-15 — D-2379 `dungeon.c` ceiling() full arms via `trap.js` export (peffect_levitation residual)

**C locus:** `nethack-c/upstream/src/dungeon.c` `ceiling` (vault/temple/SHOPBASE `*in_rooms` → "vault's/temple's/shop's ceiling"; `Is_waterlevel` → "water above"; `IS_AIR` → "sky"; `Is_firelevel` → "flames above"; `In_quest` → "expanse above"; `Underwater` → "water's surface"; `(IS_ROOM && !Is_earthlevel) || IS_WALL || IS_DOOR || SDOOR` → "ceiling"; else "rock cavern"); `hack.c` `in_rooms` `goodtype` macro (SHOPBASE also matches `rtype > SHOPBASE` — the JS clone already matches); `youprop.h:279` (`Underwater` ≡ `u.uinwater`).
**JS:** 2 js files (+27/−26 per `git diff --numstat`: `js/trap.js` +19/−6, `js/potion.js` +8/−20; plus the `turns.md` map touch + queue mark), under the 600/10 caps. Below the 40-insertion floor because C `ceiling()` is 33 lines and the `peffect_levitation` body shipped in D-1419 — this iteration retires only the named residual (same disclosure as D-2377).
**Change:** `js/trap.js` `ceiling` ported arm-for-arm in C order (wording cross-checked against the live full `js/zap.js:2482` `ceiling_updown` that serves zap_updown probing). JS adaptations, all file-idiomatic: `uz = game.u?.uz` passed explicitly to `Is_waterlevel`/`Is_firelevel`/`In_quest`/`Is_earthlevel` (matches `allmain.js:424` convention); Underwater reads `(game.u?.uinwater | 0)` per the C macro — never `u.Underwater`, which nothing in `js/` writes (see Named omissions); `game.level?.at?.` keeps the potion/zap null-safe read. `Is_firelevel`/`VAULT`/`TEMPLE` join the existing `./const.js` import; `in_rooms`/`In_quest`/`Is_earthlevel`/`Is_waterlevel`/`SHOPBASE`/IS_* were already imported — no new module edge.
**Verify:** pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Post-change `node scripts/verify.mjs --fn ceiling` → PASS syntax (2 files) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped by the tool: no shared file changed) → VERIFY: PASS. Hand probe (arms no corpus session reaches; out of tree at `/tmp/probe_ceiling.mjs`, deleted before finish): vault/temple/shop/shop-variant/water/sky/fire/quest/uinwater-surface/room/earthlevel-room/wall/door/sdoor/corridor + vault-beats-water + vault-beats-underwater C-order checks — 17/17 ok.
**Named:** `js/dothrow.js` toss_up `ceiling_at` clone (same partial arms; own row when a falsifier fires); `js/zap.js:2482` `ceiling_updown` reads dead `game.u?.Underwater` (never written anywhere in `js/`; C says `u.uinwater` — own row); `has_ceiling` local clones (`potion.js:582`, `dothrow.js:1051` both verified bit-exact vs the `dungeon.js:902` export this iter; `mon.js:3075` not audited) — consolidation needs new module edges for zero behavior delta, deferred; FLYING timed-land / potionhit-mix / SPE_RESTORE_ABILITY / SPE_INVISIBILITY stay their own rows.
**Next:** do not re-pop `peffect_levitation`/`ceiling` for these arms. Falsifier: a rescore or fresh `verify ceiling` showing a session blocked with `ceiling` as owner (not mere caller presence — 12 call sites run on many turns); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2378 `artifact.c` set_artifact_intrinsic defn/cary resists + PROTECT + W_ART-off invoke reversal

**C locus:** `nethack-c/upstream/src/artifact.c:715–893` (`set_artifact_intrinsic`; `dtyp=(wp_mask!=W_ART)?defn:cary` `:738`, seven-way E* mask `:740–754`, W_ART-off other-carrier guard on `art->cary.adtyp==dtyp` `:756–764`, `*mask|=wp_mask` / `&=~wp_mask` `:765–768`; SPFX_PROTECT `:873–878`; `wp_mask==W_ART && !on && inv_prop<=LAST_PROP && uprops[inv_prop].extrinsic&W_ARTI → arti_invoke(otmp)` `:880–885`); `arti_invoke` property path `:2178–2229` (xor off, tired re-xor, `!on → age=moves+rnz(100)`, other-source → `nothing_special`, else CONFLICT/LEVITATION/INVIS tails); callers `invent.c:991` (`addinv_core1`) / `:1383` (`freeinv_core`, extract-first per `:1403–1409` — JS `freeinv` already splices first, same order) / `worn.c:106/130/173` (slot masks, never W_ART — the `:880` arm fires only on invent exit); `do.c:758–772` (no-floor drop `finesse_ahriman` levhack preset + post-`hitfloor` `float_down` — own row, untouched).
**JS:** 4 js files (+89/−9 per `git diff --numstat`: `js/artifact.js` +74/−2, `js/do.js` +5/−1, `js/zap.js` +4/−3, `js/invent.js` +6/−3 doc-only; plus `CURRENT.md` Next-cluster copy + `data.md` map touch + queue mark), under the 600/10 caps. Above the 40-insertion floor (C is 179 lines; three arms + vehicle).
**Change:** `js/artifact.js` — defn/cary→(prop, E* flat) seven-way map written through the existing dual-write `set_spfx_extrinsic` (C E* macros ARE `uprops[].extrinsic`; JS gates read the flats, e.g. `mcastu.js:86`), with the W_ART-off guard walking `game.invent` in C conjunct order (`obj!==otmp`, `art!==list[0]`, `cary.adtyp===dtyp`); SPFX_PROTECT between REFLECT and Sunsword (C order); new exported async `revoke_invoked_property` (same file — no new module edge, no TDZ: `arti_invoke` is a hoisted same-file declaration) with the C guard verbatim (`inv_prop`, `<=LAST_PROP`, `uprops[inv].extrinsic&W_ARTI` — only Orb 40/INVIS, Sceptre 44/CONFLICT, Heart 48/LEVITATION qualify). Vehicle (Constitution §2.6 — the tail reaches `pline`/`float_down`, so async propagates; sync `freeinv_core` with ~25 sites stays sync): `js/do.js` `dropx` awaits it right after `freeinv_drop` (covers every drop incl. swallowed, before `ship_object`/`doaltarobj` — C `dropx` order), `js/zap.js` poly awaits it between `freeinv_core(obj)` and `addinv_core1` (C `:1910–1914` order); both extend pre-existing `artifact.js` import edges.
**Verify:** pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Post-change `node scripts/verify.mjs --fn set_artifact_intrinsic` → PASS syntax (4 files) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) → VERIFY: PASS. Hand probe (arms no corpus session reaches; out of tree at `/tmp/probe_arti2378.mjs`, deleted before finish): Orb/Mirror carry ANTIMAGIC on/off, other-carrier guard incl. the C-quirk stale-bit parity (guard is a pure `cary.adtyp` table check — dropping mirror-then-orb with orb present keeps the bit, exactly like C), Fire Brand wield/carry split, Mitre carry-FIRE vs wear-PROTECT split, Tsurugi wield PROTECT, all six `revoke` no-op guards drawing no RNG, toggle-on fires through to the live `arti_invoke_property` (W_ARTI clears before the headless-only display throw) — 24/24 ok.
**Named:** message paths (pre-existing); `finesse_ahriman` + no-floor-drop levhack ordering incl. the INVIS/CONFLICT tail on that arm (own row); sync-only W_ART-off envelopes (steal/destroy/useup local-shadow paths) keep the sync bit arms — the message tail awaits a finesse-style vehicle there (own row when a falsifier fires); HALRES stays a direct `EHalluc_resistance` write (D-0633, untouched).
**Next:** do not re-pop `set_artifact_intrinsic` for these arms. Falsifier: a rescore or fresh `verify set_artifact_intrinsic` showing a session blocked with it as owner (not mere caller presence), or the `finesse_ahriman` row landing (wires the no-floor arms). No seed/step/coordinate gates.
## 2026-09-15 — D-2377 `potion.c` peffect_water blessed-cure `make_sick` call

**C locus:** `nethack-c/upstream/src/potion.c:716–768` (`peffect_water`; blessed+!hates arm `:744–750` calls `make_sick(0L, NULL, TRUE, SICK_ALL)`); `make_sick` `:137–` (xtime=0 cure path: `usick_type &= ~type`, talk messages, `botl`, `dealloc_killer`). `potionbreathe` `POT_WATER` vapor arm `:2080–2091` audited and already exact in JS (`js/potion.js:2853–2866` — gremlin `split_mon` + lycan vapor `you_unwere(FALSE)`/`you_were()` with no cure, C comment included) — no change there.
**JS:** 1 js file (+5/−4 per `git diff --numstat`: `js/potion.js`; plus the `turns.md:526` map touch + queue refill to 12 Open), under the 600/10 caps. Below the 40-insertion density floor because C is 51 lines and the body shipped in D-1004 — this iteration retires only the named residual.
**Change:** replaced with `await make_sick(0, null, true, SICK_ALL)` (same-file callee — no new import, no new module edge, no TDZ; matches the `curesick` precedent at `js/potion.js:2171`); doc comment + `turns.md:526` updated. Other arms audited against C and left untouched: plain-water pline + `rnd(10)` + `newuhs`; `potion_unkn++`; hates via `mon_hates_blessings||CHAOTIC`; Your-affinity via pline (same text); `youmonst.data==&mons[ulycn]` as `umonnum==ulycn` (C itself compares `u.umonnum` in `potionbreathe` `:2081`); `Upolyd` ≡ `umonnum!=umonster` both sides (`you.h:554` vs `js/const.js:3172`). No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Post-change `node scripts/verify.mjs --fn peffect_water` → PASS syntax (1 file) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 → VERIFY: PASS.
**Named:** none new.
**Next:** do not re-pop `peffect_water` for this body. Falsifier: a rescore or fresh `verify peffect_water` showing a session blocked with `peffect_water` as owner (not mere dopotion/caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2376 `mkobj.c` shrink_glob full body + ice/eat/catch-up/messages

**C locus:** `nethack-c/upstream/src/mkobj.c:1497–1669` (`shrink_glob`: `item_on_ice` before the globby gate; `impossible` + return for non-glob; `check_glob(obj,"shrink obj ")`; `expire_time < svm.moves` catch-up with `(moves-expire+24)/25` delta, `25-(delta%25)` moddelta, ice `/3`, gone→`owt=0`+`shrinking_glob_gone` else `-delta`+`container_weight`+`start_glob_timeout(moddelta)`; `eating_glob`/buried/ice-`moves%3==1` reschedule; `partly_eaten_hack`+`Yname2` before decrement; `msgwt=(max(basewt,1)+1)/2` threshold, oeaten `>1` decrement; invent vs nested-container-in-invent messaging with `near_capacity()!=go.oldcap`; gone→floor cansee gate→`shrinking_glob_gone`→`newsym`→`The→A`→"fades away" else reschedule; `update_inventory`+`encumber_msg` tail) + static helpers `item_on_ice` `:1442–1469` (outermost container, `BURIED_TOO`, FLOOR/BURIED ice switch), `check_glob` `:3419–3443` (quan 1, owt nonzero, GLOB range; `#if 0` arm stays out), `shrinking_glob_gone` `:1672–1701` (invent unwear/stop_occupation/useupall vs MIGRATING-mask-clear/MINVENT-unwield/extract+unhide+obfree), `start_glob_timeout` `:1473–1495` (already exact, untouched), `eat.c:2077–2081` (`eating_glob` occupation-identity), `objnam.c:775–781` (hack before the globby arm), `obj_on_ice` enum `:1435–1439` (0/1/2).
**JS:** 3 js files (+285/−32 per `git diff --numstat`: `js/mkobj.js` +271/−30, `js/eat.js` +10/−0, `js/objnam.js` +4/−2; plus the 1-line `CURRENT.md` Next-cluster copy + `data.md:287` map touch), under the 600/10 caps. Above the 40-insertion density floor (C is 173 lines + 3 static helpers).
**Change:** `js/mkobj.js` — file-local `NOT_ON_ICE/SET_ON_ICE/BURIED_UNDER_ICE` consts + `item_on_ice` (JS `get_obj_location` returns `{x,y}|null`, so the C boolean+out-param gate becomes a null check with the FLOOR/BURIED switch on `otmp.where`) + async `check_glob` + async `shrinking_glob_gone` + exported `async shrink_glob(obj, expire_time = game.moves)` in exact C order/conjuncts; dispatch passes `curr.timeout | 0` (burn_object precedent). JS adaptations, all file-idiomatic: `insane_object` (no JS port — own row) → `impossible()` (same disorder-pline class, no state change); missing `quan` reads as 1 (established JS-unset convention, simpleonames guard); `objs()?.[…]?.oc_weight ?? 20` (file guards `objs()` the same way at weight `:261`); pline templates for the three C formats (JS `pline(msg)` takes one string); `strsubst` return-value form for the `The→A` rewrite. New static edges `mkobj→zap` (`is_ice`), `→steal` (`remove_worn_item`), `→hack` (`stop_occupation`) are all `imports.mjs --can` SAFE (hoisted function declarations, runtime-only use inside the async pair — same shape as the 1866 established cycle edges); `MON_WEP`/`setmnotwielded`, `impossible`, `simpleonames`, `strsubst`, `near_capacity`/`encumber_msg`/`useupall`, `BURIED_TOO` ride pre-existing edges.
**Verify:** `node scripts/verify.mjs --fn shrink_glob` → PASS syntax (3 files) · PASS rule2 · note hidden (0 blocked at HEAD; row cited 0 blocks so no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 → VERIFY: PASS. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. Full `sessions` (mkobj+objnam are shared per the runbook matrix) → 44/44 PASS.
**Named:** `insane_object` full port (object-diagnostic helper; own row when a falsifier fires); `start_glob_timeout` non-glob `impossible` (adding it would flip a sync 3-line helper async across its callers — own row); ice-box containment equivalence for `BURIED_UNDER_ICE` reschedule (C FIXME, same as C).
**Next:** do not re-pop `shrink_glob` for this body. Falsifier: a rescore or fresh `verify shrink_glob` showing a session blocked with `shrink_glob` as owner (not mere run_timers/stepFns presence — the timer fires on many turns); re-queue under the writer it names. No seed/step/coordinate gates.
