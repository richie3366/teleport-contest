# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2055 readobjnam spe/corpsenm writer + oname PL_PSIZ (queue owner readobjnam)

**C locus:** (1–2) `objnam.c readobjnam :5143–5165` STATUE/FIGURINE/CORPSE spe arm — gender-unspecified (C default `mgend=-1`) non-fixed-gender monster draws `rn2(2)`; and `:5192–5236` corpsenm writer — CORPSE arm `set_corpsenm(d.otmp, d.mntmp)` overwrites mksobj's random pick (whose draws both sides already matched through `start_corpse_timeout`). (3) `do_name.c oname` + `global.h:404` (`PL_PSIZ 63`): oname truncates to PL_PSIZ *before* `exist_artifact`/`artifact_exists`, so the 37-char wish name must survive intact to attach the artifact — whose deny check at `:5372–5375` draws `rn2(nartifact_exist())` even in wizard mode (`&& !wizard` gates only the denial, not the draw).
**JS:** 2 files (`readobjnam.js` +~150, `do_name.js` +1/−2), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); /tmp probes only (pure-helper `name_to_monplus`/`artifact_name` checks), none committed.
**Change:** `js/readobjnam.js` — full C spe switch in exact C order before the recharged hunk (TIN 0 + EMPTY/SPINACH contents arms; TOWEL wetness; SLIME_MOLD `ftype` deferred with comment; KEY/CHEST/BOX/BALL/CHAIN retain; STATUE/FIGURINE/CORPSE gender incl. `rn2(2)`; SCR_MAIL + venoms spe=1; WAN_WISHING non-wizard `rn2(10)` else fallthrough to `d.spe`); full corpsenm writer (LONG_WORM_TAIL remap, were→human `counter_were`, TIN `dead_species`, CORPSE `set_corpsenm` + guardian `genus`, EGG `can_be_hatched`, FIGURINE, STATUE direct; SCALE_MAIL stays in its pre-existing hunk). New imports extend safe edges only (`set_corpsenm`→mkobj.js; `mons/G_UNIQ/G_NOCORPSE/is_male/is_female/is_neuter/is_human/is_were`→monsters.js; `MALE/FEMALE/CORPSTAT_*`→const.js; `genus/dead_species/can_be_hatched`→mon.js and `counter_were`→were.js are hoisted functions, cycle-safe per `imports.mjs --can`).
**Verify:** `node scripts/verify.mjs --fn readobjnam` → `PASS syntax 2 changed js file(s): js/do_name.js js/readobjnam.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify readobjnam: 0 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Caveman-92148: moved 59 → inuse_classify@241; scen-wish-Priest-92136: moved 125 → dump_artifact_info@126; scen-wish-Rogue-91119: moved 59 → goodpos@67) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `tin of`/`of`-split + `mgend`/`ishistoric`/`wetness`/`ftype`/`contents`/`zombify` wish-prefix parsing (all read C-defaults here); CORPSE zombify `start_timer` (JS has no `obj_to_any`); STATUE verysmall-spellbook `delete_contents`; `is_quest_artifact` short-circuit in the deny check (identical draws in wizard mode; non-wizard quest-artifact wishes draw one extra `rn2` in JS — needs one exported `is_quest_artifact`, own future row).
**Next:** the three later owners (inuse_classify@241; dump_artifact_info@126; goodpos@67) — revealed downstream, own future rows. Pop the next Open row (`dothrow.c` dofire, 3 corpus blocks).
## 2026-09-07 — D-2054 arrival-message --More-- behind level_tele: DELPHI verbalize + COURT throne + Sam goal_first (queue owner level_tele)

**C locus:** queue owner `teleport.c level_tele :1427` is the *symptom* owner (the `schedule_goto` post-msg literal); the differing screen is written downstream in `goto_level` arrival order. (1) `hack.c check_special_room` DELPHI arm `:3713–3725`: `monstinroom(PM_ORACLE)` → peaceful `verbalize("%s, %s, welcome to Delphi!", Hello(0), plname)` (33 chars; 38+33+3=74 ≥ 72=`CO-8`, so C `update_topl` mores — `topl.c:264`). (2) `quest.c on_goal` → `qt_pager("goal_first")` → `com_pager_core` output=text → `deliver_by_window` NHW_TEXT, whose tty display flushes the pending materialize through the blocking WIN_MESSAGE more (`wintty.c tty_display_nhwindow`). (3) `hack.c` COURT arm `:3677–3681`: `You("enter an opulent%s room!", !furniture_present(THRONE,roomno)?"":" throne")` — 33 chars with throne (more) vs 26 without (append). Supporting: `hack.c monstinroom :3466`, `furniture_present :3482` (both staticfn), `mkroom.c inside_room`, `dat/quest.lua` Sam goal_first text + synopsis/output.
**JS:** 4 files (`hack.js` +~70/−12, `questpgr.js` +12/−2, `quest.js` +3/−2 header docs, `mklev.js` +1/−1), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + recorded C screens + `hidden-proxy show` only).
**Change:** `js/hack.js` — file-local `monstinroom(mndx,roomno)` (fmon scan, DEADMONSTER skip, `mnum ?? data.mndx` + local `in_rooms`, mirroring C pointer-equality via the lock.js oracle idiom) and `furniture_present(furniture,roomno)` (room bbox scan + `inside_room` via lazy `import('./mklev.js')`, same lazy shape as the shk/priest/dungeon imports in this function); COURT awaits the scan and appends `' throne'` iff present; DELPHI SetVoices the oracle and verbalizes the peaceful welcome vs the hostile `"You're in Delphi, …"` (msg_given FALSE only with no oracle, so `room_discovered` now runs — overview/mapseen only, no live-map effect). Imports extend safe edges only (`verbalize` joins the existing display.js import; `Hello`→roles.js and `SetVoice`→sndprocs.js are cycle-free per `imports.mjs --can`; `THRONE`→const.js). `js/mklev.js` — one-word `export` on the existing `inside_room` (mkroom.c port; no clone written, per D-1849).
**Verify:** `node scripts/verify.mjs --fn level_tele` → `PASS syntax 4 changed js file(s): js/hack.js js/mklev.js js/quest.js js/questpgr.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify level_tele: 1 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (tour-Samurai-70015-d5-8-15-17-22: PASS 50/50 — the 10 frozen `--More--` screens reproduce byte-exact once the throne text matches, so the freeze needed no extra port; scen-tour-Samurai-92161: moved 26 → next_ident@34 with toplines matching incl. `--More--`; scen-tour-Barbarian-92129: moved 51 → step-177 map-glyph diff, RNG 38280/38280 fully matched; the "js-throw" token in the verify line is the proxy's null-owner fallback label — replay `error:null`) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap); working scoreboard.json re-scored in the same tree.
**Named:** BARRACKS `monstinroom` occupied-vs-abandoned + `wake_msg` canseemon text (same switch, untouched); Sam `goal_next`/`goal_alt` + all other-role quest bodies (miss→common-retry path untouched); `msg_fallbacks` beyond goal_alt, array rn2, NHW_MENU delivery (pre-existing).
**Next:** (1) 92129's later step-177 map-glyph diff (row 18 `*` vs `·`, owner null, stepFns exercise; my new live-state deltas are message/more/mapseen/sound only — revealed downstream, own future row). (2) 92161's later owner `next_ident`@34 (mkobj.c:521 `rnd(2)` vs JS `monmulti` `rnd(1)` in the shuriken-catch arm; revealed downstream). (3) Pop the next Open row (`objnam.c` readobjnam, 3 corpus blocks).
## 2026-09-07 — D-2053 spell.c study_book dull-book sleep arm (queue owner study_book)

**C locus:** `spell.c study_book :468–496` (dull arm; `rnd(25)` at :478): `if (!confused && !Sleep_resistance && objdescr_is(spellbook,"dull")) { dullbook = rnd(25)-ACURR(A_WIS); if (spbook.delay && book==spbook.book) dullbook -= rnd(oc_level); if (dullbook>0) { eyes=body_part(EYE); plural if eyecount>1; pline dull; dullbook += rnd(2*oc_level); fall_asleep(-dullbook,TRUE); return 1; } }` — draws fire even when the hero stays awake. Supporting: `youprop.h:34–36` Sleep_resistance = H||E; `o_init.c objdescr_is`; `hack.c fall_asleep`.
**JS:** 1 file (`spell.js` +33/−9), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + `hidden-proxy show` only).
**Change:** `js/spell.js` — port the dull arm in exact C order (before the interrupted-continue arm, after context creation): `!confused && !sleepRes && objdescr_is(spellbook,'dull')` with `sleepRes = H||E||flat` (C H||E plus the JS flat fallback, same idiom as mhitu/mthrowu); `rnd(25)-acurr(A_WIS)`; resume `rnd(ocLevel)` only when `spbook.delay && book===spbook.book`; sleep path pluralizes `body_part(EYE)` via `eyecount(youmonst.data)>1`, awaits the dull pline, adds `rnd(2*ocLevel)`, calls sync `fall_asleep(-dullbook,true)`, returns 1. Imports extend existing edges only (`makeplural`→objnam, `eyecount`→monsters, `fall_asleep`→hack, `objdescr_is`→apply, `body_part`→polyself, `EYE`→const — all hoisted functions, cycle-safe per `imports.mjs --can`, same SCC shape as the 1866 existing edges).
**Verify:** `node scripts/verify.mjs --fn study_book` → `PASS syntax 1 changed js file(s): js/spell.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify study_book: 1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Healer-92029: PASS; scen-wish-Healer-92066: moved → one_characteristic@51 was 40, strictly later step + later owner) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap); scoreboard.json re-scored in the same working tree (92029 PASS, 92066 scrM 40→58).
**Named:** `confused_book` body (still deferred in study_book + module header — neither blocked session reaches the confused arm); `check_unpaid`/lenses-speed/deadbook polish (pre-existing, untouched).
**Next:** (1) 92066's later owner `one_characteristic` (insight.c:926, step 51). (2) Pop the next Open row (`teleport.c` level_tele, 3 corpus blocks).
## 2026-09-07 — D-2052 uhitm.c passive proxy row: true owners find_roll_to_hit maybe_polyd + abon poly arm + passive mdamageu lethality

**C locus:** (a) `uhitm.c find_roll_to_hit :375–380` (`+ maybe_polyd(youmonst.data->mlevel, u.ulevel)`); (b) `weapon.c abon :950–956` (`if (Upolyd) return adj_lev(&mons[u.umonnum]) - 3` — STR/DEX bands skipped); (c) `uhitm.c passive` AD_ACID/MAGM/COLD/FIRE/ELEC arms, which call `mdamageu`, and `mhitu.c mdamageu :1902–1927` (non-poly `uhp<1` → `done_in_by` noreturn — no healmon/split after).
**JS:** 1 file (`uhitm.js` +49/−8 per diff stat incl. comments), under the 600/10 caps. No DIAG/FORCE/seed gates; probes reverted before verify.
**Change:** `js/uhitm.js` — `find_roll_to_hit` adds `Upolyd(u) ? youmonst.data.mlevel : u.ulevel`; `abon` early-returns `adj_lev(youmonst.data)-3` when poly'd (`adj_lev` joins the existing `makemon.js` import — no new edge); all five passive arms `await mdamageu(mon, tmp)` (dynamic `mhitu.js` import, same cycle dodge as the hitum caller) + `gameover` bail mirroring C's noreturn; `hmonas` bails before knockback (RNG) on `gameover`. Proxy owner `passive` was symptom for (a)/(b) — hit verdict is decided upstream — and true owner for (c).
**Verify:** `node scripts/verify.mjs --fn passive` → `PASS syntax 1 changed js file(s)` · `PASS rule2` · `hidden: 1 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Healer-92107 → `readobjnam_postparse1`@126; Rogue-92026 → `mattacku`@163; Wizard-92153 PASS) · `PASS green 2/2` · `PASS strict` · `PASS cohort 7/7`; then `--full` → `PASS full 44/44`.
**Named:** `find_roll_to_hit` monk-armor/encumbrance/`utrap`/orc-vs-elf (RNG-free, no corpus demand); `abon` STR≥18/100 `sbon=3` band (pre-existing named deferral); `monstunseesu/monstseesu` + `shieldeff` + `ugolemeffects` in the five arms; `showdamage`/`impossible` in `mdamageu`; post-death continuation in `dokick` passive callers (own future row if the corpus demands it).
**Next:** (1) Healer-92107's later owner `readobjnam_postparse1`@126. (2) Rogue-92026's later owner `mattacku`@163.
## 2026-09-07 — D-2051 hack.c losehp noreturn drained at touch_artifact blast + poisoned HP arm

**C locus:** `hack.c losehp :4256–4292` (fatal arm: killer-name copy, `urgent_pline("You die...")`, `done(DIED)` — noreturn, so no caller code after a fatal losehp ever runs) + callers `artifact.c touch_artifact :944–960` (blast `losehp(dmg,...)` :958 then `exercise(A_WIS)` :959, evade/control pline :963–971) and `attrib.c poisoned :388–391` (HP-damage arm `losehp(loss,...)` :391, trailing `if (uhp<1) done(POISONING|DIED)` only reachable via the instant-kill arm). Supporting: `end.c done :1106–1116` (wizard/discover "Die?" offer — explains the Ranger JS screen: JS reached done() with the death flag but without the pline).
**JS:** 2 files (`artifact.js` +10/−1, `attrib.js` +11/−0), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); /tmp scratch probe only, nothing committed.
**Change:** `js/artifact.js` — after the blast-arm losehp, `await finish_maybe_wail()` (no-op unless the low-HP flag was set; C runs maybe_wail inside losehp before returning) then `if (game._losehp_needs_done) { await finish_losehp_done(); return 0; }` before `exercise` (C never reaches :959+ on a fatal blast). `finish_maybe_wail` extends the existing `./hack.js` static edge; `finish_losehp_done` via dynamic `import('./end.js')` (hoisted, cycle-safe per `imports.mjs --can`; same shape as the `hack.js:2958` fall-on-sink idiom). `js/attrib.js` — identical drain after the HP-arm losehp (`attrib.c:391`); the survived-reaction arm needs none (`adjuhploss` only shrinks, and entry requires `uhp > loss`, so that losehp is provably non-fatal).
**Verify:** `node scripts/verify.mjs --fn losehp` → `PASS syntax 2 changed js file(s): js/artifact.js js/attrib.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify losehp: 2 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Tourist-92081: PASS; Ranger-92126: PASS; Archeologist-91132 → really_done@25 was 23; Valkyrie-92014 → exercise@48 was 46 — both strictly later steps + later owners) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Mid-iteration run (touch_artifact drain only) showed 1 PASS + Ranger unchanged, which localized the second cause to `poisoned` (JS RNG tail `rn2(30)@poisoned:407` + `rn2(10)@poisoned:434`, i>5 HP arm); final verify ran after the last edit (no D-1831 gap).
**Named:** `showdamage` / `rehumanize` in losehp (pre-existing deferred, untouched — no blocked session reaches Upolyd-fatal here); poisoned G_UNIQ/`the()` killer polish, Half_gas_damage towel (pre-existing, untouched); touch_artifact monster covetous/mplayer arms (pre-existing, untouched).
**Next:** (1) 91132's later owner `really_done` (step 25). (2) 92014's later owner `exercise` (step 48). (3) Pop the next Open row (`uhitm.c` passive, 3 corpus blocks).
## 2026-09-07 — D-2050 polyself.c polymon eyeless-form see-again arm (queue owner Blindf_off)

**C locus:** `polyself.c polymon :735–902` (entry `was_blind = !!Blind` :739, before `u.umonnum=mntmp; set_uasmon()` swaps the FROMFORM eyeless bit; `:899–902 if (was_blind && !Blind) { set_itimeout(&HBlinded,1L); make_blinded(0L,TRUE); }` after `break_armor()` :888 / `drop_weapon(1)` :889 / `find_ac()` :890, before `newsym` :903) + `potion.c make_blinded :260–331` (probe-set-restore + `can_see_now && !u_could_see` → `You("can see again.")` + xor `toggle_blindness` — already faithful in JS `do.js:2823`) + `youprop.h:103 #define Blind ((HBlinded||EBlinded) && !BBlinded)` (matches JS `Blind()` modulo `uroleplay.blind`) + FROMFORM `PROPSET(BLINDED, !haseyes(mdat))` :107 (already live in JS `set_uasmon`).
**JS:** 1 file (`polyself.js` +22/−2), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + `hidden-proxy show` only).
**Change:** `js/polyself.js` — capture `wasBlind` at polymon entry with the C `Blind` predicate (same inline shape as `polyman`, incl. `uroleplay.blind`); after `break_armor` + `drop_weapon` (C `:888`-before-`:899` order kept) and before `newsym`, `if (wasBlind && !nowBlind)` sets `HBlinded`/`uprops[BLINDED].intrinsic` TIMEOUT to 1 then `await make_blinded(0, true)`. No import changes (`make_blinded`, `BLINDED`, `TIMEOUT` already imported; no new cross-module edge, so no `imports.mjs --can` needed).
**Verify:** `node scripts/verify.mjs --fn Blindf_off` → `PASS syntax 1 changed js file(s): js/polyself.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify Blindf_off: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-poly-Ranger-92090 → mhitm_knockback@236 was 211; scen-poly-Ranger-92133 → damageum@150 was 129 — both strictly later steps + later owners) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `gulp_blnd_check` gate in `Blindf_off` (pre-existing map omit, untouched — neither blocked session reaches it); polymon Stoned/Sick/Slimed/strangle/glib, hideunder, utrap, egg/swallow/steed, skinback, retouch_equipment (pre-existing, untouched). No corpus session reaches the blindfold-plus-headless-eyed double-print edge (Blindf_off see-again followed by this arm) — stated honestly; arm order mirrors C exactly.
**Next:** (1) 92090's later owner `mhitm_knockback` (step 236). (2) 92133's later owner `damageum` (uhitm.c, step 150). (3) Pop the next Open row (`hack.c` losehp, 4 corpus blocks).
## 2026-09-07 — D-2049 uhitm.c mhitm_ad_were mhitu lycanthropy arm (queue owner mhitm_ad_were)

**C locus:** `uhitm.c mhitm_ad_were :4264–4293` (full body read from the brief): three arms — uhitm (magr==you → mhitm_ad_phys + done check), mhitu (mdef==you → unconditional `hitmsg(magr,mattk)` then `!rn2(4) && u.ulycn==NON_PM && !Protection_from_shape_changers && !defends(AD_WERE,uwep) && !mhitm_mgc_atk_negated(magr,mdef,TRUE)` → feverish + exercise + set_ulycn + retouch_equipment), mhitm (else → mhitm_ad_phys + done check). Caller `mhitm_adtyping :4782+` (`case AD_WERE` at :4789) dispatches all three shapes through this one function. Supporting reads: `mhitm_mgc_atk_negated :74–99` (mcan → TRUE no roll; else `rn2(10) >= 3*magic_negation(mdef)`; verbose hero arm is `You("avoid harm.")`), `mhitu.c hitmsg :29–81` (RNG-free: could_seduce branch or aatyp verb + `mattk==prev+1` " again"), `artifact.c retouch_equipment :2640–2705` (67-line untouchable/retouch_object/bypass chain — deliberately not ported), `youprop.h:359–360` Protection macro (H||E).
**JS:** 2 files (`mhitu.js` +32/−3, `mhitm.js` +3/−4 comment-only), +35/−7 total, under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + recorded C-vs-JS draws/toplines only).
**Change:** `js/mhitu.js` — new `mhitm_ad_were_u(mtmp,mattk,mhm)` in the D-2043 `mhitm_ad_slow_u` shape: unconditional `await hitmsg(mtmp,mattk)` first (RNG-free both sides), then the exact C short-circuit (`!rn2(4) && ulycn==NON_PM && !Protection_from_shape_changers() && !defends(AD_WERE,uwep) && !mgc-negated(TRUE)`) → `urgent_pline('You feel feverish.')` + `exercise(A_CON,false)` + `set_ulycn(monsndx)` via the `magr?.data?.mndx ?? mnum` idiom; leftover `d()` kept (the were arm never zeroes damage, like FAMN/SLOW). `case AD_WERE` added to `mhitm_adtyping_u` (+ doc list). Negation call is `mhitm_mgc_atk_negated(mtmp,null,true)` — null-defender is the file's hero idiom (slow arm), matching C's `mdef==&youmonst` verbose `You avoid harm` path.
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_were` → `PASS syntax 2 changed js file(s): js/mhitm.js js/mhitu.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify mhitm_ad_were: 1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (genesis-Knight-92149: PASS; wish-Rogue-92184 → ready_weapon@132 was 54, strictly later step + later owner) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS` + full `node frozen/ps_test_runner.mjs sessions` → **44/44** (fortress holds; speed `77+0.47/turn`). Final verify ran after the last edit (no D-1831 gap).
**Named:** `retouch_equipment(2)` after `set_ulycn` (same as eat.js cpostfx D-0945 — untouchable/retouch_object/bypass chain unported; fires only on a contracted-infection turn, which neither blocked session reaches — both C draws are nonzero rn2(4), so the feverish arm is C-cited but corpus-unreached, stated honestly); uhitm you-as-agr arm stays shared-shape via mhitm_ad_phys (pre-existing, untouched).
**Next:** (1) wish-Rogue-92184's later owner `ready_weapon` (wield.c, step 132 — parked before per LOOP-QUEUE Parked `ready_weapon`: needs the Knight-92204 spin falsifier first; do not pop from this). (2) Pop the next Open row (`do_wear.c` Blindf_off, 2 corpus blocks).
## 2026-09-07 — D-2048 invent.c addinv_core0 quiver fill fires on merge (review 1014 C-wrong 1)

**C locus:** `invent.c addinv_core0 :1055–1148` (full body read from the brief): the `other_obj` reinsert, the quiver-prefer merge, and the general merge loop all end in `goto added` — every one bypasses the `:1128–1140` fill arm (`obj_was_thrown && flags.pickup_thrown && !uquiver && oartifact != ART_MJOLLNIR && otyp != AKLYS && (throwing_weapon || is_ammo)` → `setuqwep`). The fill fires only on the fresh-insert fall-through (assigninvlet + chain link, then fill, then `added:`).
**JS:** 1 file (`u_init.js`), +2/−8, under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + review 1014's C-vs-JS control-flow read).
**Change:** `js/u_init.js` — deleted the merge-survivor fill hunk; left a two-line C comment (`merge paths goto added, bypassing :1128–1140 — no setuqwep here`). Fresh-insert hunk untouched. No import changes (`throwing_weapon`/`is_ammo`/`setuqwep` still used by the fresh-insert arm and `ini_inv_use_obj`).
**Verify:** `node scripts/verify.mjs --fn addinv_core0` → `PASS syntax 1 changed js file(s): js/u_init.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify addinv_core0: no corpus session is blocked on it at HEAD` (vacuous — the queue row cites a review, not corpus blocks; NOT claimed as a corpus PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap). Deletion-only on a C-bypassed path; the surviving fresh-insert arm keeps its D-2044 corpus movement (Rogue-92115 → start_tin@118, Rogue-92030 → dofire@63 per review 1014's re-measure).
**Named:** quiver-prefer merge, `addinv_before` reinsert, oname absorb, worn-slot merge, `how_lost` clear (pre-existing map omits in the same `addinv` envelope, untouched — the deleted hunk sat in the general-merge arm only).
**Next:** pop the next Open row (`uhitm.c` mhitm_ad_were, 2 corpus blocks).
## 2026-09-07 — Audit reviews 1010–1017 (D-2040…D-2047) + cadence 44/44

Review-only, no js/. Each SHA re-measured `verify <fn> --base SHA~1`
vs pinned C. 7 ACCEPT, 1 QUALITY-RISK: 1014 fills the addinv thrown
quiver on merge, C skips it (`goto added`) — Must-fix prepended, Next
cluster set. Notes: caitiff sites are five not ten; Valkyrie `js-throw`
label phantom (error null); sym.mjs misses export-list consts. Full
`sessions` 44/44 (Scr 11405, RNG 792838). Queue mf=1 open=10, no refill.
## 2026-09-07 — D-2047 monmove.c m_search_items shop gate + scan arms (queue owner m_search_items)

**C locus:** `monmove.c m_search_items :1329–1450` (full body read from the brief) + caller `monmove.c:1906` (`if (getitems && m_search_items(...)) return postmov(...)`) + `monst.h:251` `#define helpless(mon) ((mon)->msleeping || !(mon)->mcanmove)` + `mon.c can_touch_safely :1958–1974` (corpse/silver/artifact checks, no RNG) + `monmove.c onscary :241–300` (no RNG). C order: minr guards → `if (*in_rooms(omx, omy, SHOPBASE) && (rn2(25) || mtmp->isshk)) goto finish_search` → per-cell scan (OBJ_AT, minr tighten, could_reach_item, hides_under+cansee, m_at helpless/mundetected/mappearance/nomove, onscary, trap-known with gg reset, m_cansee, costly_spot) → per-item (ROCK skip, mines/soko prize skip, costly+!no_charge skip, `((take && carry>0) || consume) && touch` with C short-circuit) → finish_search tail (appr −1 → mux/muy or 1).
**JS:** 1 file (`monmove.js`, scan-body rewrite + 2 import names), +72/−54, under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean); no committed probes (diagnosis via `brief.mjs` C body + `hidden-proxy show` only).
**Change:** `js/monmove.js` — `const shopSkip = in_rooms(omx, omy, SHOPBASE) && (rn2(25) || mtmp.isshk)` with C short-circuit order (rn2 draws only in shop) gating the whole scan as C's `goto finish_search` (falls through to the tail); cell arms added in C order (`hides_under(ptr) && cansee(xx,yy)`, `onscary(xx,yy,mtmp)`, per-cell `costly = costly_spot(xx,yy)` after m_cansee); per-item arms added in C order (ROCK, `is_mines_prize||is_soko_prize`, `costly && !no_charge`, outer `can_touch_safely` preserving `((take&&carry>0)||consume)&&touch` short-circuit). Imports: `onscary` extends the existing mon.js edge, `costly_spot` extends the existing shk.js edge (`imports.mjs --can`: both ALREADY, no new edge; sync callees, same SCC, runtime calls only); `in_rooms`/`SHOPBASE`/`hides_under`/`cansee`/`is_mines_prize`/`is_soko_prize`/`rn2` were already imported. The `helpless` expansion (`!mcanmove||msleeping`) already matched C exactly — comment now cites monst.h:251.
**Verify:** `node scripts/verify.mjs --fn m_search_items` → `PASS syntax 1 changed js file(s): js/monmove.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify m_search_items: 0 PASS, 2 moved past (2 re-attributed at the same step), 0 unchanged, 0 worse → PROGRESS` (Samurai-92032 → obj_resists@59 was 59; Tourist-92100 → mpickstuff@100 was 100) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `mon_would_consume_item` body (still stub false — own future row); `can_touch_safely` silver/artifact/petrify inner arms (stub true); `onscary` inhishop/inhistemple + lminion/unique resist arms (deferred inside the live mon.js arms); `searches_for_item` subset + FOOD corpse/tin/egg + can_blow polish (pre-existing map omits, untouched). No corpus session reaches the hides/onscary/costly/prize/touch arms in isolation — stated honestly; the corpus-verified arm is the shop rn2 gate.
**Next:** (1) Tourist-92100's later owner `mpickstuff` (mon.c:1857 `rn2(25)` — the same shop-gate shape, still deferred in JS `mpickstuff`) is its own future row. (2) Samurai-92032's later owner `obj_resists` (zap.c:1469) ditto. (3) Pop the next Open row (`uhitm.c` mhitm_ad_were).
