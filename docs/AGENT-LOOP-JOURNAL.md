# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-06 — D-1982 wintty.c CLIPPING cliparound pan offsets + tty consumer gates

**C locus:** `nethack-c/upstream/win/tty/wintty.c` — `setclipped` `:3806–3814` (clipping on, pan at origin, `clipxmax = CO`, `clipymax = LI - 1 - wc2_statuslines`) + `newclipping` `:471–485` (small screen → setclipped + `tty_cliparound` when x nonzero; full screen → clipping off, offsets zeroed) + `tty_cliparound` `:3817–3842` (x hysteresis 5-margin/shift-20 clamped to COLNO; y hysteresis 2-margin/shift-half-viewport clamped to ROWNO; origin moved → `redraw_map(TRUE)` `:3840`) + `tty_print_glyph` clip gate `:3869–3873` (`x <= clipx || y < clipy || x >= clipxmax || y >= clipymax` → return) + `tty_curs` WIN_MAP offset `:2119–2124` (`x -= clipx, y -= clipy`) + `tty_display_nhwindow` board loop `:3696–3705` (skip + `row_refresh(xmin + clipx - offx, …, y + clipy - offy)`). Dispatch: `include/winprocs.h:141–142` `#define cliparound (*windowprocs.win_cliparound)` → tty procs. `CLIPPING` always compiled in (`config.h:537–538`); HUPSKIP empty without HANGUPHANDLING (`:93–94`, no arm); `wc2_statuslines` defaults 2 (`options.c:7263`). Core callers (still unwired): `allmain.c:546`, `dungeon.c:1580`, `getpos.c:851/1146`, `muse.c:2637`, `restore.c:629`.
**JS:** `js/display.js` (+153/−11); 1 js file, under 600 cap. Density note: C is ~7 + ~12 + ~24 + gate/offset arms (~15 lines); the JS is the four full C-ordered functions with `:line` citations plus the three live consumer gates (single queue row, every callee live — no stubs in a live arm).
**Change:** `js/display.js` — module-local `clipping`/`clipx`/`clipxmax`/`clipy`/`clipymax` (C file-statics) + `clip_screen_size()` (`CO`/`LI` ≡ `game.nhDisplay.cols/rows`, 80x24 fallback = the layout JS always renders) + exported `setclipped()` in exact C order (`?? 2` reproduces the C `wc2_statuslines` default) + exported async `newclipping(x, y)` (`CO < COLNO || LI < 1 + ROWNO + statuslines` → setclipped + `if (xi)` pan, else clipping off; async only for the `tty_cliparound`→`redraw_map` nhgetch reach, same shape as `redraw_map` D-1974) + exported async `tty_cliparound(x, y)` (early `!clipping` return; x/y hysteresis in C order with `Math.max/min` and `| 0` truncation matching C `/ 2`; origin moved → `await redraw_map(1)`) + exported async `cliparound(x, y)` dispatch alias (the `winprocs.h` macro shape). Sync consumer gates, all shut at 80x24: `_paint_gbuf_cell` early return (the `tty_print_glyph` gate), `docorner` skip (`y + clipy > ROWNO`) + offset `row_refresh` segment (the `:3696–3705` arms), hero cursor minus clips when clipping (the `tty_curs` arm). No new cross-module import (all same-module), so no `imports.mjs --can` owed.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. /tmp small-screen probe of the arms no suite reaches (40x20: base paints map-x 1..39 only; `tty_cliparound(70,10)` pans to 21..59; `tty_cliparound(70,19)` pans to 41..79 with y = 3 gated by clipy = 4; back at 80x24 `newclipping` switches clipping off and all 79 cells paint) → 4/4 C-arithmetic arms exact (deleted before finishing). `node scripts/verify.mjs --fn cliparound` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify cliparound: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — map-driven Open row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing` (auto: shared file changed).
**Named:** core `cliparound` call sites (`allmain.c:546` moveloop, `dungeon.c:1580` u_on_newpos, `getpos.c:851/1146`, `muse.c:2637`, `restore.c:629`) — functions live, unwired; resize/preference_update `newclipping` callers (no JS resize path; display fixed 80x24); `flush_screen` dirty-span loop stays its own Open row.
**Next:** pop the next Open row (`display.c` map_glyphinfo ov_* override tables).
## 2026-09-06 — D-1981 display.c under_water/under_ground engulfed-water/buried map arms + docrt routing

**C locus:** `nethack-c/upstream/src/display.c` — `under_water` `:1395–1437` (guard `:1402–1403` waterlevel/swallow outrank; full `:1406–1408` cls; delayed-full `:1411–1414` dela latch + return; limited `:1418–1423` blank old 3x3 y-outer/x-inner; repaint `:1425–1434` pool/lava/ice 3x3 x-outer/y-inner, Blind `:1430` blanks off-hero) + `under_ground` `:1445–1467` (swallow guard `:1450–1451`; full `:1454–1456` cls only; delayed `:1459–1462`; limited `:1464–1466` newsym hero cell) + `docrt_flags` arms `:1730–1732` (`Underwater` ≡ `u.uinwater`, youprop.h:279, non-waterlevel → `under_water(1)`) and `:1734–1736` (`u.uburied` → `under_ground(1)`, C's own `/* [not implemented] */` marker notwithstanding). Callers: `allmain.c:432,434`, `detect.c:99,101`, `dig.c:2225,2234`, `trap.c:5123`.
**JS:** `js/display.js` (+104/−1); 1 js file, under 600 cap. Density note: C is ~42 + ~22 + ~7 arm lines; the JS is the two full C-ordered functions with statics plus the two docrt arms (single queue row, every callee live or imported — no stubs in a live arm).
**Change:** `js/display.js` — new exported async `under_water(mode)` in exact C order with `:line` citations (guard via imported `Is_waterlevel` + `u.uswallow`; `await cls()`; mode `| 0` arms; limited blank via `show_glyph_cell(x, y, ' ', NO_COLOR, false, 0, GLYPH_UNEXPLORED)` per the show_memory_glyph precedent; repaint via local `is_pool_or_lava_disp`/`is_ice_disp` clones — NOT a 4th clone, reused — + `newsym`, Blind via local `hero_Blind` D-0716 + imported `u_at`; module `_underwater_lastx/lasty/dela` statics) + new exported async `under_ground(mode)` (same shape, full = cls only, limited = `newsym(ux, uy)`; `_underground_dela` static) + the two `docrt_flags` arms in `docrt()` in C order after uswallow (`(u.uinwater|0) && !Is_waterlevel` → `await under_water(1)`; `u.uburied` → `await under_ground(1)`), each returning before the vision path. Async rides the `cls`/`show_glyph_cell` bot/more reach (same shape as `redraw_map` D-1974). No new cross-module import (all callees same-module or already imported), so no `imports.mjs --can` owed.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. /tmp headless probe of the arms no suite reaches (water mode-2 latch → mode-0 full-cls far marker; swallow guard holds mode-1; docrt routes engulfed; docrt routes buried; ground mode-2 latch → full-cls; ground mode-0 paints hero cell only, far cell untouched) → 6/6 PASS (deleted before finishing). `node scripts/verify.mjs --fn under_water` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify under_water: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none for under_water/under_ground/docrt — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing` (auto: shared file changed).
**Named:** non-docrt caller wiring — functions live, unwired (`allmain.c:432,434` moveloop limited updates, `detect.c:99,101` map_redisplay delayed updates, `dig.c:2225` bury full + `:2234` unearth limited, `trap.c:5123` drown full); `docrt_flags` maponly/redrawonly/nocls + `disp.botlx`/`update_inventory` post_map (still named on `docrt`); display.js legacy `u.Underwater` reads (`feel_location`, `covers_objects`, `hack.js:1503`) vs the live `u.uinwater` field this port reads per C — out of scope, flag for a state-unification pass.
**Next:** pop the next Open row (`display.c` cliparound + CLIPPING clipx/clipy).
## 2026-09-06 — D-1980 sounds.c domonnoise verbl_msg_mcan epilogue + oracle_loc save-rest

**C locus:** `nethack-c/upstream/src/sounds.c` — `domonnoise` epilogue `:1222–1241` (`pline_msg`, then `:1224–1226` `mtmp->mcan && verbl_msg_mcan` → `SetVoice` + `verbalize1`, then `verbl_msg` with the Death caps arm); only MS_NURSE `:1161` sets `verbl_msg_mcan`. `nethack-c/upstream/src/rumors.c` — `save_oracles` `:598–620` (called from `save.c:321`: `Sfo_unsigned` cnt + `Sfo_ulong` live `oracle_loc[0..cnt)`; `release_data` FREEING arm omitted) + `restore_oracles` `:623–636` (called from `restore.c:712`: `Sfi` cnt + deck, `oracle_flg = 1`, no `init_oracles`). Callers: `dogmove.c:965`, `sounds.c:527,1302,1408,1456,1526`.
**JS:** `js/sounds.js` (+13/−8) `js/rumors.js` (+35/−1) `js/save.js` (+7/−1); 3 js files, ~55 insertions, under 600 cap. Density note: C is ~20 + ~23 + ~14 lines across the epilogue and the two rumors.c functions; the JS is the one C-ordered arm plus the two full C-ordered save/rest functions with wire-up (single queue row, every callee live or imported — no stubs in a live arm).
**Change:** `js/sounds.js` — mcan arm between `pline_msg` and `verbl_msg` in exact C order (`mtmp.mcan && verbl_msg_mcan` short-circuit; `SetVoice` + `await verbalize`, the same `verbalize1` fold the `verbl_msg` arm uses); no new import (`SetVoice`/`verbalize` already imported). `js/rumors.js` — new exported `save_oracles()` (live swap-remove deck prefix; C dlb offsets vs JS `ORACLE_RECORDS` indices noted as the Rule #2 embed, same deck role) + new exported `restore_oracles(saved)` (cnt 0/missing = old save no-op, fresh-boot flg 0 → `init_oracles`, exactly as C assigns no flg on cnt 0; short entries padded). `js/save.js` — `oracles: save_oracles()` in the `dosave0` payload (artidisco precedent) + `restore_oracles(payload.oracles)` in `try_restore_save`; static `./rumors.js` edge (`imports.mjs --can` CHECK — same 90-module SCC, both names read lazily in function bodies only, no TDZ; `restore_artifacts` precedent).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. /tmp headless probe of the arms no suite reaches (cancelled vs normal nurse exact toplines via `game._pending_message`; save shape `{cnt:3,loc:[0,5,9]}` from a 4-deep deck; restore deck+flg; old-save no-op) → 8/8 PASS (deleted before finishing). `node scripts/verify.mjs --fn domonnoise` → VERIFY: PASS — `PASS syntax 3 changed js file(s): js/rumors.js js/save.js js/sounds.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify domonnoise: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — TOP30 depth row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; plus `node scripts/verify.mjs --fn domonnoise --full` → `PASS full 44/44 passing`.
**Named:** `outoracle` open-failure `oracle_flg = -1` arm (`rumors.c:690–693` — embed cannot fail, flg<0 only re-arms if ever set); `MS_ARREST` peaceful keeps the epilogue fold (C's in-case `verbalize` is the same `SetVoice`-no-op + quote, D-1977); `demon_talk`/`cuss` caller wiring beyond domonnoise (`monmove.c:823/985` stay — D-1978); `save_killers` still named on `dosave0`.
**Next:** pop the next Open row (`display.c` under_water/under_ground).
## 2026-09-06 — Audit D-1971…D-1979 against pinned C (reviews 941–949) + cadence score

**Scope:** 9 JS-touching SHAs since the 932–940 audit (`b05d6229`…`816104a5`); one SHA file written per SHA, grouped commit. **Verdicts:** 8 ACCEPT, 1 ACCEPT-WITH-DEBT (941 travel TEST_TRAV: obstructed arm drops C's unconditional Underwater block `:1024–1029` while the header claims "outcome identical" — real only for underwater tunneling/passes-bars polyforms; review-listed debt, no falsifier, not Must-fix), 0 QUALITY-RISK, 0 Must-fix prepended. Notable confirms: `rn2(SIZE(vampmsg))` 2-entry table (947), clang L→R roll order in `cuss` (948), demon_talk `rnd(80)`-before-demand + bribe-Deaf short-circuit (948), ARREST-peaceful via epilogue identical-today with an mcan-consumption landmine flagged for the next row (947), all `--can` probes ALREADY or leaf-safe (949 PM_HEALER zero-import leaf). Every D-log vacuous-verify note re-measured true at parent baselines (0 blocks throughout).
**Score:** full `sessions` 44/44 at `816104a5`; Scr 11,405/11,405, RNG 792,838/792,838 (identical). CURRENT Score refreshed; Next cluster advanced to the queue head (`verbl_msg_mcan` + `oracle_loc`, D-1979 shipped the old pointer); D-1979 `**Addressed:**` short hash filled (`816104a5`).
**Next:** pop the first Open row (`sounds.c` domonnoise verbl_msg_mcan + oracle_loc).
## 2026-09-06 — D-1979 sounds.c domonnoise MS_NURSE/GUARD nurse/guard chat depth

**C locus:** `nethack-c/upstream/src/sounds.c` — `domonnoise` MS_NURSE `:1160–1172` (`verbl_msg_mcan` + uwep WEAPON_CLASS/`is_weptool` weapon ladder / uarmc-uarmf armor ladder with `Role_if(PM_HEALER)` Doc gate / uarmu shirt / relax) + MS_GUARD `:1173–1178` (`money_cnt(gi.invent)` drop-gold vs follow-me). Callers: `dogmove.c:965`, `sounds.c:527,1302,1408,1456,1526`.
**JS:** `js/sounds.js` (+46/−9); 1 js file, under 600 cap. Density note: C is ~18 arm lines; the JS is the two full C-ordered arms + 3 import extends + `verbl_msg_mcan` declaration (single queue row, every callee live or imported — no stubs in a live arm).
**Change:** `js/sounds.js` — MS_NURSE + MS_GUARD arms in C switch order (before SOLDIER, as in C) with `:line` citations. uwep is `game.u.uwep` and uarm* are `game.u.uarm*` (do_wear.js precedent); `Role_if(PM_HEALER)` is `(game.urole?.mnum|0)===PM_HEALER` (potion.js Role_if_healer precedent, local inline — no new static edge); `is_weptool` extends the new `./wield.js` edge (`imports.mjs --can` hoisted function, cycle-safe, same 90-module SCC, runtime-only read, no TDZ); `money_cnt` extends the existing `./shk.js` edge (ALREADY); `WEAPON_CLASS` extends the existing `./objects.js` edge; `PM_HEALER` is a new `./generated/monsters_data.js` edge (leaf data module, no cycle). `verbl_msg_mcan` is set per C `:1161`; its `:1224–1226` mtmp->mcan epilogue consumption stays named for the next Open row. No RNG in either arm (order-exact by construction).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. Headless probe of the arms no suite reaches (nurse weapon/armor/shirt/naked, guard gold/broke, healer-armor Doc, plain-tool relax, weptool-pickaxe weapon) → 8/8 exact C strings, ECMD_TIME, no throw (deleted before finishing). `node scripts/verify.mjs --fn domonnoise` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/sounds.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify domonnoise: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — TOP30 depth row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`.
**Named:** `verbl_msg_mcan` cancelled-speech epilogue (`:1224–1226` mtmp->mcan arm — next Open row) + save-rest `oracle_loc` (same next row).
**Next:** pop the next Open row (`sounds.c` domonnoise verbl_msg_mcan + oracle_loc).
## 2026-09-06 — D-1978 sounds.c domonnoise MS_BRIBE/CUSS/SPELL demon/caster chat depth

**C locus:** `nethack-c/upstream/src/sounds.c` — `domonnoise` MS_BRIBE `:1142–1145` (peaceful untame `demon_talk` + break, else FALLTHROUGH), MS_CUSS `:1146–1156` (hostile `cuss` / minion patience / doom), MS_SPELL `:1157–1160` (vague cantrip). `nethack-c/upstream/src/minion.c` — `demon_talk` `:262–358` (blade anger / faint wake vs stop_occupation+nomul/unmul / dprince reveal / S_DEMON kin + rloc / cash×(rnd(80)+20×Athome) demand / amulet-Deaf unmeetable rn1(1000,125) / bribe offer / rnd(5×ACURR(A_CHA)) fallback / livelog + mongone; Athome `:259`). `nethack-c/upstream/src/wizard.c` — `cuss` `:845–883` + `random_insult` `:824–833` (28) + `random_malediction` `:835–843` (11); ROLL_FROM `hack.h :1493`. Callers: `dogmove.c:965`, `sounds.c:527,1302,1408,1456,1526`; `monmove.c:823` + `sounds.c:1143` (demon_talk); `monmove.c:985` + `sounds.c:1150` (cuss).
**JS:** `js/minion.js` (+~118/−~11) `js/wizard.js` (+~85/−~4) `js/sounds.js` (+~33/−~4); 3 js files, ~236 insertions, under 600 cap. Density note: C is ~95 + ~39 + ~18 arm lines; the JS is two full C-ordered functions with verbatim tables plus the three C-ordered arms (single queue row, every callee live or imported — no stubs in a live arm).
**Change:** `js/minion.js` — new exported async `demon_talk(mtmp)` in exact C order with `:line` citations (local `u_wield_art`/`Amonnam`/`sgn`/`Inhell` reused, not re-cloned; `rnd(80)` burns before the no-gold early return as in C; `Athome` ≡ `Inhell() && cham == NON_PM` with null-cham read as NON_PM per `makemon.js:1437`; `ACURR(A_CHA)` is `acurr(A_CHA)`; short-circuits preserved — bribe uncalled when Deaf, `rnd(5×CHA)` unrolled only when offer>0, `mon_has_amulet` before Deaf). `js/wizard.js` — new `random_insult`/`random_malediction` const tables verbatim from C (incl. `(sic.)`) + new exported async `cuss(mtmp)` in exact C order (clang L→R: message `rn2` burns before the ROLL_FROM insult; `!rn2(SIZE)` bound is `.length`; `verbalize(fmt, arg)` folded to template strings per the `resurrect` precedent; `EMIN(mtmp)?.renegade` optional-chains the C deref).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. `node scripts/verify.mjs --fn domonnoise` → VERIFY: PASS — `PASS syntax 3 changed js file(s): js/minion.js js/sounds.js js/wizard.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify domonnoise: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — TOP30 depth row, brief confirmed none for domonnoise/demon_talk/cuss — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; plus `node scripts/verify.mjs --fn domonnoise --full` → `PASS full 44/44 passing`.
**Named:** MS_NURSE / MS_GUARD (next Open row); `verbl_msg_mcan` cancelled-speech epilogue + save-rest `oracle_loc` (third Open row); `demon_talk`/`cuss` caller wiring beyond domonnoise (`monmove.c:823/985` stay on their current paths — functions live, unwired there).
**Next:** pop the next Open row (`sounds.c` domonnoise MS_NURSE/GUARD).
## 2026-09-06 — D-1977 sounds.c domonnoise MS_VAMPIRE/DJINNI/ARREST/SOLDIER chat depth

**C locus:** `nethack-c/upstream/src/sounds.c` — `domonnoise` MS_VAMPIRE `:744–821` (night/kindred/nightchild + `rn2(SIZE(vampmsg))` 2-entry table + `body_part`/`pmname`/`an`), MS_DJINNI `:991–1004` (WATER_DEMON/PRISONER gates), MS_ARREST `:1129–1141` (peaceful facts + `arrest_msg[rn2(3)]`), MS_SOLDIER `:1179–1191` (foe/pax `rn2(3)` tables). Callers: `dogmove.c:965`, `sounds.c:527,1302,1408,1456,1526`.
**JS:** `js/sounds.js` (+~135/−~12); 1 js file, under 600 cap. Density note: C is ~90 arm lines across four cases; the JS is the four full C-ordered arms + 9 PM consts + 5 import extends (consecutive Open rows of one C function, every callee live — no stubs in a live arm).
**Change:** `js/sounds.js` — four arms in C switch order (VAMPIRE after SELL, DJINNI after ORC, ARREST+SOLDIER after SEDUCE) with `:line` citations. `midnight` extends the existing `./calendar.js` edge; `pmname`/`an` extend the `do_name`/`objnam` edges; `Upolyd`/`BLOOD`/`FEMALE`/`MALE` extend the `./const.js` edge; `body_part` is a new `./polyself.js` edge (`imports.mjs --can` SAFE — hoisted function, same 89-module SCC). `&mons[]` pointer gates become `mndx` comparisons (JS `mons()` is fresh per call; `youmonst.data` carries `.mndx` via `set_mon_data`).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS. Headless probe of the arms no suite reaches (peaceful/tame/hostile/silver vampire, hostile djinn, peaceful/hostile kop, hostile soldier; seeded RNG + queued keys) → 8/8 ECMD_TIME, no throw (deleted before finishing). `node scripts/verify.mjs --fn domonnoise` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/sounds.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify domonnoise: no corpus session is blocked on it at HEAD` (vacuous; rows cited 0 blocks — TOP30 depth rows, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; plus `node scripts/verify.mjs --fn domonnoise --full` → `PASS full 44/44 passing`.
**Named:** MS_BRIBE+MS_CUSS (`demon_talk`/`cuss` absent — separate Open row); MS_SPELL / MS_NURSE / MS_GUARD; `verbl_msg_mcan` cancelled-speech epilogue; save-rest `oracle_loc`.
**Next:** pop the next Open row (`sounds.c` domonnoise MS_BRIBE/CUSS/SPELL).
## 2026-09-06 — D-1976 display.c curs_on_u/doredraw cursor-on-hero + redraw pair

**C locus:** `nethack-c/upstream/src/display.c` — `curs_on_u` `:1687–1690` (body `:1689` `flush_screen(1)` "Flush waiting glyphs & put cursor on hero"); `doredraw` `:1694–1698` (body `:1696` `docrt()`, `:1697` `return ECMD_OK`); the pair sits immediately before "the main refresh-the-screen routine". Callers: `allmain.c:475,478` (moveloop `bot()`/`timebot()` cursor parks), `eat.c:1222`, `end.c:79,104,743`, `explode.c:401,418`, `hack.c:3007`, `trap.c:3349`; `cmd.c:1819` ext table `C('r')` "redraw screen" IFBURIED|GENERALCMD|CMD_INSANE + `cmd.c:3917` `redraw_cmd` `ef_funct == doredraw` check; `files.c:1768` call commented out.
**JS:** `js/display.js` (+34/−0); 1 js file, under 600 cap. Density note: C is 7 lines; the JS is both one-line bodies in full C order with file-style docs (queue-paired consecutive Open rows of the same C file, every C callee live — no stubs in a live arm).
**Change:** `js/display.js` — new exported async `curs_on_u()` (`await flush_screen(1)`; async for bot/more nhgetch reach, same shape as `redraw_map` D-1974) + new exported async `doredraw()` (`await docrt()`; `return ECMD_OK`) placed in C order immediately before `docrt`, with `:line` citations and the C comment contract. `ECMD_OK` added to the existing `./const.js` import (`--can` ALREADY, no new cycle surface; runtime-only read, no TDZ). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). Export smoke (`typeof` both `function`) + `doredraw→0` (ECMD_OK, docrt early-return with no hero) probe. `node scripts/verify.mjs --fn curs_on_u` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify curs_on_u: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** caller wiring — C call sites stay on their current flush/paint path (`allmain`/`eat`/`end`/`explode`/`hack`/`trap` above; functions live, unwired); `cmd.c:1819` ext-table wiring (JS ext lookup has no redraw row); `cmd.c:3917` check (`js/getpos.js:97` keeps its local key clone).
**Next:** pop the next Open row (`sounds.c` domonnoise MS_VAMPIRE).
## 2026-09-06 — D-1975 display.c reglyph_darkroom darkroom reglyph singleton

**C locus:** `nethack-c/upstream/src/display.c` — `reglyph_darkroom` `:1818–1854` (loop `:1822–1823` `x 1..COLNO-1` / `y 0..ROWNO-1`; `!dark_room: S_corr+waslit→S_litcorr` `:1826–1829`; `dark_room: S_litcorr+!cansee→S_corr` `:1830–1833`; `!dark_room||!use_color||Rogue: S_darkroom→waslit?S_room:NOTHING` `:1836–1840`; `else: S_room+seenv+waslit+!cansee→S_darkroom` `:1842–1844` / `NOTHING+ROOM+seenv+!cansee→S_darkroom` `:1845–1847`; `gs.showsyms[S_darkroom]` equate `:1850–1853`); callers `do.c:1715` goto_level, `options.c:7347` + `:8999` reset_needed_visuals, `restore.c:926`.
**JS:** `js/display.js` (+103/−0); 1 js file, under 600 cap. Density note: C is 36 lines; the JS is the full C-ordered singleton (single C `file.c:function`, every callee live or an established same-file mapping — no stubs in a live arm).
**Change:** `js/display.js` — new exported sync `reglyph_darkroom()` in exact C order with `:line` citations (`dark_room`/`use_color` default-On via `!== false` per `get_bkglyph_and_framecolor`; `Is_rogue_level(game.u?.uz)` hoisted; `| 0` int idiom; `lev->glyph` is `loc.remembered_glyph` via `memory_is_cmap` id-first + tty fallback with `cansee` last in each conjunction; writes via `cmap_idx_to_glyph` tty+id and blank+NOTHING; second block re-reads memory after the first block's store; NOTHING falls back to blank shape only when no integer id is stored). No new import edge (all callees live in-module). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). /tmp probe of the arms no suite reaches (dark S_litcorr→S_corr, S_room+seenv+waslit→S_darkroom, NOTHING+ROOM+seenv→S_darkroom; nodark S_corr+waslit→S_litcorr, S_darkroom+waslit→S_room, S_darkroom+!waslit→NOTHING) → 6/6 PASS (deleted before finishing). `node scripts/verify.mjs --fn reglyph_darkroom` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify reglyph_darkroom: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** `gs.showsyms[S_darkroom]` equate (`:1850–1853`, no showsyms[]/glyphmap[] machinery — D-1972; tty derives from remembered ch/color); caller wiring (`do.c:1715`, `options.c:7347`+`:8999`, `restore.c:926` — function live, unwired).
**Next:** pop the next Open row (`display.c` curs_on_u/doredraw).
## 2026-09-06 — D-1974 display.c redraw_map cliparound pan-resend singleton

**C locus:** `nethack-c/upstream/src/display.c` — `redraw_map` `:1778–1812` (guard `:1792` `!u.ux || suppress_map_output() || !on_level(&u.uz0, &u.uz)` short-circuit; resend loop `:1800–1807` `y 0..ROWNO-1` / `x 1..COLNO-1` with `_glyph_at` `:153` = `gg.gbuf[y][x].glyphinfo.glyph`, `get_bkglyph_and_framecolor` `:1803–1804`, `print_glyph(WIN_MAP, ...)` `:1805–1806` with default `!UNBUFFERED_GLYPHINFO` `Glyphinfo_at` `:1641–1643` (glyph arg ignored — hence `nhUse(glyph)` `:1809–1811`); `flush_screen(cursor_on_u)` `:1808`); callers `docrt_flags :1722–1724` and `wintty.c:3840`; no `UNBUFFERED_GLYPHINFO` in the pinned build flags, so the buffered expansion governs.
**JS:** `js/display.js` (+42/−1); 1 js file, under 600 cap. Density note: C is 34 lines; the JS is the full C-ordered singleton (single C `file.c:function`, every callee live or an established same-file mapping — no stubs in a live arm).
**Change:** `js/display.js` — new exported async `redraw_map(cursor_on_u)` in exact C order with `:line` citations (guard short-circuit; full-cell resend with no UNEXPLORED gate unlike `row_refresh`; `_glyph_at` read as `loc.disp_glyph` per D-1767, hushed via `void` like C's `nhUse`; per-cell `get_bkglyph_and_framecolor` (D-1973) + `_paint_gbuf_cell(x, y, x - 1, y + 1)` for `print_glyph`/`Glyphinfo_at` at WIN_MAP offx 0 / offy 1 per the D-1964 precedent — loop bounds keep every cell in range so never `&no_ginfo`; `await flush_screen(cursor_on_u)`, async for bot/more nhgetch reach, pass-through like C's boolean→int promotion). `on_level` added to the existing `./dungeon.js` import (`--can` ALREADY, no new cycle surface). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). /tmp probe of the arms no suite reaches (all three guard arms return silently; live arm paints with level/display doubles) → guards PASS + live PROBE-PASS painted=3423 (deleted before finishing). `node scripts/verify.mjs --fn redraw_map` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify redraw_map: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** caller wiring — docrt_flags redrawonly stays named on `docrt`; tty cliparound resend has no JS window-port caller yet (function live, unwired); `nul_glyphinfo` initializer has no JS record (overwritten each cell before any read); `gw.wsettings.map_frame_color` store + CLIPPING `clipx`/`clipy` ride with `get_bkglyph_and_framecolor` (D-1973).
**Next:** pop the next Open row (`display.c` reglyph_darkroom).
## 2026-09-06 — D-1973 display.c get_bkglyph_and_framecolor background/frame singleton

**C locus:** `nethack-c/upstream/src/display.c` — `get_bkglyph_and_framecolor` `:2507–2579` (UNEXPLORED default `:2512`, use_background_glyph/seenv/gbuf gate `:2515–2516`, SCORR/STONE-arboreal→ROOM/CORR/ICE/AIR/CLOUD/POOL/MOAT/WATER/LAVA/LAVAWALL switch `:2517–2553` with S_room default, out-of-sight litcorr→corr + DARKROOMSYM darken `:2555–2562`, S_room→no-bg `:2563–2564`, frame arm `:2574–2578`; `#if 0` null guard `:2567–2573` compiled out); `windmain.c:332` use_background_glyph=FALSE except mswin; `getpos.c:93–99` mapxy_valid (FALSE with no callback); `sym.h:96` DARKROOMSYM.
**JS:** `js/display.js` (+116/−9), `js/getpos.js` (+14/−0); 2 js files, under 600 cap. Density note: C is 69 lines; the JS is the full C-ordered singleton + 1:1 getpos helper + caller rewire (one C `file.c:function` + direct callee, every callee live — no stubs in a live arm).
**Change:** `js/display.js` — new exported `get_bkglyph_and_framecolor(x, y)` returning `{ bkglyph, framecolor }` in exact C arm order with `:line` citations (gbuf read is `loc.disp_glyph` per D-1767; arboreal is `game.level.flags.arboreal`; DARKROOMSYM is the local `darkroom_sym()`; dark_room/use_color default-On via `!== false`, lit_corridor/bgcolors/use_background_glyph default-Off via truthy read — matching file conventions; `mapxy_valid` imported); `row_refresh` rewired to the C `:2178–2179` call shape with `framecolor` feeding the repaint gate live. `js/getpos.js` — new exported `mapxy_valid(x, y)` in its 1:1 home (`getpos.c:93–99`, FALSE default kept exactly, unlike `getpos_validate`'s true). Import edges: S_tree/S_darkroom/S_corr/S_litcorr/S_pool/S_ice/S_lava/S_lavawall/S_air/S_cloud/S_water extend the existing `./const.js` edge; `mapxy_valid` is a same-SCC `display↔getpos` pair both already in (getpos imports 12 display bindings) with runtime-only calls — `--can` IN-SCC, no TDZ surface.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). /tmp probe of the arms no suite reaches (default-shut, S_room no-bg, litcorr, litcorr→corr darken, DARKROOMSYM, dark_room-off→stone, arboreal tree, pool, seenv-0, gbuf-U, mapxy_valid default/callback, frame live/no-callback/no-bgcolors) → 16/16 PASS (deleted before finishing). `node scripts/verify.mjs --fn get_bkglyph_and_framecolor` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/display.js js/getpos.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify get_bkglyph_and_framecolor: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** `gw.wsettings.map_frame_color` store + its getpos_sethilite HI_ZAP/NO_COLOR maintenance (`getpos.c:57–58`, HiliteBackground deferred per `js/getpos.js`) — frame arm reads shut until that wiring lands; CLIPPING `clipx`/`clipy` (callers pass map coords); `redraw_map`/`reglyph_darkroom` caller loops are separate Open rows (JS `flush_screen` rebuilds via `_buildScreenOutput`, no per-cell loop to wire).
**Next:** pop the next Open row (`display.c` redraw_map).
## 2026-09-06 — D-1972 display.c map_glyphinfo glyphinfo render singleton

**C locus:** `nethack-c/upstream/src/display.c` — `map_glyphinfo` `:2594–2656` (is_you `:2601–2610`, gmap base copy `:2612`, hero color ladder `:2619–2636`, accessibility hero override `:2637–2644`, MG_HERO `:2645`, pet-NOOVERRIDE `:2647–2652`, ttychar/glyph `:2653–2655`), called by `show_glyph` with mgflags 0 (`:2006`); `display.h` `hero_glyph` macro `:654–656`, MG flags `:990–1000`.
**JS:** `js/display.js` (+110/−2); 1 js file, under 600 cap. Density note: C is 57 arm lines; the JS is the full C-ordered singleton + 3 consts + 12-line caller wire (one C `file.c:function`, every callee live or named below — no stubs in a live arm).
**Change:** `js/display.js` — new `MG_FLAG_NORMAL`/`MG_FLAG_NOOVERRIDE`/`MG_HERO` consts (`display.h :990–996`; MG_HERO is write-only in C — no reader in src/win/include); new exported `map_glyphinfo(x, y, base, mgflags)` in exact C arm order (`u_at && glyph_is_monster` is_you; `!use_color || Upolyd || glyph != hero_glyph` skip with `hero_glyph().glyph` as the integer; RogueIBM currentgraphics/handling/nocolor live-read — shut since JS ROGUESET always sets nocolor = 1; showrace → `HI_DOMESTIC`; accessibility hero-override gate with the ov_* table read shut (deferred) + `MG_HERO`; pet-NOOVERRIDE → `MLET_CH` letter via live `glyph_to_mon`/`mons`); caller passes the resolved base (glyphmap[] deferred — the constructors' {ch,color,dec} carry the tty values); `hero_glyph` exported; `show_glyph_cell` wired at the C `:2006` mgflags-0 shape (strip-first order kept: rogue strip models the reset_glyphmap base, arms run after). `H_IBM`/`HI_DOMESTIC` added to the existing `./const.js` edge (`--can` ALREADY, no new cycle surface). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). /tmp probe of the arms no suite reaches (off-hero passthrough, hero keep-color + MG_HERO, showrace → white, pet-NOOVERRIDE mlet letter) → 4/4 PASS (deleted before finishing). `node scripts/verify.mjs --fn map_glyphinfo` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/display.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify map_glyphinfo: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT Tier C row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `PASS full 44/44 passing (auto: shared file changed)`.
**Named:** glyphmap[] base copy + sym.symidx/tileidx (no showsyms/tile machinery — `reset_glyphmap` still deferred); `go.ov_primary_syms`/`ov_rogue_syms` + numeric SYM_OFF_X (hero-override gate shut); HAS_ROGUE_IBM_GRAPHICS MSDOS/TILES variant (compiled out upstream); `get_bkglyph_and_framecolor` background + `map_frame_color` arms and CLIPPING `clipx`/`clipy` (separate Open rows: `get_bkglyph_and_framecolor`, `redraw_map`).
**Next:** pop the next Open row (`display.c` get_bkglyph_and_framecolor).
