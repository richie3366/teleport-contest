# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
## 2026-09-09 — Audit 820594ec..04fff85d (reviews 1192–1197: 4 ACCEPT, 2 DEBT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 820594ec (D-2226..D-2231), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: m_dowear_type PROGRESS (Samurai-92017 PASS), next_ident PROGRESS (Wizard-92187 PASS), nh_timeout PROGRESS (Rogue-91119 PASS) — zero worse throughout; makeroguerooms/maybe_finished_meal/mondead vacuous-0-confirmed exactly as each D-log labeled (rows cited 0 blocks). C spot-checks held throughout (roguecorr 9 guards vs extralev.c:44–135 all in real branches + splev_initlev sp_lev.c:2981–3018 arm-order exact; m_dowear_type worn.c:798–1002 arm-for-arm incl. another-substitution + 4-way shine; dig.c:1596–1604 + zap.c:3308–3318 unconditional rock after losehp; eat.c:472–493 gate + timeout.c:813–843 arms with itimeout(1) flat coherence; eat.c:3877–3890 + steal.c:367–371 position; lifesaved :2838–2884 + vamprises :2888–2987 + logdeadmon :2995–3068 + m_detach :2734–2803 + shkgone shk.c:234–269 + grddead vault.c:174–189 + monstone :3301–3303 + monkilled :3398). Debt, not C-wrong-blocks: 1193 worn.js:945 vision_recalc(1) has no C counterpart (C defers via vision_full_recalc; RNG-free, display-convergent); 1197 game.disintegested missing xkilled writer/reset (stale-true leaks wording only) + kill_genocided fire-and-forget (safe by run-to-completion, display-order only). Notes, not C-wrongs: D-2226 "new display.js edge" is ALREADY-static per --can; m_useup_mm lacks obfree (pre-existing helper, GC-convergent). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.29/turn` (R² 0.79). No Must-fix prepended. Next cluster unchanged (mondata big_little_match parked stale — full walks live since D-1772). Queue at 10 Open, no refill owed; scoreboard stamp-only (verify runs, no row flips).
## 2026-09-09 — Audit 44843418..820594ec (reviews 1185–1191: 7 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 7 JS-touching SHAs since 44843418 (D-2219..D-2225), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: weldmsg PROGRESS (Priest-92041 PASS, confirmed call-for-call); pleased/spec_applies/flip_worm_segs_vertical/cant_finish_meal/mon_set_minvis/rest_track vacuous-0-confirmed exactly as each D-log labeled (no PASS claimed, rows cited 0 blocks). C spot-checks held throughout (pat_on_head case-5 ladder pray.c:1310–1338 + C source-order cases 1/3/2/4 + 7/8→6 fallthrough noted behavior-neutral; spec_applies DFLAG1 :1024–1025 + defended guard :1036–1037 with defended arm-for-arm mondata.c:89–124; weldmsg body :1060–1074 + call sites :383/:473/:572 with doswapweapon correctly lacking reset_remarm; worm walkers worm.c:967–987 char-for-char + flip_level :661–666 order; cant_finish_meal eat.c:3892–3912 + zap.c:909 site; mon_set_minvis worn.c:473–485 with deleted clones proven divergent subsets; rest_track :92–105 + SFCTOOL ifndef scope verified to wrap only settrack/gettrack). Notes, not C-wrongs: D-2220/D-2221/D-2222 "new edge" framings are ALREADY-static per --can (zero new cycle risk); D-2219 cases-7/8 fallthrough should be named when give_spell ships. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.28/turn` (R² 0.79). No Must-fix prepended (7 ACCEPT). Next cluster unchanged (extralev LVLINIT_ROGUE/ROGUEOPTS); refill exhausted at 6 Open (fresh queue --limit 60 all parked/guard/already-Open; map omits gated at 90%); Score refreshed from this run. Also backfilled D-2190 `74944545` Addressed hash.
## 2026-09-09 — Audit 5cd7c741..44843418 (reviews 1179–1184: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 5cd7c741 (D-2213..D-2218), oldest-first, one file per SHA written as each finished. Corpus claims re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: use_grapple PROGRESS (Priest-92041 146→weldmsg@158); givit PROGRESS (Rogue-91119 70→nh_timeout@108); dosearch 1 PASS (Archeologist-92012) + 2 moved (Samurai-92017 54→m_dowear_type@142, Wizard-92127 31→getpos@72) + 1 parked residual (92023@65 identical toplines) — zero worse; populate_maze/dosounds/findgd/u_entered_shop vacuous-0-confirmed exactly as each D-log labeled (no PASS claimed). C spot-checks held throughout (all 7 whip/grapple surface() sites vs apply.c:3051/:3183/:3788/:3795/:3819/:3855/:3857; givit "%s." format eat.c:1082–1095; m_throw envelope mthrowu.c:702–786 + blindinc tail :836–841 incl. something="something" decl.c:45 and vision_clears; populate_maze loops + fallback :1197–1222 + walkfrom :1306 vs :1293–1294 + mazexy gx/gy globals; throne/beehive/vault Soundeffect vols + temple gate :330–331 + halu_gname pray.c:2577–2619 with randrole(TRUE)≡rn2_on_display_rng(SIZE-1) and roles[NUM_ROLES+1] terminator accounting; findgd vault.c:204–232 line-for-line; shkname shknam.c:873–890 with 11+1 shtypes parity + bill_p consumer shk.c:5001). Notes, not C-wrongs: sym.mjs misses mhitm.js AT_SPIT/AT_WEAP const exports (resolved by direct read :235/:242/:688); D-2215/D-2217 "new edge" framings are ALREADY-static per --can (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.29/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (shk Soundeffect/Hallu remainder shipped D-2218; Open head pray.c gifts); Score refreshed from this run.
## 2026-09-09 — Audit 32f374ab..5cd7c741 (reviews 1176–1178: 3 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 3 JS-touching SHAs since 32f374ab (D-2210..D-2212), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: ustatusline PASS (Archeologist-92004, stronger than the D-log's 141→145 PROGRESS — later commits closed the tail); use_misc PASS (Archeologist-92004, same session); dosearch0 PASS (Knight-92130) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (ustatusline branch-for-branch insight.c:3402–3489 + macro reads youprop.h:81/93/96/138/376–377, monst.h:71, you.h:554; C-locus sticks engrave.js:346 over divergent monmove.js:1629; digests mondata.h:71–72; passive_obj AD_FIRE uhitm.c:6156–6162 gate-for-gate incl. steam-vortex carve-out, weapon≡C-resolved obj per preamble :6135–6143; dosearch0 Norep :2022–2024 + SDOOR :2050 + SCORR :2056/:2060, SDOOR correctly keeps recalc). Notes, not C-wrongs: Strangled/flat-OR broadening is the house dual-store idiom (mirrors display.js); AD_CORR passes raw obj vs AD_FIRE's weapon (equivalent — callers never pass Null); passive_obj carried→update_inventory tail still unported (pre-existing, shared with ACCEPTED AD_CORR arm). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.28/turn` (R² 0.79). No Must-fix prepended (3 ACCEPT). Next cluster unchanged (Open head use_grapple); Score refreshed from this run. Also backfilled D-2212 `5cd7c741` Addressed hash.
## 2026-09-09 — Audit 2b917b52..38bd5d4f (reviews 1170–1175: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 2b917b52 (D-2204..D-2209), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: stairs_description PROGRESS (Healer-92109 208→do_statusline2@241); getpos PASS (Healer-91124); trapeffect_sqky_board PASS (Barbarian-92064); prinv PASS (Caveman-92059); enlightenment PROGRESS (Monk-92213 108→chwepon@109); steal_it PROGRESS (Priest-92097 152→do_statusline2@180) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (wasinside arm mon.c:3632–3640: outside the accessible gate, link-zeroing N/A under GC; getpos exitgetpos :1151–1157 clear gate; sqky both arms trap.c:1402–1476 byte-exact formats + wake_nearby file-local clone; mergable body :4378–4499 holds zero owornmask reads + merged fixup obj-worn-only :877–913; innategend :474–479 + title :396–401, form arm keeps flags.female; steal_it call-for-call :2173–2278 incl. SITM→SEDU fallthrough :4798–4799). Notes, not C-wrongs: `sym.mjs` misses `export {}` lists (could_seduce false "NOT EXPORTED", resolved by direct read of mhitm.js:685–694); D-2206/D-2209 messages call extended imports "new edges" but `--can` reports both ALREADY (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `64+0.40/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (Open head chwepon); Score refreshed from this run.
## 2026-09-09 — Audit ab784680..2b917b52 (reviews 1164–1169: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since ab784680 (D-2198..D-2203), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: legs_in_no_shape PROGRESS (Healer-91123 PASS); float_vs_flight vacuous-0-confirmed as labeled; make_hallucinated PROGRESS (Caveman-92052 PASS); do_statusline2 PROGRESS (2 PASS 92035/92091 + 2 moved, 9 unchanged, 0 worse — byte-identical to the D-log); next_ident PROGRESS (4 PASS + 92187 residual, 0 worse); make_stunned PROGRESS (Archeologist-92226 PASS). C spot-checks held throughout (fpostfx royal-jelly arm-for-arm incl. single-draw rnd(20); mount_steed gate + plural quirk; set_utrap/reset_utrap routing via C trap.c:1044–1057; HALLUC/STUNNED re-arm shapes; Stunned≡HStun youprop.h:81; statusline2 rank table botl.c:781–813 + cond_cmp + windows.c fieldorder read from pinned C; arise arm bones.c:457–478 + tail :505–511 + res_to_mr prop.h:25–27). Notes, not C-wrongs: TIMEOUT-masked wounded gate (house canonical, shared trap.js:2977), debug||wizard idiom, dual botl mirrors, end.js-local give_u_to_m clone (no mondata export exists). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `83+0.59/turn` (R² 0.76). No Must-fix prepended (6 ACCEPT). Next cluster repointed to Open head stairs_description (make_stunned shipped D-2203, PASS at HEAD); Score refreshed from this run.
## 2026-09-09 — Audit 74944545..ab784680 (reviews 1156–1163: 8 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 8 JS-touching SHAs since d22f6c29 (D-2190..D-2197), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: burnarmor/thitm/m_harmless_trap/dosounds vacuous-0-confirmed as labeled; unstuck PROGRESS (Knight-92002 52→one_characteristic@81); accessory_or_armor_on PASS (Tourist-92144); look_here PROGRESS (Wizard-92223 121→130, residual null-owner label as disclosed); minliquid_core PASS (stale row, PASS pre/post as disclosed). C spot-checks held throughout (burnarmor literal, savelife tail, Helmet_on 7 arms + set_bknown/adj_abon, expels tail + um_dist, defended/resists_magm + sqky/rocktrap arms, thitm full body + AD_RBRE=242, dosounds 7 arms draw-for-draw, mdrop_obj/steed/flush). Minor message framings noted (ALREADY-edges called "new", D-2196/D-2194) — substance safe, no C-wrongs. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `92+0.57/turn` (R² 0.77). No Must-fix prepended; Next cluster unchanged (fountain gush/engulfed map-driven).

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-09 — D-2235 `mon.c` newcham wizard arms: dragon-armor ordinary arm + RECORD `tt_doppel` body (queue row `mon.c` newcham wizard arms, no corpus block)

**C locus:** `mon.c:5198–5207` (`case NON_PM`: `which_armor(mon, W_ARM)` + `Is_dragon_scales` → `Dragon_scales_to_pm`, else `Is_dragon_mail` → `Dragon_mail_to_pm`); `mon.c:5209–5211` (wizard `iflags.mon_polycontrol` → `wiz_force_cham_form`, stays named); `topten.c:1355–1375` (`classmon`: `strncmp` filecode ROLESZ → mnum else HUMAN, legacy "E" → Ranger, else impossible + Human mummy); `topten.c:1380–1414` (`get_rnd_toptenentry` RECORD file, stays null in JS); `topten.c:1444–1464` (`tt_doppel`: `rn2(13)` gate, null → `rn1` Archeologist..Wizard, else plgend/classmon/canseemon-christen); `obj.h:347–356` (dragon gear ranges + `Dragon_*_to_pm` = PM_GRAY_DRAGON + otyp − base).
**JS:** 1 file (`js/makemon.js`: +64/−9 — 3 import lines, 17-line dragon consts/predicates, 10-line dragon arm, 17-line `classmon`, 10-line `tt_doppel` body + doc updates), under the 600/10 caps. Density note: the family is three small C arms (~50 lines C); the diff is the complete portable behavior, not padding — wizard `wiz_force_cham_form` (78 lines + `mkclass_poly`/`validvamp`) stays named, not stubbed.
**Change:** `select_newcham_form` gains the `cham === NON_PM` arm in C position (before the wizard gate/random tail): `which_armor(mon, W_ARM)` (already imported from `worn.js`; W_ARM joins the pre-existing `const.js` edge — no new module edge) + local `Is_dragon_scales`/`Is_dragon_mail` (`obj.h` ranges over `objectNames.indexOf` consts, same idiom as `do_wear.js`/`artifact.js`) → `pm('GRAY_DRAGON') + otyp − base` (extractor tables keep gray..yellow contiguous in both tables, probe-verified). New `classmon` (C order incl. mnum-NON_PM→HUMAN arm; async `impossible` called sync-fire-and-forget per `worn.js:417` precedent) via a new `roles.js` edge (`imports.mjs --can makemon.js roles.js classmon` → SAFE, no cycle at all). `tt_doppel` gains the RECORD body (plgend female, `classmon`, `canseemon`→`christen_monst` — both already imported) with `get_rnd_toptenentry` still null (no RECORD VFS under Rule #2, `rnd(10)` footprint kept).
**Verify:** `node scripts/verify.mjs --fn newcham` → PASS syntax (1 changed js file: js/makemon.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. /tmp probe (scales/mail contiguity + color order, dragon-block contiguity, all-10 `Dragon_*_to_pm` landings, 13 roles filecode/mnum live, Wiz→Wizard, Ranger/Human-mummy live): PROBE-PASS, deleted before finishing. Bare-node direct `makemon.js` import fails identically at HEAD (`objnam`/`polyself` TDZ — pre-existing boot-order artifact, not this change). No maintained js/ unit layout exists in-repo (sessions are the suite; no new framework per skill) — disclosure, not a gap.
**Named:** wizard `mon_polycontrol` (`mon.c:5209–5211` interactive `wiz_force_cham_form` — async `getlin` boundary through sync `select_newcham_form`, callees `mkclass_poly`/`validvamp` unported, no setter in scored runs); RECORD `get_rnd_toptenentry` (filesystem record, Rule #2 — same class as the D-2234 SHOPTYPE getenv deferral).
**Next:** do not re-pop the newcham-wizard family (every portable arm live; remaining names are async-boundary/filesystem-banned). A future polymorph-form divergence attributes to `accept_newcham_form`/`validspecmon`/`polyok`, not these arms. Do not invent a FAIL peel.
## 2026-09-09 — D-2234 `shknam.c` nameshk Izchak arm closes the SHOPTYPE/veggy family (queue row `shk.c` SHOPTYPE/veggy, no corpus block)

**C locus:** `shknam.c:495–500` (`nlp == shklight && In_mines(&u.uz) && Is_special(&u.uz)->flags.town` → `shname = "+Izchak"`, `shk->female = FALSE`, skipping the `nseed`/clash game straight to `strncpy`); measured live: `shknam.c:380–405` `veggy_item` (matches `js/shknam.js:307–330` arm-for-arm incl. `ismnum` short-circuit order); deferred: `mkroom.c:102–158` (`mkshop` SHOPTYPE `nh_getenv` zoo/temple/swamp/symb/`g`/`v` arms + `wizard && ep && doorct != 0` eligibility) and `mklev.c:1347` (`wizard && nh_getenv("SHOPTYPE")` → `do_mkroom(SHOPBASE)`).
**JS:** 1 file (`js/shknam.js`: 7-line Izchak arm + 3 one-line updates — header omit, const import, C-range doc), under the 600/10 caps. Density note: 7 new lines on a non-Must-fix port is below the ~40 guideline, but the C arm is 6 lines and the row's other two members are proven no-code (veggy live since D-0994, SHOPTYPE env-banned) — C is that small.
**Change:** `nameshk` gains the C early arm in C position (before the `nseed` computation, which C also skips): `nlpIn === shklight` (reference check — `shkinit` passes `shp.shknms`, and the lighting-store `shtypes` entry holds the same module `shklight` const) `&& In_mines(game.u?.uz) && Is_special(game.u?.uz)?.flags?.town` (both idioms match established use: `In_mines` at `js/const.js:3210`, `sp.flags?.town` at `js/mklev.js:1218,28625`) → `shk.female = 0`, `eshk.shknam = '+Izchak'`, return (mirrors C falling straight to `strncpy`; `'+Izchak'` is 7 chars so the shared `slice(0, 31)` PL_NSIZ store is exact). `In_mines` joins the pre-existing `const.js` edge (no new module edge, no TDZ read); `Is_special` was already imported. Header omit retired to wizard-SHOPTYPE-only with the getenv/Rule #2 reason.
**Verify:** `node scripts/verify.mjs --fn nameshk` → PASS syntax (1 changed js file: js/shknam.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file per runner) · VERIFY: PASS. No hand probe: `nameshk` is module-local (reachable only through `shkinit` level scaffolding — minetown lighting-shop generation, which no public or corpus session performs), and the arm is draw-free; evidence is the C-body match plus the three guard idioms each verified against live in-repo use. No probes committed.
**Named:** wizard SHOPTYPE env arms (`mkshop` symb/`g`/`v`/zoo/temple/swamp overrides, `doorct != 0` wizard eligibility, `mklev.c:1347` forced `SHOPBASE`) — `nh_getenv`, Rule #2, same class as the SPLEVTYPE deferral; `veggy_item` type-only vs obj-path split needs nothing (both live).
**Next:** do not re-pop the SHOPTYPE/veggy family (every portable arm live; remaining names are env-banned). A future minetown lighting-shop divergence attributes to `shkinit`/`stock_room` placement, not naming. Do not invent a FAIL peel.
## 2026-09-09 — D-2233 M_SEEN family: `castmu`/`buzzmu` m_seenres gates + `setnotworn` monstunseesu_prop (queue row `muse.c` M_SEEN gates, no corpus block)

**C locus:** `mcastu.c:174–179` (`mtmp->mcan || mtmp->mspec_used || !ml || m_seenres(mtmp, cvt_adtyp_to_mseenres(mattk->adtyp))` → `cursetxt` + `M_ATTK_MISS`); `mcastu.c:993–999` (`buzzmu`: `BZ_VALID_ADTYP` silent MISS, then `mtmp->mcan || m_seenres(...)` → `cursetxt(mtmp, FALSE)` + `M_ATTK_MISS`); `worn.c:160–171` (`setnotworn`: `p = objects[obj->otyp].oc_oprop`, extrinsic clear, `monstunseesu_prop(p)`, owornmask clear); `vision.h:44–54` (`#if 0` buried vs `#else` live `m_canseeu`); `monst.h:92–94` (`monstseesu_ad`/`monstunseesu_prop` macros).
**JS:** 2 files (mcastu.js +11/−4: 1 import line + 3-line castmu gate + 5-line buzzmu gate + comment updates; do.js +4/−1: 1 import line + 2-line wiring + header), under the 600/10 caps. Density note: C is three one-line gates; the 16-line diff is the complete missing behavior, not padding (cf. D-2230).
**Change:** `js/mcastu.js` — `castmu` condition gains `|| m_seenres(mtmp, cvt_adtyp_to_mseenres(adtyp))` in C order (draw-free disjuncts; AD_SPEL/CLRC map to `M_SEEN_NOTHING` so the new arm is a proven no-op on the spell-selection path and C-correct for exotic AT_MAGC adtyps); `buzzmu` gains the `mcan || m_seenres` → `await cursetxt(mtmp, false)` + `M_ATTK_MISS` gate in C position (module-local `cursetxt` already live; `lined_up rn2(3)+buzz` real zap path stays named); both names join the pre-existing `mondata.js` edge (`imports.mjs --can` → hoisted-function, cycle-safe; `mcastu.js` already imports that edge). `js/do.js` — `setnotworn` calls `monstunseesu_prop(game.objects?.[obj.otyp]?.oc_oprop | 0)` immediately after `confer_oc_oprop(..., false)`, mirroring C adjacency (extrinsic clear → forget → owornmask clear); new `mondata.js` edge likewise hoisted-function cycle-safe (`--can do.js mondata.js monstunseesu_prop` → SAFE); header omit retired. No RNG touched (all three gates draw-free); no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs` on the working tree → PASS syntax (2 changed js files: js/do.js js/mcastu.js) · PASS rule2 · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. `hidden-proxy verify` ×5 (castmu, buzzmu, setnotworn, monstunseesu_prop, m_canseeu) → all vacuous (0 blocked at HEAD; row cited 0 blocks so no --base re-run owed — NOT corpus PASSes, shipped on the public gates). /tmp scratch probe (cvt SPEL/CLRC→NOTHING, MAGM→MAGR, FIRE→FIRE, set/seen/unseen/NOTHING-never, Underwater gate): 8/8, deleted before finishing. No maintained js/ unit layout exists in-repo (sessions are the suite; no new framework per skill) — disclosure, not a gap.
**Named:** `buzzmu` real zap path (`lined_up` + `rn2(3)` + buzz pline/effects; stub still returns MISS past the gate); `m_canseeu` `#if 0` buried arms (dead C — never port); `MUSE_SCR_FIRE` `#if 0` arm (dead C); `setnotworn` `update_inventory` (pre-existing deferral, untouched); `mon_avoiding_this_attack` standalone (only reachable via the `ranged_attk_assessed` callback C itself inlines away; `ranged_attk_available` live in monmove.js).
**Next:** do not re-pop the M_SEEN family (all live C arms now live; remaining names are dead-C or the deferred buzz path). A future first-diff on a monster spell/zap refusal attributes to `spell_would_be_useless`/`choose_monster_spell`/the buzz path, not these gates. Do not invent a FAIL peel.
## 2026-09-09 — D-2232 `trap.c` acid_damage + grease_protect bodies wired into `zap.c` zhitu ZT_ACID arm (queue row `zap.c` zhitu, no corpus block)

**C locus:** `trap.c:4618–4654` `acid_damage(obj)` (Null return; victim = hero when carried else OBJ_MINVENT ocarry else Null; vismon gate; hero inventory_resistance_check(AD_ACID) early return; greased → grease_protect; scroll-not-blank → fade message except blank/mail, then blank to SCR_BLANK_PAPER with spe/dknown cleared; else erode_obj ERODE_CORRODE EF_GREASE|EF_VERBOSE); `trap.c:360–386` `grease_protect(otmp, ostr, victim)` (ostr hero-Your / visible-monster pline arms else ostr-less Yobjnam2 arm for hero/visible victims; rn2(2) wear-off with carried `The grease dissolves.` + update_inventory; boolean return); `zap.c:4528–4546` zhitu ZT_ACID arm (resist/damage + `!rn2(twoweap?3:6)` acid_damage(uwep) + twoweap `!rn2(3)` acid_damage(uswapwep) + `!rn2(6)` erode_armor(youmonst, ERODE_CORRODE)).
**JS:** 2 files (trap.js +82/−2: two functions + 2 import names; zap.js +14/−8: arm wiring + 3 import names + header), under the 600/10 caps.
**Change:** `js/trap.js` — new `export async function grease_protect` (C branch order; Your→`pline('Your …')` house idiom, Monnam/s_suffix/Yobjnam2/vtense arms; sync `update_inventory()` per the water_damage greased-arm precedent; boolean return kept though the caller ignores it, same as C) and new `export async function acid_damage` (C order; `carried_obj` hero test + OBJ_MINVENT `ocarry` monster path; in-file local `canseemon`/`s_suffix`/`is_youmonst` reused, no new clones; MAIL_STRUCTURES SCR_MAIL fade-skip via objectNames guard, eat.js D-1204 idiom; blanking reuses the file's `SCR_BLANK_PAPER` const; async only because pline/erode_obj await). `aobjnam`/`Yobjnam2` join the existing objnam edge, `OBJ_MINVENT` the existing const edge. `js/zap.js` — ZT_ACID arm calls `acid_damage(u.uwep)` / `acid_damage(u.uswapwep)` / `erode_armor(youmonst || {_youmonst:true}, ERODE_CORRODE)` under the exact C rn2 gates (`which_armor` accepts the `_youmonst` sentinel, worn.js:406); `acid_damage` joins the existing trap edge, `erode_armor` (live in mhitm.js) + `ERODE_CORRODE` join existing edges (`imports.mjs --can` ×4 → ALREADY, no new module edge, no top-level TDZ read). zhitu header omission retired.
**Verify:** `node scripts/verify.mjs --fn zhitu` → PASS syntax (2 changed js files: js/trap.js js/zap.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file per runner) · VERIFY: PASS. Headless probe /tmp/probe_acid.mjs (objects_globals_init + initRng seeded; out of tree, kept out of the commit): null guard, floor-weapon corrode (oeroded2 0→1), floor-scroll blank (otyp→SCR_BLANK_PAPER, spe/dknown 0), greased floor weapon (grease wore off via the live rn2(2)), grease_protect ostr arm boolean, hero-carried tool — 6/6, no throw. Full `frozen/ps_test_runner.mjs sessions` after the last js/ edit: 44/44 RNG+screen exact (speed 48+0.30/turn).
**Named:** `inventory_resistance_check(AD_ACID)` hero gate deferred (same class as the AD_FIRE/AD_ACID deferrals in erode_obj — C draws rn2(100) only when an extrinsic acid ward is worn; `u_adtyp_resistance_obj` is an invent.js-local clone with its dwarvish-cloak arm deferred); zhitu ZT_ACID `monstseesu/monstunseesu(M_SEEN_ACID)` still absent (FIRE/COLD arms already name theirs; untouched this iter); ZT_DEATH disintegration-breath arm, poison arm, killer buzzer verb polish unchanged (still named in the zhitu header).
**Next:** do not re-pop zhitu-acid (erosion tail live). A future first-diff on a hero-hit acid ray attributes to the resistance-check ward arm or the M_SEEN_ACID seen-state, not this body. Do not invent a FAIL peel.
