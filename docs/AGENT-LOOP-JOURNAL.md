# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-18 — D-2485 `mon.c` sanity_check_single_mon `has_egd` import miss (review 1438 Must-fix)

**C locus:** `nethack-c/upstream/src/mon.c:72–255` (`sanity_check_single_mon`, staticfn) — the `isgd && !has_egd(mtmp)` guard arm (`impossible("guard without egd (%s)")`).
**JS:** `js/mon.js:25` (import only; body untouched).
**Change:** one word — `has_egd` added to the existing const.js import in `js/mon.js:25` (no new edge: `imports.mjs --can mon.js const.js has_egd` reports mon.js already statically imports const.js; same line shape as the sibling `has_*` guards). No durable unit test: repo has no `tests/` harness (no `tests/` dir, no `js/*.test.*`) — sessions + `verify --fn` are the maintained checks (D-2482 precedent).
**Verify:** `node scripts/verify.mjs --fn sanity_check_single_mon` → PASS (syntax 1 file `js/mon.js`; rule2; hidden note: no corpus session blocked; reach: no RNG tags, smoke 24/24 REACH-OK; green 2/2; strict ×2; cohort 7/7; full skipped — no shared file changed). Operative check per review 1438: `grep -n has_egd js/mon.js js/const.js` → import `:25`, use `:477`, export `const.js:3141` — bound.
**Named:** none new (D-2479 `panic`/`levltyp_to_name`/`#if 0` arms + unwired `mon_sanity_check` callers stand).
**Next:** queue head per breadth phase (first Open — coverage row).
## 2026-09-18 — Audit 1435–1443 (D-2476…D-2484) + cadence: 8 ACCEPT, 1 QUALITY-RISK

1438: `has_egd` used at `js/mon.js:477`, never imported (live `js/const.js:3141`) — latent ReferenceError; Must-fix prepended, Next cluster set. Prior Must-fix closed: 1430 by D-2476 (ENGRAVE-OK re-probed), 1429 by D-2477 (RUST/anti-magic vs C). Every SHA re-measured (`verify --base HASH~1 --reach-all`, 0 regressed); role_init REACH 91/91. Rule #2 clean. Cadence: public 44/44, held-out 11/44 (unchanged), corpus 495/540 (91.7 %, identical). Refill: 22 tool rows skipped as shipped/parked dupes; 7 verbatim appended (corpse_xname/makemaz/see_monsters/flush_screen/setmangry/help_dir/mpickobj) to 12 coverage rows. No `js/` edits, no REJECT.
## 2026-09-18 — D-2484 `zap.c` wishcmdassist whole-body port (makewish help arm wired)

**C locus:** `nethack-c/upstream/src/zap.c:6165–6219` (`wishcmdassist`, staticfn); sole live caller `makewish` `:6348–6351` (`wishcmdassist(MAXWISHTRY - tries)` on `!strcmpi(buf, "help")`, then clear + `goto retry`). `apply.c:635` is a comment-only cardinal-names mention, not a call; `zap.c:42` is the prototype.
**JS:** `js/zap.js` — `wishcmdassist` (new export), `WISHCMDASSIST_INFO`, `plur` (file-local), `makewish` (help arm + iflags predicate).
**Change:** `js/zap.js` only + one import — new `export async function wishcmdassist(triesleft)` in C order: `WISHCMDASSIST_INFO[]` (all 15 `wishinfo` lines verbatim, trailing 0 excluded per `SIZE - 1`), wishless-conduct line while `!(u.uconduct.wishes)`, blank, `retry_info` line (cardinal word for 0–5 else `too many`, ` more` while `triesleft < MAXWISHTRY`, local `plur` sibling-idiom), `retry_too` + blank, suppress-cmdassist line while `iflags.cmdassist`; window via live `show_text_pages` (pager.js — the NHW_TEXT path `help_dir` uses; `imports.mjs --can` hoisted/cycle-safe), with `!game.nhDisplay` early return for C `if (!win) return`. Caller: `makewish` awaits `wishcmdassist(MAXWISHTRY - tries)` before the clear + `continue` (C clear-then-retry), and the prompt suffix now reads `game.iflags?.cmdassist` per C `:6330` (review 784 predicate). RNG 0 — none added.
**Verify:** `node scripts/verify.mjs --fn wishcmdassist` → PASS (syntax 1 file `js/zap.js`; rule2; hidden note: no corpus session blocked; reach: no RNG tags, smoke 24/24 REACH-OK; green 2/2; strict ×2; cohort 7/7; full skipped — no shared file changed).
**Named:** `wish_history_add`/`wish_history_menu` DEBUG menu (pre-existing no-op/deferred, map turns.md); MAXWISHTRY retry loop for unrecognized names (pre-existing single-shot fall-through, unchanged).
**Next:** continue breadth phase at the next Open — coverage row.
## 2026-09-18 — D-2483 `objnam.c` xname_flags whole-body port (export + SINGULAR/override_ID callers wired)

**C locus:** `nethack-c/upstream/src/objnam.c:581–1029` (`xname_flags`); callers `xname` `:575–578`, `cxname_singular` `:1934–1939`.
**JS:** `js/objnam.js` — `xname_flags` (new export), `xname` (wrapper), `cxname_singular` (SINGULAR flag, quan hack deleted), `pretty_base` (towel + coin/chain arms), `TOWEL` const.
**Change:** `js/objnam.js` only, no new imports — new `export function xname_flags(obj, cxn_flags)` holding the former `xname` body in C order (prologue `:632–650`, pname nameit `:674–676`, boulder `:814–823`, pluralize incl. slime ick, gameover `:971–996`, oname `:998–1009`, the-strip `:1011–1012`); `xname(obj)` is now `return xname_flags(obj, CXN_NORMAL)` verbatim per C; override_ID stages known/dknown/bknown + `oc_name_known` and restores in `finally`, while `find_artifact` gates on the pre-override `dknown` per the C comment; wet-towel prefix/suffix per `obj.h:256` + `:705–723` (`wizard` ≡ `flags.debug` idiom); explicit COIN_CLASS/CHAIN_CLASS actualn arm (`:788–791`).
**Verify:** `node scripts/verify.mjs --fn xname_flags` → PASS (syntax 1 file; rule2; hidden: none blocked; reach: no RNG tags, smoke 24/24 REACH-OK; green 2/2; strict ×2; cohort 7/7). Full `sessions`: 44/44 PASS.
**Named:** nextobuf/PREFIX/ConcUpdate/Concat truncation + eos overflow paniclog (by-design JS strings); glorkum default + SLIME_MOLD-bad-fruit `impossible()` (async in JS); `armor_simple_name` for called (still `dn`); xname article arms; `hawaiian_design`/doread.
**Next:** open D-2098 (figurine) / D-1882 (statue) / D-2121 (pname) / D-1935 (find_artifact) name xname_flags arms already live here — closable by their owners, not this port.
## 2026-09-18 — D-2482 `uhitm.c` mhitm_ad_heal whole-body port (mhitm phys arm + mdamagem AD_HEAL dispatch; mhitu arm already live)

**C locus:** `nethack-c/upstream/src/uhitm.c:4296–4385` (`mhitm_ad_heal`); uhitm arm `:4300–4304` (phys + done), mhitu arm `:4305–4378` (D-2059), mhitm arm `:4379–4384` (phys + done); sole C caller `mhitm_adtyping` case `:4790` (`:4782`).
**JS:** `js/mhitm.js` (+~50: 1 const, 1 export, 1 dispatch arm). No durable unit test: repo has no `tests/` harness (sessions + `verify --fn` are the maintained checks, as in D-2049).
**Change:** `js/mhitm.js` in C order, mirroring shipped `mhitm_ad_were` (D-2049, identical 3-arm shape) — `const AD_HEAL = 27` (monattk.h `:69`); exported `mhitm_ad_heal(magr, mattk, mdef, mhm)` (`:1321`: `is_youmonst(mdef)` guard + `mhitm_ad_phys` delegate, done via mhm); `mdamagem` AD_HEAL arm (`:4166`, C `:4790` analogue: mhm with `dieroll` for `artifact_hit` like WERE, knockback preempt, done check, HP/monkilled/grow_up tail). No new imports (`is_youmonst`, `mhitm_ad_phys`, knockback tail all file-local). No `damageum_adtyping` row — hero-polymorphed-into-nurse attacks share the phys shape per the were convention (named here, not a second dispatch).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_heal` → syntax 1 file · rule2 · hidden note (no corpus session blocked — coverage row) · reach smoke 24/24 PASS → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS.
**Named:** uhitm arm (`:4300–4304`) has no `damageum_adtyping` row (hero-as-nurse-attacker keeps default leftover; were-precedent, vanishes with hero poly rarity); `is_youmonst(mdef)` guard mirrors `mhitm_ad_were` (mhitu routing lives in `mhitm_adtyping_u`).
**Next:** queue head per breadth phase (next Open coverage row).
## 2026-09-18 — D-2481 `role.c` role_init whole-body port (selection + quest-pm + pantheon arms; split u_init helpers merged)

**C locus:** `nethack-c/upstream/src/role.c:1980–2117` (`role_init`); callees `plnamesuffix` `:1664–1721`, `randrole_filtered` `:730–744` (staticfn), `randrole` `:718–728`, `randrace` `:786–810`, `randalign` `:915–940`, `validrole` `:712–716`, `str2role` `:746–775`, `align_gtitle` (pray.c `:2627–2649`); helpers `findword`/`strNsubst` (hacklib.c `:600–621`/`:555–597`).
**JS:** `js/roles.js` (+~290: 5 exports + module-local filtered + role_init); `js/hacklib.js` (+~80: 2 exports); `js/u_init.js` (−~115 helpers/+10 preserve, import trim); `js/allmain.js` (+9 wire); `scripts/role_init.test.mjs` (6 tests).
**Change:** `js/roles.js` (C home) in C order — `randrole` (`rn2(roles.length)` ≡ `rn2(SIZE-1)`; display arm via live `rn2_on_display_rng`), module-local `randrole_filtered` (live `ok_*` filter chain, `randrole(FALSE)` fallback), `randrace` (x100 pick with C integer-division `Math.trunc`, `rn2(races.length)` fallback), `randalign` (plain `rn2(n)`, `rn2(ROLE_ALIGNS)` fallback), `plnamesuffix` (genericusers/`findword` arm, `askname` arm via live `tty_askname` — async only per Constitution §2 — dash-token `str2*` parse with `*eptr=0` truncation, comma→space via `strNsubst`; `game.plname`/`game.plnamelen` are the C buffers), and exported async `role_init` (whole body: plnamesuffix → validrole/str2role(pl_character)/randrole_filtered → pl_character copy-back with `[PL_CSIZ-1]` truncation → validrace/randrace → pantheon-gated gender flip + initgend fill → validalign/randalign → urole/urace table copy → leader/guardian/nemesis fixups with ldrgend/nemgend in-block (C `:2036–2041`/`:2055–2060`) via the D-1094 `commit_pm_fixup` overlay → pantheon select + god fill → godgend via live `align_gtitle` → `Role_if(PM_CLERIC)` SPE_LIGHT skill; `#if 0` infravision stays a comment like C). `js/hacklib.js` (C home): `findword` + `strNsubst` exports, exact C loop/short-circuit semantics. `js/u_init.js`: the five split helpers deleted (not stubbed); `setup_role_race_from_rc` keeps object shaping + flag sync and preserves role_init's Priest god fill across its rebuild (shaped `lgod ?? null` would otherwise re-null it).
**Verify:** `node scripts/verify.mjs --fn role_init --reach-all` → syntax 4 files · rule2 · hidden note (no corpus session blocked — coverage row) · REACH 91/91 PASS → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 → VERIFY: PASS. Focused `node --test scripts/role_init.test.mjs` 6/6 (findword/strNsubst C cases, plnamesuffix token split + zero draws, rand validity + single-draw tags, specified-flags role_init no-op + pl_character/godgend/fixups).
**Named:** restore-path re-derivation (above); `iflags.defer_plname`/`sysopt.genericusers` arms ported but unset in JS (unixmain-owned); `options.c:3609` pl_character writer (OPTIONS name back-compat; JS sets pl_character only via role_init copy-back).
**Next:** queue head per breadth phase (first Open coverage row: `uhitm.c` mhitm_ad_heal MISSING).
## 2026-09-18 — D-2480 `cmd.c` randomkey whole-body port (+ pgetchar/random_response callers, cmd_from_dir export)

**C locus:** `nethack-c/upstream/src/cmd.c:3517–3578` (`randomkey`); callers `pgetchar` `:445–453`, `random_response` `:3580–3597`; callee `cmd_from_dir` `:3029–3032` via `cmd_from_func` `:3036–3064` over `move_funcs` `:2070–2083`; `global.h:487` (`C(c)`); `hack.h` movemodes.
**JS:** `js/cmd.js` (+3 exports, +4 const/rng import names on existing edges); `js/dokeylist.js` (+`cmd_from_dir` export, hoisted tables, +3 const names on existing edge); `scripts/randomkey.test.mjs` (6 tests).
**Change:** `js/cmd.js` in C order — `pgetchar` (fuzzer arm returns `randomkey()`, else `await nhgetch()`; async only per Constitution §2), `randomkey` (full body: ^A/^P repeat gate on `game.program_state.input_state === commandInp`, `rn2(16)` switch default-ESC through case 14, `last_c` latch; statics as module lets + `reset_randomkey` test support), `random_response` (accumulate to `\n`, ESC discards, ≤ sz-1 chars; returns the JS string for C buf+NUL). JS adaptations: key codes as numbers (nhgetch convention); `C('a')/C('p')` as 1/16; case 8 cycles `i++ % (EXTCMDLIST.length + 1)` with index == length yielding key 0 (C SIZE counts the donull sentinel `cmd.c:2068`, generated list omits it); case 10–12 draws `rn2(N_DIRS)` then `rn2(7) ? MV_WALK : (!rn2(3) ?
**Verify:** `node scripts/verify.mjs --fn randomkey --full` → syntax 2 files · rule2 · hidden note (no corpus session blocked — coverage row) · REACH smoke spread 24/24 PASS → REACH-OK · green 2/2 · strict 2/2 · cohort 7/7 · full 44/44 → VERIFY: PASS. Focused `node --test scripts/randomkey.test.mjs` 6/6 (seed-pinned C-order stream, ^A/^P gate, cmd_from_dir modes, response bound, pgetchar arms). Probe scratch in /tmp only.
**Named:** `readchar_core` fuzzer arm (`cmd.c:5218`); `wintty.c:4068` tty fuzzer arm.
**Next:** queue head moves to `role.c` role_init (split? — brief first).
## 2026-09-18 — D-2479 `mon.c` sanity_check_single_mon whole-body port (+ static pet_sanity_check)

**C locus:** `nethack-c/upstream/src/mon.c:72–255` (`sanity_check_single_mon`); static `pet_sanity_check` `:56–70`; `alloc.c:125` (`fmt_ptr`); `youprop.h:359` (`Protection_from_shape_changers` macro).
**JS:** `js/mon.js` (new `pet_sanity_check` + `sanity_check_single_mon`, module-local; +10 import names, all live exports, +1 new apply.js edge; `SADDLE` const); `js/mondata.js` (`export` on existing `monsndx`, one word).
**Change:** both functions added module-local in `js/mon.js` (C home, matching C `staticfn`) in C order — data-pointer range, mnum fixup, HP bounds (gremlin `m_lev` arm kept commented-out like C), dead-monster early return, genocided/tame arms, shk/priest/guard/minion extras, pet→`pet_sanity_check` (droptime vs `game.moves`), steed saddle arms, trapped/frozen arms, full hiding arm (eel/ceiling/non-pit), full mimic arm (`migr` vs Protection arms, non-mimic pose arm), leash arms. JS adaptations: range arm keys on numeric `data.mndx` (`< LOW_PM`, `> NUMMONS-1` for `&mons[HIGH_PM]`, HIGH_PM=NUMMONS-1 per dogmove.js:1414) with `0x` hex where C prints `fmt_ptr` (D-2375 precedent); `DEADMONSTER` is `(mhp|0)<1` (do.js idiom); Protection is `uprops[PROT_FROM_SHAPE_CHANGERS]` intrinsic||extrinsic (C macro verbatim); `%u`/`%ld` print as `%d`; `levl[mx][my].typ` is `game.level?.at(mx,my)?.typ` (ball.js idiom); illegal-data `panic` is a loud throw (lev_json precedent); `impossible` awaited. All callees live: `monsndx` (export added, one word), `pmname`/`Mgender`/`mon_pmname`, `m_carrying` (local), `which_armor`, `t_at`, `m_at` (local), `isok`, `hides_under`, `OBJ_AT`, `is_pool`, `Is_waterlevel`, `ceiling_hider`/`has_ceiling` (locals), `accessible`, `is_pit`, `get_mleash` (apply.js edge, `--can`: hoisted, cycle-safe), `has_eshk/epri/egd/emin/edog`+`EDOG`, `M_AP_TYPE`, `G_GENOD`, `POOL/MOAT/WATER/LAVAPOOL`, `SADDLE` const (4-file idiom).
**Verify:** `node scripts/verify.mjs --fn sanity_check_single_mon --full` → syntax 2 files · rule2 · hidden note (no corpus session blocked — coverage row, not corpus) · REACH smoke spread 24/24 PASS → REACH-OK · green 2/2 · strict 2/2 · cohort 7/7 · full 44/44 → VERIFY: PASS.
**Named:** `panic` (own unported row; throw used); `levltyp_to_name` + all three `#if 0` arms (dead-mon fmon/guard `:107–112`, mimic inaccessible-location `:222–236`, leash `distu>90` `:306–313`) — omitted exactly like C; C callers `mon_sanity_check` fmon/migr sites (unported owner, wire later).
**Next:** `mon_sanity_check` (`mon.c:258–324`) is the natural next row in this file (wires both call sites + `sanity_check_worm`/`wormno_sanity_check`/`mon_offmap` neighbors); `cmd.c` randomkey is queue head.
## 2026-09-18 — D-2478 `mail.c` newmail whole-body port (md_start/md_stop/md_rush + daemon delivery)

**C locus:** `nethack-c/upstream/src/mail.c:399–456` (`newmail`); static `md_start` `:148–239`, `md_stop` `:247–274`, `md_rush` `:287–394`; `mail_text`/`md_exclamations` `:277–279`; `mail.h` `MSG_*`/`mail_info`.
**JS:** `js/mail.js` (+340/−10: readmail kept, whole delivery family added), `js/worm.js` (+1/−1 export keyword).
**Change:** `js/mail.js` in C order — file-local C-macro equivalents (`Deaf`/`Blind`/`Blind_telepat`/`distu`, sibling-idiom verbatim; `mail_text` + `md_exclamations` via `rn2(3)`); `md_start` (Blind-nearby arm, stairwell-in-sight scan over `game.stairs`/`tolev.dnum` + `couldsee`, farthest-edge `viz_rmin/rmax` loop with lax `retry` as `for(;;)/continue`, `enexto`/`cansee`/`couldsee` short-circuits verbatim); `md_stop` (3×3 `isok`/`u_at`/`accessible`/`m_at` scan, closest-to-start with `rn2(2)` ties, `enexto` fallback with `mons(PM_MAIL_DAEMON))`); `md_rush` (off-map greedy descent on `dist2` skipping only `!isok`/`IS_STWALL`, displacement shouts + `"Excuse me."` through the hero, per-step `place`/`newsym`/`flush_screen`/`nh_delay_output` with monster restore, crowded-destination refusal, worm-seg restore via the `mx/my` test); exported async `newmail` (`md_start && md_stop` short-circuit gates `makemon`, message block only on inward-rush success, `Hello`/`plname` greeting or Deaf `Message:` pline, `SCR_MAIL` scroll with `object_nam`/`response_cmd`, `"Catch!"` when `distu > 2` (C `!m_next2u`) with C's empty Deaf else, `display_nhwindow` as `flush_topl_more`, `hold_another_object` + `void obj` for `nhUse`, zip-back with `mx=my=0` fallback + `mongone`, `MSG_OTHER` `"Hark!"` fallback when nothing seen). `js/worm.js`: one-word `export` on `place_worm_seg` (zero behavior change) so the `md_rush` restore arms import the live export instead of cloning it. Async propagates only via `verbalize`/`pline`/`flush_screen`/`nh_delay_output` (nhgetch-reaching); no physics deferred or reordered.
**Verify:** `node scripts/verify.mjs --fn newmail` → PASS syntax (2 files) · PASS rule2 · note hidden (no corpus session blocked at baseline) · REACH-OK (no RNG-tagged reach; smoke 24/24 PASS, 0 regressed) · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. No maintained unit harness exists (`tests/` absent); `verify --fn` + syntax + REACH smoke is the honest verification.
**Named:** `ckmailstatus` mustgetmail/stat/broadcast variants (mailbox `stat()` is Rule #2 filesystem; no broadcast queue in scored JS); `m_next2u`/`MON_AT`/`distu` read as `distu > 2`/`m_at`/local (C-macro, mklev :18897 precedent); `display_nhwindow(WIN_MESSAGE, FALSE)` as `flush_topl_more` (readmail precedent); viz rows missing pre-recalc treated as no-position (C arrays always set; falls into C's found-nothing FALSE path).
**Next:** queue head per breadth phase.
## 2026-09-18 — D-2477 nobj-chain walk over array-model `game.invent` (D-2470 follow-up: `trapeffect_anti_magic` hero carried-scan + `immune_to_trap` RUST hero walk)

**C locus:** `nethack-c/upstream/src/trap.c:2360–2365` (`for (otmp = gi.invent; …)` carried `defends_when_carried(AD_MAGM)` + quest exclusion, hero arm) and `:2878–2892` (`immune_to_trap` RUST_TRAP: `is_you ? gi.invent : mon->minvent` rustprone+`owornmask` walk with `uquiver`/`uswapwep && !u.twoweap` skip). Monster `minvent` arm (`:2419`-region, `mtmp->minvent`) is a genuine nobj chain — untouched.
**JS:** `js/trap.js` (+17/−11 across the two sites).
**Change:** `js/trap.js` only, no new imports — anti-magic site walks `for (const _am of game.invent || [])` with the same predicate (`oartifact` + `!is_quest_artifact` + `defends_when_carried(AD_MAGM)`, live exports) and break-on-first-match into `otmp` (keeps the `if (otmp) dmgval2 += rnd(4)` shape); RUST site splits hero (`for…of` array, same `is_rustprone` + `owornmask` predicate and quiver/swapwep skip, `return TRAP_NOT_IMMUNE` on hit) from monster (`mon.minvent` nobj walk, skip inapplicable), shared `return TRAP_CLEARLY_IMMUNE` tail preserved. Short-circuit, RNG order, and integer semantics unchanged.
**Verify:** `/tmp/antimagic-invent-probe.mjs` 7/7: RUST hero worn-iron → NOT_IMMUNE, empty → CLEARLY_IMMUNE, quiver-only → CLEARLY_IMMUNE (skip), monster chain worn → NOT_IMMUNE / empty → CLEARLY_IMMUNE; `dotrap(FORCETRAP)` on ANTI_MAGIC with `HAntimagic` hero, no throw both runs, carried MR-artifact run draws exactly one extra `rnd(4)=2 @ trapeffect_anti_magic(trap.js:5186)` (3→4 draws, all else identical). `node scripts/verify.mjs --fn trapeffect_anti_magic` → PASS syntax (1 file) · PASS rule2 · note hidden (0 blocked) · REACH-OK (smoke 24/24) · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. `node scripts/verify.mjs --fn immune_to_trap` → identical PASS tail → VERIFY: PASS.
**Named:** none new (D-2470's "none" stands; FIRE_TRAP invent-burn walk stays the map-named `immune_to_trap` omission per the function docstring).
**Next:** queue head after this ships per breadth phase (first Open coverage row: `mail.c` newmail MISSING).
## 2026-09-18 — D-2476 `vpline` re-scan of verbatim-text call sites (D-2471 follow-up: engrave/rumors/pager `%s` routing + zap-`You` dupe merge)

**C locus:** `engrave.c:396` (`You("%s: \"%s\"%s",…)`); `rumors.c:573` (`pline1(line)`); `pager.c:1922` (putmixed literal) + `:2700–2708` (`"%s"`, `"%s,"`, `"%8.8s%s"`); `pline.c:365–374` (`You` prefix-on-format + same va_list) for the merge; zap.c `You("release %s."/:598, "are released %s."/:607, "disrupt %s!"/:227, "aren't sure whether %s has %s…"/:2243, "find %s%c"/:3790, "probe towards the %s."/:3241, "probe beneath %s."/:3257) + `Your("%s tingles."/:4986)`; mon.c:5429 (`You("%s %s%s!"…)`); polyself.c (`:1413, :1508, :1513, :1578, :1689, :1717, :1739, :1749`); vault.c:877 (`You(see/confronted "%s."…)`).
**JS:** 11 files, +86/−59 — `js/engrave.js` (+You import, read-back format) · `js/rumors.js` (`%s` arm) · `js/pager.js` (outH + trio) · `js/zap.js` (import You/Your, delete 2 clones, 8 C-format restores) · `js/cmd.js`/`js/hack.js`/`js/makemon.js`/`js/mklev.js`/`js/polyself.js`/`js/pray.js`/`js/vault.js` (re-point + 10 C-format restores).
**Change:** route each through the `%s` arm (verbalize/impossible D-2471 precedent — substituted args are never re-scanned by `vpline_expand`): engrave keeps the C format+args shape `You('%s: "%s"%s', feel/read, et, endpunct)`; rumors `pline('%s', line)`; pager `pline('%s', outH.s)` + `pline('%s', reslt)` / `pline('%s,', slice)` / `pline('%s%s', 8-slice, second-line)` (keeps the slice emulation of the NUL-cut and `%8.8s` key field); zap dupe deleted, `You`/`Your` imported from `display.js` (no new edge — zap already imports display; display imports no zap), all 7 importers re-pointed (each already imported display); zap-internal + importer interpolated sites restored to their exact C format+args (`release/are released/disrupt/%s has %s/find %s%c/probe ×2/tingles/steed return/gaze ×4/web ×3/vault guard/makemon emerge`). Literal-only callers re-pointed unchanged (net-identical).
**Verify:** `/tmp/vpline-probe.mjs`: `You('%s: "%s"%s','read','%s %d %% 50% of treasure','.')` → exactly `You read: "%s %d %% 50% of treasure".` (ENGRAVE-OK; rumor text likewise verbatim). `node scripts/verify.mjs --fn vpline` → PASS syntax (11 files) · PASS rule2 · note hidden (0 blocked) · REACH-OK (no RNG-tagged reach; smoke 24/24) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared files changed) → VERIFY: PASS. `hidden-proxy score --jobs 8`: 495/540 PASS (91.7%), RNG 99.29%, screens 99.0% — identical to the pre-change baseline, 0 regressed.
**Named:** the remaining ~100 single-arg `pline(variable)` sites (apply/artifact/bones/detect/dig/do/eat/hack/pickup/region/…) — each needs its own C check (constant vs `pline1`/verbatim) and ships as its own row when `port-coverage` or a review names it; `You("")` early-return vs C's printed `"You "` (noted in review 1430, unreachable in practice); zap `You_hear` local (different C function, untouched); pager `getversionstring`/`reslt`-adjacent literals (constant text, no `%` source).
**Next:** queue head after this ships per breadth phase (second Must-fix `nobj-chain` untouched — Must-fix stays one item, alone).
## 2026-09-18 — Audit 8299d47b..f8881130 (reviews 1426–1434: 6 ACCEPT, 1 ACCEPT-WITH-DEBT, 2 QUALITY-RISK) + cadence 44/44, proxy 495/540, held-out 11/44

Review-only iteration, no `js/` edits. 1430 QUALITY-RISK: D-2471 `vpline` re-scans preformatted single-arg pline text — engraving `%s` prints empty vs C `engrave.c:396` verbatim `%s` arg (demonstrated with the shipped regex); same family: rumors `pline(line)` (C `pline1`), `pline(outH.s)`, zap-`You` + duplicate display-`You`. 1429 QUALITY-RISK (caught on re-inspection after filing ACCEPT): D-2470 hero carried-artifact scan walks `game.invent` as an nobj chain — dead on the array model (`invent.js:369`), conditional `rnd(4)` never fires. Both prepended under LOOP-QUEUE Must-fix (vpline first = Next cluster). 1433 WITH-DEBT: `ghod_hitsu` writes dead `game.buzzer`, live channel is `game._buzzer` — inert today, map-noted. Cadence: full `sessions` 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, `47+0.30/turn`); `hidden-proxy score` 495/540 (91.7 %, RNG 99.29 %, screens 99.0 %, identical); held-out steady 11/44. Rule #2 clean. Refilled Open — coverage to 12 (skipped 6 parked-STALE + 2 just-verified tool rows by name, documented in commit).
## 2026-09-18 — D-2475 `topten.c` prscore whole-body port (score-subset display + score_wanted filter, all 7 message arms)

**C locus:** `nethack-c/upstream/src/topten.c:1194–1353` (`prscore`); static `score_wanted` `:1112–1192`; callees `fopen_datafile`, `dlb_init`, `init_dungeons` (dungeon.c:1205, live), `readentry`/`newttentry`, `free_dungeons`, `dlb_cleanup`, `outheader`/`outentry`, `free_ttlist`, `raw_print`/`raw_printf`, `getuid`, `str2role`/`str2race`, `gh.hname`; caller `scores_only` (earlyarg.c:406, via `:309`).
**JS:** `js/topten.js` (+239/−3: score_wanted + ch + getuid + prscore + 2 import names + docstring retire).
**Change:** `js/topten.js` only, in C order — new module-local `score_wanted` (version gate; `pers_is_uid` uid arm; `-uname` strip; `-p/-r/-u` + next-arg arms with `i++` consume; `all`/name-prefix/`-<roleletter>`/maxrank; `ch()` helper mirrors C NUL-terminated OOB reads incl. the `strchr("pru",'\0')` match; `strncmp`→`slice(0,NAMSZ)`; `digit`/`atoi`→range test + `parseInt`); module-local `getuid()` returning 0 (POSIX uid has no browser counterpart; `topten()` records uid 0 for every entry, so 0 matches the player's own entries); new `export async function prscore(argc, argv)` in C order (bad-arguments `raw_printf`; missing VFS record = failed `fopen_datafile`; AMIGA omit; `wiz1_level.dlevel==0` → dynamic-import live `init_dungeons` (no new static cycle, D-2463 precedent), `dlb_init` named; `-s`/`--scores`/`-s<anything>` split and `-v` arms via local slice-copies of argv; `pers_is_uid` getuid selector vs plname/`all`/explicit list; `raw_print("")`; record scan via live `read_record_entries` with exact `points==0` break + per-entry rank; `fclose`/`free_dungeons`/`dlb_cleanup` named; match → live `outheader`/`outentry(rank, t1, false, emit)`; else the cannot-find `pbuf` arm with exact BUFSZ overflow branches, `-u` skip/`continue`, `-p`/`-r` space-vs-colon separators, trailing `.`, Usage + player-types lines; `free_ttlist`/AMIGA tail named; every line through `render_topten_lines`, the file's `!toptenwin` panel idiom). New imports `BUFSZ` (const) + `str2role`/`str2race` (roles, pre-existing edge); `topten()` docstring retires the `prscore` omission.
**Verify:** `node scripts/verify.mjs --fn prscore` → PASS syntax (1 file) · PASS rule2 · note hidden (0 blocked) · REACH-OK (no RNG-tagged reach; smoke 24/24, 0 regressed) · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. Scratch probe `/tmp/probe-prscore.mjs`: 25/25 arm checks (bad-args ×2, missing/empty record, uid-default, `all`, `-v` incl. old-version entry, name/`-u`/`-uName`, `-p`/`-r`, role-letter, maxrank, `-s` concat, `--scores`, cannot-find separators ×3, `init_dungeons` arm no-throw with seeded RNG).
**Named:** `fopen_datafile` (VFS read is the Rule #2 analogue); `readentry`/`newttentry` allocation mechanics (live `read_record_entries`/`readentry_line` reused — no second clone); `dlb_init`/`free_dungeons`/`dlb_cleanup` (no DLB/free layer; table persists in game state); `free_ttlist` (GC); `raw_print`/`raw_printf` + `topten_print`/`topten_print_bold` routing (no scored surface per D-2471; panel lines instead); AMIGA scaffolding + tail (platform); `gh.hname` literal `'nethack'` (argv[0]-derived, no argv in JS); `scores_only` caller chain (no JS CLI layer).
**Next:** queue head is now `mail.c` newmail (MISSING).
