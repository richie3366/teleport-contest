# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-19 — D-2511 `uhitm.c` mhitm_ad_heal uhitm arm + mhitm_ad_acid uhitm/mhitm arms (coverage THIN/MISSING → live)

**C locus:** `nethack-c/upstream/src/uhitm.c:4296–4385` (`mhitm_ad_heal`: uhitm `:4300–4304` phys+done, mhitu `:4305–4378` nurse envelope, mhitm `:4379–4384` phys+done) + `uhitm.c:2742–2786` (`mhitm_ad_acid`: uhitm `:2747–2751` resists/defended zero, mhitu `:2752–2767`, mhitm `:2769–2786` mcan-return + harmless/burns + erode/acid dice). Sole C caller each is the `mhitm_adtyping` switch (`:4790` AD_HEAL, `:4795` AD_ACID); C `damageum :4854` routes the hero attacker through that same switch, C `hitmu` (`mhitu.c:1191`) routes mdef==you through it. Callees: `mhitm_ad_phys`, `resists_acid`, `defended`, `hitmsg`, `rn2`, `pline`/`pline_mon`, `hliquid`, `Monnam`/`mon_nam`, `erode_armor`, `acid_damage`, `Monnam`, `mhitm_knockback`, `mon_offmap`, `grow_up`, `mdamagem_monkilled` — all live, no new edges.
**JS:** `js/uhitm.js` + `js/mhitm.js`; 2 files, +80/−4 working-tree diff, under caps.
**Change:** `js/uhitm.js` — `AD_HEAL = 27` const (monattk.h:69) + `mhitm_ad_heal` word on the live mhitm edge + `defended` word on the live mondata edge (`imports.mjs --can` ALREADY both) + two `damageum_adtyping` rows in C order: AD_HEAL delegates `mhitm_ad_heal(game.youmonst, mattk, mdef, mhm)` (C `:4300–4304`; the `is_youmonst(mdef)` guard is false here, phys runs, done via mhm like C `damageum :4856–4858`), AD_ACID zeroes leftover on `resists_acid(mdef) || defended(mdef, AD_ACID)` else keeps it (C `:2747–2751`, no mcan gate in C). `js/mhitm.js` — new exported `mhitm_ad_acid` (`:1336`, C `:2769–2786` in order: mcan → damage 0 + return; resists/defended → vis-gated harmless pline + zero; else vis-gated `pline_mon` + `pline` burns with leftover kept; unconditional `!rn2(30)` erode + `!rn2(6)` acid dice; `_mm_vis` = C `gv.vis`, `acid_damage` null-safe) + `acid_damage` word on the live trap edge + `mdamagem` AD_ACID dispatch (`:4252`, sibling knockback/done/HP tail). `defended` is called live in both acid arms (colder siblings omit it; here the mondata.js export is the C-exact `:89–124` port, so C order wins).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_heal` → PASS syntax (2 files) · rule2 · hidden note (0 blocked) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. `node scripts/verify.mjs --fn mhitm_ad_acid` → identical PASS tail → VERIFY: PASS.
**Named:** none in these two bodies — every arm of both functions is now live. D-2247 stays open for the dren/conf uhitm+mhitm arms.
**Next:** Open — coverage head after mhitm_ad_acid (`rumors.c` getrumor).
## 2026-09-19 — D-2510 `hack.c` domove_swap_with_pet whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/hack.c:2098–2225` (`domove_swap_with_pet`, staticfn); sole C caller `:2922` (`domove`). Callees: `sobj_at`, `M_AP_TYPE`, `seemimic`, `t_at`, `is_pit`, `NODIAG`, `You`, `YMonnam`, `verysmall`, `curr_mon_load`, `bad_rock`, `bigmonst`, `trapname`, `feeltrap`, `just_an`, `handle_tip`, `goodpos`, `mundisplaceable`, `remove_monster`, `place_monster`, `newsym`, `x_monnam`, `has_mgivenname`, `type_is_pname`, `minliquid`, `mintrap`, `abuse_dog`, `adjalign`, `livelog_printf`, `experience`, `more_experienced`, `newexplevel`, `rn2`, `You_feel`, `impossible`; data `svm.mvitals[mndx].died`, `u.uconduct.killer`, `u.ugangr`.
**JS:** `js/hack.js` (restart + import words) + `js/monmove.js` (1 export word); 2 files, +112/−96 working-tree diff incl. docs, under caps.
**Change:** `js/hack.js` — restarted `domove_swap_with_pet` in C order with `:line` cites: `:2101–2105` guard + boulder capture; `:2107–2114` park/seemimic via live `M_AP_TYPE(mtmp)`; `:2116–2118` trap lookup + `mtrapped` clear; `:2120–2124` pit+boulder; `:2125–2128` NODIAG diagonal (`swap_nodiag` kept — C macro, no live export); `:2129–2136` boulder fit via exported `curr_mon_load`; `:2137–2143` squeeze; `:2144–2160` peaceful-trapped (`trapname`/`feeltrap`/`just_an` live, `handle_tip` same-file); `:2161–2169` peaceful-refusal with the `!goodpos(u.ux0,u.uy0,mtmp,0)` conjunct restored + live `mundisplaceable`; `:2171–2185` swap via `remove_monster(x,y)` + `place_monster(mtmp,ux0,uy0)` + dual `newsym` + `You("swap places with"/"frighten" + x_monnam)`; `:2187–2223` aftermath switch (`minliquid ? Trap_Killed_Mon : mintrap(NO_TRAP_FLAGS)`; Caught/Moved → `abuse_dog` + `adjalign(-3)`; Killed → killer-conduct `livelog_printf(LL_CONDUCT)` in the `js/uhitm.js:682` shape, `monsndx` + `mvitals.died` + `experience`/`more_experienced`/`newexplevel`, `rn2(4)` guilt `You_feel` + `ugangr++` + `adjalign(-15)`; default `impossible`). Deleted all four clones and rebound to live exports; `curr_mon_load` is now `export`ed from `js/monmove.js` (C `mon.c` has one function — the monmove local was clone drift).
**Verify:** `node scripts/verify.mjs --fn domove_swap_with_pet` → PASS syntax (2 files: js/hack.js js/monmove.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach; RNG 1) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed) → VERIFY: PASS.
**Named:** C `:2147` `assert(trap != NULL)` — implied by `mtrapped` (cleared `:2116–2118` when `!trap`); no JS assert export, comment only. Every other C arm is live.
**Next:** Open — coverage head after domove_swap_with_pet (`teleport.c` random_teleport_level).
## 2026-09-19 — D-2509 `insight.c` record_achievement whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/insight.c:2407–2472` (`record_achievement`); 32 call sites in `do.c` (6), `do_wear.c` (`#if 0`), `end.c` (3), `exper.c`, `hack.c`, `insight.c` self (2), `invent.c` (6), `mon.c`, `music.c` (2), `pray.c`, `priest.c`, `rumors.c`, `shk.c`, `spell.c` (2). Callees: `impossible`, `SoundAchievement` (macro), `livelog_printf`, `rank_of`, `rank_to_xlev`; data `achieve_msg[]` (`:57–91`), `Role_switch` (`you.h:248`), `OBJ_NAME` (`objclass.h:190`).
**JS:** `js/insight.js` (1 import word + restart, interface-identical `export function record_achievement(achidx)` kept sync so all 32 caller sites stay wired) + `scripts/record-achievement.test.mjs` (new); 2 files, under caps.
**Change:** `js/insight.js` only (same-edge import word `impossible` on the live display edge — `imports.mjs --can` ALREADY, no new edge) — restarted `record_achievement` in C order with `:line` cites: `:2414–2421` guard with `:2419` `impossible('Achievement #%d is out of range.', ai)` fire-and-forget (sync-caller precedent `js/do_wear.js:613` `setworn`); `:2423–2445` duplicate scan on abs value + append with 0-terminator kept; `:2435–2441` SoundAchievement named with build proof (active `:232–237` branch needs a `SND_LIB_*` backend, none defined in this build → `:274` empty definition, compile-time no-op); `:2447–2451` gameover skip; `:2453–2461` rank arm (`game.urole.mnum` = `Role_switch`, `(ai<0)?true:false` for C `TRUE/FALSE`); `:2462–2468` prize arm indexed `achieve_msg[ai]` like C (`ai>0` ⇒ same row) with `OBJ_NAME` = generated oc_name table; `:2469–2471` plain arm. `achieve_msg[]` verified row-identical to C `:57–91` (33 rows, flags + strings). New maintained `scripts/record-achievement.test.mjs` (5 tests: record+livelog, dup suppression, negated-rank abs match, gameover record-without-livelog, out-of-range no-record).
**Verify:** `node scripts/verify.mjs --fn record_achievement` → PASS syntax (1 file: js/insight.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach; RNG 0) · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. Focused `node --test scripts/record-achievement.test.mjs` 5/5. /tmp probe: 6/6 headless arm checks (record/dup/neg-rank/abs-dup/gameover/OOR) with no throw.
**Named:** C `spell.c:292` ACH_INVK (deadbook invocation-ritual success path — no `arti1_primed` ritual in `js/`; owns a future coverage row, not this one); SoundAchievement runtime dispatch (compile-time no-op in this build, proof in Fix); `really_done` ascension-split logging (pre-existing, D-2435 row owns `end.c`); the `context.h:131` / `do_wear.c:2516` lines are comments, not calls.
**Next:** Open — coverage head after record_achievement (`hack.c` domove_swap_with_pet).
## 2026-09-19 — D-2508 `pager.c` look_all whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/pager.c:1979–2074` (`look_all`, staticfn); callers `:1860` (`m` nearby mons), `:1863` (`M` all mons), `:1866` (`o` nearby objs), `:1869` (`O` all objs) in `dowhatis`. Callees: `look_region_nearby`, `glyph_at`, `glyph_is_monster/invisible/warning/object`, `u_at`, `canspotself`, `self_lookat`, `m_at`, `look_at_monster` (NULL monbuf), `glyph_to_warning`, `def_warnsyms`, `look_at_object` (via `object_from_map`'s `glyph_to_obj`), `upstart`, `coord_desc`, `strkitten`, `encglyph`, NHW_TEXT `putstr`/`putmixed`/`display_nhwindow`/`destroy_nhwindow`, `pline`.
**JS:** `js/pager.js` (restart + 1 import word); 1 file, +73/−40 interface-identical (`async function look_all(nearby, do_mons)` kept, still module-local like C staticfn).
**Change:** `js/pager.js` — restarted `look_all` in C order with `:line` cites: `:1989` region, `:1992–1993` buf init + `glyph_at`, `:1994–2004` do_mons monster branch (`u_at && canspotself` → `self_lookat`, else `mon_at` → `look_at_monster_buf` NULL-monbuf half), `:2005–2008` invis arm (`remembered, unseen, creature`), `:2009–2013` warning arm (`def_warnsyms[glyph_to_warning].desc`), `:2015–2018` object arm (`glyph_to_obj` → live `look_at_object`), `:2021–2042` header (`upstart`, `coord_desc(u)`, compass `!canspotself ? your position : you`, `"    "` separator), `:2043–2063` prefix (live `look_coord_prefix` widths + kitten, shown char + BUFSZ guard in the `look_traps` shape), `:2067–2073` `show_text_pages` / `No … shown` pline. Shown char is cell `disp_ch` (JS gbuf, D-1767) — exactly what C `encglyph(glyph_at)` renders, with no display-RNG re-roll; fallbacks `@`/`I`/warnsym ch/`?`. All imports pre-existing except `upstart` added to the live hacklib edge (`imports.mjs --can` ALREADY, no new edge); `mon_at` is the pre-existing uhitm wrap of `m_at`.
**Verify:** `node scripts/verify.mjs --fn look_all` → PASS syntax (1 file: js/pager.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach; RNG 0) · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** compass-full coord text (local `coord_desc` `(here)` deferral, pre-existing — header split and MAP/SCREEN paths are C-exact); `self_lookat` steed/`y_monnam` arm (pre-existing deferral, untouched); `look_at_object` TREE dangling/stuck suffix (pre-existing named, untouched); C `strkitten`/`eos`/`encglyph`/NHW_TEXT window plumbing served by the live local equivalents (`look_coord_prefix` kitten, string concat, `disp_ch`, `show_text_pages`); no committed unit test (repo has no js/ unit harness — corpus `verify --fn` + REACH is the maintained suite).
**Next:** Open — coverage head after look_all (`insight.c` record_achievement).
## 2026-09-19 — D-2507 `mthrowu.c` ohitmon whole body + `muse.c` munstone (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/mthrowu.c:321–502` (`ohitmon`); callers `do.c:210` (deliberate non-call — inline dmgval path, drop_throw→flooreffects), `explode.c:861`, `mthrowu.c:685` (`m_throw`), `trap.c:3408` (`launch_obj` ROLL −1). Callees: live `observe_object/find_mac/omon_adj(local)/rnd/miss(local)/distant_name/spec_abon/potionhit/seemimic/dmgval/resists_acid/stone_missile/passes_rocks(macro)/exclam(local)/hit(local)/is_poisonable/resists_poison/mon_hates_silver/noncorporeal/amorphous/s_suffix/hliquid/touch_petrifies/munstone(NEW)/minstapetrify/resists_ston/xkilled/mondied/can_blnd/setmangry/drop_throw(local)/obj_extract_self`; `muse.c:2883–2903` (`munstone`, deps same-file locals `mcould_eat_tin/cures_stoning/mon_consume_unstone` + helpless macro).
**JS:** `js/mthrowu.js` + `js/muse.js`; 2 files, +134/−39, under caps.
**Change:** `js/mthrowu.js` — restarted `ohitmon` in C order with `:line` cites: `:334–339` notonhead/ismimic/vis/observe; `:341–349` tmp +marcher level +`MON_WEP` launcher `spec_abon`; `:350–360` miss arm (`distant_name`, range-0 drop at mon cell); `:361–368` potion arm (kept); `:370–371` material + harmless via `stone_missile` + inlined `mondata.h:208` passes_rocks macro (`passes_walls && !unsolid`); `:372–375` dmgval + acid-immune 0 (`:376–378` `#if 0` orc/elf arm compiled out in C — not ported); `:383` splat sfx; `:384–401` egg (`known ? an(pmnames[corpsenm][2/NEUTRAL]) : 'an'`) / hit messages; `:403–417` poison (`rn2(30)` +`rnd(6)` vs deadly `damage=mhp`); `:418–432` silver (`s_suffix` flesh, seared); `:433–443` acid-burn (`hliquid`); `:444–455` egg-petrify (`munstone`→`minstapetrify`, `resists_ston` zeroes); `:457–474` kill incl. vampshifter verb + boulder-trap xkilled/mondied gate; `:476–490` `can_blnd(NULL,…,venom?AT_SPIT:AT_WEAP)` + mblinded `min(127, +rnd(25)+20)`; `:492` setmangry; `:494–496` drop_throw + boulder re-extract (D-0700). `js/muse.js` — new `export async function munstone` (`:2883–2903`) reusing same-file locals (async: `mon_consume_unstone` plines). New static edges (all `imports.mjs --can` IN-SCC/call-time-safe except SAFE sndprocs): dothrow `stone_missile` (trap.js precedent), artifact `spec_abon`, trap `minstapetrify`, muse `munstone`; same-edge words only otherwise (display `pline_The`, do_name `hliquid`, monsters ×9, objnam `distant_name`, zap `resists_poison`).
**Verify:** `node scripts/verify.mjs --fn ohitmon` → PASS syntax (2 files: js/mthrowu.js js/muse.js) · rule2 · hidden note (0 blocked at baseline) · **reach 12/12 PASS, 0 regressed → REACH-OK** (RNG-tagged reach, not smoke) · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** mon_notices unfreeze in same-file `omon_adj` (pre-existing local deferral, untouched); `#if 0` orc/elf damage+1 (`:376–378`, compiled out in C); no committed unit test (skill disclosure: repo has no js/ unit harness — corpus `verify --fn` + REACH is the maintained suite; no /tmp probe kept — behavior is message/RNG arms on paths no committed session walks).
**Next:** Open — coverage head after ohitmon (`pager.c` look_all).
## 2026-09-19 — D-2506 `options.c` handler_menu_colors whole body + coloratt.c menucoloring core (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/options.c:6407–6499` (`handler_menu_colors`, staticfn); sole caller `:8383` (`optfn_o_menu_colors` do_handler). Callees: `coloratt.c:320–328` attr2attrname, `:475–518` query_color, `:396–472` query_attr, `:530–580` basic_menu_colors, `:585–613` add_menu_coloring_parsed, `:684–706` free_one_menu_coloring, `:709–717` count_menucolors; `options.c:7871–7900` test_regex_pattern, `:9208–9253` handle_add_list_remove (staticfns); live `update_inventory`, `getlin`, `pline`, `wait_synch`, `clr2colorname` (artifact.js), `strNsubst`, `makeplural`, `select_menu` family.
**JS:** `js/options.js` (1 file, +~390/−8, under caps).
**Change:** `js/options.js` only (same-edge import words: hacklib `strNsubst`, display `tty_wait_synch`, invent `select_menu_pick_none`, terminal C CLR_* + `NO_COLOR`, objnam `makeplural`, const `HL_*` + `BUFSZ`/`QBUFSZ`, new `artifact.js` edge `clr2colorname` — `imports.mjs --can` ALREADY/SAFE on all five): local `MENU_COLORNAMES` (16 pre-alias rows, coloratt.c:14–30) + C-valued `MC_ATR_*` (wintype.h:128–134; terminal.js bits differ) + `MENU_ATTRNAMES` + module-level coloring list/save slots (decl.h:278/599/884–885; never saved, no save.c reader); ported in C order with `:line` cites — `attr2attrname`, `count_menucolors`, `free_one_menu_coloring`, `add_menu_coloring_parsed` (prepend + `use_menu_color`), `basic_menu_colors` (plain `%s` patterns — unix links posixregex, Makefile.src:229), `query_color` (basic swap around PICK_ONE, -1 on cancel), `query_attr` (Choose-prefix → PICK_ANY `HL_*` mask, else PICK_ONE), `test_regex_pattern` (validate-and-discard), `handle_add_list_remove` (a/l/r/x accelerators, added-rows-only `a_int` so exit-with-empty returns 1 like C, cancel → 3), `menucolors_done` (`:6420–6430`), `handler_menu_colors` (`:6416–6498`: done/add/ESC/list-PICK_NONE/remove-PICK_ANY with `'"orig"\=color[&attr]'` suffix, BUFSZ `...` truncation, `-k` remove shift).
**Verify:** `node scripts/verify.mjs --fn handler_menu_colors` → PASS syntax (1 file: js/options.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed) → VERIFY: PASS. /tmp probe: 18/18 pure-path checks (count/LIFO add/free/attr names/swap-restore/13-color cache reuse).
**Named:** `config_error_add` + `regex_error_desc` (msgtype_add precedent); native `create_nhwindow`/`start_menu`/`add_menu`/`end_menu`/`select_menu`/`destroy_nhwindow` served by C-matched `select_menu_pick_one/any/none` clones (helper-mapping deltas: C pick_cnt==2 preselected+explicit arms collapse to the explicit pick; PICK_ANY finish-empty and ESC both read `[]` → remove/list arms map to the ESC exit); `all_options_menucolors` config-dump sibling (`options.c:9595`, not a callee/caller); `add_menu_coloring` MENUCOLOR= parser + `free_menu_coloring`; no committed unit test (skill disclosure: repo has no js/ unit harness — sessions are the maintained suite; probe kept in /tmp).
**Next:** Open — coverage head after handler_menu_colors (`mthrowu.c` ohitmon).
## 2026-09-19 — D-2505 `do_wear.c` Amulet_on whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/do_wear.c:963–1087` (`Amulet_on`, staticfn); callers `:1551` (`set_wear`), `:2417–2418` (accessory put-on; `potion.c:2484` is a comment); callees `mondata.c:590–619` can_be_strangled, `region.c` region_danger, `polyself.c` float_vs_flight, `potion.c` make_slimed; `youprop.h:110/113/276` Strangled/Slimed/Breathless, Flying.
**JS:** `js/do_wear.js` (restart + `amulet_flight_now` + 10 import words), `js/uhitm.js` (export word); 2 files, ~+140/−50, under caps.
**Change:** `js/do_wear.js` — restarted `Amulet_on` as the C-order switch with `:line` cites: `:972–977` no-op group; `:978–995` breathing (W_AMUL masked out for the `region_danger` test, had-guarded restore, `makeknown`+`on_msg`+`You`); `:996–999` slime cure via live `make_slimed(0, null)`; `:1000–1035` change (same live logic, now `You`/`pline_The` per C); `:1036–1046` strangulation (`can_be_strangled(game.youmonst)`, Strangled 6 mirrored field+intrinsic like `polyself.js:1258`, both botls, constrict pline); `:1047–1055` sleep kept; `:1056–1076` flying (`float_vs_flight`, uprops-extrinsic had-guarded dance, new `amulet_flight_now` complete Flying read incl. ridden flyer); `:1077–1080` guarding kept; `:1081–1083` yendor no-op; `:1085–1087` trailing on_msg. `js/uhitm.js` — `can_be_strangled` exported (body untouched, pre-existing C-cited clone). New static edges: do_wear→region (`region_danger`, hoisted, IN-SCC cycle-safe per `imports.mjs --can`) and do_wear→uhitm (`can_be_strangled`, hoisted fn; reverse `hard_helmet` edge pre-exists); same-edge extensions only otherwise (display `You`/`pline_The`, polyself `float_vs_flight`, potion `make_slimed`, monsters `is_flyer`, const FLYING/STRANGLED).
**Verify:** `node scripts/verify.mjs --fn Amulet_on` → PASS syntax (2 files: js/do_wear.js js/uhitm.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach) · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS (re-run after cite-only edits: identical PASS; working-scoreboard diff from the runs is commit/at metadata only, no session rows).
**Named:** `livelog_newform` (log-only, no live helper — map); `Flying_dw` uprops gap (reads u.* mirrors only; the new arm uses `amulet_flight_now` instead — pre-existing, untouched); `Amulet_off` breathing/strangle/flying arms (that function's own named omits, untouched); no committed unit test (skill disclosure: `Amulet_on` is module-local like C staticfn and `set_wear` fixtures need makeknown/xname/display with zero precedent in scripts/*.test.mjs — disproportionate; corpus gates are the maintained suite per D-2453).
**Next:** Open — coverage head after Amulet_on (`options.c` handler_menu_colors).
## 2026-09-19 — D-2504 `teleport.c` rloc_to_core whole core as exported composer + mtele_trap RLOC_MSG

**C locus:** `nethack-c/upstream/src/teleport.c:1644–1768` (`rloc_to_core`, staticfn); callers `:1771–1774` (`rloc_to`→NOMSG), `:1776–1782` (`rloc_to_flag`), `:1893` (rloc found_xy), `:1988` (`mtele_trap` teledest RLOC_MSG); `you.h:560` `m_next2u`≡`distu<=2`; `hack.h:1391–1394` RLOC flags.
**JS:** `js/teleport.js` (`rloc_to_core` + `rloc_to_flag` + `mtele_trap`); no new imports (`m_at`/RLOC consts already in-module).
**Change:** `js/teleport.js` only, no new module edges — new `export async function rloc_to_core(mtmp, x, y, rlocflags)` composing the live helpers in C order (same-cell `:1658–1659` return first so no vanish prints; pre `:1661–1677`; move `:1679–1702` via `rloc_to` defer; post `:1703–1737`; tail `:1739–1767`); `rloc_to_flag` is now the thin C `:1776–1782` wrapper delegating to it (behavior-identical); `mtele_trap` teledest calls `rloc_to_core(..., RLOC_MSG)` per C `:1988`, fallback `await rloc(mtmp, 0)`; omission note retired. Silent `rloc_to` (NOMSG path, many external callers) byte-untouched.
**Verify:** `node scripts/verify.mjs --fn rloc_to_core` → PASS syntax (1 file) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach; repeat with `--reach-all`: same gates, REACH-OK) · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS.
**Named:** `u_on_newpos` inlined (ux/uy/uundetected/steed; `see_nearby_objects` correctly skipped — uswallow true fails C's gate; `earth_sense` map-named, turns.md:1632); `place_monster` mx/my inline (grid occupancy/mstate have no JS counterpart; D-1160); `remove_monster`≡mx/my zeroing (`m_at` scans fmon); `m_next2u`≡`distu_xy>2` (`you.h:560`); dog.js un-awaited `rloc_to` calls (pre-existing, C `rloc_to` callers outside this row's 4).
**Next:** Open — coverage head after rloc_to_core (`hack.c` domove_core).
## 2026-09-19 — D-2503 `muse.c` precheck whole-body port (ghost/djinni occupant + cursed wand backfire)

**C locus:** `nethack-c/upstream/src/muse.c:59–160` (`precheck`, staticfn); C callers `:805` (`use_defensive`), `:1837` (`use_offensive`, non-potion guard), `:2392` (`use_misc`).
**JS:** `js/muse.js` only (1 file, +93/−48 net per diff). Same-edge import words only, all `imports.mjs --can` ALREADY on the file edge: `rndmonnam, a_monnam` (do_name), `paralyze_monst` (mhitm), `set_malign` (makemon), `Something, AD_RBRE` (const), `SetVoice` (sndprocs), `se_zap_then_explosion` (generated/seffects_data). File-local `objdescr_is`/`m_useup`/`mquaffmsg`/`mdistu` kept (same-file, pre-existing). `pline1` → `pline` (no live `pline1` export; identical `vpline` path).
**Change:** restarted `precheck` in C order with `:line` cites. Milky `:73–101`: `G_GONE` + `POTION_OCCUPANT_CHANCE` gate, `enexto` early-0, `mquaffmsg`, `m_useup`, `makemon(MM_NOMSG)`, `pline1(empty)` → live `pline`, ghost emerge pair via `%s` args with `Hallucination() ? rndmonnam(null) : 'ghost'`, live `paralyze_monst(mon, 3)`, `return 2`. Smoky `:102–131`: same skeleton, then `pline_mon('In a cloud of smoke, %s emerges!', a_monnam)`, `pline('%s speaks.', vis ?
**Verify:** `node scripts/verify.mjs --fn precheck` → VERIFY: PASS — syntax 1 file (`js/muse.js`); rule2 clean; hidden note (no corpus session blocked at HEAD; row cited 0 blocks so no `--base` re-run owed); REACH-OK (2 baseline-PASS reach sessions run: 2 PASS, 0 regressed); green 2/2; strict both sessions; cohort 7/7; full skipped (no shared file changed).
**Named:** none in the ported body — every C arm is live. (Pre-existing file-local `m_useup`/`objdescr_is` clones owned by their own functions' ports, untouched.)
**Next:** pop the next Open — coverage row (`potion.c` make_blinded).
## 2026-09-19 — Audit 12fef55e..31ee9a50 (reviews 1453–1461: 8 ACCEPT, 1 ACCEPT-WITH-DEBT) + cadence 44/44, proxy 495/540

**Scope:** every JS-touching commit since review 1452 (D-2494 setmangry, D-2495 help_dir, D-2496 mpickobj, D-2497 doname_base, D-2498 windowport-notify, D-2499 m_consume_obj, D-2500 recharge, D-2501 movebubbles, D-2502 trapeffect_web). One SHA at a time, file written to disk per SHA, single grouped commit.
**Findings:** 8 ACCEPT (branch-by-branch C confirms; real fixes verified: help_dir bottom-row order caught by its own reach FAIL, doname_base AMULET gate, p_glow3/You() in recharge, pline_mon in web). 1 ACCEPT-WITH-DEBT: 1457 windowport-notify — `status_update` throws loud in live arms but is unreachable (nothing imports botl.js), test-pinned both ways, caller `bot():1277` wiring + dispatch named in `c-js-map/startup.md`. No C-wrong ⇒ no Must-fix prepend; Next cluster unchanged (`trap.c` trapeffect_web row is now done — next pop is `muse.c` precheck).
**Cadence:** full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838 (identical totals); held-out 11/44 (cached 2026-09-18); proxy 495/540 (91.7 %) RNG 99.29 % screens 99.0 % — identical, no flips. Per-SHA re-runs: recharge reach 3/3 PASS; rest 0-blocked + smoke 24/24, 0 regressed.
**Refill:** `port-coverage --rows 12` @31ee9a50 emits 12, all 12 Stale-parked (newcham/getobj/checkfile/yn_function/getdir/mon_arrive/display_pickinv/mhitm_ad_phys/vpline/makeplural/do_look/make_corpse) — nothing appended (never re-pop Stale).
**Check:** `check-hot-docs --fix --review 1453..1461` → All ok, no cap edits.
## 2026-09-19 — D-2502 `trap.c` trapeffect_web whole-body port (hero/steed/strength-tim arm, bear-roar Soundeffect, pline_mon)

**C locus:** `nethack-c/upstream/src/trap.c:2106–2276` (`trapeffect_web`); sole C caller `trap.c:2972` (WEB arm of the trapeffect selector switch).
**JS:** `js/trap.js:4990` `async function trapeffect_web` (local, same signature); new same-edge import words only — `You` (display.js), `strongmonst` (monsters.js), `se_roar` (generated/seffects_data.js); `u_locomotion("stumble")` via existing fuller local `u_locomotion_verb` (Lev/Fly + `locomotion()` poly path); `youmonst = game.youmonst ?? mtmp` documents the C `&gy.youmonst` identity incl. the dotrap `_youmonst` stand-in.
**Change:** restarted `trapeffect_web` in C order. Hero arm (`:2116–2202`): NOWEBMSG/FORCETRAP|FAILEDUNTRAP/VIASITTING flags, named-steed article suppression, `feeltrap`, `mu_maybe_destroy_web`, webmaker walk (`You take a walk on your web.` / `There is a spider web here.`), caught-by/lead-into/`u_locomotion("stumble")`-into `You('%s %s spider web!')`, `set_utrap(1, TT_WEB)`, mounted-steed `mintrap` pre-pass (mtrapped clear, `strongmonst` → str 17, else `reset_utrap` + return), full `ACURR(A_STR)`→`acurr(A_STR)` `rn1`/`rnd` ladder incl. str≥69 tear-through + `deltrap` + `newsym`, final `set_utrap(tim, TT_WEB)`. Monster arm: added `Soundeffect(se_roar, 60)` before the unseeing-bear `You_hear`; the three in-sight messages now call `pline_mon` per C (text-identical: `pline_mon` = `set_msg_xy` + `vpline`).
**Verify:** `node scripts/verify.mjs --fn trapeffect_web` → VERIFY: PASS — syntax 1 file; rule2 clean; hidden `no corpus session blocked`; REACH-OK (no RNG-tagged reach; fixed smoke spread 24 run: 24 PASS, 0 regressed); green 2/2; strict both sessions; cohort 7/7; full skipped (no shared file changed).
**Named:** none in the ported body — every C arm is live. (Pre-existing deferrals elsewhere untouched: `u_locomotion_verb` poly-`locomotion()` path as already ported; `Soundeffect` is a display-sound no-op in JS.)
**Next:** pop the next Open — coverage row (`muse.c` precheck).
