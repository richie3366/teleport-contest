# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-18 — D-2441 `polyself.js` dogaze `setmangry` import wired (ReferenceError deleted)

**C locus:** `nethack-c/upstream/src/polyself.c:1642–1773` (dogaze; `setmangry(mtmp, TRUE)` after the peaceful-confirm gate, C `:1704`); dispatched from C `cmd.c:909` AT_GAZE arm.
**JS:** `js/polyself.js:38`; map `docs/c-js-map/turns.md:296`.
**Change:** `js/polyself.js:38` only — added `setmangry` to the existing `./mon.js` import. `node scripts/imports.mjs --can polyself.js mon.js setmangry` → ALREADY, no new edge. Live callee `js/mon.js:1120` is `export async`, call site already awaits.
**Verify:** `node scripts/verify.mjs --fn dogaze` → PASS syntax (1 file: polyself.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** review 1395 item 2 — `dospinweb` PIT arm `bury_objs` (`js/polyself.js:2584`, live `js/dig.js:450` ASYNC) stays open as its own Must-fix row; Must-fix stays one item, alone.
**Next:** dospinweb `bury_objs` import row, then the Open — coverage head (`invent.c` getobj).
## 2026-09-18 — D-2440 `lock.js` getdir caller dz-zeroing deleted (up/down restored)

**C locus:** `nethack-c/upstream/src/cmd.c:3956–4119` (getdir `:4095` `else if (!(is_mov = movecmd(dirsym, MV_ANY)) && !u.dz)`); callee `movecmd :3868–3898` returns `!u.dz` while KEEPING dz=±1 on up/down.
**JS:** `js/lock.js:662–667`; map `docs/c-js-map/turns.md:1946`.
**Change:** `js/lock.js` only — deleted the `if (!applied) { u.dz = 0; }` block. `apply_dirsym` already zeroes dz on every true-failure exit (code=0 arm, fallthrough arm), mirroring C; D-1387 kept. Compass/self/mouse/quit/help/confdir arms untouched, in C order.
**Verify:** `node scripts/verify.mjs --fn getdir` → PASS syntax (1 file: lock.js) · rule2 · hidden note (0 sessions blocked on `getdir` at baseline — the 3 flips are owned by exercise/distfleeck/could_untrap) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. Plus `node scripts/hidden-proxy.mjs score --ids scen-death-Wizard-92187,scen-kit-Archeologist-92190,scen-normal-Archeologist-92012` → all 3 PASS; working `hidden-corpus/scoreboard.json` back to 495/540 (91.7%).
**Named:** review 1393 item 2 — num_pad `'5'` self disjunct has no C counterpart (C binds `5` to the run prefix, `movecmd` matches only directional move_funcs); session-unreachable (num_pad never set in any suite); next `cmd.c`/`lock.js` iter takes it. Must-fix stays one item, alone.
**Next:** Must-fix dogaze `setmangry` + dospinweb `bury_objs` imports (review 1395), then the Open — coverage head.
## 2026-09-18 — Audit 3ea4873c..e6289b5b (reviews 1391–1398: 5 ACCEPT, 1 ACCEPT-WITH-DEBT, 2 QUALITY-RISK) + cadence 44/44, proxy 492/540

**Scope:** 8 JS-touching SHAs since review 1390 (D-2432 polymon, D-2433 newcham, D-2434 getdir, D-2435 really_done, D-2436 dogaze/dospinweb/rehumanize, D-2437 init_dungeons, D-2438 adjattrib, D-2439 test_move). One SHA reviewed then filed immediately; one grouped commit.
**Findings:** (1) Review 1395: D-2436 dogaze/dospinweb call unimported `setmangry`/`bury_objs` (ReferenceError on live arms; suite-blind, zero corpus reach) → Must-fix ×2, both ALREADY-edge one-line imports. (2) Corpus re-score 495→492/540: 3 PASS→FAIL (Wizard-92187/exercise, Arch-92190/distfleeck, Arch-92012/could_untrap) audit-bisected via worktree `--ids` replays to `1d21e3be` getdir — `apply_dirsym('</>')` sets dz=∓1 and returns false per C movecmd, then `if (!applied) u.dz = 0` destroys it, so every up/down at a direction prompt prints cmdassist help and fails (JS toplines match exactly); test_move re-verified clean. Review 1393 raised to QUALITY-RISK → Must-fix (delete the block; apply already zeroes on true failure). Lesson: getdir has no RNG-tagged reach, so REACH-smoke can never catch input-path regressions — a whole-function port on the input path needs a key-consumption diff, not just smoke.
**Cadence:** public 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, `65+0.41/turn`); held-out 11/44 unchanged (breadth falsifier needs ~30 iters, 8 in); corpus 492/540 with the 3-flip Must-fix.
**Verify method:** every SHA re-measured via `hidden-proxy verify <fn> --base HASH~1 --reach-all` (all D-log claims confirmed); `imports.mjs --rulecheck` clean; `sym.mjs` on all deleted/re-pointed symbols (test_move_viable deletion verified; Fire_resistance twin identical).
**Next:** Must-fix getdir dz (top), then dogaze/bury imports, then the first Open — coverage row (refilled below; test_move shipped as D-2439).
## 2026-09-18 — D-2439 `hack.c` test_move whole body in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/hack.c:991–1255` (test_move; DO_MOVE/TEST_MOVE/TEST_TRAV/TEST_TRAP); same-file helpers `hack.c:59–66` Known_wwalking/Known_lwalking + `hack.c:932` may_passwall; `dbridge.c:77` is_pool_or_lava; 14 live C call sites (cmd.c ×2, hack.c ×7, steed.c ×2, trap.c, uhitm.c, zap.c).
**JS:** `js/hack.js:302–560` (helpers + test_move); `js/mon.js:141` may_passwall export; `js/trap.js:4612` Fire_resistance export; map `docs/c-js-map/turns.md:1449`.
**Change:** `js/hack.js:302–560` new `export async function test_move(ux, uy, dx, dy, mode)` in C order with per-arm `:line` cites: entry `door_opened=false` on all modes (`:1000`); obstructed/IRONBARS with Blind feel, Passes_walls+may_passwall pass, Underwater block, rust/corr/metallivore `still_chewing`, `Passes_walls||passes_bars`, tunnels-eat-rock, autodig+`use_pick_axe2` (live on `game.flags.autodig`, no JS option yet), drawbridge/Sokoban-resist/mention_walls (`back_to_glyph`/`glyph_is_cmap`/`glyph_to_cmap`, S_stone→solid stone, tree/wall heuristic, impossible-glyph fallback, `pline_dir`); closed-door Passes_walls/ooze/Underwater/tunnels then autoopen (`doopen_indir`, `door_opened=!closed_door`, `move=(ux!=u.ux||uy!=u.uy)`) vs orthogonal ouch (`u.usteed` lead / `Ouch!`+`exercise(A_DEX)`, `door_opened=move=TRUE`, `nomul(0)`); TEST_TRAV/TRAP `goto testdiag` as the shared diagonal gate; squeeze `cant_squeeze_thru` 3/2/1; `worm_cross`+`YMonnam(m_at)`; run==8 trap/liquid returning `(mode==TEST_TRAP)` with WATERWALL/LAVAWALL + Levitation/Flying + Known_wwalking/lwalking; `TEST_TRAP→FALSE`; diagonal-out-of-doorway; boulder run>=2 abort, DO_MOVE chew-then-`moverock()`, TEST_TRAV Sokoban/two-in-row (pick/mattock/`WAN_DIGGING`-unknown via `game.objects oc_name_known`, apply.js idiom). New locals `Blind_tm` (cmd.js:3390 idiom) + `test_move_known_wwalking/lwalking` (C `:59–66`); `may_passwall` exported from mon.js (teleport.js keeps its D-1100 copy), `Fire_resistance` exported from trap.js; all new edges `imports.mjs --can` SAFE hoisted (dbridge/lock/dig/zap/monmove/worm). `You` imported (zap.js live export); You_cant/Your/There/pline_The rendered as net-identical pline text (lock.js:602 precedent, no new clones).
**Verify:** `node scripts/verify.mjs --fn test_move` → PASS syntax (5 files) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared files changed) → VERIFY: PASS.
**Named:** block_door (shk.c:5791 — stub-false js/cmd.js:1176, no shop ESHK wire-up); block_entry (shk.c:5826 — no JS impl); ECMD_OK + canned-kick fake (JS doopen_indir returns bool; cmdq_peek cmd.js-local); defsyms[].explanation prose (tree/wall/solid-stone heuristic); autodig flag (no JS option).
**Next:** domove→test_move DO_MOVE rewire (own iteration, hot path); block_door/block_entry shopkeeper arms; click_to_cmd mouse callers; zap.c:4102–4121 ball-stop arm.
## 2026-09-18 — D-2438 `attrib.c` adjattrib whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/attrib.c:117–199` (adjattrib); callees `Fixed_abil`, `Your`/`pline`/`You_feel`, `rn2` (decr path), `encumber_msg`; 34 call sites (attrib.c ×7 incl. gainstr/losestr/poisoned/exerchk/vary_init_attr, eat.c ×4, end.c, fountain.c, mhitu.c ×4, potion.c ×4 incl. sickness contaminant, pray.c ×5, sit.c ×3, timeout.c, trap.c, u_init.c ×2, uhitm.c:3263 mhitu arm).
**JS:** `js/attrib.js` (ATTRNAME const; adjattrib restart); map `docs/c-js-map/startup.md:18`.
**Change:** `js/attrib.js` restart of the body in C order with per-arm `:line` cites: old_abase/old_amax snapshot beside old_acurr; abonflg from `u.abon.a[ndx]` (`<0` on gains, `>0` on losses); ACURR-unmoved arm with msgflg==0-exact + verbose-default-on gate printing "You're currently/already as {attrstr} as you can get" when base+peak unmoved else "Your innate {name} has improved/declined" via pline (prefix inline — no 5th Your clone per brief); new local ATTRNAME from C `:20–21`; You_feel via the existing static display.js import (dynamic import removed; `imports.mjs --can` → ALREADY, no new edge). No DIAG/FORCE/seed logic; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn adjattrib` → PASS syntax (1 file: attrib.js) · rule2 · hidden note (0 blocked at baseline) · **REACH smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** u_init.c:933/:935 (`u_init_carry_attr_boost` STR/CON loop) — JS `js/u_init.js:1689` stub stands pending an `inv_weight` port (loop condition unported, so the calls are unreachable, not dropped); Your() renders as `pline('Your …')` (text-identical, no new file-local Your clone).
**Next:** head is now `hack.c` test_move (MISSING).
## 2026-09-18 — D-2437 `dungeon.c` init_dungeons whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/dungeon.c:1205–1319` (init_dungeons); callees `nhl_init`/`nhl_loadlua`/`nhl_done` (private Lua state), `init_dungeon_dungeons` (`:997+`), `init_level`, `place_level`, `add_level`, `init_castle_tune`, `fixup_level_locations`, `free_proto_dungeon` (`:1185–1203`), `dumpit` (DEBUG); callers `allmain.c:789`, `mklev.c:1260–1263`, `topten.c:1232–1234`, `vision.c:845` (comment only).
**JS:** `js/dungeon.js:1046–1139` (init_dungeons restart, function at :1053); `js/mklev.js:138` (import), `:23890–23899` (makelevel guard); map `docs/c-js-map/startup.md:22`.
**Change:** `js/dungeon.js` restart in C order: memset/re-zero cited on the pd literal; nhl_init/nhl_loadlua failure panics named omits (generated `dungeon_data.js` embed, D-0477 pattern) keeping the observable nhlib align shuffle; window_inited→clear_nhwindow(WIN_MAP) named omit (screen side effect, no primitive); sp_levchn NULL→`[]`; Array.isArray + per-entry object checks for the two lua-table panics; MAXDUNGEON/place panics stay throws; lua_next loop with `cl`-carries-across-dungeons and `i`-counts-successes notes; DDEBUG/debugpline2/nhl_done/free_proto_dungeon/dumpit named omits. `js/mklev.js`: init_dungeons joins the existing dungeon.js import (no new edge — dungeon.js never imports mklev) + the C makelevel guard (`wiz1_level.dlevel==0` → impossible + init_dungeons). No DIAG/FORCE/seed logic; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn init_dungeons` → PASS syntax (2 files: dungeon.js mklev.js) · rule2 · hidden note (0 blocked at baseline) · **REACH smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared dungeon.js changed) → VERIFY: PASS.
**Named:** nhl_init/nhl_loadlua/nhl_done Lua-state scaffolding + load-failure tbuf panic (generated-data embed); window_inited clear_nhwindow(WIN_MAP) (display side effect); DDEBUG block + debugpline2 DONE + DEBUG dumpit (debug-only); free_proto_dungeon (GC); topten.c:1234 standalone-scores caller.
**Next:** no other dungeon.c row queued; head is now `attrib.c` adjattrib (PARTIAL).
## 2026-09-18 — D-2436 `polyself.c` dogaze + dospinweb + rehumanize whole bodies in C order (coverage MISSING/MISSING/THIN → live)

**C locus:** `nethack-c/upstream/src/polyself.c:1642–1773` (dogaze); `:1497–1621` (dospinweb); `:1367–1418` (rehumanize); predicates `monst.h:251` helpless, `mondata.h:81` perceives, `dbridge.c:77–83` is_pool_or_lava, `youprop.h` Blind/Invis/See_invisible/Free_action/Confusion/Hallucination; `mhitu.c:1699` expels; `zap.c:1706` destroy_items.
**JS:** `js/polyself.js:75–76` (vision/light edges), `:277–278` (PM_MEDUSA/AMULET_OF_UNCHANGING), `:961` (rehumanize), `:2383` (dogaze), `:2523` (dospinweb), `:2686` + `:2692` (domonability arms); map `docs/c-js-map/turns.md:296`.
**Change:** `js/polyself.js` only, in C order. dogaze: AT_GAZE mattk scan with AD_CONF/AD_FIRE gate else impossible; Blind/Hallucination/uen<15 gates; uen-=15 + botl; snapshot fmon loop (one visit per monster while killed() unlinks); Invis/perceives, minvis/See_invisible, mimic-appear looked--, safe_dog (!== false default-On) tame, confirm (!== false) peaceful y_n, setmangry, helpless/stun/blind/eyeless looked--, AD_CONF mconf, AD_FIRE d(2,6)+resists_fire+lev>rn2(20) destroy+ignite+killed, floating-eye nomul freeze (multi_reason + nomovemsg=0, ECMD_TIME) or stiffen, Medusa done(STONING); looked==0 pline. dospinweb: Levitation/terrain gate; uswallow animal-expels / whirly AT_ENGL sweep / dissolve; utrap; exercise DEX; full trap switch (pit+bury, squeaky, teleports vanish, WEB thicken, hole/door, boulder, armed dotrap, default impossible); stairs cop-out; maketrap WEB + madeby_u + feeltrap + shop damage via dynamic shk import (zap.js precedent, no new static edge). rehumanize restart: Unchanging stuck arm with amulet Your/observe/makeknown, emits_light del_light_source, uhp<1 death, update_inventory, flying-steed You, gloveless selftouch. New static edges: couldsee←vision.js, emits_light/del_light_source←light.js (both `imports.mjs --can` SAFE, hoisted fns); rest join existing edges. You_cant/Your/pline_The render as prefixed pline (file idiom); no new Your/See_invisible/perceives/helpless clone.
**Verify:** `node scripts/verify.mjs --fn dogaze/dospinweb/rehumanize` → each PASS syntax (1 file: polyself.js) · rule2 · hidden note (0 blocked at baseline) · **REACH smoke spread 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · VERIFY: PASS (full skipped: no shared file). Scoreboard timestamp churn reverted. No new test file (no tests/ harness; REACH smoke + green/strict/cohort is the maintained gate).
**Named:** retouch_equipment(2) (`artifact.c:2639`, own coverage row); regen_hp/losexp/explode/losehp/passiveum caller gaps (above).
**Next:** same-file Open rows none (dogaze/dospinweb/rehumanize all shipped); head is now `dungeon.c` init_dungeons (THIN).
## 2026-09-18 — D-2435 `end.c` really_done whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/end.c:1130–1590` (`really_done`); achievements `:1173–1183` via `insight.c` record_achievement; score+ascension `:1321–1349` via `u.ualignbase[A_ORIGINAL/A_CURRENT]`; grave `:1300–1319` via `topten.c` formatkiller + `rm.h:220` emptygrave≡flags; suffix `:1400–1411` via `hack.c` carrying + `Is_astralevel`; `u_init.c:950/989` beginner/arise init parity.
**JS:** `js/end.js:5–8` (hack edge), `:38–40` + `:69–72` (const/insight edges), `:99` (FAKE_AMULET), `:968–986` (doc), `:985` (`really_done`), `:1015–1025` (achievements), `:1110` (finish_paybill), `:1112–1130` (grave), `:1132–1153` (score+ascension), `:1179–1181` (done_money), `:1201–1213` (suffix); map `docs/c-js-map/turns.md` end.c section.
**Change:** `js/end.js` only, in C order — achievements via live `record_achievement` (ACH_BLND/NUDE gated on `uachieved[0]||!beginner`, ACH_UWIN on ASCENDED; gameover-quiet, no RNG/output); `finish_paybill` moved before grave+score (it money2mons invent gold and drop_upon_deaths invent, so C scores post-payment invent); grave via live `formatkiller(how, true)` epitaph + `flags = 1` emptygrave on new graves, NOCORPSE on `u.umonnum` like C (null/undefined arise ≡ NON_PM per C `u_init.c:989`); score + ascension bonus on the JS `{current,original}` ualignbase shape (`Math.trunc` for the `/2L`, no `|0` wrap past 2^31); `corpse = null` after savebones; `_done_money` moved to C's post-savebones site (rip.js reads it); killer-name suffix after the tombstone via live `carrying` + `Is_astralevel` + `uhave.amulet`. New edges: `carrying`←`hack.js` (IN-SCC, hoisted decl, call-time use), `record_achievement`←`insight.js` (ALREADY), ACH_BLND/NUDE/UWIN + IS_GRAVE←`const.js` (existing edge), local FAKE_AMULET const. No DIAG/FORCE/seed logic; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn really_done` → PASS syntax (1 file: end.js) · rule2 · hidden note (0 blocked at baseline) · **REACH smoke spread 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · VERIFY: PASS (full skipped: no shared file). Reverted the run's `hidden-corpus/scoreboard.json` timestamp churn (no session rows changed) before handoff.
**Named:** dumplog family (`dump_open_log`/`dump_everything`/`dump_redirect`/`genl_outrip`/`dump_forward_putstr`/`dump_close_log` — file side-channel, DUMPLOG retired D-1776); `livelog_printf`/logfile/xlogfile (no session effect); `wait_synch`/signals/`sethanguphandler`/`exit_nhwindows` (platform/windowing, no JS counterpart); `sound_exit_nhsound` (no sound lib); `panic()` caller (above); pre-existing `done_object_cleanup` inven_inuse/ball-chain, `unleash_all`, `savelife` arise-reset (`end.c:741`, savelife's function), `u_init` arise init (`u_init.c:989`, covered by the null≡NON_PM check).
**Next:** same-file Open rows none (really_done was the only `end.c` coverage row); head is now `polyself.c` dogaze (MISSING).
## 2026-09-18 — D-2434 `cmd.c` getdir whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/cmd.c:3958–4119` (`getdir`); callees `movecmd :3868–3898`, `dxdy_moveok :3901–3907`, `redraw_cmd :3910–3918`, `help_dir :4168–4296`, `confdir :4302`→`hack.c:2420`, `decl.c:96` quitchars, `decl.c:77` xdir/ydir/zdir, `cmd.c:3161` spkeys_binds + `:3346` sdir/ndir + `:2070` move_funcs.
**JS:** `js/lock.js:7` (display edge), `:19–24` (const edge), `:59–60` (dokeylist/getpos edges), `:65` (QUITCHARS), `:80–110` (spkey defaults/helpers), `:114–120` (SDIR/NDIR/move txt), `:129–170` (`apply_dirsym` movecmd), `:173–177` (`getdir_is_redraw`), `:506–514` (CMDQ_DIR), `:538–600` (`getdir_read_dirsym`), `:605–750` (`getdir`); map `docs/c-js-map/turns.md:1941–1946`.
**Change:** `js/lock.js` only, in C order — cmdq DIR respects num_pad NDIR/SDIR + dirz `>`/`<`, non-DIR/KEY now `await impossible('getdir: command queue had no dir?')`; retry keeps `getdirInp` + in_doagain-nhgetch, adds short-circuit fuzzer (`debug_fuzzer && rn2(20)` → `rn2(20)`/`rn2(2)`/`rn2(N_DIRS)` on spkeys/dirchars, zero draws when off); redraw via live `cmdbind_get`→`redraw` txt + `await flush_screen(1)` retry; SELF/SELF2/MOUSE/HELP/ESC via `getdir_spkey` (game.Cmd.spkeys overlay + binds defaults); mouse arm via live `getpos` + `visctrl` pick-key prompt, sgn inline, CLICK_1/2 on `pos+NHKF_GETPOS_PICK`, `getdir_click` in/out, impossible on bad pos; movecmd via live `cmdbind_get` txt→move/run/rush/down/up + xdir/ydir/zdir (`!u.dz`, else dz=0), num_pad digit fallback; quitchars constant; help_dir ESC via spkey; You_cant stays net-identical pline. Imports join existing edges (`impossible`/`flush_screen`←display, `cmdbind_get`←dokeylist ALREADY, `getpos`←getpos.js SAFE hoisted, CLICK/NHKF/N_DIRS/xdir/ydir/zdir←const). No DIAG/FORCE/seed logic; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn getdir` → PASS syntax (1 file: lock.js) · rule2 · hidden note (0 blocked at baseline) · **REACH smoke spread 24/24 PASS, 0 regressed → REACH-OK** (also `--reach-all`: same, no RNG-tagged reach) · green 2/2 · strict ×2 · cohort 7/7 · VERIFY: PASS (full skipped: no shared file). No new test file (no tests/ harness; REACH smoke is the maintained gate).
**Named:** `readchar`/`readchar_queue`/`readchar_core` (altmeta pushback empty in sessions; nhgetch covers it); `docrt_flags` (live `flush_screen(1)` is the redraw_map path); `sgn` (inline, no 16th clone); `You_cant` (net-identical pline); swap_yz/phone_layout/pcHack num_pad variants + `M(digit)` run/rush + `yn_function` menu-pick arm + BIND= overrides beyond cmdbind overlay (no session evidence).
**Next:** same-file Open rows none (getdir was the only `cmd.c` coverage row); `end.c` really_done stays head.
## 2026-09-18 — D-2433 `mon.c` newcham whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/mon.c:5278–5535` (`newcham`); `monattk.h` AT_ENGL=11; `trap.c` `mselftouch`; `mhitu.c` `expels`/`unstuck`; `display.c` `swallowed`; `pline.c` `You`.
**JS:** `js/makemon.js:178–182` (imports), `:1722` (`AT_ENGL`), `:1728–1759` (`newcham_light_invis`), `:1761–1803` (`newcham_ustuck`), `:1805–1818` (`newcham_worm_newsym`), `:1862–1932` (`newcham_after_unleash`), `:1960` (`newcham`), `:2053` (`newcham_apply_form`); map `docs/c-js-map/turns.md:2749`.
**Change:** `js/makemon.js` only — split the post-`set_mon_data` block into `newcham_light_invis` (`:5399–5412`), `newcham_ustuck` (`:5413–5450`: break-out `You` + mhp 1 + `expels` consuming SHOW_MSG even when msg is FALSE, silent expels for noncorporeal/whirly/amorphous/light, `swallowed(0)` repaint when the new form keeps AT_ENGL, `unstuck()` release unless new form or hero still sticks or new form unsolid-proof), `newcham_worm_newsym` (`:5452–5456`); `newcham_after_unleash` re-chained to C order (light → ustuck → worm/newsym → SHOW_MSG → vampire cham → possibly_unwield → mon_break_armor → mselftouch → check_gear → boulders → poly_steed → Elbereth); `l_oldname` threaded through `newcham_apply_form` for the break-out name. 5 new edges (`expels`/`unstuck`/`digests`←`mhitu.js`, `mselftouch`←`trap.js`, `You`←`zap.js`, C-locus `sticks`←`engrave.js`, `swallowed` joins the existing `display.js` edge; `W_ARMG`/`ARTICLE_NONE` join the `const.js` edge) all `imports.mjs --can` SAFE (hoisted declarations); local `AT_ENGL = 11` per monattk.h (cf. `monmove.js:153`). No DIAG/FORCE/seed logic; Rule #2 clean; sync-boolean contract for NO_NC_FLAGS preserved (D-1648).
**Verify:** `node scripts/verify.mjs --fn newcham` → PASS syntax (1 file: makemon.js) · rule2 · hidden note (0 blocked at baseline) · **REACH smoke spread 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** none inside the function. Caller-function gaps above stay named (vamp_shift/engulfer-digest have no JS body; sp_lev fixup is D-2249).
**Next:** same-file Open row `mon.c` xkilled (PARTIAL) stays queued for its own iteration.
## 2026-09-18 — D-2432 `polyself.c` polymon whole body `:735–1071` in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/polyself.c:735–1071` (`polymon`); same-file staticfn `check_strangling :167–194`; hero arm of `mondata.c can_be_strangled :590–619`; inlined 8-line `dbridge.c:76 is_pool_or_lava`; `artifact.c:2639 retouch_equipment` (named, own row).
**JS:** `js/polyself.js:53–76` (import block), `:260–263` (`PM_GREEN_SLIME`/`AMULET_OF_STRANGULATION`/`no_longer_petrify_resistant`), `:1181–1225` (`check_strangling`), `:1246–1620` (new body); map `docs/c-js-map/turns.md:291`.
**Change:** `js/polyself.js` only — restart of the thin body in C order: entry `sticking`/`wasHidingUnder`/`wasExpelled`/`ustuckNam` locals; first-poly `livelog_printf(LL_CONDUCT)`; `unmul('')` mimic-gold stop; Stoned→`PM_STONE_GOLEM` redirect + `make_stoned`; post-set_uasmon Stone/Sick cures + Slimed burn/silent arms; `check_strangling(FALSE)`; `nohands→make_glib(0)`; `hideunder`; pit `set_utrap(rn1(6,2))`; swallow expel / ustuck release / `uunstick` triple; steed `rnl(3)` petrify + `DISMOUNT_POLY`; find_ac#2 moved to true `:967` pre-pool site (supersedes D-2402, verify-guarded); pool `spoteffects`; Passes_walls/lava/amorphous/web/webmaker arms; `check_strangling(TRUE)`; `selftouch` when bare-handed. Module-local `async check_strangling` (same-file staticfn, no clone); `is_pool_or_lava` inlined at its only site (no clone #4). 4 new edges (`pline`/`potion`/`dig`/`pickup`) all `imports.mjs --can` SAFE; rest join existing edges. Rule #2 clean, no DIAG/FORCE/seed logic.
**Verify:** `node scripts/verify.mjs --fn polymon` → PASS syntax (1 file) · rule2 · hidden note (0 blocked at baseline) · **REACH 38/38 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · VERIFY: PASS (full skipped: no script-defined shared file).
**Named:** `retouch_equipment(2)` (`artifact.c:2639`, own coverage row); light-source bookkeeping (set_uasmon map note); break_armor horns/flimsy-helm/ublindf (break_armor map note); C FIXME early-returns after expels()/spoteffects() rehumanize kept as comments (no JS rehumanize signal to branch on).
**Next:** same-file Open rows `dogaze` (MISSING) + `dospinweb` (MISSING) + `rehumanize` (THIN) still queued for later iterations.
## 2026-09-18 — Architect: breadth phase opened (Constitution §10.17); loop relaunched after STOP at #3160

**Why:** leaderboard 2026-09-17 — held-out **11/44, 5,637/11,265 pts, RNG 26.6 %, screens 50.0 %** (rank 4/22, 2nd agentic; best agentic 35/44, 93 % screens) while the local corpus read 495/540 (91.7 %) and its 45 residuals are parked symptom owners needing C instrumentation. The corpus went 48 % → 91.7 % over 2026-09-06..17 for +4 held-out sessions: saturated and unrepresentative. `port-coverage.mjs`: 2,345/4,868 pinned-C functions MISSING/THIN. Human decision: **port the game as completely as possible first**, corpus-driven debugging second.
**Process changes (one-shot, architect):** picker = `node scripts/port-coverage.mjs --rows N` (new mode: LOOP-QUEUE rows measured on the JS tree, live rows / by-design names skipped, `split?` flag) under a new `## Open — coverage` section; one **whole C function** per iteration, 200–800 lines; supervisor caps 600/10 → **1500/15**, iteration timeout 3600 → 5400 s; `[measure]` rows moved to a closed `## Phase 2` section; corpus-residual rows ship only with a same-C-file coverage row. `hidden-proxy verify <fn>` now also runs **REACH** (baseline-PASS sessions whose C RNG log executes `<fn>`, spread ≤ 80 / `--reach-all`; smoke spread otherwise) and `verify.mjs` FAILs on any PASS→FAIL. New `scripts/leaderboard.mjs` (held-out line for audits). Cadence prompt: leaderboard row + scoreboard PASS→FAIL = Must-fix; no scenario-gen / queue refill (phase 2). Review prompt: arm-only port of a coverage row = QUALITY-RISK; re-measure with `--reach-all`. Edited: Constitution §10.13/§10.17, Cursor rule, playbook §1/2/2a/2b/4/5/6/11, port/cadence/review/continue prompts, runbook density, CURRENT (Held-out row, objective), LOOP-QUEUE (header, sections, 14 coverage rows), HIDDEN-PROXY (demotion + REACH), PORT-GAP-HELDOUT (126/126 levels done), PORT-GAP-TOP30 (snapshot notice), NOTES (phase hypothesis + falsifier), AGENT-PORT-LOOP (knobs), check-hot-docs (evidence class).
**Falsifier:** `node scripts/leaderboard.mjs` at ≈ iteration 3190 — held-out must move while MISSING/THIN falls; otherwise revisit the picker.
