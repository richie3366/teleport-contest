# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
## 2026-09-15 — Audit 828e88b4..5c766aef (reviews 1348–1353) + cadence 44/44

1348 D-2382 findtravelpath → **ACCEPT** (visited set + VALID mark-only +
Underwater gate; UNSURE deferral is a forced async-You adaptation,
message-before-step preserved). 1349 D-2383 region_danger/safety →
**ACCEPT** (REG_HERO_INSIDE bit; blind tail + breathing dual-write vs
youprop.h). 1350 D-2384 back_on_ground → **ACCEPT** (matrix arm-for-arm;
hliquid('lava') fires the lava arm on both sides). 1351 D-2385 use_skill
→ **ACCEPT** (5 C callers = 5 awaited JS sites; spell clone retired to
the canonical export). 1352 D-2386 TIP_GETPOS → **ACCEPT** (+ committed
handle-tip.test.mjs 3/3; D-log `:1583–1587` cite is the JS lines, C arm
is `:1871–1873` — doc nit only). 1353 D-2387 gulpmu BLND → **ACCEPT**
(check_visor tail; HBlinded≡uprops mirror). Per-SHA `--base` re-verify
0/0 throughout, rulecheck/banned-grep clean. Cadence 44/44 (Scr 11,405,
RNG 792,838, `48+0.30/turn`). No Must-fix; 11 Open in band, no refill.
## 2026-09-15 — Audit 3f602af2..a1b96e3f (reviews 1346–1347) + cadence 44/44

1346 D-2380 → **ACCEPT** (1339 Must-fix retired as ordered; `were.js:57`
verified vs `youprop.h:355–360`). 1347 D-2381 float_up → **ACCEPT**
(buried_ball/Lev_at_will/surface/WEB/Flying arm-exact; fixes latent
`TT_WEB`-vs-`WEB` + `Flying_fu`-sans-steed-flyer C-wrongs). Per-SHA verify
0/0, rulecheck/banned-grep clean. Cadence 44/44 (Scr 11,405, RNG 792,838,
`65+0.43/turn`). No Must-fix; 9 Open in band, no refill.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — D-2392 uhitm.c mhitm_ad_slow mhitm + uhitm arms: the two slow branches behind the D-2043 mhitu arm

**C locus:** `nethack-c/upstream/src/uhitm.c` `mhitm_ad_slow :3652–3687` — gate FALSE always `:3657`, `defended(mdef, AD_SLOW)` early return `:3659–3660`, uhitm branch `:3662–3670` (`!negated && mspeed!=MSLOW` → `mon_adjust_speed(mdef,-1,NULL)`, changed+canseemon → plain `pline "slows down."`), mhitm branch `:3676–3686` (same adjust, then `mstrategy &= ~STRAT_WAITFORU :3682`, changed+vis+canspotmon → `pline_mon "slows down." :3683–3684`); leftover `d()` kept in both (no zero, no done). Dispatch `case AD_SLOW` in `mhitm_adtyping :4819`; `damageum` tail clears WAITFORU for every arm (`:4859`). `worn.c mon_adjust_speed` (`:488+`) prints its own vis-gated "seems to be moving slower." (`:557–558`, +learnwand `:562`) — so the arm's second line is C-exact, not a double print (JS muse.js D-0871 already carries message #1).
**JS:** 3 js files (`mhitm.js` +51, `uhitm.js` +34, `mhitu.js` +2/−1) + map line, ~89 insertions total, under the 600/10 caps. No new module edges, no DIAG/FORCE/seed gates (Rule #2 clean); no committed probes.
**Change:** `js/mhitm.js` — exported `mhitm_ad_slow` in exact C mhitm-branch order (gate first, `|0` mspeed/MSLOW guard, oldspeed snapshot, `await mon_adjust_speed(mdef,-1,null)`, WAITFORU clear, `_mm_vis && canspotmon` + `pline_mon` on change) + `mdamagem` AD_SLOW block in the keep-leftover AD_PLYS shape (falls to shared knockback+HP tail). `js/uhitm.js` — `damageum_ad_slow` in exact C uhitm-branch order (gate, adjust, canseemon + plain `pline` on change; no WAITFORU — damageum tail `:4859` owns it) + `damageum_adtyping` AD_SLOW row after AD_PLYS. Same-edge import extensions only (`muse.js`: `mon_adjust_speed` onto the live `munslime` edge in both files — `imports.mjs --can` → ALREADY, static cycle mhitm↔muse pre-exists via `muse.js:109`/`mhitm.js:119`, hoisted declarations called at runtime only; `const.js`: `MSLOW`; file-local `AD_SLOW = 13`, monattk.h, mhitu.js:196 precedent).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_slow` → `PASS syntax 3 changed js file(s)` · `PASS rule2` · `note hidden verify mhitm_ad_slow: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks — brief lists none — so no --base owed; ship is on the C citation + public gates, stated honestly here, not as a corpus PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last code edit; the one later edit (docstring `:4855`→`:4859` line-number correction) is comment-only — re-ran `node --check` on all three files after it (SYNTAX-OK), no behavior touched (no D-1831 gap).
**Named:** `defended(mdef, AD_SLOW)` early return (`:3659–3660`, RNG-free wielded-artifact / blue-scales arm) stays deferred with the fire/cold defended omits per D-2043 — named identically in all three arm docstrings; `mhitm_ad_drst` mhitm dispatch arm (D-2043-named, different C function, own row); mhitu AD_SLOW gaze path (mhitu.js:497 note).
**Next:** pop the next Open row (`do.c` obj_no_longer_held, `brief obj_no_longer_held`).
## 2026-09-15 — D-2391 `weapon.c` autoreturn_weapon canonical + `thrwmu` always_toss arm

**C locus:** `nethack-c/upstream/src/weapon.c` `autoreturn_weapon` `:519–529` over `arwep[]` `:513–517` (`AKLYS_LIM` `:512` = `BOLT_LIM / 2`; `{ AKLYS, AKLYS_LIM², tethered }`; `{ BOOMERANG, 5, 0 }` commented out, so only AKLYS hits) + `mthrowu.c` `thrwmu` `:1241–1259` (`(arw = autoreturn_weapon(otmp)) != 0 && !mwelded(otmp)` short-circuit, `rang = dist2` vs `arw->range` then `!couldsee` return, `always_toss = TRUE`, `!lined_up || (URETREATING && (!always_toss && rn2(...)))`). All callees live: `dist2` (hacklib), `couldsee` (vision), `lined_up` (mthrowu D-2257), `mwelded` (wield.js `:179–182` ≡ `wield.c:1078–1084` owornmask&W_WEP + will_weld), `monshoot`/`mon_wield_item`/`select_rwep`/`nomul` (existing thrwmu_body edges).
**JS:** 4 js files (`weapon.js` +15/−1, `mthrowu.js` +39/−15ish across imports/clone/arm/gate/docs, `dothrow.js`/`monmove.js` import+clone each) = 48 insertions total + new `scripts/autoreturn-weapon.test.mjs` (node:test, query-classes-tally style, 4 tests) + `CURRENT.md` Next-cluster line + map section. Under 600/10 caps. Density note: C is ~28 lines across table + arm; JS proportional with citations (small-C exception, D-2385–D-2389 precedent).
**Change:** `js/weapon.js` — canonical exported `autoreturn_weapon` (C order: null guard, `otyp('AKLYS')` compare, `{ otyp, range: AKLYS_LIM², tethered: 1 }`; `BOLT_LIM`/`AKLYS_LIM` join the const.js import; `otyp`/`objectNames` already in-module, no new edge). `js/mthrowu.js` — imports `autoreturn_weapon` (weapon.js) + `mwelded` (wield.js), both `imports.mjs --can` → ALREADY (same SCC, call-time use only, no top-level TDZ read); local clone deleted; `thrwmu_body` gains the `:1241–1247` arm in C short-circuit order (autoreturn before !mwelded; range before couldsee) and the `:1255–1259` gate as `!always_toss && rn2(...)` so the retreat roll is not drawn for tethered AKLYS. `js/dothrow.js` + `js/monmove.js` — local clones deleted, canonical import added to the existing weapon.js import (throwit/m_throw/m_balks call sites unchanged).
**Verify:** `node scripts/verify.mjs --fn autoreturn_weapon` → VERIFY: PASS — `PASS syntax 4 changed js file(s)` · `PASS rule2` · `note hidden … no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared weapon.js changed). `node --test scripts/autoreturn-weapon.test.mjs` → 4/4 (null input, non-returning DAGGER, BOOMERANG commented-row null, AKLYS range 16 + tethered). Pre-change failure state: no `autoreturn_weapon` export in `js/weapon.js` (three uncited clones) and no always_toss path in `thrwmu_body` — the retreat `rn2` was observably drawn for AKLYS throws.
**Named:** `thrwmu` polearm arm (`mthrowu.c:1195–1240` is_pole/MON_POLE_DIST/couldsee/canseemon + mswings_verb/dmgval/thitu; AKLYS is never is_pole so the shipped subset is exact; JS + map docs named; own row when a falsifier fires). `select_rwep` throw-and-return preference walk (`weapon.c:585–605` arwep `oselect` + propellor force-wield; map turns.md deferred line stands).
**Next:** pop the next Open row (`uhitm.c` mhitm_ad_slow mhitm-arm + dispatch). Refill note: queue 10→9 Open on archive — in band (8–12), no refill; map singletons stay gated at corpus 88.5 % < 90 %.
## 2026-09-15 — D-2390 `vault.c` wallify_vault body restores vault boundary walls

**C locus:** `nethack-c/upstream/src/vault.c` `wallify_vault` `:646–731` (boundary-ring scan with interior skip; `:666–668` non-wall-or-gold-or-rock gate + `!in_fcorridor`; `:669–674` tame `yelp` + `rloc(RLOC_MSG)` else `m_into_limbo`; `:676–679` `move_gold` into `EGD(grd)->vroom`; `:682–691` ROCK/BOULDER extract+`obfree` loops; `:690–691` `deltrap`; `:693–702` corner/HWALL/VWALL typ by side; `:704–716` `wall_info=0` + `xy_set_wall_state` + `del_engr_at` + IN_SIGHT|COULD_SEE `newsym` pulse with viz restore + `block_point`; `:721–730` whisper vs distant-chant + gold-moved + `pline_The` walls-restored tail). Callers `:855` (`gd_move_cleanup`, already wired in JS) and `:913`/`:920` (hostile `gd_move`, still early-return in JS — named, not this row).
**JS:** 1 js file (`vault.js` +127/−15 across imports, ROCK/BOULDER consts, body, cleanup await, header + `gd_move` doc touch-ups) + `CURRENT.md` Next-cluster line + map section. Under 600/10 caps.
**Change:** `js/vault.js` — full async `wallify_vault` in C order/branch structure (boundary-ring loops with interior `continue`; `IS_WALL || g_at || sobj_at(ROCK) || sobj_at(BOULDER)` gate short-circuit + `!in_fcorridor`; `m_at` occupant arm with `await yelp` + `await rloc(RLOC_MSG)` else dynamic `m_into_limbo` (clear_fcorr idiom); same-file `move_gold`; ROCK then BOULDER extract+`obfree(rocks, null)` re-scan loops; `t_at`/`deltrap`; side-ordered corner typ; `wall_info=0`; `del_engr_at`; viz-row save/set/restore around `newsym`; `block_point`; tail `in_fcorridor(grd, mx, my) || cansee` whisper (`noit_Monnam`) vs `You_hear('a distant chant.')`, gold-moved pline, walls-restored as plain pline with the The-phrase per the `read.js:1771`/`lock.js` convention). New static edges `m_at` (`mon.js`) + `obfree` (`shk.js`) both `imports.mjs --can` → SAFE (hoisted, same SCC, call-time use only); `g_at`/`sobj_at`/`block_point`/`You_hear`/`IN_SIGHT`/`COULD_SEE` join ALREADY-imported modules; ROCK/BOULDER via `objectNames.indexOf` (apply.js/dig.js idiom). `gd_move_cleanup` now `await`s it.
**Verify:** `node scripts/verify.mjs --fn wallify_vault` → VERIFY: PASS — `PASS syntax 1 changed js file(s)` · `PASS rule2` · `note hidden … no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** `xy_set_wall_state` paint (mklev.js file-local, not exported — `wall_info=0` set exactly, same deferral as `invault`; map + JS doc named); hostile-`gd_move` `:913`/`:920` `wallify_vault` calls (`gd_move` still early-returns when `!mpeaceful`; own row when a falsifier fires).
**Next:** pop the next Open row (`weapon.c` autoreturn_weapon always_toss). Refill note: queue 11→10 Open on archive — in band (8–12), no refill; map singletons stay gated at corpus 88.5 % < 90 %.
## 2026-09-15 — D-2389 `do_wear.c` better_not_take_that_off stoning-corpse gloves guard + `carrying_stoning_corpse`

**C locus:** `nethack-c/upstream/src/do_wear.c` `better_not_take_that_off` `:2989–3010` (corpse scan; `st_corpse|st_petrifies` guard `:3002` deliberately omitting `st_resists` per the `:2995–3000` comment; `Snprintf` prompt with `gloves_simple_name(otmp)` + `obj_pmname(corpse)`; `paranoid_ynq(TRUE, buf, FALSE) != 'y'`); sole caller `select_off` `:2742–2743` (inside the `otmp == uarmg` arm, after welded/Glib, before the mask set); `invent.c` `carrying_stoning_corpse` `:1507–1516` (first invent CORPSE with `touch_petrifies`); `hack.h:857–861` enum (`st_corpse` 0x2, `st_petrifies` 0x4); `cmd.c:5595` (`paranoid_ynq(TRUE,…)` always takes the typed-"yes" getlin path). All five callees live in JS: `u_safe_from_fatal_corpse` (`pickup.js:1077`), `gloves_simple_name` (`objnam.js:1248`), C-faithful `obj_pmname` as `obj_pmname_corpse` (`objnam.js:1118`, aligned-cleric remap incl.), `touch_petrifies`+`mons` (`monsters.js`), `paranoid_ynq` (`getline.js:1301`, async).
**JS:** 2 js files (`do_wear.js` +52/−3: 2 imports extended, CORPSE const, two helpers with C citations, 1-line wiring + doc updates; `pickup.js` 2-line export keywords) + new `scripts/better-not-take-that-off.test.mjs` (node:test, handle-tip/query-classes-tally style); under 600/10 caps. Density note: C is 32 lines across both functions; JS is proportional with citations (small-C exception, D-2385–D-2388 precedent).
**Change:** `js/do_wear.js` — new `carrying_stoning_corpse()` (C invent walk over `game.invent`, `CORPSE` + `touch_petrifies(mons(corpsenm))`; exported: C declares it extern, `extern.h:1394`, unlike the staticfn below) + new async `better_not_take_that_off(otmp)` (C order: scan, `!u_safe_from_fatal_corpse(corpse, st_corpse|st_petrifies)`, prompt, `!== 'y'` blocks) wired into `select_off` at the C site (`if (await better_not_take_that_off(otmp)) return 0;` after Glib, inside the uarmg arm). `st_corpse`/`st_petrifies` exported from `js/pickup.js` (values unchanged, single source of truth — no magic literals); `u_safe_from_fatal_corpse`, `paranoid_ynq`, `obj_pmname_corpse`, `touch_petrifies`, `mons` join existing static edges (`imports.mjs --can` → ALREADY ×5, same SCC, no top-level TDZ read — all call-time uses). No clone #2 of `obj_pmname` (trap.js local untouched) or `select_off`.
**Verify:** `node scripts/verify.mjs --fn better_not_take_that_off` → VERIFY: PASS — `PASS syntax 2 changed js file(s)` · `PASS rule2` · `note hidden … no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `skip full (no shared file changed)`. `node --test scripts/better-not-take-that-off.test.mjs` → 6/6 (empty/missing invent null, non-stoning skip, cockatrice+chickatrice find, first-match order, found corpse fails the guard + prompt nouns, lichen corpse passes). Pre-change failure state: both functions absent from `js/` (brief NOT FOUND) — the prompt was observably never asked.
**Named:** none new — ring-arm `gloves_simple_name` gauntlets variant, `cloak_simple_name` robe, `surface()` infloor noun stay map-named in the `select_off` doc (other arms' rows when a falsifier fires). Trap.js `obj_pmname` local untouched (its minstapetrify callers keep it; `obj_pmname_corpse` is the canonical C-faithful export per `do.js:2333`).
**Next:** pop the next Open row (`vault.c` wallify_vault body). Refill note: queue 12→11 Open on archive — in band (8–12), no refill; map singletons stay gated at corpus 88.5 % < 90 %.
## 2026-09-15 — D-2388 `pickup.c` query_classes own-row: canonical tally, m_seen persistence, simple_look impossible

**C locus:** `nethack-c/upstream/src/pickup.c` `query_classes` `:140–262` (m_seen `:158` + ask_again `:198–201`; ESC `*inbuf == '\033'` `:205`; ':' cknown `:219–220`; 'm' -2/-3 `:249–253`); `simple_look` `:75–98`; `invent.c` `tally_BUCX` `:3580–3616` (cleric force `:3593–3595`; ocnt tallied but unused — no ilet either side) + `count_unpaid` `:3525–3538` (nobj walk; JS `place_object` threads the same level-wide fobj chain, so the floor-head 'u' ilet is already exact — D-2350's "pre-existing" note dissolves, no code).
**JS:** 2 js files (`pickup.js` +21/−23 incl. clone deletion, `invent.js` 1-line doc touch) + new `scripts/query-classes-tally.test.mjs` (node:test, handle-tip.test.mjs style); under 600/10 caps. Density note: residual-only iter — the 122-line body shipped D-1581/D-1620/D-2350; this retires the last clone + two one-line C-order fixes (small-C exception, D-2386/D-2387 precedent).
**Change:** `js/pickup.js` only (+ import line): `query_classes` calls canonical `tally_BUCX(objs, here)` from `./invent.js` (`imports.mjs --can` → ALREADY, same SCC, no new edge) and the local clone is deleted (D-2347/D-2349 idiom); in-loop `m_seen` reset removed with a C ask_again citation; `simple_look` null arm now `await impossible('simple_look(null)')` (`impossible` already imported). ESC left as `=== '\x1b'`: JS getlin returns exactly that on empty-ESC cancel and clears nonempty ESC (`getline.js:239/273–276`), so first-char vs exact-match is observationally identical — documented in the function docstring, not churned. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn query_classes` → VERIFY: PASS — `PASS syntax 2 changed js file(s)` · `PASS rule2` · `note hidden verify query_classes: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` · `skip full (no shared file changed)`. `node scripts/verify.mjs --full` → `PASS full 44/44 passing`. `node --test scripts/query-classes-tally.test.mjs` → 3/3 (non-priest X/no-mutation control, priest force + B/U counts with coin exempt, nexthere floor walk). /tmp wiring probe (pickup.js loads with the new import) PROBE PASS; probe deleted.
**Named:** none new — `count_unpaid` floor-head nobj note retired as already-exact (see C locus); qbuf has no safe_qbuf truncation in C either (`Sprintf`, `:202–203`); 'i'/':' re-prompt and There/You no-class texts verified arm-for-arm, unchanged.
**Next:** pop the next Open row (`pickup.c` in_or_out_menu more-containers 'n'). Refill note: queue 9→8 Open on archive — in band (8–12), no refill; map singletons stay gated at corpus 88.5 % < 90 %.
## 2026-09-15 — D-2387 `mhitu.c` gulpmu BLND visored-helmet tail + flat/uprops mirror

**C locus:** `nethack-c/upstream/src/mondata.c:305–398` (`can_blnd`: `check_visor` set only by the BLINDING_VENOM obj arm and AT_CLAW; tail `:388–396` scans `gi.invent`/`minvent` for `owornmask & W_ARMH` + `objdescr_is(o, "visored helmet")` → FALSE; AT_ENGL leaves `check_visor` FALSE so engulf never scans); `nethack-c/upstream/src/mhitu.c:1471–1484` (AD_BLND arm; `:1482` `incr_itimeout(&HBlinded, 1L)` on the already-blind path); `nethack-c/upstream/include/youprop.h:87` (`HBlinded` ≡ `uprops[BLINDED].intrinsic`) + `:96` (`Blindfolded` ≡ `EBlinded`, already matched by the ENGL arm's `EBlinded` read).
**JS:** 1 js file (`mhitu.js` +49/−8: const names +1 import line, helper + tail, mirror block) + `CURRENT.md` Next-cluster rotation; under 600/10 caps. Density note: C is the 8-line visor tail + 1-line incr; JS is the flag + scan helper + dual-write with C citations (small-C exception, D-2385/D-2386 precedent).
**Change:** `js/mhitu.js` — `gulpmu_can_blnd` gains C's `check_visor` flag (set in the CLAW arm only) + the `:388–396` tail via new file-local `visored_helmet_worn()` (`owornmask & W_ARMH` + imported `objdescr_is` "visored helmet" over `game.invent` in both stored shapes plus a `u.uarmh` fallback); AT_ENGL returns before the tail with `check_visor` FALSE, so the live engulf path is behavior-identical by construction. Else-branch now dual-writes flat + `uprops[BLINDED].intrinsic` TIMEOUT bits (the `do.js` `set_itimeout_HBlinded` convention). Imports: `W_ARMH`/`BLINDED` join the existing `const.js` edge (no new module edge); `objdescr_is` from `./apply.js` — `imports.mjs --can mhitu.js apply.js objdescr_is` → SAFE (hoisted function, same SCC, no top-level TDZ read); `node -e "await import('./js/mhitu.js')"` → MHITU_LOAD_OK.
**Verify:** `node scripts/verify.mjs --fn gulpmu` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mhitu.js` · `PASS rule2` · `note hidden verify gulpmu: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` · `skip full (no shared file changed)`. No maintained unit harness exists for game logic (no `tests/` dir; sessions + corpus gates are the suite), so no new test file per the no-new-framework rule; both arms are suite-unreached by construction (no caller passes CLAW/VENOM to the gulpmu helper — its `obj` is always NULL per the two call sites; the mirror needs an already-Blind hero inside an AD_BLND engulfer).
**Named:** `uhitm.js` exported `can_blnd` + `mhitu.js` `can_blnd_u` keep their own visor defers (other functions' rows when a falsifier fires — not this row). VENOM-obj arm in `gulpmu_can_blnd` stays `return false` (always-NULL obj at both call sites — C-identical).
**Next:** pop the next Open row (`pickup.c` loot_in_first 'r' reversed + explain_container_prompt).
## 2026-09-15 — D-2386 `hack.c` handle_tip TIP_GETPOS arm + getpos call-site + `l_nhcore_call` dispatch

**C locus:** `nethack-c/upstream/src/hack.c:1852–1881` (TIP_GETPOS arm `:1871–1873` `l_nhcore_call(NHCORE_GETPOS_TIP); break` → `return TRUE`); `getpos.c:838–839` (`if (handle_tip(TIP_GETPOS)) show_goal_msg = TRUE`); `nhlua.c:169–192` (`l_nhcore_call`: pcall `nhcore.<name>` when `LUA_TFUNCTION`, else `available[callidx] = FALSE`); `dat/nhcore.lua:108–121` `show_getpos_tip` text + `:138` `getpos_tip` registration; `include/context.h:15–20` TIP enum (`TIP_GETPOS` = 3, `NUM_TIPS` = 4).
**JS:** 3 js files (`hack.js` +10/−5, `do.js` +9/−4, `getpos.js` +4/−9) + new `scripts/handle-tip.test.mjs` (node:test guard pins, same style as `loop-raw.test.mjs`) + `turns.md:410` deferred-line retirement; under 600/10 caps. Density note: the C arm is 3 lines; the JS is the arm plus its two chain ends (caller + Lua-dispatch) required for the arm to function — small-C exception (D-2385 precedent).
**Change:** `js/hack.js` — TIP_GETPOS arm in C switch order (`await l_nhcore_call(NHCORE_GETPOS_TIP); return true`, `:1583–1587`) + `NUM_TIPS` range check; `js/do.js` — GETPOS_TIP dispatch (`await show_getpos_tip(); return`, `:1108–1113`, the C pcall-function-exists path) with the generic tail simplified to the unconditional C else-arm disable; `js/getpos.js` — `show_getpos_tip` exported (`:1140`) and the call site is now the C line (`if (await handle_tip(TIP_GETPOS)) show_goal_msg = true`). Imports: `TIP_GETPOS`/`NUM_TIPS`/`NHCORE_GETPOS_TIP` from `const.js`, `l_nhcore_call` over the existing `hack.js → do.js` edge, `handle_tip` over the existing `getpos.js → hack.js` edge, one new `do.js → getpos.js` edge inside the same 90-module SCC with runtime-only reads (no top-level TDZ read; all three modules import clean in Node). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn handle_tip` → VERIFY: PASS — `PASS syntax 3 changed js file(s)` · `PASS rule2` · `note hidden verify handle_tip: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` · `PASS full 44/44 passing` (auto: shared file changed). `node --test scripts/handle-tip.test.mjs` → 3/3 (tips-disabled sets no bit, preset-bit once-only without display, out-of-range). The enabled first-time arm needs `nhgetch`, so it is covered by session verify, not the unit test (stated in the test file).
**Named:** `game.context.tips` is not save-persisted in JS (shared by all four tips since D-1963; C `svc.context.tips` persists) — systemic, own row when a falsifier fires. `getpos` `msg_given` default-true vs C verbose-gated — pre-existing, out of scope.
**Next:** pop the next Open row (`mhitu.c` gulpmu BLND visored-helmet residual). Refill note: queue 8→7 Open on archive; all 30 live corpus-queue owners verified this iter against LOOP-QUEUE Parked + NOTES/CURRENT parks (symptom/misattributed classes), and map singletons stay gated at corpus 88.5 % < 90 % — no filler invented.
## 2026-09-15 — D-2385 `weapon.c` use_skill may-advance arm + async caller cascade

**C locus:** `nethack-c/upstream/src/weapon.c:1424–1434` (`if (skill != P_NONE && !P_RESTRICTED(skill)) { advance_before = can_advance(skill, FALSE); P_ADVANCE(skill) += degree; if (!advance_before && can_advance(skill, FALSE)) give_may_advance_msg(skill); }`; `skills.h:118` `P_RESTRICTED ≡ skill == P_ISRESTRICTED`). Callers: `dokick.c:122`, `hack.c:2086`, `spell.c:1599`, `steed.c:395`, `uhitm.c:1498`. Callees live: `can_advance` (`js/weapon.js:796`, sync) + `give_may_advance_msg` (`js/weapon.js:856`, async — pline can reach nhgetch, D-1963).
**JS:** 7 js files (`weapon.js` +12/−9, `spell.js` +2/−12 incl. clone removal + import, `uhitm.js` +4/−3, `steed.js` +3/−3, `hack.js`/`dokick.js`/`cmd.js` +1/−1 each) + `turns.md` 4 deferred-line updates; under 600/10 caps. Density note: C is 10 lines; the JS is the arm plus the five-caller async cascade and the clone retirement (small-C exception, D-1963 precedent).
**Change:** `js/weapon.js` — `use_skill` is now `async`, C order (`P_NONE`/`P_ISRESTRICTED` guards, `advance_before = can_advance(skill, false)`, `+= degree|0`, `!before && can_advance` → `await give_may_advance_msg(skill)`); stale "still deferred" doc on `enhance_weapon_skill` updated. Cascade (all five C callers): `hack.js` web-cut `await`, `dokick.js` kickdmg (async) `await`, `uhitm.js` `hmon_hitmon_dmg_recalc` → async + `await` with single caller `hmon` (async) awaiting, `steed.js` `exercise_steed` → async + `await` with single caller `cmd.js` domove (async) awaiting, `spell.js` clone deleted + `import { use_skill } from './weapon.js'` + `spelleffects` (async) `await`. `imports.mjs --can spell.js weapon.js use_skill` → SAFE (hoisted function; same 90-module SCC, no top-level TDZ read).
**Verify:** `node scripts/verify.mjs --fn use_skill` → VERIFY: PASS — `PASS syntax 7 changed js file(s)` · `PASS rule2` · `note hidden verify use_skill: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` · `PASS full 44/44 passing` (auto: shared file changed). /tmp hand probe (deleted before handoff): P_DAGGER BASIC 79→80 fires Tip + tips-bit, 10→11 silent, 80→81 silent (already-true), P_NONE/restricted no-ops — PROBE PASS.
**Named:** `lose_weapon_skill`/`drain_weapon_skill` may-advance arm stays deferred (turns.md); PROJECTILE→launcher skillwep swap in the recalc stays (turns.md); TIP_GETPOS stays (Rule #2-hostile, turns.md:410).
**Next:** pop the next Open row (`hack.c` handle_tip TIP_GETPOS arm).
## 2026-09-15 — D-2384 `trap.c` back_on_ground full surface matrix + `pager.c` ice_descr

**C locus:** `nethack-c/upstream/src/trap.c:4975-5008` (over/on preposit; `surface()`; is_ice→ice_descr; strcmpi matrix floor/ground→solid ground, bridge/altar/headstone→`an`, stairs/lava/bottom→`the`, else→`in` + `an`/`the`-air; rescued→`You find yourself` else verbose→`You are back`/`Back`; `last_msg`) + `pager.c:ice_descr` (icetyp[6], xray neardist, ice_rating −1, non-ICE `[ice:%d?]`, far/unseen→waterbody_name unless decor_levitate_override, else MELT_ICE_AWAY rating→`%s %s`) + `dungeon.c:1750 surface` (already ported D-2008) + `rm.h:146 SURFACE_AT`.
**JS:** `js/trap.js` only, 1 js file, one new static edge (`sit.js` `surface` — `imports.mjs --can` SAFE, hoisted function declaration) + three same-edge name joins (`SURFACE_AT`→hack.js, `spot_time_left`→mkobj.js read lazily in the body, `is_ice`→zap.js); no new modules, no clones (brief-listed surface/is_ice/an/the clones untouched), no DIAG/FORCE/seed gates.
**Change:** `back_on_ground` ported arm-for-arm in C order over the shared `surface()` (sit.js D-2008) with the file-local `hero_Levitation()`/`hero_Flying()` preposit gate, `an`/`the` (objnam.js) matrix arms, `game.flags?.verbose !== false` (trap.js verbose idiom) and unchanged `last_msg`. New exported `ice_descr(x, y)` ports pager.c in C order (C `char icebuf[QBUFSZ]` out-param becomes the return value — every C caller uses it); C `distu` via the already-imported symmetric `dist2`, `u_at` via the existing const.js edge.
**Verify:** `node scripts/verify.mjs --fn back_on_ground` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/trap.js` · `PASS rule2` · `note hidden verify back_on_ground: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` (full skipped by the runner: no shared file changed). /tmp hand probe (deleted before handoff) against the real module graph: floor verbose/terse/rescued/levitate, near-ice wording + ice_rating 0, direct ice_descr, bridge, lava-rescued — 9/9.
**Named:** `surface()` uswallow maw/husk arm (stays the sit.js D-2008 note — fires only while swallowed by an animal). `ice_descr`'s other C callers (invent.c lookat, objnam.c, pager.c lookat, pickup.c probe_decor) keep their current JS wording until their own rows.
**Next:** pop the next Open row (`weapon.c` use_skill may-advance arm).
## 2026-09-15 — D-2383 `region.c` region_danger/region_safety use the REG_HERO_INSIDE bit + safety blind tail

**C locus:** `nethack-c/upstream/src/region.c:1341–1363` (`region_danger`: `!hero_inside` skip, gas_cloud-only, nonliving/Breathless + Poison_resistance skips) and `:1368–1405` (`region_safety`: same membership; `n>1||(n==1&&!r)` → `safe_teleds` + danger recheck → `set_itimeout(&HMagical_breathing, d(4,4)+4)`; single expiring → `remove_region`; gone message; `BlindedTimeout==1 → make_blinded(0L,TRUE)`); `include/region.h:17` (hero_inside bit); `include/youprop.h:270` (HMagical_breathing ≡ uprops slot), `:93` (BlindedTimeout ≡ HBlinded&TIMEOUT). Flag writers verified live in JS: `in_out_region` (C `:480–527`), `update_player_regions` (C `:582–592`), `add_region` (C `:334–337`) — all C-cited.
**JS:** `js/region.js` only, 1 js file, no new modules, no new static edges (one const name joins an existing edge; two runtime dynamic imports).
**Change:** both loops use the file-local `hero_inside(reg)` (no new edge, no TDZ — same-file function declaration); safety dual-writes flat + `uprops[MAGICAL_BREATHING].intrinsic` TIMEOUT bits (HBlinded precedent in `do.js`; `MAGICAL_BREATHING` joins the existing `const.js` edge); blind tail reads `(u.HBlinded & TIMEOUT) === 1` (`BlindedTimeout` is local to `do.js`) and dynamic-imports `make_blinded` from `./do.js` — same pattern as the existing `safe_teleds` dynamic imports, no new static edge. Docs + file header updated; `pline('The …')` kept (`pline_The` has no JS export; text-identical).
**Verify:** `node scripts/verify.mjs --fn region_safety` → VERIFY: PASS — `PASS syntax 1 changed js file(s)` · `PASS rule2` · `note hidden verify region_safety: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing`. Same tail for `--fn region_danger`. /tmp hand probe (deleted before handoff): flag-set/geo-miss → danger true, flag-clear/geo-hit → false, single-expiring safety removes region, empty-regions no-throw, HBlinded&TIMEOUT==1 cured to 0 — 5/5.
**Named:** none new — geometric residual, blind tail and breathing dual-write all retired. `pline_The` wrapper shape (pre-existing, text-identical); `safe_teleds` dynamic-import shape (pre-existing).
**Next:** pop the next Open row (`trap.c` back_on_ground full surface matrix).
## 2026-09-15 — D-2382 `hack.c` findtravelpath travelmap visited set + VALID mark-only + Underwater rock gate

**C locus:** `nethack-c/upstream/src/hack.c:1266–1523` (`findtravelpath`: entry alloc `:1268–1269`, TRAVEL/VALID success `:1400–1418`, GUESS no-guess mark `:1484–1487`) + `test_move` obstructed Underwater `:1016–1023`; `cmd.c` `reset_cmd_vars` `:3616–3618` and `hack.c` `end_running` `:4151–4153` (both free travelmap; end_running unconditionally).
**JS:** `js/cmd.js` (+~110/−40: helpers, BFS arm, tri-state returns, two wrapper mappings, VALID param, underwater gate, 3 import lines) · `js/hack.js` (+4/−1) · `js/mklev.js` (3 `export` words). 3 js files, no new modules, no new static edges (`--can` ALREADY on all four names).
**Change:** `js/cmd.js` — `travelmap_ensure()` (C `:1268–1269`; per-game heap on `game.travelmap`, never saved, like C) + `TRAVEL_NOPATH/STEP/STEP_UNSURE` tri-state (C returns boolean, but `You` is async-only in JS, so the sync BFS reports UNSURE and the async TRAVEL wrappers `continue_run`/`dotravel_target` print `You('stop, unsure which way to go.')` in C order); BFS success arm in exact C shape (visited read off current cell, dx/dy set either way, TRAVEL-only stop on dest-or-visited with travelcc-clear on fresh dest, `selection_setpoint(u.ux,u.uy)` + return on all three success shapes); `findtravelpath_bfs` takes `mode` (`TRAVP_TRAVEL`/`TRAVP_VALID` join the existing `const.js` edge; default TRAVEL); `is_valid_travelpt` passes `TRAVP_VALID` and maps to boolean (getpos stays sync); GUESS no-guess success marks travelmap (C `:1484–1487`); `reset_cmd_vars` clears `game.travelmap` (C `cmd.c:3616`); obstructed arm gains the all-modes `uinwater → return false` gate (review-941 debt). `js/mklev.js` — `export` on `selection_new`/`selection_getpoint`/`selection_setpoint` (file-local clones; `--can` ALREADY, no new edge, still hoisted). `js/hack.js` — `end_running` clears `game.travelmap` unconditionally + doc updated (no import needed; game-object write).
**Verify:** `node scripts/verify.mjs --fn findtravelpath` → VERIFY: PASS — `PASS syntax 3 changed js file(s)` · `PASS rule2` · `note hidden verify findtravelpath: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim) · `PASS green 2/2 passing` · `PASS strict` both gate sessions · `PASS cohort 7/7 passing` · `PASS full 44/44 passing (auto: shared file changed)`. No maintained unit harness exists for game logic (only `scripts/*.test.mjs` for loop scripts) — the sessions + corpus gates are the suite, so no new test file per the no-new-framework rule; the revisit arm fires only on travel loops (unreached by any suite, same as C) and its callees (`You`, `nomul`, `selection_*`) are live elsewhere.
**Named:** full `test_move` TEST_MOVE/DO_MOVE modes (adjacent + GUESS no-guess arms keep the `blocksMove`/`boulder_at` stand-ins); `may_passwall` rock gate, `worm_cross`, `block_entry` (diagonal-out hardcoded `false`), WAN_DIGGING-unknown two-in-row arm, Known_wwalking/Known_lwalking/WATERWALL/LAVAWALL (all pre-existing, unchanged); `found:` dx/dy-zero shape at the travel/guess callers (pre-existing); menu-getpos `#retravel` family (own row).
**Next:** pop the next Open row (`region.c` region_danger/region_safety geometric residual).
## 2026-09-15 — D-2381 `trap.c` float_up retires D-0956 residuals + dead-WEB C-wrong

**C locus:** `nethack-c/upstream/src/trap.c:3937–4006` (`float_up`); `dig.c:1884–1932` (`buried_ball`); `dungeon.c:1749–1759` (`surface` maw/husk first branch); `include/youprop.h:242–245` (`Lev_at_will`), `:253–255` (`Flying`); `include/trap.h:77` (WEB=18) vs `include/you.h:347–353` (TT_WEB=3).
**JS:** `js/trap.js` `float_up` (+doc) · `js/dig.js` export `buried_ball` · `js/mhitu.js` export `enfolds`. 3 files, +51/−19, no new modules or static edges.
**Change:** BURIEDBALL arm calls `buried_ball(cc)` (newly exported from `js/dig.js:514`, verified arm-for-arm vs C: `!u.utrap || TT_BURIEDBALL` gate, exact-spot return, dist2≤8 nearest + cc mutation) and reads `IS_ROOM` at the ball cell; steed arm ports `Lev_at_will` inline over the `HLevitation`/`ELevitation` flats (`I_SPECIAL`/`W_ARTI`/`TIMEOUT` value-matched to `prop.h`; bits maintained live at `js/artifact.js:1587`, `js/hack.js:3193–3194`, `js/potion.js:1267/1294`) with `Monnam` magically-floats-up vs dismount; uswallow-animal arm inlines the only reachable `surface()` branch (maw/husk/nonesuch via `mhitu.js` `digests` + newly exported `enfolds` — shared `sit.js` `surface` still names that arm); WEB arm kept literal-dead per C so TT_WEB falls through; tail uses canonical `mhitu.js` `Flying`. All cross-module names via runtime `await import` (no new static edges; `W_ARTI` joins the existing `const.js` edge).
**Verify:** `node scripts/verify.mjs --fn float_up` → PASS syntax (3 changed files) · rule2 clean · hidden vacuous note (0 blocked at HEAD; row cited 0 blocks) · green 2/2 · strict ×2 · cohort 7/7. Runtime shape probe (plain-node dynamic imports): `float_up`/`buried_ball`/`Flying`/`digests`/`enfolds` all `function`. No corpus PASS claimed.
**Named:** none new in `float_up` (D-0956 float_up residuals fully retired). Pre-existing surroundings untouched: file-local `Flying_fu` stays for `float_down` and below (own row when a falsifier fires); `flags.botl` extra set left as-shipped.
**Next:** Open `hack.c` still_chewing body (next queue head after this row archives).
