# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-06 — D-1909 pager.c Inhell_pager reads the dungeon hellish flag, not the Gehennom dnum

**C locus:** `dungeon.c` `In_hell` `:1941–1945` (`svd.dungeons[lev->dnum].flags.hellish`); `dungeon.h:140` `Inhell` = `In_hell(&u.uz)`; consumed in `pager.c:1232` (`idx != S_vibrating_square || Inhell || ...`).
**JS:** `js/pager.js` only (helper body + doc line + 1 import word); `docs/c-js-map/turns.md` one section.
**Change:** body now reads `!!(game.dungeons?.[game.u?.uz?.dnum | 0]?.flags?.hellish)` — the same flag expression as the `do.js`/`trap.js` siblings (same `| 0` undefined→0 fallback shape); doc comment cites `dungeon.c:1941–1945`. No new import (flag read is inline, zero TDZ risk); now-unused `GEHENNOM` dropped from the `const.js` import.
**Verify:** `node scripts/verify.mjs --fn Inhell_pager` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/pager.js`; `PASS rule2`; `note hidden verify Inhell_pager: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — review states latent/no live caller, brief confirms none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. No hand probe: single boolean predicate, byte-identical shape to two session-covered siblings, no live caller yet.
**Named:** none new — `do_screen_description` showsyms scan + `describe_looked` rewiring still deferred (own rows); hoisting one shared `In_hell` export (e.g. from `js/dungeon.js`) left for a future iter that touches all three clones together.
**Next:** next queue row in order (`mkmap.c` join_map + join_map_cleanup).
## 2026-09-06 — Audit D-1901…D-1908 against pinned C (reviews 871–878) + cadence score

7 ACCEPT (871 domindblast deletion branch-exact; 872 mkmap passes, DIRS/macros byte-identical; 874 role parsers incl. end-anchored priest/priestess check; 875 tty_putstr wrap re-measured blocked→PASS PROGRESS; 876 wiz-goal 14-empty count; 877 mhitm AD arms, all import edges pre-existing; 878 mkmap driver, C-order exact), 1 QUALITY-RISK (873: `Inhell_pager` checks `dnum===GEHENNOM` instead of C `dungeon.c:1941–1945` hellish flag — clone #3 diverging from siblings `do.js:1202`/`trap.js:604`; Must-fix prepended, CURRENT Next cluster set to it). Full sessions 44/44 fortress holds (Scr 11,405/11,405, RNG 792,838/792,838); hidden proxy 255/265 excl. env (suit_simple_name closed by D-1905, +1).
## 2026-09-06 — D-1908 mkmap.c mkmap + init_map/init_fill cavern assembly + RNG fill envelope

**C locus:** `mkmap.c` `init_map` `:23–34`, `init_fill` `:36–52`, `N_P1_ITER`/`N_P2_ITER`/`N_P3_ITER` `:438–440` (=1/1/2), `litstate_rnd` `:442–448`, `mkmap` `:450–486`; caller `sp_lev.c` `splev_initlev` `:3002–3008` LVLINIT_MINES arm.
**JS:** `js/mkmap.js` (+~110: 3 consts, 4 exports, 4 import lines, header); `js/mklev.js` (+2 words: `export` on `join_map_fixed`/`finish_map`); `docs/c-js-map/data.md` one section.
**Change:** canonical exports in `js/mkmap.js`: `N_P1_ITER`/`N_P2_ITER`/`N_P3_ITER`, `init_map` (C field order NO_ROOM/typ/unlit), `init_fill` (limit `(WIDTH*HEIGHT*2)/5`=624, `rn1(WIDTH-1,2)`/`rnd(HEIGHT-1)`, occupied cells retry drawing no extra RNG), `litstate_rnd` (depth-gated `rnd(1+|depth|)<11 && rn2(77)` short-circuit, boolean passthrough), `mkmap` driver in C order (lit resolve → init_map → init_fill → N_P1/N_P2 loops → smooth-gated N_P3 loop → join-gated join → finish → walled+join cavernous stamp). C `:460`/`:485` new_locations alloc/free maps to the D-1902 per-call scratch (each pass fully rewrites its region before copy-back; strictly sequential calls) — documented, no shared buffer to own. join/finish delegate to the live `mklev.js` clones via aliased import (newly exported, behavior-neutral); their canonical ports are the next two queued rows.
**Verify:** `node scripts/verify.mjs --fn mkmap` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mklev.js js/mkmap.js`; `PASS rule2`; `note hidden verify mkmap: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`. Import smoke: `node -e import('./js/mkmap.js')` → all four functions live, N_ITERS 1/1/2, no cycle. No hand probe: new exports are dead code (zero importers, like D-1902) and logic-identical to the session-covered live clones (same constants, same RNG call order).
**Named:** `join_map` + `join_map_cleanup` (own queued row; `somexy`-failure `impossible()` arm dropped by the `mklev.js` clone — adjudicated there, not here), `finish_map` (own queued row; ICED_POOL/ICED_MOAT encoding, wallify/lit arms), live cutover of `splev_initlev` MINES to `js/mkmap.js` + deletion of the `mklev.js` clones (queued with those rows).
**Next:** next Open queue row in order (`mkmap.c` join_map + join_map_cleanup).
## 2026-09-06 — D-1907 uhitm.c mhitm AD_SGLD/AD_TLPT/AD_WERE/AD_SLIM mon→mon arms + mdamagem dispatch

**C locus:** `uhitm.c` `mhitm_ad_sgld` `:2790–2857`, `mhitm_ad_tlpt` `:2859–2955`, `mhitm_ad_were` `:4265–4293`, `mhitm_ad_slim` `:3526–3599` (each `magr==&gy.youmonst` uhitm / `mdef==&gy.youmonst` mhitu / else mhitm); dispatched by `mhitm_adtyping` `:4782–4848` (`AD_SGLD`→sgld, `AD_TLPT`→tlpt, `AD_WERE`→were, `AD_SLIM`→slim); `monattk.h` `:62`/`:65`/`:71`/`:82` (20/23/29/40).
**JS:** `js/mhitm.js` (+248: 4 consts + exports, 3 import lines, 4 arms, 4 dispatch blocks); `docs/c-js-map/turns.md` one row.
**Change:** four exported C-order arms in `js/mhitm.js` (mhitm branches only, `is_youmonst` guards): sgld zeroes dice, `findgold`/`obj_extract_self`/`add_to_minv` gold move, WAITFORU clear, vis-gated steal pline, `!tele_restrict` attacker `rloc(RLOC_NOMSG)` with AGR_DONE + disappears pline; tlpt short-circuits (`mcan`/damage≥mhp/`tele_restrict`, no RNG) before `mhitm_mgc_atk_negated(TRUE)`, then WAITFORU clear + defender rloc + disappears pline (name saved first, `u.usteed` excluded) + clamp leftover below mhp (1-HP bump first); slim burns negation(FALSE) before the `rn2(4)` gate, then `munslime(FALSE)` + `newcham`→green slime (`NC_SHOW_MSG` when vis&&canseemon), HIT flag, AGR/DEF-DIED OR, dice zeroed; were delegates to local `mhitm_ad_phys`. Four `mdamagem` dispatch blocks in the file's envelope (knockback + done/damage/HP/monkilled/grow_up), WERE carrying `dieroll`. New static imports (all `imports.mjs --can` hoisted-safe): `findgold` (steal.js), `munslime` (muse.js), `obj_extract_self`/`add_to_minv` (mkobj.js), `M_ATTK_AGR_DONE` + `RLOC_NOMSG` (const.js).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_sgld --full` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mhitm.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify mhitm_ad_sgld: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (queue row is map-driven HELDOUT Tier C, cited no blocks, so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing`. No hand probe: no session reaches these arms; every callee call-shape matches session-covered neighbor arms (slee/drin/poly/phys).
**Named:** uhitm you-as-agr arms (sgld hero-inventory merge_choice/inv_cnt/addinv/dropy+exercise; tlpt u_teleport_mon; slim You-turn-to-slime; were shares the phys shape) and mhitu you-as-def arms (sgld hitmsg+stealgold; tlpt hitmsg+tele()+half-damage clamp; slim hitmsg+flaming/Unchanging/make_slimed; were hitmsg+rn2(4) lycanthropy/set_ulycn/retouch_equipment) — each named in its callee doc; `You`/`Your`/`pline_The` have no display.js export (file uses `pline`/`pline_mon` only). `hmon_hitmon` + `hmon_hitmon_do_hit`/`_jousting`/`_poison`/`_potion`/`_weapon`/`_splitmon` hero-attack helpers stay deferred (own work; queue row split, this ships the AD half).
**Next:** next Open queue row in order (`mkmap.c` mkmap + init_map/init_fill).
## 2026-09-06 — D-1906 mkmaze.c makemaz Wiz-goal random-object count (14 empty, not 15)

**C locus:** `dat/wiz-goal.lua` :73–87 (1 named Eye + 14 empty `des.object()`); `sp_lev.c` `lspo_object` :3557–3741 (every call falls through to exactly one `create_object`); same 1+14 shape in `dat/Bar-goal.lua` :43–57 (D-1819 loop is ×14).
**JS:** `js/mklev.js` only (1 bound + 2 comment lines).
**Change:** bound 15→14 with comment citing :74–87; Bar-goal comment corrected to the shared 1+14 shape.
**Verify:** `node scripts/verify.mjs --fn makemaz` → PASS syntax (mklev.js); PASS rule2; note hidden (vacuous — no corpus session blocked on makemaz at HEAD; row cited no blocks, HELDOUT content row, so no `--base` re-run owed); PASS green 2/2; PASS strict seed8000/seed0900; PASS cohort 7/7; PASS full 44/44 (auto: shared file changed). Counts audited against lua: map 20×76 byte-identical (probe, deleted), doors 16/16, regions 1 temple + 1 lit + 14 unlit + 1 lit, traps 6/6, monsters 28 + 8 captives.
**Named:** unchanged from D-1818 (humidity-aware `get_location`; `spo_end_moninvent` `m_dowear`; `fill_special_room` TEMPLE beyond `has_temple`; `G_UNIQ` extinct early return; fakewiz; `create_maze` fallback; `dmonsfree`).
**Next:** live `val-*`/`sam-*` rows next.
## 2026-09-06 — D-1905 wintty.c tty_putstr NHW_TEXT wrap remainder consumes break space (suit_simple_name corpus misattribution)

**C locus:** `win/tty/wintty.c` `tty_putstr` NHW_TEXT/MENU arm `:2412–2420` — `cw->data[cw->cury-1][++i] = '\0'` keeps the break space in the stored fragment, then `tty_putstr(window, attr, &str[i])` recurses from AFTER the break space (post-`++i`): the space is consumed, not kept. The recursion re-enters through `compress_str`, whose `strlen >= CO || \n` gate leaves a short tail untouched — so C's tail starts without the space. Feeder: `show_gamelog` (`insight.c :2562–2589`) formats `"%5ld: %s"` via `putstr` on an NHW_TEXT window; the 92-char compressed wish line breaks at the space after the second `gray` (index 73; head 74 chars incl. space, rtrimmed to 73 on screen).
**JS:** `js/pager.js` (+3/−3); `docs/c-js-map/turns.md` NHW_TEXT putstr row.
**Change:** `s.slice(i)` → `s.slice(i + 1)` + doc correction (`&str[i]` post-`++i`, space consumed). Same-file, no new imports. Falsifier before edit: `wrap_text_window_line` on the 92-char compressed wish line returned tail ` dragon scale mail"` — byte-identical to the failing JS screen row; after edit `dragon scale mail"` — byte-identical to C row 0.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** `suit_simple_name` dragon mail/scales arms still deferred (D-1884; `js/do_wear.js` canonical + `js/trap.js`/`js/invent.js` local clones) — no corpus session reaches them through a live diverging path; this row's block premise was the wrap, not the noun. NHW_MENU putstr parity still deferred (D-1892 row, untouched).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `wiz-goal`).
## 2026-09-06 — D-1904 role.c role-select parsers (str2role/str2race/str2gend/str2align, setrolefilter, root/build_plselection_prompt)

**C locus:** `role.c` `str2role :746–775` / `str2race :812–841` / `str2gend :879–904` / `str2align :942–967` / `validrole :712–716` / `setrolefilter :1284–1300` / `promptsep :1383–1396` / `role_gendercount :1398–1412` / `race_alignmentcount :1414–1428` / `root_plselection_prompt :1430–1580` / `build_plselection_prompt :1583–1656`; `randomstr :710`; `BP_ALIGN/GEND/RACE/ROLE`+`NUM_BP` `hack.h :1066–1070` (role.c-local, `#undef`'d); `options.c :3605` (keeps str2* result verbatim incl. ROLE_RANDOM).
**JS:** `js/roles.js`, `js/player_selection.js`; `docs/c-js-map/startup.md` rows 12–13.
**Change:** `js/roles.js` (+~150): index-aligned `ROLE_FILECODES` attached to `roles[]`; `randomstr`; `str2role`/`str2race`/`str2gend`/`str2align` in C order (male/noun/adj prefix, then female/adj, then exact filecode, then `*`/`@`/random-prefix → ROLE_RANDOM; roles-before-random so `"r"` finds Rogue); `str2gend` loops `i < ROLE_GENDERS` (neuter/group unreachable) and `str2align` loops `i < ROLE_ALIGNS` (never matches name "law"/"balance"/"chaos"), as in C; `validrole`, `role_gendercount`, `race_alignmentcount` (index-range guard is a JS memory-safety adaptation — C only guards NONE/RANDOM). `js/player_selection.js` (+~230/−40): `init_role_flags_from_rc` keeps str2* results verbatim per `options.c`; `setrolefilter` as the C str2* chain with ROLE_RANDOM excluded from every arm (keeps the pre-existing module-local `rfilter` for C's `gr.rfilter`); `promptsep`/`root_plselection_prompt`/`build_plselection_prompt` in C branch order (alignnum reassignment, `gr.role_pa`/`role_post_attribs` on `game.gr`, strsubst/`s_suffix`/trailing-`priest/priestess'`→`'s` fix, race/role/gender/alignment post-list, `buflen < QBUFSZ` defprompt), wired into the `genl_player_setup` Shall-I-pick prompt. New edges: `s_suffix` (`do_name.js`), `strsubst`/`strstri` (`hacklib.js`) — call-time use only, green boot proves no TDZ; `str2*` reuse the live `roles.js` edge.
**Verify:** `node scripts/verify.mjs --fn str2role` → `PASS syntax 2 changed js file(s): js/player_selection.js js/roles.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify str2role: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited no blocks — HELDOUT Tier C map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`. Probe (deleted, /tmp/probe_role.mjs, 33 cases + 3 prompts): full-name/prefix/filecode/`*`/`@`/`random`/empty/null/`neuter`-rejected/`balance`-rejected/`unaligned`-rejected/`validrole` bounds/Valkyrie gendercount 1/human aligncount 3/orc 1 all exact; `build_(-1,-1,-1,-1)` byte-identical to the old hardcoded prompt modulo the trailing space `shall_i_pick_prompt` normalizes; Valkyrie-fixed → `"Shall I pick your Valkyrie's race and alignment for you? [ynaq] "` (gender correctly omitted — female-only per C); `buflen=64` → defprompt.
**Named:** `setup_rolemenu`/`setup_racemenu`/`setup_gendmenu`/`setup_algnmenu` (+ `plsel_startmenu`) still deferred — C `add_menu`/`winid` menu builders vs the JS corner-menu infra; both arms (filtering / reset-preselect) already run inline in `pick_*_menu`/`reset_role_filtering`, so extraction is a behavior-preserving refactor with fortress risk — own Open follow-up row, no stub left; `u_init.js` ROLE_FILECODE consolidation onto `roles[].filecode` deferred (u_init out of scope); C `aligns[3]` evil/unaligned absent from JS (pre-existing); `findRole`/`findRace`/`findAlign` stay exported for the `u_init.js` callers.
**Next:** next Open row (`objnam.c` suit_simple_name dragon arms, 1 corpus block).
## 2026-09-06 — D-1903 pager.c history/descr arms (dohistory, dispfile_*, hmenu_*, look_region_nearby, append_str, add_cmap_descr, add_quoted_engraving)

**C locus:** `pager.c` `dohistory :2961–2965` / `dispfile_help :2748–2752` / `dispfile_shelp :2754–2758` / `dispfile_optionfile :2760–2764` / `dispfile_optmenu :2766–2770` / `dispfile_license :2772–2776` / `dispfile_debughelp :2778–2782` / `dispfile_usagehelp :2784–2788` / `hmenu_doextversion :2790–2794` / `hmenu_dohistory :2796–2800` / `hmenu_dowhatis :2802–2805` / `hmenu_dowhatdoes :2808–2812` / `hmenu_doextlist :2814–2818` / `look_region_nearby :1966–1974` / `append_str :82–106` / `add_cmap_descr :1133–1245` / `add_quoted_engraving :1631–1667`; `sym.h :98–99` (`is_cmap_trap`/`is_cmap_drawbridge`); `display_file` is the `winprocs.h:127` window-procs indirection (JS `display_file` in `pager.js` already live).
**JS:** `js/pager.js` (only file); `docs/c-js-map/turns.md` pager section.
**Change:** C-order ports in `js/pager.js` (+~277/−24): `look_region_nearby(out, nearby)` holder-mutating export (C lo_y/lo_x/hi_y/hi_x order, `ux|0`/`uy|0` ints) with `look_region` kept as a delegating wrapper (clone drift deleted); `append_str` (strstri dup-gate, BUFSZ-1 cap, `" or "` + truncated tail, 1/0); local `is_cmap_trap`/`is_cmap_drawbridge` per `sym.h`, local `Inhell_pager` (Gehennom dnum, no new edge); `add_cmap_descr` with holder out-params (`{v}`/`{s}` for C pointers), NO_GLYPH water arm, levl-typ + `EHalluc_resistance=1` save/restore around live `waterbody_name` (`do_name.js:888` precedent), pool→`pool`/molten-lava→`lava` shortening, the article-suppression prefix list (C overwrites article so `the()` is dead on that path, noted), first-match `a trap` arm, and the hit_trap/drawbridge/vibrating-square (`VIBRATING_SQUARE=23`) append guards with short-circuit order preserved; `add_quoted_engraving` (`{s}` buf holder, BUFSZ strncat cap, exact `" (engraving"`/`" (grave"` match, eread remembered-text vs unread, force gate); seven `dispfile_*` + exported `dohistory` (`ECMD_OK`) + five `hmenu_*` (dynamic `cmd.js` import for `doextlist`, same as before — no new static edge), all wired as the live `dohelp` items in C `help_menu_items` order on the `HELP`/`SHELP`/`HISTORY`/`OPTIONFILE`/`OPTMENUHELP`/`USAGEHELP`/`LICENSE`/`DEBUGHELP` consts. Same-edge import extensions only (`the`, `NO_GLYPH`, `strstri` SAFE/no-cycle per `imports.mjs`, const macros); C `mon_nam` scratch buffer noted as unneeded (JS string values).
**Verify:** `node scripts/verify.mjs --fn dohistory` → `PASS syntax 1 changed js file(s): js/pager.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify dohistory: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited no blocks — HELDOUT Tier C map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`. Probes (deleted, inline `node -e`): nearby clamp (40,10)→32,2–48,18 and whole-map 1,0–79,20; `append_str` join + case-insensitive dup-gate; first-match trap arm `^        a trap` + hit/firstmatch; no-engraving no-op; NO_GLYPH water arm; hit_trap append guard.
**Named:** `do_screen_description` full showsyms cmap scan still deferred (later owner after D-1843; `add_cmap_descr` is complete but not yet called by `describe_looked`); `look_engrs` headstone/`strsubst` wiring still deferred (JS keeps its `remembered text` line; `add_quoted_engraving` complete but farlook `:1612`/`look_engrs :2171` call sites not yet rewired); `sysopt.hideusage` gate on usagehelp not ported (no JS sysopt usage flag); `PORT_HELP` item not ported (pre-existing deferral).
**Next:** next Open row (`role.c` role-select parsers, HELDOUT Tier C).
## 2026-09-06 — D-1902 mkmap.c cavern generator passes + room removal (get_map/pass_one/pass_two/pass_three/remove_room/remove_rooms)

**C locus:** `mkmap.c` `get_map :54–60` / `pass_one :67–96` / `pass_two :100–121` / `pass_three :123–144` / `remove_rooms :378–401` / `remove_room :411–436`; `dirs[16] :62–65`, `new_loc :98`, `WIDTH :9` / `HEIGHT :8`. `remove_rooms` is extern (`include/extern.h:1614`) with zero src callers — ported as the public entry C provides.
**JS:** `js/mkmap.js` (new, 6 exports); `docs/c-js-map/data.md` new `### src/mkmap.c cavern generator` section.
**Change:** new `js/mkmap.js` (+~170, 6 exports, C names/signatures): bounds-exact `get_map` (OOB→bg); in-place `pass_one` (writes hit levl mid-sweep — later cells see updated neighbours, preserved); double-buffered `pass_two` (==5→bg) / `pass_three` (<3→bg) with a per-call scratch array at the C `new_loc` layout (observationally identical: each pass fully rewrites its region before the copy-back reads it; C's `gn.new_locations` alloc/free in `mkmap :460/:485` rides with the deferred `mkmap` row); `remove_rooms` async solely for `await impossible` (`artifact.js`/`do.js` precedent); `remove_room` last-over-slot swap as a spread copy (never aliases `maxroom`, or the `hx=-1` tombstone would kill the moved room) with the C `oroomno→roomno` cell rewrite plus a `roomnoidx` restamp (JS-only index cache read by dog.js/hack.js/mklev.js). `imports.mjs --can` is N/A pre-creation (new file unknown to its index); edges are leaf-only (`gstate.js`, `const.js`, `display.js`) used inside function bodies, no top-level TDZ read, and nothing imports `mkmap.js` yet so no cycle.
**Verify:** `node scripts/verify.mjs --fn pass_one` → `PASS syntax 1 changed js file(s): js/mkmap.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify pass_one: no corpus session is blocked on it at HEAD` (row cited no blocks — HELDOUT Tier C map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`. Probe (deleted, /tmp): 9/9 — `get_map` OOB edges, `pass_one` death + birth-8, `pass_two` 5→bg + 8-kept, `pass_three` <3→bg + interior-kept, `remove_room` swap/rewrite/tombstone/no-alias, `remove_rooms` no-overlap-keep + total-remove. First probe draft mis-predicted a `pass_one` birth by assuming simultaneous update; C writes `levl` mid-sweep so earlier cells' deaths lower the count — port matches C order (falsified expectation, not code).
**Named:** `mkmap` + `init_map`/`init_fill` RNG envelope (own queue row); `join_map` + `join_map_cleanup` (`somexy`/`dig_corridor` missing; own queue row); `finish_map` (wallify/lit/lava-ice + maze/cavernous flags; own queue row). Nothing imports `mkmap.js` yet — no live cavern path (level_init INIT styles unported).
**Next:** next Open row (`pager.c` history/descr arms, HELDOUT Tier C).
## 2026-09-06 — D-1901 polyself.c domindblast invented gaze-retaliation blocks deleted

**C locus:** `polyself.c` `domindblast :1893–1938` (46 lines): uen<10 refuse, uen−=10 + botl, `rnd(15)` blast gated on BOLT_LIM² range / `!mpeaceful` / `!mindless` / telepathy-`rn2(2)`-`rn2(10)` with `u_sen` short-circuit, wakeup-before-blast hostility rule, mhp−=dmg → `killed()`. C never calls `passive()` from this function; gaze retaliation fires on melee only.
**JS:** `js/polyself.js` (−~35: two gaze blocks + guard; doc comment; 3 import lines, 1 const line); `docs/c-js-map/turns.md` domonability line.
**Change:** deleted both blocks including the `mhp<1 continue` guard line that only served them; `passive()` already owns gaze retaliation on its real melee trigger path. Updated the doc comment to cite `:1893–1938` and the no-`passive()` fact (review 868). Removed the now-unused `l_monnam` (do_name.js), `KILLED_BY`/`STONING` (const.js), and `PM_MEDUSA` const imports; `nomul`/`urgent_pline`/`done`/`d` stay (used elsewhere in the file).
**Verify:** `node scripts/verify.mjs --fn domindblast` → `PASS syntax 1 changed js file(s): js/polyself.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify domindblast: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (queue row cited no corpus blocks — review Must-fix — so public gates carry it, as the review itself states) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** none new — `dogaze`/`dohide`/`dospinweb` + steed `pet_ranged_attk` stay deferred on the `domonability` row (D-1898); gaze retaliation itself stays owned by `passive()` on the melee path.
**Next:** next Open row (`mkmap.c` cavern generator, HELDOUT Tier C).
## 2026-09-06 — Audit D-1893…D-1900 against pinned C (reviews 863–870) + cadence score

**Scope:** 8 JS-touching SHAs since review 862 (`04dd88ea`…`29ce55d7`), one SHA at a time, review file written per SHA before opening the next.
**Verdicts:** 7 ACCEPT (863 wallify epilogue closes review 861 Must-fix; 864 losexp PROGRESS re-measured 1 blocked→PASS; 865 tut-2 lua-exact; 866 eleven seffects, 21 edges all ALREADY; 867 travel delay, run==8 gate is C `hack.c:1179`; 869 six muse cures; 870 wizard/quest latches), 1 QUALITY-RISK — 868: `domindblast` ships floating-eye-freeze + Medusa-stoning blocks absent from C `polyself.c:1893–1938` (self-admitted "too weird"/passive-consistency invention; Medusa arm has no Blind gate). Must-fix prepended to LOOP-QUEUE; CURRENT Next cluster set to it. Rule #2 `imports.mjs --rulecheck` clean across `js/`.
**Score:** full `sessions` 44/44 fortress holds (Scr 11,405/11,405, RNG 792,838/792,838, `46+0.36/turn` R² 0.84). Hidden proxy 254/265 (95.8 %) excl. env-only; `hitmsg` owner closed by D-1894.
**Next:** next port pops the Must-fix first, alone.
## 2026-09-06 — D-1900 wizard.c clonewiz family + quest.c death latches (which_arti/mon_has_arti/other_mon_has_arti/on_ground/wizdeadorgone/leaddead/nemdead/nemesis_stinks)

**C locus:** `wizard.c` `which_arti :141–157` / `mon_has_arti :164–177` / `other_mon_has_arti :183–195` / `on_ground :201–213` / `wizdeadorgone :813–822`; `quest.c` `nemdead :106–113` / `leaddead :115–122` / `nemesis_stinks :425–438`; callers `target_on :236–270`, `tactics :438–453`, `mon.c` mongone/mondead `:2760–2778`.
**JS:** `js/wizard.js` (+~100: four otyp consts, five functions, 3 import lines); `js/quest.js` (+~55: three functions, 1 import line); `docs/c-js-map/turns.md` clonewiz-family line.
**Change:** C-order ports. `which_arti` (exact M3 switch; default 0 = quest artifact, incl. M3_WANTSARTI/combined masks).
**Verify:** `node scripts/verify.mjs --fn wizdeadorgone` → `PASS syntax 2 changed js file(s): js/quest.js js/wizard.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify wizdeadorgone: no corpus session is blocked on it at HEAD` (all nine checked: 0 blocked each; queue row cited no blocks — HELDOUT Tier C map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** `target_on`/`you_have`/covetous-pursuit `strategy` (the live caller envelope — own row when pursuit ships); `stinky_nemesis` picker (caller-side predicate for `nemesis_stinks`); `mon.c` mongone/mondead death-dispatch callers (mon.c row owns the wiring).
**Next:** next Open row (`mkmap.c` cavern generator, HELDOUT Tier C).
## 2026-09-06 — D-1899 muse.c slime/stone cures (cures_sliming/cures_stoning/munslime/muse_unslime/green_mon/m_sees_sleepy_soldier)

**C locus:** `muse.c` `cures_stoning :2985–2998` / `munslime :3031–3100` / `muse_unslime :3104–3242` / `cures_sliming :3246–3261` / `green_mon :3269–3307` / `m_sees_sleepy_soldier :361–381`; wired call sites `searches_for_item` FOOD_CLASS `:2779–2787` (CORPSE/TIN/EGG).
**JS:** `js/muse.js` (+~300: six functions, FOOD arms, 10 import lines, 8 const lines); `docs/c-js-map/turns.md` slime/stone-cure line.
**Change:** C-branch-order ports in `js/muse.js`: `cures_stoning` (POT_ACID/glob+CORPSE+openable-TIN, NON_PM gate, LIZARD/acidic); `cures_sliming` (fire scroll eyes+hands, oil hands, charged wand/blowable horn); `green_mon` (mcolor CLR_GREEN/BRIGHT_GREEN, Hallu false; `#if 0` stays out); `munslime` (exported, C-public; slimeproof/meating/helpless gates, STRAT_WAITFORU clear, breath→odummy, inventory scan, adjacent-fire-trap Fisher-Yates via `rn1`); `muse_unslime` (async: trap step+`mintrap`, breath `rn2(3)` mspec+`zhitm`, scroll confused-pretty-fire vs `explode(-11)`, oil split+`begin_burn`+burning-quaff, wand/horn+`zhitm`, by_you `xkilled(NOMSG|NOCONDUCT)` else `monkilled(fire)`, `exclam` burn punctuation, slime-burned-away+`makeknown`, movement/mlstmv tax); `m_sees_sleepy_soldier` (3×3 merc scan, guard excluded). `helpless()` is a C macro — inlined `msleeping || !mcanmove`, no 7th clone; `exclam` inlined as one cited ternary, no 4th clone. FOOD_CLASS CORPSE/TIN/EGG arms live with C short-circuit (`tinok` from `mcould_eat_tin`).
**Verify:** `node scripts/verify.mjs --fn munslime` → `PASS syntax 1 changed js file(s): js/muse.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify munslime: no corpus session is blocked on it at HEAD` (queue row cited no blocks — HELDOUT Tier C map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (--full forced: FOOD arms sit on the live pickup path)`.
**Named:** `munstone` + `mon_consume_unstone` stoning-TRUE envelope (uhitm corpse/egg-throw + `mhitm_ad_ston`/`do_stone_mon` callers — separate uhitm.c row owns AD_STON); `mhitm_ad_slim` callers of `munslime` (same uhitm.c Open row owns AD_SLIM); MUSE_BUGLE select+use in find/use_defensive (needs monster-bugler `awaken_soldiers` — `js/music.js:404` clone covers the hero envelope only); bugle comment narrowed accordingly.
**Next:** next Open row (`wizard.c` clonewiz family, HELDOUT Tier C).
## 2026-09-06 — D-1898 polyself.c domonability C-order dispatch + five #monster arms

**C locus:** `cmd.c` `domonability` (hide/web prompt + 15-arm chain) ; `polyself.c` `dospit :1450–1478` / `doremove :1481–1494` / `dosummon :1624–1639` / `dopoly :1877–1890` / `domindblast :1894–1938`.
**JS:** `js/polyself.js` (+278/−17: `mdistu`/`MS_SHRIEK`/venom+PM consts, five arms, C-order `domonability`); `js/pray.js` (+2/−2: one `export` word on `Punished`); `docs/c-js-map/turns.md` domonability line.
**Change:** C-order dispatch incl. the hide/web `yn_function` prompt (`hidespinchars`, q/ESC → ECMD_OK) ahead of every arm; `dospit` (`getdir`→ECMD_CANCEL, `attacktype_fordmg` AT_SPIT/AD_ANY=-1, BLND/DRST→blinding else ACID→acid with the C `impossible` fallthrough, spe=1, `throwit`); `doremove` (nymph `Punished` — one-word export from `pray.js` — buried-ball `surface` arm, `unpunish`); `dosummon` (uen−10+botl, `exercise` WIS, tame `were_summon`); `dopoly` (vampire/vampshifter → `polyself(POLY_MONSTER)`, transform+newsym on change, `Ugender` `pmname`); `domindblast` (uen−10+botl, BOLT_LIM² range, peaceful/mindless gates, telepathy/rn2 gates with `u_sen` short-circuit, wakeup-before-blast hostility rule, mhp−=dmg→`killed`, floating-eye `nomul` freeze + `multi_reason`, Medusa killer+`done(STONING)`); gremlin `split_mon`/`dryup` fountain arm; shriek/`aggravate` arm. `You`/`You_cant`/`Your`/`There`/`pline_The` rendered as full-text `pline` (dobreathe/file convention — no new `You` clone beside `zap.js`). Same-edge static imports only (82-module SCC per `imports.mjs --can`; all uses inside async fns, no top-level TDZ read).
**Verify:** `node scripts/verify.mjs --fn domonability` → `PASS syntax 2 changed js file(s): js/polyself.js js/pray.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify domonability: no corpus session is blocked on it at HEAD` (queue row cited no blocks — HELDOUT Tier C map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** `dogaze`/`dohide`/`dospinweb` (131/97/124 ln C; `y_n`/`setmangry`/`destroy_items`/`expels`/`deltrap`/`maketrap` gangs — follow-up queue row); `armor_to_dragon`/`check_strangling`/`livelog_newform` (dead code without the `polyself()`/`polymon()` draconian/rehumanize envelopes — same follow-up row); steed breath (`pet_ranged_attk` missing).
**Next:** follow-up Open row (remaining `polyself.c` arms); then `muse.c` slime/stone cures (HELDOUT Tier C).
## 2026-09-06 — D-1897 hack.c findtravelpath door/boulder delay re-queue + grid-bug NODIAG

**C locus:** `hack.c` `findtravelpath :1266–1523` — delay arm `:1403–1420` (`(!Passes_walls && !can_ooze && closed_door(x,y)) || (sobj_at(BOULDER,x,y) && !could_move_onto_boulder(x,y)) || test_move(TEST_TRAP)` → re-queue current cell once per round via `alreadyrepeated`, matrix untouched, while `travel[x][y] > radius-3`); `dirmax :1330–1333` (`NODIAG(u.umonnum) ? 4 : N_DIRS` over cardinals-first `dirs_ord`); start cell matrix stays 0 (`memset` + step arrays only). TEST_TRAP TRUE ≡ run==8 trap/liquid arm of `test_move :1189–1212` (`return (mode == TEST_TRAP)`), which TEST_TRAV then rejects — so the trap delay is C-order churn with no behavior change.
**JS:** `js/cmd.js` (+68/−11: helper, two loop arms, NODIAG, 3 import lines); `js/hack.js` + `js/monmove.js` (one `export` word each); `docs/c-js-map/turns.md` travel line.
**Change:** New `travel_delay_current(x,y)` (`!Passes_walls_prop() && !can_ooze(youmonst) && closed_door` || `boulder && !could_move_onto_boulder`, C `:1403–1407`) on same-edge imports only — exported `Passes_walls_prop` (`hack.js`) and `can_ooze` (`monmove.js`), both hoisted functions, no new edge (`imports.mjs --can` ALREADY all three). Both BFS loops: per-cell `alreadyRepeated` + per-direction delay arm (door/boulder current-cell, or run==8 `travel_avoids_cell` target as the TEST_TRAP stand-in) re-queueing while `travel==0 > radius-3`, then falling through to the existing TEST_TRAV-equivalent skips; removed the start-cell seed; grid-bug `DIRS_ORD.slice(0, 4)` via generated `PM_GRID_BUG` (same comparison as `hack.js:1799`, not the local monsterNames index).
**Verify:** `node scripts/verify.mjs --fn findtravelpath` → `PASS syntax 3 changed js file(s)` · `PASS rule2` · `note hidden verify findtravelpath: no corpus session is blocked on it at HEAD` (queue row cited no blocks — TOP30 map row — so public gates carry it) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)`.
**Named:** travelmap visited stop (`You stop, unsure which way to go` — needs shared selection infra; clones live only in `mklev.js`); boulder-TARGET routing (C TEST_TRAV pushes through single non-Sokoban boulders — needs tunnels/c `carrying` predicates); TRAVP_VALID adjacent fast path (`next2u`/`crawl_destination`/TEST_MOVE); TEST_TRAP Known_wwalking/Known_lwalking already deferred under `travel_avoids_cell`.
**Next:** next Open row (`polyself.c` missing domonability arms, HELDOUT Tier C).
