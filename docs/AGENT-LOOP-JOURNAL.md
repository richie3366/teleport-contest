# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-14 — D-2296 `dog.c` mon_leave worm arm: count/truncate/wormgone/head-back wired at keepdogs + migrate_to_level (queue row `worm.c` wormgone mondead/dog callers)

**C locus:** `dog.c:728–763` `mon_leave` (worm arm `:748–760`: `count_wsegs`, `min(cnt, MAX_NUM_WORMS-1)` per `monst.h:160` = 32, `wormgone` `:755`, `place_monster` head-back `if (mx)`); callers keepdogs `:861/:865` (mydogs) and `migrate_to_level` `:904/:916` (migrating_mons). `m_detach` `:2786–2787` already live (D-2231).
**JS:** 3 files (`js/dog.js` +~40 incl. docs, `js/teleport.js` +~8, `js/worm.js` doc-only; + map + queue + this log), under the 600/10 caps. Density note: the residual is one small C staticfn plus two one-line wirings — the mon.c half of the row shipped under D-2231.
**Change:** new exported `mon_leave(mtmp)` in `js/dog.js` (C home; worm arm verbatim in C order, sync — `wormgone`/`place_monster` are sync, their `impossible` is fire-and-forget); keepdogs follower arm calls it before the fmon splice and stores the count in `wormno` (C `:861/:865`); `migrate_to_level` calls it after mx/my capture and stores the count before `mlstmv` (C `:904/:916`). Imports extend existing edges only (`MAX_NUM_WORMS` joins the `const.js` import, `place_monster` the `steed.js` import, `count_wsegs`/`wormgone` the `worm.js` import); the one new edge (`teleport.js` ← `dog.js`) is same-SCC (`imports.mjs`: IN-SCC, cycle not a defect) and `mon_leave` is a hoisted function declaration used at call time only — no top-level TDZ read. No DIAG/FORCE/seed-gate/coordinate/RNG-index reads (arm is RNG-free on the success path); Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn wormgone` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited no N blocks) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (forced `--full`: migration-adjacent change) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_monleave.mjs` (out of tree, deleted after — no corpus session reaches a migrating worm): via the real `jsmain.js` entry — non-worm `mon_leave` returns 0 unmutated; wormno'd off-map mon runs the `wormgone` arm (wormno 7→0, returns 0); `migrate_to_level` end-to-end sets `wormno`, zeroes mx/my, lands on `migrating_mons`, no throw. (A direct `dog.js`-first import trips a pre-existing objnam/do_name TDZ, reproduced identical at clean HEAD in a scratch worktree — harness-order artifact, not this change; worktree removed.) No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); the full-44 green is the regression proof for the shared-file change.
**Named:** `mon_leave`'s minvent `no_charge`/`picked_container` loop; isshk `set_residency` (C says `mon_arrive` sets it back); `migrate_to_level` leash + light-source arms (pre-existing names, untouched); `place_wsegs` restore/replmon callers (own Open row, untouched).
**Next:** do not re-pop `wormgone`/`mon_leave` for these arms. Falsifier: a rescore or fresh `verify wormgone` showing a session blocked with it as owner, or a migrating-worm session diverging on stale segs/wormno.
## 2026-09-14 — D-2295 `mon.c` kill_genocided_monsters chameleon newcham arm (queue row `cmd.c` makemap_prepost kill_genocided_monsters, D-1097/D-1190 residual)

**C locus:** `mon.c:5639–5677`, arm `:5663–5667` (`if (ismnum(mtmp->cham) && !kill_cham) (void) newcham(mtmp, NULL, NC_SHOW_MSG); else mondead(mtmp)`); `hack.h:1462–1464` (`NC_SHOW_MSG` 0x01); callers `cmd.c:1048` (makemap_prepost post), `do.c:1817` (goto_level), `read.c:2749` (do_class_genocide), `read.c:2993` (do_genocide).
**JS:** 4 files (`js/mon.js` +13/−7 incl. doc, 3 files one-word awaits each; +18/−12 total with the CURRENT Next-cluster line), under the 600/10 caps. Density note: the C locus is 40 lines and ~90 % shipped by D-1097/D-1190/D-1288 — this closes the last named arm, so the small diff is the whole residual.
**Change:** `js/mon.js` — the arm awaits the already-imported live `newcham(mtmp, null, NC_SHOW_MSG)` (C `(void)` return = result unused; the await only completes C's inline message before the loop continues, scored runs stay sync per D-1648); function is now `async`; `await` added at all four call sites (`js/wizcmds.js:595` makemap_prepost post, `js/do.js:1854` goto_level, `js/read.js:2405` do_class_genocide, `js/read.js:2599` do_genocide — nearest-fn scan confirms all four are already `async`); `kill_eggs` exported to match C linkage (kills the sym.mjs clone-drift flag; no second copy). No new module edge (`imports.mjs --can mon.js makemon.js newcham`: ALREADY). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn kill_genocided_monsters` → PASS syntax (4 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited no N blocks) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_kgm.mjs` (out of tree, deleted after — no corpus session reaches genocide+cham-imitator): cham imitator of genocided grid bug reshaped 116 → 111 (non-genocided), survives with mhp intact, non-genocided bystander untouched, async return confirmed, no hangs or unhandled rejections.
**Named:** mondead fire-and-forget (review-1197 debt — amulet+More corner suspends detach past later loop iterations, display-order only; the await-ripple is a bigger iter); makemap_prepost pre discard/savelev package (review-250 debt); kill_eggs TIN/CORPSE arms (`#if 0` in C, correctly absent).
**Next:** do not re-pop `kill_genocided_monsters` / `makemap_prepost` for this arm. Falsifier: a rescore or fresh `verify kill_genocided_monsters` showing a session blocked with it as owner.
## 2026-09-14 — Audit 48180ad1..e6f16d72 (reviews 1255–1260: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Six JS SHAs vs pinned C (`csym` + `sym.mjs` each): D-2289 acid
boom/blank_novel; D-2290 steedintrap ×4 (Antimagic_prop = pre-existing
D-1089 CLONE); D-2291 DFLAG2 truth table ≡ C; D-2292 elements + full
DRLI (sync/async call shapes checked); D-2293 wiz_intrinsic count +
wintty machine re-read; D-2294 MM_ANGRY ternary (skips 2 tail draws).
All `verify --base SHA~1` re-runs 0-blocked both sides; rulecheck clean.
Nits (no Must-fix): D-2290 `:1211/:1276` prose swap, landmine map row
stale omit, keep_saddle codelog-only. Cadence 44/44, Scr
11,405/11,405, RNG 792,838/792,838, `47+0.29/turn` — identical to
standing score. Queue 11 open, no refill. No js/ edits.
## 2026-09-14 — D-2294 `makemon.c` birth knowledge residuals: mpeaceful MM_ANGRY arm + mwandexp zeromonst/save field (queue row `makemon.c` birth knowledge residuals, D-2107-named)

**C locus:** `makemon.c:1297` (`mtmp->mpeaceful = (mmflags & MM_ANGRY) ? FALSE : peace_minded(ptr)`); `monst.h:166` (`mwandexp` bitfield, zeroed by `*mtmp = cg.zeromonst`) + `save.c:826–834`/`restore.c:307` (`savemon`/`restmon` write the whole struct via `Sfo_monst`/`Sfi_monst`, so `mwandexp` persists across save/restore by construction).
**JS:** 1 file (`js/makemon.js`, +4/−1), under the 600/10 caps. Density exception: the C locus is one line plus one struct field (same class as D-2107's +24/−1).
**Change:** `js/makemon.js` only — `mpeaceful` ports the C ternary verbatim (`(mmflags & MM_ANGRY) ? 0 : (peace_minded(ptr) ? 1 : 0)`); template gains `mwandexp: false` beside `mtame` so the saved blob always carries the field like C's whole-struct write. No new import (same module), no DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean. `mcastu.js:624` already zeroes `mpeaceful` post-call per C `:667`; `dokick.js:724` killer bees relied solely on this arm.
**Verify:** `node scripts/verify.mjs --fn makemon` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe (out of tree, deleted after): `/tmp/probe_mwandexp.mjs` through the real `serMon`→JSON→`deserMonList` path (via `jsmain.js` import order) — `mwandexp=true→true`, `false→false`, both OK, matching C `savemon`/`restmon` whole-struct persistence.
**Named:** none new (D-2107's pair is now fully live; `place_monster` cutworm/makemon callers belong to the next Open row, not this one).
**Next:** do not re-pop `makemon` birth knowledge. Falsifier: a rescore or fresh `verify makemon` showing a session blocked with it as owner, or a save-oracle probe showing `mwandexp` lost across a save/restore round-trip.
## 2026-09-14 — D-2293 `wizcmds.c` wiz_intrinsic residuals: menu count-prefix + unavailcmd ecname + make_blinded talk (queue row `wizcmds.c` wish count-prefix + unavailcmd + make_blinded talk variants, D-2283 residuals)

**C locus:** `wizcmds.c:1004–1008` (`amt = count == -1 ? DEFAULT_TIMEOUT_INCR : count`, `amt <= 0` paranoia-skip); `:1094` (`pline(unavailcmd, ecname_from_fn(wiz_intrinsic))`, ecname `"wizintrinsic"` per `cmd.c:1965`); `:1020–1021` (BLINDED arm is just `make_blinded(newtimeout, TRUE)`). Menu machinery: `wintty.c:1564–1602` (digit accumulation, `!counting && gacc` digit-wins note), `:1395–1399` (one-key reset), `:1112–1151` (`toggle_menu_curr`), `:1182` (`#` mark), `:1698–1731` (search carries count), group-accel `counting ? count : -1`; `integer.h:120` (AppendLongDigit overflow → -1); `add_menu` `:2611` (count starts -1).
**JS:** 2 files + map/docs (`js/options.js` counting + `#` mark + invert/unselect counts; `js/wizcmds.js` two arms + doc), under the 600/10 caps. Density note: insertions are small because the C-cited toggle/gacc/search helpers shipped earlier — the value is wiring them into the shared PICK_ANY path all four callers (wizintrinsic, pickup ×2, wizid) ride, retiring three residuals at once.
**Change:** `js/options.js` — `select_menu_pick_any` ports the C counting verbatim through the existing helpers (no new module edge — `toggle_menu_curr`/`menu_digit_is_gacc` join the existing `invent.js` import, same SCC, call-time use): clones start `count: -1`; `counting`/`count`/`resetCount` state with the `:1395–1399` one-key reset applied per keypress; digit arm (`menu_digit_is_gacc` guard, `AppendLongDigit` spirit via `Number.isSafeInteger` overflow-drop, leading zeros don't count); ESC-during-count only stops the count (`:1604–1615`); selector hits go through `toggle_menu_curr`; search gets `(counting, count)`; group-accel stamps `(counting && count > 0) ? count : -1`; UNSELECT_PAGE/ALL clear counts (C `unset_all_on_page`); selected-with-count paints `#` (`:1182`); `invert_pick_any_matching` takes the C `count` param (bulk paths pass -1). `js/wizcmds.js` — count arm (`-1`/undefined → default, `| 0` cast, `amt <= 0` skip) + `Unavailable command 'wizintrinsic'.` (house hardcodes like wizwhere/wizidentify `:490/:505`) + doc retires all three notes (talk variants live in shared `make_blinded`, D-1755 `make_blinded_notoggle_talk` — stale note, no `do.js` change). Map: `turns.md` wizcmds section retires `count-prefix #wizintrinsic`; `startup.md` options.c line drops the stale `count-prefix` deferral (MENU_SEARCH already D-1646).
**Verify:** `node scripts/verify.mjs --fn wiz_intrinsic` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probes (out of tree, deleted after — no corpus session reaches a count-prefixed pick): `/tmp/probe_count.mjs` 18/18 through the real menu (`5b`→5, `30c`→30, leading-zero ignore, one-key consume, ESC-stops-count, recount-sticks/plain-retoggle-clears, select-all untouched) + `/tmp/probe_unavail.mjs` non-wizard arm returns ECMD_OK with exactly `Unavailable command 'wizintrinsic'.`.
**Named:** none new (pickup `query_objlist`/`query_category` count consumption was already live downstream; `select_menu_pick_one` counts stay out — its doc never named them).
**Next:** do not re-pop `wiz_intrinsic` or `select_menu_pick_any` counting for this envelope. Falsifier: a rescore or fresh `verify wiz_intrinsic` showing a session blocked with it as owner, or a digit-in-menu public/corpus divergence naming the counting arms.
## 2026-09-14 — D-2292 `artifact.c` artifact_hit SPFX_DRLI + FIRE/COLD/ELEC destroy/ignite arms (queue row `artifact.c` spec_dbon/spec_applies SPFX_DRLI destroy/ignite)

**C locus:** `artifact.c:1490–1495` (FIRE `destroy_items` + `ignite_items(minvent)` inside `!rn2(4)`, bonus kept only when `!youdefend`); `:1505–1509` (COLD same shape, no ignite); `:1524–1528` (ELEC same shape under `!rn2(5)`); `:1645–1720` (SPFX_DRLI: `nonliving` life string, drain clamp `:1657–1658`, `distant_name` side-effect calls, m_lev==0 fatal, heal-half-rounded-up, youdefend `losexp("life drainage")` + magr heal `:1714`); `decl.h:17` (NH_BLACK ≡ c_black); `exper.c:232–237` (level-1 drain with drainer is fatal — probe expectation, not new code).
**JS:** 1 file, +102/−18 (`js/artifact.js` only), under the 600/10 caps. Density: ~100 lines of C-faithful JS for the ~75-line DRLI arm plus three 4-line destroy wirings.
**Change:** FIRE/COLD/ELEC arms call the live canonicals in C order — `(await destroy_items(mdef, AD_*, dmgBox.dmg | 0)) | 0` added only when `!youdefend`, `await ignite_items(mdef?.minvent)` after on FIRE (hero minvent undefined → early return, matching C NULL); full DRLI arm verbatim (life string via imported `nonliving`, entry-snapshot m_lev/mhpmax, clamp, `distant_name(otmp, xname)` on both print paths, Stormbringer `hcolor(NH_BLACK)` vs `The(otmpname)` split, `2*mhp+200` fatal, `Math.trunc((drain+1)/2)` heal-half-up with `healup`/`healmon` (C `assert(magr!=0)` covered by null-safe canonical), youdefend `Blind()` → `You_feel("an …")` vs pline split + `await losexp('life drainage')` + `(Math.abs(oldhpmax-uhpmax)+1)/2` trunc heal). `NH_BLACK='black'` file-local const (potion.js:250 / muse.js:194 convention); `ART_STORMBRINGER` joins the generated import; `nonliving`/`destroy_items`/`hcolor`/`healmon`/`The`/`distant_name`/`ignite_items`/`healup` join existing edges; `losexp` (exper.js) + `monhp_per_lvl` (makemon.js) are new edges, both `imports.mjs --can` IN-SCC hoisted-function cycle-safe, call-time use only. Doc comments retired (artifact_hit "none left", spec_ability).
**Verify:** `node scripts/verify.mjs --fn artifact_hit` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_drli.mjs` (out of tree, deleted — no corpus session reaches these arms): 18/18 over two fresh `runSegment` boots — A1 drain math exact (D=8, bonus=2∈{1,2}, lev 5→4, r=true), A2 m_lev==0 fatal exact (box 224=2·12+200, max/lev kept), C Fire Brand 12→7 scrolls burned with 0 throws, B-fatal level-1 hero (killer `life drainage`/KILLED_BY, uhpmax floor 10, gameover), B2 nonfatal (ulevel 3→2, max 30→26, attacker 10→12=+trunc(5/2)), drain/drained/blade plines SEEN on screen.
**Named:** none new — `artifact_hit` has no deferred arms left (FIRE/COLD/ELEC/MAGM + Mb_hit + BEHEAD + DRLI all live).
**Next:** do not re-pop `artifact.c` spec_dbon/spec_applies SPFX_DRLI destroy/ignite for any arm shipped here. Falsifier: a rescore or fresh `verify artifact_hit` showing a session blocked with artifact_hit as owner.
## 2026-09-14 — D-2291 `artifact.c` spec_applies DFLAG2 yours/Upolyd/ulycn arms (queue row `artifact.c` artilist DFLAG2)

**C locus:** `artifact.c:1026–1031` (`spec_applies` SPFX_DFLAG2: `(ptr->mflags2 & weap->mtype) || (yours && ((!Upolyd && (gu.urace.selfmask & weap->mtype)) || ((weap->mtype & M2_WERE) && ismnum(u.ulycn))))`); `you.h:554` (`Upolyd` ≡ `u.umonnum != u.umonster`); `monst.h:285` (`ismnum(x)` ≡ `x >= LOW_PM && x < NUMMONS`); `monflag.h:125` (`M2_WERE` 0x4).
**JS:** 1 file, +12/−4 (`js/artifact.js` only: 2 import names + DFLAG2 arm + doc-comment truth), under the 600/10 caps. Density note: small-C exception to the ~40-line floor — the row's entire remaining C gap is ~5 lines (`:1026–1031` tail).
**Change:** the C `||` chain verbatim in C order (mflags2 first, then the yours gate): `!Upolyd(u)` + `(game.urace.selfmask & mtype)` selfmask arm, then `(mtype & M2_WERE) && ismnum(u.ulycn)` were arm with no Upolyd gate, per C short-circuit. `Upolyd`/`ismnum` join the existing `./const.js` import, `M2_WERE` joins the existing `./monsters.js` import — no new module edge (same SCC, call-time use only, no top-level TDZ read, so no `imports.mjs --can` owed); `game.urace?.selfmask ?? 0` matches the `your_race` convention (`js/mklev.js:18530`, `js/monsters.js:479`); `ismnum(u.ulycn)` matches the `allmain.js:130` call shape (JS canonical drops the `< NUMMONS` upper bound — ulycn only ever holds NON_PM or a valid PM, so the bound never binds). `| 0` integer idiom throughout, 1/0 return shape kept.
**Verify:** `node scripts/verify.mjs --fn spec_applies` → PASS syntax (1 changed js file: js/artifact.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_dflag2.mjs` (out of tree, deleted): 8/8 through the exported `spec_dbon` → local `spec_applies` path — orc hero unpoly Orcrist applies / human misses / orc poly misses (Upolyd gate) / human lycanthrope Werebane applies unpoly AND poly (no Upolyd gate on the were arm) / human non-lycanthrope misses / monster M2_WERE-flagged applies / monster unflagged misses.
**Named:** none new (resists_* artifact/worn grants and hero Poison/Stone H/E/sticky flats stay in the `spec_applies` doc comment; SPFX_DRLI destroy/ignite stays with the `artifact_hit` Open row).
**Next:** do not re-pop `artifact.c` artilist DFLAG2 yours/Upolyd/ulycn for any arm shipped here. Falsifier: a rescore or fresh `verify spec_applies` showing a session blocked with spec_applies as owner.
## 2026-09-14 — D-2290 `trap.c` steedintrap dart/arrow/landmine/poly caller wiring (queue row steedintrap)

**C locus:** `trap.c:1211` (arrow hero `u.usteed && !rn2(2) && steedintrap(trap, otmp)` before `thitu(8,…)`); `trap.c:1276` (dart hero, same gate before `thitu(7,…)` with the `!rn2(6)` poison roll kept ahead); `trap.c:2528–2597` (landmine hero else-arm: `recursive_mine` static guard, KAABLAMM pline, `steed_mid` capture, steedintrap, `sobj_at(SADDLE)` + `keep_saddle_with_steedcorpse` tail `:2591–2592`, then wounded legs); `trap.c:2453–2525` (`trapeffect_poly_trap` hero verbbuf/iron-shoes/Antimagic/Unchanging/steedintrap+deltrap+polyself + monster iron-shoes/resists_magm/newcham arms); `hack.c:1817–1829` (`u_locomotion` Levitation→float / Flying→fly / `locomotion(data, def)`).
**JS:** 1 file, +155/−34 (`js/trap.js` only), under the 600/10 caps. Density: ~155 lines of C-faithful JS for ~110 row-named C lines (dart/arrow gates + landmine guard + full poly hero/monster + selector + `u_locomotion_verb`).
**Change:** dart/arrow hero arms gate `u.usteed && !rn2(2) && await steedintrap(trap, otmp)` ahead of `thitu` in C order (t_missile → poison → dmgval → gate → thitu/place), steed-hit arm a bare `;` with the otmp-consumed note, miss arm re-indented under `else` (unmounted: `u.usteed` short-circuits before `rn2(2)` — zero RNG delta); new file-local `trapeffect_poly_trap` in C position (before landmine, matching C `:2453` before `:2528`) with the hero verbbuf triple (`trigger` / `lead <x_monnam> onto` / `<u_locomotion_verb('step')> onto`), iron-shoes `poly_obj` + `update_inventory` + `prinv`, Antimagic/Unchanging `shieldeff` + `You_feel`, else steedintrap + deltrap + newsym + `polyself(POLY_NOFLAGS)`, and the monster forcible-unwear/poly_obj/re-equip + resists_magm + `newcham` arms; landmine else-arm gains the module-level `recursive_mine` guard, `steed_mid` capture, steedintrap call, and `sobj_at(SADDLE)` in C order ahead of the existing wounded-legs lines; selector gains `case POLY_TRAP`. New file-local `u_locomotion_verb` over the imported live `locomotion` (C `hack.c` order; "step" is lowercase so the capitalize arm never fires). Imports join existing edges only (shieldeff→display, Yname2→objnam, POLY_NOFLAGS→const, prinv→invent, Unchanging+polyself→polyself, extract_from_minvent+update_mon_extrinsics→worn, poly_obj→zap, locomotion→monmove — same SCC, hoisted declarations, call-time use, no top-level TDZ read).
**Verify:** `node scripts/verify.mjs --fn steedintrap` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Preflight `verify --no-cohort` green on a clean tree before edits. No hand probes: no corpus session reaches the new arms with a recorded expectation (mounted- trap hits, poly-trap squares); behavior proved by C-order reading against the brief bodies.
**Named:** `keep_saddle_with_steedcorpse(steed_mid, fobj, saddle)` (C `:2591–2592` — no live importer; steed_mid/saddle captured with `void` notes, floor saddle stays); monster poly resists_magm `shieldeff_mon` flash (display-only, no live exporter — same deferral as mhitm.js:615); slp-gas hero arm incl. its steedintrap call (C `:1578` — stays with the Sleep_resistance/fall_asleep body, separate C function).
**Next:** do not re-pop steedintrap dart/arrow/landmine/poly caller wiring or `trapeffect_poly_trap` for any arm shipped here. Falsifier: a rescore or fresh `verify steedintrap` / `verify trapeffect_poly_trap` showing a session blocked with either as owner.
## 2026-09-14 — D-2289 `trap.c` water_damage residuals: `pot_acid_damage` boom + scroll/book/potion message arms + chain acid_ctx/bhitpos; `zap.c` `blank_novel` + cancel wiring (queue row `apply.c` splash_lit residuals)

**C locus:** `trap.c:4657–4710` (`pot_acid_damage`: Blind off-invent `dknown = 0`, `acid_ctx` dkn/unk_boom `exploded` word, described vs `simpleonames`+`vtense` arms, ctx counters, `setnotworn`+`delobj`+invent `update_inventory`); `trap.c:4712–4851` (`water_damage`: grease `described = TRUE` + acid call `:4745–4751`, SCR_MAIL early-out under MAIL_STRUCTURES `:4780`, scroll/book/potion `Your` fades+dilutes + `update_inventory` sites `:4793/:4807/:4828/:4838/:4845`, Book steam + `CONTAINED_TOO|BURIED_TOO` locate `:4796–4806`, `blank_novel` on SPE_NOVEL `:4819–4820`); `trap.c:4854–4890` (`water_damage_chain`: acid_ctx init/reset, `bhitpos` save/restore, `CONTAINED_TOO` locate, otmp snapshot before the call); `zap.c:1367–1380` (`blank_novel`: `novelidx = 0`, `free_oname`, `container_weight`) + `:1329–1331` (`cancel_item` novel arm).
**JS:** 3 files, +160/−34 (`js/trap.js` pot_acid_damage + arms + chain; `js/zap.js` blank_novel + cancel arm + 2 import names; `js/potion.js` comment truth), under the 600/10 caps. Density: ~125 lines of C-faithful JS for ~95 row-named C lines plus the shared `blank_novel` helper.
**Change:** new `export function blank_novel(obj)` in `js/zap.js` directly after `cancel_item` (C position `:1367` after cancel), wired at both C call sites (cancel `:1330`, water_damage `:4820`); new file-local `pot_acid_damage(obj, in_invent, described)` in `js/trap.js` directly before `water_damage` (C staticfn position), C order (Blind gate, ctx-valid-guarded `exploded`, described `The potion…` via `otense` vs `simpleonames`+`vtense` with Another/More, ctx counters, `setnotworn`+`delobj`+invent update); `water_damage` gains `described`, the grease acid call, SCR_MAIL early-out (MAIL_STRUCTURES on per `global.h:430`, same idiom as the file's acid-damage arm), all five `Your` arms + mutation `update_inventory`s, Book steam via `get_obj_location(CONTAINED_TOO|BURIED_TOO)` + `isok`/`cansee`, `blank_novel` on SPE_NOVEL; `water_damage_chain` inits/resets `game.acid_ctx`, saves/restores `game.bhitpos` around a head `CONTAINED_TOO` locate, snapshots otmp before the call (C loop shape). Imports join existing edges only (`blank_novel`→zap, `setnotworn`→do, `otense`/`simpleonames`→objnam, `CONTAINED_TOO`/`BURIED_TOO`→const, `free_oname`→do_name/zap, `container_weight`→mkobj/zap — all `imports.mjs --can … → ALREADY`, same SCC, hoisted declarations, call-time use, no top-level TDZ read). C `&&` short-circuit preserved at the potion.js:2488 `ER_DESTROYED && obj.in_use` site (frees never read — verified against `potion.c:2355`); stale `pot_acid_damage … (ER_DESTROYED without delete)` comment retired.
**Verify:** `node scripts/verify.mjs --fn water_damage` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Plus forced `node frozen/ps_test_runner.mjs sessions` → **44/44** (new-pline/delete blast radius — fortress holds, speed `47+0.29/turn`). Preflight `verify --no-cohort` green on a clean tree before edits.
**Named:** none new (zap `cancel_item` corpse revive→rot timer swap stays; `You/Your/pline_The`-as-`pline` is the house idiom).
**Next:** do not re-pop splash_lit/pot_acid_damage/blank_novel/water_damage for any arm shipped here or in D-1337. Falsifier: a rescore or fresh `verify water_damage`/`verify blank_novel` showing a session blocked with either as owner.
## 2026-09-14 — Audit 8c4803e9..e210b23d (reviews 1250–1254: 5 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration, no js/ edits. Audited one SHA at a time against pinned C
(csym bodies + callers, sym.mjs on every re-point, --can on every new edge,
hidden-proxy verify --base re-measure per SHA): 1250 drown (trap.c:5058–5199
branch-verbatim; iron-golem-lifesave-continue follows the house
finish_losehp_done idiom, not queued); 1251 sobj_at 10-clone retire
(canonical mkobj.js:2201; mon.js negative-guard deletion dead for all call
sites; objects_at removals verified zero-use); 1252 doclose Blind arms
(isok→nodoor order kept, glyph-half drop matches D-2167 doopen_indir idiom);
1253 set_moreluck verbatim + 4 bless-family luck heads preserving C else-if
exclusion (remaining arms pre-existing named omits); 1254 mlevel_tele_trap
(pline_mon is real C at all 5 sites; dng_bottom is a trap.c staticfn so the
file-local is correct; onscary keep justified by monmove.c:240–303 room
resist covering (0,0)). Strongest verify of the five: D-2288's 13/13 hand
probe. Queue stays 8 Open (no refill owed); filled the D-2288 Addressed
short hash (`e210b23d`).
