# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-06 — D-1930 weapon.c select_hwep HTH weapon-select arms (oselect cockatrice filter, Balrog, resists_ston, touch_artifact order)

**C locus:** `weapon.c` `select_hwep` `:704–741`; staticfn `oselect` `:475–496`; `Oselect` macro `:469–473`; `hwep[]` `:691–701`; `touch_petrifies` `mondata.h:200–201` (cockatrice/chickatrice); `can_touch_safely` `mon.c:1957–1974`; `oc_bimanual` `objclass.h:65` (`#define oc_bimanual oc_big`); `resists_ston` `monst.h:279`; `mon_hates_silver` `mondata.c:516–520`; `touch_artifact` `artifact.c:907–974`; `uwep` hero global (`decl.c:105`, `game.u.uwep` in JS).
**JS:** `js/weapon.js` (+41/−15); 1 js file, under 600 cap.
**Change:** `js/weapon.js` — C-order ports in C branch order: artifact loop `oclass && oartifact && touch_artifact(otmp, mtmp) && ((strong && !shield) || !oc_big)`; giant `Oselect(CLUB)` / `else if` Balrog (`mndx === PM_BALROG && game.u?.uwep`) `Oselect(BULLWHIP)` with hwep-walk fallthrough like the macro; CORPSE gate `&& !resists_ston(mtmp)`; `oselect` first-match walk with the `(type === CORPSE || type === EGG) && (corpsenm === NON_PM || !touch_petrifies(mons(corpsenm)))` skip in C position; deleted the local `is_giant` for the canonical `js/monsters.js` import (`mons`/`resists_ston`/`touch_petrifies` extend the same existing edge; `NON_PM` extends the const.js edge; `touch_artifact` extends the artifact.js edge — all `imports.mjs --can` ALREADY, no new edge, call-site reads only so no TDZ); `EGG`/`BULLWHIP` otyp consts beside `CORPSE`/`CLUB`; `oc_big` kept with an `objclass.h:65` alias comment (not a rename — review 569's `oc_big`-vs-`oc_bimanual` drift claim is falsified by the define). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn select_hwep` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/weapon.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify select_hwep: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (`/tmp/probe-select-hwep.mjs`, 4 assertions on the real export, deleted): Balrog+uwep takes bullwhip; no-glove no-resist skips corpse; resists_ston takes cockatrice corpse. PROBE-OK (no-uwep single-whip case still returns the whip via the `hwep[]` walk, which is C-correct — the special only outranks higher-preference hwep items).
**Named:** `can_touch_safely` inside `oselect` (`mon.c:1957–1974` — corpse petrify/rider + silver + `touch_artifact` deny; `js/monmove.js:230` stub stays always-safe, call site named not wired); `touch_artifact` non-yours bane/covetous arms deferred in `js/artifact.js:944` (hero-path subset, always 1 for monsters — call wired for C order, behavior activates with that port).
**Next:** next Open queue row in order (`objnam.c` mshot_xname — Nth/xname arms).
## 2026-09-06 — D-1929 weapon.c hitval blessed/spear/trident/pick arms (vs-mon remaining arms)

**C locus:** `weapon.c` `hitval` `:149–187`; `kebabable[]` `:71–75` (S_XORN, S_DRAGON, S_JABBERWOCK, S_NAGA, S_GIANT); `obj.h` `is_spear` `:233–234` (WEAPON_CLASS + P_SPEAR), `is_pick` `:220–222` (WEAPON/TOOL + P_PICK_AXE), `is_weptool` `:249–250`; `mondata.h` `is_swimmer` `:25` (M1_SWIM), `passes_walls` `:29` (M1_WALLWALK), `thick_skinned` `:69` (M1_THICK_HIDE); `mondata.c` `mon_hates_blessings` `:532–536`; `dbridge.c` `is_pool` `:45–59`.
**JS:** `js/weapon.js` (+21/−4), `js/objects.js` (+10/−1); 2 js files, under 600 cap.
**Change:** `js/weapon.js` — C-order ports of all four arms in C branch order (blessed `Is_weapon && blessed && mon_hates_blessings` +2; spear `is_spear && kebabable` +2; TRIDENT + `is_swimmer` → `is_pool(mx,my)` +4 else eel/snake +2; `is_pick && passes_walls && thick_skinned` +2); file-local `KEBABABLE_MLETS` (C static-array shape, mlet strings per the `monsters_data.js` idiom); TRIDENT via the `objectNames` string idiom like `dmgval`; `ptr = mon?.data` like `dmgval`. `js/objects.js` — canonical `export function is_spear` beside `is_pick`/`is_axe` (P_SPEAR added to the const.js import) instead of clone #3. `is_pick` joins the existing objects.js import; `is_swimmer`/`passes_walls` extend the existing monsters.js edge (`mon_hates_blessings`/`thick_skinned` were already imported); `is_pool` from `js/hack.js` (`imports.mjs --can`: hoisted, cycle-safe; precedent `apply.js`).
**Verify:** `node scripts/verify.mjs --fn hitval` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/objects.js js/weapon.js`; `PASS rule2`; `note hidden verify hitval: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; re-ran `--full` → `PASS full 44/44`. Hand probe (`/tmp/hitval-probe.mjs`, 6 assertions on the real export, deleted): dagger oc_hitbon base 2; spear vs dragon +2, vs human 0; dagger vs dragon no spear bonus; pick vs wall+thick +2, thick-only 0. PROBE-OK. (Blessed/trident arms ride the same live `mon_hates_blessings`/`is_pool` calls `dmgval`/`apply.js` already make.)
**Named:** `dothrow.js:354` / `u_init.js:1037` file-local `is_spear` clones kept (not this cluster); no silver arm exists in C `hitval` — the row's "silver" is the `dmgval` blessed/axe/silver `rnd()` bonus shipped in D-1793.
**Next:** next Open queue row in order (`weapon.c` select_hwep — HTH weapon-select arms).
## 2026-09-06 — D-1928 zap.c bhit monster-hit dispatch + rock skip (beam-hit remaining arms)

**C locus:** `zap.c` `bhit` `:3832–4139` (setup `:3842–3868`, skip `:3944–3970`, dispatch `:3994–4039`, draw/tail `:4089–4139`); staticfn `skiprange` `:3579–3588` (only caller bhit); `M_IN_WATER` macro `:61` (`S_EEL || cant_drown`); `perceives` `mondata.h:81` (`M1_SEE_INVIS`).
**JS:** `js/zap.js` (+156/−38); 1 js file, under 600 cap.
**Change:** `js/zap.js` — file-local `bhit_skiprange` (C staticfn shape, `Math.trunc` int division, `rnd` 1..n, `skipend >= tmp` clamp); rock setup (`THROWN_WEAPON` + ROCK → window + `!rn2(3)`, before tether remap like C); DISP_BEAM open for FLASHED; full `:3994–4039` dispatch (notonhead-all; FLASHED minvis `ox/oy` + `flash_hits_mon` vs visible stop; INVIS perceives-stop; thrown/kicked `map_invisible`; ZAPPED fhitm-stop else `r -= 3`; WAN_PROBING `unmap_object` + `newsym`); C-predicate erase/tmp/END (no obj gate); pay gated on `!bhit_done`. Five added import names, all same-edge (`imports.mjs --can` ALREADY ×5: display `unmap_object`/`cmap_to_glyph`, const `S_flashbeam`, monsters `M1_SEE_INVIS`, mon `cant_drown`); `perceives`/`M_IN_WATER` as inline macro expansions over canonical imports (no clone #8). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn bhit` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/zap.js`; `PASS rule2`; `note hidden verify bhit: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; re-ran `--full` → `PASS full 44/44`.
**Named:** THROWN_WEAPON fly callers (throwit inlines those); `is_pick`/`shkcatch` pick (`:3887–3892`, no JS export); HEAVY_IRON_BALL boulder/uball stop (`:4102–4121`, needs sobj_at export + test_move); apply.js keeps its own `bhit_invis_beam`/flash-walk clones for mirror/camera paths (not this cluster).
**Next:** next Open row `weapon.c` hitval (blessed/spear/trident/pick/silver arms).
## 2026-09-06 — D-1927 invent.c getobj takeoff live getobj + takeoff_ok (prompt/filter remaining arms)

**C locus:** `invent.c` `getobj` `:1751–2089` (prompt/filter arms `:1832–2089`); `do_wear.c` `takeoff_ok` `:3471–3475` (`equip_ok(obj, TRUE, FALSE)` `:3403–3447`); `do_wear.c` `dotakeoff` `:1831–1852` (`Narmorpieces != 1 || ParanoidRemove || gi.item_action_in_progress` → `getobj("take off", takeoff_ok, GETOBJ_NOFLAGS)`).
**JS:** `js/do_wear.js` (+16/−57); 1 js file, under 600 cap.
**Change:** `js/do_wear.js` — added `takeoff_ok(obj) { return equip_ok(obj, true, false); }`; deleted `takeoff_lets` + `getobj_takeoff` (57 lines); `dotakeoff` calls live `getobj('take off', takeoff_ok, GETOBJ_NOFLAGS)` with the C gate (`Narmorpieces !== 1 || paranoid || game.item_action_in_progress`). No new import (`getobj` already imported); no new cross-module edge. uskin merged-with-skin stays a named omit.
**Verify:** `node scripts/verify.mjs --fn getobj` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/do_wear.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify getobj: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks at HEAD — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`.
**Named:** uskin merged-with-skin (`do_wear.c` `:1840–1845`, needs GRAY_DRAGON_SCALES constants); `display_pickinv` body (not this cluster); remaining getobj_* clones drop/wield/apply/write/dip; `canwearobj` polyform; readchar_core fuzzer/queue/ALTMETA.
**Next:** next Open queue row in order (`zap.c` bhit — beam-hit body remaining arms).
## 2026-09-06 — D-1926 pickup.c pickup autopickup arm (autopick/autopick_testobj/exceptions, unconscious skip, shared tail)

**C locus:** `pickup.c` `check_autopickup_exceptions` `:912–927`; `autopick_testobj` `:929–965` (costly `:939–946`, thrown/stolen `:950–953`, dropped/exploding `:954–957`, types `:960`, exceptions `:963–965`); `autopick` `:974–1003` (check_costly TRUE once `:987–989`, fill loop `:993–999`); `pickup` `:672–910` (unconscious `:684–688`, `menu_pickup` `:772–784`, traditional `end_query`, tail `:893–903`); `hack.c` `cannot_push` `autopick_testobj(otmp, TRUE)` `:274–276`; `do.c:777` `how_lost = LOST_DROPPED`; `optlist.h` defaults `pickup_thrown`/`pickup_stolen`/`dropped_nopick` all On; `extern.h:2461–2462` (both functions extern, not static).
**JS:** `js/pickup.js` (+184/−79), `js/hack.js` (+3/−12), `js/options.js` (+6/−1); 3 js files, under 600 cap; `docs/c-js-map/turns.md` one section.
**Change:** `js/pickup.js` — exported `check_autopickup_exceptions` (C-order walk over `game.apelist`, `makesingular(doname(obj))` + `regex_match`, null on empty); module-static `autopick_costly` + exported `autopick_testobj(otmp, calc_costly)` in exact C order (costly via live `costly_spot`, thrown/stolen override with `!== false` = C On defaults, dropped/exploding reject, types through the repo `oclass_to_sym` symbol-string convention — C stores `(char)` class indices and compares raw oclass, same verdict — then exceptions); file-local `autopick(olist, followNobj)` with C's two-pass shape and `{item, count = quan}`; `pickup()` runs the unconscious skip before the encumbrance reset, the autopick arm through `autopick` + menu_pickup loop, C tried/picked discipline on every manual arm (ct==1 returns tried, traditional returns `{tried, picked}`), and one shared tail (`hides_under → hideunder(game.youmonst)`, `n_picked → newsym_force`, autopickup-only `check_here`) in `finally` before the pickupdone reset. `js/hack.js` — clone deleted, canonical `js/pickup.js` import (imports.mjs: pickup→teleport SAFE; hack→pickup CHECK, lazy runtime read of a hoisted export only). `js/options.js` — `regex_match` exported (no new edge; pickup already imports from options).
**Verify:** `node scripts/verify.mjs --fn pickup` → VERIFY: PASS — `PASS syntax 3 changed js file(s): js/hack.js js/options.js js/pickup.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify pickup: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). Hand probe (`/tmp/probe-pickup-autopick.mjs`, 24 lines, deleted): 10/10 sync assertions on the real export — default dropped/exploding reject, default normal pick, thrown/stolen override non-empty types, types include/exclude, `nopick_dropped:false` opt-out picks, `pickup_thrown:false` falls back to types, empty-apelist null. PROBE-OK.
**Named:** AUTOPICKUP_EXCEPTION option parsing — no `game.apelist` producer (options.c), so the exceptions arm is live but dormant; engulfer minvent chain (`objchain_p`/`traverse_how` stay floor-only; `autopick` keeps C's followNobj param for it); count-N PICK_ONE/`n_or_more` menu; full `is_pool`; `safe_qbuf` truncation.
**Next:** next Open queue row in order (`invent.c` getobj — prompt/filter remaining arms).
## 2026-09-06 — Audit D-1918…D-1925 (reviews 888–895) + cadence score, no port

**Scope:** all 8 `js/`-touching SHAs since review 887 (`4a49397f` lava_effects Wwalking re-read, `55ee2d4d` mattackm arms, `240139ea` grow_up, `eb154b73` hitmu, `7868cc8b` mintrap, `53b920c5` makeplural, `b87ddc27` dotrap, `7571aee9` explode), one SHA at a time, each file written before opening the next. Every corpus claim re-measured via `hidden-proxy verify <fn> --base HASH~1`: all 0-at-baseline/0-now (all rows cited 0 blocks, nothing owed). `imports.mjs --rulecheck` clean. `sym.mjs` run on every re-pointed/deleted symbol (Conflict→export, mhe/YMonnam canonical, YMonnam_grow/mhe_grow gone, sticks=engrave export).
**Result:** 7 ACCEPT, 1 ACCEPT-WITH-DEBT (893 makeplural: `strcasecpy_at` overrun reads `base[at-1]` where C `strcasecpy` reads the just-written char — `CHILD`→`CHILDren` vs C `CHILDREN`; fires only on all-caps heads hitting longer one_off plurals, no in-game caller; one-line fix `out[out.length-1]` listed in the review, no Must-fix per verdict rules). No Must-fix prepend; Next cluster unchanged. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `77+0.61/turn` (R² 0.80).
**Next:** next Open queue row in order (`pickup.c` pickup body remaining arms).
## 2026-09-06 — D-1925 explode.c explode remaining arms (mdef credit, grab double-damage, hallu renames, Invulnerable, monstsee, TRAP killer, fatal It/The)

**C locus:** `explode.c` `explode` `:199–696` — expltype<0 mdef `:264–269`; grabbed/grabbing/grabxy `:275–284`; MON_EXPLODE str/do_hallu `:298–305`; adtyp switch + default impossible/return `:306–352`; per-target hallu `:490–502`; grabbed×2 `:543–544`; mdef self-kill `:562–576`; hero hallu + last_msg `:595–603`; Invulnerable `:608–610`; grabbing×2 `:624`; monstsee `:636–639`; killer arms `:645–660`; fatal It/The `:668–672`. Helpers: `sticks` is `mondata.c:654` (AD_STCK / non-engulf WRAP / HUGS); `monstseesu_ad` is `monst.h:92` (`monstseesu(cvt_adtyp_to_mseenres(adtyp))`); `Invulnerable` is `youprop.h:73` (`uprops[INVULNERABLE].intrinsic`); `uhim`/`uhis` are the `you.h` gender forms.
**JS:** `js/explode.js` (+155/−23, 1 file, under 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** `js/explode.js` — C-order ports of every arm above: mdef + expltype negation right after the olet preamble; grabbed/grabbing/grabxy from `ustuck`/`uswallow`/`Upolyd` + canonical `sticks` (`js/engrave.js`, the C-shaped export; imports.mjs hoisted-SAFE); do_hallu gate (`Hallucination()` + `strstri`, live `js/hacklib.js` edge) with per-target 20-try and hero unbounded rename loops (ASCII-upper redraw check = C `lowc` semantics, no new clone); file-local `next2u` (mon.js idiom) + `dist2` for the two double-damage arms in C positions (monster arm between resist and cold/fire double; hero arm inside uhurt==2 before the Upolyd split); mdef branch between the xkilled and monkilled arms (existing xkilled import hoisted one level); hero verbose hallu + `last_msg = PLNMSG_CAUGHT_IN_EXPLOSION`; Invulnerable → damu=0 + "You are unharmed!" as the if before the PHYS/ACID half-phys else-if; monstsee pair after the damage block; TRAP arm with `uhim()` + "in a", own-blast arm with `uhim()`/`uhis()` (canonical `js/roles.js` edge); fatal It/The on last_msg CAUGHT_IN_EXPLOSION/TOWER_OF_FLAME; `await impossible(...)` on wand-bad-type and on the switch default with return. New static edges hacklib (strstri/dist2), mondata (monstseesu/monstunseesu/cvt_adtyp_to_mseenres), roles (uhim/uhis), engrave (sticks) — all imports.mjs hoisted-SAFE, same shape as the 1866 existing edges; display/do_name/const extended on existing edges only.
**Verify:** `node scripts/verify.mjs --fn explode` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/explode.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify explode: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (`/tmp/probe-explode.mjs`, 33 lines, deleted): real `explode()` headless, seeded RNG, 60 pre-fed keys — dam0 MON_EXPLODE leaves last_msg≠CAUGHT (uhurt 0); hallu gate (killer "gas spore's explosion" + HHallucination) runs clean; TRAP dam1 with poly + STCK-grabber → mh 10→8 (grabbing×2 exactly once) + CAUGHT stamp; hero-hallu MAGM run → uhp 10→9 + CAUGHT (rename loop terminates on real display-RNG draws); bad base type → impossible + early return. PROBE-OK. (Two probe-harness fixes, not port fixes: `--More--` needed `pushKeys`, RNG needed `initRng`, and the ×2 assertion reads `mh` under Upolyd per C `:628–631`.)
**Named:** You_hear Underwater/Unaware prefixes (no live Unaware export — D-1919 map note); ugolemeffects/golemeffects (no JS port); Upolyd rehumanize fatal path (separate-subsystem call, kept from the old deferral); resists_magm worn/artifact ANTIMAGIC scan (kept inside the helper's doc); Role_switch damu roles beyond the known five (kept).
**Next:** next Open queue row in order (`pickup.c` pickup — pickup body remaining arms).
## 2026-09-06 — D-1924 trap.c dotrap full dispatch body (FAILEDUNTRAP, fixed_tele, Sokoban air, plunge/conj/adj, clinger, steed)

**C locus:** `trap.c` `dotrap` `:2996–3060` — `forcetrap=(FORCETRAP||FAILEDUNTRAP)`, `forcebungle=FORCEBUNGLE`, `plunged=(TOOKPLUNGE)`, `conj_pit=conjoined_pits(trap,t_at(u.ux0,u.uy0),TRUE)`, `adj_pit=adj_nonconjoined_pit(trap)`; `nomul(0)`; `fixed_tele_trap(trap)` → `trflags|=FORCETRAP`, `forcetrap=TRUE`; `Sokoban && (is_pit||is_hole)` → `pline("Air currents pull you down into %s %s!", a_your[madeby_u], trapname(ttype,TRUE))` then fall through; else `!forcetrap`: `floor_trigger && check_in_air(&youmonst,trflags)` → `You("%s over %s %s.", u_locomotion("step"), ARROW-an/a_your, trapname(FALSE))` + return; `already_seen && !Fumbling && !undestroyable && !=ANTI_MAGIC && !forcebungle && !plunged && !conj && !adj && (!rn2(5) || (is_pit && is_clinger(youmonst.data)))` → `You("escape %s %s.", …)` + return; `u.usteed → mon_learns_traps(usteed,ttype)`; `mons_see_trap(trap)`; `trapeffect_selector(&youmonst,trap,trflags)`. `a_your[2]={"a","your"}` (`trap.c:77`); `fixed_tele_trap` is `trap.h:125-126` (`TELEP_TRAP && isok(teledest)`); `u_locomotion("step")` is `hack.c:1817` (`Levitation?float:Flying?fly:locomotion(data,def)`).
**JS:** `js/trap.js` (+34/−14 in the dotrap hunk; 2 files incl. `docs/CURRENT.md` Next-cluster, under 600 cap); `docs/c-js-map/data.md` one section.
**Change:** `js/trap.js` — C-order full body, no new imports (all names live on existing edges: `FAILEDUNTRAP`/`TOOKPLUNGE`/`FORCETRAP` from `js/const.js`, `Fumbling` from `js/attrib.js`, `is_clinger`/`mon_learns_traps` from `js/monsters.js`, `is_pit`/`is_hole` from `js/const.js`; file-locals `conjoined_pits`/`t_at`/`fixed_tele_trap`/`floor_trigger`/`check_in_air`/`undestroyable_trap`/`adj_nonconjoined_pit` reused, no clone #2): `forcetrap` includes `FAILEDUNTRAP`; `plunged`/`conj_pit`/`adj_pit` computed before `nomul(0)` per C; `fixed_tele_trap` mutates `trflags` before the selector; Sokoban arm with the repo `game.level?.flags?.sokoban_rules || game.Sokoban` idiom and `trapname(ttype,true)`; step-over verb via file-local `u_locomotion_pit('step')` (Lev/Fly only); escape keeps C short-circuit `(!rn2(5) || (is_pit && is_clinger(youmonst.data)))` so the `rn2(5)` draw count is unchanged; `Fumbling()` replaces the `u.Fumbling` sticky read; `u.usteed → mon_learns_traps`.
**Verify:** `node scripts/verify.mjs --fn dotrap` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/trap.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify dotrap: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2 passing`; `PASS strict` both gate sessions; `PASS cohort 7/7 passing`; `skip full (no shared file changed)`. No hand probe: every newly wired callee is live and cohort-covered; the Sokoban/clinger/plunge/conj arms are reachable only off-corpus and follow C line-for-line.
**Named:** poly-form `locomotion()` verbs beyond Lev/Fly (file-local `u_locomotion_pit` keeps the helper's existing Lev/Fly-only deferral); none new.
**Next:** next Open queue row in order (`explode.c` explode — explosion body remaining arms).
## 2026-09-06 — D-1923 objnam.c makeplural full plural-name envelope (singplur_lookup, badman, ch_ksound live)

**C locus:** `objnam.c` `makeplural` `:2836–3022` — pair-of skip `:2879`; compound split `:2883` with verbatim excess; blank strip (`spot > str`) `:2891`; single-letter/non-letter `'s` `:2897`; `singplur_lookup` already_plural `:2905–2916`; ya `:2917`; man→men (badman guard) `:2922`; [aeioulr]f→ves with erf fall-through `:2928`; ium→ia `:2940`; alga-type +e `:2944`; us→i except lotus/wumpus `:2954`; sis→ses `:2962`; eau→eaux except bureau `:2967`; matzoh/matza→matzot `:2973–2980`; dex/dix/tex→ices except index `:2987`; z/x/s/ch/sh+es with `ch_ksound` gate + ato/dingo `:3000`; consonant-y→ies `:3011`; default +s `:3016`; all through `Strcasecpy`. Helpers: `singplur_lookup` `:2707–2779` (as_is `:2719`, alt `:2724`, craft `:2732`, slice/mongoose whole-word `:2739`, ox except muskox `:2746`, badman-man +s `:2753`, one_off same-stays/sing-transforms `:2764`); `badman` `:3193–3239` (no_men/no_man, start-or-after-space anchor); `ch_ksound` `:3167–3191`; `strcasecpy`/`chrcasecpy` (`hacklib.c:322–341`/`300–313`); `letter`/`lowc` (`hacklib.c`); `BSTRCMPI` (`objnam.c:66`); `vowels` (`decl.c:111`); `one_off[]` `:2662`, `as_is[]` `:2689`, `already_plural[]` `:2905`, `compounds[]` (JS clone verified identical, reused).
**JS:** `js/objnam.js` (+270/−38, 1 file, under 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** `js/objnam.js` — C-order full body on a head/excess split (no recursion): strip, `'s` arm, `singplur_lookup_plural` (file-local, full `as_is[]` 8+25 + `already_plural` + craft + whole-word slice/mongoose + ox/muskox + badman-man + one_off same-first loop), ya, man→men (`badman(head, true)`), f→ves with erf fall-through to the later arms (not straight to default, per C control flow), ium/alga/us/sis/eau/matzoh/matzo/dex/zxs-ch_ksound-ato-dingo/y/default arms, every write through file-local `strcasecpy_at`/`chrcasecpy`/`eqCI`/`plural_lowc`/`plural_letter` mirroring `strcasecpy`, `strcmpi`, `lowc`, `letter` (incl. `@`..`Z` gap chars and the `spot > str` lone-blank keep). New file-local `badman` (both directions; singular arm ready for a future makesingular port), `ch_ksound` + 19-word `ch_k` (the row's dead callee, now live).
**Verify:** `node scripts/verify.mjs --fn makeplural` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/objnam.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify makeplural: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (`/tmp/makeplural-probe.mjs`, 26 lines, deleted): 116/116 C-derived expectations PASS — -of/compounds/pronouns/A's/7's. (First run 115/116: probe expected `supermans`; C `no_men[]` carries `superhu` not `super`, so C also yields `supermen` — probe corrected, port unchanged.)
**Named:** impossible("plural of null?") `:2841` log on null/empty (async pline chain through `urgent_pline`; sync makeplural keeps C's `"s"` return); `badman` !to_plural direction ported but uncalled (`makesingular` is a separate C function with its own inline men→man + badman defer — untouched); `makesingular` otherwise untouched.
**Next:** next Open queue row in order (`trap.c` dotrap — hero/monster trap dispatch body).
## 2026-09-06 — D-1922 trap.c mintrap dispatch body (trapped-arm escape/chew, fresh-arm gates, unhide envelope)

**C locus:** `trap.c` `mintrap` `:3733–3840` — trapped arm: seetrap reveal for pit/bear/hole/web; `!rn2(40) || (is_pit && m_easy_escape_pit)`; boulder-in-pit `!rn2(2)` → mtrapped=0, `pline_mon "%s pulls free..."`, `fill_pit`, else `set_msg_xy` + `pline "%s climbs %sout of the pit."` (`"easily "` iff easy) / `pline "%s pulls free of the %s."` for bear/web; `metallivorous` bear-trap eat (deltrap, meating=5, mtrapped=0) / spiked-pit munch (ttyp=PIT, meating=5); result `mtrapped ? Trap_Caught_Mon : Trap_Effect_Finished`. Fresh arm: `fixed_tele_trap` → `mintrapflags |= FORCETRAP`; usteed no-op; Sokoban `is_pit||is_hole` without `madeby_u` skips both gates; else `floor_trigger && check_in_air` and `already_seen && rn2(4) && !forcebungle` early-outs; `mon_learns_traps` + `mons_see_trap`; `madeby_u && rnl(5)` → `setmangry(mtmp, FALSE)`; `trapeffect_selector` with the mutated flags; `!DEADMONSTER && mtrapped` → `maybe_unhide_at` + `pline_mon "%s appears."` (`Amonnam`) iff newly spotted.
**JS:** `js/trap.js` (+84/−29, 1 file, under 600 cap); `docs/c-js-map/data.md` one section.
**Change:** `js/trap.js` — C-order full body: `!rn2(40) || (is_pit && m_easy_escape_pit(mtmp))` (short-circuit kept; the file-local `m_easy_escape_pit` and `sobj_at` reused, no new clones); boulder arm with `!rn2(2)`, `pline_mon` pulls-free and `fill_pit`; `set_msg_xy` + `easily ` ternary in the else arm; `metallivorous(mtmp.data)` chew arms (canonical `js/monsters.js` import); fresh arm threads mutated `trflags` (fixed_tele_trap force) through `check_in_air` and `trapeffect_selector`; usteed/Sokoban arms with the repo `game.level?.flags?.sokoban_rules || game.Sokoban` idiom; `trap.madeby_u && rnl(5)` → `await setmangry(mtmp, false)` (canonical `js/mon.js` import); result captured, then `(mhp|0) > 0 && mtrapped` → `canspotmon` + `await maybe_unhide_at` (new `js/monmove.js` edge, imports.mjs hoisted-SAFE) + `Amonnam` appears via `pline_mon` (both canonical imports added to existing edges). All RNG draws keep C short-circuit order.
**Verify:** `node scripts/verify.mjs --fn mintrap` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/trap.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify mintrap: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (`/tmp/probe-mintrap.mjs`, 27 lines, deleted): metallivorous + bear trap → `{"freed":true,"trapGone":true,"meating":5}` (chew arm state effects exact); huge non-eater in pit → freed (easy-escape arm; pre-fix this stuck 97.5%); HURTLING over dart trap → `r:0` + `mtrapseen` still 0 (in-air gate before learning). PROBE-OK.
**Named:** `trapeffect_selector` arm coverage still partial (arrow/anti-magic/… named at the selector); file-local `m_easy_escape_pit` pit-fiend identity arm is dead (`data === mons[PM_PIT_FIEND]` indexes the `mons()` function → undefined; pit fiend msize 3 < MZ_HUGE 4, so C's always-escapes pit fiend never easy-escapes in JS — verified live, queued as its own Open row); turns.md untrap/chest_trap envelope untouched.
**Next:** next Open queue row in order (`objnam.c` makeplural — plural-name envelope), plus the new `trap.c` m_easy_escape_pit identity row.
## 2026-09-06 — D-1921 mhitu.c hitmu envelope: midnight undead extra, Half/Mitre halve, permdmg hpmax cut, knockback defender

**C locus:** `mhitu.c` `hitmu` `:1144–1267` — `mhm.damage = d(damn,damd)` then `is_undead(mdat) || is_vampshifter(mtmp)` + `midnight()` bonus `d()`; `mhitm_adtyping` + `mhitm_knockback(mtmp, &gy.youmonst, mattk, &hitflags, MON_WEP!=0)`; `Half_physical_damage || (Role_if(PM_CLERIC) && uarmh && is_quest_artifact(uarmh) && mon_hates_blessings(mtmp))` → `(damage+1)/2` (not applied to permdmg); `permdmg = rn2(damage/2+1)` with Upolyd/`uhpmax > 25/10/5 * ulevel` scaling, `hpmax_p = &mhmax` (`min(youmonst.data->mlevel, ulevel)`) vs `&uhpmax` (`minuhpmax(1)`), clamp at lowerlimit, `disp.botl = TRUE`. `you.h:247` `Role_if`, `youprop.h:339–341` `Half_physical_damage`, `questpgr.c:67` `is_quest_artifact ≡ oartifact == urole.questarti`.
**JS:** `js/mhitu.js` (+52/−12, 1 file, under 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** `js/mhitu.js` — C-order midnight arm (`is_undead(mdat) || is_vampshifter(mtmp)` from live `js/monsters.js`, `midnight()` from live `js/calendar.js`); knockback defender `game.youmonst` (flags still by value per the `js/mhitm.js` stub convention); Half arm with `HHalf_physical_damage || EHalf_physical_damage` plus Cleric+Mitre (`game.urole.mnum === PM_CLERIC` from `js/generated/monsters_data.js`, `u.uarmh.oartifact === urole.questarti` with `want!==0` repo guard, `mon_hates_blessings(mtmp)`) and `Math.trunc((damage+1)/2)`; permdmg block with `rn2(Math.trunc(damage/2)+1)`, `Math.trunc(damage/2|/4)` scaling, `Upolyd(u)` poly (`Math.min(youmonst.data.mlevel, ulevel)` → `mhmax`) vs normal (`minuhpmax(1)` from `js/attrib.js` → `uhpmax`) clamp, both `disp`+`flags` `botl`; removed the orphan dupe doc, kept the C-range doc naming the adtyping subset. All added names ride existing same-SCC imports (no new module edge).
**Verify:** `node scripts/verify.mjs --fn hitmu` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mhitu.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify hitmu: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. No hand probe: `hitmu` is not exported (local clone), and every newly wired callee is live and cohort-covered; the midnight/Half/permdmg arms are reachable only off-corpus and follow C line-for-line.
**Named:** full `mhitm_adtyping` arms beyond `mhitm_adtyping_u` (AD_FIRE hero, ACID/WERE/HEAL/SGLD/TLPT/CURS/CORR/DCAY/DREN/STCK/PLYS/SLIM/ENCH/SLOW/CONF/DISE/SAMU/DETH/PEST/FAMN/DGST/HALU — C default zeroes damage, JS helper matches the default); `mhitm_knockback` hurtle body still deferred in `js/mhitm.js` (stub burns prefix `rn2(3)`/`rn2(6)` and returns false); `passiveum` deferred detail already named at its doc.
**Next:** next Open queue row in order (`trap.c` mintrap — monster-trapped dispatch body).
## 2026-09-06 — D-1920 makemon.c grow_up remaining arms (little_to_big form change, lev_limit caps, sanity tail)

**C locus:** `makemon.c` `grow_up` `:2049–2178` — victim arm `:2082–2105` (`m_lev*8`, `!m_lev→4`, golem `((mhpmax/10)+1)*10-1`, home-elemental `*3`; `lev_limit = 3*ptr->mlevel/2` raised to `mons[newtype].mlevel`; `rnd(victim->m_lev+1)` clamped to `hp_threshold+1`, `cur = max>1 ? rn2(max) : 0`); no-victim arm `:2106–2112` (one `rnd(8)` for both, threshold 0, limit 50); shared tail `:2114–2176` (`mhpmax<=threshold` early return; mplayer 30 / `<5→5` / `>49→(mlevel>49?50:49)`; unconditional `++m_lev` with conditional form change; GENOD `pline` + `set_mon_data` + `mondied`; gender-hack `pline_mon`; cham/newsym/`lev_limit=m_lev`/female/`mleashed→update_inventory`; sanity undo + 400 cap + `mhp` clamp). `monst.h:268` `Mgender ≡ female ? FEMALE : MALE` (kept the existing inline ternary — canonical `Mgender()` adds youmonst handling C's macro lacks); `you.h:322` `mhe` (canonical import replaces the clone).
**JS:** `js/mhitm.js` (+84/−59, 1 file, under 600 cap); `docs/c-js-map/data.md` one section.
**Change:** `js/mhitm.js` — full C-order port: `newtype = little_to_big(oldtype)` (canonical `js/mondata.js` export, same-SCC import already present); victim arm with golem `Math.trunc(mhpmax/10)*10+10-1` (C integer division) and `is_home_elemental` (live `js/makemon.js` import) threshold arms + `lev_limit` base/raise; no-victim arm as one `rnd(8)` shared by both gains; unconditional `++m_lev` before the form-change test (C short-circuit order); mplayer/min/max `lev_limit` chain; GENOD arm with canonical `mhe` (`js/mondata.js`) and existing `mon_nam`/`pmname` ternaries; growth pline with canonical `YMonnam` (`js/do_name.js`); `cham`/`newsym`/`lev_limit=m_lev`/female/`mleashed→update_inventory()` (new `js/invent.js` import, imports.mjs-checked hoisted-SAFE); unconditional sanity tail returning `ptr`. Deleted the two local clones (`y_monnam` import entry removed with `YMonnam_grow`).
**Verify:** `node scripts/verify.mjs --fn grow_up` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mhitm.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify grow_up: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (`/tmp/probe-growup.mjs`, 24 lines, deleted): real `grow_up` — wraith-arm +1 level / +rnd(8) HP OK; kill under threshold keeps level OK; kitten(14,112hp)+m_lev-20 victim → HOUSECAT at 15 OK (earlier seed drew rnd(21)≤12 for a legitimate C no-gain — probe setup, not port); self-type giant rat at 49 + potion stays 49 OK (C sanity undo); gnome at 49 + potion → GNOME_LEADER at 50 OK (`lev_limit = m_lev` arm). The form-change / cap-undo / promotion arms cover paths absent pre-fix by code inspection (newtype was always oldtype so no promotion ever fired; cap was min-50 rather than C undo).
**Named:** none new — `monsndx` has no JS export (existing `data?.mndx ?? mnum ?? NON_PM` idiom kept); `deadmonster` local kept for the gas-spore pre-check (C `DEADMONSTER`).
**Next:** next Open queue row in order (`mhitu.c` hitmu — hero hit-by-thrown-object envelope).
## 2026-09-06 — D-1919 mhitm.c mattackm remaining dispatch arms (preamble, mswingsm, BREA/SPIT, melee guards, epilogue)

**C locus:** `mhitm.c` `mattackm` `:292–592` — grid-bug angle MISS `:316–317`; mundetected clear/newsym/notice `:327–352`; elf-vs-orc `tmp++` `:354–355`; `mswingsm` `:413–414` + staticfn `:1282–1297` (verbose/`!Blind`/`mon_visible`, polearm bash sans Snickersnee at dist2≤2, "one of" for quan>1); cockatrice no-swing `:434–439` (`break` keeps `attk` so passivemm still runs); unsolid pre-check `:447–453`; pudding division `:455–472` (iron/metal, mhp>1, !mcan → `clone_mon` + `mintrap`); AT_BREA/AT_SPIT `:538–559` (`!monnear` gate, non-MISS pretends hit, DEF_DIED/AGR_DIED overrides, else `strike=0,attk=0`); epilogue `:581–586` (AGR_DONE/helpless/`mon_offmap` returns). Materials `objclass.h:24–25` (IRON=11, METAL=12).
**JS:** `js/mhitm.js` (+129/−6), `js/mhitu.js` (+1/−1); `docs/c-js-map/turns.md` two sections; under 600 cap, 2 js files.
**Change:** `js/mhitm.js` — local `mswingsm` (C `:1282–1297` order; `game.flags?.verbose !== false` repo idiom + local `Blind_slee()` + `mon_visible`; `is_pole`/`is_art(,ART_SNICKERSNEE)`/`dist2`/`mswings_verb`/`mhis`/`xname`/`Monnam`/`mon_nam` all live imports, edge verified cycle-safe); preamble (grid-bug MISS; mundetected clear + `newsym` + generic `Suddenly, you notice` arm; `is_elf(pa) && is_orc(pd)` `tmp++`); `if (mwep && _mm_vis) await mswingsm(...)` at `:413–414`; petrifier `break` (C `:434–439`, `!magr.mconf && !Conflict() && mwep && aatyp != AT_WEAP`); unsolid+`failed_grab` pre-check; pudding division (`clone_mon` + `mintrap(mclone, NO_TRAP_FLAGS)`, AGR_DIED fold); AT_BREA/AT_SPIT case wiring live `breamm`/`spitmm` (same-SCC hoisted imports per `imports.mjs --can`, replacing the stale cycle comment); epilogue AGR_DONE/helpless/`mon_offmap` returns. `js/mhitu.js` — one-word `export` on the youprop.h-Conflict reader (was a local clone; mattackm imports it instead of cloning).
**Verify:** `node scripts/verify.mjs --fn mattackm` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mhitm.js js/mhitu.js`; `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates`; `note hidden verify mattackm: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and the log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. No hand probe: every wired callee is already live and cohort-covered; no arm is reachable only off-corpus in a way a 30-line probe could falsify beyond the gates.
**Named:** `thrwmm` ranged AT_WEAP arm (mthrowu `monshoot` is a local clone, not an export — distant AT_WEAP stays a miss; own follow-up when exported); mundetected Unaware "dream of %s" arm (`Unaware` state absent) + HIDE_UNDER/`last_hider` arms (`iflags.last_msg`, `gl.last_hider` absent); SEDUCE/`c_sa_no` already named at `get_mattk`.
**Next:** next Open queue row in order (`makemon.c` grow_up remaining arms).
## 2026-09-06 — D-1918 trap.c lava_effects post-boots-burn Wwalking live re-read

**C locus:** `trap.c` `lava_effects` `:6794–6987` — entry `usurvive = Fire_resistance || (Wwalking && dmg < u.uhp)` reads pre-burn values; the boots-burst block (`Boots_off()` then `useup`) runs before `if (!Fire_resistance) { if (Wwalking) … } else fall`, the double-fail countermeasure `if (!Wwalking) set_itimeout(&HWwalking, 5L)`, and the `else if (!Wwalking && (!u.utrap || u.utraptype != TT_LAVA))` sink arm. Macro `youprop.h:260` Wwalking ≡ `(HWwalking||EWwalking) && !Is_waterlevel`.
**JS:** `js/trap.js` (+13/−3, 1 file, < 600 cap); `docs/c-js-map/data.md` one section.
**Change:** `liveWwalking()` closure (same slot+flats+`Is_waterlevel` idiom as the snapshot) read at the three post-boots points (`if (Wwalking)` burns-you gate, sink-arm `else if (!Wwalking…)`, countermeasure `if (!Wwalking)`); entry snapshot kept for entry `usurvive` + the flag loop, where C also reads pre-burn values. Fire stays snapshot (no worn burnable Fire source; boots don't grant Fire). Chain confirmed in-tree, all synchronous: `Boots_off` (`js/do_wear.js:669`) → `clear_worn(W_ARMF)` (`:604`) → `setworn(null, …)` (`:519`, deconfer at `:534–544`) → `confer_oc_oprop(off)` clears `uprops[WWALKING].extrinsic` (`:322–331`); WW state lives in the slot, not the flats (`js/teleport.js:250`).
**Verify:** `node scripts/verify.mjs --fn lava_effects` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/trap.js`; `PASS rule2`; `note hidden verify lava_effects: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none, review 883 re-measured 0 at baseline — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probe (`/tmp/ww-probe.mjs`, 24 lines, deleted): WW water-walking boots worn as sole WW source (W_ARMF extrinsic, organic ungreased) + no Fire + uhp 100 (entry survive deterministic, `d(6,6)` ≤ 36) + lava step on the real `lava_effects` → post-fix `uarmf` burned + extrinsic 0 + `uhp` 0 + gameover (fall path); same setup pre-fix → `uhp` 83 + no gameover (stale burns-you path). FAILS pre-fix / PASSES post-fix — a true falsifier.
**Named:** none new.
**Next:** next Open queue row in order (`mhitm.c` mattackm remaining arms).
