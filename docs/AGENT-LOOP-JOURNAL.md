# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-07 — D-2028 pickup.c use_container: thesimpleoname export (sack, not bag) + Tobjnam locked branch (2 PASS + 3 moved)

**C locus:** `pickup.c:2972–3226` `use_container` — olocked `:2994–2999` (`pline("%s locked.", Tobjnam(obj, "are"))`, held-only `You("must put it down to unlock.")`, no `lknown` touch); emptymsg `:3043–3046` (`Ysimple_name2`); static `in_or_out_menu` `:3397–3477` (`:3420` `thesimpleoname`, `:3448` stash row); `do_loot_cont` `:2103–2111` owns the Hmmm/`lknown` arm for floor #loot; `objnam.c:1038` `minimal_xname` (undiscovered → descr "bag", known → "sack"); `u_init.c` Rogue/Archeologist `knows_object(SACK)` + `preknown = SACK` with `ini_inv` `dknown = 1`.
**JS:** 1 file (`pickup.js` +14/−13), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/pickup.js` — `Tobjnam` + `thesimpleoname as thesimpleoname_objnam` extend the pre-existing `./objnam.js` import (same SCC edge, runtime-only reads, no TDZ risk); `in_or_out_menu` Look/stash rows via the discovery-aware export (known sack → "the sack"); emptymsg + both loot-out fallbacks via the already-imported `Ysimple_name2_objnam`; olocked arm is C-verbatim `Tobjnam(obj, 'are')` + held put-down with no `lknown` touch; `do_loot_cont` keeps its Hmmm arm untouched. Local clones stay for `pickup_object`/`loot_mon`/`tipcontainer` (out of cluster, NOTES clone drift).
**Verify:** `node scripts/verify.mjs --fn use_container` → `PASS syntax 1 changed js file(s): js/pickup.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify use_container: 2 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (Archeologist-92228 PASS; Rogue-92160 PASS; intrinsic-Rogue-92089 → `disclose`@102 was 84; kit-Rogue-92225 → `peffect_sickness`@47 was 22; wish-Knight-92045 → `tipcontainer_gettarget`@79 was 71) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · full skipped (no shared file changed) · `VERIFY: PASS`. Baseline `hidden-proxy verify use_container` before the edit was 0/0/5 NO MOVEMENT. Final verify ran after the last edit (no D-1831 gap).
**Named:** chest trap; bag-of-tricks/horn; cursed-mbag `boh_loss` + `"now "` (pre-existing envelope, untouched); `lknown` pre-branch + `sellobj_first` (pre-existing, untouched); local `simpleonames`/`thesimpleoname`/`ysimple_name` clones for non-cluster call sites (pre-existing NOTES drift, untouched).
**Next:** moved owners `disclose`@102, `peffect_sickness`@47, `tipcontainer_gettarget`@79 join future queue rows for those owners.
## 2026-09-07 — Audit D-2020…D-2027 against pinned C (reviews 990–997) + cadence score 44/44

7 ACCEPT, 1 ACCEPT-WITH-DEBT (991: readobjnam `:4469–4477` grey-spell/armour bp fixups unported+unnamed → map-name debt, no Must-fix). Every D-log corpus claim re-measured via `hidden-proxy verify <fn> --base <sha>~1`: 990 vacuous-confirm; 991/992/993/995/996/997 reproduce exactly (0 worse throughout); 994 reproduces incl. the wish-Tourist-91125 same-step flag (rebase drift, recipe never polyselfs — attribution accepted). Rule #2 clean, full `sessions` 44/44 (Scr 11405/11405, RNG 792838/792838). Retired stale Next-cluster pick_nasty pointer → queue head use_container. Must-fix stays empty; Open 11 in band 8–12, no refill.
## 2026-09-07 — D-2027 wizard.c pick_nasty: juvenile name-string gate on big_to_little alt (genesis-symptom writer, 1 moved)

**C locus:** `wizard.c:537–581` `pick_nasty` — `:560–566` geno/difcap/hell demotion `alt = big_to_little(res)`; `:567–579` accept arm with the juvenile name-string gate: `mnam = mons[alt].pmnames[NEUTRAL]`, `lastspace = strrchr(mnam, ' ')`, `res = alt` only if `strncmp(mnam, "baby ", 5)` and `lastspace` is null or not ` hatchling`/` pup`/` cub` (`/* only non-juveniles can become alternate choice */`). ROLL_FROM hit adult GREEN_DRAGON (a NASTIES entry), demotion produced the baby alt, C kept the adult.
**JS:** 1 file (`makemon.js` +16/−4), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/makemon.js` `pick_nasty` — verbatim port of the `:567–579` gate (`pmnames[alt]?.[NEUTRAL]`, `lastIndexOf(' ')`→slice for `lastspace`, `startsWith('baby ')` + the three suffix comparisons, same short-circuit shape; pure string ops, no RNG). `pmnames` + `NEUTRAL` extend the pre-existing `./monsters.js` static import (`imports.mjs --can`: ALREADY, no new edge). Doc comment retires the juvenile omission (rogue monsym retry stays named).
**Verify:** `node scripts/verify.mjs --fn pick_nasty` → `PASS syntax 1 changed js file(s): js/makemon.js` · `PASS rule2` · `note hidden` (vacuous, as row predicts) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared file changed) · `VERIFY: PASS` · corpus: `node scripts/hidden-proxy.mjs verify newmonhp` → `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (Caveman-92118 → `mhitm_mgc_atk_negated`@127 was 97) · direct replay `frozen/ps_test_runner.mjs .cache/hidden/sessions/scen-genesis-Caveman-92118.session.json`: RNG 6371→6489/6758, screens 105→128/167. Final verify ran after the last edit (no D-1831 gap).
**Named:** rogue-level monsym uppercase re-ROLL (`:545–547`, pre-existing, untouched — monsym table not wired in makemon.js).
**Next:** moved owner `mhitm_mgc_atk_negated`@127 joins future queue rows for that owner.
## 2026-09-07 — D-2026 eat.c done_eating: nomovemsg wins over finish-eating line + fire-elemental consuming (lesshungry-symptom writer, 4 PASS + 3 moved)

**C locus:** `eat.c:543–573` `done_eating` — `:552–556` `if (gn.nomovemsg) { if (message) pline1(gn.nomovemsg); gn.nomovemsg = 0; } else if (message) You("finish %s %s.", fire-elemental ? "consuming" : "eating", food_xname(piece, TRUE))`. The recorded literal lives in `lesshungry` `:3315` (`gn.nomovemsg = "You're finally finished."`), which is why the proxy attributes the row to `lesshungry`.
**JS:** 1 file (`eat.js` +13/−3), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (diagnosis via recorded C screens + `brief`/`csym` C bodies, outside the repo).
**Change:** `js/eat.js` `done_eating` — nomovemsg arm first (print when message, always clear to null, cf. `unmul` in `hack.js`); else-branch gains the fire-elemental `consuming` variant via the canonical `Upolyd(u) && umonnum === PM_FIRE_ELEMENTAL` idiom (`mons()` builds a fresh record per call; `timeout.js:694` precedent). No new imports (`Upolyd` + `PM_FIRE_ELEMENTAL` already module-local).
**Verify:** `node scripts/verify.mjs --fn lesshungry` → `PASS syntax 1 changed js file(s): js/eat.js` · `PASS rule2` · `PASS hidden 4 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (Monk-92139 PASS; normal-Archeologist-92236 PASS; normal-Valkyrie-92077 PASS; normal-Valkyrie-92108 PASS; kit-Archeologist-92190 → `monshoot`@8 was 7; kit-Valkyrie-91116 → `doengrave`@51 was 6; normal-Archeologist-92228 → `use_container`@80 was 54) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · full skipped (no shared file changed) · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `start_eating` `:2048–2062` old/save_nomovemsg dance around the bite-finish `done_eating(FALSE)` (pre-existing, untouched — all 7 checked sessions finish via the `eatfood` `done_eating(TRUE)` path); `done_eating` `!piece` defensive early return (pre-existing; C assumes non-null); `useup`/`useupf` + victual-reset tail unchanged.
**Next:** moved owners `monshoot`@8, `doengrave`@51, `use_container`@80 join future queue rows for those owners (`use_container` already queued).
## 2026-09-07 — D-2024 polyself.c polymon mhmax: golemhp table + home-elemental x3 (do_statusline2-symptom writer, 2/8 sessions)

**C locus:** `polyself.c:859–872` `polymon` mhmax block — dragon arm `:861`, `is_golem → golemhp(mntmp)` `:862–863`, `rnd(4)`/`d(mlvl,8)` `:865–868`, `is_home_elemental → u.mhmax *= 3` `:869–870`, `u.mh = u.mhmax` `:872`; `makemon.c:2233–2259` `golemhp` fixed table (STONE 100, IRON 120, no RNG); `makemon.c` `is_home_elemental` (S_ELEMENTAL on its home plane).
**JS:** 2 files (`makemon.js` +1/−1 export keyword; `polyself.js` +4/−3 plus 1 import line), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (diagnosis via recorded C screens + `hidden-proxy show`, outside the repo).
**Change:** `export` on `makemon.js golemhp` (no clone #2); `polyself.js` imports `{ golemhp, is_home_elemental }` from `./makemon.js` (new edge, same 90-module SCC; `imports.mjs --can` CHECK verdict — both are hoisted function declarations read lazily inside async `polymon`, never at top level; `jsmain` load smoke ok); golem arm → `u.mhmax = golemhp(mntmp)` (C `:863` verbatim); else arm += `if (is_home_elemental(mdat)) u.mhmax *= 3` (`mdat = mons(mntmp)` is the new form, same object `set_uasmon` installs; C `:869–870`).
**Verify:** `node scripts/verify.mjs --fn do_statusline2` → `PASS syntax 2 changed js file(s): js/makemon.js js/polyself.js` · `PASS rule2` · `hidden 0 PASS, 2 moved past, 5 unchanged, 1 same-step flag → REGRESSION-label` (Priest-91137 → `dmgval`@123 was 86, rngM 3114→3260, scrM 86→123; Tourist-92047 → `do_attack`@154 was 88, rngM 2874→4659, scrM 88→158; unchanged byte-identical: death-Tourist-92095@46, Caveman-92138@66, Monk-92164@63, Priest-92021@180, Healer-92092@58; wish-Tourist-91125 still `do_statusline2`@82 with rngM 2746→2745 — STASHED rerun without the fix reproduces rngM 2745/scrM 83/step 82/identical rowDiff exactly, and the recipe never polyselfs (wish-lembas-eat, `Xp:30` vs `Xp:30 Satiated` eat-uhs class), so the flag is rebase-window drift d7b4d542→b18a67bd, not this port) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared file changed) · working scoreboard re-based to `b18a67bd` with only these rows touched. Final verify ran after the last edit (no D-1831 gap).
**Named:** dragon-arm `In_endgame` gate (already live, untouched); polymon Stoned/Sick/Slimed/strangle/glib/hideunder/utrap/Blind-restore/egg/swallow/skinback/livelog-first-poly/retouch arms (pre-existing envelope, untouched); `golemhp` default 0 for non-golems (C-identical; unreachable behind the `is_golem` gate).
**Next:** remainders re-queue under `do_statusline2` with true writers — Monk-92164 (2-HP gap appears exactly at the step-63 jackal bites with matching mhmax: combat-damage chain `mattacku`/`hitmu`/`dmgval`; rng-diff to localize) · Caveman-92138 (cond ORDER `Burdened Slime` vs `Slime Burdened` contradicts pinned `botl.c:170–206` under any state: needs a C-binary measurement or capture audit; do NOT touch the painter) · Priest-92021 AC:3v7 + death-Tourist-92095 AC:6v10 (`u.uac` writers after break_armor/find_ac — D-1991 aftermath) · Healer-92092 + Tourist-91125 `Satiated`-vs-absent (`eat.c` uhs writer per D-1990). Moved owners `dmgval`@123 / `do_attack`@154 join future queue rows for those owners.
## 2026-09-07 — D-2023 timeout.c slimed_to_death: Slimed expiry polymorphs into a slime (exercise-symptom writer, 4/6 sessions)

**C locus:** `timeout.c:456–521` `slimed_to_death` — killer from the delayed SLIMED entry (default "turned into green slime"), mvitals ungenocide dance, `polymon(PM_GREEN_SLIME)` (whose `:756–759` CON/FALSE + WIS/TRUE arms are the recorded draws), `done_timeout(TURNED_SLIME, SLIMED)` (`:574–585`: I_SPECIAL + done + clear/botl on lifesave); expiry dispatch `:686–688` (`case SLIMED` in the uprops `--` loop).
**JS:** 1 file (`timeout.js` +116/−9), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (diagnosis via recorded-session RNG dumps + `hidden-proxy show`, outside the repo).
**Change:** `js/timeout.js` — new `done_timeout(how, which)` (C `:574–585` verbatim shape) + new `slimed_to_death(kptr)` (killer setup, emits_light/del_light_source, mvitals dance, `await polymon`, done_timeout, gameover guard for C's non-returning done, genocided-lifesave slimicide arm) + `case SLIMED` in the generic expiry loop (`find_delayed_killer(SLIMED)`). The C data-pointer check renders as `umonnum === PM_GREEN_SLIME` (JS `mons()` builds a fresh record per call; set_uasmon points data at mons[umonnum]). Imports extend pre-existing edges (const/polyself/light/hack) + one new static `end.js` edge (`done`, `find_delayed_killer`, `dealloc_killer`; same 90-module SCC per `imports.mjs --can`, runtime calls only, `eat.js:123` precedent — no TDZ).
**Verify:** `node scripts/verify.mjs --fn exercise` → `PASS syntax 1 changed js file(s): js/timeout.js` · `PASS rule2` · `PASS hidden 0 PASS, 4 moved past, 2 unchanged, 0 worse → PROGRESS` (Monk-92000 → `enlightenment`@67 was 60; Tourist-92095 → `do_statusline2`@46 was 45, row-23 botl `Burdened Blind`; Wizard-92120 → `enlightenment`@48 was 39; Caveman-92138 → `do_statusline2`@66 was 63; Ranger-92212 still `exercise`@66; Tourist-92067 still `exercise`@140) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44` (manual frozen-runner run after the port; per-turn shared path) · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** STONED `done_timeout` (same switch, no corpus coverage — untouched); genocided-lifesave slimicide arm (no corpus session genocides slime — straight-line C, unreached); polymon's pre-existing Slimed/strangle/glib omits (untouched).
**Next:** Ranger-92212 → new Open row `read.c seffect_fire` (doread allowlist lacks SCR_FIRE; seffects-head INC arm already live); Tourist-92067 → new Open row `uhitm.c mhitm_ad_famn` (mhitu arm `:3793` + `morehungry(rn1(40,40))`; no AD_FAMN dispatch in `mhitu.js`). The 4 moved owners (`enlightenment` ×2, `do_statusline2` ×2) belong to already-queued families.
## 2026-09-07 — D-2022 makemon mitem SPE_DIG + trap.c trapeffect_arrow_trap: two next_ident-symptom writers (first-Wizard earth spellbook; monster-stepped arrow-trap missile)

**C locus:** (1) `makemon.c:1369–1384` mitem block — `PM_WIZARD_OF_YENDOR` sets `iswiz`, `no_of_wizards++`, `mitem = SPE_DIG` iff first (`== 1`) and `Is_earthlevel`; `:1383–1384` `mongets(mitem)` (→ `mksobj(SPBOOK,TRUE)` = o_id + `blessorcurse(17)` `:1083`, zero other draws) runs between `newmonhp` and `m_initweap`/`m_initinv`. (2) `trap.c:1189–1248` `trapeffect_arrow_trap` — monster arm `:1223–1246` (once/tseen `rn2(15)` gone-gate, `t_missile(ARROW)` `:1016–1026`, `thitm(8, …)`); hero arm `:1198–1222` (click-gone, `t_missile(ARROW)`, `thitu(8, …)`, steed arm).
**JS:** 2 files (`makemon.js` +7, `trap.js` +~70), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (diagnostics via `scripts/rng-diff.mjs` + a /tmp raw-tag dump, outside the repo).
**Change:** (1) makemon.js: `if (no_of_wizards === 1 && Is_earthlevel(game.u?.uz)) mitem = otyp('SPE_DIG')` (C `:1372–1373` gate verbatim; `imports.mjs --can makemon.js const.js Is_earthlevel`: already statically imported, no new edge, no TDZ). (2) trap.js: new `trapeffect_arrow_trap` (hero + monster arms, shape-cloned from `trapeffect_dart_trap`; omissions mirrored: Soundeffect, steedintrap, gone-arm pline_mon, in_sight-gated seetrap, obfree resists) + `case ARROW_TRAP` + selector comment updates. All helpers already used in-file — no new cross-module edge.
**Verify:** `node scripts/verify.mjs --fn next_ident` → `PASS syntax 2 changed js file(s): js/makemon.js js/trap.js` · `PASS rule2` · `PASS hidden 0 PASS, 2 moved past, 1 unchanged, 0 worse → PROGRESS` (tour-Wizard-92103 → `collect_coords`@74 was 59; poly-Priest-92021 → `do_statusline2`@180 was 157; wish-Knight-92130 still `next_ident`@90) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared files changed) · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** arrow-trap Soundeffect / steedintrap / gone-arm pline_mon / in_sight seetrap gating / obfree resists (mirrored from the dart port); anti-magic trap effect (still selector-default); Wizard cham/Protection `if` vs C `else if :1356–1369` (no corpus coverage, untouched); other mitem arms untouched.
**Next:** wish-Knight-92130 → new Open row `objnam.c readobjnam` (mechanism proven NOT to be srch match-set: C fuzzymatch ≡ JS full-equality (`hacklib.c:782–808` vs `readobjnam.js:178–181`), C tables hold "slime mold" (prob 75, `objects.h`), C strips "cursed " pre-srch (`:3997–4003`), C `actualn=bp` (`:4719`) — every documented path says C's srch should match+draw, yet C draws nothing; and D-2021's bare-game harness resolved `cursed slime mold`→285 with zero srch draws while the live Knight draws `rn2(76)` — so the trigger is live-session state, not tables; D-2021's prescribed C-recorder wish experiment stands as falsifier — do not re-read, run the experiment). tour/poly new owners (`collect_coords`@74, `do_statusline2`@180) belong to future rows for those owners.
## 2026-09-07 — D-2021 objnam.c readobjnam zero-draw exact paths (Amulet real/fake, alt_spellings, gem-exact/tin): JS srch drew where C resolves with no RNG, desyncing next_ident

**C locus:** `objnam.c` `readobjnam` zero-draw exact paths that resolve `d.typ` before the random `srch` path: real-vs-fake Amulet of Yendor (`:4284–4309` — fake's descr contains the real name; cheap/plastic/imitation or preparsed fake selects fake, `:4306` forces real otherwise, zero draws); alternate-spellings loop (`:4457–4467` over `spellings[] :3377–3429`, `wishymatch(bp,sp,TRUE)`, zero draws); postparse3 real-gem exact + plain "tin" (`:4731–4747`, `!oclass` only, zero draws); preparse `real `/`fake ` flags (`:4125–4133`, fake overrides, real does not negate fake).
**JS:** 2 `js/` files (`readobjnam.js` +~70, `generated/alt_spellings.js` +~50 generated) + 1 `scripts/` extractor, under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). Transient in-process probes removed before the final verify (`grep DEBUG/console/process.env` clean).
**Change:** `js/readobjnam.js` — `real `/`fake ` preparse arms + `d.real`/`d.fake` fields (C `:4125–4133`); Amulet real/fake block before `makesingular` (C `:4284–4309`, incl. the `:5002–5006` typfnd non-wizard→fake fold for this arm only); `makesingular` now skipped when `d.typ` already set (C early-`return 2` equivalent); `ALT_SPELLINGS_RESOLVED` loop after `makesingular` (C `:4457–4467`); gem-exact + `tin` before the srch chain (C `:4731–4747`, `bases[GEM_CLASS]..LAST_REAL_GEM` verbatim bounds); srch chain guarded by `!d.typ`. `scripts/extract-alt-spellings.py` (new checked-in extractor, dat_text/artifacts precedent) emits `js/generated/alt_spellings.js` (46 `[sp, OBNAME]` pairs, C order; all 46 resolve via `objectNames.indexOf`, verified). Imports extend pre-existing edges (`hacklib.js` `strstri`, `objects.js` consts) except the leaf generated table (no imports → no cycle/TDZ possible; `imports.mjs --can` has no entry for brand-new files).
**Verify:** `node scripts/verify.mjs --fn next_ident` → `PASS syntax 2 changed js file(s)` · `PASS rule2` · `PASS hidden verify next_ident: 0 PASS, 3 moved past, 3 unchanged, 0 worse → PROGRESS` (scen-wish-Archeologist-92038 → `mhitm_mgc_atk_negated`@143 was 96; scen-wish-Knight-92072 → `give_to_nearby_mon`@150 was 138; scen-wish-Knight-92132 → `show_conduct`@213 was 197) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap). Unit oracle (bare-game harness + caller-tagged RNG log): `diamond`→440, `uncursed luckstone`→470, `3 cheap plastic imitation of the Amulet of Yendor`→212, `tin`→296, `saber`→51, `cursed slime mold`→285 — all exact otyps with zero pre-`mksobj` srch draws; `mksobj` order (`o_id` then `mksobj_init`) confirmed identical C-vs-JS, killing the swap hypothesis.
**Named:** `named`/`called`/`labeled`/`of spinach` strips; `o_ranges` + postparse2 stone/gem/glass branches; fruit path; postparse1 remainder (pair-of, globs, scales, holy water, paperback, unlabeled scroll/spellbook, orange, single-char class code); typfnd `!wizard` remaps beyond this arm's Amulet→fake (BELL/CANDELABRUM/Book/MAGIC_LAMP/`oc_nowish`); `d.real` conduct use; `rnd_otyp_by_namedesc` `oc_uname` (user-called-name) arm — C `:3513–3514`, JS name/of-name/descr/of-descr only (pre-existing match-set gap, untouched); `dn`/`origbp` POINTER-vs-value srch-skip nuance (C `!=` on pointers re-draws for value-equal `X labeled X`; JS string-compare skips — rare, untouched).
**Next:** queue row stays live for three different writers — (a) scen-wish-Knight-92130 `cursed slime mold` → C `2 slime molds`, zero draws, cnt=2 unreachable via every documented C path (makesingular leaves `slime mold` unchanged — C `:3037–3164` read; fruit ftyp=1 gives cnt 0→quan 1; pair-of/preparse-count absent): needs a C-recorder wish experiment as falsifier, not more reading; (b) scen-tour-Wizard-92103 throne-sit `m_initinv` vs `m_id` creation-path writer (`makemon.c`); (c) scen-poly-Priest-92021 `distfleeck` vs creation event-order writer (`monmove.c`/wizgenesis). The three moved sessions' new owners (`mhitm_mgc_atk_negated`@143, `give_to_nearby_mon`@150, `show_conduct`@213) belong to future rows for those owners.
## 2026-09-07 — D-2020 pager.c describe_looked self '@' found-count: `found += append_str("you")` 1→2 skips checkfile, JS kept 1 and prompted

**C locus:** `pager.c:1346–1353` — `'@'`-as-you tack-on gated on `u_at` + race ∉ {human, elf} + `!Upolyd`: `found += append_str(out_str, "you")`. `append_str` (`pager.c:82–104`) returns 1 on append, so C `found` goes 1→2; `do_look :1941` (`found == 1`) then skips the `checkfile(firstmatch, …)` block.
**JS:** 1 file (`pager.js`, +6/−4 incl. comment), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No committed probes — inline logic probe only (nothing to delete).
**Change:** `js/pager.js` — self branch now returns `found: orYou ? 2 : 1`; comment cites the `append_str` return and `:1941`. No new imports (PM consts/`Upolyd` already in file); `first` unchanged (C's firstmatch is equally unused once skipped).
**Verify:** `node scripts/verify.mjs --fn describe_looked` → `PASS syntax 1 changed js file(s): js/pager.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify describe_looked: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks, so no `--base` re-run owed; honest per D-1831) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Cross-check `verify --fn do_screen_description` → 1 blocked (scen-normal-Barbarian-92064 step 56, `:1481` room-floor vs unexplored — a different arm from `:1346–1353`), 0 moved, 0 worse → NO MOVEMENT, expected: that arm is untouched by this one-line count fix. Scoreboard working-tree delta from the verify re-score (scen-normal-Caveman-92006 scrM 82→83) proven pre-existing: replay with the fix stashed is also 83/95 (human hero → `orYou` is `''`, no-op).
**Named:** `checkfile` LOOK_VERBOSE `chkfilDontAsk` vs `chkfilNone` flags, LOOK_QUICK/LOOK_ONCE, `clicklook`, supplemental-info (pre-existing caller simplifications at `pager.js:1985–1991`, untouched — Must-fix stays one item, alone); rest of the self branch (invisible/swallowed re-modeling) stays as D-2013 left it.
**Next:** none from this row; refill/advance per `check-hot-docs` (first Open is `mkobj.c` next_ident).
## 2026-09-07 — Audit D-2012…D-2019 (reviews 982–989) + cadence 44/44

8 js/ SHAs oldest-first, every hidden-verify claim re-measured at `--base HASH~1`: 7 ACCEPT (982 interrupt_multi Norep; 984 engrave full port; 985 disclose suffix; 986 doup ledger yn + docs-only stamp sib 348aed9e; 987 newmonhp rider/elemental; 988 tipcontainer_gettarget; 989 get_count custompline), 1 QUALITY-RISK — 983 (D-2013) ports the 'or you' string but keeps `found: 1` where C `found += append_str` → 2 skips checkfile; constructible extra `More info about "dwarven archeologist"?` on verbose self-look (data keys measured) → Must-fix prepended (first in queue), Next cluster set. Cadence full sessions 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, `75+0.47/turn`). Queue: 1 Must-fix + 10 Open, no refill. Filled `**Addressed:** D-2019 2ae51922` (D-2016 already stamped).
