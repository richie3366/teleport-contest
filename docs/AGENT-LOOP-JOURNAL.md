# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-18 — D-2454 `zap.c` destroy_items whole-body port (coverage THIN → bypass traversal + deferral second pass)

**C locus:** `nethack-c/upstream/src/zap.c:5965–6097` (destroy_items); callees `worn.c` bypass_objlist/nxt_unbypassed_obj, `zap.c` destroyable + maybe_destroy_item (both live in-file clones), `display.c` impossible; consts `DMG_DESTROY_SCALE=5`/`MAX_ITEMS_DESTROYED=20` (`:5957–5959`), `NOBJ_STATES`, `LEVITATION`/`FLYING` oc_oprop, `POT_WATER` + `ismnum(u.ulycn)` + `Upolyd` water arm.
**JS:** `js/zap.js` (+~50 net: restart + 2 import words); `scripts/destroy-items-defer.test.mjs` (new, node:test — worn levitation ring + fire wand, seed 1: wand `rnd(10)=5` at log[1] proves pass-0-before-pass-1, ring survives, bypass cleared, `dmg_out=5`; dmg_in=0 early-return after the single C gate draw; verified FAIL on the pre-fix body via stash).
**Change:** `js/zap.js:1711` restart in C order — limit + unconditional `rn2(DMG_DESTROY_SCALE)` gate (`:1712–1716`), live-chain `objchn()` getter (C `:5984 obj**`), `{oid,otmp,deferred}` array (`:1722–1726`), `bypass_objlist(clear)` + `nxt_unbypassed_obj` reservoir walk (`:1730–1732`), `where` multi-chain `impossible` (`:1740–1744`, awaited — async in JS), u_carry worn-LEVITATION/FLYING + `POT_WATER && ismnum(game.u.ulycn) && (Upolyd(game.u)?blessed:cursed)` deferral (`:1749–1758`), `elig_stacks` cap + `defer 0..1` sweep with `o_id`/`where`/`deferred` identity (`:1760–1778`), final bypass clear (`:1779`), `dmg_out` return. New imports extend existing edges only (`worn.js` bypass pair — `--can` ALREADY; `const.js` LEVITATION/FLYING/NOBJ_STATES — same module already imported). Gameover break kept as the JS rendering of C losehp→done noreturn.
**Verify:** `node scripts/verify.mjs --fn destroy_items` → PASS syntax (1 file: js/zap.js) · rule2 · hidden note (0 blocked at baseline) · **reach 55/55 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. Focused suite: `node --test scripts/destroy-items-defer.test.mjs` 2/2 (fails pre-fix, passes post-fix).
**Named:** none new (in-file destroyable/maybe_destroy_item clones reused, not duplicated; uhitm hero arms stay with their owners as above).
**Next:** Open — coverage head after destroy_items (`mon.c` newcham).
## 2026-09-18 — D-2453 `hack.c` domove_core missing arms (coverage MISSING → wired into live `domove`)

**C locus:** `nethack-c/upstream/src/hack.c:2712–2991` (domove_core); callees `hack.c:2342–2360` air_turbulence, `:2396–2415` slippery_ice_fumbling, `:1955–1990` domove_attackmon_at, `:2900–2927` displaceu swap, `:2775` bhitpos, `:2789–2791` attack nomul, `:2817–2820` stucksteed arm, `:2937` u_on_newpos-final, `:2939` reset_occupations; `steed.c:878–895` stucksteed (`monst.h:251` helpless macro); `dungeon.c:1548–1565` earth_sense; `cmd.c:195–200` reset_occupations; caller `hack.c:2695–2710` domove wrapper.
**JS:** `js/cmd.js` (+167), `js/hack.js` (+59: two callees), `js/steed.js` (+26: stucksteed + export word), `js/do.js` (export word); 4 files, ~240 insertions, under caps.
**Change:** `js/hack.js` — new `air_turbulence` (`:2273`, Is_airlevel+rn2(4)+Levitation/Flying gate, rn2(3) tumble/You_cant/thin-air + DEX exercise) and `slippery_ice_fumbling` (`:2300`, snow-boots objdescr / resists_cold / Flying / floater-clinger-whirly exemptions, Cold_resistance rn2(2|3) → HFumbling FROMOUTSIDE+1, off-ice clear), both wired at the else-branch head in C order (`js/cmd.js:3332–3333`, bare return like C). `js/steed.js` — new `stucksteed` (`:452`, helpless→"won't move!", checkfeeding meating→"still eating"); `helpless_steed` exported (`:1029`, C helpless macro, reused by the displaceu gate). `js/do.js` — `reset_occupations` exported (`:2470`, cmd→do edge already static).
**Verify:** `node scripts/verify.mjs --fn domove_core` → PASS syntax (4 files) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach; `--reach-all` identical) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared files changed) → VERIFY: PASS. Durable collateral per skill: repo has no unit harness — corpus sessions are the maintained suite (untouched); no new framework added.
**Named:** displaceu middle-skip (C skips ironbars/test_move/swim when displaceu; JS runs the middle then swaps — converges next turn); travel block (handled upstream in `continue_run`, `findtravelpath_travel/guess` + travel1=0, same order vs the carrying check); final CLIPPING (wintty cliparound singleton, Deferred list); pre-existing arrangements untouched (test_move inline vs live D-2439, fight_empty pre-branch, u_rooted air/water omits, mimic "Wait!" in do_attack); mtele_trap RLOC_MSG (rloc_to_core stale park, code-named at `js/teleport.js:1347`, future teleport.c companion).
**Next:** Open — coverage head after domove_core (`zap.c` destroy_items).
## 2026-09-18 — D-2452 `potion.c` make_blinded caller-arm sweep (coverage PARTIAL → wired)

**C locus:** `nethack-c/upstream/src/potion.c:260–331` (make_blinded); callers `eat.c:1822–1830` rottenfood (`!rn2(4) && !Blind` → `make_blinded(BlindedTimeout+d(2,10),FALSE)` → `if (!Blind) Your1(vision_clears)`), `potion.c:861–865` (`if (!cursed) make_blinded(0L,TRUE)` before the HSee_invisible branch), `apply.c:174–181` (`incr_itimeout(-ucreamed); ucreamed=0; if (!Blinded)…`); `youprop.h:77` Punished≡`uball!=0`, `:92` Blinded≡`HBlinded && !BBlinded`, `:93–94` BlindedTimeout/PermaBlind, `:103` Blind≡`(H||E)&&!B`, `:399` Unaware; `:257–258` vismsg/eyemsg templates; `decl.h:40` vision_clears.
**JS:** `js/eat.js` (import + rottenfood arm), `js/potion.js` (see-invisible call), `js/apply.js` (towel guard), `js/do.js` (export word); no new module edges (both `--can` ALREADY).
**Change:** `js/eat.js` — rottenfood guard is now `!rn2(4) && !Blind()` (live `invent.js` Blind; `imports.mjs --can` ALREADY on the invent edge; the two function-scoped `const Blind` mirrors at :1644/:1788 shadow it legally, untouched), then `await make_blinded(BlindedTimeout()+d(2,10), false)` + `if (!Blind()) pline('Your vision clears.')` per C `:1824–1828` (RNG-neutral: same `d(2,10)` draws). `js/potion.js` — `peffect_see_invisible` calls `make_blinded(0,true)` under `!cursed` in C position `:865` (file's dynamic-import idiom; set_mimic_blocking/see_monsters/newsym stay named). `js/do.js` — `BlindedTimeout` exported for the eat.js call (one word; hoisted function decl, no TDZ risk).
**Verify:** `node scripts/verify.mjs --fn make_blinded` → PASS syntax (4 files) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** (no RNG-tagged reach) · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared files changed) → VERIFY: PASS.
**Named:** eat.c:2520 fpostfx CARROT blindness (`js/eat.js:1961` + map; EGG/other otyps with it); `Your` stays inlined via pline literals (no clone #5 — artifact/mhitu/pray/zap clones pre-date); `Blind()` uroleplay.blind extension shared with all existing callers; SND_SPEECH arm (contest C compiled out, `js/sounds.js:74–103`), DUMPLOG_CORE (retired D-1776), paniclog file writes (Rule #2), `free(cmdq)` n/a, gulp_blnd_check in use_towel (pre-existing); nyNaq/ynNaq macro wrappers (callers pass tables directly: `js/pickup.js:3544`).
**Next:** Open — coverage head after make_blinded (`teleport.c` rloc_to_core).
## 2026-09-18 — D-2451 `pager.c` checkfile dbase-side `" ("` strip (review 1402 item 1)

**C locus:** `nethack-c/upstream/src/pager.c:977–981` (`/* remove charges or "(lit)" or wizmode "(N aum)" */ if ((ep = strstri(dbase_str, " (")) != 0 && ep > dbase_str) *ep = '\0';` + the alt-side twin `:980–981`); reachable because `objnam.c:1477–1490` appends `" (lit)"`/`" (%d:%d)"` to the doname strings the `/i` and glance paths feed to `checkfile`.
**JS:** `js/pager.js` (`checkfile_split_names` + doc only); no new module edges.
**Change:** `js/pager.js` only — `checkfile_split_names` now truncates `dbase` at the first `" ("` (`indexOf`, matching C `strstri` on the already-lowered string, `> 0` matching C `ep > dbase_str`), placed after the named/called truncation per C order; helper doc now cites `:944–981`.
**Verify:** `node scripts/verify.mjs --fn checkfile` → PASS syntax (1 file: js/pager.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. Probe `/tmp/probe-checkfile-split.mjs` (shipped function source extracted at runtime, real `strstri`): HEAD misses `"oil lamp (lit)"`/`"wand of striking (0:3)"`/`"oil lamp (10 aum)"` (PROBE-FAIL), fixed tree truncates all three + plain names unchanged + alt `" ("` strip intact (PROBE-OK).
**Named:** none new (D-2443 omissions stand: do_supplemental_info `:2255`, dlb I/O-error arms).
**Next:** Must-fix head after this row (none — pop Open coverage head `cmd.c` yn_function).
## 2026-09-18 — D-2450 `mon.c` xkilled holder-release re-layered/re-ordered/gated (review 1403 item 1)

**C locus:** `nethack-c/upstream/src/mon.c:2702–2703` (`mon_leaving_level`: `mtrapped=0` + `unstuck`) reached through `m_detach` inside `mondead` — every death path including lifesaved, before xkilled's lifesave check (`:3553–3562`); never on the `monstone` path (`:3286–3373` has no unstuck call). `unstuck :3437–3467` draws `rnd(2)` into `mspec_used` for AD_STCK/AT_ENGL/AT_HUGS holders.
**JS:** `js/uhitm.js` (xkilled body only).
**Change:** `js/uhitm.js` only — the two lines moved to right after `game.disintegested = false`, gated on `!was_stoned`, before the lifesaved return; the stale "before the rn2(6) draw" comment replaced with the `mon_leaving_level`/`monstone` cite. Export name/signature unchanged; no new module edges (`mhitu.js` dynamic import already the file's cycle idiom).
**Verify:** `node scripts/verify.mjs --fn xkilled` → PASS syntax (1 file: js/uhitm.js) · rule2 · hidden note (0 blocked at baseline) · **reach 125/125 PASS, 0 regressed → REACH-OK** (`--reach-all`; 80/80 spread sample first) · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** none new (D-2444 omissions stand: `mhitm_ad_rust`/`mhitm_ad_fire` uhitm arms, `wiz_kill`; deeper unstuck-inside-JS-mondead for all callers stays a same-file follow-up debt per review 1403).
**Next:** Must-fix head after this row (`pager.c` checkfile dbase-side `" ("` strip).
## 2026-09-18 — D-2449 `pager.c` do_screen_description unlooked cmap scan Primary byte (review 1406 item 1)

**C locus:** `nethack-c/upstream/src/pager.c:1475` (`sym == (looked ? gs.showsyms[alt_i] : defsyms[alt_i].sym)` — the unlooked `/`-query path always uses the Primary def byte); caller `pager.c:1917` do_look (`from_screen || clicklook` gates looked; typed symbol takes the unlooked arm).
**JS:** `js/pager.js` (cmap scan match only); no new module edges (`DEFSYMS_CH` already imported at `js/pager.js:43`).
**Change:** `js/pager.js` only — the cmap-scan match now compares against `looked ? cmap_showsym_code(altI) : DEFSYMS_CH[altI].charCodeAt(0)` (Primary byte, `-1` fallback when the slot has no Primary char so no false hit), with a `:1475` cite. Looked path unchanged (glyph-bank DEC byte vs `cmap_showsym_code` DEC byte). Monster/object/warning arms untouched — identical under DEC (letters un-remapped), diverge only under custom `OPTIONS=monsters/objects` overrides, already named in D-2447.
**Verify:** `node scripts/verify.mjs --fn do_screen_description` → PASS syntax (1 file: js/pager.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. Probe `/tmp/probe-dec-looked.mjs` under `symset:DECgraphics`: unlooked `|` → found=3 "a wall or an open door or a grave" (PROBE-PASS); unlooked `0xF8` → found=0 (correct: raw DEC byte is not a Primary query).
**Named:** none new (D-2447 omissions stand: rogue_syms, non-boulder ov slots, `gw.warnsyms`, gameover hallucinate gate, getpos_menu, do_supplemental_info).
**Next:** Must-fix head after this row (`mon.c` xkilled holder-release).
## 2026-09-18 — Audit 678a0821..38249822 (reviews 1399–1407: 6 ACCEPT, 3 QUALITY-RISK) + cadence 44/44, proxy 495/540

**Scope:** 9 JS-touching SHAs since review 1398 (D-2440 getdir-dz delete, D-2441 setmangry + D-2442 bury_objs imports, D-2443 checkfile, D-2444 xkilled+disintegrate_mon, D-2445 look_at_monster, D-2446 mstatusline, D-2447 do_screen_description, D-2448 gd_move). One SHA reviewed then filed immediately; one grouped commit.
**Findings:** (1) Review 1402: D-2443 `checkfile_split_names` drops C's dbase-side `" ("` strip (`:977–981`) — probe shows `"oil lamp (lit)"` keeps the suffix; reachable via objnam.c:1477–1490 names; old regex had it → Must-fix. (2) Review 1403: D-2444 `xkilled` holder-release (`uhitm.js:745–750`, kept pre-existing lines) runs after the lifesaved return and on the stoned path; C releases inside mondead via mon_leaving_level `:2702–2703`, before the lifesave check, never via monstone → Must-fix (move + `!was_stoned` gate; mondead-wide fix is follow-up debt). (3) Review 1406: D-2447 cmap scan compares the DEC showsyms byte on the unlooked `/`-query path (`pager.js:1540`); C uses defsyms when `!looked` (`DEC_CMAP_BYTE[S_vwall]=0xF8` vs Primary `|`) → Must-fix. Closes: 1393#1 (D-2440 verified: 3 flipped sessions PASS, 495/540 restored), 1395#1+#2 (both imports ALREADY-edge, awaited). Debts named (no rows): is_quest_artifact 2nd export, flash_str one-arg idiom, def-table vs showsyms override arms, dead howmonseen_look_buf.
**Cadence:** public 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, `58+0.36/turn` R² 0.79); held-out 11/44 unchanged (5,637 pts, RNG 26.6 %, screens 50.0 %); corpus 495/540 (91.7 %, RNG 99.29 %, screens 99.0 %) — D-2440 recovery holds, no new flips.
**Verify method:** every SHA re-measured via `hidden-proxy verify <fn> --base HASH~1 --reach-all` (xkilled 125/125 reach; all D-log claims confirmed, 0 regressed); `sym.mjs` on all deleted/re-pointed symbols pasted per review; `imports.mjs --can` edges + `--rulecheck` clean; /tmp probes for the paren-strip.
**Next:** Must-fix do_screen_description unlooked-cmap (top), then xkilled release, then checkfile paren-strip; Open coverage head unchanged.
## 2026-09-18 — D-2448 `vault.c` gd_move whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/vault.c:888–1201` (gd_move); helpers `:734–750` gd_mv_monaway, `:752–833` gd_pick_corridor_gold (staticfn), `:869–886` gd_letknow (staticfn), `:836–867` gd_move_cleanup (already live); `mon.c:1827–1843` mpickgold; `rm.h:213` (`doormask` IS `flags`); callers `mon.c:1236` movemon_singlemon, `monmove.c:1808` m_move, `vault.c:276` uleftvault.
**JS:** `js/vault.js` (helpers + gd_move restart); `js/mon.js` (mpickgold + movemon_singlemon wiring + 3 import names); `js/objects.js` (GOLD const); `js/teleport.js` + `js/shk.js` (doc comments only); map `docs/c-js-map/turns.md` vault rows.
**Change:** `js/vault.js` restart in C order — off-level `:893-894`, dead/parked/gddone cleanup `:896-899`, both-out wallify `:909-911`, hostile rloc/wallify/clear_fcorr/gd_letknow `:913-928`, teleported-guard reject `:934-935`, witness scold + live (no-op) SetVoice `:937-947`, follow-me warncnt==3 + dropgoldcnt `:953-962`, warn-knave warncnt==7 with mnexto + fakecorr[0] restore + recalc/del_engr/newsym `:963-984`, fainted-or-`multi>=0` warncnt++ `:985-987`, teleported-gold rloc + restore + gd_letknow `:991-998`, Well-begone `:999-1006`, fcend>1 corridor-disappears `:1010-1017` + demand-warn-6 / So-be-it `:1018-1049` (Deaf/Blind palm/rub plines via live noit_mhis), goldincorridor scan + new `gd_pick_corridor_gold` (under-hero enexto approach ×9 with dist2-to-hero per hack.h:1531, in-place, third-spot move-and-back, calm-down infix) `:1051-1065`, Move-along with `sticks(youmonst.data)` ustuck arm `:1066-1074`, look-around with goto-proceed/newpos phase mapping `:1080-1110`, nextpos dig while-loop with C-exact DOOR/redirect/STONE→CORR mutation + del_engr_at(ex,ey) `:1111-1156`, proceed unblock/cansee-newsym + fakecorr append with FCSIZ throw ≡ panic `:1157-1174`, stuck find_guard_dest retry via `goto nextpos` loop `:1175-1180`, newpos monaway + remove/place_monster + mpickgold + canspotmon pline + restfakecorr `:1182-1201`. New in-file `gd_mv_monaway` (m_at ≡ MON_AT), `gd_letknow` (whistle vs shouting / approaching vs confronted), `restore_fakecorr0` (IS_DOOR split per clear_fcorr idiom — C stores post-mutation `crm->flags`). New `js/mon.js` export `mpickgold` (verbose-gated pline_mon, GOLD-vs-money via new `GOLD = 15` in `js/objects.js` per objclass.h:28).
**Verify:** `node scripts/verify.mjs --fn gd_move` → PASS syntax (5 files) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared files changed) → VERIFY: PASS.
**Named:** debugpline1 wizard log; defensive `!isok`/`!crm` early-0 in the dig loop (C in-bounds by construction); clear_fcorr Punished/uball arm (pre-existing).
**Next:** Open — coverage head after gd_move (`cmd.c` yn_function).
## 2026-09-18 — D-2447 `pager.c` do_screen_description whole body in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/pager.c:1247–1627` (do_screen_description); showsyms machinery `symbols.c:100–108` (defaults), `dat/symbols:689–777` (DECGraphics block), `display.c:1850–1853` (darkroom equate), `display.c:2594+` (map_glyphinfo ttychar); `get_othersym` X-slot defaults (`:100–150`); callers `cmd.c:1360` dolookaround, `display.c:2068` show_glyph, `getpos.c:649` auto_describe, `getpos.c:700` getpos_menu, `pager.c:1917` do_look.
**JS:** `js/pager.js` (helpers + `do_screen_description` + `do_look` rewire); `js/getpos.js` (`auto_describe_text` → firstmatch, `DEFSYMS_CH` export); `js/uhitm.js` (table 38–73/86–87); `js/mondata.js` (export); `js/cmd.js` + `js/display.js` (doc comments only); map `docs/c-js-map/turns.md` look rows.
**Change:** `js/pager.js` restart in C order — restricted vision (`:1291–1305`), x_str (`:1307–1325`), check_monsters incl `@`-as-you (`:1327–1354`), objects with boulder/statue split + venom skip (`:1356–1404`), DEF_INVISIBLE (`:1406–1417`), dark-room + unexplored with showsyms-slot halves (`:1419–1445`), cmap scan with lava rotation/article logic/moat/`need_to_look` (`:1447–1520`), warnings + boulder co-locate (`:1522–1542`), venom restore, option overrides with check_monsters re-entry loop (`:1554–1600`, `#if 0` boulder noted), `found > 4` collapse, didlook lookat/ice/blocked-stair/engraving/monbuf (`:1607–1640`); holders `{s}/{v}/{pm}` for C out-pointers. Showsyms emulation: `glyph_showsym_code` resolves the matching byte from glyph banks (monster→class letter incl statue/body depicted mnum peel, object→oclass letter, trap→cmap row, cmap→DEC/Primary, warning→ch, invis→`I`, unexplored/nothing→space); `DEC_CMAP_BYTE` table from the DECGraphics block gated on the live symset name/boolean (`decSymsActive`); `cmap_showsym_code(S_darkroom)` mirrors the runtime equate (room copy when dark_room+use_color else NOTHING slot); `is_swallow_code` checks the symset's swallow bytes; prefix renders via the display.js paint rule (`rendered_glyph_char`); unlooked arm takes the int sym with `%c` prefix. `defsym_explanation` extended to cmap 38–73 + 86/87 from defsym.h descs (74–85 stay falsy → furniture fallback).
**Verify:** `node scripts/verify.mjs --fn do_screen_description` → PASS syntax (6 files) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared file changed) → VERIFY: PASS. js insertions ≈575 < 1500 cap, 8 files < 15.
**Named:** rogue_syms table (Primary used on rogue levels); non-boulder `go.ov_*_syms` slots (boulder arm live); `program_state.gameover` in hallucinate gate; `gw.warnsyms[]` (def_warnsyms defaults); getpos_menu (unimplemented); do_supplemental_info (own row; pm fill stays live in forSupplement); local char-based `is_swallow_sym` kept unused (code-based `is_swallow_code` is the live path).
**Next:** Open — coverage head after do_screen_description.
## 2026-09-18 — D-2446 `insight.c` mstatusline whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/insight.c:3275–3398` (mstatusline); helpers `worm.c:946–966` wseg_at (ported to `js/worm.js`), `priest.c:280–290` mon_aligntyp, `insight.c:3203–3230` size_str, `mondata.c:654` sticks (live `js/engrave.js:361` already C-cited); callers `apply.c:348/350/353/357/395/442`, `zap.c:625–640` probe_monster.
**JS:** `js/insight.js` size_str/mon_aligntyp/mstatusline; `js/worm.js` (`wseg_at` after `count_wsegs`); `js/apply.js` (bhitpos/notonhead setup); map `docs/c-js-map/turns.md` stethoscope row.
**Change:** `js/insight.js` restart in C order — `mon_aligntyp` fixed to EPRI shralign / EMIN min_align / data.maligntyp with A_NONE passthrough (`:3277`); tame + `wizardMode()` count and EDOG hungry/apport unless isminion (`:3281–3290`); long-worm `count_wsegs` with head-inclusive `++nsegs` and new live `wseg_at(bhitpos)` + `ordin` (`:3292–3306`); shapechanger via `ismnum(cham)` + mndx compare (`:3307`); meating (`:3311`); mhidden on mundetected/m_ap_type/`visible_region_at(bhitpos)` (`:3315`); cancelled/confused/blind/stunned (`:3320–3327`); asleep/frozen-or-!mcanmove/meditating with the `#else` arm live (`:3328–3337`); scared/trapped/fast-slow-`[? speed]`/invisible (`:3338–3347`); ustuck uswallow `digests`/`is_animal`+`enfolds` vs `sticks(youmonst.data)` hold/held (`:3348–3366`); usteed carrying + `EWounded_legs & BOTH_SIDES` `mbodypart(LEG)`/`makeplural` injured (`:3368–3380`); leashed (`:3381`); `x_monnam(ARTICLE_YOUR, null, SUPPRESS_IT|SUPPRESS_INVISIBLE)` (`:3386`); C-exact pline format (`:3391`). `size_str` fixed (MZ_GIGANTIC=7 local per monflag.h:183 + unknown-size default). `bhitpos` reads `game.bhitpos ?? game._bhitpos` with head-pos fallback (apply stethoscope never set it).
**Verify:** `node scripts/verify.mjs --fn mstatusline` → PASS syntax (3 files: apply.js, insight.js, worm.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS. (Pre-ship self-check: `mcansee`/`mcanmove` default 1 in `js/makemon.js:3219`, so the new blind/can't-move arms stay quiet on ordinary monsters.)
**Named:** none inside mstatusline (whole C body ported, `#else` can't-move arm live, no TODO in a live arm). Caller-side: `use_stethoscope` usteed/uswallow/dz/cursed-heartbeat/confdir/Deaf/SDOOR/its_dead arms (`apply.c:344–379`) stay deferred in `js/apply.js` (turns.md row, pre-existing).
**Next:** Open — coverage head after mstatusline (`pager.c` do_screen_description).
## 2026-09-18 — D-2445 `pager.c` look_at_monster whole body in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/pager.c:422–555` (look_at_monster, staticfn); callee `do_name.c:1526–1536` coyotename (ported to `js/do_name.js`); `pager.c:138–160` monhealthdescr (disabled `#if 0`, always empty — ported as `''`); callers `:710` lookat (buf+monbuf), `:2002` look_all (NULL monbuf).
**JS:** `js/pager.js:416` (`monhealthdescr`), `:438` (`look_at_monster`), `:559`/:568 (halves); `js/do_name.js:807` (`coyotename` + COYNAMES table, `PM_COYOTE` export `:98`); map `docs/c-js-map/turns.md:845`.
**Change:** `js/pager.js` restart in C order — new `export function look_at_monster(mtmp, x, y)` returning `{ buf, monbuf }`: accurate gate (`:429`); coyote `data.mndx === PM_COYOTE && accurate ? coyotename : distant_monnam(ARTICLE_NONE)` (`:430–432`); tail prefix with isshk&&accurate split (`:433–436`); health + accurate-gated tame/peaceful (`:437–442`); ustuck `digests(data)` swallowing/engulfing vs `Upolyd && sticks(youmonst.data)` held/holding (`:443–451`); mfrozen/msleeping/STRAT_WAITMASK (`:452–464`); mleashed (`:466–467`); mtrapped + `cansee` + `t_at` BEAR_TRAP/is_pit/WEB + `an(trapname(tt, false))` + `tseen = 1` (`:468–478`); mhidden on mundetected/M_AP_TYPE/`visible_region_at(look x,y)` (`:479–484`); monbuf bits in C order with C ", " separators (`:486–554`), WARNMON via `Mgender`, leftover `(%u)` suffix preserved. Old helpers kept as thin halves delegating to it. Live imports only (`cansee`/`sticks`/`digests`/`Mgender`/`distant_monnam`/`coyotename`/`M_AP_TYPE`/`is_pit`/trap consts — `imports.mjs --can` SAFE/ALREADY on all edges).
**Verify:** `node scripts/verify.mjs --fn look_at_monster` → PASS syntax (2 files: do_name.js, pager.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** `impossible("lookat: unknown method of seeing monster")` on the unreachable leftover-bits arm (`:550–553` — all 7 MONSEEN bits consumed above; async debug log skipped in this sync look path, `(%u)` suffix preserved in code).
**Next:** Open — coverage head after look_at_monster (`insight.c` mstatusline).
## 2026-09-18 — D-2444 `mon.c` xkilled whole body in C order (coverage PARTIAL → live) + `zap.c` disintegrate_mon

**C locus:** `nethack-c/upstream/src/mon.c:3477–3740` (xkilled); callees `mon.c:4527` iter_mons + `:3072` anger_quest_guardians (staticfn, ported to `js/mon.js`); `nethack-c/upstream/src/zap.c:4724–4758` disintegrate_mon (staticfn, ported to `js/zap.js` with its `oresist_disintegration` macro `:4738–4741`).
**JS:** `js/uhitm.js:644` (`xkilled`), `:612` (`xkilled_treasure_drop` async); `js/mon.js:2519` (`iter_mons`), `:2536` (`anger_quest_guardians`); `js/zap.js:2119` (`oresists_disintegration`), `:2133` (`disintegrate_mon`), `:2258`+`:2267` (wirings); map `docs/c-js-map/data.md:339`.
**Change:** `js/uhitm.js` restart in C order — sad_feeling save/clear; conduct; kill message; pit `t_at`+`is_pit` with `sobj_at(BOULDER)` nocorpse / `m_carrying(BOULDER)` bury; tame `EDOG.killed_by_u`; engulfer missile via live `mpickobj` (potion/uball/returning_missile exclusions); `vamp_rise_msg=false` + `disintegested=nocorpse` writers around `monstone`/`mondead` with reset; lifesaved stoned-reset + unseen "Maybe not..."; `be_sad`; MAIL_DAEMON `mksobj_at(SCR_MAIL)` (MAIL_STRUCTURES always on, `global.h:430`); treasure `!rn2(6)` + mvflags/square/Kop/clone gates at the call site, helper now awaits live `flooreffects(nomsg ? '' : 'fall')`; corpse + "ends up buried" message; museum copy with nmon/minvent/mextra cleared + `spoteffects(TRUE)`; unconditional `newsym`; murder arm + `see_monsters()` under live `hero_Blind_telepat` (new `display.js` export); experience; leader/nemesis/guardian/priest/tame/peaceful adjalign in C order + `adjalign(malign)` with live `p_coaligned`, `ALIGNLIM()`, `iter_mons(anger_quest_guardians)`, `Soundeffect` thunder/applause, `LL_KILLEDPET` gamelog via `unique_corpstat` (new `mon.js` export), `MGIVENNAME`/`uhis`/`Mgender`; C `#if 0` HARDFOUGHT block stays out. New live exports: `mon.js` `iter_mons`/`anger_quest_guardians`, `mhitm.js` `mlifesaver`, `dogmove.js` `is_quest_artifact`, `explode.js` `completelyburns` (all ALREADY/SAFE edges, `imports.mjs --can` checked). `zap.js`: `disintegrate_mon` port (mlifesaver spare, `canseemon` message via live `hit_msg`, minvent nobj-chain strip with `extract_from_minvent`+`obfree`, `monkilled(null, -AD_RBRE)` vs `xkilled(NOMSG|NOCORPSE)`); MAGIC_COOKIE branch wired to it, DEADMONSTER branch computes `xkflags` with `completelyburns`.
**Verify:** `node scripts/verify.mjs --fn xkilled` → PASS syntax (7 files) · rule2 · hidden note (0 blocked at baseline) · **reach 80/80 → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared files) → VERIFY: PASS; then `--reach-all` → **125/125 PASS, 0 regressed → REACH-OK** → VERIFY: PASS. (Two self-caused throws fixed within the iteration: unexported `mlifesaver`, missing `flooreffects` name on an ALREADY edge.)
**Named:** `mhitm_ad_rust` (C:2294) + `mhitm_ad_fire` (C:2547) uhitm arms — enclosing C functions unported (no `rust`/`smell burning` in `js/`); `wiz_kill` (`wizcmds.c:315`, no JS counterpart) — own coverage rows.
**Next:** Open — coverage head after xkilled (`pager.c` look_at_monster).
## 2026-09-18 — D-2443 `pager.c` checkfile whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/pager.c:829–1129` (checkfile, staticfn); callees lcase/strstri/copynchars/pmatch/fruit_from_name/makesingular/y_n + dlb family (Rule #2: embedded DAT_TEXT, D-0477) + NHW_MENU putstr/display; callers `:813` ia_checkfile, `:1838` do_look `/i`, `:1853` `?`, `:1948` verbose glance.
**JS:** `js/pager.js:720` (`checkfile_dbase_str`), `:765` (`checkfile_split_names`), `:801` (`checkfile_alt_for`), `:827` (`checkfile`), `:895` (`ia_checkfile`); map `docs/c-js-map/turns.md:806`.
**Change:** `js/pager.js` only — `checkfile_dbase_str` (`:867–935` all strips with C else-if chains), `checkfile_split_names` (`:944–976` incl. live supplemental_name fill from original-case inp), `checkfile_alt_for` (fruit → `slime mold` per obj_descr[SLIME_MOLD].oc_name, else makesingular), `checkfile(inp, pm, chkflags, supplementalHolder)` in C order (flags/open-guard/bad-buffer-impossible/pm+lcase/strips/split/alt/pass1offset-skip via entry index/y_n/display/miss-message), `ia_checkfile` rewired onto the shared sync core (lookup-only = C IaCheck arm). Live imports only (objnam/hacklib/getline/monsters/const/display — `imports.mjs --can` ALREADY on all three new edges). Deleted dead `simplify_for_db`/`split_db_query`/`lookup_data_base` wrapper + unused yn_function import.
**Verify:** `node scripts/verify.mjs --fn checkfile` → PASS syntax (1 file: pager.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** do_supplemental_info (`pager.c:2255`, own row — verbose-glance fill stays live); supplemental_pm out of do_screen_description (own row); dlb I/O-error arms (`? Seek error`, `bad_data_file` format impossibles — no dlb over embedded text).
**Next:** Open — coverage head after checkfile (`mon.c` xkilled).
## 2026-09-18 — D-2442 `polyself.js` dospinweb `bury_objs` import wired (ReferenceError deleted)

**C locus:** `nethack-c/upstream/src/polyself.c:1497–1621` (dospinweb PIT arm `bury_objs(x, y)` after `deltrap`, C `:1564`); dispatched from C `cmd.c:915`.
**JS:** `js/polyself.js:89`; map `docs/c-js-map/turns.md:296`.
**Change:** `js/polyself.js:89` only — added `bury_objs` to the existing `./dig.js` import. `node scripts/imports.mjs --can polyself.js dig.js bury_objs` → ALREADY, no new edge. Live callee `js/dig.js:450` is `export async`, call site already awaits.
**Verify:** `node scripts/verify.mjs --fn dospinweb` → PASS syntax (1 file: polyself.js) · rule2 · hidden note (0 blocked at baseline) · **reach smoke 24/24 PASS, 0 regressed → REACH-OK** · green 2/2 · strict ×2 · cohort 7/7 (no shared file → full skipped) → VERIFY: PASS.
**Named:** none new; review 1395 both C-wrongs now closed (item 1 D-2441, item 2 this entry).
**Next:** Open — coverage head (`invent.c` getobj).
