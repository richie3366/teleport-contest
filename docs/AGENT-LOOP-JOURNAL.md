# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-20 — D-2654 `mkobj.c` check_contained whole-body port (live `js/mkobj.js` export)

**C locus:** ``nethack-c/upstream/src/mkobj.c:3374–3416`` (check_contained; staticfn decl ``:26``) + callers ``objlist_sanity :3051`` / ``mon_obj_sanity :3226`` + callees obj.h:334 (Has_contents), hacklib.c:287 (copynchars), ``insane_object :3314`` / ``check_glob :3420`` (same file). Decisive C facts: ``:3380`` early return when no cobj; ``:3384–3386`` "contained " prefix via mesgbuf[40] only when mesg lacks "contained"; ``:3390`` direct-cycle panic; ``:3392`` wrong-where → insane_object(ofmt0-equivalent literal, null mon); ``:3394–3398`` wrong-owner → impossible with three fmt_ptr identities; ``:3399`` globby → check_glob; ``:3404`` holds-parent panic; ``:3408–3410`` Strcpy "nested " + copynchars (≤112 chars, newline-stop, always terminated; eos = append point); ``:3413`` self-recursion.
**JS:** ``check_contained`` js/mkobj.js:1640.
**Change:** exported ``async check_contained`` in C order with per-arm ``:line`` cites — ``Has_contents`` joins the const.js import (same-module edge, live const.js:3191); ``strstri``/``impossible`` already imported; ``OFMT0_SANITY``/``fmt_ptr``/``insane_object`` same-file; file-local ``check_glob`` awaited (hoisted declaration, no new edge); panics → loud throws (dealloc_obj precedent); mesgbuf/nestedmesg as plain strings with the 112-char newline-stop inline (no second copynchars clone — topten.js:44 stays the only one); async only because the three report callees can reach --More--.
**Verify:** ``node scripts/verify.mjs --fn check_contained`` → syntax 1 file · rule2 · hidden note (0 blocked, coverage row) · REACH smoke 24/24 REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. Probe /tmp/probe-check-contained.mjs: empty early-return, both cycle throws with C messages, well-formed silent, nested recursion silent; /tmp/probe-cc-report.mjs: wrong-where (insane_object) + wrong-owner (impossible) arms run headless with no crash.
**Named:** ``objlist_sanity`` (mkobj.c:3032) + ``mon_obj_sanity`` (mkobj.c:3204) caller wiring (unported, own rows); ``copynchars`` export (inline port of the one call site; topten clone untouched); ``panic`` (no JS abort — loud throw); ``eos``/``Strcpy`` (string ops, no helper).
**Next:** objlist_sanity / mon_obj_sanity rows (each wires one check_contained call site on ship).
## 2026-09-20 — Audit reviews 1604–1612 (D-2645…D-2653 window) + cadence

**Audit:** 9 JS-touching SHAs since 9ff53550, one SHA at a time; files 1604–1612 were written by the prior attempt (died `resource_exhausted` before commit) and verified structurally complete here — required headings, verdict lines, INDEX rows all present; 1608 read in full. **8 ACCEPT + 1 ACCEPT-WITH-DEBT, 0 Must-fix** (Must-fix stays empty; Next cluster unchanged). 1604 D-2645 lspo_monster + create_altar: unpacked bindings, gender rn2(2) burns once in C position, TEMPLE adopt + shrine arms. 1605 D-2646 singplur_lookup singular arms. 1606 D-2647 query_category ParanoidAutoAll. 1607 D-2648 readentry restart (13-field fscanf regex + pre-3.3 compat). 1608 D-2649 parse_status_hl2 + hl1: WITH-DEBT for one proven `splitsubfields` overflow-gate off-by-one (15-subfield no-trailing-separator rejects where C returns 15; config-path only, no corpus reach) — batched with the next botl.c/config row, map-named, not Must-fix. 1609 D-2650 writexlentry + XLOGFILE family. 1610 D-2651 shared_menu_optfn family. 1611 D-2652 scores_only + 2 stale parks. 1612 D-2653 populate_nomakedefs (new C-home js/date.js, mdlib.c:842 wired). Every per-SHA `--reach-all` re-run: 0 blocked (honest vacuous) + REACH-OK, no REGRESSED. Rule #2 clean (`imports.mjs --rulecheck`).
**Cadence:** full `sessions` 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, speed `86+0.61/turn` R² 0.78); held-out 11/44 (5,972 pts, RNG 26.7%, rngSteps 81.7%, screens 53.0% — unchanged); corpus 497/540 (92.0%) RNG 99.31% screens 99.1% (+0/−0). check-hot-docs --review 1604–1612: all ok, no cap edits. Coverage queue at 13 open rows — no refill.
## 2026-09-20 — D-2653 `date.c` populate_nomakedefs whole-body port (new C-home `js/date.js`) + `mdlib.c:842` caller wired

**C locus:** ``nethack-c/upstream/src/date.c:52–131`` (populate_nomakedefs) + caller ``mdlib.c:842`` (runtime_info_init) + callees hacklib.c:921–937 (case_insensitive_comp), mdlib.c:235–242 (md_ignored_features), mdlib.c:348–370 (bannerc_string), live mdlib.c:299–308/315–344 (mdlib_version_string/version_id_string). Decisive C facts: contest patch 001:132–133 replaces the `:82` Snprintf with pinned `"May  2 2026 12:00:00"` (NETHACK_REAL_BUILD_DATE unset); recorded C `#version` screens show exactly `last build May  2 2026 12:00:00`; SOURCE_DATE_EPOCH 1777723200 = Sat May 2 12:00:00 2026 UTC (001 patch); date_via_env FALSE (mdlib.c:56, never set); NH_STATUS_RELEASED (patchlevel.h:33); no PORT_SUB_ID on MacOS (global.h:199–209 PC-only); NETHACK_GIT_* only via Windows nmake/makedefs (undefined here).
**JS:** `populate_nomakedefs` js/date.js:107; hook `__setPopulateNomakedefs` js/version.js:458, call js/version.js:473; `PINNED_BUILD_DATE` export js/version.js:30; registration js/date.js:187; side-effect import js/pager.js:19. No new version.js static edge (D-1881 import-free invariant — const.js:21 top-level COMMIT_NUMBER read).
**Change:** new C-home `js/date.js` in C order with per-arm `:line` cites — file-local `extract_field` (`:44–49`), `case_insensitive_comp`, `md_ignored_features` (`(1<<19)|SFCTOOL_BIT`, live const.js import), `bannerc_string` (C out-param → return, mirroring live version_id_string); `:85` length gate kept; extracts/atoi/month-loop `:86–104`; mktime→`Date.UTC(...)/1000` = 1777723200 (recorder TZ=UTC per 001 patch; zone-independent, Rule #2 safe); all 12 fields in global.h:357–370 order with `>>> 0` on C `unsigned long`s; git arms compiled-out → null; C 1987 static dummies deliberately NOT copied (pre-populate readers keep fallbacks — populating changes no observed value).
**Verify:** `node scripts/verify.mjs --fn populate_nomakedefs` → syntax 3 files · rule2 · hidden note (0 blocked, coverage row) · REACH smoke 24/24 REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 → VERIFY: PASS. Probe /tmp/probe-date.mjs: 12/12 fields incl. version_id byte-equal to recorded C `#version` screen and hook-driven runtime_info_init populating build_time 1777723200.
**Named:** `free_nomakedefs` (date.c:134–173 remainder; release_runtime_info:871); `make_version` (mdlib.c:248–295 — interim above, collapses on its row); `dupstr` (C strdup; JS assignment copies, no helper); `__DATE__`-absent `#else` (compiled out); NETHACK_GIT_* arms (compiled out); const.js May-3 startup banner pin belongs to the version.c/askname path (different row — untouched). Review 1494 (ACCEPT, no actionables) names populate as omitted — now live; no stamp (nothing to address).
**Next:** make_version row (own version struct, collapse hook arg); version.c banner-getter row (may reconcile const.js banner with populated copyright_banner_c).
## 2026-09-20 — D-2652 `earlyarg.c` scores_only whole-body port (new C-home `js/earlyarg.js`) + STALE parks pot_acid_damage / left_side

**C locus:** ``nethack-c/upstream/src/earlyarg.c:404–441`` (scores_only; ATTRNORETURN staticfn) + caller ``early_options :180`` case 's' ``:309`` (``scores_only(argc + 1, argv - 1, *hackdir_p)``) + ``cfgfiles.c:1592`` config_error_done + ``end.c:1674`` nh_terminate. Decisive C facts: config_error_done flushes queued config errors now (``:407–410``); CHDIR live (config.h:438) so chdirx ``:412–416``; SYSCF live (config.h:233) so the initoptions_noterminate/initoptions arm ``:417–422``; PANICTRACE live via CRASHREPORT on linux (config.h:244–276) so ARGV0+panictrace_setsignals ``:423–427``; UNIX live (config.h:18) so whoami ``:428–430``; prscore ``:431``; MSWIN_GRAPHICS undefined (config.h:61) so wait_synch ``:432–437`` compiles out; nh_terminate(EXIT_SUCCESS) bypasses opt_terminate ``:439`` (in_moveloop=0 end.c:1676; freedynamicdata/dlb_cleanup; exit()).
**JS:** ``js/earlyarg.js`` (new, +~60); ``docs/c-js-map/startup.md`` (earlyarg.c row, cited D-2652). No new cross-module edge.
**Change:** new C-home ``js/earlyarg.js`` (Constitution §3.1 1:1) in C order with per-arm ``:line`` cites — exported ``async scores_only(argc, argv, dir)`` (async only because live ``prscore`` must be awaited; C ATTRNORETURN unrepresentable without a process — returns after the terminate tail): config_error_done/chdirx/SYSCF-initoptions/PANICTRACE/whoami arms as cited named omits (below), ``await prscore`` live, MSWIN arm noted compiled-out, nh_terminate tail as ``program_state.in_moveloop = 0`` + live ``nh_terminate_capture()`` (same call as done2 js/end.js:1208 / save-quit js/save.js:1164) + return (C NOTREACHED). Imports: ``game`` (gstate.js leaf) + ``prscore``/``nh_terminate_capture`` (topten.js — ``imports.mjs --can``: no new cycle, new file has no importers).
**Verify:** ``node scripts/verify.mjs --fn scores_only`` → VERIFY: PASS — syntax (1 changed: js/earlyarg.js); rule2; hidden note (no corpus session blocked); reach (RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7. ``/tmp/scores-only-smoke.mjs`` → SMOKE PASS (bad-args arm returns, in_moveloop=0, no throw/hang).
**Named:** ``config_error_done`` (cfgfiles.c:1592 flush — JS config_error_add is a sink js/botl.js:1149, no queue can exist); ``chdirx``/``nhUse`` (Rule #2, no CWD in scored JS); SYSCF ``initoptions`` + ``initoptions_noterminate`` toggle (no initoptions port; JS options resolve in-process, sysconf has no scored counterpart); ``panictrace_setsignals`` + ARGV0 save (no signals/stack-trace in scored JS); ``whoami`` (no OS user; cf. getuid→0; plname default owned by JS startup/askname); ``nh_terminate`` process exit proper (no exit() in scored JS — capture boundary + return); ``opt_terminate`` (bypassed in C itself); ``early_options`` caller (own future row).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2651 `options.c` shared_menu_optfn whole-body port (menu-command key family + 13 allopt rows wired)

**C locus:** ``nethack-c/upstream/src/options.c:2052–2074`` (shared_menu_optfn; staticfn decl ``:362``) + file-local callees ``check_misc_menu_command :694–706`` / ``spcfn_misc_menu_cmd :5452–5476`` / ``illegal_menu_cmd_key :8037–8057`` + C-global ``add_menu_cmd_alias :8080–8097`` (extern.h:2317) + ``to_be_done[] :125`` + 13 callers ``optfn_menu_* :2077–2177`` (optlist.h NHOPTC wires one wrapper per option name) + ``get_option_value :8481–8505`` CompOpt arm. Decisive C facts: do_set resolves the index first (``:2060``) and delegates with the same req (``:2064``); ``spcfn`` overwrites incoming ``op`` with the ``string_for_opt`` tail and skips on the empty sentinel (``:5462`` pointer compare); ``illegal_menu_cmd_key`` rejects digits, letters-except-'@' (C ``letter()`` counts '@' as a letter; hacklib.c ``:62–72``) and default object-class symbols (``:8048–8049`` loop from 1); get_val reports ``to_be_done`` while get_cnf_val clears (``:2066–2071`` vs ``spcfn :5471–5472`` clearing both); the ``:649–659`` parseoptions call site is ``#if 0`` (dead in C).
**JS:** ``js/options.js`` (+~190: family + 2 same-module import joins + 13 row wirings + holder fix); ``docs/c-js-map/data.md`` (family clause, cited D-2651). No new cross-module edge (``imports.mjs --can options.js objects.js def_oc_syms``: ALREADY).
**Change:** C-home ``js/options.js`` in C order with per-arm ``:line`` cites — exported ``shared_menu_optfn`` (do_init no-op, do_set resolve-then-delegate, get_val ``to_be_done``, get_cnf_val clear); file-local ``check_misc_menu_command`` (live ``default_menu_cmd_info`` + live ``match_optname`` — full-name ``strlen`` minimum kept, so abbreviated heads fail exactly like C); file-local ``spcfn_misc_menu_cmd`` (live ``string_for_opt``/``txt2key``/``bad_negation``); file-local ``illegal_menu_cmd_key`` (inline digit/letter ranges, live ``def_oc_syms`` + ``MAXOCLASSES`` joined to the existing objects.js edge — same module, no new edge); exported ``add_menu_cmd_alias`` onto ``game.mappedMenu`` read by live ``mapped_menu_strings``/``get_menu_cmd_key`` (``MAX_MENU_MAPPED_CMDS`` joined to the existing const.js edge); file-local ``set_optbuf`` holder for C char*-out params; 13 exported ``optfn_menu_*`` forwarders in C order. ``get_option_value`` CompOpt arm now passes a ``{ buf }`` holder (C static retbuf ``:8485``).
**Verify:** ``node scripts/verify.mjs --fn shared_menu_optfn`` → VERIFY: PASS — syntax (1 changed); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (auto: shared file changed). ``/tmp/probe_menu_optfn.mjs`` → ALL PASS 23/23 (legal-key alias + rebound lookup, unknown-head err, get_val/cnf texts, negated/digit/letter rejects incl. '@' exception, missing-value no-op, C-parity prefix reject, ``port-coverage --name`` now covered).
**Named:** ``config_error_add`` sink (+4 sites: both ``illegal_menu_cmd_key`` arms, ``bad_negation`` inside ``spcfn``, ``string_for_opt`` missing-parameter — map's existing sink line); C ``parsebindings :7658–7662`` menu-command arm (travels with the future BINDINGS row); ``menu_headings``/``menu_objsyms`` optfns (own rows); ``nhl_get_config`` lua caller (pre-existing).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2650 `topten.c` writexlentry + XLOGFILE family whole-body port (xlog line builder + conduct/achieve encoders)

**C locus:** ``nethack-c/upstream/src/topten.c:340–391`` (writexlentry; staticfn decl ``:71``) + static callees ``encodexlogflags :393–408`` / ``encodeconduct :410–452`` / ``encodeachieve :454–476`` / ``add_achieveX :478–488`` / ``encode_extended_achievements :490–581`` / ``encode_extended_conducts :583–610`` + ``allmain.c:987–993`` (timet_to_seconds) + caller ``:718`` (topten XLOGFILE append arm). Decisive C facts: XLOG_SEP is '\t' ``:343``; version/points/deathdnum/deathlev/maxlvl/hp/maxhp/deaths/deathdate/birthdate/uid in one buf[] flush ``:347–356``; death via formatkiller(how, FALSE) ``:360–364`` (no helpless copy); multi<0 while= with multi_reason-or-"helpless" ``:365–367``; conduct/turns/achieve hex ``:368–370``; achieveX from achbuf ``:371–372``, conductX reusing buf[] ``:373–374``; realtime/starttime/endtime ``:375–378``; gender0/align0 filecodes ``:379–381``; flags ``:382``; gold = invent + hidden ``:383–384``; wish/arti-wish/bones/reroll ``:385–388``; trailing newline ``:389``. encodeachieve offset 31 + 1..31→bits 0..30 ``:469–473`` with the signed-31-bit comment ``:461–468``; rank arm builds "attained_the_rank_of_<title>" with Role_switch=gu.urole.mnum and achidx<0 female pick ``:565–572``; ACH_BLND/ACH_NUDE have no arm (default-continue); "bonesless" reads !flags.bones with C default TRUE ``:605``.
**JS:** ``js/topten.js`` (+~300: XLOGFILE family + 5 import edges); ``js/insight.js`` (+1 word: ``export`` on ``sokoban_in_play``); ``js/allmain.js`` (+12: exported ``timet_to_seconds``); ``docs/c-js-map/turns.md`` (writexlentry clause, cited D-2650).
**Change:** new exports in C-home ``js/topten.js`` in C order with per-arm ``:line`` cites — ``writexlentry(tt, how)`` returns the full tab-separated line (C FILE* becomes the return; out-param ``char *buf`` callees return their string); ``encodexlogflags`` (wizard/discover via the same ``flags.debug||flags.wizard`` / ``flags.explore||flags.discover`` read as ``topten()``); ``encodeconduct`` (sokoban bit gated on live ``sokoban_in_play``); ``encodeachieve(secondlong)``; file-local ``add_achieveX`` returning the joined string; ``encode_extended_achievements`` full 22-arm switch (rank arm via live ``rank_of(rank_to_xlev(1..8), game.urole.mnum, achidx<0)`` + live ``strNsubst``/``lcase``); ``encode_extended_conducts`` (19 names; ``bonesless`` is ``flags.bones === false`` since C defaults bones TRUE and unset means enabled — ``js/bones.js:651``). Live imports joined ``js/topten.js`` (``imports.mjs --can`` SAFE, hoisted): ``formatkiller`` (end.js), ``money_cnt`` (shk.js), ``hidden_gold`` (vault.js), ``num_genocides`` + ``sokoban_in_play`` (insight.js — one-word ``export`` added), ``timet_to_seconds`` (allmain.js — newly exported at its C home, via live ``timet_delta``); ``strNsubst``/``lcase`` (hacklib.js) + ``rank_of``/``rank_to_xlev`` (roles.js) + 22 ACH_* (const.js) joined pre-existing edges.
**Verify:** ``node scripts/verify.mjs --fn writexlentry`` → VERIFY: PASS — syntax (3 changed); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (auto: shared file changed). ``--fn encode_extended_achievements`` → same PASS (full 44/44). ``/tmp/xlog-probe.mjs`` → PROBE-OK 24/24 (empty/full conduct bits incl. sokoban gating 0x2FFF→0x3FFF, achieve packing incl. secondlong shape, neg-id abs switch, default-arm skip, 33-field line shape, while= arm on/off).
**Named:** FILE*/fopen/lock append plumbing (no xlogfile VFS consumer — D-2585 arm stands); ``alloc`` (GC); ``wizard``/``discover`` macros read via flags (same as ``topten()``).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2649 `botl.c` parse_status_hl2 whole-body port (hilite_status threshold parser + hl1 splitter)

**C locus:** ``nethack-c/upstream/src/botl.c:2814–3106`` (parse_status_hl2; staticfn decl ``:650``) + static callees ``s_to_anything :1930`` / ``fieldids_alias :2189`` + ``fldname_to_bl_indx :2221`` / ``is_ltgt_percentnumber :2652`` / ``has_ltgt_percentnumber :2673`` / ``splitsubfields :2688`` / ``is_fld_arrayvalues :2730`` / ``status_hilite_add_threshold :2784`` / ``match_str2conditionbitmask :3171`` + ``str2conditionbitmask :3209`` + ``parse_condition :3233`` / coloratt.c ``colornames :12`` + ``attrnames :47`` + ``match_str2clr :348`` + ``match_str2attr :374`` + wintype.h ATR_* + hacklib ``trimspaces``/``digit`` + callers ``parse_status_hl1 :2593`` (C sites ``:2616`` + ``:2637``) / recursive ``:2852``. Decisive C facts: CHARACTERISTICS recursion rewrites s[0] per BL_STR..BL_CH ``:2849–2852``; trailing-token always-arm via ``*s[sidx+1]=='\\0'`` + sidx-- ``:2884–2888`` (so ``hitpoints/<10%/red/blue`` is rule + always-blue, TRUE); ``eq`` never set but read in the rel chain ``:2976–2990``; ``(void) trimspaces`` discards the leading shift — only the trailing strip lands ``:3096``; splitsubfields -1 overflow folds into ``sf < 1`` FALSE ``:3034–3037``; percent ``-1/0/100/101`` int-vs-enum edge compares ``:3002–3012``; hl1 lowercases into hsbuf ``:2608`` with the title-space exception ``:2611–2615`` and ``hilite_delta = 3L`` tail ``:2645``.
**JS:** ``js/botl.js`` (+717: 8 const.js names + 15 terminal.js CLR_* + hacklib ``lowc`` (same modules, no new edge) + ~690-line parser family after ``cond_menu``); ``docs/c-js-map/startup.md`` (botl.c status row clause, cited D-2649).
**Change:** new exports ``parse_status_hl2(s, from_configfile)`` + ``parse_status_hl1(op, from_configfile)`` in C-home ``js/botl.js`` in C order with per-arm ``:line`` cites, plus file-local ports of every static callee (``s_to_anything`` with ``c_atoi``; ``fldname_to_bl_indx`` over ``initblstats[].name`` + local ``fieldids_alias``; C-valued ``C_ATR_*`` (wintype.h — NOT terminal.js numbering) + local color/attr tables + ``match_str2clr``/``match_str2attr``; ltgt/has/split (split keeps empty middles, drops one trailing empty, null = C -1)/arrayvalues; ``status_hilite_add_threshold`` appending to ``game.gb.blstats`` chains with on-demand shelf; ``match_str2conditionbitmask``/``str2conditionbitmask``/``parse_condition`` producing ``game.gc.cond_hilites``). Same-module import extensions only (const.js + terminal.js CLR_* + hacklib ``lowc`` — no new edge, no ``imports.mjs`` needed). Only behavior beyond the row: the ``:2593`` caller (same C file, both C sites wired).
**Verify:** ``node scripts/verify.mjs --fn parse_status_hl2`` → VERIFY: PASS — syntax (1 changed: js/botl.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (tool: no shared file changed). ``/tmp/hl2-smoke.mjs`` → 24/24 (doc examples, error arms, dup-color, condition+attr, characteristics fan-out; stored HP rule fld/rel/coloridx + hilite_delta + cond_hilites[CLR_RED] inspected).
**Named:** ``config_error_add`` sink (options.c; bad_negation precedent — FALSE propagation kept at all 14 sites); ``threshold_value``/``is_out_of_range``/``op`` message fragments (sink-only); ``query_arrayvalue`` menu prompter (never called by this path); options.c ``:1873`` do_set dispatch (negated/empty arms + ``clear_status_hilites``) + cfgfiles.c ``:1173`` caller (file-infra class); interactive ``status_hilite_menu_add`` family (``:3890+``, own rows); ``alloc``/``free`` (GC).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2648 `topten.c` readentry whole-body port (record parser + pre-3.3 compat arm)

**C locus:** ``nethack-c/upstream/src/topten.c:220–298`` (readentry; staticfn decl ``:68``) + callees ``discardexcess :207–215``, ``copynchars`` (hacklib.c:286–297), ``str2role`` (role.c) + ``roles[].filecode``, ``nsb_unmung_line :1478–1484`` (NO_SCAN_BRACK-only) + callers ``:764`` (topten rank loop) / ``:1276`` (prscore scan) / ``:1398`` (get_rnd_toptenentry pickentry). Decisive C facts: TTFIELDS-13 fscanf ``:238–245``; fgets remainder + cut at [SCANBUFSZ-2] + discardexcess when no newline ``:250–257``; ver<3.3 fmt32 "%c%c %[^,],%[^\n]" ``:259–276`` with unconditional role re-truncate + str2role>=0 filecode fixup + race "?" + Mal/Fem + align "?"; fmt33 six-field ``:277–287``; Y2K gated on points>0 ``:293–297``. Neither NO_SCAN_BRACK nor UPDATE_RECORD_IN_PLACE is defined in this build (no ``define`` anywhere in nethack-c) — the dot-fmt / ``%[^,]`` ``#else`` branch is the live one.
**JS:** ``js/topten.js`` (+81/−21: SCANBUFSZ + readentry export + alias + import name + 2 comment retouches); ``docs/c-js-map/turns.md`` (readentry clause, cited D-2648).
**Change:** restarted as exported ``readentry(line)`` in C order with per-arm ``:line`` cites — 13-field fscanf regex (``:238–245``), SCANBUFSZ remainder cut (``:246–257``; SCANBUFSZ const from topten.c:59), pre-3.3 fmt32 arm with one-char role/gender + str2role→``roles[i].filecode`` + Mal/Fem + "?" (``:259–276``), fmt33 six-field arm (``:277–287``), Y2K gated on points>0 (``:293–297``). ``roles`` joined the existing ``./roles.js`` import (``imports.mjs --can``: ALREADY, no new edge). ``readentry_line`` kept as a one-line delegate; ``read_record_entries`` now calls ``readentry`` directly.
**Verify:** ``node scripts/verify.mjs --fn readentry`` → VERIFY: PASS — syntax (1 changed: js/topten.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (tool: no shared file changed). ``/tmp/readentry-probe.mjs`` → PROBE-OK 6/6 (modern, pre-3.3 fixup + Mal/?, garbage/empty→0, Y2K gated, 3.3 boundary→modern).
**Named:** ``discardexcess`` (topten.c:207 — FILE-streaming only; a VFS line carries no excess past its newline, and every fail arm still sets points = 0); UPDATE_RECORD_IN_PLACE fpos (``:235–237``, VMS-only — whole-record VFS writes per D-2585); NO_SCAN_BRACK fmts + ``nsb_unmung_line`` (``:225–233``, ``:283–292`` — ifdef never defined in this build; unmunging '|'→' ' would corrupt modern record text); ``alloc`` (GC — fresh object per call).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2647 `pickup.c` query_category ParanoidAutoAll verify_All + A-hint arms

**C locus:** ``nethack-c/upstream/src/pickup.c:1225–1508`` (query_category) + ``decl.h:167–168`` / ``decl.c:193`` (ga.A_first/second_hint, init 0) + ``flag.h:556/584`` (ParanoidConfirm/ParanoidAutoAll over flags.paranoia_bits) + callers ``do.c:994`` (menu_drop FULL, CHOOSE_ALL|ALL_TYPES PICK_ANY) / ``do_wear.c:3100`` (menu_remarm FULL, WORN|ALL|UNPAID|BUCX PICK_ANY, no CHOOSE_ALL) / ``invent.c:3873`` (dotypeinv, no CHOOSE_ALL/ALL_TYPES, PICK_ONE) / ``pickup.c:3286`` (menu_loot MENU_FULL PICK_ANY). Decisive C facts: ``verify_All = (how == PICK_ANY) && ParanoidAutoAll :1326`` (inits FALSE ``:1246``, so only CHOOSE_ALL callers); hints ``!ga.A_first_hint++ || cmdassist :1328`` vs ``else if (show_a) !ga.A_second_hint++ || cmdassist :1331–1333`` + blank ``:1337``; confirm ``paranoid_ynq(ParanoidConfirm, "Really autoselect All?", TRUE) :1464–1465`` with y-honor / n-remove (``n > 1`` shift) / n-convert lone-'A' to ALL_TYPES_SELECTED (ALL_TYPES set) with FALLTHROUGH to q-cancel + free ``:1466–1492``; lone-'A' without verify ``n = 0`` + free + ``pline("No relevant items selected.") :1495–1501``; ``query_done :1503`` (destroy window, ``n < 0`` → 0).
**JS:** ``js/pickup.js`` (+~65/−12: 2 import names + doc + CHOOSE_ALL hints + verify block); ``docs/c-js-map/turns.md`` (doddrop clause retires the query_category ParanoidAutoAll named omit, cited D-2647).
**Change:** in C order with per-arm ``:line`` cites — ``PARANOID_CONFIRM``/``PARANOID_AUTOALL`` joined the existing ``./const.js`` import (same module, no new edge); ``paranoid_ynq`` joined the existing ``./getline.js`` import (``imports.mjs --can``: ALREADY edge, no new edge; runtime-only call, no TDZ read). ``let verify_All = false :1246``; CHOOSE_ALL arm computes ``how === PICK_ANY && paranoia AUTOALL :1326`` and picks the hint by ``!(game.A_first_hint | 0) || cmdassist`` / ``else if (show_a)`` second hint (counters on ``game`` per the ``ga.abort_looting``→``game.abort_looting`` precedent, ``| 0`` covers the decl.c init; add_menu_str ⇒ non-selectable row). Post-menu ``verify_All`` block ``:1455–1493`` (findIndex first 'A'; y-honor / n+splice / n+convert ``picked[0]`` to ALL_TYPES_SELECTED / else cancel ⇒ ``[]`` for free) and the ``else if`` lone-'A' rejection ``:1495–1501``; common count-map return stands (``query_done :1503`` ⇒ GC, nothing to destroy; ``n < 0`` ⇒ ``[]`` pre-existing).
**Verify:** ``node scripts/verify.mjs --fn query_category`` → VERIFY: PASS — syntax (1 changed: js/pickup.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (tool: no shared file changed).
**Named:** menu_loot ``query_loot_category`` ParanoidAutoAll + unpaid/billed + WORN_TYPES + venom (pre-existing clone omissions, own envelope); C ``assert(*pick_list != NULL)`` (runner invariant); ``debugpline0`` no-single-match (compiled out); ``alloc``/``free`` pick_list (GC object); PICK_NONE select path (no caller passes it — pre-existing, untouched).
**Next:** pop the next Open — coverage row (``topten.c`` readentry). Refill-dupe removals in this commit (Stale-parked, re-emitted post-park, never re-pop — proofs in the Parked **Stale** index, 0 blocked): ``uhitm.c`` mhitm_ad_dcay (3 arms split-live uhitm.js:2344/mhitu.js:3001/mhitm.js:3975, dispatch wired uhitm:2421/mhitu:3212/mhitm:4779), ``vision.c`` right_side (whole body live js/vision.js:545, view_from callers wired), ``worn.c`` clear_bypasses (whole body live js/worn.js:1132).
## 2026-09-20 — D-2646 `objnam.c` singplur_lookup singular arms (craft/slice-mongoose/badman-men keep + men→man gate)

**C locus:** ``nethack-c/upstream/src/objnam.c:2707–2779`` (singplur_lookup; staticfn decl ``:43``) + singular caller ``makesingular :3076`` (``singplur_lookup(bp, p, FALSE, special_subjs)``) + plural caller ``makeplural :2912`` (already wired) + ``badman :3194–3231`` (no_men/no_man tables, BSTRNCMPI + space boundary) + ``makesingular :3135–3155`` else-branch (men→man ``:3137–3140``). Decisive C facts: craft needs ``baselen > 5`` (bare "craft" falls through); slice/mongoose is whole-string ``strcmpi`` (not suffix); singular men arm keeps (no transform) iff ``badman(bp, FALSE)``; the ``:3137`` conversion runs iff ``!badman``; ``BSTRCMPI(base,ptr,str) = (ptr < base || strcmpi)``.
**JS:** ``js/objnam.js`` (+~25/−3: makesingular arms + guard + doc); ``docs/c-js-map/turns.md`` (singular-arms clause, cited D-2646).
**Change:** three arms inserted in C order between special_subjs and one_off-reverse with per-arm ``:line`` cites (craft ``:2732`` via in-file ``eqCI`` tail; slice/mongoose ``:2736–2743`` whole-word; men-keep ``:2758–2762`` via live ``badman(bp, false)``), and the later ``men→man`` gated with ``!badman(bp, false)`` per ``:3137–3140`` (mirrors C's redundant double-check). Doc ``Named omissions`` updated. No new imports (``eqCI``/``badman`` in-file, hoisted); no caller rewiring (both C call sites already dispatch through ``makeplural``/``makesingular``).
**Verify:** ``node scripts/verify.mjs --fn singplur_lookup`` → VERIFY: PASS — syntax (1 changed: js/objnam.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (tool: no shared file changed). ``/tmp/singplur_probe.mjs`` → PROBE-OK 22/22 (slice/Slice, mongoose, specimen/abdomen/omen, cavemen→caveman, aircraft/hovercraft/craft, plus 11 singularization regressions: dogs/boxes/rubies/knives/wolves/children/mice/geese/lice/men/boots).
**Named:** ``makesingular`` pronoun they/them/their→it block (``:3053–3068``, pre-existing); ia→ium balactherium arm (``:3149–3153``, own row); one_off same-form keep check (analyzed outcome-equivalent: JS fall-through conserves every real case); full ``Strcasecpy`` case polish (pre-existing).
**Next:** pop the next Open — coverage row. Stale-parked this iteration (bodies complete under split names, 0 blocked — one line each under Parked **Stale**): ``uhitm.c`` mhitm_ad_dcay, ``vision.c`` right_side (check-(2) set_min subsumed in mark_visible_range — unverified edge, falsifier stands), ``worn.c`` clear_bypasses.
## 2026-09-20 — D-2645 `sp_lev.c` lspo_monster des.monster binding + same-file create_altar whole-body ports

**C locus:** ``nethack-c/upstream/src/sp_lev.c:3214–3400`` (lspo_monster: defaults :3223–3244, string forms :3246–3295, table form :3296–3380, coord pack :3382–3385, class-from-monsym :3387–3388, create_monster call :3390, custom-invent tail :3392–3397) + ``:2446–2486`` (create_altar) + post-spawn arms from ``create_monster :1994–:2186`` + ``get_table_align :3113–3128``, ``find_montype :3142–3164``, ``get_table_montype :3166–3179``, ``get_table_monclass :3130–3140``, ``spo_end_moninvent :3030–3036``.
**JS:** ``js/mklev.js`` (+~300: 2 exports + 5 file-locals + 6 import names); ``js/trap.js`` (+1/−1 export keyword); ``docs/c-js-map/data.md`` (clause, cited D-2645).
**Change:** new unpacked bindings in C-home ``js/mklev.js`` in C order with per-arm ``:line`` cites (mirroring ``l_create_object``): file-local ``get_table_align_unpacked`` (:3113–3128 7-string table, default random), ``lspo_bool_opt`` (boolean-opt reader preserving explicit BOOL_RANDOM), ``lspo_monster_appear`` (:3326–3344 obj:/mon:/ter: parse, unknown throws), ``lspo_monster_from_string`` (:3246–3295 class-vs-name + coord/x,y), ``lspo_monster_normalize_table`` (:3296–3380, RNG-free; unknown id throws; classLetter via live ``monsym``; has_invent from inventFn presence), exported async ``l_create_monster`` (string forms delegate with C defaults; table form spawns through live ``splev_create_monster`` so the gender rn2(2) burns once in C position, then christen :1994, female mgend rule :3355–3367 + :2125 over the replayed outcome, cancelled/revived/avenge/stunned/confused/invis/blinded/paralyzed/fleeing :2135–2159 with %127, waiting vampshifted newcham :2160–2167, m_lev_adj clamp :2168–2175, invent drop :2176–2182 awaited, CUSTOM global :2183–2184, inventFn + spo_end_moninvent :3392–3397 with m_dowear awaited per D-2335), exported ``splev_create_altar`` (room/packed/random coord via live ``get_free_room_loc(_coord)``/``get_location_coord``, TEMPLE adopt via live ``in_rooms``, ``set_levltyp`` gate, sp_amask, shrine rn2(2) :2472, priestini + SHRINE/SANCTUM + has_temple). ``set_levltyp`` exported from ``js/trap.js`` (one keyword; its SDOOR→AIR + count-scan deltas unobservable with newtyp ALTAR). Only new names on pre-existing edges (``imports.mjs --can`` ALREADY ×5).
**Verify:** ``node scripts/verify.mjs --fn lspo_monster`` → VERIFY: PASS — syntax (2 changed: js/mklev.js js/trap.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — new exports, no live callers — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared files changed). ``--fn create_altar`` → identical PASS tail. ``/tmp/lspo_monster_probe.mjs`` → PROBE-OK 3/3 (unknown-id / appear_as / non-table error arms).
**Named:** ``lspo_altar`` + ``lspo_monster`` Lua argc dispatch (no Lua layer; callers pass unpacked); seentraps trap list (C TODO :3318, stays 0); G_UNIQ extinct / G_GONE random (:1949–1953, pre-existing splev behavior); FURNITURE/OBJECT appear arms (:2016–2061, pre-existing splev fixup deferral); Lua stack juggling (lua_remove/pop) + GC Free (no JS analog).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — Audit reviews 1595–1603 (D-2636…D-2644 window) + cadence

**Audit:** 9 JS-touching SHAs since fe53547d, one SHA at a time, each file written before the next SHA. **9 ACCEPT, 0 Must-fix** (Must-fix stays empty; Next cluster unchanged: `sp_lev.c` lspo_monster). 1595 D-2636 closes 1592's C-wrong (`i !== fsel`, test 2/2). 1596 onscary: full arm + caller closure (13 C sites; teleport locals verified CLONEs; distfleeck named). 1597 avoid_trap: clone→live ×3 + hallu prompt `trapname(t,true)` ≡ C's picked-name expression. 1598 rest_regions: field order + ttl floor + ghostly arms + 3 restore-path wires; `max_monst = n_monst` confirmed in C body. 1599 destroy_armor: BUC worry checked unfounded (bknown:0 gate); pray.c:878 already wired via inline override idiom. 1600 experience: macro-expansion CLONE verified vs `_uprop_he` body. 1601 change_sex: PL_CSIZ 32=32; D-log "rank_of fallback" wording loose, code accurate. 1602 coder_init: prior-coder read + Free+NULL finally verified at :6459/:6497–6499. 1603 relink: panic texts C-exact; ghostly-obj-map narrowing unreachable (no ghostly lights) with its own row. Every per-SHA `--reach-all` re-run: 0 blocked (honest vacuous) + REACH-OK, no REGRESSED.
**Cadence:** full `sessions` 44/44 (Scr 11,405/11,405, RNG 792,838/792,838, speed `59+0.34/turn` R² 0.77); held-out 11/44 (5,972 pts, RNG 26.7%, screens 53.0% — unchanged); corpus 497/540 (92.0%) RNG 99.31% screens 99.1% (+0/−0). check-hot-docs --review 1595–1603: all ok, no cap edits. Coverage queue at 8 (band floor) — no refill.
## 2026-09-20 — D-2644 `light.c` relink_light_sources whole-body port (flag-gated relink + both restore.c caller guards)

**C locus:** ``nethack-c/upstream/src/light.c:517–563`` (relink_light_sources). Decisive C facts: walk ``gl.light_base :538``; only ``LSF_NEEDS_FIXUP`` entries ``:539``; type must be LS_OBJECT/LS_MONSTER else panic ``:540/:556–557``; ``nid = id.a_uint :541``; ghostly remap via ``lookup_id_mapping`` with panic on miss ``:542–543``; ``which`` sentinel ``'\0' :545``; object arm assigns ``find_oid(nid)`` then NULL-checks ``:546–548``; monster arm ``find_mid(nid, FM_EVERYWHERE) :549–551``; miss panics ``can't find %c_id %u :553–555``; flag cleared ``:559``. Callers restore.c ``:726`` (restgamestate, FALSE) and ``:1300`` (getlev, ghostly).
**JS:** ``js/light.js`` (+~60: 1 export + 4 import names); ``js/save.js`` (+12: import + two C-order guard calls, below).
**Change:** new exported ``relink_light_sources(ghostly)`` in C-home ``js/light.js`` in C order with per-arm ``:line`` cites — array walk ``:538``, flag gate ``:539``, type arms ``:540``, bare-numeric ``nid :541`` (deserLightList shape), ghostly remap ``:542–543`` via live ``lookup_bones_id`` (bones.js — documented JS analogue of C ``lookup_id_mapping`` restore.c:1484), ``which = '\0' :545`` (explicit escape — a raw NUL byte written mid-edit was replaced), assign-then-check obj/mon arms ``:546–551`` via live ``find_oid`` (shk.js) / ``find_mid(nid, FM_EVERYWHERE)`` (mon.js), miss/bad-type panics ``:553–557`` as throws (lev_json relink precedent; ``%u`` via ``>>> 0``, ``%d`` with parens), flag clear ``:559``. New edges (``imports.mjs --can``: IN-SCC, hoisted functions, runtime-only calls): ``find_oid``/``find_mid``/``lookup_bones_id`` + ``FM_EVERYWHERE`` (const.js import extended).
**Verify:** ``node scripts/verify.mjs --fn relink_light_sources`` → VERIFY: PASS — syntax (2 changed: js/light.js js/save.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (tool: no shared file changed). ``/tmp/relink_probe.mjs`` → PROBE-OK 11/11 (obj/mon resolve + flag clear, unflagged skip, o_id/m_id miss messages, bad-type parens, ghostly remap hit/miss). The probe caught one real bug pre-verify: bad-type message missing C's parens (fixed).
**Named:** C ``lookup_id_mapping`` as its own row (restore.c:1484; bucket/list state = bones id map; ghostly obj-id recording unported — the arm resolves through ``lookup_bones_id`` like region.js reset_region_mids); ``find_mid`` FM_MIGRATE/FM_MYDOGS arms (pre-existing named omit in mon.js — flag passed for fidelity); ghostly light install in getlev_bones (pre-existing bones omission); LSF_NEEDS_FIXUP producers (deser paths link before install by architecture).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2643 `sp_lev.c` sp_level_coder_init whole-body port (coder object + update_croom + create_des_coder lifecycle)

**C locus:** ``nethack-c/upstream/src/sp_lev.c:6336–6376`` (sp_level_coder_init) + ``:6323–6333`` (update_croom) + ``:6443–6448`` (create_des_coder) + ``load_special :6454–6501``. Decisive C facts: field defaults ``:6341–6348`` (allow_flips 3, n_subroom 1); statics ``:6350–6351``; tmproomlist/failed_room over ``0..MAX_NESTED_ROOMS`` ``:6353–6356`` (sp_lev.h:49 = 5); the ``:6358`` update_croom() reads the *prior* ``gc.coder``, not the local under construction; container zero ``:6360–6362``; invent NULL ``:6364``; SpLev_Map memset ``:6366``; flags ``:6368–6371`` (``In_hell(&u.uz)``); reset ``:6373``; return ``:6375``. Sole caller create_des_coder guards ``:6446`` + assigns ``:6447``; load_special creates at entry ``:6459`` and Free+NULLs at give_up ``:6497–6499``.
**JS:** ``js/mklev.js`` (+~100: 2 exports + 1 local + wrapper split + const import + invent let); ``docs/c-js-map/data.md`` (coder-lifecycle clause, cited D-2643).
**Change:** new exported ``sp_level_coder_init`` in C-home ``js/mklev.js`` in C order with per-arm ``:line`` cites — object literal for ``alloc :6339`` (GC, no live alloc export), ``MAX_NESTED_ROOMS + 1`` arrays (const.js import extended), statics via the existing helper ``:6350–6351``, prior-coder ``update_croom() :6358``, container loop + ``container_idx = 0 :6360–6362``, new ``invent_carrying_monster`` module let (sp_lev.c:198) nulled ``:6364``, SpLev_Map fresh Set ``:6366``, ``game.level.flags`` arm ``:6368–6371`` (hellish-flag read ≡ ``In_hell``, mklev.js:1416 precedent), ``reset_xystart_size() :6373``. New exported ``update_croom`` (``:6326–6332``; ``?? null`` maps OOB slots to C NULL) + local ``create_des_coder`` (``:6446–6447`` guard). ``load_special_proto`` split into a wrapper (``create_des_coder`` at entry ``:6459`` + ``try/finally`` Free+NULL ``:6498–6499`` so every load re-inits fresh like C) over the renamed dispatch body (its old reset+statics preamble removed as subsumed).
**Verify:** ``node scripts/verify.mjs --fn sp_level_coder_init`` → VERIFY: PASS — syntax (1 changed: js/mklev.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared mklev.js changed). ``/tmp/smoke_coder.mjs`` → ALL SMOKE CHECKS PASS (defaults incl. allow_flips 3 / n_subroom 1 / 6+6 arrays; flags incl. hellish→temperature 1; xystart/size; SpLev_Map cleared; croom track + null arms with live coder; no-coder no-op).
**Named:** ``alloc`` (GC object — allocation, not semantics); ``load_special`` post-``load_lua`` epilogue reads of the coder fields (link/edge, wallification, flip, solidify, fixup, premap — each loader's named omits stand, future rows); ``lvl_is_joined`` has no live reader yet (kept on the object per C ``:6347``).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2642 `polyself.c` change_sex whole-body port (pl_character rename + amorous-demon set_uasmon + live max_rank_sz)

**C locus:** ``nethack-c/upstream/src/polyself.c:273–303`` (change_sex). Decisive C facts: the ``!Upolyd`` disjunct is necessary because ``is_male()/is_female()`` may be true for certain roles (``:273–281``); poly'd flips saved ``u.mfemale`` too (``:285–286``); ``max_rank_sz()`` ``:287`` (``[this appears to be superfluous]`` — still called); ``svp.pl_character`` takes ``gu.urole.name.f`` iff the new sex is female and ``name.f`` exists, else ``name.m`` (``:288–291``); ``!Upolyd`` restores ``u.umonnum = u.umonster`` (``:292–293``); ``PM_AMOROUS_DEMON`` flips ``flags.female`` again and calls ``set_uasmon()`` (``:294–301``) with the succubus/incubus swap ``#if 0``-disabled (``:296–300``). Callee ``max_rank_sz`` is ``botl.c:402–415``: max ``strlen`` over ``gu.urole.rank[9]`` ``m``/``f`` into ``gm.mrank_sz``.
**JS:** ``js/polyself.js`` (restarted export + 2 import names); ``js/botl.js`` (+24: ``max_rank_sz`` export); ``docs/c-js-map/turns.md`` (change_sex clause, cited D-2642).
**Change:** restarted the export in C order with per-arm ``:line`` cites — sexless-form first flip ``:281–284``, mfemale ``:285–286``, live ``max_rank_sz()`` ``:287``, pl_character rename ``:288–291`` (``String(name).slice(0, PL_CSIZ - 1)``, roles.js shape; C ``Strcpy``), umonnum restore ``:292–293``, amorous flip + ``set_uasmon()`` ``:294–301`` (``:296–300`` kept as a disabled comment, exactly as in C). New live ``max_rank_sz`` export in C-home ``js/botl.js`` (``game.urole.title`` = C ``rank[9]``, rank_of fallback shape; ``.length`` ⇔ ``strlen`` on ASCII titles; writes ``game.gm.mrank_sz``). New edges: ``PL_CSIZ`` (extends the existing const.js import) + ``max_rank_sz`` from botl.js — ``imports.mjs --can``: IN-SCC (existing 98-module cycle), runtime-only call, no TDZ read.
**Verify:** ``node scripts/verify.mjs --fn change_sex`` → VERIFY: PASS — syntax (2 changed: js/botl.js js/polyself.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (no shared file changed). ``/tmp/change_sex_probe.mjs`` → PROBE-OK 14/14: unpoly'd flip + Cavewoman/Caveman rename + umonnum restore + mrank_sz 10; amorous mfemale flip + C-literal double-flip net + umonnum kept + ``set_uasmon`` repoint; poly'd non-amorous kept.
**Named:** none new — every callee is live; the ``:296–300`` swap stays a comment (disabled in C); null-``name.m`` guard (C would deref; unreachable — every role has ``name.m``).
**Next:** pop the next Open — coverage row.
