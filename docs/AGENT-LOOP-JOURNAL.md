# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2123 `do_wear.c` canwearobj polyform/weld/trap gates: poly-unwearable armor DOWNPLAY makes wear prompt `[*]` (queue row `invent.c` getobj, 1 session)

**C locus:** `do_wear.c:2029–2206` `canwearobj` (verysmall/nohands `:2036–2042`, cantweararm cloak-exception + racial `:2043–2057`, worn `:2058–2063`, welded-bimanual suit/shirt `:2065–2072`, helm horns `:2074–2084`, shield bimanual/twoweap `:2085–2103`, boots slithy/centaur/utrap `:2104–2134`, gloves welded/Glib `:2135–2153`, shirt/cloak/suit occupancy `:2154–2188`, else silly_thing `:2189–2194`); `equip_ok :3403–3447` armor-unwearable → DOWNPLAY; `wear_ok :3464–3468`; `invent.c` getobj filter `:1832–1917` + prompt `:1914–1919` (`!buf[0]` → ` [*]`, else ` [%s or ?*]`).
**JS:** 2 files (`js/do_wear.js` +133/−17, `js/worn.js` +4/−3 — 137 insertions), under the 600/10 caps.
**Change:** `js/worn.js` — `cantweararm` exported (import-the-export, no second copy; `breakarm`/`sliparm` stay private). `js/do_wear.js` — same-edge import extensions only (no new module edge): `worn.js` +`cantweararm`/`racial_exception`/`WrappingAllowed`/`is_flimsy`/`has_horns`/`num_horns`, `wield.js` +`bimanual`, `monsters.js` +`slithy`/`MZ_SMALL`, `const.js` +`Upolyd`/`TT_LAVA`/`TT_BURIEDBALL`; full C-order ports in both `canwearobj` (noisy, with C messages) and `canwearobj_silent` (equip_ok DOWNPLAY, silent) — verysmall/nohands, cantweararm with the MUMMY_WRAPPING/`WrappingAllowed`/small-cloak exception + `racial_exception`, worn, welded-bimanual, helm horns (`Upolyd` + `has_horns` + `!is_flimsy`), shield bimanual/`twoweap`, boots slithy/centaur/4-trap, gloves welded/`hero_glib`, occupancy, else `silly_thing`. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn getobj` → PASS syntax (2 changed js file(s): js/do_wear.js js/worn.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify getobj: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-poly-Wizard-92169 moved → `collect_coords` at step 164, was 61) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** none new. Noisy utrap infloor/lava message omits the `surface()` terrain name and buried-ball uses FOOT (C LEG) — display-only approximation, functional return exact; helm-occupied keeps the pre-existing generic `already_wearing('that')` (C `an(helm_simple_name)`).
**Next:** Wizard-92169 now diverges at `collect_coords` step 164 (parked symptom owner — true writer is `wizard.c` tactics per the Parked row; do not re-pop `getobj` for it); the corpus queue will surface the tactics row.
## 2026-09-08 — Audit reviews 1083–1088 (26431cce…10ea68f1, D-2117…D-2122) + cadence 44/44

Review iter, no js/ edits. Six SHAs audited oldest-first against pinned C, each re-measured with `hidden-proxy verify --base HASH~1`: 1083 dobreathe (polyself.c:1420–1447 branch-exact, all callees LIVE, BZ_U_BREATH=hack.h:1484 verbatim) ACCEPT; 1084 Inhell (dungeon.c:1941–1945 flag, matches live do.js:1215 idiom, GEHENNOM still exported) ACCEPT; 1085 kickedloc (hack.c:2708 unconditional, finally covers all early returns, mechanism fits stale-(26,9) symptom) ACCEPT; 1086 enlightenment swim/air/walls (insight.c:1758–1765 C-order, Swimming guard a genuine tautology per youprop.h:279, --can SAFE) ACCEPT; 1087 xname pname bare-ONAME (nameit :1001–1012 exact, doname article gate :1292 intact, dunlev N/A) ACCEPT; 1088 death-disclosure tail (end.c:326–340 chain + :1351–1361 pline + :1521–1541 summary with dunlev≡dlevel per dungeon.c:1324–1328, outentry topten.c:973–980 slice-exact) ACCEPT. 0 Must-fix, 0 queue/Next-cluster changes; queue mf=0 open=11 (no refill). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `67+0.40/turn` (R² 0.79). check-hot-docs: review 1085 expanded 52→64 L, NOTES spoteffects line shrunk to Parked pointer (detail lives in LOOP-QUEUE); all ok, no cap edits.
## 2026-09-08 — D-2122 `end.c` death-disclosure tail: killer-arise, arise pline, death-location guard, `outentry` escape `[max level]` (queue row `end.c` really_done, 4 sessions)

**C locus:** `end.c:326–340` killer-based `ugrave_arise` in `done_in_by` (wraith/mummy/zombie/vampire/ghoul + genocided-arise reset; mptr is `mtmp->data`, mimicker arm resets it at `:255`); `really_done :1351–1361` arise `Your()` + `display_nhwindow(WIN_MESSAGE, FALSE)`; `:1521–1541` death-location (level-teleported-out arm, `Is_astralevel` override, `!In_endgame && !single_level_branch` suffix guard, quest `dunlev`); `topten.c:973–980` escape `escaped the dungeon %s[max level %d]` (`%s` = `death+9` past `escaped (`, i.e. the with-Amulet tail) + closing-paren fixup (truncate on astral, blank elsewhere); `role.c` races[] mummy/zombie columns (human/elf/dwarf/gnome/orc all mapped); `dungeon.c:1325` `dunlev` (= `dlevel`), `:1967` `single_level_branch` (= `Is_knox`).
**JS:** 5 files (`js/end.js` +73/−15, `js/roles.js` +11, `js/teleport.js` +1/−1, `js/topten.js` +13/−2, `js/u_init.js` +4 — 99 insertions), under the 600/10 caps.
**Change:** `js/end.js` — `done_in_by` ports the full `:326–340` chain in C order (`mlet` on `mtmp.data`, `zombie_maker` live import, `Race_if(PM_HUMAN)` as `urace.mnum`, ghoul by `mndx`, genod reset via live `mvitals`); `really_done` ports the `:1351–1361` pline (`Your ${act} as ${an(pmname(arise, Ugender()))}...` + `flush_topl_more`, the `Your()` prefix per the `end.js:1557` idiom) in exact C position (after score, before bones); summary ports the full `:1521–1541` block (`dunlev` inlined as `dlevel`, C's `dunlev(lev)=lev->dlevel`). `js/roles.js` + `js/u_init.js` — race mummy/zombie columns via the existing `pm()` helper, carried into `game.urace` (C struct copy). `js/teleport.js` — `single_level_branch` exported (import-the-export, no second Knox implementation; `imports.mjs` confirms no new edge).
**Verify:** `node scripts/verify.mjs --fn really_done` → PASS syntax (5 changed js file(s)) · PASS rule2 · PASS hidden (2 PASS: scen-normal-Barbarian-92208, scen-tour-Wizard-92103; 2 moved past: scen-death-Wizard-92120 → next_ident at step 57 was 55, scen-genesis-Knight-92068 → next_ident at step 96 was 94; 0 unchanged, 0 worse → PROGRESS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Plus full `sessions`: 44/44 PASS (`79+0.47/turn`, R² 0.75) run separately after the death-path change. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** none new. `outentry` astral-plane text + choked/poisoned/crushed/petrified first-line arms stay deferred (map + `topten.js` header); `done_in_by` G_UNIQ/ghost/mimicker/vampshifter/priest/minvis/hallu/monhealthdescr/multi_reason stay deferred.
**Next:** Wizard-92120 and Knight-92068 now diverge at `next_ident` steps 57/96 (symptom owner per NOTES — fix the writer, not the table reader; no rows added, the corpus queue will surface them); do not re-pop `really_done` for them.
## 2026-09-08 — D-2121 `objnam.c` xname/doname `obj_is_pname` bare-ONAME: gameover inventory shows bare artifact name (queue row `invent.c` inuse_classify, 2 sessions)

**C locus:** `objnam.c:660–664` `xname_flags` (`find_artifact` on real `dknown`, then `if (obj_is_pname(obj)) goto nameit`); `:999–1012` `nameit` (bare `ONAME`, `The`→`the` downcase for artifacts, strip leading `the `); `obj_is_pname :334–344` (gameover skips the `not_fully_identified` gate, so disclosure takes this arm for every artifact with an oname); `doname_base :1247` (`bp = xname(obj)` arrives bare) + `:1294–1305` (pname → `the ` article) with BUC/erosion/`spe` prefix arms intact and `ispoisoned` FALSE.
**JS:** 1 file (`js/objnam.js`, +35/−6), under the 600/10 caps. C is ~30 lines so the small diff is the whole locus.
**Change:** `js/objnam.js` only — `xname` returns bare `ONAME` (`The` downcase + strip leading `the `) when `obj_is_pname(obj) && has_oname(obj)`; `doname` uses the same bare `ONAME` as `base` for `isPname`, skips the `poisoned ` strip (already bare) and the ` named ONAME` suffix; article/BUC/erosion/`spe` prefix logic untouched (pname → `the ` via the existing `obj_is_pname` gate). No new imports (`ONAME`/`has_oname` already imported; `obj_is_pname` is same-module hoisted, call-time use only). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn inuse_classify --full` → PASS syntax (1 changed js file(s): js/objnam.js) · PASS rule2 · PASS hidden (0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS: scen-genesis-Knight-92068 moved → `really_done` at step 94, was 84; scen-wish-Caveman-92148 moved → `one_characteristic` at step 245, was 241) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 · VERIFY: PASS. Pre-change JS-vs-C screen dumps showed the two old steps byte-identical except the artifact lines; post-change both old steps render byte-identical (`\x1b[24C`/`\x1b[32C` with bare names). Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new. `inuse_classify` body untouched (faithful D-1589 — this row's writer was `objnam`, not `invent.c:70–144`).
**Next:** Knight-92068 now diverges at `really_done` step 94 (`Your body rises from the dead as a human zombie...--More--` vs `Save bones?`) and Caveman-92148 at `one_characteristic` step 245 (later owners, no rows added — the corpus queue will surface them); do not re-pop `inuse_classify` for them.
## 2026-09-08 — D-2120 insight.c attributes_enlightenment transportation arms: Swimming + Breathless/Amphibious + Passes_walls in both builders

**C locus:** `insight.c:1758–1765` (Swimming+Underwater guard `:1758–1759`, Breathless `:1760–1761`, Amphibious else `:1762–1763`, Passes_walls `:1764–1765`); `youprop.h` Swimming/Breathless/Amphibious/Passes_walls/Underwater macros; `attrib.c` from_what/is_innate (form-granted MAGICAL_BREATHING carries no intrinsic FROM bits and no what_gives extrinsic → empty suffix, matching C's bare line).
**JS:** `js/dbridge.js` (+8/−8: four `export` keywords + comment notes), `js/invent.js` (+68/−0: three consts + two ~33-line arms) — 76 insertions, 2 files, under the 600 cap.
**Change:** `js/dbridge.js` exports the four D-1967 predicates (import-the-export, no second macro implementation); `js/invent.js` extends the same-edge static `./const.js` import (SWIMMING/MAGICAL_BREATHING/PASSES_WALLS) and ports the exact C block in C order in BOTH builders (final tense via closures, `^X` via `o()` overlay), predicates via lazy in-body `./dbridge.js` import (imports.mjs CHECK-safe: runtime read only). C's `(Underwater || !u.uinwater)` is identically true (Underwater IS u.uinwater) — ported as Swimming with a citing comment. Wwalking stays deferred (no `walking_on_water` export in `js/`).
**Verify:** `node scripts/verify.mjs --fn attributes_enlightenment` → `PASS syntax 2 changed js file(s)` · `PASS rule2` · `PASS hidden verify attributes_enlightenment: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-death-Wizard-92120 → `really_done@55` was 50 — full Final Attributes menu + Miscellaneous now match; scen-tour-Barbarian-92079 → `mon_adjust_speed@62` was 57 — already byte-identical at 57 pre-change per screen dump, stale row, no regression) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`.
**Named:** Wwalking arm (`:1755–1756`, needs a `walking_on_water` clone); blocked-Lev/Fly + clinger above, Slow_digestion/combat-inc/defense/half-dmg and the rest of the deferred catalogue (unchanged from D-1997/D-2104).
**Next:** Wizard-92120's `really_done@55` (`Your revenant persists as a green slime` vs `Save bones?`) and Barbarian-92079's `mon_adjust_speed@62` move to their natural owner rows; pop next Open row (`invent.c` inuse_classify).
## 2026-09-08 — D-2119 `hack.c` domove: `kickedloc` cleared unconditionally at `domove()` end, not only on stepped moves (queue row `dogmove.c` dog_move, 1 session)

**C locus:** `hack.c:2693–2709` `domove()` (writer behind the `dog_move` symptom): `gk.kickedloc.x = 0, gk.kickedloc.y = 0` (`:2708`) runs UNCONDITIONALLY at `domove()` end — every direction key, even bumps/failed steps — plus `cmd.c:3821–3825` clears after any non-kick `ECMD_TIME` command. `monmove.c:1297–1309` `m_avoid_kicked_loc` itself is faithful on both sides (peaceful/tame + mcansee + `isok` + `next2u`). Recipe kicks west early (`\x04`, hero at (27,9) → kickedloc (26,9)); the following `h` bumps (wall, no step). C clears there; the pet then evaluates (26,9) with `j==0` (`ndist==nidist==1` to the hero-goal) and draws `rn2(1)` first.
**JS:** 1 file (`js/cmd.js`, +7/−7), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No live probes left in `js/` (`git diff js/dogmove.js` empty after revert; throwaway probes kept under `/tmp` only).
**Change:** `js/cmd.js` only, exact C shape — `game.kickedloc = { x: 0, y: 0 }` unconditional in `domove()`'s `finally` beside `game.domove_attempting = 0` (C `:2708` position); removed the `did_step`-gated clear and the three now-redundant fight_empty-arm clears (finally covers every return; no interleave possible). Call-site parity checked: C calls `domove()` per direction key (`cmd.c:3788/3799/5375`) and per run/travel continuation turn (`allmain.c:526`); JS `domove()` runs on the same set (`cmd.js:1703/2049/2445/2460/2621/2654/2670`). No new imports/edges.
**Verify:** `node scripts/verify.mjs --fn dog_move` → PASS syntax (1 changed js file(s): js/cmd.js) · PASS rule2 · PASS hidden (0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-normal-Rogue-92146 moved → `hmon_hitmon_weapon_melee` at step 122, was 48) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new. `m_avoid_kicked_loc`/`m_avoid_soko_push_loc` ports untouched (faithful); `cmd.js:740` rhack `ECMD_TIME` non-kick clear and `dokick.js:1636` set Falcon; `dog_move` body untouched (behaved C-correctly given the stale state — no port of it could move this row).
**Next:** Rogue-92146 now diverges at `hmon_hitmon_weapon_melee` step 122 (later owner, no row added — the corpus queue will surface it); do not re-pop `dog_move` for it. Open rows 11→10 after archive (in the 8–12 band, no refill).
## 2026-09-08 — D-2118 `dungeon.c` In_hell: pray.js `Inhell()` used `dnum===GEHENNOM` instead of the hellish flag (queue row `eat.c` gethungry, 1 session)

**C locus:** `dungeon.c:1941–1945` `In_hell` (`svd.dungeons[lev->dnum].flags.hellish`) via `dungeon.h:140` `Inhell`, read at `pray.c:2265` `dopray` (`if (gp.p_type == 3 && !Inhell)` → shimmer pline + `u.uinvulnerable = TRUE`) and `prayer_done :2280` (clears it). Step 103 asked wizard «Force the gods to be pleased?» (playmode:debug) and the recipe answered `y`, so `p_type` is 3 on both sides; the hero is at `uz {dnum:1, dlevel:1}` Gehennom (`dungeons[1]` hellish=true — step 105 C «Since you are in Gehennom…» + `rnl @ prayer_done(pray.c:2311)` proves it). `eat.c:3163–3277` `gethungry` itself is faithful (early return on `uinvulnerable`) — symptom owner, not the writer.
**JS:** 1 file (`js/pray.js`, +3/−2), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. Throwaway probe `/tmp/probe-inhell.mjs` (prefix replay to the Force-`y`, kept under `/tmp`, never in `js/`) confirmed pre-fix state `uz dnum 1` + `dungeons[1]` hellish=true + shimmer topline.
**Change:** `js/pray.js` only — `Inhell()` now `!!(game.dungeons?.[game.u?.uz?.dnum | 0]?.flags?.hellish)` (the `dungeon.c:1941–1945` idiom, same as the live `do.js` `In_hell` / `display.js` hellish reads and the `dungeon.js` writer); removed the now-unused `GEHENNOM` name from the `const.js` import (edge unchanged, no new imports). All seven `Inhell()` call sites in `pray.js` (`can_pray` ×2, `prayer_done`, `dopray`, ×3 more) are fixed by the one helper. C is 5 lines so the small diff is the whole function.
**Verify:** `node scripts/verify.mjs --fn gethungry` → PASS syntax (1 changed js file(s): js/pray.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify gethungry: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-tour-Healer-92198 moved → `prayer_done` at step 105, was 104) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** same-shape `dnum===GEHENNOM` clones left untouched (no corpus session proves them this iter): `js/minion.js:93`, `js/fountain.js:1041`, `js/makemon.js:1018` (whose own comment at `:767` already names the hellish-flag idiom); `prayer_done` Inhell-branch `ublesscnt`/`rnl`/angrygods gate (`pray.c:2307–2314`) stays deferred — that is the step-105 residual.
**Next:** Healer-92198 now diverges at `prayer_done` step 105 (later owner): JS prints the Gehennom message then returns 0 («angrygods gate deferred») where C draws `rnl @ prayer_done:2311` → `angrygods` → `godvoice`; leave for the corpus queue to surface as its own row. Do not re-pop `gethungry` for it.
## 2026-09-08 — D-2117 `polyself.c` dobreathe: uen drain + getdir + breath dispatch (queue row `cmd.c` getdir, 1 session)

**C locus:** `polyself.c:1420–1447` `dobreathe` (writer behind the `getdir` screen literal): Strangled / `u.uen < 15` refuses; `u.uen -= 15` + `disp.botl = TRUE`; `getdir(0)` (ECMD_CANCEL on quit); `attacktype_fordmg(youmonst.data, AT_BREA, AD_ANY)`; self-directed → `ubreatheu`, else `ubuzz(BZ_U_BREATH(BZ_OFS_AD(adtyp)), damn)`. `cmd.c:3958–4119` `getdir` itself is already faithful (D-1729/D-1806/D-1815) — the session never reached it.
**JS:** 2 files (`js/const.js` +2; `js/polyself.js` 31 changed lines), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the corpus session reaches the changed arm.
**Change:** full C-order port in `js/polyself.js` (energy cost lands before the prompt, so a cancelled breath still costs 15 — dosummon botl idiom); `BZ_U_BREATH` added to `js/const.js` (`hack.h:1484` mirror beside `BZ_OFS_AD`/`BZ_M_BREATH`); `BZ_OFS_AD` + `BZ_U_BREATH` extend the existing `const.js` edge, `ubuzz` + `ubreatheu` imported from `./zap.js` (`imports.mjs --can`: both hoisted functions, cycle-safe; call-time use only, no top-level reads).
**Verify:** `node scripts/verify.mjs --fn getdir` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden (0 PASS, 1 moved past → PROGRESS: Healer-92109 moved → `stairs_description` at step 208, was 115) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed per runner). Preflight `verify.mjs --no-cohort` was green before edits; final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new in `dobreathe` (fully ported; the seed0108 `uen<15` refuse path is unchanged and still covered). Map `turns.md:287` updated (getdir/ubuzz deferred → live D-2117).
**Next:** Healer-92109 now diverges at `stairs_description` step 208 (later owner, no row added — the corpus queue will surface it); do not re-pop `getdir` for it. Refilled Open 7→12 (missum, arti_light_description, self_invis_message, zhitm, mhitm_ad_plys). Observed but not queued: `js js-throw` (Knight-92182 is the parked mattackm/can_carry hang; 92034 undiagnosed) and `burnarmor` Ranger-92212 (D-2116 Next anticipated it, but a burnarmor row is already archived — left for a future queue pass rather than duplicating archive).
## 2026-09-08 — Audit 20e09ee9..12ef27f6 (reviews 1074-1082: 7 ACCEPT, 1076 + 1082 debt, 0 Must-fix) + cadence 44/44.

Every D-log corpus claim re-measured with `--base <sha>~1`, all matched:
makedog 0/1-moved, one_characteristic 1 PASS, dolook 0/1-moved,
next_ident 0/2-moved (2nd mover is later D-2115 progress, not this SHA),
chwepon 0/2-moved, were_change 1 PASS + 1 unchanged (Healer park stands),
prisoner_speaks 1 PASS, zap_dig 0/1-moved, resist 0/2-moved.
Debts (map-pointed, no Must-fix): R-1076 gulpmu BLND else-branch skips
the `uprops[BLINDED].intrinsic` mirror sync (latent, unread);
R-1082 zhitm sleep drops the `mhitm.c:1226–1229` mimic-reveal arm (live
`seemimic`, ~6 lines). Sym re-points pasted per review; `--can`
confirms no new edges (quest/mon, do_name, were/mon all ALREADY;
dig triad same-SCC call-time). Rule #2 clean across scored js/.
Cadence: fortress 44/44, Scr 11405/11405, RNG 792838/792838,
`75+0.49/turn` (R² 0.80). check-hot-docs: 9/9 reviews ok; hot sum
40.1→40.0kB trimmed from own CURRENT lines (HEAD was 39919).
Reverted a `verify`-run stamp-only bump of scoreboard.json (no row
change — not a rescore). Queue 9 Open (band 8–12), no refill, no
Next-cluster change (no Must-fix).
## 2026-09-08 — D-2116 `zap.c` resist: mplayer arms + `zhitm` ZT_SLEEP `resist(how)` gate + explode.js `olet` alev table (queue row `zap.c` resist, 2 sessions)

**C locus:** `zap.c:6100–6158` `resist` — RING_CLASS/0/NOTELL fake-player (`is_mplayer`) early pass; alev table (WAND 12 / TOOL 10 / WEAPON 10 / SCROLL 9 / POTION 6 / RING 5 / default `u.ulevel`); dlev clamp with `is_mplayer → u.ulevel else 1`; `rn2(100+alev-dlev) < mr`. Call protocol: `zhitm` ZT_SLEEP → `sleep_monst(mon, d(nd,25), type==ZT_WAND(ZT_SLEEP) ? WAND_CLASS : '\0')` (`mhitm.c:1231–1232`: `resists_sleep || defended(AD_SLEE) || resist(how)`); `explode.c:534` `resist(mtmp, olet, 0, FALSE)` with variable `olet`.
**JS:** `js/zap.js` resist + zhitm sleep arm (+2 import names, no new edges); `js/explode.js` clone (+6 import names, no new edges); `js/mhitm.js` one-word export.
**Change:** `js/zap.js` — canonical `resist` gains the Conflict early pass + mplayer dlev (needs `is_mplayer`, same-module import extension, no new edge); ZT_SLEEP arm draws `amt=d(nd,25)` first (C arg order) then `if (!resists_sleep_slee(mon) && !await resist(mon, how, 0, NOTELL)) sleep_monst_zap(mon, amt)` with `how = type===ZT_SLEEP ? WAND_CLASS : 0` (`ZT_WAND(x)=(x)`, `zap.c:55`). `js/explode.js` — clone gains the full C alev table + both mplayer arms (sync shape kept: sole caller passes damage 0 + FALSE, so HP/death/shield arms cannot fire).
**Verify:** `node scripts/verify.mjs --fn resist` → PASS syntax (3 files) · PASS rule2 · PASS hidden (0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS: Priest moved → `zapyourself` at step 60 (was 59); Ranger moved → `burnarmor` at step 73 (was 72)) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed). Preflight `verify.mjs --no-cohort` before edits was green.
**Named:** `tell → shieldeff_mon` display stays deferred (pre-existing `void tell`); sleep `Resists_Elem` wielded/worn/artifact scan + `defended(mon, AD_SLEE)` stay deferred (no live `defended` in `js/`); `music.js:182` clone (callers always TOOL_CLASS) and `pray.js:2200` clone (callers always `'\0'`) are exact for their call sites — untouched; `potion.js` `resist_potion` (fixed POTION alev=6) untouched.
**Next:** Priest-92235 now blocked on `zapyourself` (step 60); Ranger-92212 on `burnarmor` (step 73) — later owners for future rows; no new queue rows added (9 Open remain, in the 8–12 band).
## 2026-09-08 — D-2115 `dig.c` zap_dig u.dz arm: bounce-off-stairs + ceiling rock (queue row `dig.c` zap_dig, 1 session)

**C locus:** `dig.c:1583–1612` — the `if (u.dz)` arm: air/waterlevel/`Underwater` (`u.uinwater`, youprop.h:279) skip; `u.dz < 0 || On_stairs` → bounce pline (`stairway_at` ladder/stairs + `ceiling`), `You loosen a rock`, `It falls on your head` (`body_part(HEAD)`), `rnd(hard_helmet(uarmh) ? 2 : 6)` + `losehp(Maybe_Half_Phys, "falling rock", KILLED_BY_AN)`, `mksobj_at(ROCK)` + `xname` + `stackobj` + `newsym`; else (down, off stairs) `watch_dig(0, ux, uy, TRUE)` + `dighole(FALSE, TRUE, NULL)`; unconditional `return`.
**JS:** 1 file (`dig.js`, +48/−6), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the corpus session reaches the changed arm.
**Change:** `js/dig.js` only, exact C order and guards (`On_stairs` called twice as in C): `Is_airlevel`/`Is_waterlevel` (pre-existing `const.js` edge) + `!(u.uinwater | 0)` (C `Underwater`; matches the `zap.c:3311` striking twin's `!(game.u?.uinwater | 0)` in `js/zap.js:6060`); bounce pline via `stairway_at` (pre-existing `mklev.js` edge, `.isladder` as in `dig_check`) + `ceiling` (extends the existing `trap.js` edge); `body_part(HEAD)` (`polyself.js`, `imports.mjs` SAFE hoisted) and `hard_helmet(u.uarmh)` (`do_wear.js`, SAFE hoisted) via call-time dynamic imports (this function's `shk.js` convention); `losehp(maybe_half_phys(dmg), 'falling rock', KILLED_BY_AN)` (`HEAD`/`KILLED_BY_AN` extend the existing `const.js` edge); fatal losehp finishes via `finish_losehp_done` (`end.js`, SAFE hoisted) and returns — C `done(DIED)` is noreturn, same as the striking twin; else `mksobj_at(ROCK, ux, uy, false, false)` + `xname` + `stackobj` (both pre-existing `mkobj.js` edges) + `newsym`, or the down/off-stairs `watch_dig` + `dighole(false, true, null)` (same-module).
**Verify:** `node scripts/verify.mjs --fn zap_dig` → `PASS syntax 1 changed js file(s): js/dig.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify zap_dig: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-death-Wizard-92187: moved → next_ident at step 48, was 20) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** unchanged — swallowed pierce; pitdig conjoined / `adj_pit_checks` / `pit_flow` (map + `zap_dig` header).
**Next:** none for `zap_dig` — the session now diverges at `next_ident` step 48 (later owner, `mkobj.c` row family); do not re-pop `zap_dig` for it.
## 2026-09-08 — D-2114 `quest.c` prisoner_speaks + quest_talk MS_DJINNI arm (queue row `quest.c` prisoner_speaks, 1 session)

**C locus:** `quest.c:451–470` prisoner_speaks + `quest_talk :495–511` MS_DJINNI arm — guard `data == &mons[PM_PRISONER] && mstrategy & STRAT_WAITMASK`; `canseemon` → `pline("%s speaks:", Monnam)`; `SetVoice(mtmp, 0, 80, 0)`; `verbalize("I'm finally free!")`; clear waitmask; `mpeaceful = 1`; `adjalign(3)`; `(void) angry_guards(FALSE)`.
**JS:** 1 file (`quest.js`), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the corpus session reaches the changed arm.
**Change:** `js/quest.js` only — `prisoner_speaks` in exact C order: `mndx` compare for the `data` identity (JS `mtmp.data` is a value, not a pointer — `sounds.js:1132` pattern), `canseemon` (extends the existing `display.js` edge), `Monnam` (`do_name.js`, `imports.mjs` SAFE hoisted), `SetVoice` (`sndprocs.js` !SND_LIB no-op, no cycle — call kept for C order), `adjalign` (extends the existing `attrib.js` edge), `await angry_guards(false)` (`mon.js`, same 90-module SCC, SAFE hoisted; C `FALSE` = not silent). `quest_talk` gains the C-order `switch (msound)` with the `MS_DJINNI` arm plus the C-order leader `return`; local `const MS_DJINNI = 29` (`monflag.h`; `sounds.js`/`monmove.js` local-const convention) and top-level `PM_PRISONER` (`monsters.js`, no cycle).
**Verify:** `node scripts/verify.mjs --fn prisoner_speaks` → `PASS syntax 1 changed js file(s): js/quest.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify prisoner_speaks: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Rogue-92210: PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `MS_NEMESIS` → `nemesis_speaks` (no live export — `quest.js` header + map).
**Next:** none — the blocked session fully PASSes; row archived.
## 2026-09-08 — D-2113 `were.c` were_change unseen-howl arm: `You_hear` + `wake_nearto` after human→beast change (queue row `were.c` were_change, 1 of 2 sessions)

**C locus:** `were.c:18–38` — after `new_were(mon)` + `gw.were_changes++` in the human→beast arm: `if (!Deaf && !canseemon(mon))` → `switch (monsndx(mon->data))` on the POST-change data (`PM_WEREWOLF`→«wolf», `PM_WEREJACKAL`→«jackal», default null) → `if (howler) { Soundeffect(se_canine_howl, 50); You_hear("a %s howling at the moon.", howler); wake_nearto(mon->mx, mon->my, 4 * 4); }`.
**JS:** 1 file (`were.js`, +40/−11), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the Ranger session reaches the changed arm; the Healer residual is diagnosed from recorded C data (see Next).
**Change:** `js/were.js` only — `were_change` is now `async` and awaits `new_were` (C is fully sequential; the armor tail must settle before the `canseemon` read), then runs the howl block in exact C order: local `Deaf()` (youprop.h `HDeaf||EDeaf||uroleplay.deaf`, same formula as the `You_hear` gate in `js/hack.js:149` — no exported `Deaf` exists, matching the repo's local-clone convention) `&& !canseemon(mon)` (pre-existing `display.js` import) → `switch (mon.data?.mndx | 0)` (C `monsndx(mon->data)`; `set_mon_data` sets both `data` and `mnum`, `mons()` builds `mndx`) → `Soundeffect(se_canine_howl, 50)` (live no-op without SND_LIB, `js/sndprocs.js:36`) + `await You_hear('a … howling at the moon.')` (extends the existing `hack.js` edge) + `await wake_nearto(mx, my, 4 * 4)` (`js/mon.js:1267`; `were.js`↔`mon.js` are in the same import SCC — `imports.mjs` confirms call-time-only use is safe, and the `dokick.js:57`→`mon.js` precedent imports the same binding). Only caller `mon.js:840` already awaits (`await were_change(mtmp)`), so the sync→async flip changes no call site. The `uhitm.c:3067` mhitm-gaze `were_change(mdef)` call site stays unwired (different envelope, no corpus coverage — untouched, still named by its absence only there).
**Verify:** `node scripts/verify.mjs --fn were_change` → `PASS syntax 1 changed js file(s): js/were.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify were_change: 1 PASS, 0 moved past, 1 unchanged, 0 worse → PROGRESS` (scen-tour-Ranger-92177: PASS — 30739/30739 RNG, 135/135 screens; scen-intrinsic-Healer-92124: still were_change at step 69) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new in this function (Soundeffect is a live no-op, not an omission). `uhitm.c:3067` call-site wiring untouched (see Fix).
**Next:** do not re-pop `were_change` for Healer-92124 (parked this commit with the falsifier below). Misattribution proof, all C-side measured: the session's recorded step-69 dice are exactly 15 draws (`distfleeck`×3, `m_move rn2(32)`, `mattacku rnd(20)`, `hitmu d(1,2)`, `mhitm_knockback rn2(3)=2`→distance 1 then `rn2(6)=4`→`return FALSE` (no knockback), `mcalcmove`×2, `maybe_generate_rnd_mon`, `regen_hp`, `gethungry`, `moveloop_core`×2) — ZERO dice in `were.c`, so no transform, no howl, no summon ran on either side (positional RNG 2729/2729 matched throughout). The owner string comes from the topline word «jackal» matching the `were.c:28` literal (same for `were_summon :168` in `cMsgOwners`), while the actual row diff is a gold-`# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
 reveal at map (57,0) with identical toplines. Prescribed falsifier for unparking: a JS prefix-state probe of cell (57,0) at step 69 (monster covering gold that C's `m_move` step moved away vs a gold-paint gap in `newsym`/`show_glyph`), then re-queue under the writer that probe names — never under `were_change`.
