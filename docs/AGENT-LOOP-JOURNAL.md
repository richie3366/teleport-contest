# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-05 — D-1891 mkmaze.c makemaz cav-strt/-loca/-goal/-fila/-filb load_special (Caveman quest 5/5)

**C locus:** `dat/Cav-strt.lua` (solidfill " ", mazelevel/noteleport/hardfloor, 76x20 map, whole-map unlit, temple {13,01,40,05} lit=1 irregular filled=1, 6 lit irregular ordinary regions, down stair (02,03), single-cell branch levregion (71,09), locked door (19,06), coaligned shrine (36,02), Shaman Karnov (35,02) + leather armor/club +5 invent, chest (34,02), 8 neanderthals, whole-map non_diggable, 2 fixed pits + 4 random traps, 12 hostile bugbears) + `dat/Cav-loca.lua` (solidfill " ", mazelevel/hardfloor WITHOUT noteleport, 76x20 map, whole-map unlit, lit irregular ordinary {52,06,73,15}, locked door (28,11), up (04,03) + down (73,10) stairs, whole-map non_diggable, 15 objects, 6 traps, 13 placed + 4 random bugbears + h + H + 3 placed + 4 random hill giants + H, all hostile) + `dat/Cav-goal.lua` (solidfill " " ONLY — no mines init — mazelevel only, 76x20 oval-cave map, whole-map lit, random up stair, whole-map non_diggable, blessed +0 "The Sceptre of Might" mace (23,10), 14 objects, asleep Chromatic Dragon on the prize cell + 3 placed shriekers) + `dat/Cav-fila.lua`/`-filb.lua` (solidfill + mines fg="." bg=" " no-lit-key smoothed/joined/walled, noflip; fila 7 objects/4 traps/5 bugbears + h + hill giant; filb 12 objects/4 traps/4 bugbears + 2x h + 2x hill giants, all hostile) via `mkmaze.c` `makemaz` → `load_special` (wallification → flip_level_rnd → fixup_special epilogue).
**JS:** `js/mklev.js` (5 loaders + dispatch + docstring); `docs/c-js-map/data.md` quest-loader row; `docs/LOOP-QUEUE.md` head row; `docs/CURRENT.md` Next cluster.
**Change:** `load_cav_strt` (STONE solidfill + triple flags, byte-identical map, whole-map unlit, temple via Pri-strt flood idiom with needfill FILL_NORMAL — filled=1 per C `sp_lev.c` lspo_region `:5600`, not FILL_LVFLAGS — 6 ordinary irregulars via the same flood path with needfill 0 for the missing `filled` key, stair, coaligned shrine via `sp_amask_to_amask(AM_SPLEV_CO)` + priestini + has_temple (Kni idiom), Shaman Karnov + CUSTOM_INVENT armor/club +5 via `splev_discard_default_minvent` + `l_create_object`/`mpickobj` (Ran Orion idiom), chest via `mksobj_at(CHEST)`, 8 default-peaceful neanderthals, whole-map non_diggable, 2 fixed PIT + 4 random traps, 12 hostile placed bugbears, wallification → `flip_level_rnd(3, false)` → pre-flip single-cell LR_BRANCH `place_lregion` → `fixup_special`); `load_cav_loca` (mazelevel/hardfloor only, exact 76x20 map, whole-map unlit, flood irregular ordinary, locked door, fixed up/down stairs, whole-map non_diggable, 15 objects, 6 traps, lua-order hostile bugbear/hill-giant/h/H stock, wallification → flip → fixup); `load_cav_goal` (solidfill-only init, mazelevel only, exact oval map, whole-map lit, random up stair, whole-map non_diggable, `l_create_object` mace with buc + spe + artifact name (Mon-goal lenses idiom), 14 objects, asleep Chromatic Dragon + 3 shriekers, wallification → flip → fixup); `load_cav_fila`/`-filb` (Ran-fila mines+noflip idiom: stairs → objects → traps → hostile stock, wallification → skip flip → fixup); 5 dispatch arms + makemaz docstring rows. No new module edges.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** humidity-aware `get_location` for water-likers (all five; carried on the map row); `ensure_way_out` (all five; carried on the map row); `spo_end_moninvent` m_dowear (strt/goal; carried on the map row); flip_level lregion coord update (strt pre-flip shortcut, same as Bar/Mon-strt).
**Next:** next Open queue row in order (`botl.c` do_statusline1, 1 corpus block).
## 2026-09-05 — D-1890 mkmaze.c makemaz mon-strt/-loca/-goal/-fila/-filb load_special (Monk quest 5/5)

**C locus:** `dat/Mon-strt.lua` (solidfill " ", mazelevel/noteleport/hardfloor, Pri-strt-identical 20×76 map, whole-map lit, temple {24,06,33,13} lit=1 with NO `filled` key, forest replace strips chance 10, floodfill(05,04) BEFORE des.terrain, down stair (52,09), 18 doors, noalign altar (28,09), Grand Master (28,10) + robe +6 invent, 8 abbots, rect non_diggable, 2 dart + 4 random traps, 8 earth elementals + 4 xorns, tin×2 spinach (29,9) + food ration×4 (46,4), branch levregion cell) + `dat/Mon-loca.lua` (solidfill " ", mazelevel only, 76×21 winding-cave map, whole-map lit, random up/down stairs, whole-map non_diggable, 15 objects, negate-filter '.' rndcoord(0) blessed spinach tin×2 + burn Elbereth engraving, 6 traps, 14 earth elementals + 9 xorns) + `dat/Mon-goal.lua` (mazelevel only, solidfill init COMMENTED OUT so mines init only, Pri-goal-identical 11×26 lava map, place {{14,04},{13,07}} rn2 pick, unlit rect, up stair (20,05), blessed +0 "The Eyes of the Overworld" lenses, 14 objects, 4 fire + 2 random traps, Master Kaen + noalign altar on the pick, 9 earth elementals + 9 xorns) + `dat/Mon-fila.lua`/`-filb.lua` (six ordinary rooms + random_corridors; hostile E/X class stock; fila rooms 3–4 carry xorn/earth-elemental pairs) via `mkmaze.c` `makemaz` → `load_special` (wallification → flip_level_rnd → fixup_special epilogue).
**JS:** `js/mklev.js` (+583/−1: 5 loaders + dispatch + docstring); `docs/c-js-map/data.md` quest-loader row; `docs/LOOP-QUEUE.md` head row; `docs/CURRENT.md` Next cluster.
**Change:** `load_mon_strt` (Pri-strt skeleton: STONE solidfill + triple flags, byte-identical map, whole-map lit, temple with `needfill = 0` per C `sp_lev.c` lspo_region `:5600` default — Pri filled=2 takes FILL_LVFLAGS — same add_room + topologize, pre-map forest replaces, lua-order floodfill-then-terrain, stair, 18 doors, AM_NONE altar, Grand Master + CUSTOM_INVENT robe +6 via `splev_discard_default_minvent` + `mksobj_at`/`mpickobj`, 8 abbots, rect non_diggable, 2 rndcoord dart + 4 random traps, lua-order 8+4 siege, `l_create_object` tin (`quan: 2`, spinach montype, random BUC) + food ration (`quan: 4`) per the Tou-goal card idiom with map-relative x/y, wallification → `flip_level_rnd(3, false)` → pre-flip single-cell LR_BRANCH `place_lregion` → `fixup_special`); `load_mon_loca` (STONE solidfill, mazelevel only, exact 21×76 map, whole-map lit, `splev_create_stair` up/down, whole-map non_diggable, 15 objects, `selection_all_room_floors` + `selection_rndcoord(..., false)` tinloc with map-relative x/y + absolute `make_engr_at(..., 'Elbereth', null, 0, BURN)` per the soko idiom, 6 traps, 14+9 monsters, wallification → flip → fixup); `load_mon_goal` (mines init ONLY — no solidfill per the commented-out lua line, Pri-goal lava mines + identical map, rn2 placeidx, unlit rect with lava-stays-lit, up stair, `l_create_object` lenses with buc + spe + artifact name and NO oerodeproof — no eroded key, unlike the Pri Mitre — 14 objects, 4 placed fire + 2 random traps, Master Kaen + AM_NONE altar on the pick, 9+9 monsters, wallification → flip → fixup); `load_mon_fila`/`-filb` (six ordinary `splev_des_room` in lua order with `splev_room_monster(r, 'E'/'X', 0)` hostiles, stairs → objects → traps → monsters, `makecorridors`, wallification → flip → fixup); 5 dispatch arms + makemaz docstring rows. No new module edges.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** humidity-aware `get_location` for water-likers (all five; carried on the map row); `ensure_way_out` (all five; carried on the map row); `spo_end_moninvent` m_dowear (strt/goal; carried on the map row); flip_level lregion coord update (strt pre-flip shortcut, same as Pri-strt).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `cav-strt`/`-loca`/`-goal`/`-fila`/`-filb` Caveman quest 0/5).
## 2026-09-05 — D-1889 mkmaze.c makemaz Ran-strt/loca/goal/fila/filb load_special (Ranger quest 5/5)

**C locus:** `dat/Ran-strt.lua` (solidfill ".", mazelevel/noteleport/hardfloor/arboreal, mines fg=bg="." smoothed/joined/lit unwalled, replace_terrain "."→"T" chance 5 over {00,00,76,19} BEFORE des.map, 41×21 halign-left/valign-center map, lit rect (00,00,40,20), down stair (10,10), branch levregion {51,2,77,18} region_islev=1, Orion at (20,10) with leather armor +4 / yumi +4 / ya +4 ×50 invent + chest, 8 hunter guards, rect non_diggable, arrow/arrow/pit fixed + spiked-pit/bear/bear random traps, asleep minotaur (33,09) + 18 placed forest centaurs + 6 random plains centaurs + 2 scorpions) + `dat/Ran-loca.lua` (solidfill " ", mazelevel/hardfloor, 55×20 centered map, lit rect (00,00,54,19), up (25,05) + down (27,18) stairs, rect non_diggable, 8 objects, spiked-pit×2/teleport×2/arrow×2 random traps, asleep wumpus (27,18) + 4 bats + 4 forest + 8 mountain centaurs + 4 scorpions + 2 s-class) + `dat/Ran-goal.lua` (solidfill " ", mazelevel only, 76×20 centered map with single-`\` throne cell, whole-map lit, up stair (19,10), whole-map non_diggable, blessed +0 "The Longbow of Diana" bow + chest + 8 fixed RANDOM_CLASS + 5 random objects, 6 random traps, 3 locked + 11 closed doors, Scorpius (37,10) + 6 placed forest + 6 placed mountain centaurs + 2+2 random + 2 C-class + 6 placed + 2 random scorpions + s-class, trailing `des.wallify()`) + `dat/Ran-fila.lua`/`-filb.lua` (mazelevel/noflip, mines fg="." bg="T"/" " with no lit key, up/down stairs, 7/11 objects, 4 traps, 2+3 / 4 mountain+forest centaurs + C + scorpion(s)) via `mkmaze.c` `makemaz` → `load_special` (wallification → flip_level_rnd → fixup_special epilogue).
**JS:** `js/mklev.js` (+541/−2: 5 loaders + dispatch + docstring); `docs/c-js-map/data.md` quest-loader row; `docs/LOOP-QUEUE.md` head row; `docs/CURRENT.md` Next cluster.
**Change:** `load_ran_strt` (ROOM solidfill + triple flags + arboreal, ROOM/ROOM mines lit/smoothed/joined/unwalled, `lspo_replace_terrain_region(0,0,76,19,ROOM,TREE,5)` BEFORE the map per lua order — mx/my are 1,0 both before and after the left/center map, but only the pre-map all-ROOM field draws the full C rn2(100) grid — left/center aligned map via `splev_map_aligned_start` with the centered-loader lit-clear, lit rect, down stair, Orion + CUSTOM_INVENT via `splev_discard_default_minvent` + `l_create_object`/`mpickobj` per the Sato/MoT idiom with string ids (`ya` `quan: 50`), chest, 8 hunters, rect non_diggable, fixed arrow/arrow/pit via maketrap + mktrap_seen_victim + typed random traps, lua-order siege, wallification → `flip_level_rnd(3, false)` → absolute islev rect `place_lregion(51,2,77,18,LR_BRANCH)` → `fixup_special` per the Val-strt shortcut); `load_ran_loca` (STONE solidfill, centered map + lit-clear, lit rect, stairs, rect non_diggable, 8 objects, paired typed traps, asleep wumpus + lua-order monsters); `load_ran_goal` (centered map + lit-clear, whole-map lit, up stair, whole-map non_diggable, `l_create_object` bow with x/y + buc + spe + artifact name per the Tou-goal card idiom + chest + 8 fixed `mkobj_at(RANDOM_CLASS)` + 5 random objects, 6 traps, 14 doors, lua-order monsters, `wallify_map` on splev extents before the epilogue); `load_ran_fila`/`-filb` (Tou-fila mines shape with omitted lit key, TREE/STONE bg, stairs → objects → traps → hostile monsters per lua order, noflip skips flip); 5 dispatch arms + makemaz docstring rows. No new module edges.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** humidity-aware `get_location` for water-likers (all five; carried on the map row); `ensure_way_out` (all five; carried on the map row).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `mon-strt`/`-loca`/`-goal`/`-fila`/`-filb` Monk quest 0/5).
## 2026-09-05 — D-1888 mkmaze.c makemaz Tou-strt load_special (Tourist quest 5/5)

**C locus:** `dat/Tou-strt.lua` (solidfill " ", mazelevel/noteleport/hardfloor, 76×20 map with a literal-backslash door row, whole-map lit + morgue (14,01,20,03) lit=0 filled=1 + 7 unlit + 1 lit rects, down stair (66,03), branch levregion cell (68,14), whole-map non_diggable, 11 locked + 4 closed + 3 open doors, 12 giant spiders + s/s + 8 forest centaurs + C, Twoflower at (64,03) with walking shoes +3 / hawaiian shirt +3 invent + chest, 11 guides + 2 watchmen placed, giant eel + 2 piranhas + 2 krakens placed, 9 random traps) via `mkmaze.c` `makemaz` → `load_special` (wallification → flip_level_rnd → fixup_special epilogue, same flags shape as Sam-strt).
**JS:** `js/mklev.js` (+191/−1: loader + dispatch + 2 consts + docstring); `docs/c-js-map/data.md` quest-loader row (Tourist 5/5); `docs/LOOP-QUEUE.md` head row; `docs/CURRENT.md` Next cluster.
**Change:** `load_tou_strt` in lua order (centered map, whole-map lit, morgue as FILL_NORMAL rect room with topologize + add_doors_to_room per the Tou-loca helper, explicit-lit rects as `light_region`, down stair, walls/bars non_diggable, 18 doors with D_ISOPEN for the 3 opens, lua-order siege monsters with single-letter class ids, Twoflower + CUSTOM_INVENT via `splev_discard_default_minvent` + `l_create_object`/`mpickobj` per the Sato/MoT idiom, chest, placed guides/watchmen/river monsters, 9 `splev_create_trap`, wallification → `flip_level_rnd(3, false)` → single-cell LR_BRANCH `place_lregion` at pre-flip offsets → `fixup_special` per the Kni/Sam-strt shortcut); dispatch arm + `LOW_BOOTS`/`HAWAIIAN_SHIRT` otyp consts + makemaz docstring row. No new module edges.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** humidity-aware `get_location` for water-likers (carried on the map row); `ensure_way_out` (carried on the map row).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `ran-strt`/`-loca`/`-goal`/`-fila`/`-filb` Ranger quest 0/5).
## 2026-09-05 — D-1887 mkmaze.c makemaz Tou-loca/goal/fila/filb load_special (Tourist quest 4/5)

**C locus:** `dat/Tou-loca.lua` (solidfill " ", mazelevel/hardfloor, 76×20 map with `\\`-escaped `S.\.S`, morgue lit=0 + 2 shops + 3 barracks + zoo + temple all filled=1, 19 ordinary rects with no lit key, up stair (10,04) + down stair (73,05), 31 closed + 4 locked doors, 14 objects + blank-paper pair at (71,12), 9 traps on '.' minus shops, 16 spiders + s/s) + `dat/Tou-goal.lua` (mazelevel only, town map with `F` features + `}}` moat, Inn/police/town light rects + 3 barracks + morgue + 2 shops, up stair (70,08), 9 locked + 8 closed + 7 random + 3 open doors, Platinum Yendorian Express Card + 14 objects, 6 traps on '.' minus shops, hostile Master of Thieves + Kops + prisoners + watchman, trailing `des.wallify()`) + `dat/Tou-fila.lua`/`-filb.lua` (mazelevel/noflip, mines fg="." bg=" " with no lit key, 7/11 objects, hostile soldiers/captains/H/C/s) via `mkmaze.c` `makemaz` → `load_special`. Ordinary rects take the C `lspo_region` room_not_needed path (`sp_lev.c:5641–5656`: rtype OROOM, non-irregular, no arrival room → `light_region` with `litstate_rnd(-1)`); `selection.area` coords pass through `get_location_coord` ANY_LOC (`nhlsel.c:559–588`), i.e. map-relative + xstart/ystart like the knox treasury; `rndcoord(1)` is x-outer/y-inner `rn2(idx)` (`selvar.c:284–318`), matching `selection_rndcoord`; `des.trap(coord)` is `create_trap` with packed coord → `mktrap` random kind (`sp_lev.c:1812–1842`).
**JS:** `js/mklev.js` (+536/−0: 4 loaders + dispatch + docstring); `docs/c-js-map/data.md` quest-loader row; `docs/LOOP-QUEUE.md` head row narrowed to `tou-strt`; `docs/CURRENT.md` Next cluster narrowed to `tou-strt`.
**Change:** `load_tou_loca` (whole-map lit, 8 FILL_NORMAL rect rooms with topologize + add_doors_to_room in lua order, 19 ordinary rects as `light_region(..., litstate_rnd(-1))` + 1 unlit + 1 lit area in lua order, stairs, whole-map non_diggable, 35 doors, 14 objects + 2× `mksobj_at(SCR_BLANK_PAPER)` at (71,12), 9 rndcoord traps with traptype_rnd + hole→ROCKTRAP + mktrap_seen_victim, lua-order monsters); `load_tou_goal` (same room pattern for 3 barracks + morgue + 2 shops, 27 doors with `rnddoor()` for the 7 randoms, `l_create_object` credit card with x/y + buc + spe + artifact name, 6 rndcoord traps minus the shop band, MoT/Kop/incubi/succubi/prisoner/watchman order, `wallify_map` on splev extents before the wallification → flip → fixup epilogue); `load_tou_fila`/`-filb` (Bar-filb mines shape with omitted lit key, stairs → objects → traps → hostile monsters per lua order); 4 dispatch arms. No new module edges.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** humidity-aware `get_location` for water-likers (all four; carried on the map row); `ensure_way_out` (all four; carried on the map row); `Tou-strt` itself (next Open row).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `tou-strt` Tourist quest start).
## 2026-09-05 — D-1886 pager.c do_screen_description object-glyph arm (queued readobjnam_postparse1 was a literal-match misattribution)

**C locus:** `pager.c` `do_screen_description` object loop `:1355–1400` (shown `%` matches FOOD_CLASS showsym → `an("piece of food")`, `defsym.h:474`; `need_to_look`) + didlook `:1607–1640` (`lookat` → `look_at_object` `:380–399` → `distant_name` → "a food ration"; `firstmatch = look_buf`, found back to 1) + `do_look` `:1923–1950` (`putmixed`, then `checkfile("a food ration")` → `*food*` data key → `--More--`) + `look_at_object` buried/embedded suffixes `:388–399`. The queued owner is falsified: `':'` is the getpos pick char, the picked cell is map (46,18) (screen 45,19, `%` food glyph, FOOD_RATION live in `objects_at`), and `readobjnam` callers are wish-only (`zap.c`/`files.c`/`nhlobj.c`) with no wish on this path and RNG fully matched — the proxy matched the `"food ration"` wish-parse guard literal, not the printer (same misattribution shape as D-1875 `glibr`). Review 850 (ACCEPT, no actionables) needs no stamp.
**JS:** `js/pager.js` (+43/−10: object arm + suffixes + docstring updates); `hidden-corpus/scoreboard.json` (working row → PASS).
**Change:** glyph-driven object arm in `describe_looked` (oclass via `glyph_to_obj` + `game.objects` oc_class + `def_oc_syms` explain; all classes 1–17 except VENOM; ROCK → "boulder"; out `<ch> an(explain) (lookat)`, `first` = look_buf, found 1 — verified chain at (46,18): glyph object, otyp 293 FOOD_RATION, oclass 7, `look_at_object` → "a food ration"); buried/stone/wall/door/pool/lava suffix chain in `look_at_object`, gated on real objects (C deallocs the fake so fakes take no suffix). Same-module import-name additions only (`objects.js`/`display.js`/`hack.js`/`const.js` edges already exist).
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** statue glyphs keep the old fallthrough (their C line needs the monster-class prefix from the unexported mlet explain table, plus the monster-letter prefix char); venom glyphs likewise (C lists the shared `'.'`-sym cmap row, not "a splash of venom"); tree `dangling/stuck` suffix (needs `is_treefruit`); `doname_with_price`/`doname_vague_quan` stand-in (pre-existing); full cmap/symbol table scan (pre-existing); `readobjnam_postparse1` itself still unported (wish-only; no live corpus block — the queued premise was the misattribution above).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `tou-strt` Tourist quest).
## 2026-09-05 — D-1885 mkmaze.c makemaz Hea-strt/loca/goal/fila/filb load_special (Healer quest 5/5)

**C locus:** `dat/Hea-strt.lua` (solidfill " ", mazelevel/noteleport/hardfloor, 76×20 map, `replace_terrain` P→'.' chance 10, branch levregion point {04,12}, Hippocrates + silver dagger spe 5, 8 attendants) + `dat/Hea-loca.lua` (mines fg="." bg="P" smoothed/joined/lit, temple filled=1 + chaos shrine, stairs (04,04)/(20,06)) + `dat/Hea-goal.lua` (Staff of Aesculapius + wand of lightning + hostile Cyclops at (20,06)) + `dat/Hea-fila.lua`/`-filb.lua` (noflip fillers) via `mkmaze.c` `makemaz` → `load_special`; monster class `';'` resolves through `def_char_to_monclass` (`sp_lev.c:1936,3686`) over the display-symbol table (`defsym.h:362` MONSYM `';'` EEL `S_EEL`).
**JS:** `js/mklev.js` (+504/−1: 5 loaders + dispatch + class-map line + const + docstring); `docs/c-js-map/data.md` quest-loader row.
**Change:** `load_hea_strt` (Sam-strt shape: STONE solidfill, triple flags, centered 76×20 map with `\\`-escaped `S.\.S`, map-relative `lspo_replace_terrain_region(1,1,74,18,POOL,ROOM,10)` immediately after the map per lua order, whole-map lit, down stair, neutral non-shrine altar, 12 doors, Hippocrates Norn-style invent + chest + attendants, whole-map non_diggable, 6 traps, lua-order siege, wallification → flip → branch-point `place_lregion` → fixup); `load_hea_loca` (Kni-loca temple/shrine/priestini shape with chaos amask, Val-loca lit/rect-nondiggable shape, 15 objects, 6 traps, lua-order monsters); `load_hea_goal` (Val-goal shape: POOL solidfill, Orb-of-Fate-style `l_create_object` Staff + wand at (20,06), hostile Cyclops); `load_hea_fila`/`-filb` (Kni-fila/Sam-fila noflip shape, POOL solidfill, lua counts); 5 dispatch arms; `';': 'S_EEL'` in the class map; `WAN_LIGHTNING` const. No new module edges.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** humidity-aware `get_location` for water-likers; `spo_end_moninvent` m_dowear (loca priest); `ensure_way_out` (all five; carried on the map row).
**Next:** next Open queue row in order (`objnam.c` readobjnam_postparse1).
## 2026-09-05 — D-1884 iactions.c itemactions W already-wearing row (armor_simple_name + armcat_to_wornmask)

**C locus:** `iactions.c` `itemactions` W arm `:631–647` (`armcat_to_wornmask(objects[otyp].oc_armcat)` → `wearmask_to_obj(Wmask)`; empty slot → `Wear this armor`, else `[already wearing %s]` over `an(armor_simple_name(o))`) + `objnam.c` `armor_simple_name` `:5434–5468` (7-way `oc_armcat` switch; default `simpleonames` + `impossible`) + `worn.c` `armcat_to_wornmask` `:249–278`. Per-category: suit `:5470–5489`, cloak `:5491–5509`, helm `:5512–5528` (`hard_helmet`), gloves `:5531–5547`, boots `:5550–5566`, shield `:5569–5596` (`#if 0` heavy/light cut), shirt `:5599–5603`.
**JS:** `js/worn.js` (+32/−0); `js/do_wear.js` (+86/−2); `js/iactions.js` (+20/−9, W arm + header/doc named-omission cleanup); `docs/c-js-map/turns.md` iactions section.
**Change:** exported `armcat_to_wornmask` from `js/worn.js` (C `worn.c` home, same 7-arm switch over the module-local `ARM_*`); added file-local `cloak_simple_name` / `boots_simple_name` / `shield_simple_name` plus exported `armor_simple_name` in `js/do_wear.js` next to `suit_simple_name` — suit reuses `suit_simple_name`, gloves reuse the canonical `objnam.js` `gloves_simple_name` (no clone #2), helm keys off module-local `hard_helmet`, shirt inline; rewrote the `js/iactions.js` W arm in C order via `await import` of `worn.js` / `do_wear.js` (edges already exist dynamically; `an` joins the existing static `objnam.js` import; `objectDescrs`/`simpleonames` join existing `do_wear.js` edges). Boots descr uses `objectDescrs[ocl?.oc_descr_idx ?? otyp]`, the `objnam.js:712` idiom.
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** `suit_simple_name` dragon mail/scales arms (pre-existing deferral, untouched); dungeon.c `surface` terrain nouns and `cantwield` `'w'` skip (carried on the map row, untouched).
**Next:** next Open queue row in order (`mkmaze.c` makemaz `wiz-goal`).
## 2026-09-05 — Audit D-1876…D-1883 against pinned C (reviews 846–853) + cadence score

**Scope:** 8 js-touching SHAs since review 845 (`305998a7`…`c9e049a8`), one SHA at a time, file written per SHA. Every D-log Verify bullet re-measured via `hidden-proxy verify <fn> --base HASH~1` — all 8 confirmed (5 exact-match, climb_pit/do_look/erode_armor PASS-on-tree; no WORSE, no vacuous checks). C spot-checks: Passes_walls macro (`youprop.h:286`, dead `u.Passes_walls` disjunct harmless), `resp` build proves `q` bells (`wintty.c:1531–1537` + `:287–292`), `doing_what_is` pointer-compare exact both sides, `date_via_env` FALSE + pinned date, `type_is_pname`/`Role_if` macros match clones, `erode_armor_mm` fully renamed (sym NOT FOUND), `--can` ALREADY ×4.
**Verdicts:** 8 ACCEPT, 0 Must-fix — no LOOP-QUEUE prepend, Next cluster untouched (`iactions.c` itemactions).
**Score:** full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `45+0.36/turn` (R² 0.86). Hidden proxy 251/265 (94.7 %, was 246) — the 8 audited ports account for the delta; new top singles: `readobjnam_postparse1` (queued), `do_statusline1`, `hitmsg`.
**Baseline correction (measured, committed in scoreboard.json):** `random-seed0360-wizard-world-tour-e115a25b` was recorded 824 `x_monnam` but replays 859 `show_conduct` deterministically (5+ replays, identical rng 120587/120587 scr 859/861) on BOTH `db5d8f30` and HEAD trees with zero `js/` edits — the 824 row is stale (moved by D-1880/81/82 or recorded under different conditions), not a D-1883 effect (D-1883 leaves this session's draws byte-identical). Implication: the `show_conduct` Parked row's "at HEAD 824" premise is stale too — the session sits at the ORIGINAL 859 premise again. Next port iter should re-baseline (`hidden-proxy verify show_conduct`) before trusting the park note; queue row left untouched by this audit (all verdicts ACCEPT).
## 2026-09-05 — D-1883 uhitm.c erode_armor export + mhitm_ad_rust mhitu arm (erode_armor corpus owner)

**C locus:** `uhitm.c` `erode_armor` `:126–185` (`rn2(5)` slot loop; case 1 body-armor always breaks) + `mhitm_ad_rust` `:2281–2336`, mhitu arm `:2299–2316` (`hitmsg`; `mcan` return; `completelyrusts` (`mondata.h:227`, iron golem) → `You("rust!")` + `rehumanize()`; else `erode_armor(&youmonst, ERODE_RUST)`; base `hitmu` `d()` kept) + `worn.c` `which_armor` `:1006–1036` (youmonst → `u.uarm*` slot table) + `mhitu.c` `hitmu` (`mhitm_adtyping` then `mhitm_knockback`, so erosion draws precede knockback draws). Queue owner `erode_armor` is the called function; the missing writer arm is `mhitm_ad_rust` mhitu.
**JS:** `js/mhitm.js` (export + caller); `js/worn.js` (hero arm); `js/mhitu.js` (arm + case + consts); `docs/c-js-map/turns.md` uhitm/mhitm section.
**Change:** exported `erode_armor` from `js/mhitm.js` (same body, C cite `:126–185`; `passivemm` caller updated); ported the `which_armor` youmonst slot table in `js/worn.js` (`u.uarm`/`uarmc`/`uarmh`/`uarms`/`uarmg`/`uarmf`/`uarmu`, `impossible` on bad flag; same worn→display edge); added `js/mhitu.js` `mhitm_ad_rust_u` (`hitmsg`, `mcan`, iron-golem `You rust!` + `rehumanize`, else `erode_armor(game.youmonst, ERODE_RUST)`) + `case AD_RUST` (local const 24, monattk.h) in `mhitm_adtyping_u`. No new module edges (`imports.mjs --can`: mhitu→mhitm already static).
**Verify:** - Preflight `node scripts/verify.mjs --no-cohort` before any edit → VERIFY: PASS (green + strict clean).
**Named:** `mhitm_ad_corr` / `mhitm_ad_dcay` mhitu arms (`hitmsg` + `erode_armor` CORRODE/ROT + `completelyrots` rehumanize; no corpus block); `mhitm_ad_rust` uhitm/mhitm arms (hero-as-attacker, mon-vs-mon); `passive_obj` AD_RUST/CORR `erode_obj` on the hitting weapon (deferred, untouched); `passive()` AD_ACID `!rn2(30)` `erode_armor(youmonst)` wiring (deferred, untouched); `passiveum` AD_ACID `erode_armor(mtmp)` (deferred, untouched).
**Next:** next Open queue row in order (`iactions.c` itemactions).
## 2026-09-05 — D-1882 objnam.c xname ROCK_CLASS STATUE historic + unique/pname article (dolook corpus owner)

**C locus:** `objnam.c` `xname_flags` ROCK_CLASS STATUE `:802–814` — `Snprintf(buf, "%s%s of %s%s", (Role_if(PM_ARCHEOLOGIST) && (obj->spe & CORPSTAT_HISTORIC)) ? "historic " : "", actualn, type_is_pname(&mons[omndx]) ? "" : the_unique_pm(&mons[omndx]) ? "the " : just_an(anbuf, statue_pmname), statue_pmname)` over `obj_pmname(obj)` (`do_name.c:1321`, gender-aware via `spe & CORPSTAT_GENDER`, aligned-cleric remap). Queue owner `invent.c` `dolook :4319–4331` is the symptom frame (`look_here(0, LOOKHERE_NOFLAGS)` → `doname_with_price` → `xname`); the writer is this arm. Level source `dat/oracle.lua:9–16` (`montype "C" historic=true`) via `sp_lev.c:3706–3713` `lflags`, already ported in `js/mklev.js` `splev_room_statue_montype`/`lspo_object_apply_montype`, so `spe` was correct and only the display dropped it.
**JS:** `js/objnam.js` (+13/−3); `docs/c-js-map/turns.md` STATUE row.
**Change:** ported the C Snprintf envelope in `pretty_base` with C citations — `obj_pmname_corpse` for the pm name, `type_is_pname_objnam ? "" : the_unique_pm ? "the " : just_an(pmname)` for the inner article, `Role_if(PM_ARCHEOLOGIST) && (spe & CORPSTAT_HISTORIC)` for the `historic ` prefix; `NON_PM`/non-`ismnum` falls back to bare `statue` (C `Strcpy(buf, actualn)`). Same-module imports only (`PM_ARCHEOLOGIST` alongside existing `monsters_data.js` names; `CORPSTAT_HISTORIC` alongside existing `const.js` names — no new module edge). Outer `doname` `a `/`an ` redo is untouched and now yields `a historic statue …` via the existing `just_an(rest||base)` path (matches C `:1686–1692`).
**Verify:** - `node scripts/verify.mjs --fn dolook` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/objnam.js`; `PASS rule2`; `PASS hidden verify dolook: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS (tour-Archeologist-70010-d3-6-10-11-12: PASS)`; `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; skip full (heuristic: objnam.js not in shared set).
**Named:** `BOULDER next_boulder` arm already live (D-1294, untouched); STATUE `iflags.wizmgender` ` (%s)` suffix (`objnam.c:1550–1558`, wizard-only, no corpus block); FIGURINE ` of <pm>` xname arm (named in the same WEAPON/TOOL comment, no corpus block); `readobjnam` `historic ` wish-parse → `spe |= CORPSTAT_HISTORIC` (`:5164`, wish path, no corpus block).
**Next:** next Open queue row in order (`uhitm.c` erode_armor).
## 2026-09-05 — D-1881 mdlib.c version_id_string + version.c doversion ('V' versionshort)

**C locus:** `mdlib.c` `version_id_string` `:316–344` (`%s NetHack%s Version %s%s - last %s %s.` over PORT_ID, subbuf, `mdlib_version_string` `:300–308`, statusbuf, build/revision, build_date) + `version.c` `doversion` `:156–165` (`iflags.menu_requested → doextversion`, else `pline(getversionstring)`) + `cmd.c:1926` `'V' → doversion` (IFBURIED|GENERALCMD|CMD_M_PREFIX). Contest build: PORT_ID `MacOS` (__APPLE__, `global.h`), no PORT_SUB_ID, NH_STATUS_RELEASED (`patchlevel.h`) so empty subbuf/statusbuf, `001-deterministic-runtime.patch` pins build_date to `May  2 2026 12:00:00`; no RUNTIME_PORT_ID / NETHACK_GIT_* strings, so `getversionstring` (`version.c:35–80`) returns the version_id unchanged.
**JS:** `js/version.js` (`mdlib_version_string`, `version_id_string`, `version_string`, `getversionstring`), `js/pager.js` (`doversion`, `doextversion` first line), `js/getline.js` (`versionshort` runner), `js/cmd.js` (`'V'` arm + repeat entries).
**Change:** `js/version.js` ports `mdlib_version_string`, `version_id_string`, `version_string`, `getversionstring` (pure, no imports; existing VERSION exports kept for `const.js`); `js/pager.js` adds `doversion` (menu_requested → `doextversion`, else `pline(getversionstring())`, ECMD_OK) and `doextversion` now takes its first line from `getversionstring()` (byte-identical); `js/getline.js` adds the non-autocomplete `versionshort` EXT_CMDS runner → `doversion`; `js/cmd.js` binds `'V'` (repeat_command/txt + direct rhack arm, move 0). The `m`-prefix gate needs no edit: `cmdbind_get('V')` already returns the CMD_M_PREFIX `versionshort` row.
**Verify:** - `node scripts/verify.mjs --fn version_id_string` → VERIFY: PASS — syntax 4 files; rule2; hidden `1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (`random-seed0900-tourist-explore-actions-614da9aa: PASS`); green 2/2; strict both sessions; cohort 7/7.
**Named:** `bannerc_string` (title-banner path, no corpus block); `status_version` / VI flags; `early_version_info` / `dump_version_info` pastebuf/raw paths; `get_feature_notice_ver` save-compat checks; RUNTIME_PORT_ID / git-sha/branch/prefix suffix arms (data-unset, structure ported).
**Next:** next Open queue row in order.
