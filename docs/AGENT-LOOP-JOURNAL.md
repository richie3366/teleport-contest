# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-14 — D-2282 `shk.c` credit_report: shop credit/debit report + scatter shop arms (queue row `shk.c` credit_report, D-2274 residual)

**C locus:** `shk.c:628–661` (`credit_report`: static `credit_snap[2][3]`; `idx` 0 → zero both rows else `idx = 1`; snapshot `eshkp->credit/debit/loan`; `idx && !silent` → credit-cut vs debit-up vs loan-up else-if, `if (amt) Your("%s by %ld %s.")`). Callers `explode.c:744–747` (shop_origin baseline, silent) + `:899–945` (`obj_left_shop`, hero-shop gate, gold-only `addtobill`, `lostgoods` tail report). BEFORE/NOW = `botl.h:160–161` (0/1).
**JS:** 2 files, +81/−10 (`js/shk.js` +43/−1 export; `js/explode.js` +38/−9 wiring + doc), under the 600/10 caps. Density note: C function is 33 lines (small-C base); the balance is its two scatter caller arms + imports (one envelope, D-2274 precedent).
**Change:** new `export async function credit_report(shkp, idx, silent)` in `js/shk.js:453` in C position (after `rob_shop`, before `remote_burglary` `:664` — matching C `:628` between its neighbors), module-level `credit_snap`, C order/branch structure verbatim, `|0` reads (= C zero-init), report via `await pline(\`Your ${msg} by ${amt} ${currency(amt)}.\`)`. `js/explode.js` runs the C caller arms: `shop_origin` (`in_rooms` + `shop_keeper`, `remote_burglary`-idiom) + `credit_report(shkp, 0, true)` baseline; per-object `obj_left_shop = shop_origin && !costly_spot(x, y)`; hero-shop gate with C's `strchr(u.urooms, '\0')`-non-null quirk preserved (`includes('')`); gold-only `addtobill(obj, false, false, true)` with the C gold-comment kept; `if (lostgoods) credit_report(shkp, 1, false)` tail. Imports: `shop_keeper/costly_spot/credit_report/addtobill` static from `shk.js` (`imports.mjs --can` hoisted-safe, same SCC, call-time use only — `shk.js` never imports `explode.js`), `in_rooms` joins the existing `hack.js` import, `SHOPBASE` joins the `const.js` import, `GOLD_PIECE` via house `objectNames.indexOf` idiom.
**Verify:** `node scripts/verify.mjs --fn credit_report` → PASS syntax (2 changed js files: js/explode.js js/shk.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_credit_report.mjs` (kept out of tree, deleted after) through the real modules, 8/8: credit 100→60 `Your credit has been reduced by 40 zorkmids.`; debit 0→50 and loan 0→30 `Your debt has increased by … zorkmids.`; no-change / silent-flag / credit-increase → no pline; `idx` 7 normalizes to NOW; debit+loan both up → debit reported (C else-if order); real-`currency` oracle `zorkmid`/`zorkmids`.
**Named:** boulder-restack `sobj_at` residual (own Open row); `VIS_EFFECTS` (commented out in C too).
**Next:** do not re-pop the `credit_report`/scatter-shop envelope. Falsifier: a rescore or fresh `verify credit_report` showing a session blocked with it as owner (e.g. a shop-explosion billing row).
## 2026-09-14 — D-2281 `invent.c` sobj_at: canonical export, 9 of 12 clones retired (queue row `invent.c` sobj_at, D-2274 residual)

**C locus:** `invent.c:1466–1475` (`sobj_at`: walk `svl.level.objects[x][y]` nexthere chain, `otyp ==` match, first hit or NULL). 100 C call sites; C neighbor `nxtobj` (:1479) already canonical in `js/mkobj.js:2182`.
**JS:** 10 files, +21/−70 (`js/mkobj.js` +12 canonical; 9 files import-line + clone-delete), exactly at the 10-file cap. Density note: shipped C is 9 lines (small-C exception to the ~40-line floor); the value is the 9-clone dedup.
**Change:** new `export function sobj_at(otyp, x, y)` in `js/mkobj.js` directly before `nxtobj` (C position :1466 before :1479; same file as its C neighbor, whose `(otyp|0)===(type|0)` compare it shares — C-exact for int otyps). 9 clones deleted, `sobj_at` joins each file's existing mkobj import (`imports.mjs --can mklev.js mkobj.js sobj_at` → ALREADY, no new edge; same SCC, hoisted function, call-time use only, no top-level TDZ read). Home is mkobj.js not invent.js: `objects_at` + sibling `nxtobj` already live there and all 12 clone files already import mkobj (invent.js would need 2 new edges). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn sobj_at` → PASS syntax (10 changed js files: js/detect.js js/dig.js js/dokick.js js/fountain.js js/hack.js js/mklev.js js/mkobj.js js/mthrowu.js js/teleport.js js/trap.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. No hand probes: every rewired call site keeps the same walk and C-exact compare (full + cohort green confirms no behavior change).
**Named:** residual exact-name clones `js/dbridge.js:121`, `js/music.js:498`, `js/steed.js:161` (10-file cap cut; own Open row) + 7 renamed same-body variants (`sobj_at_monmove`, `sobj_at_shk`, `sobj_at_nexthere`, `sobj_at_otyp` ×2, `sobj_at_hurtle`, `sobj_at_look` — rename-to-import, same row).
**Next:** rewire the residual row to the canonical; do not re-pop this envelope. Falsifier: a rescore or fresh `verify sobj_at` showing a session blocked with it as owner.
## 2026-09-14 — D-2280 `timeout.c` obj_move_timers: migrating-object timer carry (queue row `timeout.c` obj_move_timers, D-1572-envelope residual)

**C locus:** `timeout.c:2339–2353` (`obj_move_timers`: walk `gt.timer_base`; per TIMER_OBJECT match on src — rebind `curr->arg.a_obj` to dest, `dest->timed++`, `count++`; `count != src->timed` → `panic("obj_move_timers")`; `src->timed = 0`). Zero C callers in `nethack-c/upstream/src` (migrating-object carry API; sibling `obj_split_timers` `:2358–2370` is the wired one, D-1572).
**JS:** 1 file, +26/−0 (`js/mkobj.js`), under the 600/10 caps. Density note: shipped C is 14 lines; insertions match C one-for-one (small-C exception to the ~40-line density floor).
**Change:** new `export function obj_move_timers(src, dest)` in `js/mkobj.js` in C position (directly before `obj_split_timers`, matching C `:2339` before `:2358` — same file as its sibling, timer home per review 533). C order verbatim: repoint walk (no reinsert — timeout is absolute so queue order is unchanged, unlike split's `start_timer` copies) → count-vs-`timed` check → zero src. C `panic` → `throw new Error('obj_move_timers')` (file precedent; `impossible()` is async and must not be fire-and-forgotten from a sync API); null guards match sibling `obj_split_timers` (C NONNULLARG12).
**Verify:** `node scripts/verify.mjs --fn obj_move_timers` → PASS syntax (1 changed js file: js/mkobj.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probe `/tmp/probe_move_timers.mjs` (kept out of tree, deleted after) through the real modules, 13/13: dual-timer move (hatch+burn absolute timeouts preserved, src zeroed/detached, other-object + TIMER_LEVEL entries untouched, queue order kept), count-mismatch throw with src.timed preserved (C panics before zeroing), null no-ops, untimed no-op.
**Named:** none new (SetVoice / migrating `#if 0` hatch arms stay with the D-1572 row; `splitbill`/`copy_oextra`/light-split all live).
**Next:** do not re-pop `obj_move_timers` for this envelope. Falsifier: a rescore or fresh `verify obj_move_timers` showing a session blocked with it as owner (e.g. a migrating-object hatch/burn timer row).
## 2026-09-14 — D-2279 `light.c` obj_split_light_source: light-source split on stack split (queue row `light.c` obj_split_light_source, copy_oextra-envelope residual)

**C locus:** `light.c:779–803` (`obj_split_light_source`: walk `gl.light_base`; per LS_OBJECT match on src — alloc + struct copy, `Is_candle(src)` → recompute both ranges via `candle_light_range` + `gv.vision_full_recalc = 1`, rebind copy to dest, head-insert, `dest->lamplit = 1`); caller `mkobj.c:500–501` (`if (obj_sheds_light(obj)) obj_split_light_source(obj, otmp)`, after `obj_split_timers`).
**JS:** 3 files, +37/−6 (`js/timeout.js` +29 export; `js/mkobj.js` +6/−4 import + wiring + doc; `js/apply.js` +2/−2 comment), under the 600/10 caps. Density note: shipped C is 24 lines; insertions match C one-for-one.
**Change:** new `export function obj_split_light_source(src, dest)` in `js/timeout.js` in C position (directly before `obj_merge_light_sources`, matching C `:779` before `:808` — same file as its sibling, both callees file-local, no new edge). Snapshot iteration + `unshift` per match = C head-insert walk safety (C comment: never interferes, already past the insertion point); struct spread copies x/y/range/flags; candle arm recomputes both ranges (post-split quans — `splitobj` reduces `obj.quan` before the call on both sides) + `game.vision_full_recalc`; null-safe guards (C NONNULLARG12). `js/mkobj.js` — name joins the existing `timeout.js` import (`imports.mjs --can mkobj.js timeout.js obj_split_light_source` → ALREADY, no new edge; same SCC, hoisted function, call-time use only, no top-level TDZ read); `splitobj` runs the C gate in C position (after `obj_split_timers`); doc Deferred retired.
**Verify:** `node scripts/verify.mjs --fn obj_split_light_source` → PASS syntax (3 changed js files: js/apply.js js/mkobj.js js/timeout.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probe (kept out of tree, /tmp, deleted after): `/tmp/probe_splitlight.mjs` 23/23 through the real modules (unlit no-op incl. no recalc; lamp copy head-bound with x/y/range/flags preserved, src untouched, no recalc; candle 7/3 split → ranges 3/2 + recalc; end-to-end `splitobj` on lit lamp stack → child quan/lamplit + head entry, parent entry intact; unlit `splitobj` → no entries, child dark; `initRng(8000)` seed for `next_ident` only).
**Named:** none new (`timers` in the data.md row = `obj_move_timers`, its own live Open row).
**Next:** do not re-pop `obj_split_light_source` for this envelope. Falsifier: a rescore or fresh `verify obj_split_light_source` showing a session blocked with it as owner (e.g. a lit-candle-split vision/RNG row).
## 2026-09-14 — D-2278 `shk.c` splitbill: unpaid bill split on stack split (queue row `shk.c` splitbill, D-2275 named omit)

**C locus:** `shk.c:3622–3658` (`splitbill`: `shop_keeper(*u.ushops)` + `inhishop` gate, `onbill(obj, shkp, FALSE)` gate, `bp->bquan -= otmp->quan` (after fall-through under/zero-quantity `impossible` arms), `billct == BILLSZ` → `otmp->unpaid = 0`, else new `bill_p[billct]` entry `{bo_id = otmp->o_id, bquan = otmp->quan, useup = FALSE, price = old price}` + `billct++`); caller `mkobj.c:493–494` (`if (obj->unpaid) splitbill(obj, otmp)` in `splitobj`, before `copy_oextra`).
**JS:** 2 files, +41/−3 (`js/shk.js` +36 export + doc; `js/mkobj.js` +5/−3 import + wiring + doc), under the 600/10 caps. Density note: shipped C is 35 lines; insertions match C one-for-one.
**Change:** new `export function splitbill(obj, otmp)` in `js/shk.js` in C position (directly before `sub_one_frombill`, matching C `:3623` before `:3660`), in C order: shkp gate → onbill gate → decrement → full-bill `unpaid = 0` else price-carrying append + `billct++`. Sync by necessity (`splitobj` is sync and called from sync paths): the four C `impossible()` diagnostics stay named omissions (precedent `sub_one_frombill`); both early-return gates and the fall-through decrement are preserved exactly. One defensive `Array.isArray` guard on `bill_p || bill` (C's `-1000` poison + strict-mode ESM would throw on index-assign; unreachable after the onbill match, costs nothing).
**Verify:** `node scripts/verify.mjs --fn splitbill` → PASS syntax (2 changed js files: js/mkobj.js js/shk.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probes (kept out of tree, /tmp): `/tmp/probe_splitbill.mjs` 11/11 through the real modules (normal split shrinks + price-carry + billct++, full bill decrements yet clears child unpaid, not-on-bill / no-shkp / not-in-shop no-ops without throw) + `/tmp/probe_splitobj_shop.mjs` end-to-end (`splitobj(obj,4)` on a 10-unpaid shop stack → parent 6, child 4, bill 10→6 + child entry at old price, billct 2, child stays unpaid).
**Named:** the four `impossible()` diagnostics (no-shkp, not-on-bill, under/zero-quantity — control flow preserved, prints named); `obj_split_light_source` (light split — live Open row); `nextoid` shop-price search (pre-existing splitobj omit); `bill_p === -1000` poison path (entry-gated, unreachable here).
**Next:** do not re-pop `splitbill` for this envelope. Falsifier: a rescore or fresh `verify splitbill` showing a session blocked with it as owner (e.g. an unpaid-stack-split bill topline or a `bquan` RNG row).
## 2026-09-14 — Audit reviews 1239–1243 (b5711dd9..3fcfefe2) + cadence 44/44

Review iteration, no js/ edits. 4 ACCEPT (1239 mb_trapped/vamprises, 1240 scatter landmine, 1242 enlightenment Upolyd, 1243 twin canonicalization), 1 ACCEPT-WITH-DEBT (1241 copy_oextra — mergable SCR_MAIL MAIL_STRUCTURES flavor arm live but unported+unnamed; map material for data.md:302, not Must-fix). Re-measured every corpus claim: vamprises/mb_trapped/scatter/copy_oextra vacuous 0-blocked (rows cited 0); chwepon --base 6106e47d → 2 PASS PROGRESS (re-confirmed); D-2276 scoreboard delta 3 newly-PASS / 0 regressed (re-counted from the artifact). Cadence full `sessions` at HEAD 3fcfefe2: 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed 90+0.61/turn (R² 0.79). Queue 11 Open, 0 Must-fix — no REFILL (8–12 band), no archive. Next port pops Open head (`shk.c` splitbill).
## 2026-09-14 — D-2277 `monmove.c` mb_trapped dig/lock twins → canonical export (queue row `monmove.c` mb_trapped dig/lock twins, D-2273 named omit)

**C locus:** `monmove.c:54–74` (`mb_trapped`: verbose KABOOM/nearby-distant via `mdistu > 49`, `wake_nearto 49`, `mstun = 1`, `mhp -= rnd(15)`, `DEADMONSTER` → `mondied` + still-dead TRUE with lifesave fallthrough, `mon_learns_traps(TRAPPED_DOOR)`); callers `dig.c:1436` (`if (mb_trapped(mtmp, sawit || seeit)) { newsym; return TRUE; }`) and `lock.c:1216` (`(void) mb_trapped(mtmp, sawit || seeit)` — return ignored; the `:1218` wake_nearto comment belongs to the no-monster else arm).
**JS:** 2 files, +17/−59 (`js/dig.js` import + doc, clone −24, `pline_mon` −1; `js/lock.js` import + doc, clone −28, `pline_mon` −1), under the 600/10 caps. Density note: net-negative wiring handoff — the 20-line C body shipped D-2273; this iter retires its exact named omission (the twin stubs), so there is no new C left to insert.
**Change:** both clones deleted; `import { mb_trapped } from './monmove.js'` in `dig.js` + `lock.js` (hoisted function declaration, call-time use only — `imports.mjs --can` SAFE on both edges, same 90-module SCC, no top-level TDZ read; precedent `mhitm.js:134`). Call sites unchanged (`await mb_trapped(mtmp, sawit || seeit)`); dig's `if (mtmp.mx) newsym` guard kept — `mondead` preserves `mx`/`my`, so it matches C's unconditional `newsym` in practice. Now-unused `pline_mon` trimmed from both display imports; every other helper (`dist2`/`Unaware` in dig; `dist2_lock`/`You_hear`/`wake_nearto`/`Deaf`/`Unaware` in lock) still used elsewhere.
**Verify:** `node scripts/verify.mjs --fn mb_trapped` → PASS syntax (2 changed js files: js/dig.js js/lock.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: dig/lock outside its shared set) + hand full `node frozen/ps_test_runner.mjs sessions` → **44/44** (fortress holds, exact RNG + screens). Preflight `verify --no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); no corpus session reaches a dig/bolt trapped-door monster kill (durable-test-collateral disclosure).
**Named:** `mb_trapped` message predicates stay the canonical's house checks (`game.u` Unaware/Deaf, raw `pline` hear) vs C `Deaf`/`Unaware` macros + `You_hear` acoustics/Underwater dream/barely arms (D-2273 envelope); `Soundeffect(se_kaboom_door_explodes/se_explosion)` in both arms (pre-existing).
**Next:** do not re-pop the `mb_trapped` twins. Falsifier: a rescore or fresh `verify mb_trapped` showing a session blocked with it as owner (e.g. an `rnd(15)` RNG row or a KABOOM topline at a dig/doorlock step).
## 2026-09-14 — D-2276 `insight.c` attributes_enlightenment Upolyd foreign-shape arm + `polyself.c` set_uasmon INFRAVISION FROMFORM (queue row `insight.c` attributes_enlightenment Upolyd foreign-shape arm, 2 corpus blocks under recorded owner chwepon)

**C locus:** `insight.c:1859–1878` (`Upolyd && u.umonnum != u.ulycn && !(final == ENL_GAMEOVERDEAD && u.umonnum == PM_GREEN_SLIME && !Unchanging)` → `Sprintf(buf, "polymorphed into %s", an(pmname(...)))` with the `vampshifted` cham variant, wizard `(mtimedone)`, `you_are(buf, "")`); `polyself.c` `set_uasmon` `PROPSET(INFRAVISION, infravision(Upolyd ? mdat : &mons[gu.urace.mnum]))`; `attrib.c` `innately` priority (role/race → FROMOUTSIDE → FROMFORM) + `from_what` FROM_FORM → `" from your creature form"`.
**JS:** 2 files, +56/−10 (`js/invent.js` +48/−9: import + slime const + two arms + two fallback gates + deferred-note updates; `js/polyself.js` +8/−1: two import names + PROPSET + omission-list retire), under the 600/10 caps. Density note: the shipped C is ~20 (foreign-shape arm) + 1 (PROPSET line); the arm ships ×2 builders plus the 1-line state writer its suffix reads — the cluster is the unit.
**Change:** `js/invent.js` final builder — the C arm verbatim in C position (after Polymorph_control, before were-form): `Upolyd && umonnum != (ulycn ?? NON_PM)`, slime-death exclusion with the house `u.Unchanging||H||E` idiom, `vampshifted(game.youmonst)` cham branch, `flags.female` (D-2208: form arms keep it), wizard `(mtimedone)`, `you_are` (were/are by final); `PM_GREEN_SLIME` via `monsterNames.indexOf` (generated table exports no such const — same pattern as eat/end.js). MAGIC builder — same arm with in-progress `"are"` (slime exclusion statically dead at final==0, noted); were-form/Hate_silver stay deferred there (pre-existing).
**Verify:** `node scripts/verify.mjs --fn attributes_enlightenment` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: nothing recorded under owner attributes_enlightenment — NOT a corpus PASS; row cited 2 blocks under chwepon, probed below) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. Row probe `node scripts/hidden-proxy.mjs verify chwepon` → **2 PASS, 0 worse → PROGRESS** (Samurai-91106@133, Monk-92213@109). Bonus: full `hidden-proxy score --jobs 8` → 466/553 (86.3 % excl. env), pass-set diff vs committed baseline: newly PASS = the 2 row sessions + scen-intrinsic-Caveman-92138, **REGRESSED: none**; Caveman attributed by ablation (stashed js/ → FAIL 81/83 screens with 3406/3406 RNG; with port → PASS; its baseline do_statusline1@72 attribution was stale — intermediate commits had already moved it).
**Named:** `lays_eggs` arm (`:1879`, neither form lays eggs — no corpus row); Unchanging/Polymorph blocked-shape arms (`:1838–1856`); MAGIC-builder were-form/Hate_silver (final builder has them per D-2062); remaining `set_uasmon` deferred PROPSETs (ANTIMAGIC, SICK_RES, STUNNED, HALLUC_RES, SEE_INVIS, TELEPAT, INVIS, LEVITATION, SWIMMING, PASSES_WALLS, REFLECTING, BLND_RES, vamp cham, polysense, light-source bookkeeping).
**Next:** do not re-pop attributes_enlightenment foreign-shape / `set_uasmon` INFRAVISION for this envelope. Falsifier: a rescore or fresh `verify chwepon` / `verify attributes_enlightenment` showing a session blocked with either as owner (e.g. a vampshifted-cham disclosure row or a poly'd-elf infravision row).
## 2026-09-14 — D-2275 `mkobj.c` copy_oextra + mailcmd helpers: split/bill-dummy oextra copy, mergable mailcmd gate (queue row `mkobj.c` copy_oextra tails, data.md:300-301 named omit)

**C locus:** `mkobj.c:416–448` (`copy_oextra`: null guards, `newoextra` when missing, `oname(ONAME_SKIP_INVUPD)`, `newomonst` + `memcpy` with `mextra`/`nmon` cleared (`#if 0` m_id renewal stays out — m_id is copied) + `copy_mextra`, `new_omailcmd`, `newomid` + assign); `:157–176` (`new_omailcmd` ensure-bag + free-old + `dupstr`, `free_omailcmd`); `obj.h:191–199` (ONAME/OMONST/OMAILCMD/OMID + has_ quartet); callers `splitobj` `:495–499` (`copy_oextra` then `free_omid`, LUAFREE→FREE normalize, `splitbill` before, timers/light after) and `bill_dummy_object` `:727–729`; `invent.c:4477–4481` (mergable mailcmd gate).
**JS:** 3 files (`js/const.js` +4 accessors, `js/mkobj.js` +73/−3 helpers + wirings + doc retires, `js/shk.js` +9/−2 wiring + doc retires), under the 600/10 caps. Density note: shipped C is ~48 (copy_oextra 31 + mailcmd pair 17) plus two 2-line caller wirings and the 4-line mergable gate — insertions match C.
**Change:** new `export function copy_oextra(obj2, obj1)` in `js/mkobj.js` in C order (guards, `newoextra`, `has_oname` → live `oname` with `ONAME_SKIP_INVUPD`, `has_omonst` → `newomonst` + exact-copy assign (stale keys deleted = C `memcpy`) + `mextra`/`nmon` null + live `copy_mextra`, `has_omailcmd` → new `new_omailcmd`, `has_omid` → `newomid` + assign); new `new_omailcmd`/`free_omailcmd` beside `newomid`/`free_omid`; `OMAILCMD`/`has_omailcmd` join the `const.js` accessor block; `splitobj` + `bill_dummy_object` run `copy_oextra` then `free_omid` in C position (splitobj also takes the LUAFREE normalize; `splitbill`/light arms stay named); `mergable` takes the C mailcmd gate verbatim. Imports: `oname` is a new `mkobj→do_name` edge (`imports.mjs --can` → same 90-module SCC, hoisted function, call-time use only, no TDZ read); `copy_oextra`/`free_omid` ride the existing `shk→mkobj` edge; rest rides existing `const`/`mon` edges. No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn copy_oextra` (+ `--fn dealloc_oextra`, `--fn extract_nobj`) → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: no shared file changed). Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probe `/tmp/probe_oextra.mjs` (kept out of tree, deleted after) through the real modules, 23/23: copy no-op guards, oname/omonst (m_id kept, mextra/nmon cleared, unaliased)/omailcmd/omid copy, memcpy stale-wipe, mailcmd new/overwrite/free, splitobj child keeps name+traits+mail and loses omid while the parent keeps it, mergable one-sided/mismatched reject with matching following the base verdict.
**Named:** `shk.c` splitbill (unpaid split billing — live Open row, queued this commit as REFILL); `obj_split_light_source` (light split — live Open REFILL row); `zap.c:5564` poly_obj `dealloc_oextra` (live Open REFILL row); `timeout.c:2339` `obj_move_timers` (live Open REFILL row); `invent.c:1466` `sobj_at` canonical export (live Open REFILL row); `shk.c:628` `credit_report` (live Open REFILL row); invent-Array vs nobj `extract_nobj` (structural — JS invent is an Array, splice stays); bill_dummy `nextoid` price-match + Is_candle lamplit gate; `free_omonst` mextra drop stays as-is (JS GC frees with the shell; no live reader after free).
**Next:** do not re-pop `copy_oextra`/`dealloc_oextra`/`extract_nobj`/`new_omailcmd` for this envelope. Falsifier: a rescore or fresh `verify copy_oextra` showing a session blocked with one as owner (e.g. a split-name topline or an omid RNG row).
## 2026-09-14 — D-2274 `explode.c` scatter landmine arm: MAY_FRACTURE/MAY_DESTROY/uball/flooreffects/tail + `blow_up_landmine` wiring (queue row `explode.c` scatter, data.md:1046 named omit)

**C locus:** `explode.c:721–947` (`scatter`: impossible site-gate `:747–749`, uball/uchain `:762–771`, quan peel `:772–780`, MAY_FRACTURE `:782–821`, MAY_DESTROY `:823–829`, chain flight `:850–905` with `!ZAP_POS`/closed_door/mon/you/sink stops, `flooreffects` placement `:906–936`, hideunder/mtrapped/maybe_unhide tail `:938–944`); `trap.c:3178–3181` (`blow_up_landmine` calls `scatter(x, y, 4, MAY_DESTROY|MAY_HIT|MAY_FRACTURE|VIS_EFFECTS, NULL)` first, before `del_engr_at`). JS flag numbering is its own (const.js `MAY_HITMON 0x1/MON 0x2/HIT 0x3/DESTROY 0x4/FRACTURE 0x8` vs C hack.h `0x02/0x04/0x06/0x08/0x10`) but used consistently on both sides of every call — internal only, not a bug.
**JS:** 2 files, +137/−41 overall (`js/explode.js` ~+120 arms+imports+docs, `js/trap.js` +12/−7 wiring), under the 600/10 caps. Density note: the shipped C is ~120 (new scatter arms) + the 4-line `blow_up_landmine` call — insertions match C.
**Change:** `js/explode.js` — `scatter` now follows C order: `await impossible` site-gate; uball/uchain arm (`u.uball`/`u.uchain` identity per ball.js, `Soundeffect(se_chain_shatters,25)`, `pline('The chain shatters!')`, `unpunish()`, chain `continue`); MAY_FRACTURE arm (`rn2(10)` short-circuit; boulder `Tobjnam 'break' + ' apart.'` vs `You_hear('stone breaking.')`, `fracture_rock` + `place_object`; statue STATUE_TRAP `deltrap` + `crumble` pline/hear + `await break_statue` + place; `newsym`, `used_up`); MAY_DESTROY arm (`!rn2(10) || oc_material==GLASS(19, objclass.h) || EGG` → `await breaks`); chain-build under `!used_up`; placement gated on `!(await flooreffects(obj,x,y,'land'))`; tail `u_at && uundetected && hides_under(youmonst.data)` → `hideunder`, `m_at` mtrapped clear, `await maybe_unhide_at`. New imports ride existing-or-SCC edges, all call-time use, no TDZ read (`imports.mjs --can` ×8: hack `You_hear` + mon `hideunder` ALREADY; do/monmove/objnam/read/dig/dothrow/trap IN-SCC; se ids from generated, leaf). `Tobjnam` is the objnam.js export (not a 13th clone); otyp/material consts are file-local data (`objectNames.indexOf`, `GLASS=19` per dothrow.js).
**Verify:** `node scripts/verify.mjs --fn scatter` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (script skips full: "no shared file changed") + hand full `frozen/ps_test_runner.mjs sessions` → **44/44** (RNG + screens exact everywhere incl. seed0014 landmine session 59178/59178) · VERIFY-equivalent held. Preflight `verify --no-cohort` green on a clean tree before edits. Throwaway probes (kept out of tree, /tmp): empty-pile full-flag scatter → 0 total + 0 RNG draws; single ROCK + MAY_HIT → total 1 with exactly 2 draws (dir+range) through real `flooreffects`/`place_object`; EGG + MAY_DESTROY → destroyed, total 0, ≥1 draw.
**Named:** shop_origin baseline + gold `addtobill`/`lostgoods` (no live `credit_report` export — data.md:1049); boulder-restack `sobj_at` (no canonical export, 12 local clones — data.md:1049); VIS_EFFECTS (commented out in C too); `blow_up_landmine` drawbridge/fillholetyp/liquid_flow/fill_pit/maybe_dunk_boulders/spot_checks (unchanged, separate arms).
**Next:** do not re-pop `scatter`/`blow_up_landmine` for this arm. Falsifier: a rescore or fresh `verify scatter` showing a session blocked with it as owner (e.g. a landmine-blast `rn2(10)`/`rnd` RNG row or a fracture/crumble topline).
## 2026-09-14 — D-2273 `mon.c` vamprises door-trap kill arm + canonical `monmove.c` mb_trapped (queue row `mon.c` vamprises door-trap kill, D-2231 named omit)

**C locus:** `mon.c:2888–2987` (`vamprises`; trapped arm `:2966–2981`: `doormask = D_NODOOR`, `recalc_block_point`, `trapped` → `save_verbose/flags.verbose = FALSE`, `trap_killed = mb_trapped(mtmp, seeit)`, restore, `trap_killed && canspotmon && !Unaware` → `pline_mon("%s is destroyed!", Monnam)`); `monmove.c:54–74` (`mb_trapped`: `flags.verbose` KABOOM/nearby-distant via `mdistu > 49`, `wake_nearto 49`, `mstun = 1`, `mhp -= rnd(15)`, `DEADMONSTER` → `mondied` + still-dead TRUE with lifesave fallthrough, `mon_learns_traps(TRAPPED_DOOR)`).
**JS:** 2 files, +35/−15 (`js/mhitm.js` +1 import, +14 arm, doc retire; `js/monmove.js` +4 import names, export + C-order body), under the 600/10 caps. Density note: the shipped C is ~15 (vamprises arm) + ~20 (mb_trapped) lines — the callee is the unit, so 35 insertions match C.
**Change:** `js/monmove.js` — exported the canonical `mb_trapped` in C order (verbose gate, KABOOM/nearby-distant, `wake_nearto 49`, `mstun`, `rnd(15)`, `DEADMONSTER` → `await mondied` + still-dead TRUE with lifesave fallthrough to `mon_learns_traps(TRAPPED_DOOR)`); message predicates stay the module's house checks. Four names join existing edges (all `imports.mjs --can` ALREADY, same SCC, call-time use only, no TDZ read): `wake_nearto` (mon.js), `mon_learns_traps` (monsters.js), `mondied` (mhitm.js), `TRAPPED_DOOR` (const.js). `js/mhitm.js` — `vamprises` imports `mb_trapped` on the existing `mhitm→monmove` edge and runs the C arm verbatim: ensure `game.flags`, save/`= false`/restore `verbose`, `trap_killed = await mb_trapped(mtmp, seeit)`, then `trap_killed && canspotmon && !unaware` → `pline_mon(Monnam + " is destroyed!")`.
**Verify:** `node scripts/verify.mjs --fn vamprises` → PASS syntax (2 changed js files: js/mhitm.js js/monmove.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); no corpus session reaches a vampire-revive trapped-door kill (durable-test-collateral disclosure).
**Named:** `js/dig.js:940` + `js/lock.js:794` `mb_trapped` twins stay stubs (no `mondied`/`mon_learns_traps`; lock's keeps its `wake_nearto`) — separate rows on rescore; `js/monmove.js` export is canonical. `turns.md:2822` "full mondied from `mb_trapped`" now half-stale (monmove full, dig/lock pending).
**Next:** do not re-pop `vamprises`/`mb_trapped` for this arm. Falsifier: a rescore or fresh `verify vamprises` / `verify mb_trapped` showing a session blocked with either as owner (e.g. a vampire-revive trapped-door topline or an `rnd(15)` RNG row at a door smash).
