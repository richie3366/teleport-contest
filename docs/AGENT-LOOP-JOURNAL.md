# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
## 2026-09-09 — Audit 0a07f32a..b95a28db (reviews 1198–1205: 7 ACCEPT, 1 DEBT, 0 Must-fix) + cadence 44/44

Review iteration over the 8 JS-touching SHAs since 0a07f32a (D-2232..D-2239), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: all 8 vacuous-0-confirmed exactly as each D-log labeled (zhitu/castmu/nameshk/tt_doppel/m_into_limbo/intervene/pray_revive/egg_type_from_parent — 0 at baseline, 0 in working scoreboard; no PASS claimed anywhere, zero worse). C spot-checks held throughout (acid_damage trap.c:4617–4654 + grease_protect :359–386 branch-for-branch incl. MAIL_STRUCTURES fade-skip; castmu :176–179 + buzzmu :992–999 order with BZ_VALID_ADTYP ≡ 1..10; nameshk :495–500 with shklight reference proven via :139/:249/:680; NON_PM dragon arm mon.c:5198–5207 with Dragon_*_to_pm obj.h:353–356 index arithmetic; tt_doppel topten.c:1444–1464 + classmon :1355–1375; collide do.c:1436–1445 / teleport.c:2279–2285 / vault.c:80–87 arms; amulet wizard.c:61–103 + intervene :785–810 + moveloop allmain.c:359–368; prayer_done :2283–2305/:2336–2340 + pray_revive :2177–2195; polymon egg block polyself.c:905–911 with BREEDER_EGG mon.c:5538). One DEBT, not Must-fix: 1202 u_collide_m limbos the original mtmp where C reassigns to the remaining occupant (do.c:1443–1445; near-dead path — monsters never stack — one-line fix). Notes, not C-wrongs: D-2235 Is_dragon clones are the 2nd/3rd copy but verified against obj.h:347–351 (consolidation debt); D-2237 Blind/m_next2u locals match C (30th/7th house-conventional copies); D-2232 resistance deferral draw-free-unobservable (inventory_resistance_check zap.c:5706–5718 FALSE without a ward). Rule #2 clean repo-wide. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.29/turn` (R² 0.79). No Must-fix prepended. Next cluster unchanged. Queue at 11 Open, no refill owed. Also backfilled D-2239 `b95a28db` Addressed hash.
## 2026-09-09 — Audit 820594ec..04fff85d (reviews 1192–1197: 4 ACCEPT, 2 DEBT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 820594ec (D-2226..D-2231), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: m_dowear_type PROGRESS (Samurai-92017 PASS), next_ident PROGRESS (Wizard-92187 PASS), nh_timeout PROGRESS (Rogue-91119 PASS) — zero worse throughout; makeroguerooms/maybe_finished_meal/mondead vacuous-0-confirmed exactly as each D-log labeled (rows cited 0 blocks). C spot-checks held throughout (roguecorr 9 guards vs extralev.c:44–135 all in real branches + splev_initlev sp_lev.c:2981–3018 arm-order exact; m_dowear_type worn.c:798–1002 arm-for-arm incl. another-substitution + 4-way shine; dig.c:1596–1604 + zap.c:3308–3318 unconditional rock after losehp; eat.c:472–493 gate + timeout.c:813–843 arms with itimeout(1) flat coherence; eat.c:3877–3890 + steal.c:367–371 position; lifesaved :2838–2884 + vamprises :2888–2987 + logdeadmon :2995–3068 + m_detach :2734–2803 + shkgone shk.c:234–269 + grddead vault.c:174–189 + monstone :3301–3303 + monkilled :3398). Debt, not C-wrong-blocks: 1193 worn.js:945 vision_recalc(1) has no C counterpart (C defers via vision_full_recalc; RNG-free, display-convergent); 1197 game.disintegested missing xkilled writer/reset (stale-true leaks wording only) + kill_genocided fire-and-forget (safe by run-to-completion, display-order only). Notes, not C-wrongs: D-2226 "new display.js edge" is ALREADY-static per --can; m_useup_mm lacks obfree (pre-existing helper, GC-convergent). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.29/turn` (R² 0.79). No Must-fix prepended. Next cluster unchanged (mondata big_little_match parked stale — full walks live since D-1772). Queue at 10 Open, no refill owed; scoreboard stamp-only (verify runs, no row flips).
## 2026-09-09 — Audit 44843418..820594ec (reviews 1185–1191: 7 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 7 JS-touching SHAs since 44843418 (D-2219..D-2225), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: weldmsg PROGRESS (Priest-92041 PASS, confirmed call-for-call); pleased/spec_applies/flip_worm_segs_vertical/cant_finish_meal/mon_set_minvis/rest_track vacuous-0-confirmed exactly as each D-log labeled (no PASS claimed, rows cited 0 blocks). C spot-checks held throughout (pat_on_head case-5 ladder pray.c:1310–1338 + C source-order cases 1/3/2/4 + 7/8→6 fallthrough noted behavior-neutral; spec_applies DFLAG1 :1024–1025 + defended guard :1036–1037 with defended arm-for-arm mondata.c:89–124; weldmsg body :1060–1074 + call sites :383/:473/:572 with doswapweapon correctly lacking reset_remarm; worm walkers worm.c:967–987 char-for-char + flip_level :661–666 order; cant_finish_meal eat.c:3892–3912 + zap.c:909 site; mon_set_minvis worn.c:473–485 with deleted clones proven divergent subsets; rest_track :92–105 + SFCTOOL ifndef scope verified to wrap only settrack/gettrack). Notes, not C-wrongs: D-2220/D-2221/D-2222 "new edge" framings are ALREADY-static per --can (zero new cycle risk); D-2219 cases-7/8 fallthrough should be named when give_spell ships. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.28/turn` (R² 0.79). No Must-fix prepended (7 ACCEPT). Next cluster unchanged (extralev LVLINIT_ROGUE/ROGUEOPTS); refill exhausted at 6 Open (fresh queue --limit 60 all parked/guard/already-Open; map omits gated at 90%); Score refreshed from this run. Also backfilled D-2190 `74944545` Addressed hash.
## 2026-09-09 — Audit 5cd7c741..44843418 (reviews 1179–1184: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 5cd7c741 (D-2213..D-2218), oldest-first, one file per SHA written as each finished. Corpus claims re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: use_grapple PROGRESS (Priest-92041 146→weldmsg@158); givit PROGRESS (Rogue-91119 70→nh_timeout@108); dosearch 1 PASS (Archeologist-92012) + 2 moved (Samurai-92017 54→m_dowear_type@142, Wizard-92127 31→getpos@72) + 1 parked residual (92023@65 identical toplines) — zero worse; populate_maze/dosounds/findgd/u_entered_shop vacuous-0-confirmed exactly as each D-log labeled (no PASS claimed). C spot-checks held throughout (all 7 whip/grapple surface() sites vs apply.c:3051/:3183/:3788/:3795/:3819/:3855/:3857; givit "%s." format eat.c:1082–1095; m_throw envelope mthrowu.c:702–786 + blindinc tail :836–841 incl. something="something" decl.c:45 and vision_clears; populate_maze loops + fallback :1197–1222 + walkfrom :1306 vs :1293–1294 + mazexy gx/gy globals; throne/beehive/vault Soundeffect vols + temple gate :330–331 + halu_gname pray.c:2577–2619 with randrole(TRUE)≡rn2_on_display_rng(SIZE-1) and roles[NUM_ROLES+1] terminator accounting; findgd vault.c:204–232 line-for-line; shkname shknam.c:873–890 with 11+1 shtypes parity + bill_p consumer shk.c:5001). Notes, not C-wrongs: sym.mjs misses mhitm.js AT_SPIT/AT_WEAP const exports (resolved by direct read :235/:242/:688); D-2215/D-2217 "new edge" framings are ALREADY-static per --can (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.29/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (shk Soundeffect/Hallu remainder shipped D-2218; Open head pray.c gifts); Score refreshed from this run.
## 2026-09-09 — Audit 32f374ab..5cd7c741 (reviews 1176–1178: 3 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 3 JS-touching SHAs since 32f374ab (D-2210..D-2212), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: ustatusline PASS (Archeologist-92004, stronger than the D-log's 141→145 PROGRESS — later commits closed the tail); use_misc PASS (Archeologist-92004, same session); dosearch0 PASS (Knight-92130) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (ustatusline branch-for-branch insight.c:3402–3489 + macro reads youprop.h:81/93/96/138/376–377, monst.h:71, you.h:554; C-locus sticks engrave.js:346 over divergent monmove.js:1629; digests mondata.h:71–72; passive_obj AD_FIRE uhitm.c:6156–6162 gate-for-gate incl. steam-vortex carve-out, weapon≡C-resolved obj per preamble :6135–6143; dosearch0 Norep :2022–2024 + SDOOR :2050 + SCORR :2056/:2060, SDOOR correctly keeps recalc). Notes, not C-wrongs: Strangled/flat-OR broadening is the house dual-store idiom (mirrors display.js); AD_CORR passes raw obj vs AD_FIRE's weapon (equivalent — callers never pass Null); passive_obj carried→update_inventory tail still unported (pre-existing, shared with ACCEPTED AD_CORR arm). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.28/turn` (R² 0.79). No Must-fix prepended (3 ACCEPT). Next cluster unchanged (Open head use_grapple); Score refreshed from this run. Also backfilled D-2212 `5cd7c741` Addressed hash.
## 2026-09-09 — Audit 2b917b52..38bd5d4f (reviews 1170–1175: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 2b917b52 (D-2204..D-2209), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: stairs_description PROGRESS (Healer-92109 208→do_statusline2@241); getpos PASS (Healer-91124); trapeffect_sqky_board PASS (Barbarian-92064); prinv PASS (Caveman-92059); enlightenment PROGRESS (Monk-92213 108→chwepon@109); steal_it PROGRESS (Priest-92097 152→do_statusline2@180) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (wasinside arm mon.c:3632–3640: outside the accessible gate, link-zeroing N/A under GC; getpos exitgetpos :1151–1157 clear gate; sqky both arms trap.c:1402–1476 byte-exact formats + wake_nearby file-local clone; mergable body :4378–4499 holds zero owornmask reads + merged fixup obj-worn-only :877–913; innategend :474–479 + title :396–401, form arm keeps flags.female; steal_it call-for-call :2173–2278 incl. SITM→SEDU fallthrough :4798–4799). Notes, not C-wrongs: `sym.mjs` misses `export {}` lists (could_seduce false "NOT EXPORTED", resolved by direct read of mhitm.js:685–694); D-2206/D-2209 messages call extended imports "new edges" but `--can` reports both ALREADY (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `64+0.40/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (Open head chwepon); Score refreshed from this run.
## 2026-09-09 — Audit ab784680..2b917b52 (reviews 1164–1169: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since ab784680 (D-2198..D-2203), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: legs_in_no_shape PROGRESS (Healer-91123 PASS); float_vs_flight vacuous-0-confirmed as labeled; make_hallucinated PROGRESS (Caveman-92052 PASS); do_statusline2 PROGRESS (2 PASS 92035/92091 + 2 moved, 9 unchanged, 0 worse — byte-identical to the D-log); next_ident PROGRESS (4 PASS + 92187 residual, 0 worse); make_stunned PROGRESS (Archeologist-92226 PASS). C spot-checks held throughout (fpostfx royal-jelly arm-for-arm incl. single-draw rnd(20); mount_steed gate + plural quirk; set_utrap/reset_utrap routing via C trap.c:1044–1057; HALLUC/STUNNED re-arm shapes; Stunned≡HStun youprop.h:81; statusline2 rank table botl.c:781–813 + cond_cmp + windows.c fieldorder read from pinned C; arise arm bones.c:457–478 + tail :505–511 + res_to_mr prop.h:25–27). Notes, not C-wrongs: TIMEOUT-masked wounded gate (house canonical, shared trap.js:2977), debug||wizard idiom, dual botl mirrors, end.js-local give_u_to_m clone (no mondata export exists). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `83+0.59/turn` (R² 0.76). No Must-fix prepended (6 ACCEPT). Next cluster repointed to Open head stairs_description (make_stunned shipped D-2203, PASS at HEAD); Score refreshed from this run.
## 2026-09-09 — Audit 74944545..ab784680 (reviews 1156–1163: 8 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 8 JS-touching SHAs since d22f6c29 (D-2190..D-2197), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: burnarmor/thitm/m_harmless_trap/dosounds vacuous-0-confirmed as labeled; unstuck PROGRESS (Knight-92002 52→one_characteristic@81); accessory_or_armor_on PASS (Tourist-92144); look_here PROGRESS (Wizard-92223 121→130, residual null-owner label as disclosed); minliquid_core PASS (stale row, PASS pre/post as disclosed). C spot-checks held throughout (burnarmor literal, savelife tail, Helmet_on 7 arms + set_bknown/adj_abon, expels tail + um_dist, defended/resists_magm + sqky/rocktrap arms, thitm full body + AD_RBRE=242, dosounds 7 arms draw-for-draw, mdrop_obj/steed/flush). Minor message framings noted (ALREADY-edges called "new", D-2196/D-2194) — substance safe, no C-wrongs. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `92+0.57/turn` (R² 0.77). No Must-fix prepended; Next cluster unchanged (fountain gush/engulfed map-driven).

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-09 — D-2246 `allmain.c` moveloop_core spine completion: clear_bypasses + resume_wish + seer do_vicinity_map + second encumber_msg + umoved reorder + clear_splitobjs (queue row `allmain.c` moveloop_core, no corpus block)

**C locus:** `allmain.c:194–201` (`if (svc.context.bypasses) clear_bypasses();` then sanity `:197–198`, then `if (svc.context.resume_wish) makewish();`); `worn.c:1054–1064` (staticfn `clear_bypass`: zero `bypass` down the chain, recurse into `cobj`) + `:1067–1116` (`clear_bypasses`: fobj/invent/migrating_objs/buriedobjlist/billobjs/objs_deleted, live fmon invents with the polymorphed-long-worm `mcorpsenm → NON_PM` revert, migrating/mydogs invents, floating uball/uchain, flag off); `zap.c:6323` (makewish clears `resume_wish` at entry) / `:6341` (term_gone sets it); `allmain.c:396–415` (`hero_seq++`, second `encumber_msg()` `:403` for message purposes after `nh_timeout`, seer block `:409–415` with the `(uhave.amulet || Clairvoyant) && !In_endgame && !BClairvoyant → do_vicinity_map(0)` caller before the `rn1(31,15)` reschedule); `allmain.c:443` (`clear_splitobjs()` first in once-per-player-input); `allmain.c:483–513` (`context.move = 1`, occupation arm with return, then `u.umoved = FALSE`).
**JS:** 5 files, +111/−12 (`js/allmain.js` +49/−12: 4 import names + 6 wiring sites; `js/worn.js` +68/−2: 3 import names + 2 functions; `js/detect.js` +1/−1; `js/monsters.js` +1; `js/zap.js` +3), under the 600/10 caps. Density note: portable C is ~70 lines (50-line clear_bypasses + 6 one-to-three-line spine sites); the balance is C-order cites the next iter needs to not "simplify" the order away.
**Change:** `js/worn.js` — module-local `clear_bypass` (C staticfn: same-file caller only; Array-or-nobj walk + `Has_contents`/`cobj` recursion) + new `export function clear_bypasses()` in C order (fobj/invent/migrating_objs/`level.buriedobjlist`/`billobjs`/guarded `objs_deleted`; fmon loop with the `(mhp|0) <= 0` DEADMONSTER skip and the `data?.mndx ?? mnum` long-worm `mextra.mcorpsenm = NON_PM` revert via live `has_mcorpsenm`; migrating/mydogs loops; `u.uball`/`u.uchain`; flag off). `PM_LONG_WORM` added to the `js/monsters.js` export block (module-local const, no new edge). `js/allmain.js` — `if (g.context.bypasses) clear_bypasses()` + `if (g.context.resume_wish)` → dynamic-import `makewish` (same pattern as the amulet block) in C position; `await encumber_msg()` after `hero_seq++` with the C message-purpose cite; seer caller `(uhave.amulet || Clairvoyant()) && !In_endgame(uz) && !BClairvoyant → await do_vicinity_map(null)` (null = random-farsight arm per D-1391) before the reschedule; `clear_splitobjs()` first in once-per-input; `umoved = false` moved after the occupation arm (occupation now sees last turn's `umoved`, like C).
**Verify:** `node scripts/verify.mjs --fn moveloop_core` → PASS syntax (5 changed js files) · PASS rule2 · note hidden (vacuous: row cited draws-in-step, 0 blocked at HEAD, no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit. Preflight `--no-cohort` green on a clean tree before edits. /tmp probe (kept out of tree): 8/8 — setter raises flag, floor/contents/invent/worm-invent cleared, worm mcorpsenm → NON_PM, DEADMONSTER skipped, ball+chain cleared, flag off. No maintained unit test added: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); the newly wired arms (bypasses flag, term_gone resume, amulet/clairvoyant seer) fire on no fortress path by construction.
**Named:** `get_nh_event` (tty no-op, `wintty.c:758` — nothing to call); POSITIONBAR / STATUS_HILITES off in `unixconf.h` (`do_positionbar`, `status_eval_next_unhilite` compiled out); MAIL `ckmailstatus` (`stat()` on the mailbox — filesystem, Rule #2); lua `NHCORE_MOVELOOP_TURN` (`:269`; no lua core in scored JS); `maybe_shuffle_customizations` (glyph-customization subsystem, live `#else` in `glyphs.c` but no JS counterpart); MICRO kbhit/`pgetchar` abort arm; WIN_MAP `display_nhwindow` branch (display paint); lava/pool/under once-per-hero (D-1000 family, already named); multi>0 run/travel shape (deliberate JS input architecture, out of envelope).
**Next:** do not re-pop `moveloop_core` for the shipped wiring (all six sites live). A future bypass/wish/seer divergence attributes to the callee owners (`bypass_objlist` marking, `getlin` wish text, `do_vicinity_map` paint), not this spine. Do not "fix" with seed/step/coordinate/RNG-index gates.
## 2026-09-09 — D-2245 `mon.c` wiz_force_cham_form + validvamp + `makemon.c` mkclass_poly wizard mon_polycontrol family (queue row `mon.c` wiz_force_cham_form, no corpus block)

**C locus:** `mon.c:5078–5154` (`wiz_force_cham_form`: `Change %s @ %s into what?` prompt from `noit_mon_nam` + `coord_desc` with QBUFSZ-1 name chop, TRYLIMIT 5 `getlin` loop with the `tryct == TRYLIMIT-1` "into what kind of monster?" first-retry reprompt, ESC keeps the above-picked form, `*`/`random`(strcmpi) forces NON_PM, `name_to_mon` then `name_to_monclass(buf, &mndx)` then `mkclass_poly` on class-only input, `validvamp` accept-or-`mndx = NON_PM` per try, `It can't become that.`, `!tryct` → `thats_enough_tries`, final `is_vampshifter && !validvamp` → `pickvampshape` "don't resort to arbitrary"); `mon.c:5028–5075` (`validvamp`: non-shifter → `validspecmon`, Vlad-with-special override-accept, shapeshifter choice collapses to `mon->cham`, plain-vampire WOLF reject, fog/bat accept, S_VAMPIRE/S_BAT/S_VORTEX/S_DOG class map with the S_DOG→default FALLTHROUGH); `makemon.c:1983–2012` (`mkclass_poly`: first/last mlet-class scan, `rn2(9) || class == S_LICH` hell-mask arm, freq-weighted `rnd(num)` walk with the `first--` off-by-one correction, no `polyok` check); gate `mon.c:5209–5211` (`if (wizard && iflags.mon_polycontrol) mndx = wiz_force_cham_form(mon)` between the cham-switch and the `:5214–5224` random arm).
**JS:** 2 files (`js/makemon.js` +257/−22: mkclass_poly 45, validvamp 55, wiz_force_cham_form 75, select gate + random extract + newcham split + 8 import names; `js/muse.js` +1/−1: export keyword), under the 600/10 caps. Density note: portable C is ~150 lines across three functions; the balance is the two promise-boundary splits plus the C-order comments the next iter needs to not "simplify" the sync path away.
**Change:** `js/makemon.js` — new `export function mkclass_poly(mletClass)` after `mk_gen_ok` in C order (same-file `mk_gen_ok`, `rn2(9)`/`rnd`, Inhell via the dungeon-hellish-flag idiom per D-0747, S_LICH string gate per the `mkclass_aligned` precedent). New `export function validvamp(mon, mndx_p, monclass)` in C order (`{ mndx }` out-box per the `name_to_monclass` convention; live `validspecmon`/`is_vampshifter`/`is_shapeshifter`/`ismnum`, imported `mon_has_special`; S_* string cases with the S_DOG fallthrough). New `export async function wiz_force_cham_form(mon)` in C order (QBUFSZ chop via `slice`, first-retry `slice(0,-1)` rewrite of the trailing `?`, `await getlin(pprompt, '')` + `mungspaces` return, `buf[0] === '\x1b'` ESC, `toLowerCase` random per the `strcmpi_eq` convention, `{ mndx }` boxes through `name_to_monclass`/`validvamp`, `await pline` on both message arms incl.
**Verify:** `node scripts/verify.mjs --fn wiz_force_cham_form` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: row cited 0 blocks, no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last js/ edit. Preflight `--no-cohort` green on a clean tree before edits. /tmp probe (kept out of tree, deleted): 18/18 — mkclass_poly class pick / bogus class → NON_PM / S_LICH arm; every validvamp arm (non-shifter delegate, Vlad override, shapeshifter collapse, wolf/fog/bat gates, all four class arms + S_DOG fallthrough both cham values, junk class); select stays a sync int with the gate off; wiz export present. No maintained unit test added: the repo has no `js/` unit harness (`sessions/` + `hidden-proxy` corpus are the suite); the wizard arms are unreachable in scored runs (flag unset) by construction.
**Named:** none new. `select_newcham_form`'s second C caller `sp_lev.c:2067` (special-level shapeshifter fixup) is a different arm from this gate — own row when a session reaches it.
**Next:** do not re-pop `wiz_force_cham_form`/`validvamp`/`mkclass_poly` (all live, sole live caller wired). A future wizard-polymorph divergence attributes to the callee owners (`name_to_mon` lookup, `getlin` editing, `mon_has_special` inventory walk), not this wiring. Do not "fix" with seed/step/coordinate/RNG-index gates.
## 2026-09-09 — D-2244 `mkobj.c` maybe_adjust_light + bless/curse/unbless/uncurse lamplit tails (queue row `mkobj.c` maybe_adjust_light, no corpus block)

**C locus:** `mkobj.c:1703–1736` (`maybe_adjust_light`: `new_range = arti_light_radius(obj)`, `delta = new - old`; `if (delta)` → `obj_adjust_light_radius(obj, new_range)`, then `if (!Blind && get_obj_location(obj, &ox, &oy, 0))` → `It`/`They` when `iflags.last_msg == PLNMSG_OBJ_GLOWS` else `Yname2` when `carried || cansee`, then `pline("%s %s %s%s.", buf, otense(obj, "shine"), abs(delta) > 1 ? "much " : "", delta > 0 ? "brighter" : "less brightly")`); light arms `bless :1747–1762` (`if lamplit old_light` before the flag flip, `if lamplit maybe_adjust_light` after), `unbless :1769–1779`, `curse :1786–1817`, `uncurse :1824–1836` (same shape); `read.c:1226–1250` (capture `was_lit`/`old_light` from the scales BEFORE the otyp flip, `lamplit = 0` across `bless`/`uncurse` so they do not double-adjust, restore `lamplit = was_lit`, `if (old_light) maybe_adjust_light`); `polyself.c:661` (uskin merge) and `:1967` (skinback) — surrounding arms unported, deferred with them.
**JS:** 7 files, +124/−46 total (`js/mkobj.js` +100/−46 incl. 47-line export; `js/read.js` +30/−12; `js/zap.js` +11/−8 incl. clone deletion; one-word awaits in pray/sit/wield; 2-line comment in light.js), under the 600/10 caps. Density note: the portable C is 32 lines + 4 two-line light arms + the 6-line dragon wire; the balance is the async-boundary documentation the next iter needs to not "simplify" the awaits away.
**Change:** `js/mkobj.js` — new `export async function maybe_adjust_light(obj, old_range)` in C branch/short-circuit order (`| 0` int idiom on the delta; `await obj_adjust_light_radius`; `get_obj_location(obj, 0)` null-gate = C FALSE arm; `!Blind()`; `game.iflags?.last_msg` It/They vs inline `where === OBJ_INVENT` (exact C `carried` macro, not eat.js's invent-membership variant) `|| cansee` → `Yname2`; `otense(obj, 'shine')` + `Math.abs(delta) > 1` much + brighter/less-brightly pline). Async because `pline` genuinely yields (`flush_screen`/`more()`); zero RNG on every arm. The four bless-family functions + `blessorcurse` become async with ONLY the light arms added — every pre-existing state change precedes the first await, so the ~30 sync callers (`mksobj_init`, mklev gen, mplayer loadout, bones `drop_upon_death`, wish parsing — all provably unlit objects, tails no-op) observe byte-identical behavior un-awaited.
**Verify:** `node scripts/verify.mjs --fn maybe_adjust_light` → PASS syntax (7 changed js files) · PASS rule2 · note hidden (vacuous: row cited 0 blocks, no --base owed — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; plus forced `node scripts/verify.mjs --full` (mkobj is constitution-listed) → PASS full 44/44. Preflight `--no-cohort` green on a clean tree before edits. /tmp probe (kept out of tree, deleted): 15/15 — sync-flag flips for all five functions incl. un-awaited `blessorcurse`, Sunsword radius 1/2/3 by curse state, bless 1→3 / curse 3→1 range + recalc, delta-0 and unlit no-ops, direct-call range update with no throw on the dipped-message arm. Probe detour recorded: bare-node `rn2` throws (RNG unseeded) — inside an async body that surfaces as a rejection, so the probe seeds via `initRng`; production paths always have RNG.
**Named:** polyself `uskin`-merge (`:661`) and `skinback` (`:1967`) light arms — surrounding dragon-merge/skin arms unported, no live JS call site (own row when those arms land). Potion dip sets bless/curse flags inline (`potion.js:3246–3247`, never calls `bless()`/`curse()`) so dipped lit artifacts still skip the adjust — dip-wiring row, not this one.
**Next:** do not re-pop `maybe_adjust_light` (symbol live, all live callers wired). A future lit-artifact-bless divergence attributes to the message owner it prints under, not this wiring. Do not "fix" with seed/step/coordinate/RNG-index gates.
