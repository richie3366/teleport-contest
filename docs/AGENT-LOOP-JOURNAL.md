# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-07 — D-2006 dat/themerms.lua Buried treasure / Massacre / Statuary fills: the reservoir picked them, JS ran nothing

**C locus:** `dat/themerms.lua` themeroom_fills `Massacre` `:173–190`, `Statuary` `:192–200`, `Buried treasure` `:134–148`, `make_dig_engraving` `:1052–1070`, `themeroom_fill` `:1009–1050` reservoir; `sp_lev.c create_object :2428–2437` (`o->buried` → `bury_an_obj` inside `create_object`, before the lua `contents` callback); `dig.c bury_an_obj :1984–2042` (obj_resists + organic ROT_ORGANIC arm).
**JS:** 1 file (`mklev.js` +119/−3), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean).
**Change:** `js/mklev.js` — `themeroom_fill_massacre` (27-name pool in C order, initial `lua_random2(1,27)`, count as five `lua_random2(1,5)` per nhlib `d` not rnd.c `d()`, per-corpse `percent(10)` re-pick, corpses via live `l_create_object` `{ id: CORPSE, montype }` DRY in croom), `themeroom_fill_statuary` (same `d(5,5)` statues + `lua_random2(1,3)` STATUE_TRAPs via DRY `get_location_coord_in_room` + `splev_mktrap_at`), `themeroom_fill_buried_treasure` (chest via `l_create_object` with container callback; bury inside the callback before the `d(3,4)` inner draws per C order — `obj_resists(0,0)`, extract, wooden `is_organic` + `obj_resists(5,95)` + `250+rnd(250)` ROT_ORGANIC, `add_to_buried`; engraving queued from chest ox/oy; inner empties via `l_create_object({})` so their placement/mkobj draws still burn; resisted/null chest keeps engraving+contents per the lua `if` scope), `make_dig_engraving_postprocess` (negate→`selection_all_room_floors`, `rndcoord(0)`, `Dig` + `tx=data.x-pos.x-1` words) + runner arm, BODIES +3, reservoir omission comment retired. Same-edge binding extensions only (`is_organic` from `./mkobj.js`, `ROT_ORGANIC` from `./const.js` — both `imports.mjs --can` ALREADY, no new edge).
**Verify:** `node scripts/verify.mjs --fn somex` → `PASS syntax 1 changed js file(s): js/mklev.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify somex: 2 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (genesis-Monk-92025: PASS; genesis-Rogue-92069: PASS; poly-Caveman-91133 → polymon@58 was 0; wish-Archeologist-92038 → next_ident@96 was 0 — both strictly later steps, different owners) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** none new — every `THEMEROOM_FILLS` entry now dispatches to a body (map `data.md` runtime-lua row updated; the `Buried treasure/Massacre/Statuary + dig postprocess absent` clause retired).
**Next:** the two later owners are each their own future row (`polymon` @58 is already the next Open row; `next_ident` @96 is a known corpus owner per HIDDEN-PROXY §4).
## 2026-09-07 — D-2005 uhitm.c mhitu FIRE/TLPT/STCK/PLYS arms: the missing mhitm_mgc_atk_negated gates before knockback

**C locus:** `uhitm.c:75–99` gate itself (already ported, D-0198/D-1405); the missing part was four mhitu arms that call it — `mhitm_ad_fire` `:2561–2587` (hitmsg, gate(TRUE), on_fire pline, golem burn-up + rehumanize, Fire_resistance zero, m_lev > rn2(20) destroy + ignite, burn_away_slime, else damage 0), `mhitm_ad_tlpt` `:2884–2927` (hitmsg, gate(FALSE) → "not affected", verbose uncertain line, tele(), non-fatal damage clamp), `mhitm_ad_stck` `:3321–3328` (gate(FALSE) FIRST at function top, hitmsg, set_ustuck when !negated && !ustuck && !sticks, barbs line), `mhitm_ad_plys` `:3443–3462` (hitmsg, multi >= 0 && !rn2(3) && !gate(TRUE) — rn2 before gate — stiffen/freeze + nomul(-rnd(10)) + nomovemsg + multi_reason + DEX exercise). C `hitmu` (`mhitu.c:1191–1194`) is adtyping→knockback, exactly the JS `hitmu` shape.
**JS:** 1 file (`mhitu.js` +182/−7), under the 600/10 caps. No new module edges, no DIAG/FORCE/seed gates (Rule #2 clean).
**Change:** `js/mhitu.js` — `mhitm_ad_fire_u` / `mhitm_ad_tlpt_u` / `mhitm_ad_stck_u` / `mhitm_ad_plys_u` in exact C branch order (incl. stck gate-first, plys rn2-before-gate, fire `(void)` destroy_items per `zap.c:5962`, tlpt `Math.trunc((damage-1)/2)` clamp with the 1-HP floor bump) + four switch cases + dispatch doc. Same-edge import extensions only (`AD_TLPT` from `./mhitm.js`; `tele, unconscious` from `./teleport.js`; `on_fire` from `./mondata.js` — all `imports.mjs --can` ALREADY), `STUNNED`/`TELEPORT_CONTROL` consts, file-local `Your`/`Teleport_control`/`Stunned`/`Free_action` (zap/potion/invent idioms), `PM_BARBED_DEVIL`/`PM_PAPER_GOLEM`/`PM_STRAW_GOLEM` consts. Live `monst[un]seesu(M_SEEN_FIRE)` kept per C (RNG-free; sibling elec_u/cold_u still defer theirs).
**Verify:** `node scripts/verify.mjs --fn mhitm_mgc_atk_negated` → `PASS syntax 1 changed js file(s): js/mhitu.js` · `PASS rule2` · `PASS hidden verify mhitm_mgc_atk_negated: 1 PASS, 5 moved past, 1 unchanged, 0 worse → PROGRESS` (Tourist-92003: PASS; Caveman-92140 → save_dungeon@123 was 58; poly-Caveman-92050 → one_characteristic@112 was 89; wish-Caveman-92183 → escape_from_sticky_mon@247 was 239; wish-Knight-92045 → use_container@71 was 48; wish-Ranger-92155 (working-board union extra) → wildmiss@198 was 144 — all strictly later steps, different owners) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** uhitm + mhitm arms of fire/tlpt/stck/plys stay deferred (D-1907 covers the mhitm SGLD/TLPT/WERE/SLIM family); `cold_u` `+= destroy_items` deviation untouched (not reached by this row — lich path RNG-matches through it); `dynamic_multi_reason` rendered as static `'paralyzed by a monster'` (`mcastu.c` precedent — surfaces only if the hero dies mid-paralysis); rest of `mhitm_adtyping` (ACID/DGST/...) still defaults to 0.
**Next:** (1) screen-only residual scen-wish-Monk-92013 step 75 (lich cold arm — gate path byte-identical both sides incl. RNG): measured C frames show step 75 `The lich touches you!  You avoid harm.--More--` (key `.`) gating step 76 `The lich points all around, then curses.` (key space) — C `topl.c` putstr mores on the 3rd message (39+3+42=84 ≥ CO−8=72); JS prints the same text with no `--More--`. Control: Tourist-92003 PASSED with `--More--`, so JS `more()` fires — the lich gap is display putstr more-timing/turn-flow, NOT the gate; needs its own display iteration, not another gate port. (2) the five later owners above are each their own future row.
## 2026-09-07 — D-2004 read.c create_particular_creation uniqueness gate: cant_revive force prompt + doppelganger newcham

**C locus:** `read.c:3260–3274` (`firstchoice = d->which`, then `cant_revive(&d->which, FALSE, NULL)` remaps and — unless the request was long worm tail — `Sprintf("Creating %s instead; force %s?", mons[d->which].pmnames[NEUTRAL], mons[firstchoice].pmnames[NEUTRAL])` + `y_n` override restoring `d->which = firstchoice` on `'y'`), `:3350–3354` (doppelganger `newcham(mtmp, &mons[firstchoice], NO_NC_FLAGS)` fixup); `cant_revive` itself `read.c:3111–3134` (guard / create-shopkeeper / clerics / angel → zombie; worm tail → worm; `unique_corpstat` → doppelganger).
**JS:** 1 file (`read.js` +40/−14), under the 600/10 caps. Small diff because the C arm is small (`:3260–3274` + `:3350–3354`, ~20 lines; D-2000/D-2003 precedent).
**Change:** `js/read.js` — `firstchoice = d.which`, `{ mtype }` box through live `cant_revive` (`./zap.js`, same edge as `resist`) with the C `firstchoice !== PM_LONG_WORM_TAIL` prompt exemption (local `monsterNames.indexOf` const, `PM_WIZARD` idiom), `await y_n('Creating … instead; force …?')` (`./getline.js`, same edge as `getlin`; steed.js D-2000 precedent) restoring on `'y'`, `d.which` rewritten like C's `*mtype` out-param; loop-end `await newcham(mtmp, mons(firstchoice), NO_NC_FLAGS)` (`./makemon.js` same edge; mhitm.js:3764 await precedent) under the exact C triple condition (`cham !== NON_PM`, safe: `makemon` inits `cham = NON_PM`). Same-edge import extensions only (`cant_revive`, `y_n`, `newcham`, `NO_NC_FLAGS`); header omission comments updated.
**Verify:** `node scripts/verify.mjs --fn create_particular_creation` → `PASS syntax 1 changed js file(s): js/read.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify create_particular_creation: 0 PASS, 10 moved past, 0 unchanged, 0 worse → PROGRESS` (death-Valkyrie-92229 → rndcurse@22 was 14; genesis-Barbarian-91118 → newmonhp@83 was 82; genesis-Knight-92149 → newmonhp@8 was 7; genesis-Samurai-92080 → save_dungeon@35 was 13; genesis-Valkyrie-92074 → disclose@70 was 53; poly-Rogue-92026 → passive@102 was 99; wish-Knight-92072 → next_ident@138 was 106; wish-Monk-92063 → castmu@124 was 92; wish-Tourist-92067 → newmonhp@133 was 132; wish-Wizard-92060 → tipcontainer_gettarget@55 was 38) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** rest of `create_particular_creation` stays deferred per D-2001 (quan-limit, randmonst/monclass, invisible/saddled/sleeping/hidden post-flags incl. `flash_mon`, tame/peaceful/hostile) — none reached by the 10 moved sessions at their new owners.
**Next:** the ten later owners are each their own future row (nearest: `rndcurse`, `newmonhp` ×3, `save_dungeon`, `disclose`, `passive`, `next_ident`, `castmu`, `tipcontainer_gettarget`); pop the next Open row (`uhitm.c` mhitm_mgc_atk_negated).
## 2026-09-07 — D-2003 read.c create_particular_parse gender-term search: bare strstri, no leading pad

**C locus:** `read.c:3186–3195` — `strstri(bufp, "female ")` then `strstri(bufp, "male ")` (bare case-insensitive substring; female first), each hit blanked with `memset(tmpp, ' ', sizeof "… " - 1)` (7 / 5 chars in place), then `mungspaces`.
**JS:** 1 file (`read.js` +10/−10), under the 600/10 caps. No new imports.
**Change:** `js/read.js` — drop the pad (`asciiLow` is now plain ASCII lower), search bare `'female '` / `'male '` female-first, and blank exactly the hit width in place (7 / 5 spaces, length-preserving like `memset`); re-`mungspaces` unchanged. Comment rewritten to state the bare-`strstri` rationale.
**Verify:** `node scripts/verify.mjs --fn create_particular_parse` → `PASS syntax 1 changed js file(s): js/read.js` · `PASS rule2` · `note hidden verify create_particular_parse: no corpus session is blocked on it at HEAD` (vacuous — NOT claimed as a corpus PASS; the row cites no corpus blocks, review 971: "no corpus session affected") · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`; differential probe `/tmp/d2003-probe.mjs` (hunk logic vs a C-model `strstri`+`memset` written from the C body): 18/18 match on fem + munged string + pre-munge length, incl. the queue probe `shemale elf-lord` → fem=0/MALE `she elf-lord`, plus the four D-2001 cases (`female elf-lord`→F, `male elf-lady`→M, `malebranche` untouched, trailing `dwarf female` no-hit). Final verify ran after the last edit (no D-1831 gap).
**Named:** none new — rest of `create_particular_parse`/`creation` stays deferred per D-2001 (quan, saddled/sleeping/invisible/hidden, tame/peaceful/hostile, `*`/random, class letters); the `read.c create_particular_creation` uniqueness Open row stays live for its own cause.
**Next:** pop the next Open row (`read.c` create_particular_creation uniqueness gate).
## 2026-09-07 — Audit reviews 966–972 (D-1996…D-2002) + cadence score 44/44

**Scope:** 7 JS-touching SHAs since review 965 (`ea1f4401`…`4a48e698`), one file per SHA written as each finished. Every D-log corpus claim re-measured via `hidden-proxy verify <fn> --base HASH~1` — all 7 reproduce (PROGRESS, 0 worse): from_what N/A (Must-fix row, honestly marked vacuous); one_characteristic 2 PASS + 3 moved; wiz_intrinsic 9 moved; set_apparxy 8 moved; doride 3 PASS + 4 moved; next_ident 3 moved + 3 unchanged; pick_lock 1 PASS + 5 moved.
**Verdicts:** 6 ACCEPT (966 DEAF import, 967 resistance catalogue, 968 wiz_intrinsic, 969 set_apparxy/Invis, 970 doride force-mount, 972 pick_lock), 1 QUALITY-RISK (971 gendered-name: leading-space pad deviates from C bare `strstri` — `shemale …` misses MALE in JS, hits in C; replica-probed) → 1 Must-fix prepended, Next cluster set to it. Queue 1 Must-fix + 11 Open = 12, no refill. Nits noted, no action: 969 D-log "five Invis clones" (sym shows 4), 972 Blind() is clone #30 of the 29-file idiom (matched to C, consistent), 972 !IS_DOOR LEARNED return pre-exists (map-named; falsifier recorded in review).
**Score:** full `sessions` 44/44 (D-1996 restored seed0002), Scr 11,405/11,405, RNG 792,838/792,838, speed 57+0.37/turn. No js/ edits.
**Next:** pop the 971 Must-fix first (bare-substring gender search, one line), then Open head.
## 2026-09-07 — D-2002 lock.c pick_lock direction-arm occupied square (kitten/"would appreciate that")

**C locus:** `lock.c:547–550` (pit rim → `You_cant reach over the edge`, DID_NOTHING), `:552–570` (`m_at` + `canseemon` + non-furniture/non-object apparition → credit-card shk/Oracle `verbalize("No checks, no credit, no problem.")`, else `pline("I don't think %s would appreciate that.", mon_nam)`; LEARNED_SOMETHING), `:571–575` (`is_door_mappear`, `monst.h:240` M_AP_FURNITURE S_hcdoor/S_vcdoor → `stumble_onto_mimic` + `maybe_absorb_item` 50%/10%), `:576–590` (!IS_DOOR → `update_mapseen_for` + `feel_location`, Blind feel/see, drawbridge `no lock` message, DID_NOTHING unless glyph/lastseentyp changed).
**JS:** 1 file (`lock.js` +55/−4), under the 600/10 caps.
**Change:** `js/lock.js` — pit gate, visible-monster arm (`mon_nam` + credit-card shk/Oracle `SetVoice`/`verbalize`), door-mimic reveal (`stumble_onto_mimic`), !IS_DOOR feel/mapseen + Blind feel/see + drawbridge message, in exact C order. Imports: same-edge extensions (`verbalize`/`feel_location` from `./display.js`; `S_hcdoor`/`S_vcdoor` from `./const.js`; `monsterNames` from `./generated/monsters_data.js` + `PM_ORACLE` indexOf const per the sounds.js idiom) + new edges each `--can` SAFE (hoisted functions): `mon_nam` (`./do_name.js`), `SetVoice` (`./sndprocs.js`, no cycle), `stumble_onto_mimic` (`./uhitm.js`), `update_mapseen_for` (`./dungeon.js`), `is_drawbridge_wall` (`./dbridge.js`); local `Blind()` per the apply.js youprop-macro idiom.
**Verify:** `node scripts/verify.mjs --fn pick_lock` → `PASS syntax 1 changed js file(s): js/lock.js` · `PASS rule2` · `PASS hidden verify pick_lock: 1 PASS, 5 moved past, 0 unchanged, 0 worse → PROGRESS` (kit-Rogue-92225 → use_container@22 was 20; kit-Tourist-91126 → dowield@8 was 6; wish-Barbarian-92102 → start_corpse_timeout@39 was 16; wish-Caveman-92174 → do_play_instrument@106 was 42; wish-Monk-92039 PASS; wish-Rogue-92019 → doread@149 was 108) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`; plus `node scripts/verify.mjs --full` → `PASS full 44/44 passing` (turn accounting held). Final verify ran after the last edit (no D-1831 gap).
**Named:** `maybe_absorb_item` (mimic keeps the pick 50%/10%) — no JS port; !IS_DOOR return stays LEARNED_SOMETHING because C's DID_NOTHING half turns on `lev->glyph`, which JS cells don't model (`js/game.js` has no glyph field; the lastseentyp half is provably stable since only recalc/mimic-display/bones write it) — coded with a cite comment; the `use_pick_axe` digging path (`js/dig.js`) untouched, the corpus owner on all six sessions is `pick_lock:567`.
**Next:** the five residuals moved to later owners (`use_container`, `dowield`, `start_corpse_timeout`, `do_play_instrument`, `doread`) — each its own cause for the refill queue; pop the next Open row.
## 2026-09-07 — D-2001 read.c create_particular ^G gendered-name MM_MALE/MM_FEMALE flags (elf-lord/Elvenking RNG)

**C locus:** `read.c:3186–3195` (`create_particular_parse` blanks explicit "female "/"male " terms — female first to avoid the mid-word "male" hit — before `mungspaces`), `:3212` (`name_to_mon(bufp, &gender_name_var)`; 5.0 gendered `pmnames[]`: `NAMS("elf-lord","elf-lady","elf-noble")`, `NAMS("Elvenking","Elvenqueen","elven monarch")` in `monsters.h`), `:3220–3229` (explicit-vs-name merge into `fem`/`genderconf`), and `create_particular_creation :3284–3305` (`MM_FEMALE`/`MM_MALE` when the type is not gender-fixed, `MM_NOEXCLAM` only when `genderconf == -1`; on conflict the explicit term wins with no NOEXCLAM). With the flag, `makemon :1262–1266` takes the `MM_MALE`/`MM_FEMALE` arm with NO draw. Refines the queue hypothesis: not a reordering — JS drew a SPURIOUS `rn2(2)` gender roll (C draws nothing for a forced gender), shifting every later draw by one. Proven C-side: the recorded C step-34 log goes `rnd(2) @ next_ident` (m_id) → `d(7,8) @ newmonhp` → straight to `rn2(2) @ m_initweap:227` with no `makemon` gender draw.
**JS:** 1 file (`read.js` +71/−10), under the 600/10 caps.
**Change:** `js/read.js` — parse: case-insensitive "female "/"male " blanking (female first; ASCII-only lower so byte indices align; leading-pad-only search so a trailing word does not hit, matching C `strstri` needing a literal trailing space), re-`mungspaces`, `name_to_mon(bufp, {gender})` init NEUTRAL, C-merge; creation: per-C `MM_FEMALE`/`MM_MALE` (+ `MM_NOEXCLAM` only when `genderconf == -1`, explicit-wins otherwise). Same-edge import extensions only (`MM_MALE`/`MM_FEMALE` from `./const.js`; `MALE`/`FEMALE`/`is_male`/`is_female` from `./monsters.js`; `NEUTRAL`/`name_to_mon` already imported). Blanking edge cases probed in node (`female elf-lord`→F, `male elf-lady`→M, `malebranche` untouched, trailing `dwarf female` no-hit per C).
**Verify:** `node scripts/verify.mjs --fn next_ident` → `PASS syntax 1 changed js file(s): js/read.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify next_ident: 0 PASS, 3 moved past (1 still next_ident at a later step), 3 unchanged, 0 worse → PROGRESS` (genesis-92175 → wishymatch@65 was 34; wish-92130 → next_ident@90 was 66, triaged: step 90 types "mold"+Enter, an object-wish `mksobj_init`/`makewish` path, not monster gender; wish-Tourist-91125 → do_statusline2@82 was 80) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** the rest of `create_particular_parse`/`creation` stays deferred (quan digit prefix + QUAN_LIMIT, saddled/sleeping/invisible/hidden, tame/peaceful/hostile, `*`/random, `name_to_monclass` class letters, `cant_revive` force prompt, doppelganger `newcham` fixup) — the `read.c create_particular_creation` uniqueness Open row stays live for its own cause.
**Next:** residuals still blocked on `next_ident` are each their own cause (tour-92103 step 59 `m_initinv` with no C gender draw and a non-^G key — not this arm; wish-92132 step 197 gem-wish object path; poly-92021 step 157 mid-movement) — re-queue `next_ident` for those three if the refill reports it; pop the next Open row (`lock.c` pick_lock).
## 2026-09-07 — D-2000 steed.c doride wizard "Force the mount to succeed?" yn arm

**C locus:** `steed.c:177–193` `doride` — `:185` `if (wizard && y_n("Force the mount to succeed?") == 'y') forcemount = TRUE;` with `wizard ≡ flags.debug` (`flag.h:30`), then `mount_steed(m_at(u.ux + u.dx, u.uy + u.dy), forcemount)`.
**JS:** 1 file (`steed.js` +6/−2), under the 600/10 caps.
**Change:** `js/steed.js` — static `import { y_n } from './getline.js'` (hoisted function, cycle-safe per `imports.mjs --can`; `mhitu.js` precedent) + `let forcemount` set by `(game.flags?.debug || game.flags?.wizard) && (await y_n('Force the mount to succeed?')) === 'y'`, preserving the C short-circuit (no prompt in normal mode), wired into `mount_steed(mtmp, forcemount)`.
**Verify:** `node scripts/verify.mjs --fn doride` → `PASS syntax 1 changed js file(s): js/steed.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify doride: 3 PASS, 4 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-intrinsic-Ranger-92193 PASS; scen-kit-Valkyrie-92131 PASS; scen-normal-Healer-92231 PASS — all three named probes; scen-normal-Priest-92020 → monster_detect@58 was 39; scen-normal-Valkyrie-92200 → doquiver_core@46 was 35; scen-tour-Barbarian-92079 → attributes_enlightenment@57 was 47; scen-wish-Barbarian-92054 → yn_function@111 was 27) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`.
**Named:** none new — `doride` is now complete vs C; `mount_steed`'s remaining deferred arms keep their existing envelope comment.
**Next:** the 4 later owners are each their own future row (nearest: `monster_detect`, `doquiver_core`, `attributes_enlightenment`, `yn_function`); pop the next Open row (`makemon.c` next_ident order + monster gender).
## 2026-09-07 — D-1999 monmove.c set_apparxy: makemon byyou call + live Invis macro

**C locus:** `makemon.c:1391–1396` (`!in_mklev && byyou` → `newsym` + `set_apparxy` BEFORE `m_initweap`/`m_initinv` `:1441–1445`); `monmove.c:2223` (`notseen = !mcansee || (Invis && !perceives)`); `monmove.c:1865–1866` (`should_see && Invis && !perceives && rn2(11)` zeroes `appr`); `youprop.h:198` (`#define Invis ((HInvis || EInvis) && !BInvis)` — a live macro, no flat).
**JS:** 3 files (`makemon.js` +4/−1, `monmove.js` +8/−4, `timeout.js` +5/−1 comment+export), ~17 insertions, under the 600/10 caps.
**Change:** `js/makemon.js` — `set_apparxy(mtmp)` after `newsym` in the `byyou` branch (C `:1393–1394` cite; import already existed, no new edge). `js/timeout.js` — `export` the existing C-cited `Invis()` (no new clone; five local `Invis` clones already exist elsewhere). `js/monmove.js` — `import { Invis } from './timeout.js'` (same 90-module SCC, runtime-only call, no TDZ) + live `Invis()` in `set_apparxy` notseen (`:2223`) and `m_move` appr (`:1866`), shadowing flats removed.
**Verify:** `node scripts/verify.mjs --fn set_apparxy` → `PASS syntax 3 changed js file(s)` · `PASS rule2` · `PASS hidden verify set_apparxy: 0 PASS, 8 moved past, 0 unchanged, 0 worse → PROGRESS` (91139 → one_characteristic@54 was 32; 92073 → one_characteristic@73 was 29; 92126 → x_monnam@38 was 7; 92151 → one_characteristic@84 was 50; Healer-92147 → wildmiss@186 was 96; 91103 → gcrownu@79 was 70; 92155 → mhitm_mgc_atk_negated@144 was 112; 92212 → exercise@66 was 64) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared file changed) · `VERIFY: PASS`. Mid-iteration the makemon hunk alone moved 7/8 (Healer-92147 unchanged at 96); the live-`Invis` hunks moved it to wildmiss@186 with no movement lost elsewhere.
**Named:** other stale `u.Invis`-flat readers unchanged (`mon.js:2403`, `mhitu.js:216/1405/2314`, `uhitm.js:1960`, `shk.js:3742/3846`, `do_wear.js:827`, `pager.js:386`) — own future row if the corpus blocks on them; remaining C `set_apparxy` call sites not in JS (`dogmove.c:1354`, `mhitu.c:593`, `mon.c:5525`, `dothrow.c:1017`, `uhitm.c:5396/5411`, `dog.c:450`) — same; `m_move` appr `uundetected`/mappear/likes_gold/stalker arms untouched.
**Next:** the 8 later owners are each their own future row (nearest: `one_characteristic`×3, `x_monnam`, `wildmiss@186`, `gcrownu`, `mhitm_mgc_atk_negated@144`, `exercise`, `m_move`); pop the next Open row (`steed.c` doride/mount_steed).
## 2026-09-07 — D-1998 wizcmds.c wiz_intrinsic SICK/STONED/STUNNED/VOMITING/WARN_OF_MON/GLIB arms + float/rescham/pool tail

**C locus:** `wizcmds.c:948–1096` `wiz_intrinsic` — SICK `:1035–1038` (`typ = !rn2(2) ? SICK_VOMITABLE : SICK_NONVOMITABLE; make_sick(newtimeout, wizintrinsic, TRUE, typ)`); STONED `:1044–1047` (`make_stoned(newtimeout, buf, KILLED_BY, wizintrinsic)`); STUNNED `:1049–1051` (`make_stunned(newtimeout, TRUE)`); VOMITING `:1053–1057` (`make_vomiting(newtimeout, FALSE); pline1(buf)`); WARN_OF_MON `:1059–1066` (grid-bug default species + `goto def_feedback`); GLIB `:1067–1072` (`make_glib` + FALLTHROUGH, no `incr_itimeout`); default `:1073–1079` (covers CONFUSION — its `make_confused` case is `#if 0`'d out `:1023–1028`); tail `:1080–1087` (`float_vs_flight` / `rescham` / `pooleffects(FALSE)` when `u.uinwater`). Callee semantics confirmed in `potion.c` (`make_sick :137`, `make_stoned :222`, `make_vomiting :243`, `make_glib :461`) and `youprop.h` (`Sick`/`Stoned`/`Vomiting`/`Glib`/`Slimed ≡ uprops[].intrinsic`, `Stunned ≡ HStun`, `Warn_of_mon ≡ HWarn||EWarn`).
**JS:** `js/wizcmds.js` (+97/−19), 1 file, under the 600/10 caps.
**Change:** `js/wizcmds.js` — per-prop switch in exact C order with `:line` citations: SICK `!rn2(2) ? SICK_VOMITABLE : SICK_NONVOMITABLE` + `make_sick(newtimeout, '#wizintrinsic', true, typ)`; STONED `You are[ still] turning into stone.` + `make_stoned(newtimeout, buf, KILLED_BY, '#wizintrinsic')`; STUNNED `make_stunned(newtimeout, true)`; VOMITING `make_vomiting(newtimeout, false)` (silent, talk=FALSE) then `pline(buf)`; WARN_OF_MON grid-bug default (`game.context.warntype.{speciesidx,species} = PM_GRID_BUG, mons(…)`, artifact.js shape) when `!Warn_of_mon()` then default incr+pline; GLIB sync `make_glib(newtimeout|0)` + Timeout pline with no incr; default (incl. CONFUSION) incr+pline; post-arm tail (`float_vs_flight()` for LEVITATION/FLYING, `await rescham()` for PROT_FROM_SHAPE_CHANGERS, `await pooleffects(false)` for WWALKING/LEVITATION/FLYING when `u.uinwater`).
**Verify:** `node scripts/verify.mjs --fn wiz_intrinsic` → `PASS syntax 1 changed js file(s): js/wizcmds.js` · `PASS rule2` · `PASS hidden verify wiz_intrinsic: 0 PASS, 9 moved past, 0 unchanged, 0 worse → PROGRESS` (Archeologist-92015 → unattributed screen row at step 42, was 16; Knight-92203 → trapmove@85; Monk-92123 → trapmove@39; Barbarian-92008 → regen_hp@33; Caveman-92070 → trapmove@38; Priest-91130 → cmd_safety_prevention@20; Priest-92096 → cmd_safety_prevention@64, was 62; Rogue-91111 → sickness_dialogue@65, was 53; Samurai-92239 → rnd_otyp_by_namedesc@92, was 44) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Second `hidden-proxy.mjs verify wiz_intrinsic` run reproduces the same 9 movers (stable). Archeologist note: the proxy prints the null-owner fallback label `js-throw`; direct replay shows `error:null, kind=screen` at step 42 — C `You kill the newt!
**Named:** count-prefix menu digits (always `DEFAULT_TIMEOUT_INCR`); non-wizard `unavailcmd`/`ecname` wording (`You can't do that.`); `make_blinded` Blindfolded/Eyes talk variants; `make_sick` KILLED_BY vs KILLED_BY_AN `#wizintrinsic` cause polish (potion.js always AN — pre-existing).
**Next:** the 9 later owners are each their own future row (nearest: `trapmove`×3, `regen_hp`, `cmd_safety_prevention`×2, `sickness_dialogue`, `rnd_otyp_by_namedesc`, Archeologist step-42 melee `--More--`); pop the next Open row (`monmove.c` set_apparxy).
## 2026-09-07 — D-1997 insight.c attributes_enlightenment resistance catalogue + misc tail (resume iter-2443 leftover, committed)

**C locus:** `insight.c:1525–1563` resistances (Fire/item-fire, Cold/item-cold, Sleep, Disint/item-disn, Shock/item-elec, Poison, Acid+`temp_resist`/item-acid, Drain, Sick, Stone+`temp_resist`, Halluc, uedibility) + `:1919–1936` moreluck/luckstone/ugangr; `eat.c:450–469` `temp_resist`; `youprop.h` H||E macros.
**JS:** `js/invent.js` (+296/−14) + `js/attrib.js` (+15/−0); 2 js files, under 600 cap. Same-idiom predicates/imports only; no DIAG/FORCE/seed gates; Rule #2 clean.
**Change:** full resistance block in exact C order in BOTH builders (final + ^X overlay), AD_FIRE/AD_COLD/AD_DISN/AD_ELEC/AD_ACID item messages, local `enl_temp_resist`, six `hero_*_resistance` predicates, moreluck/luckstone (live `carrying` dynamic import + `stone_luck`) / ugangr tail; six `PROP_HFIELD` rows + const imports (D-1995 precedent).
**Verify:** `node scripts/verify.mjs --fn one_characteristic` → VERIFY: PASS — hidden 2 PASS (scen-death-Valkyrie-92176 Cold arm + `innately`; scen-normal-Priest-92056 ugangr) · green 2/2 + strict ×2 + cohort 7/7 · `verify enlightenment` → 1 unchanged NO MOVEMENT (scen-intrinsic-Caveman-92150 step 92 `^X`: C 62 turns vs JS 61 — turn-counter value, not page order; re-queued as own moveloop-accounting row, do NOT +1) · `verify status_enlightenment` vacuous at HEAD (NOT claimed).
**Named:** Sick `defended(AD_DISE)` form arm; vision Warn/Clairvoyant/Detect/umconf; appearance/transport/physical/shape leftovers (see D-1997).
**Next:** `allmain.c` moveloop / `cmd.c` multi-turn count row (Caveman-92150), then `wizcmds.c` wiz_intrinsic sick/stone/strangle/vomit/stun arms.
## 2026-09-07 — D-1996 invent.js missing DEAF import (D-1995 deaf ^X ReferenceError)

**C locus:** `attrib.c:905–1001` `from_what` (DEAF `:931` roleplay-deaf arm); `insight.c:1059–1074` Blind/Deaf `you_are("deaf", from_what(DEAF))`.
**JS:** `js/invent.js` (+2/−1), 1 file, under the 600 cap.
**Change:** one-word addition to the existing same-edge `./const.js` import (after `BLINDED`, matching C `BLINDED=15, DEAF=16` order) with a C-citation comment. No new module edge (`const.js` already imported 3× in-file; same 82-module SCC; hoisted const, no TDZ read). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node frozen/ps_test_runner.mjs sessions/seed0002-healer-reflection-drummer.session.json` → `PASS (RNG 27158/27158, Screen 595/595)` — fortress 44/44 restored · `node scripts/verify.mjs --fn from_what` → `PASS syntax 1 changed js file(s)` · `PASS rule2` · `note hidden verify from_what: no corpus session is blocked on it at HEAD` (vacuous — this is a Must-fix review row, not a corpus-owner row; NOT claimed as a corpus PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`.
**Named:** none (import-only; D-1988 precedent — no `c-js-map` section names this binding as omitted, so no map edit).
**Next:** `insight.c` enlightenment family residuals (Open head) then `wizcmds.c` wiz_intrinsic sick/stone/strangle/vomit/stun arms (9/553; SLIMED landed D-1995 unreached).
## 2026-09-07 — Audit D-1980…D-1995 against pinned C (reviews 950–965) + cadence score (resume iter-2440 leftover)

Resumed timed-out iter-2440 (950–963 on disk): reviewed D-1994 (964) + D-1995 (965) with parent-baseline re-measures. 14 ACCEPT, 1 debt (961), 1 QUALITY-RISK (965: `from_what(DEAF)` without import → `ReferenceError`). Cadence: **43/44** (sole fail seed0002). Must-fix prepended; Next cluster set to it. No js/ edits.
## 2026-09-07 — D-1995 insight.c enlightenment family (continue iter-2438 leftover, committed)

**C locus:** `insight.c:1006–1194` `status_enlightenment` + `:398–2000` `enlightenment` + `doattributes :647` live `midnight()/night()`; `wintty.c` tty_end_menu + `tty_display_nhwindow(NHW_MENU, H2344)` corner-vs-fullscreen; `wizcmds.c:953,1040–1043` SLIMED; `attrib.c` `dwa_abil[]/gno_abil[]` + `is_innate` H-fields.
**JS:** `js/invent.js` (+396/−45) + `js/attrib.js` (+22/−1) + `js/wizcmds.js` (+18/−3); 3 js files, under 600 cap. Same-edge imports only; no DIAG/FORCE/seed gates; Rule #2 clean.
**Change:** status arms in C order with `:line` citations; resistance catalogue rows with `from_what`; `doattributes` live midnight/night + Sleep/Infravision/spell-cast rows; corner-overlay-vs-fullscreen menu via in-file `nhw_menu_geometry` (no snapshot/restore — D-1831); dwa/gno infra + SLEEP/INFRA/SEENVIS H-fields; SLIMED via live `make_slimed`.
**Verify:** `enlightenment` → 0 PASS/6 moved/1 unchanged/0 worse PROGRESS; `one_characteristic` → 5 PASS/8 moved/0 worse PROGRESS; `status_enlightenment` → 0 PASS/4 moved/0 worse PROGRESS; `wiz_intrinsic` → 9 unchanged/0 worse NO MOVEMENT (SLIMED unreached; sick/stone arms remain). Green 2/2 + strict ×2 + cohort 7/7 each run.
**Named:** non-swallow held/steed/cause_known; Cold/item-cold/half dmg; Warn_of_mon/Clairvoyant; shape/Hate_silver/Free_action/Fixed_abil; blocked-Stealth; SICK/STONED `wiz_intrinsic` arms.
**Next:** 92150 post-header enlightenment delta; 92176/92056 one_characteristic later-step deltas; pop `wizcmds.c` wiz_intrinsic row.
## 2026-09-07 — D-1994 attrib.c exercise residual: exerper Clairvoyant/Regen/Monk arms + makeknown-credit caller arms (Gloves/Amulet/addinv/spell)

**C locus:** `attrib.c:520–584` `exerper` — SATIATED `:534–536` (+Monk WIS), WEAK `:542–544` (+Monk WIS), status `:572–575` (Clairvoyant WIS / Regen STR); `o_init.c:483` `discover_object` credit arm reached via `hack.h:1530` `makeknown` from `do_wear.c:592–594`+`adj_abon :3322–3331` (DEX gauntlets), `do_wear.c:1000–1035` `Amulet_on` CHANGE, `invent.c:1025–1050` `addinv_core2` decipher; `spell.c:1517–1531` scroll-duplicate arm + `read.c:2194–2200` `seffects` head.
**JS:** `js/allmain.js` + `js/do_wear.js` + `js/polyself.js` + `js/spell.js` + `js/u_init.js` (129 insertions total), 5 files, under the 600 cap.
**Change:** `js/allmain.js` — SATIATED/WEAK Monk WIS arms (`(game.urole?.mnum|0)===PM_MONK` idiom) + every-5 H-only Clairvoyant WIS arm (flat+intrinsic, blocked vetoes) and H-only Regen STR arm, all with C `:line` comments (`PM_MONK`/`CLAIRVOYANT` extend existing same-edge imports). `js/polyself.js` — export `poly_gender` + `Unchanging` (additive). `js/do_wear.js` — Gloves DEX arm (`makeknown` when spe nonzero + `ABON(A_DEX)+=spe` + botl) and full Amulet CHANGE arm (Unchanging gate, `change_sex`, `makeknown`-if-changed, on_msg-in-arm, newsym/botl/message, disintegrate + `trycall` + `useup`; `livelog_newform` log-only omit).
**Verify:** `node scripts/verify.mjs --fn exercise` → `PASS syntax 5 changed js file(s)` · `PASS rule2` · `PASS hidden verify exercise: 0 PASS, 7 moved past, 0 unchanged, 0 worse → PROGRESS` (92084 → `regen_hp`@64 was 48; 92124 → `were_change`@69; 92158 → unattributed map/menu row-6 `####` screen@49 — `js-throw` label is the null-owner fallback, worker `error:null`, all 3170 RNG matched; 92007 → `name_to_monclass`@66; 92139 → `lesshungry`@28; 92216 → `mcalcmove`@180; 92206 → `mhitm_knockback`@220) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)`.
**Named:** `exercise()` STR/CON `encumber_msg` tail (sync→async ripple; same standing omit as `adjattrib` `js/attrib.js:541`); `addinv_core2` luck `set_moreluck` (no JS Luck recompute); Amulet CHANGE `livelog_newform` (log-only); `exerper` Sick/Vomiting full timeout bodies (unchanged).
**Next:** 92158's unattributed map/menu `####`-wall row-6 screen@49 is a geometry-owner candidate (`geom-probe.mjs` first); 92124 `were_change`, 92007 `name_to_monclass`, 92139 `lesshungry` move to their natural owner rows; queue refill per `check-hot-docs` if Must-fix+Open < 8.
