# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-07 — D-1996 invent.js missing DEAF import (D-1995 deaf ^X ReferenceError)

**C locus:** `attrib.c:905–1001` `from_what` (DEAF `:931` roleplay-deaf arm); `insight.c:1059–1074` Blind/Deaf `you_are("deaf", from_what(DEAF))`.
**JS:** `js/invent.js` (+2/−1), 1 file, under the 600 cap.
**Change:** one-word addition to the existing same-edge `./const.js` import (after `BLINDED`, matching C `BLINDED=15, DEAF=16` order) with a C-citation comment. No new module edge (`const.js` already imported 3× in-file; same 82-module SCC; hoisted const, no TDZ read). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node frozen/ps_test_runner.mjs sessions/seed0002-healer-reflection-drummer.session.json` → `PASS (RNG 27158/27158, Screen 595/595)` — fortress 44/44 restored · `node scripts/verify.mjs --fn from_what` → `PASS syntax 1 changed js file(s)` · `PASS rule2` · `note hidden verify from_what: no corpus session is blocked on it at HEAD` (vacuous — this is a Must-fix review row, not a corpus-owner row; NOT claimed as a corpus PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`.
**Named:** none (import-only; D-1988 precedent — no `c-js-map` section names this binding as omitted, so no map edit).
**Next:** `insight.c` enlightenment family residuals (Open head) then `wizcmds.c` wiz_intrinsic sick/stone/strangle/vomit/stun arms (9/553; SLIMED landed D-1995 unreached).
## 2026-09-07 — Audit D-1980…D-1995 against pinned C (reviews 950–965) + cadence score (resume iter-2440 leftover)

Resumed timed-out iter-2440 (950–963 on disk): reviewed D-1994 (964) + D-1995 (965) with parent-baseline re-measures. 14 ACCEPT, 1 debt (961), 1 QUALITY-RISK (965: `from_what(DEAF)` without import → `ReferenceError`). Cadence: **43/44** (sole fail seed0002). Must-fix prepended; Next cluster set to it. No js/ edits.
## 2026-09-07 — D-1995 insight.c enlightenment family (continue iter-2438 leftover, committed)

**C locus:** `insight.c:1006–1194` `status_enlightenment` + `:398–2000` `enlightenment` + `doattributes :647` live `midnight()/night()`; `wintty.c` tty_end_menu + `tty_display_nhwindow(NHW_MENU, H2344)` corner-vs-fullscreen; `wizcmds.c:953,1040–1043` SLIMED; `attrib.c` `dwa_abil[]/gno_abil[]` + `is_innate` H-fields.
**JS:** `js/invent.js` (+396/−45) + `js/attrib.js` (+22/−1) + `js/wizcmds.js` (+18/−3); 3 js files, under 600 cap. Same-edge imports only; no DIAG/FORCE/seed gates; Rule #2 clean.
**Change:** status arms in C order with `:line` citations; resistance catalogue rows with `from_what`; `doattributes` live midnight/night + Sleep/Infravision/spell-cast rows; corner-overlay-vs-fullscreen menu via in-file `nhw_menu_geometry` (no snapshot/restore — D-1831); dwa/gno infra + SLEEP/INFRA/SEENVIS H-fields; SLIMED via live `make_slimed`.
**Verify:** `enlightenment` → 0 PASS/6 moved/1 unchanged/0 worse PROGRESS; `one_characteristic` → 5 PASS/8 moved/0 worse PROGRESS; `status_enlightenment` → 0 PASS/4 moved/0 worse PROGRESS; `wiz_intrinsic` → 9 unchanged/0 worse NO MOVEMENT (SLIMED unreached; sick/stone arms remain). Green 2/2 + strict ×2 + cohort 7/7 each run.
**Named:** non-swallow held/steed/cause_known; Cold/item-cold/half dmg; Warn_of_mon/Clairvoyant; shape/Hate_silver/Free_action/Fixed_abil; blocked-Stealth; SICK/STONED `wiz_intrinsic` arms.
**Next:** 92150 post-header enlightenment delta; 92176/92056 one_characteristic later-step deltas; pop `wizcmds.c` wiz_intrinsic row.
## 2026-09-07 — D-1994 attrib.c exercise residual: exerper Clairvoyant/Regen/Monk arms + makeknown-credit caller arms (Gloves/Amulet/addinv/spell)

**C locus:** `attrib.c:520–584` `exerper` — SATIATED `:534–536` (+Monk WIS), WEAK `:542–544` (+Monk WIS), status `:572–575` (Clairvoyant WIS / Regen STR); `o_init.c:483` `discover_object` credit arm reached via `hack.h:1530` `makeknown` from `do_wear.c:592–594`+`adj_abon :3322–3331` (DEX gauntlets), `do_wear.c:1000–1035` `Amulet_on` CHANGE, `invent.c:1025–1050` `addinv_core2` decipher; `spell.c:1517–1531` scroll-duplicate arm + `read.c:2194–2200` `seffects` head.
**JS:** `js/allmain.js` + `js/do_wear.js` + `js/polyself.js` + `js/spell.js` + `js/u_init.js` (129 insertions total), 5 files, under the 600 cap.
**Change:** `js/allmain.js` — SATIATED/WEAK Monk WIS arms (`(game.urole?.mnum|0)===PM_MONK` idiom) + every-5 H-only Clairvoyant WIS arm (flat+intrinsic, blocked vetoes) and H-only Regen STR arm, all with C `:line` comments (`PM_MONK`/`CLAIRVOYANT` extend existing same-edge imports). `js/polyself.js` — export `poly_gender` + `Unchanging` (additive). `js/do_wear.js` — Gloves DEX arm (`makeknown` when spe nonzero + `ABON(A_DEX)+=spe` + botl) and full Amulet CHANGE arm (Unchanging gate, `change_sex`, `makeknown`-if-changed, on_msg-in-arm, newsym/botl/message, disintegrate + `trycall` + `useup`; `livelog_newform` log-only omit).
**Verify:** `node scripts/verify.mjs --fn exercise` → `PASS syntax 5 changed js file(s)` · `PASS rule2` · `PASS hidden verify exercise: 0 PASS, 7 moved past, 0 unchanged, 0 worse → PROGRESS` (92084 → `regen_hp`@64 was 48; 92124 → `were_change`@69; 92158 → unattributed map/menu row-6 `####` screen@49 — `js-throw` label is the null-owner fallback, worker `error:null`, all 3170 RNG matched; 92007 → `name_to_monclass`@66; 92139 → `lesshungry`@28; 92216 → `mcalcmove`@180; 92206 → `mhitm_knockback`@220) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)`.
**Named:** `exercise()` STR/CON `encumber_msg` tail (sync→async ripple; same standing omit as `adjattrib` `js/attrib.js:541`); `addinv_core2` luck `set_moreluck` (no JS Luck recompute); Amulet CHANGE `livelog_newform` (log-only); `exerper` Sick/Vomiting full timeout bodies (unchanged).
**Next:** 92158's unattributed map/menu `####`-wall row-6 screen@49 is a geometry-owner candidate (`geom-probe.mjs` first); 92124 `were_change`, 92007 `name_to_monclass`, 92139 `lesshungry` move to their natural owner rows; queue refill per `check-hot-docs` if Must-fix+Open < 8.
## 2026-09-07 — D-1993 mhitu.c gulpmu wires the DGST/PHYS/ACID exercise(A_STR,FALSE) arms

**C locus:** `mhitu.c:1421–1469` `gulpmu` switch — DGST else `:1434`, PHYS else `:1452`, ACID else `:1467`, each `exercise(A_STR, FALSE)` immediately after its pline.
**JS:** `js/mhitu.js` (+6/−0), 1 file, under the 600 cap (C is three one-line arms; density floor waived).
**Change:** three one-line `exercise(A_STR, false)` insertions with C `:line` comments, each directly after its C pline. `exercise`/`A_STR` already imported and used in-file (same module, live callee, no new edge). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn exercise` → `PASS syntax 1 changed js file(s)` · `PASS rule2` · `PASS hidden verify exercise: 1 PASS, 1 moved past, 7 unchanged, 0 worse → PROGRESS` (Samurai-92083 PASS, was exercise@49; Samurai-92110 → `one_characteristic`@68, was exercise@61) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** none new — DGST `Slow_digestion` early-out (`:1422–1425`) and total-digest `Half_physical_damage tmp *= 2` (`:1428–1430`) stay named in the `gulpmu` doc comment (`js/mhitu.js:1523–1525`, damage-only, no corpus session blocked on them).
**Next:** residual `attrib.c` exercise row (7 sessions, non-gulpmu mechanisms — exerper-tick/moves-drift class plus read/wish/inc-arm callers); 92110's new owner feeds the queued `enlightenment` row.
## 2026-09-06 — D-1992 polyself.c drop_weapon full port: live weapon_descr/is_sword names, gone-function drops, could_twoweap arm

**C locus:** `polyself.c:1305–1362` `drop_weapon` — `:1313` !alone||cantwield gate; `:1316–1317` canletgo pair (`do.c:664–711`); `:1320–1332` alone message (`obj.h:223–226` is_sword → "sword" else `weapon.c:87–142` weapon_descr, twoweap whichtoo `strcmp`, quan/twoweap `makeplural`, `the_your` corpse gate); `:1334–1353` swap-then-main `uswapwepgone`/`uwepgone` (`wield.c:872–894`) + `in_use` defer (`updateinv`) + `update_inventory`; `:1354–1356` `!could_twoweap` (`mondata.h:129–132`) → `untwoweapon` (`wield.c:905–914`).
**JS:** `js/polyself.js` (+50/−58), `js/wield.js` (+3/−2) — under the 600 cap, 2 files.
**Change:** deleted the clone; message via live `is_sword` (`./objects.js`) + `weapon_descr` (`./invent.js`) + live `makeplural` (`./objnam.js`) with C `:line` comments; drops via live sync `uswapwepgone()` + async `uwepgone()` with the `updateinv` flag and `update_inventory()` tail; `else if (!could_twoweap(...)) await untwoweapon()`; one-line C-fidelity fix in `untwoweapon` (the deferred `update_inventory()` per `:905–914`). All four import edges already existed (`imports.mjs --can` ALREADY on all four; same 82-module SCC, no new edge). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn drop_weapon` → `PASS syntax 2 changed js file(s)` · `PASS rule2` · `PASS hidden verify drop_weapon: 0 PASS, 7 moved past (2 still drop_weapon at a later step), 2 unchanged, 0 worse → PROGRESS` (Caveman-92202 → `encumber_msg`@98, Priest-92021 → `next_ident`@157, Ranger-91131 → `uunstick`@207, Ranger-92090 → `regen_hp`@160, Ranger-92217 → `polymon`@127, Ranger-92049 → `drop_weapon`@85 was 82, Ranger-92133 → `drop_weapon`@91 was 90; Valkyrie-92195 still @107, Wizard-92076 still @84 — drop_weapon text byte-exact on all four residuals, cause pinned in Next) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** `weapon_descr` P_NONE specials (corpse/tin/egg/statue/boulder/towel/opener/glob) + sling/bow/crossbow-ammo + flail-hook/mattock arms stay named in `invent.js` (live callee, C-matched on the skill-name path these sessions take); C `canletgo(uswapwep)` NULL deref unreachable in practice (twoweap implies set — JS `canletgo` null-guards FALSE); dropp-vs-dropx Heart-of-Ahriman note lives in `dropx` (`do.js`).
**Next:** two diagnosed follow-up Open rows queued from this port's residuals (same `scen-poly-*` falsifier, writers pinned with C-side evidence): `polymon` verbose-tip block (`:1046–1070` — Valkyrie-92195@108 + Ranger-92049@86 `Use the command #sit to lay an egg.`, Wizard-92076@85 `Use the command #monster to hide.`; JS `polymon` has the breath tip only) and `surface()` stairs arm (`break_armor :1267` helm fall — Ranger-92133@91 `falls to the stairs` vs `floor` with matching screens/positions).
## 2026-09-06 — D-1991 polyself.c break_armor full port: breakarm destroy order, cloak 3-way names, horns/eyewear arms

**C locus:** `polyself.c:1157–1302` `break_armor` — breakarm uarm `:1162–1176` (`end_burn` lamplit, `exercise`, `Armor_gone`, `useup` destroy), cloak 3-way `:1177–1195` (wrapping tears + `useup` / smock knot + `dropp` / clasp + `dropp`, all `cloak_simple_name` + `Cloak_off`), shirt `:1196–1199` (`useup`, no `_off`), sliparm uarm `:1201–1211` (`racial_exception < 1`, `Armor_gone`, `dropp`), sliparm cloak/shirt `:1212–1228` (wrapping gate, whirly variants), horns `:1230–1251` (flimsy pierce via `num_horns`/`vtense`/`yname`, else `helm_simple_name` + `surface()` fall), nohands gloves/shield/helm `:1253–1276`, boots `:1278–1292`, ublindf `:1294–1307` (`simpleonames` pair-of strip, `Blindf_off(Null)` skips off-msg per `do_wear.c:1498`); `mondata.c:640–650` `breakarm` marilith/winged-gargoyle cases.
**JS:** `js/polyself.js` (+~100/−~40), `js/do_wear.js` (+5/−1 `nooffmsg`), `js/worn.js` + `js/mhitu.js` (4 one-word exports) — under the 600 cap, 4 files.
**Change:** exact-C arm order with `:line` citations — `end_burn` (new `./timeout.js` edge) + `await Armor_gone()` + `useup` (same-module `./invent.js`) for breakarm uarm; cloak 3-way on `(otyp !== MUMMY_WRAPPING || !WrappingAllowed)` with `cloak_simple_name` (same-module `./do_wear.js`) + `await Cloak_off()` + `useup`/`dropx`; sliparm `racial_exception(game.youmonst, otmp) < 1` (new `./worn.js` edge); horns `is_flimsy` pierce vs `helm_simple_name` (one-word export from `./mhitu.js`) + `surface(u.ux,u.uy)` (already imported from `./sit.js`); nohands helm via the same pair; ublindf arm with `has_head` (same-module `./monsters.js`), `simpleonames`/`vtense`/`yname` (same-module `./objnam.js`) + `await Blindf_off(null)`; `Blindf_off` gains C `:1498` `nooffmsg` (all 4 existing callers pass non-null, behavior unchanged); `num_horns`/`has_horns`/`WrappingAllowed` one-word exports from `./worn.js`. New-module edges (`./worn.js`, `./mhitu.js`, `./timeout.js`) are `imports.mjs --can` SAFE/CHECK-lazy (hoisted functions, function-body use only, same SCC); `ROBE`/`MUMMY_WRAPPING`/`ALCHEMY_SMOCK`/`PM_MARILITH`/`PM_WINGED_GARGOYLE` follow the file's `objectNames`/`monsterNames.indexOf` idiom. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn break_armor` → `PASS syntax 4 changed js file(s)` · `PASS rule2` · `PASS hidden verify break_armor: 0 PASS, 10 moved past (1 re-attributed at the same step; 1 still break_armor at a later step), 0 unchanged, 0 worse → PROGRESS` (Caveman-92202 → `drop_weapon`@97, Knight-92220 → `encumber_msg`@97, Monk-92005 → `mhitm_ad_plys`@78, Priest-91137 → `do_statusline2`@86 same-step re-attribution with matching topline `The clasp on your robe breaks open!`, remaining diff is status-row `HP:120(120)` vs `HP:85(85)` — the D-1990-documented `polymon` `u.mh` misattribution class, not the painter; Priest-92021 → `drop_weapon`@142, Priest-92097 → `do_statusline1`@125, Ranger-92049/92133/92217 → `drop_weapon`@85/91/82, Tourist-92171 → `break_armor`@88 one step later) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)`.
**Named:** `donning`/`cancel_don` (`do_wear.c` locals, unwired — no corpus session polyselfs mid-don).
**Next:** `polyself.c` drop_weapon (queued Open row, same family — 4 sessions now block on its `yname` naming: Ranger-91131/92090, Valkyrie-92195 + 3 from this port). Tourist-92171 step 88 (`You drop your gloves!` matches both sides; map row 5 `@` vs `q`) is revealed latent drift, not this arm: the step-88 gloves arm is operationally identical before/after (pline + `drop_weapon(0)` + `Gloves_off` + `dropx`, no helm/eyewear present), and the step-87 + step-88 chains draw no asymmetric RNG on either side (C `useup`/`dropp`/`drop_weapon`/`canletgo` and the JS `Armor_gone`/`Cloak_off`/`useup` paths are all `rn2`-free; `exercise` is called identically) — the cell's writer is upstream of `break_armor`'s outputs.
## 2026-09-06 — D-1990 botl.c do_statusline2 ports the full cond list (Stone/Slime/Strngl/Sick before hunger) + Hallu/Fly fidelity + COLNO reorder

**C locus:** `botl.c:100–250` `do_statusline2` — pieces `:130–163` (dloc+gold, hlth `min(...,9999)`, expr HD/Xp, tmmv), cond list `:170–206` (Stone/Slime/Strngl/Sick, hunger `hu_stat`, enc, Blind/Deaf/Stun/Conf/Hallu/Lev/Fly/Ride), `showvers` `:208–211`, COLNO reorder + `mungspaces` `:212–250`; `youprop.h` Blind `(HBlinded||EBlinded)&&!BBlinded`, Deaf `H||E||uroleplay.deaf`, Stunned `HStun`, Confusion `HConfusion`, Hallucination `HHallucination && !Halluc_resistance`, Levitation `(H||E)&&!B`, Flying `(H||E||steed is_flyer)&&!B`; `hu_stat[]` (`eat.c:70`); `COLNO 80` (`global.h:382`), `MAXCO` (`botl.h:30`).
**JS:** `js/display.js` (+95/−55, one file, under the 600 cap).
**Change:** cond built in exact C order with `:line` citations — fatal four first (flat `u.Stoned/Slimed/Sick` from `make_*` OR `uprops[].intrinsic` from `#wizintrinsic incr_prop_timeout`, like `timeout.js intr_bits`; Strangled also covers the H/E flat mirrors), then hunger, enc, Blind/Deaf/Stun/Conf, `Hallucination()` (same-module helper with the resistance gate), Levitation/Flying with uprops intrinsic/extrinsic/blocked mirrors + `is_flyer(u.usteed.data)`; `suppress_map_output` guard (`:113–114`); `uen`/`uenmax` `min 9999`; gold `0…999999`; pieces assembled per the C fit/reorder chain with `dx = 0` (no `\GXXXXNNNN` encoding on this path) and `vers = ""`, overflow arms `mungspaces` (new `./getline.js` edge — `imports.mjs --can` SAFE, hoisted function, same 90-module SCC, lazy use only). Same-module import extensions only otherwise: `SICK/STONED/STRANGLED/SLIMED/SICK_VOMITABLE/SICK_NONVOMITABLE/LEVITATION/FLYING` + `is_flyer` on the existing `./const.js` / `./monsters.js` lists. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn do_statusline2` → hidden `0 PASS, 8 moved past, 4 unchanged, 1 worse → REGRESSION` (baseline HEAD scoreboard `08b77b57`, 13 blocked): all 8 strangling/intrinsic sessions move to strictly later owners (92159/92121/92191/92046/92222/92178 → `cmd_safety_prevention`@19–30, Ranger-92234 → `regen_hp`@42, Tourist-92046 → `one_characteristic`@59, Caveman-92141 → step-92 choke `You suffocate.--More--` topline timing with null owner — the `js-throw` label on that line is the verify script's null-owner fallback, `error: null`, no throw). The 4 unchanged are the misattributed rows above (byte-identical step/owner/rngM/scrM before and after — the painter is faithful, the state comes from `polymon`/`eat`/menu code). The 1 worse (Tourist-92047 step 88, rngM 2874→2871) is pre-existing drift, not this port: stashed re-run without the fix reproduces step 88 / rngM 2871 / identical rowDiff exactly.
**Named:** `flags.showvers`/`status_version` (`:208–211`, vers `""`); `MAXCO` panic (`:230–235`, unreachable — last-resort order + `mungspaces`); C `%-2d` gold/AC pads (display-length only; fit lines render single-spaced, which the suite matches — D-0500 precedent).
**Next:** `polyself.c` break_armor (queue head). True-owner follow-ups for the rescore queue (not this function): `polymon` `u.mh` while polyd (poly-Tourist-92047, poly-Monk-92164), `eat.c` lembas `uhs` SATIATED-vs-NOT_HUNGRY (wish-Healer-92092), tour attributes menu pagination (tour-Ranger-92033, tour-Valkyrie-92196).
## 2026-09-06 — D-1989 calendar.c getlt skipped the EDT→EST localtime shift, hiding the new-moon welcome pline

**C locus:** `calendar.c:40–46` `getlt() = localtime(getnow())`; contest patch 001 `time_from_yyyymmddhhmmss` fills `struct tm` from the recording machine's current `localtime` (tm_isdst = 1, EDT at September record time) then `mktime`, so a winter civil stamp becomes the stamp-as-EDT epoch; `localtime` under America/New_York re-reads it in EST, one hour earlier (`2000-02-06 00:00` → Feb 5 23:00, tm_yday 36→35). `phase_of_the_moon` (`:190–203`) then yields 0 (new) instead of 1, and `moveloop_preamble` (`allmain.c:57–63`) prints the new-moon pline whose pending `--More--` marks the welcome row.
**JS:** `js/calendar.js` (+78/−30, one file).
**Change:** `getlt()` = `nyLocaltime(getnow())`; new module-local America/New_York engine, plain arithmetic per Rule #2 (no Intl / node TZ; only `Date.UTC`/getUTC* decomposition): pre-2007 first-Sun-Apr → last-Sun-Oct, 2007+ second-Sun-Mar → first-Sun-Nov, 02:00 transitions (07:00/06:00 UTC), EST (UTC−5) / EDT (UTC−4) offset, yday/wday/tm_isdst. `lt_for_date(date)` uses the same engine for the `localtime(&date)` arm. `time_from_yyyymmddhhmmss` (stamp-as-EDT epoch) unchanged.
**Verify:** `node scripts/verify.mjs --fn welcome` → `VERIFY: PASS` — hidden `1 PASS, 50 moved past, 0 unchanged, 0 worse → PROGRESS` (baseline HEAD scoreboard `b438efbd`; sole PASS scen-kit-Barbarian-92028; the 50 move to strictly later owners, e.g. `do_statusline2`, `break_armor`, `doride`, `pick_lock`, `create_particular_creation`); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `node --check js/calendar.js` OK, imports unchanged (`./gstate.js`, `./const.js`, Rule #2 clean). Cohort: `seed0013-friday13-save-then-fullmoon-restore` + `seed0013-rogue-friday13-combat` 2/2 PASS (RNG 4804/4804 + 4838/4838, screens exact) — Oct 13 2000 is EDT on both sides (Friday + full moon preserved).
**Named:** none — getlt/phase/friday/night/midnight/yyyymmdd/hhmmss/yyyymmddhhmmss/getyear all live. Non-fixed `getnow()` wall-clock dependence intentionally not replicated (contest always pins datetime; fixed-EDT matches the recorded corpus).
**Next:** `botl.c` do_statusline2 (11 sessions, new queue head).
## 2026-09-06 — D-1988 missing JS imports (is_pit/FORCEBUNGLE/otense/STARVED) threw at first step in 8 corpus sessions

**C locus:** `muse.c:650` find_defensive trap kludge `is_pit(t->ttyp)` (`trap.h:113` macro); `muse.c:950` use_defensive `mintrap(mtmp, FORCEBUNGLE)` (`hack.h:1308`); `read.c:1182/1186/1255` seffect_enchant_armor `otense(…)` (`objnam.c:2531`); `eat.c:3438` newuhs `u.uhs = STARVED` (`hack.h:571`).
**JS:** `js/muse.js`, `js/read.js`, `js/eat.js` (+4 names across 3 import lists); 3 js files, under 600 cap.
**Change:** three one-line import extensions, no new module edges (`imports.mjs --can` ALREADY on all three; same 82-module SCC, no TDZ): `muse.js` gains `is_pit` + `FORCEBUNGLE` on the existing `./const.js` list; `read.js` gains `otense` on the existing `./objnam.js` list; `eat.js` gains `STARVED` on the existing `./const.js` list. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn randomize_gem_colors` → VERIFY: PASS — `PASS syntax 3 changed js file(s)`; `PASS rule2`; `PASS hidden verify randomize_gem_colors: 0 PASS, 6 moved past (2 re-attributed at the same step), 0 unchanged, 0 worse → PROGRESS` (Healer-92124 → `exercise`@48, Barbarian-92129 → `level_tele`@51, Monk-91117 → `show_conduct`@67, Wizard-91112 → `burnarmor`@72; Tourist-92134 + wish-Arch-92238 re-attributed at step 0 to the themerms/`rnd_rect` step-0 RNG row — working rows show `err null`, `jsOwner rnd_rect`, so the throw is gone and they now sit on the `somex` Open row); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Probe `score --ids Barbarian-92129,Monk-91117,Healer-92124,poly-Arch-92119` (~120 s): movement recorded in the working scoreboard. STARVED arm: poly-Arch-92119 baseline `worker:` timeout → `polyself`@63 screen row (`You can't polymorph into [a] Croesus`), proving `newuhs` now runs; wish-Knight-92034 still `worker:` timeout (the row's noted ~128 s slow path — same one-line fix, verified via poly-Arch + syntax; left for a longer-budget runner).
**Named:** none (import-only; no map section names these as omitted, so no `c-js-map` edit).
**Next:** pop the next Open row (`calendar.c getlt` DST, 51 sessions).
