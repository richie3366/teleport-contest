# Agent loop journal
## 2026-09-21 — Audit 2a3c61e5..f6f4a0b2 (reviews 1674-1682: 8 ACCEPT + 1 WITH-DEBT, 0 Must-fix) + cadence 44/44.

Reviews audit D-2715..D-2723 against pinned C (csym bodies + callers,
sym.mjs, per-SHA hidden-proxy --reach-all re-runs: 4× 0-blocked
vacuous + REACH-OK; 5 genuine PROGRESS — 1676 distfleeck Caveman-92202
PASS at HEAD (later owner fixed by D-2719), 1677/1679 do_statusline2
Healer-92107→retouch_object@298 / Healer-92092 PASS, 1678 monhp_per_lvl
Caveman PASS, 1680 destroy_arm Caveman PASS; 0 REGRESSED anywhere).
Notable: 1674 null-mon `is_magic_key` + zero-questarti nuances
pre-existing, unreachable from autokey; 1676 `mstrategy != null` guard
null-safe rendering; 1679 `cmdq_clear` pre-existing house rendering;
1682 WITH-DEBT solely for the `!oldmem` map line (display-only corner,
commit-disclosed) + "`digests` new edge" message imprecise (`--can`
ALREADY, no new module edge). No Must-fix prepend; CURRENT Next cluster
unchanged (shipped dokick; next port pops queue head
done_object_cleanup). Cadence: public 44/44 (Scr 11405/11405, RNG
792838/792838, speed 60+0.33 R² 0.78); held-out 12/44 (+0);
corpus 500/540 (+2 Caveman-92202 via D-2721, Healer-92092 via D-2720).
Queue 2 Open, pool exhausted (`--rows 600`: 74 machine-fresh all
class-deferred; hidden-proxy queue 0 untagged-eligible) — no refill.
Rule #2 clean.
## 2026-09-21 — Audit 29baae20..70a4bf39 (reviews 1665-1673: 9 ACCEPT, 0 Must-fix) + cadence 44/44.

Reviews audit D-2706..D-2714 against pinned C (csym bodies + callers,
sym.mjs, per-SHA hidden-proxy --reach-all re-runs: 8× 0-blocked
vacuous + REACH-OK; 1673 distfleeck genuine PROGRESS — Wizard-92127
PASS, 3 unchanged at own writer rows, 475-session reach 475 PASS, 0
REGRESSED). Notable: 1667 impossible() fire-and-forget in sync loaders
(pre-existing precedent, disclosed); 1669 TOP/BOTTOM==SPLEV_TOP/BOTTOM
verified at sp_lev.c:172-173; 1671 raw-index "true"→0 clears-flag quirk
preserved exactly; 1672 MARK=4 alias via const import; 1673 no RNG
call touched — the missing Blind-look turn re-aligns rnl(20). Cadence:
public 44/44 (Scr 11405/11405, RNG 792838/792838); held-out 12/44
(+0); corpus 498/540 (+1 Wizard-92127 via D-2714). Queue 5 unchecked,
pool exhausted (78 machine-fresh all class-deferred; queue owners all
tagged) — no refill. Rule #2 clean.
## 2026-09-21 — Audit 3de22e5b..51e65db7 (reviews 1656-1664: 9 ACCEPT, 0 Must-fix) + cadence 44/44.

Reviews audit D-2697..D-2705 against pinned C (csym bodies + callers,
sym.mjs, per-SHA hidden-proxy --reach-all re-runs: all 0-blocked
vacuous + REACH-OK, no REGRESSED). 1656 closes the 1654 Must-fix
(43 remaining coord-form sites wired, 15+43+3=61 arithmetic shuts).
Notable: 1661 movecmd txt-vs-funct equivalence + stale lock.js:128
comment; 1664 trimspaces leading-strip justified via describe_level
formats. Cadence: public 44/44 (Scr 11405/11405, RNG 792838/792838);
held-out 12/44 (+0); corpus 497/540 (+0/-0). Queue 8 unchecked, no
refill (at band). Rule #2 clean.

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-21 — D-2736 `sp_lev.c` lspo_trap des.trap binding + create_trap whole body

**C locus:** `nethack-c/upstream/src/sp_lev.c:4397–4470` (`:4402` create_des_coder; `:4404–4406` defaults; `:4408–4427` string/coord-pair/triple dispatch; `:4428–4461` table form — xy, traptype_opt, boolean opts, launchfrom + teledest both → gl.launchplace; `:4463–4464` NO_TRAP error; `:4466–4467` coord pack; `:4469` create_trap(croom); `:4470` launchplace reset) + `create_trap` `:1812–1846` (`:1816` MAZEFLAG init; `:1819–1823` vib arm; `:1824–1825` croom arm; `:1826–1835` DRY retry loop; `:1837–1842` flags; `:1844` mktrap) + `get_table_traptype_opt` `:4350–4364` + `get_traptype_byname` `:4379–4389` (staticfn). Only C caller is the Lua dispatch table (`:162` decl).
**JS:** `js/mklev.js` (+~130 before the lspo_feature block: table + 2 locals + 2 exports).
**Change:** `js/mklev.js` only (same-file locals — no new cross-module edge; `maketrap` already imported from `./trap.js`): `LSPO_TRAPTYPES` table (C `:4322–4347`); file-local `lspo_traptype_byname` (C staticfn shape, strcmpi → lowercase compare, NO_TRAP default) + `lspo_traptype_opt` (empty/unmatched → defval, C `:4355–4362`); exported `create_trap(tmp, croom)` in C order (vib arm via live `pick_vibrasquare_location` + `game.svi.inv_pos` idiom; croom arm via `get_free_room_loc_coord`; DRY retry loop with C `(A || B) && ++trycnt <= 100` precedence; flags; `mktrap(type, flags, null, {x, y})`); exported `lspo_trap(a, b, c)` with `arguments.length` dispatch like `lspo_gold` (string / string+coord via live `get_coord_unpacked` / triple with checkstring-equivalent throw / table-or-empty with live `get_table_xy_or_coord` + `splev_opt_boolean` + launchfrom/teledest both → `game.launchplace` + reset). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn lspo_trap` → PASS syntax (1 changed: js/mklev.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; fixed smoke spread 24 run, 24 PASS, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** Lua-stack callback (lspo_trap takes no function arg — contentsFn pattern N/A); Lua argc dispatch itself (unpacked `arguments.length` form per file precedent); `lcheck_param_table` (≡ table-or-empty guard); `nhl_push_obj` return count (unpacked `return 0` per siblings).
**Next:** queue holds 4 Open after archive (map/replace_terrain/region + residuals) — refill top-up next iter.
## 2026-09-21 — D-2735 `mon.c` golemeffects whole body (flesh/iron slow via mon_adjust_speed) + mhitm_ad_cold mhitm arm

**C locus:** `nethack-c/upstream/src/mon.c:5680–5707` (`:5683` heal/slow init; `:5685–5689` flesh ELEC-heal / FIRE+COLD-slow; `:5690–5694` iron ELEC-slow / FIRE-heal; `:5695–5697` non-golem return; `:5698–5700` slow → mon_adjust_speed(-1) unless MSLOW; `:5702–5705` heal → healmon + cansee pline). Callers: `explode.c:525`, `mhitm.c:1399/:1423/:1435` (passivemm), `mhitu.c:2565/:2588/:2598` (passiveum), `uhitm.c:2555/:2617` (fire uhitm), `:2644` (cold uhitm), `:2676` (cold mhitm), `:2701/:2734` (elec uhitm), `:5148/:5159/:5170` (gulpum). Same-file companion: `uhitm.c` mhitm_ad_cold mhitm arm `:2664–2680` + dispatch `:4796`.
**JS:** `js/mhitm.js` (clone completion + export :2175, `mhitm_ad_cold` :1228, dispatch row :4554), `js/uhitm.js` (import :58, cold call :2166, clone removal, gulpum rewire :3545), `js/mhitu.js` (import, arms :3367/:3390/:3400).
**Change:** `js/mhitm.js` — completed + exported `golemeffects_mm` in full C order (slow block before heal block, per-arm `:line` cites); slow calls live `mon_adjust_speed` (pre-existing static `./muse.js` edge, damageum_ad_slow precedent — no new import, no cycle). Added `mhitm_ad_cold` (mhitm arm, C `:2664–2680`: negate gate, `_mm_vis && canseemon` frost pline via `pline_mon`, resists_cold/defended → chill pline + `shieldeff` + golemeffects + zero in C order, destroy_items(orig) via hoisted dynamic `./zap.js` import, fire-arm precedent) + `mhitm_adtyping` AD_COLD row (C `:4796`, FIRE-row shape). `js/uhitm.js` — `damageum_ad_cold` calls the export (C `:2644` position); deleted `golemeffects_you`, gulpum calls the export (C `:5148/:5159/:5170` position, arm structure already C-ordered); name added to the existing `./mhitm.js` import.
**Verify:** `node scripts/verify.mjs --fn golemeffects` → PASS syntax (3 changed: js/mhitm.js js/mhitu.js js/uhitm.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; fixed smoke spread 24 run, 24 PASS, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** `explode.c:525` (map-named no-port); hero-side `ugolemeffects` slow (`polyself.c:2160–2187`, other C function — next row); `defended(mdef, AD_COLD)` worn walk (no JS export; same commented omit as the pre-existing cold/fire/elec sites); passiveum `resists_mr`≡`resists_*` + plain-`pline`≡`pline_mon` shapes (pre-existing, untouched).
**Next:** queue holds 0 Open after archive (below band 8) — refill below.
## 2026-09-21 — D-2734 `mkobj.c` place_object `:2330` obj_no_longer_held arm (sync revert core; COST_DEGRD billing floated)

**C locus:** `nethack-c/upstream/src/do.c:893–920` (`:895–896` null return; `:897–902` `Has_contents` cobj/nobj recursion; `:904–919` CRYSKNIFE arm — normal reverts, fixed reverts on `!rn2(10)` with no stack splitting; `:913–914` `!mon_moving && !gameover` → costly_alteration(COST_DEGRD); `:915–916` otyp=WORM_TOOTH + oerodeproof=0), called from `nethack-c/upstream/src/mkobj.c:2330` with otmp OBJ_FREE.
**JS:** `js/mkobj.js` (2 consts :114–115, 2 import names :77/:84, helper :2451–2481 + 1 call :2511; +38/−5).
**Change:** `js/mkobj.js` only — module-local `place_object_no_longer_held` replicating do.c:893–920 in C order (`Has_contents` joins the existing const.js import; `rn2` the rng.js edge; `game.context?.mon_moving` / `game.program_state?.gameover` mirror do.js:699); `CRYSKNIFE`/`WORM_TOOTH` module consts via the file's `objectNames.indexOf` pattern (BOULDER precedent :112); `COST_DEGRD` joins the const.js edge and `costly_alteration` the shk.js edge (both `imports.mjs --can` ALREADY, runtime-only calls — no TDZ risk); the billing runs in C position before the revert, floated (`void` — invent.js:652 + do_wear.js:619 precedents; its sync prefix returns before the first await unless obj is unpaid shop goods, shk.js:2288). `place_object` stays sync — signature unchanged. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn place_object` → PASS syntax (1 changed: js/mkobj.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; fixed smoke spread 24 run, 24 PASS, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** async completion of the COST_DEGRD billing tail (message + bill float, only when obj is unpaid shop goods; the bill then prices the post-revert worm tooth — residual, map-named); `add_to_container` `mkobj.c:2683` + `extract_from_minvent` `worn.c:1413` stay named (own rows on a falsifier); D-2732 omits stand (panic→throw, assert dropped, `_objects_at` Map, `!otmp` guard).
**Next:** queue holds 0 Open after archive (below band 8) — refill below.
## 2026-09-21 — D-2733 `invent.c` learn_unseen_invent whole body C-order (cleric/arch gates, addinv_core2, update_inventory tail)

**C locus:** `nethack-c/upstream/src/invent.c:2750–2775` — `:2755–2756` Blind sanity return; `:2759–2761` skip when `dknown && (bknown || !Role_if(PM_CLERIC)) && (oclass != SCROLL_CLASS || !Role_if(PM_ARCHEOLOGIST))`; `:2763–2766` `invupdated=TRUE`, `maybereleaseobuf(xname(otmp))` (xname sets dknown, perhaps bknown for priest[ess]), `addinv_core2(otmp)`; `:2770–2774` eknown-deferred comment; `:2776–2777` `if (invupdated) update_inventory()`. Sole C caller `potion.c:363` (blindness cure).
**JS:** `js/invent.js` (function + 2 import names), `js/do.js` (one await).
**Change:** restarted `learn_unseen_invent` in C order as `export async function` (async: live `addinv_core2` awaits pline on its decipher arm): role gates via the same-file `urole.mnum` convention (`PM_CLERIC` already imported; `PM_ARCHEOLOGIST` added to the `monsters_data.js` import); `xname(otmp)` return discarded — dknown + cleric bknown live in `xname_flags` (objnam.js `:637`), maybereleaseobuf is a GC no-op, and xname runs `observe_object` when !Blind (objnam.js `:638` via `set_xname_observe`, wired invent.js:3407), so no discovery is lost; `await addinv_core2(otmp)` (new name on the existing `./u_init.js` edge — `imports.mjs --can` ALREADY, no new edge); `if (invupdated) update_inventory()` (same-file live export). `js/do.js:3086` (`toggle_blindness`, async) now awaits the call.
**Verify:** `node scripts/verify.mjs --fn learn_unseen_invent` → VERIFY: PASS — syntax (2 changed files) · Rule #2 · hidden note (no corpus session blocked at baseline) · REACH-OK (no RNG-tagged reach — function draws no RNG; fixed smoke spread 24 run, 24 PASS, 0 regressed) · green 2/2 · strict 2 sessions · cohort 7/7 · full 44/44 (auto: shared file changed).
**Named:** `maybereleaseobuf` (C obuf release — GC no-op, no JS counterpart by design); luckstone `set_moreluck` (inside `addinv_core2`'s named omit, no JS Luck engine); the `:2770–2774` eknown comment stands (`learnwand` live, zap.js).
**Next:** `mkobj.c` place_object obj_no_longer_held arm row stays open (async costly_alteration chain); coverage pool exhausted at `--rows 60` (all Stale-parked never-re-pop per @d012b82b sets) — nothing appended.
## 2026-09-21 — D-2732 `mkobj.c` place_object whole body C-order (isok/not-free throws; no_charge lapse; obj_no_longer_held named)

**C locus:** `nethack-c/upstream/src/mkobj.c:2305–2366` — `:2309–2323` isok gate (OOB → panic, x=0 → impossible + fall through); `:2325–2327` where!=OBJ_FREE → panic; `:2329` assert (debug-only); `:2330` obj_no_longer_held; `:2332–2336` boulder block_point gate; `:2338–2351` under-last-consecutive-boulder vs on-top threading; `:2353–2356` ox/oy/OBJ_FLOOR; `:2358–2361` no_charge lapse off-shop; `:2363–2364` fobj chain; `:2365–2366` timed. C callers: 89 sites (apply/ball/bones/dig/do/dog/dokick/dothrow/end/explode/hack/invent/light/lock/mklev/mkmaze/mkobj/mon/mthrowu/muse/nhlobj/objnam/pray/read/restore/shk/steal/teleport/trap/uhitm/vault/weapon/worn/zap + extern.h:1709; polyself.c:1144 is a dropx-chain comment, not a call).
**JS:** `js/mkobj.js:83` (shk import), `:52` (simple_typename), `:68–69` (COLNO/ROWNO), `:2448–2505` (place_object body).
**Change:** `js/mkobj.js` only — restarted `place_object` in C order with per-arm cites: isok gate (OOB throws — no live JS panic export, mklev.js:19190 precedent; x=0 warns via floating impossible(), do_wear.js:619 precedent, then falls through per C; message via sync simple_typename since C safe_typename is async in JS); not-free throws (`|0` treats unset where as OBJ_FREE — mksobj births it); assert dropped (no asserts in js/); boulder gate + threading unchanged (D-0270 recalc kept); no_charge lapse via live shk.js `costly_spot`/`costly_adjacent`/`find_objowner` (join the existing `./shk.js` edge — no new edge; `COLNO`/`ROWNO` join the const.js edge, `simple_typename` the objnam.js edge); fobj chain + timed unchanged. Stays sync — signature unchanged, all 91 existing JS sites stay wired. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn place_object` → PASS syntax (1 changed: js/mkobj.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; fixed smoke spread 24 run, 24 PASS, 0 regressed → REACH-OK; `--reach-all` same 24/24) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** `:2330` obj_no_longer_held (async in JS — costly_alteration await chain, do.js:690; awaiting it would force async across 91 sites in 32 files — own follow-up row); `!otmp` guard kept though C is NONNULLARG1 (defensive, pre-existing); `_objects_at` Map stands in for svl.level.objects (architecture); C panic → throw (no live panic export).
**Next:** queue holds 3 Open after archive (below band 8) — refill `port-coverage --rows` verbatim + the brief-verified `:2330` follow-up arm row.
## 2026-09-21 — D-2731 `questpgr.c` com_pager_core whole body C-order (array rn2 angel_cuss/demon_cuss + banished live; rawtext-before-array; impossible silent)

**C locus:** `nethack-c/upstream/src/questpgr.c:468–621` — `:484` skip_pager gate; `:487–497` nhl_init + QTEXT_FILE load; `:501–514` questtext + section tables; `:517–541` entry table with msg_fallbacks tryagain (lua has only `goal_alt→goal_next`, `dat/quest.lua:31–33`); `:543` text field; `:544–548` rawtext arm (dupstr, res TRUE, no display); `:549–550` synopsis + output options; `:552–568` array arm (nelems<2 impossible+done, else `rn2(nelems)+1` lua 1-based); `:570–590` default+newline/`BUFSZ-1` promote-to-window with synthesized `[text]`; `:592–595` pline/window delivery (3 → NHW_MENU); `:597–610` convert_line(synopsis) + putmsghistory(FALSE) (`#else` Strcpy arm); `:612–621` compagerdone frees + nhl_done. C callers: `:163` is `#if 0` dead code (role-loop variant, never compiled); `:172` stinky_nemesis rawtext (live); `:626` com_pager (live); `:632–633` qt_pager role-then-common (live).
**JS:** `js/questpgr.js:14` (rn2 import), `:832–866` (QUEST_COMMON + banished), `:867–909` (QUEST_CUSS_ARRAYS), `:938–960` (lookup common branch), `:1028–1108` (com_pager_core body), `:1129–1137` (com_pager/qt_pager doc touch-up).
**Change:** `js/questpgr.js` only — restarted `com_pager_core` in C order with per-arm cites: entry-miss → silent FALSE; rawtext arm before the array arm (`rawOut.text = text`, TRUE even when null); array arm via new `QUEST_CUSS_ARRAYS` (angel_cuss ×14 `quest.lua:76–90`, demon_cuss ×27 `:106–133`, verbatim) with `arr[rn2(nelems)]` (one rn2, same element as lua 1-based pick) through the existing pline path (output default, no synopsis per C); `banished` text+meta (`:92–104`) added to `QUEST_COMMON`; `quest_portal` converted to object form so output=pline is data-driven like C; `rn2` joins via new `./rng.js` edge (`imports.mjs --can` SAFE, no cycle). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn com_pager_core` → PASS syntax (1 changed: js/questpgr.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; fixed smoke spread 24 run, 24 PASS, 0 regressed → REACH-OK; `--reach-all` same) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS. Tail pasted above.
**Named:** impossible() text on all miss arms (tables are embedded constants so load cannot fail, and a JS miss also covers unported role bodies where C shows text and never calls impossible() — emitting would be C-wrong); lua VM init/load/teardown (nhl_init/nhl_loadlua/nhl_done — no VM) and get_table_str_opt/get_table_option/dupstr (the lookup/assign above); NHW_MENU delivery except legacy (com_pager_legacy owns menu incl. pauper_legacy); TEST_PATTERN (lua self-test entry, never called in production); other-role bodies (map-named); convert_arg catalogue D-1649, convert_line %Xh D-1634, qt_pager retry D-1662 (unchanged).
**Next:** queue holds 5 Open after archive (below band); `--rows 600` pool is class-deferred only (77 machine-fresh: optfn/handler/config, sfo/sfi save-infra, color/sound/symset/status_hilite, wiz-debug) and `hidden-proxy queue` shows 0 untagged-eligible — nothing appended.
## 2026-09-21 — D-2730 `uhitm.c` mhitm_ad_fire whole body (uhitm arm `:2529–2560` new + `damageum_adtyping` AD_FIRE row; `defended(AD_FIRE)` wired in mhitm arm)

**C locus:** `nethack-c/upstream/src/uhitm.c:2521–2623` — `:2526` orig_dmg snapshot; `:2529–2560` uhitm (mgc-negate gate, `!Blind` on_fire pline, completelyburns burns/engulfed pline or Blind smell-burning + `xkilled(NOMSG|NOCORPSE)`, resists/defended → golemeffects-then-shield + zero, destroy(orig)+ignite); `:2561–2587` mhitu (hitmsg, negate gate, on_fire pline, paper/straw burn-up + rehumanize, Fire_resistance zero, `m_lev > rn2(20)` destroy+ignite, burn_away_slime); `:2588–2621` mhitm (negate gate, vis+canseemon on_fire, completelyburns monkilled + grow_up + done, resists/defended → shield-then-golemeffects + zero, destroy(orig)+ignite). C callers via `mhitm_adtyping` (`uhitm.c:4792`): `mhitm.c:1059` (mon→mon), `mhitu.c:1191` (mon→you), `uhitm.c:4854` (`damageum`, you→mon).
**JS:** `js/mhitm.js:12` (`You` import), `:1136–1220` (three-arm `mhitm_ad_fire`), `:4481–4484` (dispatch comment); `js/uhitm.js:56` (import), `:2498–2508` (AD_FIRE row), `:762–765` (doc).
**Change:** `js/mhitm.js` — restarted `mhitm_ad_fire` as an exported three-arm port in C order with C cites inline: uhitm arm via live `mhitm_mgc_atk_negated` (same-file `:2558`), `!Blind_slee()` (C `Blind`, elec precedent `:1309`), plain `pline` on_fire (no vis gate per C), `completelyburns_mm` (`:2762`) + `mlifesaver` (`:2768`) with the Blind `You smell burning paper/straw` fork (`You` joins the existing `./display.js` import; mndx compare per D-2259), `xkilled(NOMSG|NOCORPSE)` via dynamic `./uhitm.js` import (rust precedent, no new static edge), `resists_fire` (`:415`) OR live `defended` (`./mondata.js`, existing edge) with golemeffects-then-shield in C arm order via clone `golemeffects_mm` (`:2145`) + live `shieldeff`, destroy(orig)+ignite via the hoisted dynamic `./zap.js`/`./trap.js` imports; `is_youmonst(mdef)` early-return to split `mhitm_ad_fire_u`; mhitm arm unchanged except `defended` wired. `js/uhitm.js` — AD_FIRE row in `damageum_adtyping` routing `mhitm_ad_fire(game.youmonst, …)` (elec/rust/tlpt precedent; `mhitm_ad_fire` joins the existing `./mhitm.js` import — `imports.mjs --can` ALREADY, no new edge); retired the stale fire-uhitm named omit in the `xkilled` doc; updated the `mdamagem` AD_FIRE comment. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_fire` → PASS syntax (2 changed: js/mhitm.js js/uhitm.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (3 baseline-PASS sessions reach it, 3 run, 3 PASS, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** `golemeffects` (C `mon.c:5680–5707`, clone `golemeffects_mm` `js/mhitm.js:2145` — iron FIRE heal live, flesh FIRE slow named per review 365); none new — whole C body live, every callee live or named above.
**Next:** queue holds 6 Open after archive (fire head); refill only if a fresh `port-coverage --rows` name survives the live/DONE/PARKED cross-check.
## 2026-09-21 — D-2729 `rumors.c` outrumor whole body (`:540–549` faint/Blind gates + `:551–573` oracle/cookie/paper arms → live; `read.c:368` cookie arm wired)

**C locus:** `nethack-c/upstream/src/rumors.c:528–574` — `:536` reading flag (BY_COOKIE || BY_PAPER); `:539–549` gates before getrumor (`is_fainted()` + BY_COOKIE → return; `Blind` → cookie scrap then pity + return); `:551` getrumor + closed-for-renovation fallback; `:554–563` BY_ORACLE (nested `!rn2(4)`/`!rn2(3)`/`rn2(2)` adverb chain, `SetVoice(0, 0, 80, voice_oracle)`, `verbalize1`, return); `:564–566` BY_COOKIE fortune_msg + fallthrough; `:567–570` BY_PAPER "It reads:"; `:571` `pline1`. C callers: `eat.c:2523` (cookie), `read.c:368` (cookie arm `:365–377`), `rumors.c:747` (outoracle); `rumors.c:139`/`:207` are get_rnd_line/getrumor init comments, not calls; `hack.h:53` mechanism macros.
**JS:** `js/rumors.js:12` (imports), `:171–186` (gate + getrumor); `js/read.js:107` (Blind), `:139` (rumors import), `:196` (const), `:2187–2208` (cookie arm); `js/muse.js:2945` (doc fix).
**Change:** `js/rumors.js` — gate block added before `getrumor` in C order with C cites inline: faint arm via live `is_fainted` (new `./eat.js` edge — `imports.mjs --can` SAFE, hoisted fn, pre-existing eat↔rumors cycle, runtime call only); Blind arm via live `Blind` (joins the existing `./invent.js` import, eat.js precedent — no new edge). Oracle/cookie/paper tail unchanged (`verbalize(line)` with no args never re-scans `%`, matching C `verbalize1` = `verbalize("%s")` per `hack.h:1029`; `pline('%s', line)` routing stands per D-2476). `js/read.js` — FORTUNE_COOKIE arm in C position (before the class gate): verbose `You` break-up (`game.flags?.verbose !== false`, file idiom), `await outrumor(bcsign(scroll), BY_COOKIE)`, `!Blind()` literate-conduct + `livelog_printf` (`eat.js:2055` fpostfx precedent), local `useup` clone, `return 1` = ECMD_TIME (file returns 1 for TIME, 0 for OK); `FORTUNE_COOKIE` const (`objectNames` file idiom); `Blind` joins the existing `./invent.js` import and `BY_COOKIE`/`outrumor` join the existing `./rumors.js` import (both ALREADY, no new edge).
**Verify:** `node scripts/verify.mjs --fn outrumor` → PASS syntax (3 changed: js/muse.js js/read.js js/rumors.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; smoke 24 run, 24 PASS → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** none new — whole C body live; every callee live or named above.
**Next:** queue holds 11 Open after archive (outrumor head); refill only if a fresh `port-coverage --rows` name survives the live/DONE/PARKED cross-check.
## 2026-09-21 — D-2728 `dogmove.c` dog_eat whole body (`:257–261` bee bypass + `:266–270` pool silence + `:285–289` tunnels + `distant_name` + `:300–311` rust spit + apport `impossible` → live)

**C locus:** `nethack-c/upstream/src/dogmove.c:217–345` — `:233–236` hungrytime floor + nutrition; `:237–242` devour halve/trim; `:243–253` satiation/mconf/starve-recover/mflee/mtame; `:254–256` moved&ate newsym; `:257–261` killer-bee royal-jelly bypass (`bee_eat_jelly >= 0` → `res + 1`); `:262–265` FOOD split + `suppress_price++`; `:266–284` pool-silence vs message gate (`cansee`+`mon_visible` sawpet, `canspotmon` second arm, `tunnels` digs-in, `distant_name(obj,doname)`); `:296–299` shop-name copy; `:300–311` rust-monster spit (`COST_DEGRD` when unpaid, derust, `mstun`, verbose disgust); `:312–330` DOGFOOD+invlet apport + `impossible` clobber guard; `:332–337` unpaid shop bill; `:339` `DEADMONSTER → 2`. C callers: `dog.c:1212` tamedog FALSE; `dog.c:1265` new-tame devour-TRUE; `dogmove.c:441` dog_invent edible; `dogmove.c:1319` dog_move do_eat (`dogmove.c:258` bypass comment + `:1294` worm comment, not calls; `extern.h:813` decl).
**JS:** `js/dogmove.js:21` (monmove import), `:25` (shk import), `:39` (const import), `:112–114` (new consts), `:424–431` (doc + head cites), `:432–573` (body; anchors `:465` bee, `:489` pool, `:503` tunnels, `:536` rust, `:555` impossible); `scripts/dog-eat.test.mjs` (new, 1 test).
**Change:** `js/dogmove.js` only — missing arms added in C order with C cites inline, function stays async (all five call sites already await): `:257–261` via live `bee_eat_jelly` (joins the existing `./monmove.js` import — `imports.mjs --can` ALREADY, same edge, runtime call only); `:266–270` via live `is_pool` (existing `./hack.js` import) + inline `game.u?.uinwater` (C `youprop.h:279`; dominant tree convention per apply/botl/cmd, no clone #2 of the sit.js local); `:285–289` via live `tunnels` (existing `./monsters.js` import); `distant_name(obj, doname)` ×3 (existing `./objnam.js` import, `js/dogmove.js:871` precedent); `:300–311` via live `costly_alteration` (joins the existing `./shk.js` import — ALREADY) + `COST_DEGRD` (joins the existing `./const.js` import); apport `impossible` arm via live `impossible` (existing `./display.js` import; C `%ld`/`%u` render as `%d` since `impossible()` formats `%d`/`%s` only); `PM_KILLER_BEE`/`PM_RUST_MONSTER`/`LUMP_OF_ROYAL_JELLY` via `monsterNames`/`objectNames.indexOf` (file idiom `:69–119`); data compare `(mtmp.data?.mndx ?? mtmp.mnum)` (current-form-first, C pointer-equality faithful). New `scripts/dog-eat.test.mjs` (rust arm: derust + stun + return 1, RNG-free). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn dog_eat` → PASS syntax (1 changed: js/dogmove.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; smoke 24 run, 24 PASS → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. `node --test scripts/dog-eat.test.mjs` 1/1 (fails pre-fix: old code falls through to `dogfood`→`obj_resists` RNG on the rustproof snack — arm absent). Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** none new — whole C body live; every callee live or named above. C `:268` TODO sea-monster reveal stays a comment (map).
**Next:** queue holds 10 Open after archive (below band); pool per recent refills is class-deferred only — refill only if a fresh `port-coverage --rows` name survives the live/DONE/PARKED cross-check.
## 2026-09-21 — D-2727 `shknam.c` shkname whole body (`:859–868` guard/fallback/panic arms → live, stays sync)

**C locus:** `nethack-c/upstream/src/shknam.c:856–897` — `:859–863` isshk save/clear + `noit_mon_nam` fallback + restore (the `mon_nam` recursion guard); `:865–866` `!isshk` impossible + fallthrough to the fallback; `:867–868` `!has_eshk` panic; `:870` eshk shknam; `:873–890` Hallu random-name (count prob!=0 types, `rn2` type, `rn2` name); `:892–893` non-letter prefix strip; `:894–896` Strcpy + return. C callers (40 refs): `dig.c:2078`; `do_name.c:273` + `:925` + `:927`; `dokick.c:481` + `:1628`; `end.c:265`; `makemon.c:906`; `shk.c:711` + `:817` + `:830` + `:846` + `:849` + `:1026` + `:1031` + `:1409` + `:1872` + `:1880` + `:1883` + `:1902` + `:1906` + `:1917` + `:1935` + `:1941` + `:1958` + `:2015` + `:3845` + `:4899` + `:5303` + `:5317` + `:5596` + `:5780` + `:5894` (`:5530` is a comment, not a call); `shknam.c:845` (Shknam); `wield.c:266`; `zap.c:5549`; `extern.h:2986` (decl).
**JS:** `js/shknam.js:30` (has_eshk), `:49` (noit_mon_nam), `:447–490` (doc + body; anchors `:466` guard, `:473` fallthrough, `:476` throw, `:486` Hallu, `:496` strip).
**Change:** `js/shknam.js` only — whole body restarted in C order with C cites inline, stays sync (30+ sync call sites, no signature change): `:859–863` via live `noit_mon_nam` (new `./do_name.js` edge — `imports.mjs --can` SAFE, hoisted fn, pre-existing do_name↔shknam cycle, runtime use only); `:865–866` returns the fallback (the impossible message itself is async — named omit, xname_flags glorkum precedent, unreachable since every caller passes a shopkeeper); `:867–868` via live `has_eshk` (extends the existing `./const.js` import) + `throw` for the C panic (remove_object precedent, D-2607); Hallu/strip/return unchanged in C order (empty-string guard kept as JS null-safety; buffer copy-out is the return per by-design strings). `Shknam` unchanged (inherits the arms through the same-file call). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn shkname` → PASS syntax (1 changed: js/shknam.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; smoke 24 run, 24 PASS → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 (full skipped: no shared file) → VERIFY: PASS. `/tmp/probe_shkname.mjs` (throwaway): basic + isshk-restore, prefix strip, Shknam, non-shopkeeper fallback + restore, eshk-missing throw, null — all as C. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** `:866` impossible message (async in JS; unreachable for valid input; C fallthrough stands); `zap.c:5549` fracture-rock shop-billable arm (pre-existing `fracture_rock` deferral, `js/dig.js:1839` comment — caller-side gap owned by fracture_rock, not this cluster); C `char *` buffer; `!mtmp` null guard.
**Next:** queue holds 11 Open after archive (worm_move head); refill this handoff: `--rows 600` exact-row cross-check (11 verbatim game-play PARTIAL/THIN + 1 brief-verified missing-arm row appended, queue back to 12), `hidden-proxy queue --limit 30` 24 owners shown, 0 untagged-eligible.
## 2026-09-21 — D-2726 `sp_lev.c` create_object missing arms (`:2284` recharged + `:2294` tknown + `:2304–2341` monster/saddle + container-NULL uncreate + `:2356–2389` Medusa + `:2391–2420` achievement + `:2428–2437` buried → live)

**C locus:** `nethack-c/upstream/src/sp_lev.c:2193–2440` — `:2284–2285` recharged (`% 8`); `:2294–2295` tknown (trapped-gated 0/1); `:2304–2341` CONTENT branch (`|| invent_carrying_monster`, `:2317–2321` SADDLE→`put_saddle_on_mon` else `mpickobj`, `:2330–2339` NULL-container `artifact_exists` + `obfree` + return NULL); `:2343–2352` CONTAINER push (live); `:2356–2389` Medusa statue fill (`rndmonnum` loop, `resists_ston`/`poly_when_stoned` veto, `propagate`, `mongone`, minvent move with `owornmask=0`); `:2391–2420` achievement prizes (mineend/sokoend `achieveo` stamp + `nomerge`, `lua_testing`-gated `impossible`); `:2422–2439` floor tail (live `stackobj`/`lit`, `:2428–2437` buried `bury_an_obj` + dealloc NULL-container). C callers: `sp_lev.c:3735` lspo_object (`:3725–3740` non-merge loop); `invent.c:999` is a comment, not a call.
**JS:** `js/mklev.js:157–160` (new edges), `:107–108` (mkobj), `:137–138` (objnam/do_name), `:151` (display), `:41` (const), `:310–311` (LEASH/POT_OIL), `:20200–20211` (Is_mineend/sokoend), `:20221–20487` (body; anchors `:20303` recharged, `:20312` tknown, `:20325` CONTENT, `:20363` Medusa, `:20408` achievement, `:20448` buried).
**Change:** `js/mklev.js` — arms added in C order with C cites inline, function stays sync (all four callers sync): recharged/tknown two-liners; CONTENT branch widened with `|| invent_carrying_monster`, monster arm via live `remove_object`/`can_saddle`/`put_saddle_on_mon`/`mpickobj`, NULL-container arm via live `artifact_exists` (new `./artifact.js` edge) + `safe_oname`/`obfree` (existing modules extended); Medusa arm via live `makemon`/`rndmonnum`/`resists_ston`/`poly_when_stoned`/`propagate`/`set_corpsenm`/`add_to_container` + sync fmon unlink (file idiom, cf. `:4032`) for async `mongone`; achievement arm via local `Is_mineend_level`/`Is_sokoend_level` (dungeon.h:136–137, const.js `Is_medusa_level` idiom) + `game.context.achieveo` (file idiom, cf. `:14967`) + live `describe_level`/`simpleonames`; buried arm as a sync `bury_an_obj` inline (dig.c:1984–2047: uchain/`obj_resists` guard, leash via live `o_unleash` (new `./apply.js` edge), lamplit via live `end_burn`, `is_ice` (new `./zap.js` edge), organic `start_timer`, rock/boulder dealloc in the dig.js `quan=0` convention, `add_to_buried`).
**Verify:** `node scripts/verify.mjs --fn create_object` → PASS syntax (1 changed: js/mklev.js) · PASS rule2 · hidden note (0 blocked at baseline — normal for a coverage row) · PASS reach (no RNG-tagged reach; smoke 24 run, 24 PASS → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) → VERIFY: PASS. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** bury-inline uball arm (dig.c:1991–1995 `unpunish`/`set_utrap`/pline) — fresh otmp is never `u.uball` by identity and `pline` is async-only; the uchain identity check stays in the guard. None other — whole C body otherwise live; every callee live or named above.
**Next:** queue holds 0 Open after archive (below band); refill this handoff: `--rows 600` machine-checked (73 word-fresh ALL class-deferred), `hidden-proxy queue --limit 30` 24 owners shown, 0 untagged-eligible → pool exhausted, nothing further appended.
## 2026-09-21 — D-2725 `allmain.c` welcome remainder (`:860` + doomed-restore + Hallu + restore tail → live, restore caller wired)

**C locus:** `nethack-c/upstream/src/allmain.c:854–929` — `:858–859` currentgend/adrift decls; `:860` l_nhcore_call(START/RESTORE); `:862–866` doomed-restore early return (`!new_game && Upolyd && ugenocided()` → udeadinside pline + return); `:869–870` Hallu studio-audience pline; `:872–910` buf build (live, `#if 0` A_ORIGINAL arm dead in C); `:912–920` welcome/welcome-back pline + new-game livelog (live); `:922–927` restore tail (hellish_smoke_mesg + print_level_annotation). C callers: `allmain.c:843` welcome(TRUE); `restore.c:948` welcome(FALSE).
**JS:** `js/allmain.js:20` (dungeon), `:22` (do), `:64–65` (polyself + read), `:69` (const NHCORE_*), `:674–735` (body; anchors `:688` nhcore, `:691–694` doomed, `:700` Hallu, `:731–732` restore tail); `js/do.js:2170` (export); `js/read.js:2368` (export).
**Change:** `js/allmain.js` — whole body restarted in C order with C cites inline: currentgend via live `Upolyd(u)` (`js/const.js:3184`) + `u.mfemale`; `:860` via `l_nhcore_call` (joins the existing static `./do.js` import); doomed arm via `ugenocided` (joins the existing `./polyself.js` import) + `udeadinside` (new `./read.js` edge — `imports.mjs --can` IN-SCC/CHECK, runtime call only, no top-level TDZ read); Hallu arm via `Hallucination()` call (the bare binding is a function, always truthy — first verify failed 10+ fortress sessions on row 0 before the `()` fix); `#if 0` noted dead, `#else` kept; restore tail via `hellish_smoke_mesg` (joins the existing `./do.js` import) + `print_level_annotation` (joins the existing `./dungeon.js` import) + `NHCORE_*` (joins the existing `./const.js` import). `js/do.js` — `hellish_smoke_mesg` exported (body unchanged, C-faithful vs `do.c:2003`: temperature pline + In_hell You-smell/sense; same-file `:2190` caller unaffected). `js/read.js` — `udeadinside` exported (body unchanged, C-faithful vs `polyself.c:2273`: dead/condemned/empty; same-file `:2515` caller unaffected).
**Verify:** `node scripts/verify.mjs --fn welcome` → PASS syntax (3 changed: js/allmain.js js/do.js js/read.js) · PASS rule2 · hidden PROGRESS (16 blocked sessions all PASS, 0 moved/worse) · PASS reach (no RNG-tagged reach; smoke 24 run, 24 PASS → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 → VERIFY: PASS. Mid-iteration FAIL triaged once: bare-`Hallucination` binding (function, always truthy) painted the studio-audience line on every fortress session — fixed to `Hallucination()` per `js/display.js:1047`, re-run clean. Preflight `verify --no-cohort` before edits likewise PASS.
**Named:** none new — whole C body live; every callee live (`l_nhcore_call` `js/do.js:1164`; `ugenocided` `js/polyself.js:841`; `udeadinside` `js/read.js:2368`; `hellish_smoke_mesg` `js/do.js:2170`; `print_level_annotation` `js/dungeon.js:2425`; `align_str`/`Hello` `js/roles.js:831/:766`; `livelog_printf` `js/pline.js:23`; `eos` ≡ `let buf=''`). `Hello(role.mnum)` arg shape pre-existing (D-0008/D-0138 area), untouched.
**Next:** queue holds 0 Open after archive (below band); refill this handoff: `--rows 12` all 12 names Stale-parked never-re-pop dupes (0 FRESH), `hidden-proxy queue --limit 30` 24 owners shown, 0 untagged-eligible → pool exhausted, nothing appended.
