# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-20 — D-2614 `mcastu.c` mcast_spell dispatcher C-exact args + FIRE_PILLAR/LIGHTNING floor effects + LIGHTNING sound

**C locus:** ``nethack-c/upstream/src/mcastu.c:801–897`` (dispatcher); callees ``:466`` (weaken), ``:504`` (stun), ``:523`` (geyser), ``:540`` (fire_pillar), ``:566`` (lightning).
**JS:** ``js/mcastu.js`` weaken/stun/geyser/fire_pillar/lightning + 5 dispatcher arms; map ``docs/c-js-map/turns.md`` (D-1825 named-omit line now live).
**Change:** ``js/mcastu.js`` — dispatcher now passes C shapes (WEAKEN/STUN/GEYSER ``dmg``; FIRE_PILLAR/LIGHTNING ``(mtmp, dmg)``); callees take the params and overwrite per C; geyser pline moved inside the callee per C order (now async); ``mon_spell_hits_spot(mtmp, AD_FIRE/AD_ELEC, u.ux, u.uy)`` wired after ignite/destroy-items per C (live ``js/zap.js:6897``, already imported); ``Soundeffect(se_bolt_of_lightning, 80)`` first in lightning (const live ``js/generated/seffects_data.js:23``, binding added to existing import — no new module edge).
**Verify:** ``node scripts/verify.mjs --fn mcast_spell`` → PASS — syntax 1 file, rule2 clean, hidden ``no corpus session blocked`` (coverage row, no cited blocks), reach smoke spread 24/24 PASS 0 regressed → REACH-OK, green 2/2, strict 2/2, cohort 7/7; full sessions skipped (no shared file changed).
**Named:** ``has_aggravatables`` (chooser-only, D-1825/D-2253); ``mcast_geyser`` ``#if 0`` water-damage (omitted per C); ``uhitm.c:3863`` ``touch_of_death`` caller (D-1825, untouched).
**Next:** none from this row (queue head moves to ``dothrow.c`` throw_obj).
## 2026-09-20 — D-2613 `mhitm.c` fightm whole-body port (ustuck/itsstuck gate, uswallow release pline, nmon-chain walk, bhitpos/notonhead stamps)

**C locus:** `nethack-c/upstream/src/mhitm.c:105–172` (resist_conflict `:110–111`; ustuck/itsstuck `:112–116`; has_u_swallowed `:117`; nmon snapshot + mtmp skip `:121–124`; DEADMONSTER/monnear `:125–130`; release/break `:131–137`; bhitpos/notonhead + mattackm `:139–143`; AGR_DIED `:145–146`; swallow-digest `:147–151`; retaliation `:153–164`; HIT tail `:166`). C caller `mon.c:1317` (Conflict → fightm last, before dochugw).
**JS:** `js/mhitm.js` (fightm restart + 2 import names); `js/monmove.js` (one-word `export` on itsstuck); `docs/c-js-map/turns.md` fightm row (deferral retired).
**Change:** restarted the export in C order with per-arm `:line` cites — `u.ustuck === mtmp → await itsstuck` (exported from `js/monmove.js:1747`, body already C-exact: `sticks(youmonst.data)` + pline_mon "cannot escape from you!"); live-array nmon snapshot with the mtmp skip (loop always returns after the first nearby mon, so no list mutation can occur between iterations — index walk ≡ C chain walk); `!rn2(4)` → `set_ustuck(null)` + `pline(Monnam + " releases you!")`, else `break`; `game.bhitpos` (zap.js:2266 idiom) + `game.notonhead = false` before both strikes; `(void)` retaliation kept as bare await. New names on already-edged imports (imports.mjs ALREADY, same SCC, call-time use): `itsstuck` (monmove.js), `set_ustuck` (mhitu.js).
**Verify:** `node scripts/verify.mjs --fn fightm` → VERIFY: PASS (syntax 2 files; rule2 clean; hidden note — 0 blocked at baseline, expected for a coverage row; reach smoke 24/24 PASS → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full 44/44 auto on shared-file change). Tail pasted verbatim in the commit.
**Named:** none new — every arm and callee is live or file-local (`mattackm`'s own `:379` bhitpos read stays that function's gap, not this row's).
**Next:** queue head after this row (first Open — coverage row).
## 2026-09-20 — D-2612 `pickup.c` tipcontainer whole-body port (shop billing, icebox thaw, cursed-mbag loss, BoH explosion, altarizing, terse comma-list, held encumber/inventory)

**C locus:** `nethack-c/upstream/src/pickup.c:3688–3841` (hero-start ox/oy + `get_obj_location` stamp `:3691`; gettarget menu `:3706`; `maybeshopgoods` `:3722`; source checks `:3724`; destination checks `:3726–3728`; highdrop/altarizing/cursed_mbag/loss `:3732–3735`; srcheld/dstheld `:3736–3737`; swallow clears `:3739–3740`; terse `:3741`; cknown `:3742`; headers `:3748–3756`; loop `:3758–3825` with icebox `:3764–3766`, cursed-mbag `:3767–3772`, per-item bill `:3774–3777`, transfer + BoH blast `:3780–3805`, highdrop `:3807–3810`, altar/verbose/terse `:3812–3823`, price reset `:3825`; loss bill `:3827–3828`; owt `:3829–3832`; encumber `:3833–3834`; inventory `:3837–3838`). C callers `:3552` (`choose_tip_container_menu`), `:3614` (`dotip` floor ynq), `:3630` (`dotip` getobj).
**JS:** `js/pickup.js` (tipcontainer restart `js/pickup.js:4845`, +168/−65; 5 import edits); `docs/c-js-map/turns.md` #tip row (D-2612 whole-body note; dotip candle/oil + menu names stay).
**Change:** restarted the export in C order with per-arm `:line` cites — `let ox/oy` hero-start + `get_obj_location_quantum` stamp; `maybeshopgoods` before checks; `IS_ALTAR(game.level.at(ox,oy).typ)`; file-local `Is_mbag` for cursed_mbag and the blast gate; `removed_from_icebox` / `mbag_item_gone(srcheld,…,FALSE)` + `continue` / per-item `addtobill(…,TRUE)` with `game.iflags.suppress_price` counting / `Is_mbag(targetbox) && mbag_explodes(otmp,0)` blast (`livelog_printf` + `urgent_pline`, inner-BoH-first `do_boh_explosion`, `obfree`, target `do_boh_explosion`, dstheld?`useup`:`useupf`, `nobj=null` normal-exit, `losehp(d(6,6),…,KILLED_BY_AN)`) / `add_to_container` else-branch / highdrop `hitfloor` / `doaltarobj` vs `Doname2/otense/surface` vs terse `doname` + `last_msg=PLNMSG_OBJNAM_ONLY`, then `how_lost=LOST_DROPPED` + `await dropy` + last_msg terse-break; unconditional `box.owt`/`targetbox.owt`; held-gated `encumber_msg`; single tail `update_inventory`. New static imports (all imports.mjs IN-SCC SAFE, hoisted fns, call-time read): `doaltarobj`+`dropy` (do.js), `surface` (sit.js), `removed_from_icebox` (muse.js), `encumber_msg` (invent.js, already-edged file), `PLNMSG_OBJNAM_ONLY` (const.js); `hitfloor` stays dynamic (dothrow cycle); `get_obj_location_quantum` kept over timeout.js (documented pickup→trap→timeout→do→pickup cycle).
**Verify:** `node scripts/verify.mjs --fn tipcontainer` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden note — 0 blocked at baseline, expected for a coverage row; reach smoke 24/24 PASS → REACH-OK, `--reach-all` identical; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped, no shared file per gate). Tail pasted verbatim in the commit.
**Named:** none new — every arm and callee is live or file-local. Display sync (not in C, named in map): one `newsym(ox, oy)` after the loop.
**Next:** queue head after this row (first Open — coverage row).
## 2026-09-20 — D-2611 `mon.c` vamp_stone whole-body port (lapidifying/rise plines, expels, door rloc, m_lev hpmax floor, sandestin NC_SHOW_MSG)

**C locus:** `nethack-c/upstream/src/mon.c:3766–3830` (is_vampshifter gate `:3769`, cham snapshot `:3770`, inner LOW_PM/monsndx/G_GENOD gate `:3773–3775`, pre-transform lapidifying buf `:3779–3786`, mcanmove/mfrozen `:3787–3788`, min-hpmax + mhp `:3789–3790`, engulfing expels `:3792–3793`, amorphous closed-door enexto+rloc_to `:3794–3800`, lapidifying pline + display_nhwindow `:3801–3804`, newcham NO_NC_FLAGS `:3804`, cham fixup `:3805–3808`, rise pline `:3809–3813`, newsym `:3814`, sandestin arm `:3817–3828`, TRUE fallthrough `:3830`). C callers `:3295` (`monstone`) and `trap.c:3866` (`minstapetrify`).
**JS:** `js/mhitm.js` (vamp_stone restart + 6 import names); `docs/c-js-map/data.md` mon.c row; new focused test `scripts/vamp-stone.test.mjs` (3 its: null/ordinary/genocided-vampshifter gates return TRUE unmutated — 3/3 PASS; the live revert arms need seeded RNG + level state past `newcham`, so they stay covered by verify/REACH, disclosed here).
**Change:** restarted the export in C order with per-arm `:line` cites — buf built before transformation (`x_monnam` ARTICLE_NONE + SUPPRESS_SADDLE|SUPPRESS_HALLUCINATION|SUPPRESS_INVISIBLE|SUPPRESS_IT, amorphous "coalesces on the" / flyer "drops to the" / "writhes on the", `surface` at the snapshot coords); canonical local `set_mon_min_mhpmax(mtmp,10)` (m_lev+1 floor, `:3134`); `engulfing_u → await expels`; amorphous `closed_door → enexto({x,y}, mons(mndx)) → await rloc_to`; canspotmon lapidifying `pline_mon` + `await flush_topl_more()` for `display_nhwindow(WIN_MESSAGE,FALSE)` (trap.js:1987 idiom — no JS export); `await newcham(mons(mndx), NO_NC_FLAGS)`; `monsndx`-compared cham fixup; `Amonnam` rise pline over post-rloc `surface`; unconditional `newsym`; sandestin arm with `NC_SHOW_MSG`. New imports (all imports.mjs SAFE / already-edged, hoisted fns): `flush_topl_more` (display.js), `monsndx` (mondata.js), `Amonnam` (do_name.js), `enexto`+`rloc_to` (teleport.js), `surface` (sit.js, same 98-module SCC), ARTICLE_NONE/SUPPRESS_SADDLE/SUPPRESS_HALLUCINATION (const.js). Await on sync `newcham` kept per D-1648 (boolean|Promise return).
**Verify:** `node scripts/verify.mjs --fn vamp_stone` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden note — 0 blocked at baseline, expected for a coverage row; reach smoke 24/24 PASS → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped, no shared file per gate). Tail pasted verbatim in the commit.
**Named:** none new — every arm and callee is live or inlined-literal; `display_nhwindow` reads as `flush_topl_more` (house idiom, no JS export).
**Next:** queue head after this row (first Open — coverage row).
## 2026-09-20 — D-2610 `insight.c` weapon_insight primary two-weapon compare leading-space restore (review 1568 Must-fix)

**C locus:** `nethack-c/upstream/src/insight.c:1355–1369` — `Sprintf(sfx, " limited by being %s with two weapons", twobuf)` (`twoskl < sklvl`) and `Strcpy(sfx, " limited by ")` (`twoskl > sklvl`); `enlght_line` is plain ` %s%s%s%s.` concat (verified against JS `enlght_line_txt`), so the space must come from `sfx`.
**JS:** `js/invent.js` (+2 chars); `scripts/weapon-insight-twoweapon.test.mjs` (new).
**Change:** restored the leading space in both primary `sfx` literals (`js/invent.js:5393,5398`). No caller, control-flow, or RNG change. New focused regression test `scripts/weapon-insight-twoweapon.test.mjs` (3 its: both primary arms at final=0 + `was` tense at final=1, each asserting the spaced phrasing present and `islimited`/`waslimited` absent).
**Verify:** `node scripts/verify.mjs --fn weapon_insight` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden note — 0 blocked at baseline, expected for an RNG-0 function; reach smoke 24/24 PASS → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped, no shared file per gate). Focused test 3/3 PASS (fails on the pre-fix body — confirmed via stash). Breach session `seed0107-samurai-twoweapon-enhance` re-run: PASS (RNG 2902/2902, screens 98/98) → fortress back to 44/44.
**Named:** none.
**Next:** queue head after this Must-fix (first Open — coverage row).
## 2026-09-20 — D-2609 `insight.c` weapon_insight whole-body port (new export, both enlightenment builders wired, shield/towel/ammo/can_advance arms live)

**C locus:** `nethack-c/upstream/src/insight.c:1270–1465` (wield line `:1277–1310` incl. `weapon_descr :1292`, shield `:1295–1296`, towel `:1297–1298`, strcmpi-some `:1301–1303`, quan an/makeplural `:1304–1308`; skill guard `:1311`; sklvlbuf `:1315–1322`; non-twoweap + enhance suffix `:1324–1329`; twoweap reads `:1330–1339`, restricted→unskilled `:1343–1350`, primary compare `:1352–1377`, secondary compare `:1379–1421` incl. also3 You-also `:1404–1412`, enhance tips `:1423–1460` with 5-case phrasing comment). Sole C caller `:1249` (`status_enlightenment`, itself called from `enlightenment()` for both `doattributes` final=0 and final disclosure).
**JS:** `js/invent.js` — `weapon_insight` (~new), final-disclosure call site, overlay call site `weapon_insight(0, {overlay:true})`; `js/do_wear.js` — `shield_simple_name` export. Net +190/−135 across the two js files (diff stat).
**Change:** new exported `weapon_insight(final, opts)` in `js/invent.js` in C order with per-arm `:line` cites — `eos()` appends as concat, `Sprintf/Strcpy` as string ops, `enl_msg/you_are/you_have` (`:105–108`) over shared `enlght_line_txt` with final-tense verbs, overlay `{overlay:true}` adds the extra leading space + D-0347 COLNO `.` clip (status_core_lines convention); strcmpi triple as lowercase compare (cite). Live imports: `is_wet_towel` + `can_advance` from `js/weapon.js` (imports.mjs SAFE — hoisted fn decls), `shield_simple_name` from `js/do_wear.js` (newly exported; cycle read is lazy, inside body only), `lcase` added to the existing hacklib import, `an`/`makeplural`/`is_ammo` already imported. Pre-existing locals kept: `weapon_type`, `skill_name`, `insight_P_SKILL`, `insight_skill_level_name` (identical to live `P_SKILL`; named, not re-ported).
**Verify:** `node scripts/verify.mjs --fn weapon_insight` → VERIFY: PASS — syntax 2 files; rule2 clean; hidden note (0 blocked at baseline, expected for a coverage row); reach smoke 24/24 PASS → REACH-OK (no RNG-tagged reach: function draws no RNG); green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped (no shared file per gate).
**Named:** none new — every arm and callee is live, inlined-literal (`eos`/Sprintf/strcmpi idiom), or a pre-existing local named above (`weapon_type`/`skill_name`/`insight_P_SKILL`/`insight_skill_level_name`; map row notes the drift vs live `P_SKILL`/`weapon_type` exports).
**Next:** queue head is now `mon.c` vamp_stone.
## 2026-09-20 — D-2608 `display.c` wall_angle whole-body completion (C-order restart with cites + live t_warn + SDOOR arboreal arm, C caller wired)

**C locus:** `nethack-c/upstream/src/display.c:3511–3787` (`wall_angle`: seenv entry `:3514`, `only` macro `:3519`, T rotations `:3521–3534` → `do_twall` `:3535–3597` (mode 0 `:3537–3551`, WM_T_LONG `:3552–3566`, WM_T_BL `:3567–3577`, WM_T_BR `:3578–3589`, default `:3590–3594`, `row[col]` `:3596`), SDOOR `:3599–3607`, VWALL `:3608–3625`, HWALL via `horiz:` `:3627–3645`, `set_corner` macro `:3647–3664`, corners `:3665–3676`, CROSSWALL `:3678–3780` (mode-0 chain `:3680–3703`, WM_X_TL/TR/BL/BR rotations `:3705–3718` → `do_crwall` `:3719–3751`, WM_X_TLBR `:3753–3762`, WM_X_BLTR `:3764–3773`, default `:3775–3778`), default `:3782–3784`, `return idx` `:3786`); callee `t_warn` `:3452–3498` (typ→name switch, `warn_str` `:3455`, `impossible` report `:3495–3498`); tables `wall_matrix` `:3416–3421` (rows T_d 0/T_l 1/T_u 2/T_r 3 `:3398–3401`) + `cross_matrix` `:3444–3449` (rows C_bl 0/C_tl 1/C_tr 2/C_br 3 `:3426–3429`).
**JS:** `js/display.js` — matrix/table cites (`:3046–3053`), `t_warn` (`:3079`), `wall_angle` (`:3106`, arboreal `:3124`), `do_twall` (`:3150`), `wall_angle_vwall` (`:3196`), `wall_angle_hwall` (`:3209`), `set_corner` (`:3223`), `wall_angle_cross` (`:3236`), `do_crwall` (`:3284`). 162 insertions / 99 deletions across `js/display.js` + map line (diff stat; single shared file).
**Change:** restarted the block in C order with per-arm `:line` cites — `t_warn` (`:3079`) ported live with the exact C typ→name switch; its `impossible` report stays a `// C:` cite because `impossible()` is async in JS (`:7992`) and `wall_angle` is a sync hot path (same convention as `display_warning`'s `// C: impossible(...)`); C-void surfaces as the name returned for debuggers only, callers ignore it. New SDOOR arm `if (lev.arboreal_sdoor) return S_TREE_CMAP` (`:3124`, C `S_tree` ≡ cmap 18 per `js/generated/glyphsyms_data.js:42`) placed before the horizontal check in C order — the C caller (`:2322`) guards arboreal before calling, so this arm is defensive there, but it also covers the JS-only `wall_glyph` caller which calls unguarded. `do_twall` takes `lev` (`:3150`) so the two `t_warn(lev)` sites (`:3548`, guarded `:3563`) match C scope; vwall/hwall keep the named `WM_W_*` constants (C literal cases 1/2 cited); all six `impossible` defaults are `// C:` cites + stone returns.
**Verify:** `node scripts/verify.mjs --fn wall_angle` → VERIFY: PASS — syntax 1 file (js/display.js); rule2 clean; hidden note (0 blocked at baseline, expected for a coverage row); reach smoke 24/24 PASS → REACH-OK (no RNG-tagged reach: function draws no RNG); green 2/2; strict seed8000+seed0900; cohort 7/7; **full 44/44** (auto: shared file changed). Preflight `verify.mjs --no-cohort` before edits was likewise green (gate was clean on entry).
**Named:** the six `impossible(...)` diagnostic reports in unreachable default arms (cite-only; async-impossible convention, zero screen effect on reachable states). (No committed unit test: repo has no tests/ layout and sessions/** is frozen; focused verify + full-44 is the gate.)
**Next:** queue head is now `insight.c` weapon_insight.
## 2026-09-20 — D-2607 `mkobj.c` remove_object + extract_nexthere named port (floor arm rewired; save/restore urealtime fortress repair, 14 C callers wired)

**C locus:** `nethack-c/upstream/src/mkobj.c:2508–2521` (`remove_object`: ox/oy snapshot `:2511–2512`, where-gate panic `:2513–2514`, nexthere unlink `:2515`, fobj unlink `:2516`, BOULDER vision `:2517–2518`, timed `:2519–2520`); callee `extract_nexthere` `:2623–2640` (head/middle unlink `:2628–2636`, object-lost panic `:2637–2638`, nexthere clear `:2639`). Repair locus: `save.c` savegamestate `:282–292` (finish=now, realtime+=delta, persist realtime + civil start, live start:=finish) + `restore.c` `:618–625` (realtime:=saved, start:=saved then overwritten by now).
**JS:** `js/mkobj.js` — `extract_nexthere` (:3148), `remove_object` (:3172), floor-arm rewire (:3216); `js/save.js` — imports (:11–12), save fold (:484–495), payload (:564–567), restore (:786–796). `urealtime` retired from the `dosave0` omissions header.
**Change:** new `extract_nexthere(obj, head)` (`js/mkobj.js:3148`) returning the new head (JS has no `struct obj **` out-param; mirrors `extract_nobj`), throw for C `:2637–2638`; new `remove_object(otmp)` (`:3172`) in C order with `:line` cites, throw for C `:2513–2514`, `game._objects_at` as `level.objects[x][y]` (lazily created like `place_object`), `game.fobj = extract_nobj(...)` (sets FREE like C). Floor arm routes `OBJ_FLOOR` through `remove_object` (`:3216`); legacy unset-where objs keep the tolerant inline path (C has no such objects, so the panic must not see them). Save: fold+persist+refresh per C (`js/save.js:484–495` + payload `:564–567` as epoch numbers — VFS JSON is ours, lossless, no civil-string TZ round-trip); restore loads then forces start=now per C `:625` (`:786–796`).
**Verify:** `node scripts/verify.mjs --fn remove_object --full` → VERIFY: PASS — syntax 2 files (js/mkobj.js js/save.js); rule2 clean; hidden note (0 blocked at baseline, expected for a coverage row); reach smoke 24/24 PASS REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; **full 44/44** (seed0013-friday13-save-then-fullmoon-restore failed 97/99 pre-repair with identical counts at stashed HEAD, passes post-repair).
**Named:** recreate_pile_at body (own row); splev monster-invent/saddle arm (map-named); moverock standalone relink (subsumed, proof above — no observable delta). (No committed unit test: repo has no tests/ layout and sessions/** is frozen; focused session + full-44 is the gate.)
**Next:** queue head is now `display.c` wall_angle.
## 2026-09-20 — D-2606 `bones.c` resetobjs save-arm completion (known/name strip + SLIME_MOLD/SCR_MAIL/EGG/TIN/corpse/invocation arms, 6 C callers wired)

**C locus:** `nethack-c/upstream/src/bones.c:50–193`. Save arm in C order: known-strip `:103–112` (`oc_uses_known` gate, dknown/bknown/rknown/lknown/cknown/tknown/invlet/no_charge/how_lost); name strip `:123–128` (artifact/STATUE/SPE_NOVEL/unique-corpse keep); SLIME_MOLD `:131–132`; SCR_MAIL `:134–138` (MAIL_STRUCTURES defined, global.h:430); EGG `:140–141`; TIN `:142–145`; CORPSE/STATUE `:147–165` (cant_revive box + doppelganger set_corpsenm); mines/soko prize `:166–169`; AMULET `:170–173`; CANDELABRUM `:174–183`; BELL `:184–186`; BOOK `:187–189`.
**JS:** `js/bones.js` — `resetobjs` (:108), otyp/PM consts (:40–55).
**Change:** restarted the save arm in C order with `:line` cites. `cant_revive` takes an inout `{ mtype }` box (zap.js:2960) — `mnumBox` carries `corpsenm` in and the remap out; `null` from_obj matches C `(struct obj *) 0`, `false` the shopkeeper revival flag. Bare `curse(otmp)` follows the mkobj mksobj sync precedent (state changes precede its first await; amulet/bell/book unlit, candelabrum `end_burn`d first).
**Verify:** `node scripts/verify.mjs --fn resetobjs` → VERIFY: PASS — syntax 1 file; rule2 clean; hidden note (0 blocked at baseline, expected for a coverage row); reach smoke 24/24 PASS REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7.
**Named:** none new — every arm and callee live. (No committed unit test: repo has no tests/ layout and sessions/** is frozen; verify.mjs --fn + REACH is the gate.)
**Next:** queue head is now `mkobj.c` remove_object.
## 2026-09-20 — D-2605 `eat.c` floorfood whole-body completion (cockatrice touch + otense/safe_qbuf questions + impossible tail, 3 C callers wired)

**C locus:** `nethack-c/upstream/src/eat.c:3579–3731`. Audited arm-by-arm against the brief: `getobj_else=0` + skipfloor gate (`iflags.menu_requested || !can_reach_floor || feeding&&usteed || pool/lava+Wwalking/clinger/Flying&&!Breathless`) pre-existing; metallivore beartrap (`check_capacity(qbuf)` ≡ `near_capacity()>=EXT_ENCUMBER`, verified `hack.c:4399–4409` returns 1 when over-encumbered) / IRONBARS / non-rust gold pre-existing; floor-loop filter (corpsecheck?CORPSE+tinnable : feeding?non-coin+edible : FOOD_CLASS) pre-existing; post-getobj `You_cant("%s that!")` validation pre-existing (text-identical pline).
**JS:** `js/eat.js` — `floorfood_eat` loop (:1266), `floorfood_tin` loop (:3888), `floorfood_sacrifice(verb)` loop (:3954), `floorfood` dispatcher (:3985).
**Change:** in C order per loop — `:3688–3691` cockatrice arm (`otyp==CORPSE && will_feel_cockatrice(otmp,FALSE)` → `await feel_cockatrice` + return null, before any question so blind bare-handed probing stays fatal); `:3696–3702` question via otense + `safe_qbuf(null,…,qsfx,otmp,doname,ansimpleoname,one?'something':'things')` with `qsfx=" here; <verb> it/one?"`; `:3718` dispatcher tail `await impossible('floorfood: unknown request (%s)',verb)` (unreachable from the three live callers). Imports only extend existing edges (objnam/invent/display already imported by `js/eat.js`).
**Verify:** `node scripts/verify.mjs --fn floorfood` → VERIFY: PASS — syntax 1 file; rule2 clean; hidden note (0 blocked at baseline, expected for a coverage row); reach smoke 24/24 PASS REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7.
**Named:** none new — every arm and callee live (`will/feel_cockatrice` from `js/invent.js`; `otense`/`safe_qbuf`/`ansimpleoname` from `js/objnam.js`; `impossible` from `js/display.js`). `is_pool_or_lava` drawbridge-under deferral and IRONBARS wall_info read stay as D-0953/D-0937 left them.
**Next:** queue head is now `bones.c` resetobjs.
## 2026-09-20 — D-2604 `mondata.c` can_blnd whole-body port (C-order restart + canonical resists_blnd/resists_blnd_by_arti, 11 C callers wired)

**C locus:** `nethack-c/upstream/src/mondata.c:305–398`; callees `resists_blnd` `:247–272` + `resists_blnd_by_arti` `:275–298` (same file, ported in this commit), `haseyes` (`mondata.h:46` macro, live), `mon_perma_blind` (`monst.h:253` macro, inlined), `objdescr_is` (live). 11 C callers: `apply.c:3584`, `dothrow.c:1297`, `mhitu.c:1279,1472`, `mthrowu.c:471,755`, `uhitm.c:1268,2966,2978,2988,5128`.
**JS:** `js/uhitm.js:104–106` (objdescr_is/resists_blnd/Unaware imports), `js/uhitm.js:210` (POT_BLINDNESS const), `js/uhitm.js:331` (`can_blnd` restart); `js/mondata.js:18,28,38–41` (haseyes/BLND_RES/impossible/Blind/Unaware/dmgtype+AT imports), `js/mondata.js:213` (`resists_blnd_by_arti`), `js/mondata.js:245` (`resists_blnd`); `js/mhitm.js:761` (`dmgtype_fromattack` export).
**Change:** restarted `can_blnd` in C order with `:line` cites — `:316–317` haseyes, `:320–321` perma-blind, `:327–328` raven-vs-raven (`mons(PM_RAVEN)` identity, gulpmu precedent), `:337–339` light arm (`magr.mcan` + canonical `!resists_blnd`), `:344–354` obj arm (pie EBlinded gate; venom ublindf/ucreamed gate + visor; POT_BLINDNESS no-defense TRUE; other objs FALSE), `:356–357` hero-swallowed gate, `:367–370` ENGL (you EBlinded/Unaware/ucreamed; monster sleeping), `:375–379` CLAW (you ublindf + swallowed + visor), `:386–387` TUCH/STNG mcan, `:394–396` visor tail (hero `game.invent` + `u.uarmh` alias per `mhitu.js` visored pattern; monster `minvent` chain), `:398` TRUE. New canonical `resists_blnd` + `resists_blnd_by_arti` exports (`js/mondata.js`, C `:247–298`: you Blind||Unaware, monster mblinded||!mcansee||!haseyes||msleeping, dmgtype AD_BLND AT_EXPL/GAZE via newly-exported `dmgtype_fromattack`, Sunsword wielded+carried scan, you Blnd_resist catchall with upstream `impossible()`; `#if 0` Overworld arm omitted upstream). `dmgtype_fromattack` added to the `js/mhitm.js` export list (1 word; body unchanged).
**Verify:** `node scripts/verify.mjs --fn can_blnd` → VERIFY: PASS (syntax 3 files: js/mhitm.js js/mondata.js js/uhitm.js; rule2; hidden note no-blocked; reach: no RNG-tagged reach, smoke 24/24 PASS → REACH-OK; green 2/2; strict ×2; cohort 7/7).
**Named:** none new — every arm is live, inlined-macro, or ported in this commit. Pre-existing file-local subsets stay as drift (map-named, not rewired): `can_blnd_cream_self`, `can_blnd_toss_self`, `can_blnd_u`, `can_blnd_mm`, `gulpmu_can_blnd`, `resists_blnd_mm`, `resists_blnd_you`, `detect.js:282`/`trap.js:4652` resists clones, `mhitu.js:655` dmgtype clone.
**Next:** next Open — coverage row (`eat.c` floorfood).
## 2026-09-19 — D-2603 `rumors.c` outoracle whole-body port (C-order restart, embed open-fail arm live, doconsult caller wired)

**C locus:** `nethack-c/upstream/src/rumors.c:640–693`; sole C caller `:755` (`doconsult` — `outoracle(cheapskate, TRUE)`); staticfn `init_oracles` `:576–595` (decl `:576`); file-local `couldnt_open_file` `:769–782` already live (`js/rumors.js:200`).
**JS:** `js/rumors.js:17` (ORACLEFILE import), `js/rumors.js:373` (`init_oracles`), `js/rumors.js:388` (`outoracle`).
**Change:** restarted `init_oracles` + `outoracle` in C order with `:line` cites — `:649–650` early return, `:652` embed-open check with the `:689–692` open-failed arm live (`couldnt_open_file(ORACLEFILE)` + `oracle_flg = -1`; unreachable under the embed, getrumor D-2513 precedent), `:655–659` first-use init + empty-deck close, `:663–664` shouldn't-happen gate, `:665` pick (`rnd(cnt-1)` is 1..cnt-1 per `js/rng.js:97`, special short-circuits the draw), `:666` seek-as-index with pre-swap `recIdx` snapshot, `:667–668` swap-remove, `:670–678` window + headers, `:680–684` record lines (`'\n'` strip subsumed by the extractor split; `xcrypt` pre-inverted at build — makedefs packs xcrypt'd at `util/makedefs.c:1469/1500`, `extract-oracles.py` stores plaintext — same visible string), `:685–688` show + fclose no-op. `ORACLEFILE` joins the existing const.js import (no new edge; `imports.mjs --can` ALREADY for rng/pager).
**Verify:** `node scripts/verify.mjs --fn outoracle` → VERIFY: PASS (syntax 1 file: js/rumors.js; rule2; hidden note no-blocked; reach: no RNG-tagged reach, smoke 24/24 PASS → REACH-OK; green 2/2; strict ×2; cohort 7/7).
**Named:** none new — every arm is live or build-subsumed (dlb handles/seek/close, comment-skip + offset parse, newline strip, xcrypt, close_oracles gotos).
**Next:** next Open — coverage row (`mondata.c` can_blnd).
