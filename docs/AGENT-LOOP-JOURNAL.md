# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-19 — D-2530 `wizcmds.c` misc_stats whole body in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/wizcmds.c:1284–1399` (`misc_stats`, staticfn). Callees: `engr_stats` (`engrave.c:1625–1640`, ported file-local this commit — `js/wizcmds.js:913`); `light_stats` (`light.c:500–511`, ported file-local — `:947`); `timer_stats` (`timeout.c:2734–2745`, ported file-local — `:973`); `region_stats` (`region.c:898–922`, ported file-local — `:990`); `Sprintf` (`template[]` `:1112` `"%-27s  %4ld  %6ld"` → file-local `stats_row` `:905`); `Strcpy` (header constant, inline); `putstr` (no JS export by architecture — `lines[]` sink consumed by the caller's `show_text_pages`, D-2508 idiom); `plur` (`:1363`, inline ternary — no clone #10, nine locals already exist).
**JS:** `js/wizcmds.js` (+268: 1 import line + ~240 stats block); under caps (1500 ins / 15 files).
**Change:** `js/wizcmds.js` — `SIZEOF_*` LP64 constants (`:890–899`) measured from the pinned headers with gcc (`trap=32 engr=64 light=32 timer=48 damage=32 region=96 rect=8 kinfo=272 cemetery=184`; probe in /tmp, not committed) + file-local `engr_text_alloc` (`:932`, `make_engr_at` `:417–454` smem*3 over the three stored text states) + exported `misc_stats(lines, total)` (`:1020`) in C order with `:line` cites: `:1296–1307` traps unconditional (game.ftrap ntrap-chain or bones array, detect.js dual shape); `:1309–1314` engravings unconditional; `:1316–1323` lights gated; `:1325–1332` timers gated (`game._timer_base`, print_queue shape); `:1334–1345` shop damage gated (`game.level.damagelist`, shk.js shape); `:1347–1354` regions gated; `:1356–1367` delayed killers gated (`game.killer.next`, end.js shape); `:1369–1379` bones gated (`game.level.bonesinfo`, bones.js shape); `:1381–1396` oc_uname names gated (`game.objects`, NUM_OBJECTS bound). `winid` → caller-collected `lines[]`, out-params → mutated `{count,size}` (NHW_TEXT idiom D-2516); +1 top-level `NUM_OBJECTS` import (`imports.mjs --can`: SAFE, no cycle).
**Verify:** `node scripts/verify.mjs --fn misc_stats` → PASS syntax (1 changed: js/wizcmds.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full skipped (no shared file changed) → VERIFY: PASS. Tail pasted verbatim. Focused `/tmp/misc_stats_probe.mjs`: 9 rows match C-template strings exactly, totals {count:12,size:1096} (throwaway probe, not committed).
**Named:** caller `wiz_show_stats` display window (future row); `gm.max_regions` preallocation (JS array has no spare capacity — base is n*sizeof); `Sprintf`/`Strcpy`/`putstr` (no JS exports by architecture — inline shapes + lines sink).
**Next:** Open head after misc_stats (`dothrow.c` breakobj).
## 2026-09-19 — Review iter 1480–1488 (9 ACCEPT) + cadence 44/44, proxy 497/540, held-out 11/44

Audit of `23771238..25a9ad2d` (D-2521…D-2529, one file per SHA written
as each SHA finished, single grouped commit): look_engrs (MAP-raw coord =
C `getpos.c:613`, macro inlines verified), tiphat (cursed()/Deaf/RNG
short-circuit exact; Glib arm unreachable for helms), canletgo (all 14 C
caller refs accounted; ball/muse silent clones equivalent/named on live
paths), dochat + shop_object/price_quote/shk_embellish (RNG call-for-call;
focused test 8/8), vtense (marker/BSTRNCMPI/special_subjs/`sing:` arms
exact; `if (subj)` guard closes the empty-head read), losexp (divergent
clone deleted for the live import; trap `minuhpmax(1)` fix), dosinkring +
teleport_sink (goto-giveback duplicated; buried path ≡ freeinv),
show_menu_controls (rebound-map keys replace hardcoded chars; focused
test 5/5), Amulet_off (async audit clean; Strangled/Flight mirrors match
Amulet_on). Per-SHA `hidden-proxy verify --base …~1 --reach-all` re-runs:
all 0-blocked + smoke 24/24 REACH-OK, matching the D-logs. No Must-fix;
no CURRENT Next-cluster change.
Cadence: full `sessions` 44/44 (Scr 11,405/11,405, RNG 792,838/792,838,
`49+0.30/turn` R² 0.79); `leaderboard.mjs` 11/44, 5,648 pts, RNG 26.6 %,
screens 50.1 % (flat); `hidden-proxy score` 497/540 (92.0 %) excl.
13 env-only, RNG 99.31 %, screens 99.1 % (+0/−0 in-window).
`check-hot-docs.mjs --fix --review 1480…1488`: all ok, no cap edits.
## 2026-09-19 — D-2529 `do_wear.c` Amulet_off whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/do_wear.c:1090–1189` (`Amulet_off`). Callees: `setworn` (`:1100/:1116/:1135/:1150/:1160/:1183`, live same-file); `off_msg` (`:1101/:1117/:1136/:1161/:1185`, live same-file async); `see_monsters` (`:1104`, live display edge); `cant_drown` (`:1121`, `mondata.h:28` is_swimmer/amphibious/breathless — live `js/mon.js:2273`, extends the ALREADY edge); `Swimming` (`:1121`, youprop.h H||E||steed — live `hero_Swimming` dbridge edge, imports.mjs SAFE hoisted); `You`/`Your` (`:1122/:1130/:1143/:1145/:1167`, live display edge); `hliquid` (`:1123`, live do_name edge); `drown` (`:1125`, live trap edge, `(void)` return discarded); `region_danger` (`:1128`, live region edge); `Breathless` (`:1142`, youprop.h — live `hero_Breathless` dbridge edge, same SAFE verdict); `body_part(NECK)` (`:1143`, live polyself edge); `float_vs_flight` (`:1164`, live polyself edge); `is_pool_or_lava` (`:1167`, dbridge.c one-liner — file-local copy, same shape as the dig/eat/trap locals, no live export); `Is_waterlevel`/`Is_airlevel` (`:1168`, live const edge); `spoteffects(TRUE)` (`:1172`, live pickup edge); `find_ac` (`:1177`, live u_init edge); `makeknown` (`:1187`, live invent edge). Macros: `Underwater` ≡ `u.uinwater` (youprop.h:279); `Strangled` ≡ uprops[STRANGLED].intrinsic (youprop.h:110); `Flying` ≡ amulet_flight_now() (pre-existing local, D-1085).
**JS:** `js/do_wear.js` (+~90/−~40 restart + 9 import words + 1 dbridge import line + file-local helper) + `js/steal.js` (+1/−2 + 1 import word); under caps (1500 ins / 15 files).
**Change:** `js/do_wear.js` — restarted `Amulet_off` in C order with `:line` cites: `:1092–1095` amul save + takeoff.mask clear; `:1098–1105` ESP early setworn/off_msg + see_monsters; `:1106–1112` six no-op amulets; `:1113–1133` BREATHING early off + uinwater/cant_drown/hero_Swimming gate + inhale/hliquid + mkn + `await drown()` + region_danger gas arm; `:1134–1148` STRANGULATION early off + Strangled-intrinsic (+u.Strangled mirror this file's Amulet_on maintains) release + disp/flags botl + Breathless-gated constricted/easier messages + mkn; `:1149–1154` RESTFUL_SLEEP early setworn + FROMOUTSIDE-safe TIMEOUT clear; `:1155–1175` FLYING was_flying capture + early off + float_vs_flight + land/stop-flying + mkn + spoteffects(TRUE); `:1176–1180` GUARDING find_ac / YENDOR no-op; `:1183–1188` trailing setworn + off_msg + mkn tail. New words join ALREADY edges (display/do_name/polyself/hack/trap/mon/pickup/const) or the SAFE dbridge edge (hoisted functions, call-time reads only); `is_pool_or_lava` file-local one-liner cited to C dbridge.c. `js/steal.js` — wired the `steal.c:264–265` W_AMUL arm to `await Amulet_off()` (extends the ALREADY do_wear edge); doc omit line retired.
**Verify:** `node scripts/verify.mjs --fn Amulet_off` → PASS syntax (2 changed: js/do_wear.js js/steal.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full skipped (no shared file changed) → VERIFY: PASS. Tail pasted verbatim.
**Named:** none in this body — every arm and callee is live or ported in this commit. (`is_pool_or_lava` 4th file-local is by design: C is a one-line `is_pool||is_lava` with no JS export; same shape as dig/eat/trap locals.)
**Next:** Open head after Amulet_off (`wizcmds.c` misc_stats).
## 2026-09-19 — D-2528 `options.c` show_menu_controls whole body in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/options.c:9070–9174` (`show_menu_controls`). Callees: `get_menu_cmd_key` (`:8093–8104`, ported this commit — `js/options.js:767`); `wc2_supported` (`:9965–9976`, ported this commit — `js/options.js:741` + `wc2_options[]` `:9823–9842` as `js/options.js:710` + `windowprocs_wincap2`); `visctrl` (live `js/dokeylist.js:42`); `putstr` (no JS export by architecture — `lines[]` sink consumed by `show_text_pages`); `Sprintf` (template shapes mirroring `mc_fmt`/`mc_altfmt`/both hardcoded fmts); `default_menu_cmd_info[]` (`:314–340`, ported verbatim as `js/options.js:777`); `hardcoded[]` (`:9080–9086`, pre-existing `HARDCODED_MENU` `js/dokeylist.js:99` kept); `MENU_*` (`wintype.h:151–163` — `MENU_SHIFT_RIGHT`/`MENU_SHIFT_LEFT`/`WC2_EXTRASTATUS` added to `js/const.js:1294/1508`, rest already live).
**JS:** `js/const.js` (+3) + `js/options.js` (+102) + `js/dokeylist.js` (+91/−53 restart) + `scripts/show-menu-controls.test.mjs` (new, 5 tests); under caps (1500 ins / 15 files).
**Change:** `js/options.js` — `wc2_options` table in C order, `windowprocs_wincap2` (unset bag → contest tty 0, cf. `allmain.js:198` read shape), `wc2_supported`, `mapped_menu_strings` (`game.mappedMenu`, fresh-empty → identity like C `n_menu_mapped=0`), `get_menu_cmd_key` (`indexOf` for `strchr`), `default_menu_cmd_info` (13 entries, C order). `js/dokeylist.js` — restarted C-order `show_menu_controls` (`js/dokeylist.js:120`, `:line` cites per arm: `:9088` shift gate, `:9094` title, `:9101–9108` dolist loop with `:9102–9104` shift skip + `%-7s %s` shape, `:9110–9111`/`:9152–9153` fmt/arg per arm, `:9113–9137` help headers + Select/Invert/Deselect + Go-to rows, `:9138–9145` `Pan view` gate (new), `:9146–9151` Search with verbatim `Exter` typo + separator, `:9155–9159` trailing loop); `show_menu_controls_lines` kept as a delegating alias so existing callers stay wired.
**Verify:** `node scripts/verify.mjs --fn show_menu_controls` → PASS syntax (3 changed: js/const.js js/dokeylist.js js/options.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed) → VERIFY: PASS. Tail pasted verbatim. Focused `node --test scripts/show-menu-controls.test.mjs` 5/5 PASS.
**Named:** `add_menu_cmd_alias` + menu-key BIND parsing (writer of `mapped_menu_*`; `game.mappedMenu` stays empty → identity, matching fresh C); `map_menu_cmd`/`collect_menu_keys` (separate C functions, untouched); non-tty `wincap2` bits (tty default 0 — shift arms verified via the focused test, not sessions).
**Next:** Open head after show_menu_controls (`do_wear.c` Amulet_off).
## 2026-09-19 — D-2527 `do.c` dosinkring + teleport_sink whole bodies in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/do.c:459–494` (`teleport_sink`, staticfn) + `:498–661` (`dosinkring`, staticfn). Callees: `rnd`/`rn2` (`:472–473/:491`, live rng edge, C `1+rnd`/`1+rn2` ranges transliterated as-is); `t_at` (`:476`, live trap edge); `engr_at` (`:476`, live engrave edge — extends the ALREADY edge); `cansee` (`:477`, live vision edge); `distu` (`:477`, file-local `js/do.js:494` squared); `set_levltyp` (`:482/:486`, shared sink-count analog `dipsink_set_levltyp` exported from `fountain.js` this commit); `newsym` (`:484/:488`, live display edge); `looted` plain field (`:480/:483/:487`, `alreadylooted ? 1 : 0` kept); `You` (`:501`, live display edge); `doname` (`:501`, live objnam edge); `in_use` plain field; `yname` (`:505`, live objnam edge); `dropx` (same-file async); `trycall` (live do_name edge — extends the ALREADY edge); `pline_The` (live display edge); `makeplural` (live objnam edge); `fruitname(FALSE)` (`:527`, `objnam.c:414` — exported from `potion.js` this commit, no clone #2); `pline` (live display edge); `rndmonnam` (`:531`, live do_name edge); `Hallucination` (file-local `js/do.js` — extends no edge); `Soundeffect`/`se_drain_noises`/`se_ring_in_drain` (`:535/:643`, re-export extended in `sndprocs.js`); `You_hear` (file-local clone `js/do.js` reused — hack.js export noted by sym, no new clone); `hliquid` (live do_name edge); `obj_resists(otmp,1,99)` (`:556`, live dogmove edge); `otense` (live objnam edge); `delobj` (live mkobj edge); `objects_at` (live mkobj edge — extends the ALREADY edge); `uball`/`uchain` (`u.uball`/`u.uchain`); `Blind` (file-local); `polymorph_sink` (`:583`, `do.c:403` non-static — exported from `fountain.js` this commit, no clone #2); `levl[][].typ != ROOM` via `level.at`; `You_see` (live display edge — extends the ALREADY edge); `hcolor` (file-local clone reused); `NH_BLACK`/`NH_SILVER`/`NH_WHITE` string prefs (read.js precedent); `freeinv` via file-local `freeinv_drop`; `add_to_buried` (live mkobj edge); `useup` (live invent edge — extends the ALREADY edge); `RIN_*`/`MEAT_RING` `objectNames.indexOf` consts (do_wear precedent); `RING_CLASS` (live objects edge); `SINK`/`ROOM`/`IS_SINK`/`COLNO`/`ROWNO` (live const edge).
**JS:** `js/do.js` (+292/−17 with the arm + consts) + `js/fountain.js` (+6: two `export` words) + `js/potion.js` (+3: one `export` word) + `js/sndprocs.js` (+2 se re-exports); under caps (1500 ins / 15 files).
**Change:** `js/do.js` — file-local `teleport_sink` (`js/do.js:2449`) in C order (`:472–473` `#else` ranges with the `#if 0` edge arm named as compiled out, `:475–490` ROOM/trap/engr/sight-distance gate + old-sink removal + new-sink creation + `newsym` pair, `:491` 200-try cap); exported `dosinkring` (`js/do.js:2489`) in C order (`:501` drop message, `:502` `in_use`, `:504–513` SEARCHING `goto giveback` tail shared with `:514–520` SLOW_DIGESTION, `:521–575` all twelve no-eyes arms incl. `:553–566` RIN_HUNGER `objects_at`/`nexthere` walk with `otmp2` saved across `delobj`, `:576–581` TELEPORTATION `nosink`, `:582–587` POLYMORPH `nosink=TRUE` + ROOM-gated `ideed`, `:588–591` default, `:592–633` Blind-gated eyes switch with `ideed=TRUE` set before it, `:634–647` trycall/drainpipe tail, `:648–661` backup/`:651–656` buried (`freeinv_drop` + `ox`/`oy` + `add_to_buried`)/`useup` lottery). New words join ALREADY edges (do_name, invent, mkobj, engrave) or the same 95-module SCC with function-body-only reads (fountain `polymorph_sink`/`dipsink_set_levltyp`, potion `fruitname` — `imports.mjs --can`: no eval-time TDZ read).
**Verify:** `node scripts/verify.mjs --fn dosinkring` → PASS syntax (4 changed: js/do.js js/fountain.js js/potion.js js/sndprocs.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (auto: shared file changed) → VERIFY: PASS. Tail pasted verbatim.
**Named:** none in these bodies — every arm and callee is live or ported in this commit. `delobj_core`'s internal resists re-check is pre-existing shared behavior (unchanged); the `#if 0` teleport edge-squares arm is compiled out of C; `hcolor` Hallu synonym stays deferred per the file-local clone.
**Next:** Open head after dosinkring (`options.c` show_menu_controls).
## 2026-09-19 — D-2526 `exper.c` losexp whole body in C order (coverage PARTIAL → live) + `trap.c:4294` caller wire

**C locus:** `nethack-c/upstream/src/exper.c:207–291` (`losexp`). Callees: `resists_drli` (`:216`, live `js/zap.js:3715` — undead/demon/were/lycan/Death/vampshifter + `defended(AD_DRLI)`); `Goodbye` (`:224`, live roles edge); `pline` (`:224`, live display edge); `adjabil` (`:229`, live attrib edge); `livelog_printf` (`:230/:245`, live pline edge); `done` (`:237`, live end edge); `minuhpmax` (`:250`, live attrib edge); `setuhpmax` (`:254/:261`, same-file `js/exper.js:104` — TRUE→true keeps uhpmax updates while Upolyd); `newuexp` (`:280`, same-file); `monhp_per_lvl` (`:283`, live `js/makemon.js:964`); `rehumanize` (`:287`, live `js/polyself.js:961` async); `Strcpy` (`:236`, plain assignment); `Upolyd` (`:282`, `Upolyd(u)` const edge).
**JS:** `js/exper.js` (+~50/−~60 net restart) + `js/trap.js` (+~10); under caps (1500 ins / 15 files).
**Change:** `js/exper.js` — restarted `losexp` in C order with `:line` cites: `:212–217` #levelchange null else live `resists_drli(game.youmonst)`; `:219–224` Goodbye message incl. level-1 fatal shape; `:226–231` level loss + adjabil + livelog; `:232–245` level-1 fatal (`KILLED_BY` + drainer, `done(DIED)`, `:240–243` fuzz early-return kept) else `uexp=0` + chronicle; `:249–261` `olduhpmax`/`minuhpmax(10)`/`uhpinc` tail with both `setuhpmax(...,true)` clamps; `:263–277` uhp/uen clamps; `:279–280` `newuexp(ulevel)-1`; `:282–288` Upolyd `monhp_per_lvl` strip + `rehumanize`; `:290` botl. New words join SAFE/ALREADY edges (`imports.mjs --can` exper→zap/polymon/makemon: same 95-module SCC, hoisted functions, no eval-time TDZ read); local clone + its now-unused `is_undead/is_demon/is_were/is_vampshifter/ismnum` import words removed. `js/trap.js` — wired the `:4285–4297` floor-path block in C order (`minuhpmax(1)` + `olduhpmax`, `d(2,4)`, rn2 burn, `setuhpmax(min(olduhpmax,uhpmin),false)` + `!Drain_resistance()` → `await losexp(null)`); names join ALREADY edges (exper/attrib/zap); doc-comment omit line updated (dofiretrap row itself stays Open for box/shieldeff/golem-alt/surface arms).
**Verify:** `node scripts/verify.mjs --fn losexp` → PASS syntax (2 changed: js/exper.js js/trap.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. Tail pasted verbatim.
**Named:** `SoundAchievement(:231)` (no SND_LIB, same as pluslvl); `fuzzer_savelife` (debug-fuzz only — `:240–243` return kept); `attrib.c:261` `#if 0` arm (C never compiles it); dofiretrap box/shieldeff/golem-alt/surface arms (own Open row, untouched).
**Next:** Open head after losexp (`do.c` dosinkring).
## 2026-09-19 — D-2525 `objnam.c` vtense whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/objnam.c:2563–2653` (`vtense`). Callees: `nextobuf` (return buffer — no JS clone needed, strings immutable); `strncmpi` (`:2581/:2585–2589`, file-local `startsWithCI` with C NUL-short semantics); `lowc` (`:2602/:2612/:2638–2647`, file-local `plural_lowc`); `BSTRNCMPI` (`:2604–2607`, underrun guards `last-3`/`last-1 >= 0`); `special_subjs` (`:2609–2620`, file-local `SPECIAL_SUBJS` — same table `makesingular` uses); `strcmpi` (`:2626/:2634/:2636`, file-local `eqCI`); `Strcpy` (plain assignment); `Strcasecpy` (`:2635/:2637/:2644/:2647/:2650`, file-local `strcasecpy_at` via `chrcasecpy` — per-char case incl. `HAVE`→`HAS`); `vowels` (`:2647`, file-local `MAKEPLURAL_VOWELS`); `strlen` (`.length`). `otense` (`:2541` same-file caller) already live (`js/objnam.js:2332`, `vtense(null, verb)` arm kept).
**JS:** `js/objnam.js` (+~100/−~27 net); under caps (1500 ins / 15 files).
**Change:** `js/objnam.js` — restarted `vtense` in C order with `:line` cites: `:2581–2582` a/an prefix (NUL-short-safe); `:2583–2593` marker scan (first-space walk, break on first marker, index-0 guard); `:2594–2596` head fallback; `:2600–2607` plural guess (`*us`/`*ss` veto, eeth/feet/ia/ae with BSTRNCMPI underrun guards); `:2609–2620` special_subjs exact + `<prefix> <spec>` space-tail veto; `:2625–2627` they/you; `:2630–2652` `vtenseSing` (are→is at 0, have→has at len-2, z/x/s + ch/sh + len-2 -o → +es at len, consonant-y → -ies at len-1, else +s at len — all via `strcasecpy_at`). No new cross-module import (all helpers file-local, defined above the use; no TDZ risk). Throwaway probe `/tmp/vtense-probe.mjs` (43 cases incl.
**Verify:** `node scripts/verify.mjs --fn vtense` → PASS syntax (1 changed: js/objnam.js) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. Tail pasted verbatim.
**Named:** none in this body — every arm and callee is live or file-local in this commit. `nextobuf` needs no port (immutable-string return).
**Next:** Open head after vtense (`exper.c` losexp).
## 2026-09-19 — D-2524 `sounds.c` dochat whole body in C order + `shop_object`/`price_quote` (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/sounds.c:1257–1409` (`dochat`). Callees: `is_silent` (`:1262`, mondata.h:62 inline `msound == MS_SILENT` — same-file `MS_SILENT` const); `pmname` (`:1264`, live do_name edge) + `an` (`:1264`, live objnam edge) + `pline` (`:1263`, live display edge); `You_cant` (`:1268`, live display edge); `Your` (`:1276`, live display edge); `shop_object` (`:1280`, `shk.c:5386–5403` — ported this commit: `in_rooms` live hack edge, `shop_keeper`/`inhishop`/`costly_spot` live shk exports, `NOTANGRY`/`muteshk` same-file clones, `objects_at` live mkobj edge + `nexthere` walk, `COIN_CLASS` live objects edge); `price_quote` (`:1288`, `shk.c:5406–5465` — ported this commit: `inside_shop` same-file, `get_cost`/`contained_cost`/`get_pricing_units` same-file clones, `Has_contents` live const edge, `doname` live objnam edge, `currency` live invent edge, `SetVoice` live sndprocs edge, `upstart` live hacklib edge, `verbalize` live display edge, `show_nhw_menu_text` pager edge via dynamic import — shk↔pager precedent `js/shk.js:4840`, `destroy_nhwindow` no-op) + staticfn `shk_embellish` (`shk.c:5467–5505` — ported same-file: `rn2` live rng edge, `objects()` table, `AMULET_CLASS`/`RING_CLASS` added to the objects edge); `getdir` (`:1291`, live lock edge); `helpless` (`:1299/:1376`, monst.h:251 inline — file-local `js/sounds.js:792`, no new clone); `Monnam` (`:1300/:1379/:1391`, live do_name edge); `domonnoise` (`:1303/:1408`, live same-file); `isok` (`:1331`, live const edge); `mon_at` (`:1333`, ≡ `m_at` — D-0140 precedent, live uhitm edge); `objects_at` (`:1337`, ≡ `vobj_at` — D-0140 precedent); `pline_The` (`:1340`, live display edge); `rndmonnam` (`:1342`, live do_name edge, display-RNG convention kept); `IS_WALL`/`SDOOR` (`:1346`, live const edge); `rn2` (`:1360`, live rng edge); `canspotmon` (`:1380/:1390/:1400`, live display edge); `map_invisible` (`:1391`, live display edge); `humanoid` (`:1397`, live monsters edge); `mon_nam` (`:1401`, live do_name edge). `Deaf`/`Blind` per youprop.h (`HDeaf|EDeaf|uroleplay.deaf`, `(HBlinded|EBlinded)&&!BBlinded`); `Underwater` ≡ `u.uinwater` (dokick precedent); `Strangled` ≡ `uprops[STRANGLED].intrinsic` (apply precedent).
**JS:** `js/sounds.js` (+~120/−~70) + `js/shk.js` (+~113) + test; under caps (1500 ins / 15 files).
**Change:** `js/sounds.js` — restarted `dochat` in C order with `:line` cites: `:1262–1278` four gates; `:1280–1290` shop arm (`shop_object` + `price_quote`, `ECMD_TIME`); `:1292–1325` getdir/steed/dz/self; `:1330–1369` isok + statue (`pline_The` + Hallu `rndmonnam`) + wall/SDOOR; `:1371–1383` M_AP consts + sleeping `canspotmon`; `:1386` `mstrategy &= ~STRAT_WAITMASK` (named const, value-identical); `:1388–1405` tame `!Deaf` + `map_invisible` and Deaf `humanoid`/`mon_nam` response; `:1407–1408` `domonnoise`. New words join ALREADY edges only (`You_cant`/`Your` display, `mon_nam`/`rndmonnam` do_name, `shop_object`/`price_quote` shk — sounds↔shk static cycle pre-existed via `shk_chat`; `imports.mjs --can` ALREADY, no TDZ read at eval) + `STRANGLED` const word. `js/shk.js` — exported `shop_object`, file-local `shk_embellish`, exported async `price_quote` (+ `THE_CONTENTS_OF` from `:63`, `AMULET_CLASS`/`RING_CLASS` import words).
**Verify:** `node scripts/verify.mjs --fn dochat` → PASS syntax (2 changed) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. Tail pasted verbatim. Focused test 8/8 PASS.
**Named:** none in these bodies — every arm and callee is live or ported in this commit.
**Next:** Open head after dochat (`objnam.c` vtense).
## 2026-09-19 — D-2523 `do.c` canletgo whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/do.c:665–711` (`canletgo`). Callees: `Norep` (`:669/:680`, live display edge); `welded` (`:673`, live wield edge — sets bknown per `wield.c:1051–1058`, no weldmsg here); `body_part(HAND)` (`:677/:706`, live polyself edge, HAND live const edge); `bimanual` (`:679`, live wield edge, same-module ALREADY); `makeplural` (`:680`, live objnam edge); `pline` (`:695`, live display edge); `plur` (`:696`, `dungeon.c` inline `quan == 1 ? '' : 's'`, no new clone); `set_bknown` (`:699`, live mkobj edge, same-module ALREADY); `pline_The` (`:706`, live display edge); `You` (`:707`, live display edge). `something` = `c_common_strings.c_something` (`decl.c:45`).
**JS:** `js/do.js` (+~40/−~30 net) + `js/dothrow.js` (+~8 net incl. import); under caps (1500 ins / 15 files).
**Change:** `js/do.js` — restarted `canletgo` in C order with `:line` cites: `:667–672` worn armor/accessory Norep; `:673–686` welded uwep with `body_part(HAND)` + bimanual plural; `:687–702` cursed loadstone with `throw`-count corpsenm kludge, full `any of`/plur message, unconditional corpsenm reset + `set_bknown`; `:703–708` leash `pline_The`; `:709–711` saddle `You`. `*word` as explicit non-empty gate. New same-module import words only (`You`/`pline_The` display, `bimanual` wield, `set_bknown` mkobj — all ALREADY edges, no TDZ risk).
**Verify:** `node scripts/verify.mjs --fn canletgo` → PASS syntax (2 changed) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 (shared file changed) → VERIFY: PASS.
**Named:** `ball.js` silent clone omits loadstone/leash gates + corpsenm/`set_bknown` state (avoids do.js cycle; its `ball.c:973` otmp is ball/chain, word="" so no messages); `muse.js` silent clone omits corpsenm reset + `set_bknown` inventory-update side effect (direct `bknown = 1`; word="" so no messages); Mjollnir/too-heavy `throw_obj` gates stay named in `dothrow.js` (separate C arms, not canletgo's).
**Next:** Open head after canletgo (`sounds.c` dochat).
## 2026-09-19 — D-2522 `sounds.c` tiphat whole body in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/sounds.c:1427–1537` (`tiphat`) + same-file staticfn `responsive_mon_at` `:1413–1425`. Callees: `cursed` (`:1437`, live home do_wear.js `cursed_check` — message stored in `game._cursed_takeoff_msg`, bknown set, do_takeoff precedent); `getdir` (`:1442`, live lock.js edge); `You`/`pline`/`pline_The` (`:1450/:1455–1462`, live display edge); `helm_simple_name` (`:1450`, C-locus home do_wear.js); `helpless` (`:1455`, monst.h:251 inline — mhitm.js local is not exported); `domonnoise` (`:1457`, live same-file, ECMD_OK=0/ECMD_TIME=1 so `await` truthiness matches C); `isok` (`:1469`, live const edge); `couldsee` (`:1469`, live vision edge); `m_at` (`:1473`, ≡ `mon_at` uhitm.js, sounds.js already imports it); `canseemon`/`glyph_at` (`:1474–1475`, live display edge); `glyph_is_invisible` (`:1476`, int form ≡ `glyph_is_invisible_id`); `glyph_is_statue` (`:1477`, live display edge); `vobj_at` (`:1478–1480`, ≡ `objects_at` — dochat precedent); `M_AP_FURNITURE/M_AP_OBJECT` (`:1481–1482`, live const edge); `responsive_mon_at` (`:1487/:1497`, ported same-file: `helpless` inline + `haseyes`/`perceives` live edges + `Invis()` timeout.js edge); `is_silent` (`:1488`, mondata.h:62 inline `msound === MS_SILENT`, same-file const); `accessible` (`:1493`, live monmove.js edge) + `IRONBARS` (`:1493`, live const edge); `humanoid` (`:1504/:1515`, live monsters edge); `which_armor` (`:1505`, live worn.js edge); `mhis` (`:1509/:1513`, live fountain edge); `Monnam` (live do_name edge); `Conflict()` (`:1504`, live mhitu edge); `Deaf` (`:1519/:1527`, dosounds :430 idiom); `rn2`/`rn1` (`:1519–1521`, live rng edge); `next2u` (`:1526`, you.h:558 inline `dist2 <= 2` — dist2 already imported); `map_invisible` (`:1528`, live display edge); `BOLT_LIM`/`GLYPH_MON_OFF`/`W_ARMH`/`STRAT_WAITMASK`/`nothing_happens` (live const/display edges). New cross-module edges all `imports.mjs --can` SAFE (`which_armor` worn, `cursed_check`/`helm_simple_name` do_wear, `accessible` monmove, `Invis` timeout); mon.js/mondata/mhitu/vision/rng additions join ALREADY edges.
**JS:** `js/sounds.js` (+168/−13 net incl. imports) + `js/pickup.js` (+9/−2); under caps (1500 ins / 15 files).
**Change:** `js/sounds.js` — file-local `responsive_mon_at` (`:1416` m_at cite, `:1418–1422` helpless/is_silent cites) + exported async `tiphat` in C order with `:line` cites: `:1432–1435` no-helm/res gates; `:1437–1438` cursed_check + stored-msg pline; `:1442–1445` getdir ESC; `:1450` doff You; `:1452–1463` nodir steed/dz/lout arms; `:1465–1495` ray walk (isok/couldsee step-back, mon_at/canseemon/glyph triple, statue-object arm, M_AP clear, four break arms incl. range-1 responsive + accessible/IRONBARS); `:1497–1534` react (ignoring / not-notice / nada fallthroughs, mstrategy clear, peaceful wave/grasp/tip, hostile reaction table with Deaf/rn1 doubling, next2u+domonnoise+map_invisible, doesn't-respond). Short-circuit, RNG-draw order, and ECMD truthiness preserved.
**Verify:** `node scripts/verify.mjs --fn tiphat` → PASS syntax (2 changed) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS.
**Named:** none in the port — every callee is a live import or a same-file port (`responsive_mon_at`); `helpless`/`is_silent`/`next2u` are header-macro inlines with cites, not clones (no JS export exists for any of the three). Caller-side spill/statue arms stay named in `dotip` (separate C arms, not tiphat's).
**Next:** Open head after tiphat (`do.c` canletgo).
## 2026-09-19 — D-2521 `pager.c` look_engrs whole body in C order (coverage PARTIAL → live)

**C locus:** `nethack-c/upstream/src/pager.c:2144–2228` (`look_engrs`, staticfn). Callees: `create_nhwindow`/`display_nhwindow`/`destroy_nhwindow`/`putstr`/`putmixed` (`:2154/:2204/:2208/:2219/:2224/:2227` — JS `show_text_pages`, the D-2508 `look_all` idiom, no new window code); `look_region_nearby` (`:2155`, live same-file export); `engr_at` (`:2166`, live); `IS_GRAVE(svl.lastseentyp[x][y])` (`:2169`, live const edge); `add_quoted_engraving(...,TRUE)` (`:2171`, live same-file); `strsubst` ×4 (`:2175–2179`, live hacklib edge); `glyph_at`/`glyph_is_cmap`/`glyph_to_cmap`/`SYM_NOTHING` (`:2182–2183`, live display edge); `is_cmap_engraving` (`sym.h:108`, inline pair); `cmap_to_glyph(S_grave)`/`engraving_to_glyph(e)` (`:2191–2192`, `display.h` CORR→`S_engrcorr` else `S_engroom`, inline via live `cmap_to_glyph`); `encglyph` ×2 (`:2190/:2215` — tty-rendered via same-file `rendered_glyph_char` for the live map glyph and `glyph_showsym_code` for the re-pointed one, no botl import); `upstart` (`:2204`, live); `coord_desc` + `%s/%8s/%12s` (`:2198/:2211–2214` — MAP formats raw `<x,y>` per `getpos.c`, SCREEN/COMPASS via the local helper); `Sprintf`/`Snprintf`/`Strcat`/`eos`/BUFSZ guard (`:2170/:2189/:2216–2218`, inline string ops). Callers `:1878/1881` (`dowhatis` `/e` nearby, `/E` level).
**JS:** `js/pager.js` (+95/−48 net; under caps 1500 ins / 15 files).
**Change:** `js/pager.js` — restarted `look_engrs` (`:2358`) in C order with `:line` cites: `:2155` region holder; `:2160` seenv gate; `:2166` engr_at (no gone-engraving fallback per `:2162–2165`); `:2169` headstone from `game.lastseentyp[x][y]`; `:2170–2171` `" (grave"`/`" (engraving"` holder + force-TRUE quote; `:2174–2179` strsubst assign-backs; `:2182–2184` sym triple (`S_engroom`/`S_engrcorr`/`S_grave`); shown arm `:2185–2186` keeps the live glyph char; covered arm `:2187–2193` appends `", obscured by <covering>"` and re-points (grave→`S_grave`, else CORR-aware engraving defsym); `:2195` redundant-guard kept; `:2200–2208` upstart header + separator; `:2210–2219` coord pad (MAP raw) + rendered char + BUFSZ slice + line; `:2223–2227` display vs `pline("No engravings…")`. Same-edge import words only (`imports.mjs --can` ALREADY: `strsubst` on hacklib, `IS_GRAVE` on const; no new cross-module edge, no TDZ risk).
**Verify:** `node scripts/verify.mjs --fn look_engrs` → PASS syntax (1 changed) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 (seed2200 screen@103 owner=look_engrs fixed by the MAP-pad arm) → VERIFY: PASS.
**Named:** compass-full coord text (local `coord_desc` `(here)` deferral, pre-existing — same as `look_all` D-2508); `eos` as a named function (inline `+=`/slice, no JS export exists); NHW_TEXT window primitives (covered by the `show_text_pages` idiom).
**Next:** Open head after look_engrs (`sounds.c` tiphat).
## 2026-09-19 — Audit 1471–1479 (reviews for D-2512…D-2520) + cadence

Review iter over 9 JS commits (3322beff…95d26622), one file per SHA written as each finished. All **ACCEPT**, no Must-fix, no STOP. Per-SHA hidden-proxy re-runs at each parent: 0 blocked baselines matched, 0 regressed; reach lines reproduced (getrumor 129/129, makerooms 481/481, mhitm_ad_drli 8/8, rest smoke 24/24). Audit note: verify's `moved → js-throw` text is the harness null-owner fallback (`hidden-proxy.mjs:468 ${r.owner || 'js-throw'}`) — Valkyrie-92162 at 144 has `error: null`, screen kind, full RNG 14975/14975, as D-2516 logged. Cadence: full sessions 44/44 (Scr 11405/11405, RNG 792838/792838); held-out 11/44 (5648 pts, RNG 26.6%, screens 50.1%, unchanged); corpus re-score 497/540 (92.0%, +2 PASS: Valkyrie-92200, Samurai-92018; 0 regressed).
## 2026-09-19 — D-2520 `mkobj.c` insane_object + where_name whole bodies in C order (coverage MISSING → live)

**C locus:** `nethack-c/upstream/src/mkobj.c:3314–3339` (`insane_object`, staticfn). Same-file callee `where_name` `:3296–3309` + `obj_state_names` table `:3289–3293`; format `ofmt0` `:2940–2944` (`"%s obj %s %s: %s"`); cross-file callees `doname` (live), `strstri` (live), `x_monnam(mon, ARTICLE_A, 0, EXACT_NAME, TRUE)` (live), `impossible` (live, async in JS), `fmt_ptr` (`alloc.c:125–135`). 29 call sites in 8 same-file functions (`obj_sanity_check` ×10, `objlist_sanity` ×6, `shop_obj_sanity` ×2, `mon_obj_sanity` ×4, `insane_obj_bits` ×1, `check_contained` ×1, `check_glob` ×1, `sanity_check_worn` ×4).
**JS:** `js/mkobj.js` (+79/−6); under caps (1500 ins / 15 files).
**Change:** `js/mkobj.js` — `OBJ_STATE_NAMES` verbatim in OBJ_* order (`:3289–3293`); exported `where_name` (`js/mkobj.js:1534`) in C order (`:3299–3308`: null → "nowhere", range/empty-slot → `unknown[${where}]`, else table); file-local `fmt_ptr` (`:1549`, alloc.c `:125–135` — no `js/alloc.js` exists; JS has no heap pointers so it renders the port's stable identity `o_id ?? m_id ?? 0` as 0x-hex, timeout.js `fmt_timer_arg` precedent); `OFMT0_SANITY` (`:1555`, ofmt0 verbatim); exported async `insane_object` (`:1565`) in C order (`:3320–3325` `override_ID++/doname/--` on `game.iflags`, allmain.js precedent; `:3327–3331` mon-or-minvent-mesg arm appends `" held by mon %s (%s)"` and names via `x_monnam(mon, ARTICLE_A, null, EXACT_NAME, true)`; `:3332–3337` plain arm). New import words all same-edge (`imports.mjs --can` ALREADY: `x_monnam` on do_name, `strstri` on hacklib; `NOBJ_STATES`/`ARTICLE_A`/`EXACT_NAME` join the live const edge). Async only because JS `impossible()` can reach --More--.
**Verify:** `node scripts/verify.mjs --fn insane_object` → PASS syntax (1 changed) · rule2 · hidden note (0 blocked at baseline) · reach smoke 24/24 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. `node --check js/mkobj.js` + import smoke (`where_name` floor/nowhere/unknown[42], `insane_object` exported function) OK.
**Named:** the 7 unported C caller functions (`obj_sanity_check`, `objlist_sanity`, `shop_obj_sanity`, `mon_obj_sanity`, `insane_obj_bits`, `check_contained`, `sanity_check_worn` — none exists in `js/`; their insane_object call sites wire up when each ships); `fmt_ptr` exported home (`alloc.c` has no `js/alloc.js` — file-local in `mkobj.js` until then); `check_contained`'s direct 3-ptr `impossible` arm (ships with `check_contained`).
**Next:** Open head after insane_object (`pager.c` look_engrs).
## 2026-09-19 — D-2519 `mklev.c` makerooms whole body in C order (coverage THIN → live)

**C locus:** `nethack-c/upstream/src/mklev.c:366–436` (`makerooms`, staticfn). Callees: `nhl_init`/`nhl_loadlua`/`nhl_done`/`nhl_pcall_handle` (lua), `create_des_coder`, `rn2`/`rnd_rect`/`create_room`/`create_vault`/`reset_xystart_size` (live), `themerms.lua:982–1008` (pre/post hooks). Data: `gl.luathemes[dnum]`, `svd.dungeons[dnum].themerms`, `gt.themeroom_failed`, `gv.vault_x/y`. Sole caller `mklev.c:1298` (`makelevel` regular-room branch).
**JS:** `js/mklev.js` (+~85/−30); under caps (1500 ins / 15 files).
**Change:** `js/mklev.js` — restarted `makerooms` in C order with `:line` cites: `:369–370` inits; `:373` themes handle ⇔ `g._luathemes_loaded[dnum]` (marked once per branch by `makelevel_ordinary`; compiled-in THEMEROOM tables, load cannot fail); `:392–399` pre block (`pre_themerooms_generate` new local, debug-env body unported); `:403` loop cond; `:404–410` vault arm unchanged; `:412–421` themed arm — return value ignored per C, break on `themeroom_failed` alone; `:422–424` else `create_room(-1,-1,-1,-1,-1,-1,OROOM,-1)` arm (dead while themes always load, live fallback); `:428–435` post block (guarded `reset_xystart_size` + empty `post_themerooms_generate` local). `themerooms_generate` no-pick path now calls `impossible('no eligible themed rooms?')` per `themerms.lua:975–978` (still returns false with the flag clear, so the loop does not break — as C). No new cross-module edge (all same-file locals; `rn2`/`rnd_rect`/`OROOM`/`MAXNROFROOMS`/`depth_of_level`/`impossible` already imported).
**Verify:** `node scripts/verify.mjs --fn makerooms` → PASS syntax (1 changed) · rule2 · hidden note (0 blocked at baseline) · reach 80/80 sample → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 · full 44/44 → VERIFY: PASS. `--reach-all` → **481/481 PASS, 0 regressed → REACH-OK**. `/tmp/probe-makerooms.mjs` 16/16 PROBE-OK (every C arm present in C order, no `!ok` consult, caller wired).
**Named:** lua runtime (`nhl_init`/`nhl_loadlua`/`nhl_done`/`nhl_pcall_handle`/`lua_getglobal` — rooms compiled in, no lua by architecture); `create_des_coder` (its SpLev_Map memset already runs in `reset_xystart_size`); `iflags.in_lua` (error-message flag, no JS counterpart); THEMERM/THEMERMFILL debug room/fill selection (`pre_themerooms_generate` body).
**Next:** Open head after makerooms (`mkobj.c` insane_object).
## 2026-09-19 — D-2518 `uhitm.c` mhitm_ad_drli defended(AD_DRLI) gap closed (coverage THIN → complete)

**C locus:** `nethack-c/upstream/src/uhitm.c:2445–2518` (uhitm `:2450–2477`, mhitu `:2479–2488`, mhitm `:2489–2515` + AD_DETH redirect); `mondata.c:199–211` resists_drli (`:210 return defended(mon, AD_DRLI)`); `mondata.c:91–124` defended. Callers `:3892` (mhitm_ad_deth redirect) and `:4804` (mdamagem-table case AD_DRLI).
**JS:** `js/zap.js` (+5/−4) + `js/uhitm.js` (+4/−5); under caps (1500 ins / 15 files).
**Change:** `js/zap.js` — `resists_drli` returns `defended(mon, AD_DRLI)` per C `:210`; `defended` joins the existing mondata edge (`imports.mjs --can` ALREADY, no new edge). `js/uhitm.js` — `damageum_ad_drli` mirrors the C caller exactly: `!(resists_drli(mdef) || defended(mdef, AD_DRLI))` (`defended` already imported at line 103). Both predicates are pure (no RNG), so short-circuit/RNG order is unchanged.
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_drli` → PASS syntax (2 changed) · rule2 · hidden note (0 blocked at baseline) · reach 8/8 PASS, 0 regressed → REACH-OK · green 2/2 · strict ×2 · cohort 7/7 → VERIFY: PASS. `node scripts/verify.mjs --full` → PASS full 44/44 (fortress holds). No maintained unit-test dir exists in-repo; corpus REACH + full sessions are the harness.
**Named:** split-arm homes stay split by architecture (damageum_adtyping / mhitm_adtyping_u / mdamagem-table); polyself `resists_drli_you` keeps its C-cited uwep-suppressed local form (never calls `resists_drli`, unaffected); sibling `defended(AD_COLD)`-style omits untouched.
**Next:** Open head after mhitm_ad_drli (`mklev.c` makerooms).
