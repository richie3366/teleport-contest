# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-20 — D-2643 `sp_lev.c` sp_level_coder_init whole-body port (coder object + update_croom + create_des_coder lifecycle)

**C locus:** ``nethack-c/upstream/src/sp_lev.c:6336–6376`` (sp_level_coder_init) + ``:6323–6333`` (update_croom) + ``:6443–6448`` (create_des_coder) + ``load_special :6454–6501``. Decisive C facts: field defaults ``:6341–6348`` (allow_flips 3, n_subroom 1); statics ``:6350–6351``; tmproomlist/failed_room over ``0..MAX_NESTED_ROOMS`` ``:6353–6356`` (sp_lev.h:49 = 5); the ``:6358`` update_croom() reads the *prior* ``gc.coder``, not the local under construction; container zero ``:6360–6362``; invent NULL ``:6364``; SpLev_Map memset ``:6366``; flags ``:6368–6371`` (``In_hell(&u.uz)``); reset ``:6373``; return ``:6375``. Sole caller create_des_coder guards ``:6446`` + assigns ``:6447``; load_special creates at entry ``:6459`` and Free+NULLs at give_up ``:6497–6499``.
**JS:** ``js/mklev.js`` (+~100: 2 exports + 1 local + wrapper split + const import + invent let); ``docs/c-js-map/data.md`` (coder-lifecycle clause, cited D-2643).
**Change:** new exported ``sp_level_coder_init`` in C-home ``js/mklev.js`` in C order with per-arm ``:line`` cites — object literal for ``alloc :6339`` (GC, no live alloc export), ``MAX_NESTED_ROOMS + 1`` arrays (const.js import extended), statics via the existing helper ``:6350–6351``, prior-coder ``update_croom() :6358``, container loop + ``container_idx = 0 :6360–6362``, new ``invent_carrying_monster`` module let (sp_lev.c:198) nulled ``:6364``, SpLev_Map fresh Set ``:6366``, ``game.level.flags`` arm ``:6368–6371`` (hellish-flag read ≡ ``In_hell``, mklev.js:1416 precedent), ``reset_xystart_size() :6373``. New exported ``update_croom`` (``:6326–6332``; ``?? null`` maps OOB slots to C NULL) + local ``create_des_coder`` (``:6446–6447`` guard). ``load_special_proto`` split into a wrapper (``create_des_coder`` at entry ``:6459`` + ``try/finally`` Free+NULL ``:6498–6499`` so every load re-inits fresh like C) over the renamed dispatch body (its old reset+statics preamble removed as subsumed).
**Verify:** ``node scripts/verify.mjs --fn sp_level_coder_init`` → VERIFY: PASS — syntax (1 changed: js/mklev.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared mklev.js changed). ``/tmp/smoke_coder.mjs`` → ALL SMOKE CHECKS PASS (defaults incl. allow_flips 3 / n_subroom 1 / 6+6 arrays; flags incl. hellish→temperature 1; xystart/size; SpLev_Map cleared; croom track + null arms with live coder; no-coder no-op).
**Named:** ``alloc`` (GC object — allocation, not semantics); ``load_special`` post-``load_lua`` epilogue reads of the coder fields (link/edge, wallification, flip, solidify, fixup, premap — each loader's named omits stand, future rows); ``lvl_is_joined`` has no live reader yet (kept on the object per C ``:6347``).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2642 `polyself.c` change_sex whole-body port (pl_character rename + amorous-demon set_uasmon + live max_rank_sz)

**C locus:** ``nethack-c/upstream/src/polyself.c:273–303`` (change_sex). Decisive C facts: the ``!Upolyd`` disjunct is necessary because ``is_male()/is_female()`` may be true for certain roles (``:273–281``); poly'd flips saved ``u.mfemale`` too (``:285–286``); ``max_rank_sz()`` ``:287`` (``[this appears to be superfluous]`` — still called); ``svp.pl_character`` takes ``gu.urole.name.f`` iff the new sex is female and ``name.f`` exists, else ``name.m`` (``:288–291``); ``!Upolyd`` restores ``u.umonnum = u.umonster`` (``:292–293``); ``PM_AMOROUS_DEMON`` flips ``flags.female`` again and calls ``set_uasmon()`` (``:294–301``) with the succubus/incubus swap ``#if 0``-disabled (``:296–300``). Callee ``max_rank_sz`` is ``botl.c:402–415``: max ``strlen`` over ``gu.urole.rank[9]`` ``m``/``f`` into ``gm.mrank_sz``.
**JS:** ``js/polyself.js`` (restarted export + 2 import names); ``js/botl.js`` (+24: ``max_rank_sz`` export); ``docs/c-js-map/turns.md`` (change_sex clause, cited D-2642).
**Change:** restarted the export in C order with per-arm ``:line`` cites — sexless-form first flip ``:281–284``, mfemale ``:285–286``, live ``max_rank_sz()`` ``:287``, pl_character rename ``:288–291`` (``String(name).slice(0, PL_CSIZ - 1)``, roles.js shape; C ``Strcpy``), umonnum restore ``:292–293``, amorous flip + ``set_uasmon()`` ``:294–301`` (``:296–300`` kept as a disabled comment, exactly as in C). New live ``max_rank_sz`` export in C-home ``js/botl.js`` (``game.urole.title`` = C ``rank[9]``, rank_of fallback shape; ``.length`` ⇔ ``strlen`` on ASCII titles; writes ``game.gm.mrank_sz``). New edges: ``PL_CSIZ`` (extends the existing const.js import) + ``max_rank_sz`` from botl.js — ``imports.mjs --can``: IN-SCC (existing 98-module cycle), runtime-only call, no TDZ read.
**Verify:** ``node scripts/verify.mjs --fn change_sex`` → VERIFY: PASS — syntax (2 changed: js/botl.js js/polyself.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (no shared file changed). ``/tmp/change_sex_probe.mjs`` → PROBE-OK 14/14: unpoly'd flip + Cavewoman/Caveman rename + umonnum restore + mrank_sz 10; amorous mfemale flip + C-literal double-flip net + umonnum kept + ``set_uasmon`` repoint; poly'd non-amorous kept.
**Named:** none new — every callee is live; the ``:296–300`` swap stays a comment (disabled in C); null-``name.m`` guard (C would deref; unreachable — every role has ``name.m``).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2641 `exper.c` experience whole-body port (eel AD_WRAP Amphibious +1000 + mail-daemon tmp=1 + per-arm C cites)

**C locus:** ``nethack-c/upstream/src/exper.c:85–166`` (experience, decl ``extern.h:1038``). Decisive C facts: eel arm is ``tmp2 == AD_WRAP && ptr->mlet == S_EEL && !Amphibious`` → +1000 (``:125–126``); ``Amphibious = HMagical_breathing || EMagical_breathing || amphibious(youmonst.data)`` (``youprop.h:272–273``); mail arm is ``mtmp->data == &mons[PM_MAIL_DAEMON]`` → ``tmp = 1`` (``:139–140``) inside ``#ifdef MAIL_STRUCTURES``, which is always on (``global.h:430`` — uhitm.js:151 precedent); revive scale-down is ``(tmp+1)/2`` on non-negative tmp (``:157–159``) ⇔ ``Math.trunc``.
**JS:** ``js/exper.js`` (+~45/−~15: helper + const + restarted export + 3 import names); ``docs/c-js-map/startup.md`` (retired the two shipped omissions, kept ``exp_percent_changing``/SCORE_ON_BOTL; cited D-2641).
**Change:** restarted the export in C order with per-arm ``:line`` cites — eel ``:125–126`` via file-local ``Amphibious_hero`` (youprop.h:272 macro expansion in the teleport.js/mhitu.js uprop-read shape, not exported so no third ``Amphibious`` export) + ``ptr.mlet === 'S_EEL'``; mail ``:139–140`` via ``(ptr?.mndx ?? -1) === PM_MAIL_DAEMON`` (monmove.js:1854 / mhitm.js:3586 shape) placed between the level bonus and the revive block per C order ``:134–143``; removed ``void AD_WRAP;``. No new import edges — extended 3 existing lines (const.js ``MAGICAL_BREATHING``, monsters.js ``amphibious``, generated/monsters_data.js ``monsterNames``; ``imports.mjs --can exper.js monsters.js amphibious``: ALREADY).
**Verify:** ``node scripts/verify.mjs --fn experience`` → VERIFY: PASS — syntax (1 changed: js/exper.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach — RNG 0 — smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (no shared file changed).
**Named:** pre-existing null-data guard (C takes NONNULLARG1 and would deref; JS returns 1 — kept, out of scope); ``exp_percent_changing`` / SCORE_ON_BOTL (botl scope, map line stands).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2640 `read.c` seffect_destroy_armor whole-body port (confused erodeproof + vibrate stun + blessed getobj choice + disintegrate_cursed_armor + actualoname)

**C locus:** ``nethack-c/upstream/src/read.c:1324–1396`` (seffect_destroy_armor, decl ``:26``) + same-file staticfn ``disintegrate_cursed_armor`` ``:1293–1321`` + ``objnam.c:2488–2498`` (actualoname) over ``minimal_xname`` ``:1038–1093``; sole C caller ``read.c:2212`` (seffects SCR_DESTROY_ARMOR). Decisive C facts: confused gate is ``Confusion != 0`` ≡ HConfusion (youprop.h, D-1048); erodeproof is zeroed ``:1342`` for messages, restored to 1 before COST_DEGRD when old&&!new (``:1344–1348``); vibrate stuns ``(HStun & TIMEOUT) + rn1(10,10)`` (``:1363``); gets_choice needs blessed scroll + 2+ worn pieces (``:1373–1374``), reveals via ``an(actualoname)`` only when ``oc_name_known`` unset (``:1379``), keeps scroll known even when the pick disintegrates nothing (``:1381`` falls through); ``*sobjp = 0`` exactly on the two strange_feeling paths.
**JS:** ``js/read.js`` (+~90/−50: disintegrate_cursed_armor local + restarted export + 4 import names + header note updates); ``js/objnam.js`` (+26: actualoname export); ``js/do_wear.js`` (2 doc lines: any_worn_armor_ok / count_worn_armor caller wiring now live); ``scripts/seffect-destroy-armor.test.mjs`` (+4 its: confused-naked null, erodeproof swap, uncursed-no-armor null, vibrate HStun).
**Change:** restarted the export in C order with per-arm ``:line`` cites — confused ``:1333–1352`` via file-local ``p_glow2(otmp, NH_PURPLE)`` + live ``costly_alteration`` COST_DEGRD; cursed ``:1354–1371`` via live ``Yobjnam2(otmp,'vibrate')`` + ``adj_abon(otmp,-1)`` + ``make_stunned((HStun&TIMEOUT)+rn1(10,10), TRUE)``; uncursed ``:1372–1395`` gets_choice via live ``count_worn_armor``/``any_worn_armor_ok``/``getobj('destroy',…,GETOBJ_PROMPT)`` + new ``actualoname`` import, same-file ``disintegrate_cursed_armor`` port (uarm→uarmu C order, ``rn2(idx)``), ``*sobjp = 0`` ⇔ null return (seffects already maps null→1). New ``actualoname`` in C-home ``js/objnam.js:2829`` mirroring C's save/force/restore (suppress oc_uname, force oc_name_known + dknown, xname singular bknown-0 copy, restore, strip cleric "uncursed "). Confusion gate is ``HConfusion || Confusion`` like the sibling seffects (D-1048 + do_mapping screw flag).
**Verify:** ``node --test scripts/seffect-destroy-armor.test.mjs`` → 6/6 (2 pre-existing + 4 new). ``node scripts/verify.mjs --fn seffect_destroy_armor`` → VERIFY: PASS — syntax (3 changed: js/do_wear.js js/objnam.js js/read.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (re-ran with --full since objnam.js/do_wear.js are shared).
**Named:** file-local ``strange_feeling_scroll`` stays (pre-existing clone of live detect.js strange_feeling, also used at ``:1496`` — own future row); ``some_armor`` monster/``which_armor`` path stays hero-only (do_wear envelope); actualoname bareobj subset (corpsenm/known/owt/AMULET, SLIME_MOLD spe, distant_name wrapper — dead arms for the scroll use-case, named at the export).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2639 `region.c` rest_regions whole-body port (stash rebuild + ttl rebase + expiry post-pass + bones ghostly arm + 3 caller wires)

**C locus:** ``nethack-c/upstream/src/region.c:798–892`` (rest_regions, decl ``extern.h:2691``); staticfn ``reset_region_mids`` ``:928–941`` (region.c:934 lookup_id_mapping use); sole C caller ``restore.c:1225`` (getlev, ghostly ⇔ NHF_BONESFILE ``:1061``; id map cleared ``:1068``/``:1304``, recorded at restmonchn ``:400–405``); save side ``save.c:550`` save_regions ⇒ C bones.c:621 savebones calls savelev (bones files carry regions). Decisive C facts: ghostly forces tmstamp 0 (no ttl decay) + clears old-player hero bits (``:872–875``); post-pass (``:879–891``, `#ifndef SFCTOOL` = always live) drops ttl==0 via remove_region with no expire_f callback, else ghostly remaps mids; enter/leave length 0 ⇔ NULL; n_regions/max_regions ⇔ array length.
**JS:** ``js/region.js`` (+127: import ``lookup_bones_id`` from bones.js — mutual hoisted-decl edge in the existing 98-module SCC, runtime-only calls, D-2638 precedent); ``js/bones.js`` (+41: ``rest_regions`` import, id-map trio, remap record, getlev_bones wire + omission-doc update); ``js/do.js`` (+11/−: import, elapsed hoisted above restore and reused by catchup); ``js/save.js`` (+6/−: import, dorecover wire).
**Change:** new ``rest_regions(stored, elapsed, ghostly)`` in ``js/region.js:700`` in C order with per-arm ``:line`` cites — clear_regions security wipe (``:806``); ghostly⇒0 tick else elapsed (``:807–811``); fresh object per record (alloc⇔GC literal) in field order (bounding_box ``:818``, nrects/rects ``:819–826``, attach ``:828–829``, msgs with 0⇔null ``:830–847``, ttl rebase floored at 0 ``:860–864`` with missing-ttl⇒-1 live default, ghostly hero-bit clear ``:872–875``, n_monst/monsters/max_monst ``:876–884``, visible/glyph/arg ``:885–887``); direct push (never add_region — C restores without its block/newsym/hero scan); reverse post-pass remove_region / reset_region_mids (``:879–891``). Ported file-local ``reset_region_mids`` (``js/region.js:664``) via new bones.js ghostly id map (``record_bones_id`` at remap ``:517`` mirroring ``:400–405`` order, ``lookup_bones_id`` null-when-unmapped ⇔ FALSE arm, ``clear_bones_ids`` at getlev_bones start ``:571`` ⇔ ``:1068`` and end ``:632`` ⇔ ``:1304``). Sfi_* binary decode ⇔ stash-field copy (binary save_regions format stays a named omission, data.md — JS saves JSON per Constitution §1.6).
**Verify:** ``node scripts/verify.mjs --fn rest_regions`` → VERIFY: PASS — syntax (4 changed: js/bones.js js/do.js js/region.js js/save.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared files changed).
**Named:** binary ``save_regions`` format (stash/JSON architecture, data.md line stands + extended); ``Sfi_any`` arg ⇔ passthrough (gas damage number); numeric cmap glyph ⇔ tag (pre-existing header omission); C ``light.c:543`` / ``timeout.c:2760`` ghostly id lookups (no JS restore counterpart — future rows); bones ``lastseentyp`` still uninstalled (its own arm, still named at getlev_bones); ``free_region`` teardown (pre-existing).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2638 `hack.c` avoid_trap_andor_region whole-body port (clone→live imports + hallu trap-prompt C-wrong + committed regression test) + walkfrom stale park

**C locus:** ``nethack-c/upstream/src/hack.c:2513–2582`` (avoid_trap_andor_region, staticfn decl ``:41``); region arm ``:2527–2552``, trap arm ``:2553–2580``; caller ``hack.c:2826`` (domove_core ParanoidTrap gate); callees ``region.c`` visible_region_at + reg_damg, ``hack.c:988–1255`` test_move (D-2439), ``hack.c:1817–1829`` u_locomotion, ``hacklib.c:113–119`` upstart, ``cmd.c`` paranoid_query, ``hack.c:4166–4167`` nomul, ``trap.c`` t_at / immune_to_trap / into_vs_onto / trapname ``:7098–7155``, ``rnd.c`` rnd. Decisive C facts: trap prompt is ``defsyms[trap_to_defsym(traptype)].explanation`` (``:2571``) — under Hallucination traptype is already C's ``rnd(TRAPNUM-1)`` pick (``:2565``), so C ``trapname(t, TRUE)`` is the identical expression (override skips the hallu re-roll).
**JS:** ``js/hack.js`` (+2 import lines incl. new region.js edge, −27 clone lines, per-arm C cites); ``scripts/avoid-trap-hallu.test.mjs`` (new, 2 its).
**Change:** deleted all three clones → live imports (``imports.mjs --can hack.js region.js visible_region_at``: SAFE — hoisted function decls in the existing 98-module SCC, runtime-only calls; ``upstart`` joins the existing hacklib.js import — no new edge); ``trapname(traptype, true)`` ≡ C ``:2571`` (dig.js/shk.js/trap.js precedent); ``u_locomotion`` exported live from C-home ``js/hack.js`` (behavior unchanged); body re-cited per-arm (``:2527–2552`` / ``:2553–2580``); new ``scripts/avoid-trap-hallu.test.mjs`` (node:test, cond-menu/seffect-destroy-armor precedent) drives the trap arm headless (timeout HHallucination, tseen bear trap, ParanoidTrap, decline 'n').
**Verify:** focused ``node --test scripts/avoid-trap-hallu.test.mjs`` FAILED pre-fix (hallu case: ``whoopie cushion`` vs ``pit``; sober bear-trap case passed both — fixture guard), 2/2 post-fix. ``node scripts/verify.mjs --fn avoid_trap_andor_region`` → VERIFY: PASS — syntax (1 changed: js/hack.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared file changed).
**Named:** ``u_locomotion`` capitalize path + ``locomotion(youmonst.data, def)`` poly fallback (C ``:1817–1829``); ``do.js:549`` / ``teleport.js:1962`` u_locomotion clones kept (other files, own future rows — no clone #4 added here). Hallucination flavor split stands: the avoid_trap gates use the do_name flavor (sticky + timeout, pre-existing D-1187), trapname's interior uses the display timeout-only flavor (D-1493) — with override=true that interior is now unreachable from this call site.
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2637 `monmove.c` onscary whole-body port (live export restart + sengr_at canonical export + 3 caller wires + music rewire) + convert_line stale park

**C locus:** ``nethack-c/upstream/src/monmove.c:241–303`` (onscary, decl ``extern.h:1935``); callee ``engrave.c:250–261`` (sengr_at); callers ``dogmove.c:1160`` (dog_move), ``hack.c:4123`` (monster_nearby ``:4106``), ``mon.c:2278`` (mfndpos), ``mon.c:4270`` (setmangry), ``mon.c:5529`` (newcham), ``monmove.c:234`` (dochugw), ``monmove.c:559`` (distfleeck), ``monmove.c:1390`` (m_search_items), ``muse.c:616``/``:1515``/``:2133``/``:2713`` (m_tele/offensive-teleport/poly-trap/searches_for_item), ``music.c:58`` (awaken_scare), ``teleport.c:168``/``:2056`` (goodpos/mlevel_tele_trap), ``were.c:135`` (new_were). Decisive C facts: ``(0,0)`` is auditory, everything else magical (``:247``); shk/priest gate applies even to auditory (``:267–274``); Inhell is the hellish-dungeon flag (``dungeon.h:140`` → ``dungeon.c:1942 In_hell`` = ``dungeons[dnum].flags.hellish``, not dnum==GEHENNOM).
**JS:** ``js/mon.js`` (restart + 5 import names); ``js/engrave.js`` (+16 sengr_at export); ``js/monmove.js`` (Displaced export + dochugw gate); ``js/teleport.js`` (Inhell export); ``js/hack.js`` (monster_nearby gate + import); ``js/were.js`` (new_were C ``:133–137`` tail after unwield, promise-aware + 3 import names); ``js/music.js`` (clone deleted, live import — no new edge).
**Change:** restarted the ``mon.js`` export in C order with per-arm ``:line`` cites — live ``is_lminion``/``Inhell`` (teleport.js; ``Inhell`` newly exported, hellish-flags shape = C ``In_hell``), ``inhistemple`` (priest.js), ``inhishop`` (new ``shk.js`` edge), canonical ``sengr_at`` newly exported from C-home ``engrave.js`` (HEADSTONE/time gate, strict=strcmpi/substring=strstri via lowercased compare, teleport.js:175 keeps its local: D-1110 cycle), ``Displaced`` newly exported from ``monmove.js``, ``vobj_at`` ≡ live ``objects_at`` (display.js:1458 precedent), own-module hoisted ``unique_corpstat``; all ``imports.mjs --can`` SAFE/ALREADY (no new top-level reads). First handoff used bare ``u.ux`` in dochugw (no local ``u`` — cohort ReferenceError); ``game.u.ux`` like the neighboring line.
**Verify:** ``node scripts/verify.mjs --fn onscary`` → VERIFY: PASS — syntax (7 changed); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared files changed). First run caught the dochugw bare-``u`` throw (cohort seed0012 + Knight smoke regressed, same root cause); fixed, re-ran once to PASS.
**Named:** ``monmove.c:559`` distfleeck sawscary — ``js/monmove.js:990`` distfleeck is a documented ``scared=0`` stub with its own residual rows (D-2420 W5/W6); wiring onscary alone would arm its ``monflee`` RNG draws, so the arm ships with distfleeck's row, not here. Teleport's ``onscary``/``sengr_at``/``goodpos_onscary`` locals stay (D-1110 cycle).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2636 `role.c` role_menu_extra RS_ROLE filter-loop compare fix (`i !== f` → `i !== fsel`) + committed regression test

**C locus:** ``nethack-c/upstream/src/role.c:1840–1844`` (RS_ROLE filter loop inside role_menu_extra ``:1816–1960``); ``f = r`` (``:1839``) is ``flags.initrole``.
**JS:** ``js/player_selection.js`` (1-line fix + 2 export keywords); ``scripts/role-menu-extra.test.mjs`` (new, 2 its).
**Change:** one-line ``i !== f`` → ``i !== fsel`` (the correctly-renamed local, already used at ``:891``/``:909``/``:935``/``:953``); exported ``menu_extra_lines`` + ``rfilter`` (additive, no caller change) so the arm is testable; new ``scripts/role-menu-extra.test.mjs`` (node:test, cond-menu.test.mjs precedent) drives RS_ROLE headless: filter-all-but-selected → ``filter forces role``; no filter → ``Pick another role first``.
**Verify:** focused ``node --test scripts/role-menu-extra.test.mjs`` FAILED before the fix (``'? - Pick another role first'`` vs ``/filter forces role/``), 2/2 after. ``node scripts/verify.mjs --fn role_menu_extra`` → VERIFY: PASS — syntax (1 changed: js/player_selection.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24 run → 24 PASS, 0 regressed → REACH-OK); green 2/2; strict ×2; cohort 7/7; full ``sessions`` 44/44 (shared startup file, re-ran like D-2633).
**Named:** none new — D-2633's list stands (add_menu/add_menu_str ⇒ menu_pick line objects; cg.zeroany/nul_glyphinfo subsumed; Random preselect text kept).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — Audit 94148365..fe53547d (reviews 1587–1594: 7 ACCEPT, 1 QUALITY-RISK) + cadence 44/44

D-2628…D-2635 audited vs pinned C (per-SHA `--reach-all` REACH-OK, no REGRESSED): 7 exact; D-2633 QUALITY-RISK (`player_selection.js:875` `i !== f`), Must-fix prepended, Next cluster set. Cadence 44/44, held-out 11/44, corpus 497/540 +0/−0.
## 2026-09-20 — D-2635 `botl.c` cond_menu whole-body port (status-conditions PICK_ANY menu + doset caller) + 2 stale parks

**C locus:** ``nethack-c/upstream/src/botl.c:1376–1454`` (cond_menu) + ``:1332–1342`` (cond_cmp) + ``:1344–1351`` (menualpha_cmp) + ``decl.h:229`` (gc.condmenu_sortorder, init 0 ``:1315``); callers ``options.c:5032`` (pfxfn_cond_ do_handler, "not used") + ``:8436–8439`` (optfn_o_status_cond do_handler). Decisive C facts: sort row a_int 1 + 'S' accelerator + SKIPINVERT (``:1402–1408``); rows ``cond_%-14s`` a_int idx+2 preselected iff enabled (``:1412–1420``); ESC res -1 skips the enable/disable loop while finish-empty res 0 runs it (``:1428`` vs ``:1445``); final loop clears test on leftover idx not i (``:1449``); disp.botl + changed on any flip (``:1450``).
**JS:** ``js/botl.js`` (+~110: export + 3 file-local fns + 2 import names); ``js/options.js`` (import name + othrPicks arm + comment); ``scripts/cond-menu.test.mjs`` (new, 5 its).
**Change:** new async export ``cond_menu`` in ``js/botl.js`` (C home) in C order with per-arm ``:line`` cites — file-local ``strcmpi_fold`` (toLowerCase shape, invent.js sortloot_cmp precedent), ``cond_cmp`` (ranking asc + alpha tiebreak), ``menualpha_cmp``; ``game.gc.condmenu_sortorder`` (decl.h:223 → game.gc precedent, cmd.js ensure-write); menu rows via live ``select_menu_pick_any`` (dynamic import — options.js statically imports botl.js; ``imports.mjs --can``: same 98-module SCC, no top-level read); end_menu prompt + heading ride non-selectable rows (wiz_intrinsic/getpos_menu precedent); ``cond_%-14s`` ≡ padEnd(14) (all useroptions < 14 chars); leftover-idx test clear replicated exactly; disp.botl sets game.flags.botl + game.disp.botl (hack.js:2932 precedent). Stable-sort divergence impossible (useroptions unique — no tie survives either comparator).
**Verify:** ``node scripts/verify.mjs --fn cond_menu`` → VERIFY: PASS — syntax (2 changed: js/botl.js js/options.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared files changed). Focused ``node --test scripts/cond-menu.test.mjs`` 5/5 (ESC-noop, enable barehanded+botl, sort-rebuild disables grab, sort-then-ESC keeps order).
**Named:** ``create_nhwindow``/``start_menu``/``add_menu``/``add_menu_heading``/``end_menu``/``select_menu``/``destroy_nhwindow`` window layer — architectural mapping to select_menu_pick_any rows (getpos_menu precedent, same protocol as doset menus); ``cg.zeroany``/``nul_glyphinfo`` (no JS carrier — a_int rides rows); ``alloc``/``free`` (GC); QBUFSZ (plain strings); cond_idx options-init sort (``:1315–1321``, different function — options.c:5002 caller); get_val ``(N currently set)`` display keeps existing hardcoded 16 = C default OPT_OUT-enabled count (not in cond_menu's body; screens unchanged).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2634 `muse.c` m_use_undead_turning whole-body port (floor-corpse ray via new linedup_callback export)

**C locus:** ``nethack-c/upstream/src/muse.c:1299–1340`` (m_use_undead_turning, staticfn) + ``:1293–1297`` (linedup_chk_corpse, staticfn) + ``mthrowu.c:1294–1327`` (linedup_callback, extern); caller ``find_offensive :1499–1500`` (nomore MUSE_WAN_UNDEAD_TURNING). Decisive C facts: ax/ay = hero + sgn(mux−mx)*3 (``:1302–1303``); guard is a negated conjunction (``:1306``); ``necrophiliac`` is comment-only (``:1309``, never called); linedup_callback sets gt.tbx/tby then walks bx,by→ax,ay stopping FALSE on blocking_terrain, TRUE on fnc (``:1305–1326``).
**JS:** ``js/muse.js`` (restart + local + 2 import names); ``js/mthrowu.js`` (+34-line export).
**Change:** restarted the export in C order with per-arm ``:line`` cites — ported ``linedup_chk_corpse`` as a muse.js local (``sobj_at(CORPSE,x,y) !== null``; sobj_at returns obj|null per ``js/mkobj.js:2704``); exported ``linedup_callback`` from its C home ``js/mthrowu.js`` reusing the live local ``blocking_terrain`` + ``sgn``/``distmin``/``BOLT_LIM`` + ``game._tbx``/``_tby`` (``linedup`` precedent, ``Math.abs`` ≡ C ``abs``); muse.js extends its existing mthrowu.js/mkobj.js imports (``imports.mjs --can``: ALREADY, no new edge). Retires review 779's named floor-corpse debt.
**Verify:** ``node scripts/verify.mjs --fn m_use_undead_turning`` → VERIFY: PASS — syntax (2 changed: js/mthrowu.js js/muse.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (no shared file changed).
**Named:** none — every callee live (carrying hack.js, sobj_at mkobj.js, blocking_terrain mthrowu.js local) or ported here (linedup_callback, linedup_chk_corpse); necrophiliac is C-comment-only, not a call.
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2633 `role.c` role_menu_extra whole-body port (filter arms, RS_NAME, impossible tail) + `pager.c` whatdoes_cond compiled-out park

**C locus:** ``nethack-c/upstream/src/role.c:1816–1960`` (role_menu_extra, static); all 24 call sites inside ``genl_player_setup`` (``:2314–2322`` role menu, ``:2409–2416`` race, ``:2497–2504`` gender, ``:2583–2590`` align). Decisive C facts: ``RS_menu_arg(x)`` ≡ ``ROLE_RANDOM-((x)+1)`` (``winprocs.h:314``); C roles[] carries UNDEFINED_ROLE so ``SIZE(roles)-1`` ≡ 13; ``MH_HUMAN``/``ROLE_MALE``/``ROLE_FEMALE``/``AM_*`` equal the table ``selfmask``/``allow`` values the old code compared against.
**JS:** ``js/player_selection.js`` only (imports: ``impossible`` from display.js; RS_NAME/ROLE_MALE/ROLE_FEMALE/MH_HUMAN/AM_LAWFUL/AM_NEUTRAL/AM_CHAOTIC from const.js — all value-verified against C headers). Caller signatures unchanged.
**Change:** restarted the body in C order with per-arm ``:line`` cites — RS_NAME (``:1834``, ``f`` stays 0 → "Pick another name first" like C); RS_ROLE filter loop (``:1840–1844``, sparse ``rfilter.roles`` ≡ C zero-init); RACE (``:1848–1866``, MH_HUMAN + ``c=ROLE_NONE`` override + filter arm); GENDER (``:1867–1888``, ROLE_MALE/FEMALE + filter arm); ALGNMNT (``:1889–1927``, role→race→filter chain + ``:1924`` forcedvalue tail); output chain in C order (``:1929–1958``); bad-arg arm awaits live ``display.js impossible`` (``imports.mjs --can``: hoisted, cycle-safe) so the function is now async — 8 call sites in the 4 pick_*_menu functions gained ``await``. First handoff shadowed module ``function f()`` with C's local ``f`` (TDZ crash at chargen, cohort 5/7); renamed to ``fsel`` (C name in comment cites). Second pass missed ``aligns[f]`` (seed0012 crash); renamed.
**Verify:** ``node scripts/verify.mjs --fn role_menu_extra`` → VERIFY: PASS — syntax (1 changed: js/player_selection.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full 44/44 (shared startup file, ``--full`` re-run).
**Named:** ``add_menu``/``add_menu_str`` (no JS export) — architectural mapping to menu_pick line objects, same protocol as setup_*menu entries (map-named here); ``cg.zeroany``/``nul_glyphinfo``/ATR_NONE/NO_COLOR plumbing subsumed by that protocol; ``'* * Random'`` vs ``'* - Random'`` preselect text kept (pre-existing presentation; callers carry ``preselected:true`` separately). whatdoes_cond compiled-out park (see Status).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2632 `do_wear.c` inaccessible_equipment caller closure (dodip post-check, do_wear clone→live)

**C locus:** ``nethack-c/upstream/src/do_wear.c:3340–3400`` (inaccessible_equipment, decl extern); callers ``apply.c:2595`` (grease_ok verb-0), ``apply.c:2629`` (use_grease "grease"), ``do_wear.c:3440`` (equip_ok verb-0, RING_CLASS flag), ``potion.c:2223`` (dip_ok verb-0), ``potion.c:2282`` (dodip "dip"), ``potion.c:2402`` (dip_into "dip"). Decisive C fact: ``hack.h:520–527`` — EXCLUDE_INACCESS is pseudo-valid by design ("allowing the player to select it and getobj to return it ... in order for the getobj caller to present a specific failure message"); ``invent.c:2072–2075`` only ``silly_thing``s on ``== GETOBJ_EXCLUDE``, so letter-picked covered items reach the caller post-checks.
**JS:** ``js/do_wear.js`` (import + 1 call site, −14 clone lines); ``js/potion.js`` (dodip post-check + 2 doc lines). Export names/signatures unchanged.
**Change:** ``js/potion.js`` ``dodip`` — ``await (import './apply.js').inaccessible_equipment(obj, 'dip', false) → ECMD_OK`` in C ``:2282`` position (dynamic import: apply.js already imports potion.js); ``js/do_wear.js`` — deleted the clone, ``equip_ok`` calls live ``equipment_is_inaccessible`` (``imports.mjs --can``: hoisted, cycle-safe). No body rewrite: predicate/message arms verified C-order identical (BLOCKSACCESS/anycovering, suit/shirt/ring, sameprefix ``shk_your`` string compare, ``xname``/``yname`` live imports).
**Verify:** ``node scripts/verify.mjs --fn inaccessible_equipment`` → VERIFY: PASS — syntax (2 changed: js/do_wear.js, js/potion.js); rule2; hidden note (no corpus session blocked); reach (no RNG-tagged reach, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (no shared file changed). Throwaway differential probe ``/tmp/inacc_probe.mjs`` (not committed): deleted clone vs live export agree 208/208 across suit/cloak/shirt/ring/glove/helm × cursed-known/unknown × flag. No committed unit test: repo has no tests/ layout (D-2630 precedent); fortress + probe cover.
**Named:** ``SetVoice``-class none here; prompt letter-hiding (which letters the dip/grease prompts suggest) stays with each getobj path's own simplification — C-observable enforcement (message + ECMD_OK) is wired at every caller.
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2631 `eat.c` consume_oeaten whole-body port (impossible arm, unsigned shift, victual reqtime)

**C locus:** ``nethack-c/upstream/src/eat.c:3808–3872`` (consume_oeaten, decl ``extern.h:974``); callers ``eat.c:1970`` (eatcorpse rotten ``otmp, 2``), ``:3036`` (doeat rotten ``otmp, 1``), ``:3149``/``:3154`` (bite ``piece, nmod`` / ``piece, -1``), ``objnam.c:5392`` (readobjnam halfeaten ``d.otmp, 1``); comment refs ``:3879``, ``:3907``.
**JS:** ``js/eat.js:1044`` (whole body); no new imports — impossible/game/CORPSE/EGG/TIN already in scope.
**Change:** restarted the export in C order with per-arm ``:line`` cites — itembuf build (corpse/egg/tin ``[corpsenm]`` vs otyp decimal, ``:3814–3821``) + sync fire-and-forget impossible (``:3822–3825``, do_wear.js setworn ``:618`` precedent); unsigned ``>>>`` shift/add (``:3854–3863``, oeaten unsigned per ``:3849``); zero-clamp sets ``game.context.victual.reqtime = usedtime`` when obj===piece (``:3865–3871``).
**Verify:** ``node scripts/verify.mjs --fn consume_oeaten`` → VERIFY: PASS — syntax (1 changed: js/eat.js); rule2; hidden note (no corpus session blocked); reach (no RNG tags, smoke 24/24 → REACH-OK); green 2/2; strict ×2; cohort 7/7; full skipped (no shared file changed).
**Named:** none — done_eating/food_disappears appear only in C's "better solution" comment (``:3832–3834``); both live elsewhere, no stub. ``!obj`` null guard kept (C NONNULLARG1; defensive, same class as live obj_nutrition ``!otmp`` guard).
**Next:** pop the next Open — coverage row (litter_scatter head).
