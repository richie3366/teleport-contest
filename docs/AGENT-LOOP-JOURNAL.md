# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — Audit reviews 1275–1281 (6ce28063..f5d3798f) + cadence 44/44

Review iteration, no `js/` edits. Seven JS-touching SHAs since 1274, oldest-first, one file per SHA written as each finished: 1275 god_zaps_you fall-through (D-2309), 1276 clear_conjoined_pits (D-2310), 1277 maybe_dunk_boulders (D-2311), 1278 dig() occupation arms (D-2312), 1279 destroy_drawbridge tail (D-2313), 1280 goto_level falling (D-2314), 1281 do_earthquake arms (D-2315). All **ACCEPT**, no Must-fix (no LOOP-QUEUE prepend; Next cluster unchanged). Every D-log corpus claim re-measured with `verify <fn> --base HASH~1` — all matched (0 blocked, vacuous-honest). Findings worth keeping: D-2309 cites `32-e3c6cff4-desecrate-altar.md`, absent from both review dirs (citation gap, debt itself real+fixed); D-2315's C line pointers are stale by patch shift (`:268/:380/:420/:443` actual); pre-existing `wake_nearto` clone (`& ~0x01` vs `~0x30000000`, no G_UNIQ/wake_msg — own future row, correctly deferred); `hero_Deaf` `|| u.Deaf` disjunct dead (zero writers); leftover dig.js namer clones still serve web/rubble/thin-air arms. Also backfilled the missing 1274 index row. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, `47+0.29/turn` (R² 0.79) — identical aggregates to the ef75880a audit. Queue at 11 Open (df1c363c refilled to 12 from debt.md, f5d3798f consumed one) — no REFILL owed this iter; map singletons stay barred <90%.
**Next:** pop LOOP-QUEUE Open head (`dbridge.c` open_drawbridge, D-0977 row).
## 2026-09-15 — D-2315 `music.c` do_earthquake quake-arm residuals: SCORR unblock_point, ALTAR altarmask_at, se_thump/se_scream

**C locus:** `music.c:430` (`unblock_point(x, y)` after `typ = CORR` — `dig_point` + full-recalc flag, vision.c:899); `:416` (`amsk = altarmask_at(x, y)` — mimic `M_AP_FURNITURE`+`S_altar` corpsenm else `levl.altarmask`, pray.c:2490–2504); `:384` (`Soundeffect(se_thump, 50)` before `You_hear("a thump.")`); do_pit `:277` (`Soundeffect(se_scream, 50)` before `You_hear("a scream!")`). Full C re-read (`do_earthquake` :343–475 + `do_pit` :221–341, in brief output): every other arm verified matching — wakeup/mundetected/ceiling-hider/seemimic prefix, `rn2(14-force)` gate, all seven terrain arms with FALLTHRU order, DOOR newsym-before-pline + shop `add_damage`, do_pit boulder/KADOOM, `fillholetyp`+`liquid_flow` re-read, hero buried-ball/levitate/fall/jostle incl. jostle short-circuit (review-41 verified), `M_AP_TYPE` masks `M_AP_TYPMASK` both sides (C monst.h:73; js/const.js:3200).
**JS:** 1 file (`music.js`, +11/−4), far under the 600/10 caps.
**Change:** `js/music.js` only, no new module edges (all three source modules already imported): SCORR arm calls live `unblock_point` (joins the existing `vision.js` import; ≡ C); ALTAR arm calls live `altarmask_at(x, y)` (joins the file's existing dynamic `pray.js` import convention — no static-cycle risk; `pray.js:altarmask_at` ≡ C incl. mimic edge); `Soundeffect(se_thump, 50)` / `Soundeffect(se_scream, 50)` restored in C order (join the existing `sndprocs.js` + `seffects_data.js` imports; no-op in this build per `sndprocs.js:36`, same as the file's :1108 convention). Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Post-fix `node scripts/verify.mjs --fn do_earthquake` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool-gated: music.js not shared) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after).
**Named:** do_pit `set_levltyp(x, y, filltype)` side effects (no JS `set_levltyp` exists — own future `trap.c` row, not this `music.c` function); boulder-arm `flooreffects` full (pre-existing D-0972 thin, untouched); `cvt_sdoor_to_door`/`in_rooms` shared shapes (verified equivalent, untouched).
**Next:** do not re-pop do_earthquake/do_pit quake arms. Falsifier: a rescore or fresh `verify` showing a session blocked with a do_earthquake/do_pit owner, or a drum-quake trace mis-painting SCORR/altar/sound arms.
## 2026-09-15 — D-2314 `do.c` goto_level falling arm: floor objects follow via impact_drop MIGR_WITH_HERO

**C locus:** `do.c:1611–1613` (`if (falling) /* assuming this is only trap door or hole */ impact_drop((struct obj *) 0, u.ux, u.uy, newlevel->dlevel);`), after the travelcc/polearm clears, immediately before `check_special_room(TRUE)` (`:1615`). Caller arg-orders re-verified against C `goto_level(newlevel,at_stairs,falling,portal)`: dig.c:791 `(FALSE,TRUE,FALSE)` ≡ js/dig.js:813; trap.c:609–697 `schedule_goto(!Flying?FALLING:NONE)` ≡ js/trap.js:3836; dungeon.c:1512 `(at_stairs,!at_stairs,FALSE)` ≡ js/do.js:1258; do.c:2085 deferred_goto typmask ≡ js/do.js:2131. No call-site order bug; the callee arm (`dlev!=0` → `MIGR_WITH_HERO`, `:1560–1564`) was live with zero production callers.
**JS:** 1 file (`do.js`, +9/−1), far under the 600/10 caps.
**Change:** `js/do.js` only: `if (falling) await impact_drop(null, u.ux|0, u.uy|0, newlevel.dlevel|0)` immediately before the keepdogs block, hence before `check_special_room(true)` — C relative order (impact_drop precedes both; the travelcc clear living later in JS is pre-existing drift, untouched). `impact_drop` joins the existing static `dokick.js` import (`imports.mjs --can do.js dokick.js impact_drop`: ALREADY via `ship_object`/`obj_delivery` — no new edge, same SCC, hoisted fn, no TDZ read). Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_impact_dlev.mjs` PROBE-PASS (dlev arm no corpus session reaches; deleted after run): the exact wire call on a staged 12-object/18-quan pile over stairs-down — no throw, `gate_str='down the stairs'`, conservation 13+5=18, every migrated obj `owornmask=9 (MIGR_WITH_HERO)` + `(ox,oy)=(0,2)` + `OBJ_MIGRATING`, floor chain intact (4/12 fell ≈ C 1/3 rate); empty-square early return migrates nothing. RNG boot note: bare-process probes must `initRng()` first (isaac64 ctx is session-booted).
**Named:** `goto_hell(at_stairs,falling)` (dungeon.c:1956–1962) has no JS counterpart (zero `goto_hell` hits in `js/`) — its falling objects will ride this now-live arm once ported; own future row, not this arm. C `maybe_reset_pick(NULL)` paragraph residue and travelcc/polearm-clear drift (pre-existing, untouched).
**Next:** do not re-pop impact_drop/down_gate/drop_to/goto_level-falling. Falsifier: a rescore or fresh `verify` showing a session blocked with an impact_drop/goto_level/fall_through owner, or a falling-descent trace whose old-square pile stays put.
## 2026-09-15 — D-2313 `dbridge.c` destroy_drawbridge full tail: Soundeffect, flooreffects, scatter, unblock, both-entity crush

**C locus:** `dbridge.c:888–1019` `destroy_drawbridge` re-read in full (brief output): `:906` `Soundeffect(se_loud_splash,100)` / `:927` `se_loud_crash` before messages; `:935–938` boulder `obj_extract_self` + `flooreffects(otmp2,x,y,"fall")`; `:955–963` `for (i = rn2(6); i > 0; --i) mksobj_at(IRON_CHAIN, rn2(2)?x:x2, rn2(2)?y:y2, TRUE, FALSE)` + `scatter(ox,oy,1,MAY_HIT,otmp)`; `:966–967` `if (!does_block(x2,y2,lev2)) unblock_point`; `:968–970` `uopened_dbridge=TRUE` before entity; `:972–1012` portcullis `set_entity(x2,y2)` + span `set_entity(x,y)` crush (`automiss` ride-through, `e_missed(…,TRUE)` spare → `spoteffects`/`minliquid`, else `E_phrase` hit/metal + `Deaf`/`is_pool` crushing-sound + `KILLED_BY_AN` `exploding/collapsing drawbridge` `e_died(NOCORPSE|GIVEMSG/NOMSG,CRUSHING)` + MOAT `do_entity`); `:1013–1017` `nokiller()` then `uheard_tune=3`.
**JS:** 1 file (`dbridge.js`, +107/−25), far under the 600/10 caps.
**Change:** `js/dbridge.js` only: both `Soundeffect` calls in C order (before messages); boulder arm `await flooreffects(otmp2,x,y,'fall')` (new static `do.js` edge); debris loop verbatim (`rn2(6)`/`rn2(2)`, `mksobj_at` + `await scatter(…,1,MAY_HIT,…)` — new static `explode.js` edge; `IRON_CHAIN` via `objectNames.indexOf`, `mhitm.js` convention); `if (!does_block(x2,y2,lev2)) unblock_point(x2,y2)` (join existing `vision.js` edge); flags split to C order; full both-entity tail in C order over the live entity family (`occupants()[1]` portcullis first per `etmp2`, then `[0]` span; `e_canseemon`/`automiss`/`E_phrase`/`is_u`/`Hallucination()`/`hero_Deaf()`/`is_pool`/`spoteffects`/`minliquid` — `minliquid` joins the existing `mon.js` edge; `game.killer` `KILLED_BY_AN` overwrites; MOAT `do_entity`; `nokiller()`; `uheard_tune=3`). `debugpline1` omitted (D_DEBUG-only, the file's standing convention). `imports.mjs --can` ×4: `do.js flooreffects` hoisted SAFE, `explode.js scatter` hoisted SAFE, `mon.js minliquid` ALREADY, `vision.js unblock_point` ALREADY; same 90-module SCC, no top-level TDZ read.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Post-fix `node scripts/verify.mjs --fn destroy_drawbridge` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool-gated) · VERIFY: PASS. Import smoke `node --input-type=module -e "await import('./js/dbridge.js'); await import('./js/dig.js')"` → IMPORT-OK (new static `do.js`/`explode.js` edges TDZ-safe; no maintained unit harness exists for game logic — sessions are the acceptance tests per Constitution §1.2, durable-test-collateral disclosure).
**Named:** local `wake_nearto` `& ~0x01` vs C `~STRAT_WAITMASK` + `wake_msg`/`G_UNIQ` skip (mon.c `wake_nearto_core` locus, review-28 residual — own future row, not this `dbridge.c` function); `revive_nasty` on open/close (D-0977 row); `!lev2` early-return guard kept (C assumes a valid wall cell; removing it risks a `js-throw` with zero C fidelity gain).
**Next:** do not re-pop destroy_drawbridge/find/is_wall. Falsifier: a rescore or fresh `verify` showing a session blocked with a dbridge owner (open/close arms belong to the D-0977 row).
## 2026-09-15 — D-2312 `dig.c` dig() occupation residuals: bear-trap rnl(7), fumble steed, drawbridge noun, earth debris

**C locus:** `dig.c` `dig` `:405–423` (occupied BEAR_TRAP `rnl(7) > (Fumbling?1:4)` self-hit `dmgval+dbon`, `<1→1` else `uarmf` halve, `body_part(FOOT)`, `uhis()` killer vs destroy + `reset_utrap(TRUE)`, effort=0); `:347–355` (welded fumble `u.usteed` bounce arm); `:331–334` (`is_db_wall` drawbridge noun); `:521–527` (`Is_earthlevel && !rn2(3)` → `rn2(2) ? PM_EARTH_ELEMENTAL : PM_XORN` + `makemon MM_NOMSG` + `pline_The`); full `dig` `:299–568` re-read for order.
**JS:** 1 file (`dig.js`, +67/−20), far under the 600/10 caps.
**Change:** `js/dig.js` only: bear-trap arm ports C order (`rnl(7)` first, `dmgval(uwep, game.youmonst)+dbon()`, `u.uarmf` halve `| 0`, `body_part(FOOT)` via dynamic `polyself.js` import — the zap_dig falling-rock convention in the same file, `uhis()` killer `chopping off … own …`, `losehp(maybe_half_phys(dmg),…,KILLED_BY)` + the file's `_losehp_needs_done`/`finish_losehp_done` convention); fumble arm gains the `u.usteed` branch and both branches use live `Yobjnam2`/`otense` (existing `objnam.js` edge) + `mon_nam` (existing `do_name.js` edge); too-hard noun via `is_db_wall` (existing `dbridge.js` edge); earth-debris arm via `mons()` (existing `monsters.js` edge) + `monsterNames.indexOf` PM consts (minion.js convention) + dynamic `makemon` (dighole convention). New static names join existing edges only; sole new edge `polyself.js body_part` is dynamic (`imports.mjs --can dig.js polyself.js body_part`: SAFE, hoisted fn, same SCC). `Soundeffect(se_bang_weapon_side)` stays named (draw-free; apply.js/ball.js precedent).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Pre-fix thin behavior is recorded in the removed code comment + review 18 (no live pre-fix probe — the arm needs a trapped-digging game state no harness builds). Import smoke `/tmp/probe_dig_import.mjs` PASS (all four dig exports load — new static imports TDZ-safe; deleted after run; no maintained unit harness exists for game logic — sessions are the acceptance tests per Constitution §1.2, durable-test-collateral disclosure).
**Named:** `Soundeffect` (draw-free); `IS_ALTAR altar_wrath`/`angry_priest` (pray family, D-0963 row); `use_pick_axe2` self-hit killer string (own future `use_pick_axe2` read — review-18 debt, needs its C body re-read, not this tick); `shk.js holetime` text-proxy (mentions non-dig 'chopping'; C compares the occupation pointer); `dighole` magic/by-magic arms (own rows D-0957/D-0954).
**Next:** do not re-pop dig_typ/pick_can_reach/is_digging/dig-occupation. Falsifier: a rescore or fresh `verify` showing a session blocked with a dig owner.
## 2026-09-15 — D-2311 `apply.c` maybe_dunk_boulders via live boulder_hits_pool; dig_check altar via altarmask_at

**C locus:** `apply.c:3897–3905` `maybe_dunk_boulders` (extract then `boulder_hits_pool(otmp,x,y,FALSE)` inside `while (is_pool_or_lava && sobj_at(BOULDER))`); `dig.c:215–219` altar arm (`altarmask_at(x,y) & AM_SANCTUM`); `do.c:49–155` `boulder_hits_pool` + `dig.c:1982–2047` `bury_an_obj` re-read (rock/boulder "merges into burying material", obfree — never buried).
**JS:** 2 files (`dig.js` +14/−5, `apply.js` +1/−1), far under the 600/10 caps. Small by nature: the cluster bulk shipped in D-0950; this is the remaining C delta (8-line C function + 1-line gate).
**Change:** `js/dig.js`: `maybe_dunk_boulders` now async with C order preserved — `boulder_hits_pool` via dynamic `do.js` import (the file's convention for `do.js`: `goto_level`/`dropx` same file; `imports.mjs --can dig.js do.js boulder_hits_pool`: SAFE, hoisted fn, same 90-module SCC, call-time use). `dig_check` altar arm calls `altarmask_at(x, y)` via a new static `pray.js` import (`--can dig.js pray.js altarmask_at`: SAFE, hoisted fn). `js/apply.js`: `await maybe_dunk_boulders(x, y)` (sole live caller; `trap.js blow_up_landmine` keeps its named deferral for its own row).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_dunk.mjs` PROBE-PASS 16/16 (arms no corpus session reaches; deleted after run): 3× single dunk burns C `rn2(10)` + morph matches the C fills_up formula (`chance != 0` on water) + boulder gone; 2-boulder C-order (fills → exactly 1 dunk draw and floor 0 — the 2nd boulder merges via `bury_objs` per the re-read C arm, loop exits on ROOM); sanctum + BY_OBJECT/monster → FAIL_ALTAR, plain altar + BY_OBJECT → PASSED. Probe detours worth recording: in-process rng log carries no `@ jsfn` tags (the worker adds those) — match `^rn2\(10\)=\d+# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
, chance is the first in-window draw; `--More--` inside direct plines needs `nhDisplay.pushKey` padding (D-2309 precedent); B observed the fills branch (chance=2) — the no-fill 2-draw arm follows from the same verified while.
**Named:** `trap.js blow_up_landmine` fill_pit/maybe_dunk/spot_checks (own future trap row); `dig_check` `On_stairs`-vs-`stairway_at` + `!lev → TOOHARD` guard (review-17 rows 1/9 — identical in all reachable states; C derefs the stairway unconditionally); `boulder_hits_pool` pre-existing thins (mondied→mtrapped-clear, burn_away_slime, Soundeffect, DRAWBRIDGE_UP mask polish); `fill_pit` flooreffects("settle") (needs live flooreffects — own row).
**Next:** do not re-pop dig_check/fillholetyp/maybe_dunk_boulders. Falsifier: a rescore or fresh `verify` showing a session blocked with any of the three as owner.
## 2026-09-15 — D-2310 `trap.c` clear_conjoined_pits + deltrap wire; fountain delfloortrap dedup

**C locus:** `trap.c:6579–6601` `clear_conjoined_pits` (staticfn); called first from `deltrap` at `:6535`.
**JS:** `js/trap.js` (+24: `clear_conjoined_pits` + `deltrap` call), `js/fountain.js` (clone −19, +1 import name, gush note). No new cross-module edge (`imports.mjs --can fountain.js trap.js delfloortrap`: ALREADY).
**Change:** port `clear_conjoined_pits` file-local in C order (`| 0` idiom, `xdir`/`ydir`/`N_DIRS`, `DIR_180`, `isok` + `t_at` neighbour lookup); `deltrap` calls it first per C `:6535`. `fountain.js` deletes the clone and imports the canonical `delfloortrap` export (no new module edge — `trap.js` already imported); reachable-domain identical because `gush` returns early on `u_at` cells, leaving the export's hero arm unreachable there.
**Verify:** `/tmp/probe_conjoined.mjs` 6/6 PASS (conjoined TRUE pre-delete; unlink; neighbour bit cleared; non-pit delete keeps neighbour bits; null guards; deleted after run — no maintained unit harness exists for `js/`, sessions are the acceptance tests per Constitution §1.2). `node scripts/verify.mjs --fn conjoined_pits` → PASS syntax (2 files) · PASS rule2 · note hidden (0 sessions blocked at HEAD — debt row cited 0 blocks, vacuous note not a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS.
**Named:** `deltrap` Sokoban `maybe_finish_sokoban` + `dealloc_trap` tail (C `:6536–6545`; shop/region cleanup still deferred as before).
**Next:** pop next Open row (`dig_check` head after archive).
## 2026-09-15 — D-2309 `pray.c` god_zaps_you lifesave fall-through + shieldeff + astral-block placement (review-32 debt #1)

**C locus:** `pray.c` `god_zaps_you` `:609–691` (no `return` after either `fry_by_god` — `done(DIED)` returns on lifesave/wizard-decline and C falls through to "is not deterred..." + beam; `shieldeff(u.ux,u.uy)` `:627`/`:634` first in both survive-lightning arms; astral/sanctum `summon_minion` ×3 `:678–687` OUTSIDE the `if (!Disint_resistance)` if/else). `fry_by_god` `:694–701` re-read, already faithful, untouched.
**JS:** 1 file (`pray.js`, +19/−9 net incl. ledger lines), far under the 600/10 caps. Small by nature: the cluster bulk shipped in D-0963 (+538); this is the remaining C delta plus review-32 debt #1.
**Change:** `js/pray.js` only (no new module edge — `shieldeff` joins the existing `display.js` import): both survive-lightning arms `await shieldeff(u.ux, u.uy)` in C order; each fry arm gates continuation on `game.program_state?.gameover` (C `done()` longjmps on death; JS `really_done` returns after setting the flag — `mhitu.c` `:938–950` idiom at `js/mhitu.js:4240-4244`); Disint arm restructured to C if/else with the astral/sanctum block outside; doc + file-header ledgers updated (`SetVoice` stays named: C `sndprocs.h` no-op without SND_LIB, verified `js/sndprocs.js:50`). Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_godzap.mjs` (arm no corpus session reaches; deleted after run): headless `runSegment` boot + direct `god_zaps_you` with Lifesaved extrinsic + `HDisint_resistance` + faked `astral_level` — post-fix PROBE-PASS (`gameover:false`, `uhp:10`, `minionDelta:3`); same probe on `git stash`ed pre-fix tree PROBE-FAIL (`minionDelta:0`), proving it discriminates the C-wrong. Probe detour worth recording: `const { game } = await import(gstate)` snapshots `export let game` (stale `{}`); use namespace access — and `--More--` inside `done()` needs padded `nhDisplay` keys.
**Named:** `SetVoice` ×2 (C-side no-op in this build); `mcastu ureflects` (pre-existing); post-death caller continuation (`digactualhole`/`angrygods` tails run on after `really_done` returns — codebase-wide C-longjmp pattern, out of this row).
**Next:** do not re-pop god_zaps_you/desecrate_altar/fry_by_god. Falsifier: a rescore or fresh `verify` showing a session blocked with a pray/wrath owner.
## 2026-09-15 — Audit 4581ce02..ef75880a (reviews 1270–1274: 5 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration, no `js/` edits. One SHA at a time, file written as each finished: 1270 mktrap dispatcher + maze trap loop (traptype_roguelvl verbatim; victim-tail delegation verified; tm pool/lava `IS_POOL` idiom noted latent-unreachable, not Must-fix), 1271 squeaky-board pair + sobj_at retire + door unblock_point (C :6078), 1272 `-`→fight (corpus session re-run PASS 72/72 by this review; every `'F'` site carries `-`), 1273 bury five arms (two genuine C-wrongs fixed: bare-lamplit leak, deltrap→delfloortrap; async ripple fully awaited), 1274 rile_shk surcharge (`(price+2)/3` bit-exact; dig `unblock_point`-vs-recalc drive-by adjudicated outcome-equivalent, not queued). Corpus re-runs all vacuous-honest except 1272's real move. Cadence full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed 46+0.29/turn. Queue 9 Open (in-band, no refill), Must-fix empty, D-2308/D-2306 archive hashes filled.
## 2026-09-15 — D-2308 `shk.c` shopdig anger arms: rile_shk bill surcharge + digactualhole make_angry_shk

**C locus:** `shk.c` `shopdig` `:5018–5110` (curse/growl arm calls `rile_shk`); `rile_shk` `:1362–1377` (NOTANGRY=FALSE + `(price+2)/3` bill walk); `dig.c` `digactualhole` `:821–822` (`if (mtmp->isshk) make_angry_shk(mtmp, 0, 0)` before `migrate_to_level`).
**JS:** 2 files (`shk.js` +8/−2, `dig.js` +7/−3 incl. doc-ledger updates), far under the 600/10 caps.
**Change:** `js/shk.js` only: `rile_shk` ports the walk verbatim in C order over the live `bill_p`/`bill` shape (same `||` fallback as `addupbill`; `| 0` integer idiom). `js/dig.js` only: the `teleport_pet` success arm calls the already-live `make_angry_shk` via the file's existing dynamic `shk.js` import (no new module edge, no `imports.mjs --can` needed); header envelope + Named-omit ledger updated. Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `verify --fn shopdig` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool-gated) · VERIFY: PASS. No hand probe: `rile_shk` is module-private (no production export added for testability) and both arms are straight-line C-order ports over live callees (`make_angry_shk` already exercised via its dokick/dothrow/teleport/trap callers); surcharge arithmetic spot-checked (price 10→14, 1→2, 0→0 both sides).
**Named:** `SetVoice` in `shopdig` warn (pre-existing); `#if 0` nolimbs curse/rile early-return (C-disabled, both sides); remaining `digactualhole` ledger (`buried_ball_to_punishment`, `ship_object`, shop `add_damage`, `liquid_flow` — other rows). Seen while reading, untouched: `dig()` `:520` `unblock_point` vs JS `recalc_block_point`, `:528–533` earth-debris elemental still deferred.
**Next:** do not re-pop shopdig. Falsifier: a rescore or fresh `verify` showing a session blocked with a shopdig/rile/make_angry owner.
## 2026-09-15 — D-2307 `dig.c` bury-family deferred arms: end_burn/maybe_unhide_at/buried-ball/delfloortrap/damage-chains/pooleffects

**C locus:** `dig.c` `bury_an_obj` `:1982–2047` (`end_burn` `:2011–2012`); `bury_objs` `:2049–2081` (`maybe_unhide_at` `:2074`); `unearth_objs` `:2084–2112` (`buried_ball` `:2091`, punish arm `:2096–2099`); `liquid_flow` `:837–879` (`u_at` `:843`, `delfloortrap` `:857`, damage chains `:868–872`, `pooleffects` `:875`). `buried_ball{,_to_punishment,_to_freedom}` + `rot_organic` re-read; bodies already faithful, untouched.
**JS:** 2 files (`dig.js` +56/−27 incl. doc-ledger updates, `zap.js` +1/−1 `await unearth_objs` in async `melt_ice`), far under the 600/10 caps.
**Change:** `js/dig.js` only (+ `js/zap.js` 1-line `await`): `end_burn` via dynamic `timeout.js` import (sync export; `stop_timer` runs `cleanup_burn` — C `timeout.c` order); `maybe_unhide_at` joins the existing `monmove.js` static import; `unearth_objs` now async with the `bball`/`TT_BURIEDBALL` arm verbatim (same-module `buried_ball_to_punishment`); `liquid_flow` in C order — `u_spot` hoisted pre-return, `delfloortrap` (already imported), `await unearth_objs`, `fire_damage_chain(objchain, TRUE, TRUE, x, y)` / `water_damage_chain(objchain, TRUE)` join the existing `trap.js` import, `pooleffects(FALSE)` via dynamic `pickup.js` import (file's convention for not-yet-imported modules). `imports.mjs --can` for the three cross-module names: monmove ALREADY, trap/pickup/timeout same 90-module SCC, call-time use only. Sanity-check `impossible` kept soft per file convention (`:1397` precedent).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_bury.mjs` PROBE PASS 16/16 (arms no corpus session reaches; deleted after run): bury/unearth round-trip with coords+where; ROCK merge; candle (burn timer + light) and magic lamp (light, no timer) bury with zero light-source leak; trapped ball within C's dist2≤8 search radius re-punished (`uball` set, not floor/buried); `liquid_flow` ROOM early-return + POOL path complete. Probe detours worth recording: `const { game } = await import(gstate)` snapshots `export let game` (took `{}`); static `import { game }` stays live — and C `buried_ball_to_punishment` searches from the *hero*, so a far-away test ball is correctly ignored (verified vs C before adjusting).
**Named:** sanity-check `impossible` text (file-convention soft); CORPSE-under-ice (C TODO `:2026–2028`, copied); RUST_METAL (`#if 0` both sides); `trap_ice_effects` in `melt_ice` (other row); `spoteffects`/`boulder_hits_pool` in `melt_ice` (pre-existing); `liquid_flow` caller in `digactualhole` (D-0954 row owns it).
**Next:** do not re-pop the bury family. Falsifier: a rescore or fresh `verify` showing a session blocked with a bury/unearth/liquid_flow owner.
## 2026-09-15 — D-2306 `cmd.c` `-`→fight binding missing from rhack dispatch (commands_init bind_key)

**C locus:** `cmd.c:2772` `(void) bind_key('-', "fight", FALSE)` in `commands_init` (unconditional — always installed, not num_pad-gated); extcmdlist `F` fight PREFIXCMD `:1722–1723`; `do_fight` `:1621–1634`; rhack `:3679` `cmdbind_get` dispatch; `cmd_from_func` `:3049–3052` skips `-` only for key display when `!num_pad` (behavior unaffected).
**JS:** 1 file (`cmd.js`, +9/−6), far under the 600/10 caps. Density note: the locus is the 1-line C bind over the already-ported `do_fight` arm — C is that small; value is the corpus PASS below.
**Change:** `js/cmd.js` only, no new module edge (`do_fight` same-module; no `imports.mjs --can` needed): `-` shares the `F` arm (`ch === 'F' || ch === '-'` → `do_fight`, `prefix_seen` = fight, C `:1621–1634` + `:2772` cited); the forcefight follow-up guard, the g/G rush-prefix guard, and the `menu_requested`-preserve gate all exempt `-` exactly like `F` (C: `-` IS the fight prefix, so F-then-`-` reaches `do_fight`'s "Double fight prefix, canceled." instead of the F-follow-up pline, and m-F-h keeps nopick); `rhack_repeat_command`/`rhack_repeat_txt` map `-`→`do_fight`/`fight` (Ctrl-A repeat parity with `F`). A user BIND= overlay on `-` still wins (overlay arm precedes the if/else, same as `F`). No RNG/message/control-flow change beyond C order; Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Direct replay `node frozen/ps_test_runner.mjs .cache/hidden/sessions/random-seed0015-valk-level2-pit-dog-wait-eb7e90ad.session.json`: before FAIL (RNG 8547/8547, Screen 70/72) → after PASS (8547/8547, 72/72). `node scripts/verify.mjs --fn rhack` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: ownerless step, 0 blocked at HEAD — NOT claimed as a corpus PASS; the row cited the session explicitly, proved by the direct replay) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (cmd.js not shared-gated) · VERIFY: PASS.
**Named:** `!`→doshell binding (2 corpus sessions, own future row); other number_pad/phone/swap_yz/pcHack dir layouts (pre-existing dokeylist omits); full CMD_gGF table (pre-existing turns.md:256).
**Next:** do not re-pop `-`→fight. Falsifier: a rescore or fresh `verify` showing a session blocked with a `-` Unknown-command step.
## 2026-09-15 — D-2305 `trap.c` disarm_squeaky_board + unsqueak_ok; try_disarm sobj_at; untrap door unblock_point

**C locus:** `trap.c` `unsqueak_ok` `:5606–5626`; `disarm_squeaky_board` `:5630–5660`; `try_disarm` boulder gate `:5456` (`sobj_at(BOULDER, …)`); untrap door set-off `:6079` (`unblock_point(x, y)`; C `vision.c:899` = `dig_point` + full-recalc flag). Audit basis: `disarm_holdingtrap` `:5553` / landmine `:5594` / shooting `:5664` / `help_monster_out` `:5700` / `disarm_box` `:5794` / `untrap_box` `:5821` vs the `js/trap.js` clones.
**JS:** 1 file (`trap.js`, +52/−14), far under the 600/10 caps. No committed probes (reads + `node --check` + `/tmp` probe only).
**Change:** `js/trap.js` only, no new module edge (`getobj`/`useup`/`consume_obj_charge` join the existing `invent.js` import; `GETOBJ_*` join `const.js`; `unblock_point` joins `vision.js`; `POT_OIL` follows the file's `objectNames.indexOf` convention; every callee same-module or already imported): `unsqueak_ok` verbatim (null→EXCLUDE; grease / dknown+`oc_name_known` oil→SUGGEST via `game.objects` table; other potions→DOWNPLAY; else EXCLUDE); `disarm_squeaky_board` verbatim (`getobj("untrap with", …)`; `bad_tool = cursed || ((not-oil || lamplit) && (not-grease || !spe))`; `try_disarm(ttmp, bad_tool)`; grease→`consume_obj_charge` else `useup`+`makeknown(POT_OIL)`; repair pline; `deltrap`; `newsym(ux+dx,uy+dy)`; `more_experienced(1, 5)`; `newexplevel`); `case SQKY_BOARD` wired in the `untrap` switch; boulder loop → `sobj_at(BOULDER, …)` (identical loop, canonical export); door set-off → `unblock_point` (identical outcome post-`D_NODOOR`, C-exact call). No RNG/message/control-flow change beyond C order; Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `verify --fn disarm_squeaky_board` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (trap.js not shared-gated) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `move_into_trap` (adjacent-Whoops; needs `test_move` export, D-1813); `stumble_on_door_mimic`. `disarm_squeaky_board` retired from the untrap doc ledger and data.md:176.
**Next:** do not re-pop untrap floor disarm_*/box. Falsifier: a rescore or fresh `verify` showing a session blocked with an untrap-envelope owner.
## 2026-09-15 — D-2304 `mklev.c` mktrap dispatcher + `mkmaze.c` populate_maze trap loop wired

**C locus:** `mklev.c:2036-2150` `mktrap` (invalid-args once-guard + paniclog; `m.x=m.y=0`; tm pool/lava abort; specified/rogue/hell-bias/`traptype_rnd`-loop kind select; hole→ROCKTRAP; tm-copy vs MAZEFLAG-mazexy/croom-somexyspace 200-try occupied/boulder loop; `maketrap`; post-tail) + static `traptype_roguelvl :2002-2030` (`rn2(7)` table); caller `mkmaze.c:1122-1123` (`mktrap(0, MKTRAP_MAZEFLAG, NULL, NULL)`).
**JS:** 1 file (`mklev.js`, +110/−7), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (reads only + `node --check`).
**Change:** `js/mklev.js` only, no new cross-module edge (every callee same-module or already imported — `MKTRAP_NOFLAGS/SEEN/MAZEFLAG` join the existing `const.js` flag import; `--can` correctly skipped): new `traptype_roguelvl` verbatim; new `mktrap(num, flags, croom, tm)` in C order (once-guard via module-level `mktrap_err`; tm pool/lava abort via the module `IS_POOL/IS_LAVA` idiom; specified → rogue (`Is_rogue_level` live import) → hellish `!rn2(5)` FIRE_TRAP → `traptype_rnd` loop; `is_hole`/`Can_fall_thru` ROCKTRAP; tm-copy vs mazexy/somexyspace 200-try `occupied`/`sobj_at(BOULDER)` loop; `maketrap`; tail delegated to live `mktrap_seen_victim` with `seen` derived from `MKTRAP_SEEN`); `populate_maze` gains the C 6th loop verbatim and the omit comment retires.
**Verify:** `node scripts/verify.mjs --fn mktrap` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` PASS on a clean tree before edits. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `mktrap` invalid-args `paniclog` write (file-only, no scored-JS equivalent under Rule #2; observable behavior — no trap — kept; C `mktrap_err` once-semantics mirrored); `traptype_rnd` LEVEL_TELEP `single_level_branch` (pre-existing Knox deferral, untouched); `makemaz` load_special-path `dmonsfree` (mon.c, own future row) and wizard SPLEVTYPE/`getenv` override (wizard-only env read, no plain-ESM equivalent; both already named at `js/mklev.js:1478/1511/1525`). Other `mktrap` call sites (`mklev.c:985` room path, `sp_lev.c:1845`) keep their existing live inlines — rewiring them is separate rows.
**Next:** do not re-pop populate_maze/mktrap (dispatcher + all six stock loops live). Future maze-level divergences attribute to their true writer.
