# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-06 — D-1951 o_init.c doclassdisco class-discovery singleton

**C locus:** `nethack-c/upstream/src/o_init.c` — `doclassdisco` `:888–1127` (discosort normalize; `menu_requested → choose_disco_sort(2)`; unique `'u'` scan; artifact `'a'` count; packorder+VENOM class collection; traditional `yn_function` vs menu class pick incl. `MENU_PARTIAL` single-class skip; `'u'/'r'`, `'a'`, default display arms; text window iff `ct`). Helpers in the cluster: `choose_disco_sort` `:611–657`, `disco_fmt_uniq` `:725–741`, `disco_output_sorted` `:743–766`, `discovered_cmp` `:553–565`, `oclass_to_name` `:877–886`; `artifact.c` `disp_artifact_discoveries` `:1147–1175`, `dump_artifact_info` `:1177–1215`. Sole C caller: extcmd `` ` `` `knownclass` (`cmd.c:1752`) + `cmd.c:60` extern decl.
**JS:** `js/o_init.js` (+309/−4), `js/artifact.js` (+61/−1; 2 js files, 365 insertions, under 600 cap). Density note: C is ~420 lines across the cluster; the JS carries the full envelope plus file-idiom JSDoc/imports.
**Change:** `js/artifact.js` — exported `disp_artifact_discoveries(lines)` (null lines ≡ WIN_ERR count-only; A_NONE prints "non-aligned" since JS `align_str` has no unaligned arm) and `dump_artifact_info(lines)` (live `"  %-36.36s%s"` shape, `#if 0` tab arm dead in C; `game.artiexist.rnd` ≡ C `.rndm`), ATR headers per the `dodiscovered` convention. `js/o_init.js` — `DISCO_ORDER_LET`/`DISCO_ORDERS_DESCR`/`UNIQ_OBJS` consts; file-local `discovered_cmp` (strcmpi past the mark; stable `Array.sort` per Constitution §4), `disco_output_sorted` (lootsort rewrite kept for shape), `disco_fmt_uniq` (C outbuf → return), `oclass_to_name`; exported `choose_disco_sort(mode)` (mode-2 note, title-as-first-item per `rename_disco`; 1 pick / −1 dismiss; PICK_ONE kills the C n>1 arm; preselect highlight has no pick_one equivalent); exported `async doclassdisco()` in C order (`'o'` normalize writes back `game.flags.discosort`; menu_requested gate; `lootsort=false` named; traditional `yn_function(prompt, discosyms, NUL)` with unseen-syms+ESC envelope and `!c‖NUL → clear + return`; `MENU_PARTIAL` single-class skip; class menu with menulet-order selectors and C group accelerators as `gselector`, incl. `'r'` for unique; `'u'/'r'` upstart header + `disco_fmt_uniq` loop; `'a'` wizard `y_n` dump vs discoveries; default `def_char_to_objclass` + `impossible`-then-fall-through + sortname header + bases-range loop; `show_text_pages` iff `ct`; `ECMD_OK`).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn doclassdisco` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/artifact.js js/o_init.js`; `PASS rule2`; `note hidden verify doclassdisco: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full` (tool: no shared file changed). Probes (/tmp, deleted): import smoke (no TDZ/cycle); count-mode 0 empty / 2 seeded with `  Excalibur [lawful …]` lines; dump 34 lines with `%-36s` padding; both exports live. (Bare-node `init_objects` RNG-seed failure is a probe-harness limit — ISAAC needs game boot, not a port fact.)
**Named:** discosort `'s'` sortloot order (`sortloot_descr` needs `loot_classify`, `invent.c:149–305` — own-row material; falls back to discovery order under a `'sortloot' order` header); caller wiring (function live, unwired): extcmd `` ` `` `knownclass` (`cmd.c:1752`; the `extcmdlist_data.js` row exists, no `cmd.js` dispatch arm); `choose_disco_sort` current-sort preselect highlight (no pick_one equivalent); `dodiscovered` `menu_requested`/`choose_disco_sort(1)` arm stays as named in `js/invent.js` (function now live for future wiring).
**Next:** pop the next Open row (`mkmaze.c` mazexy + is_solid).
## 2026-09-06 — D-1950 stairs.c On_ladder ladder predicate singleton

**C locus:** `nethack-c/upstream/src/stairs.c` — `On_ladder` `:154–159` (`stairway *stway = stairway_at(x, y); return (stway && stway->isladder)`). Sole C caller `dig.c:1812` (`adj_pit_checks` ladder arm, before the `supporting` stairs/drawbridge else-chain; `On_stairs` arm follows in the else).
**JS:** `js/mklev.js` (+7/−0), `docs/c-js-map/turns.md` stairs section (+2/−1; 2 files, under 600 cap). Density note: C is 5 lines; the JS is the lookup + boolean plus file-idiom JSDoc.
**Change:** `js/mklev.js` — exported `On_ladder(x, y)` in C order (`stairway_at(x | 0, y | 0)` then `!!(stway && stway.isladder)` for C boolean; `| 0` int idiom per the `hack.js`/`dogmove.js` `On_stairs` clones), placed directly after `stairway_at` beside the stairway lookups with the C line citation. Same-module callee — no new import edge. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn On_ladder` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mklev.js`; `PASS rule2`; `note hidden verify On_ladder: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). Hand probe (inline `node -e`, no file left): no stairs → false; stairs non-ladder → false; ladder → true; off-cell → false.
**Named:** caller wiring (function live, unwired): `dig.c:1812` `adj_pit_checks` ladder + `supporting` stairs/drawbridge envelope still named omitted in `js/dig.js` (zap_dig header lists `adj_pit_checks` deferred).
**Next:** pop the next Open row (`o_init.c` doclassdisco).
## 2026-09-06 — D-1949 steed.c exercise_steed riding-skill training singleton

**C locus:** `nethack-c/upstream/src/steed.c` — `exercise_steed` `:386–398` (`if (!u.usteed) return; ++u.urideturns >= 100 → urideturns = 0 + use_skill(P_RIDING, 1)`). Sole C caller `hack.c:2883` (domove tentative occupy: `u.ux += u.dx; u.uy += u.dy; m_postmove_effect; usteed->mx/my = u.ux/uy; exercise_steed()` — training counts even when the move later bounces on a safemon swap, per the C comment).
**JS:** `js/cmd.js` (+4/−0), `js/steed.js` (+22/−2 header+import; 2 js files, under 600 cap). Density note: C is 11 lines; the JS is the guard + counter + one call plus file-idiom JSDoc, import, and caller wiring.
**Change:** `js/steed.js` — exported `exercise_steed()` in C order (`!u.usteed` early return; `| 0` int idiom, which also covers fresh JS saves where `urideturns` was never set vs C decl zero-init; `>= 100` → reset + `use_skill(P_RIDING, 1)`), placed before `maybewakesteed` beside the riding helpers with the C line citation. Wired in `js/cmd.js` domove exactly at the C site (after `m_postmove_effect` + usteed mx/my set, before the safemon-swap arm) with the `hack.c:2883` comment. Static `cmd.js → steed.js` edge follows the existing `do.js`/`dog.js`/`dogmove.js`/`dokick.js` precedent; `imports.mjs --can` notes the shared 88-module SCC (cycle alone not a blocker) and the call is runtime-only, never a top-level TDZ read.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn exercise_steed` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/cmd.js js/steed.js`; `PASS rule2`; `note hidden verify exercise_steed: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probe (inline `node -e`, no file left): no-usteed → no-op (`urideturns` untouched); `urideturns` 98 → 99 with no train; 99 → 0 with `advance` 0 → 1.
**Named:** none new — the domove steed envelope around the call site stays as named in `c-js-map/turns.md` (steed lead-through, displacer swap, `domove_bump_mon` deferred).
**Next:** pop the next Open row (`stairs.c` On_ladder).
## 2026-09-06 — D-1948 mondata.c mon_hates_light light-hatred predicate singleton

**C locus:** `nethack-c/upstream/src/mondata.c` — `mon_hates_light` `:547–550` (`return (boolean) hates_light(mon->data)`); `hates_light` is the `mondata.h:215` macro (`(ptr) == &mons[PM_GREMLIN]`), already live in JS as the `hates_light` mndx export (`js/monsters.js:335`). Sole C caller `uhitm.c:1039` (hitmsg `lightobj` arm).
**JS:** `js/monsters.js` (+9/−0; 1 js file, under 600 cap). Density note: C is 3 lines; the JS is the one-line wrapper plus file-idiom JSDoc.
**Change:** `js/monsters.js` — exported `mon_hates_light(mon)` (`return hates_light(mon?.data)`), placed directly after `hates_light` beside the sibling `mon_hates_silver` wrapper (D-1254) with the C line citation; `?.` only guards foreign callers (C is NONNULLARG1). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn mon_hates_light` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/monsters.js`; `PASS rule2`; `note hidden verify mon_hates_light: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probe (/tmp, deleted): PM_GREMLIN mndx 40 — `hates_light`/`mon_hates_light` true on gremlin data, false on orc data, false on null.
**Named:** caller wiring (function live, unwired): `uhitm.c:1039` hitmsg `lightobj` arm has no JS symbol in `js/uhitm.js` (grep-verified: no `artifact_light`/`lamplit`/`lightobj` there; only the `weapon.js:333` spec_dbon arm covers the predicate).
**Next:** pop the next Open row (`steed.c` exercise_steed).
## 2026-09-06 — D-1947 mthrowu.c ucatchgem thrown-gem catch singleton

**C locus:** `nethack-c/upstream/src/mthrowu.c` — `ucatchgem` `:505–529` (`otyp <= LAST_GLASS_GEM && is_unicorn(youmonst.data)`; glass `FIRST..LAST` → `You catch` + `You are not interested` + `makeknown` + `dropy`, else `You accept` + `hold_another_object`; rock/gray stone above LAST never caught). Sole C caller `m_throw` `:692` (`oclass == GEM_CLASS && ucatchgem` → `break`, before the tethered-gated `u_catch_thrown_obj` `:695` and `potionhit`).
**JS:** `js/mthrowu.js` (+41/−3 with the header line; 1 js file, under 600 cap). Density note: C is 21 lines; the JS carries the same guard + two arms plus file-idiom header/comment/const lines.
**Change:** `js/mthrowu.js` — exported `async ucatchgem(gem, mon)` in C order (`| 0` int idiom; `game.youmonst?.data` per the allmain/apply idiom; `gem_xname` then `mon_s_name` per C initializer order; glass pline×2 → `makeknown` → dynamic `await import('./do.js')` `dropy` per the file's flooreffects idiom, else accept-pline → `hold_another_object` with C's format strings verbatim; `true`/`false` for C TRUE/FALSE). Wired in `m_throw` exactly at `:692` (`GEM_CLASS && await ucatchgem` → `break`, before the `:695` tethered catch). Static extensions reuse live edges only (`s_suffix as s_suffix_ucatch` from do_name.js, `is_unicorn` from monsters.js, `makeknown`+`hold_another_object` from invent.js); `dropy` dynamic.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn ucatchgem` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mthrowu.js`; `PASS rule2`; `note hidden verify ucatchgem: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probes (deleted, /tmp only): non-unicorn + glass → `false`; true-unicorn (`M2_JEWELS`) + rock-otyp (`LAST+5`) → `false`; FIRST 461 / LAST 469 confirmed via `objectNames`.
**Named:** `m_throw` envelope still named per review 296 (pre-existing, not this arm): `thrwmu` always_toss/polearm, `shade_miss` is live but iron-bars/sink arms, egg/pie/venom `thitu` switch, elf-bow `hitv`, and the neighboring `u_catch` early-`return` vs C `break` (+`thrownobj` clear) gap.
**Next:** pop the next Open row (`mondata.c` mon_hates_light).
## 2026-09-06 — D-1946 vault.c move_gold vault gold-move singleton

**C locus:** `nethack-c/upstream/src/vault.c` — `move_gold` `:632–643` (`remove_object`; `newsym(old)`; `nx = rooms[vroom].lx + rn2(2)`, `ny = rooms[vroom].ly + rn2(2)`; `place_object`; `stackobj`; `newsym(new)`). Sole C caller `wallify_vault` `:677` (gold sitting on a wallified boundary cell is moved into the vault).
**JS:** `js/vault.js` (+30/−2 across the file; 1 js file, under 600 cap). Density note: C is 11 lines; the JS carries the same 7 statements plus file-idiom header/comment lines.
**Change:** `js/vault.js` — exported `move_gold(gold, vroom)` in C order (ox/oy saved before extract since C reads them post-extract; `obj_extract_self` → `newsym(old)` → `(rm.lx | 0) + rn2(2)` / `(rm.ly | 0) + rn2(2)` via `game.level?.rooms` per the paygd idiom → `place_object` → `stackobj` → `newsym(new)`; `| 0` int idiom). One new static edge (`place_object`/`stackobj`/`obj_extract_self` from mkobj.js), `imports.mjs --can` SAFE (same SCC, hoisted function declarations, no top-level TDZ read). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn move_gold` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/vault.js`; `PASS rule2`; `note hidden verify move_gold: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probe abandoned: an isolated `import './js/vault.js'` probe hits an objnam↔polyself `_body_part` TDZ under the probe's import order (app entry order unaffected — cohort boots green); probe file deleted, no code changed for it.
**Named:** caller wiring (function live, unwired): `wallify_vault` body (still stub — wall repair / whisper / distant-chant / gold-move plines deferred, map-named).
**Next:** pop the next Open row (`mthrowu.c` ucatchgem).
## 2026-09-06 — D-1945 steal.c stealamulet amulet-steal singleton

**C locus:** `nethack-c/upstream/src/steal.c` — `stealamulet` `:688–767` (quest-artifact sweep, `rnd(n)` random pick past the first; else uhave amulet/bell/book/menorah otyp sweep with the fake-count `!mtmp->iswiz` gate; outer-gear strip in C order — suit cloak, shirt armor, gloves via weapon-first plus twoweap swap, ring gloves; worn-target strip; unpaid `subfrombill(shop_keeper(*u.ushops))`; `freeinv`; `doname` captured before `mpickobj`; `"%s steals %s!"` pline; teleporter `rloc(RLOC_MSG)`; `encumber_msg`). `any_quest_artifact` is the `obj.h:271` macro (`oartifact >= ART_ORB_OF_DETECTION`).
**JS:** `js/steal.js` (+130/−3 across the file; 1 js file, under 600 cap). Density note: C is 78 live lines; the JS carries the same arms plus the file-idiom header/envelope lines.
**Change:** `js/steal.js` — exported async `stealamulet(mtmp)` in C order (`!--n` pre-decrement pick; `++n` + trailing-target sweep shape, last match wins before the `rnd` reselect, exactly as in C; `real`/`fake` init 0 so the book/menorah arms leave `fake` unset exactly as in C; buf captured before `mpickobj`; `can_teleport(mtmp.data) && !(await tele_restrict(mtmp))` short-circuit so `rloc` never fires under restriction; `*u.ushops` as `(u.ushops || '')[0]` per the zap.js idiom, null-shopkeeper-safe via the shk early return). `any_quest_artifact` inlined at both sweeps (`(o.oartifact | 0) >= ART_ORB_OF_DETECTION`) per the muse.js `mon_has_special` idiom — macro expansion, not a second symbol. Imports: `rnd` on the existing rng edge, `can_teleport` on monsters, `RLOC_MSG` on const, `ART_ORB_OF_DETECTION` on generated/artifacts_data; three new edges all `imports.mjs --can` SAFE (same 88-module SCC, call-time reads only, no top-level TDZ): `subfrombill`/`shop_keeper` from shk.js, `tele_restrict`/`rloc` from teleport.js.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn stealamulet` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/steal.js`; `PASS rule2`; `note hidden verify stealamulet: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row, brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; forced `--full` → `PASS full 44/44` (import-edge change: new static shk/teleport edges). Hand probe (/tmp, 6 asserts, deleted): empty-uhave silent return, real-amulet steal, fake stolen non-wiz but kept vs iswiz, quest-artifact preferred over amulet, two-artifact `rnd` steals exactly one.
**Named:** caller wiring (function live, unwired): `mhitm_ad_samu` `!rn2(20)` arm (`uhitm.c:4584` — no JS symbol, grep-verified); module-local `freeinv` reuse (sibling-`steal()` shape; the fuller invent.js export drift is pre-existing map debt, brief-flagged).
**Next:** pop the next Open row (`vault.c` move_gold).
## 2026-09-06 — D-1944 nhlsel/selvar selection_sub + selection_recalc_bounds (`-` operator)

**C locus:** `nethack-c/upstream/src/nhlsel.c` — `l_selection_sub` `:361–385` (fresh push_new result, `rect_bounds` union loop per `rect.c:134–141`, `val = (a ^ b) & a`, `selection_recalc_bounds`, pop operands); `selvar.c` `selection_recalc_bounds` `:99–165` (dirty-gated; left/right/top/bottom scans; empty keeps lx=COLNO/ly=ROWNO/hx=hy=0). Live Lua callers: `dat/Tou-loca.lua:131` `validtraps - (area + area)`, `dat/Tou-goal.lua:112` `validtraps - selection.area(60,14,71,18)`.
**JS:** `js/mklev.js` (+62/−7 across the file; 1 js file, under 600 cap). Density note: C is ~100 live lines for the pair incl. recalc scans; the Set model compresses the four scans to one min/max pass with identical endpoints.
**Change:** `js/mklev.js` — file-local `selection_recalc_bounds(sel)` (C endpoint semantics; unconditional recompute — the Set model has no dirty flag since set expands bounds and delete never shrinks, so C's dirty=FALSE early-return implies already-tight bounds and the endpoints are identical); file-local `selection_sub(sela, selb)` (sela membership minus selb membership ≡ C's union cell loop, then recalc so bounds stay tight like C instead of a clone's wider bounds; no RNG). Rewired both sites to C evaluation order: Tou-loca → `selection_sub(validtraps, selection_or(A, B))`, Tou-goal → `selection_sub(validtraps, fillrect)`. Same module, no new cross-module edge.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn selection_sub` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/mklev.js`; `PASS rule2`; `note hidden verify selection_sub: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — HELDOUT map row — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared file changed). No hand probe: the rewired arms (Tourist quest loca/goal trap placement) are reached by no public or corpus session; equivalence is set-theoretic (same membership + same tight bounds as the replaced and+not form, no RNG in either), stated here not probed.
**Named:** `l_selection_xor` (`~` — no `dat/*.lua` use, verified by grep); mutating `selection_clear` (both endpoints already expressed: `selection_new()` ≡ clear(0), `selection_not(selection_new())` ≡ clear(1) per `l_selection_not:265–268`); `selection_do_ellipse`/`selection_do_gradient`/`l_selection_circle` (no `dat/*.lua` circle/ellipse/gradient use); full Lua VM + remaining `des.*` (pre-existing).
**Next:** pop the next Open row (`steal.c` stealamulet).
## 2026-09-06 — Audit D-1935…D-1943 against pinned C (reviews 905–913) + cadence score

Review iter: 9 JS-touching SHAs since review 904, one file each written as its SHA finished, committed together. 905 `45c4bb85` found_artifact/find_artifact ACCEPT (ternary + xname :660–661 exact). 906 `efc7a95b` spec_ability ACCEPT (SPFX values vs artifact.h, identity sound on stable _artilist, detect :2027/sit :609 loci confirmed). 907 `23674982` backstab/disguise ACCEPT (zap :197/:4952–4955 verbatim; helpless/miss_msg clones pre-existing). 908 `9cafb38d` boh loss ACCEPT (Is_mbag obj.h:339, BAG_OF_TRICKS const present despite sym miss, stolen_value/scatter awaited). 909 `53deed83` polyuse ACCEPT (MAIL_STRUCTURES unconditional, ZT_SPELL=10+x, wish no-op is production shape :6277 #ifdef DEBUG). 910 `628d0ae3` gcrownu ACCEPT (193-line body walked arm-by-arm; give_may_advance_msg empty-if is a named OMIT; 3rd Your clone noted as debt, divergence-proof). 911 `78298ee6` gloc_filter ACCEPT (flood push/pop accounting equivalent; macros sym.h:104–106/rm.h:121; init/done placement :530/:553). 912 `48c80d04` Armor_gone ACCEPT (takeoff guards = struct idiom; dragon_armor_handling pre-existing clone). 913 `82cffe79` dbridge entities ACCEPT (all nine walked; vtense-null + youmonst-identity idioms confirmed). 9 ACCEPT, 0 Must-fix. Each hidden-verify re-measured with --base at parent: all 0-blocked vacuous as stated. Rule #2 clean suite-wide.
**Score:** full `sessions` at HEAD `82cffe79` — fortress holds 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `70+0.49/turn` (R² 0.84).
**Next:** pop the next Open row (`sp_lev.c` lspo_* + selvar selection primitives).
## 2026-09-06 — D-1943 dbridge.c e_at/m_to_e/u_to_e/e_nam/E_phrase entity family

**C locus:** `nethack-c/upstream/src/dbridge.c` — `e_at` `:286–301` (first `go.occupants` record with edata at (x,y), else null; debugpline D_DEBUG-only); `m_to_e` `:304–319` (fill from monster, worm-tail edata off-head, null clears); `u_to_e` `:321–328` (`&gy.youmonst`, u.ux/uy, youmonst.data); `set_entity` `:330–339` (u_at→u_to_e else m_to_e, null m_at ok); `is_u` `:341` / `e_canseemon` `:342` macros; `e_nam` `:351–355` ("you"/mon_nam); `E_phrase` `:361–377` (Monnam + vtense-null-subject verb). Callers all in `do_entity` (`:650–682`, still omitted).
**JS:** `js/dbridge.js` (+108/−5 across the file; 1 js file, under 600 cap). Density note: C is ~70 live lines incl. set_entity/macros, so this is the C-matched size.
**Change:** `js/dbridge.js` — file-local `occupants()` (C decl.c zero-init shape, lazily ENTITIES `{emon:null,edata:null,ex:0,ey:0}` records on `game`); exported `e_at` (C scan order, early return ≡ break+index, `| 0` int idiom), `m_to_e` (C order incl. wormno off-head → `mons(PM_LONG_WORM_TAIL)`, null → edata null + ex=ey=0), `u_to_e` (C order; `game.youmonst` identity per the allmain/u_init idiom), `set_entity` (C u_at/m_at branch order), `is_u` (`=== game.youmonst` pointer identity), `e_canseemon` (macro order), `e_nam` ("you"/mon_nam), `E_phrase` (Monnam + `vtense(null, verb)`; `!verb||!verb[0]` ≡ `!verb||!*verb`; C static-80-char buffer → returned string, callers consume immediately). Imports: `canseemon`/`mon_nam`/`Monnam` on existing display/do_name edges; three new edges all `imports.mjs --can` SAFE (`m_at` from mon.js hoisted; `vtense` from objnam.js hoisted; `mons` from monsters.js no-cycle; `PM_LONG_WORM_TAIL` + `ENTITIES` generated/const data). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn e_at` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/dbridge.js`; `PASS rule2`; `note hidden verify e_at: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (/tmp, 12 asserts, deleted): empty e_at → null, u_to_e identity/coords/edata, is_u/e_nam/E_phrase hero arms ("you"/"You try"/bare "You"), m_to_e null-clear + mon edata/coords.
**Named:** caller wiring (functions live, unwired): `do_entity` crush/jump/relocate (`e_jumps`/`e_survives_at`/`e_died`, `dbridge.c:380–700`, js/dbridge.js header); pre-existing omits untouched (revive_nasty, scatter debris, flooreffects body, nokiller, You_see polish).
**Next:** pop the next Open row (`sp_lev.c` lspo_* + selvar selection primitives).
## 2026-09-06 — D-1942 do_wear.c Armor_gone/count_worn_armor/any_worn_armor_ok armor-count family

**C locus:** `nethack-c/upstream/src/do_wear.c` — `Armor_gone` `:939–960` (uarm snapshot → takeoff.mask clear → setnotworn → cancelled_don reset → was_arti_light end_burn + "stop shining" → dragon_armor_handling(FALSE, FALSE)); `any_worn_armor_ok` `:3480–3485` (owornmask & W_ARMOR → SUGGEST else EXCLUDE); `count_worn_armor` `:3489–3502` (seven-slot count); callers `polyself.c:1173/1207` (break/slip armor) and `read.c:1368/1378` (blessed gets_choice gate + getobj filter).
**JS:** `js/do_wear.js` (+73/−3 across the file; 1 js file, under 600 cap). Density note: C is ~39 live lines so this is the C-matched size.
**Change:** `js/do_wear.js` — exported async `Armor_gone()` in C order (was_arti_light snapshot before setnotworn since unwearing clears the W_ARM bit artifact_light reads on gold DSM/scales; a prior end_burn via the polyself lamplit gate suppresses the message exactly as in C; `game.context?.takeoff` guards per the Cloak_off idiom; `!Blind()` + `` `${Tobjnam(otmp, 'stop')} shining.` `` per the wield.js:321 shape; `await dragon_armor_handling(otmp, false, false)` after the cancelled_don reset so the BLUE doff "slow down" gate reads C order). Exported sync `any_worn_armor_ok(obj)` (`(obj.owornmask | 0) & W_ARMOR` per the file idiom) and `count_worn_armor()` (seven `game.u` slots in C order). Imports: `Tobjnam` + `setnotworn` on existing edges; one SAFE new edge per `imports.mjs --can` (`artifact_light`, `end_burn` from `./timeout.js` — hoisted function declarations, no top-level TDZ read).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn Armor_gone` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/do_wear.js`; `PASS rule2`; `note hidden verify Armor_gone: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none for all three — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Hand probe (/tmp, 11 asserts, deleted): count 0/3/7, any_worn null/unworn/cloak, Armor_gone null → 0 + mask-clear + cancelled_don reset, worn plain suit → 0 + uarm slot cleared.
**Named:** caller wiring (functions live, unwired): polyself `break_armor` suit arms (`polyself.c:1173/1207` — JS still setworn+dropx, js/polyself.js); read.c blessed destroy-armor gets_choice arm (`read.c:1366–1384` — getobj destroy prompt + disintegrate arms, js/read.js seffect_destroy_armor).
**Next:** pop the next Open row (`dbridge.c` e_at/m_to_e entity family).
## 2026-09-06 — D-1941 getpos.c gloc_filter family (classify/matcharea/floodfill/init/done + AREA gate)

**C locus:** `nethack-c/upstream/src/getpos.c` — `gloc_filter_classify_glyph` `:340-361`, `gloc_filter_floodfill_matcharea` `:363-379`, `gloc_filter_floodfill` `:381-388` (+ `selvar.c` `selection_floodfill` `:395-440` for the walk), `gloc_filter_init` `:390-409`, `gloc_filter_done` `:411-419`, `GLOC_SAME_AREA` `:336-339`, AREA gate in `gather_locs_interesting` `:446-449`, init/done in `gather_locs` `:530/553`.
**JS:** `js/getpos.js` — file-local `gloc_filter_classify_glyph` / `gloc_filter_floodfill_matcharea` / `gloc_same_area` / `gloc_filter_floodfill` / `gloc_filter_init` / `gloc_filter_done` (C staticfn shape); AREA gate in `gather_locs_interesting`; init/done around the `gather_locs` scan; `GFILTER_AREA`/`S_upstair`/`S_fountain` added to the existing `const.js` import (no new cross-module edge).
**Change:** ported the family in C branch order (`| 0` int idiom; `sym.h` `is_cmap_*` macro shape for the three missing predicates; `IS_DOOR` ≡ C `(typ == DOOR)`; `selection_getpoint` null-map → 0; seed joins unconditionally with `isok` gating the pop; 4-neighbour walk, `diagonals` FALSE; doorway far-side flood with the C TODO-nothing else-arm). Map state on `game.gloc_filter_map` (Set of `x,y`, null when inactive) + `game.gloc_filter_floodfill_match_glyph`, mirroring `gg.*`. No RNG consumed on the path.
**Verify:** `node scripts/verify.mjs --fn gloc_filter_init` → PASS syntax (1 file) · PASS rule2 · hidden note (no corpus session blocked — HELDOUT map row, vacuous by construction, not claimed as corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7. Hand probe (<30 lines, deleted): AREA gate with null map returns false, with live map evaluates without throw. Inert under default filter (both new branches filter-gated) — green+cohort prove no regression.
**Named:** `getpos_menu` listing; `S_goodpos` tmp_at hilite; engraving full showsyms; `docrtRefresh` redraw_map-only; `cmdq_pop` at getpos start; mouse/do_run prefix (all pre-existing, untouched).
**Next:** next Open row (`do_wear.c` armor-count family).
## 2026-09-06 — D-1940 pray.c gcrownu/at_your_feet crowning/vicinity family + weapon.c add_weapon_skill

**C locus:** `pray.c` `at_your_feet` `:788–802` (Blind→Something; uswallow→into-ustuck-stomach; else beneath/at + feet); `gcrownu` `:805–996` (outside intrinsics → godvoice → wizard/monk class-gift → lawful/neutral/chaotic crowning → weapon enhance → add_weapon_skill(1)); callers `pray.c:1343` (pleased pat_on_head case 7/8, record>=PIOUS && !uhand_of_elbereth) and at_your_feet at `:883/:932/:958/:1063/:1815`; `weapon.c` `add_weapon_skill` `:1437–1452` (before/after can_advance count around `weapon_slots += n` → give_may_advance_msg(P_NONE)).
**JS:** `js/pray.js` + `js/weapon.js`, 318 insertions / 16 deletions across the commit incl. the CURRENT.md Next-cluster line (~300 of them the C-order port); 2 js files, under 600 cap. Density note: C is ~205 lines so this is the C-matched size.
**Change:** `js/pray.js` — exported async `at_your_feet(str)` and `gcrownu()` in C order (`| 0` int idiom; `ok_wep` arrow from the C macro; otyp via `objectNames.indexOf` per the file idiom; ART_* from `./generated/artifacts_data.js`; chaotic swordbuf via `hcolor('black')` per the pray.js convention for C `hcolor(NH_BLACK)`; actualoname inlined as override_ID=1 + `xname` with restore per objnam.c:2490, minimal_xname having no JS export; lawful Excalibur transform keeps simpleonames-before-oname order; `Your` added file-local per the artifact.js/zap.js shape, not a third clone import). `js/weapon.js` — exported sync `add_weapon_skill(n)` in C order on the local `can_advance`. Imports extended on existing edges only (carrying, mksobj/bless, known_spell/spe_Unknown, oname/s_suffix, observe_object/update_inventory, ansimpleoname/simpleonames, WEAPON_CLASS/SPBOOK_CLASS, uhis, is_weptool, mbodypart, dropy, artifact five) plus two SAFE new edges per `imports.mjs --can` (upstart from `./hacklib.js` — no cycle at all; weapon trio from `./weapon.js` — same 88-module SCC, runtime calls only, no top-level TDZ read).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` before the change → VERIFY: PASS (green 2/2 + strict). `node scripts/verify.mjs --fn gcrownu` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/pray.js js/weapon.js`; `PASS rule2`; `note hidden verify gcrownu: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none for both — so no `--base` re-run owed, and this log makes no corpus-PASS claim); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. Export smoke: `gcrownu`/`at_your_feet`/`add_weapon_skill` all `function`.
**Named:** caller wiring (functions live, unwired): pleased pat_on_head case 7/8 `gcrownu()` gate (`:1339–1347`) and case 6 `give_spell()`; `SetVoice` pitch (repo-wide defer); `give_may_advance_msg(P_NONE)` You_feel/handle_tip inside `add_weapon_skill`; minimal_xname-vs-xname nuance inside the inlined actualoname (livelog-only string).
**Next:** pop the next Open row (`getpos.c` gloc_filter family).
