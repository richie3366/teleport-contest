# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2076 wield.c can_twoweapon used xname where C prints Yname2: dual-wield refusal dropped the "Your " prefix, so JS drew «battle-axe isn't one-handed.» where C draws «Your battle-axe isn't one-handed.» (queue owner can_twoweapon)

**C locus:** `wield.c:760–804` `can_twoweapon` — TWOWEAPOK arm `:781–786` (`pline("%s %s suitable %s weapon%s.", Yname2(otmp), is_plural(otmp) ? "aren't" : "isn't a", primary/secondary, plur(quan))`); bimanual arm `:787–790` (`pline("%s isn't one-handed.", Yname2(otmp))`). `Yname2` = upstart(yname) = "Your \<name\>" for carried non-pname wielded gear; `is_plural` = obj.h:421 (`quan != 1` or undiscovered Eyes of the Overworld); `plur(quan)` is quan-only ("" iff 1).
**JS:** 1 file (wield.js +13/−8) — under the 600/10 caps. Small because the C locus is two one-line plines and every callee (`Yname2`, `is_plural`) was already live/exported; density exception applies (corpus-driven Open row with a recorded expectation, C that small — D-2071/D-2074/D-2075 precedent).
**Change:** `js/wield.js` — both arms now print `${Yname2(otmp)}` (`Yname2` joins the existing `./objnam.js` import — same module edge as `xname`, no new cycle, no TDZ); suitability arm uses `is_plural(otmp)` for aren't/isn't-a while `plur` stays quan-only (`((otmp.quan ?? 1) | 0) !== 1 ? 's' : ''`, matching C `plur(quan)`); C line cites in comments; `can_twoweapon` doc retires `Yname2` from the named-omission list.
**Verify:** `node scripts/verify.mjs --fn can_twoweapon` → PASS syntax (1 changed file: js/wield.js) · PASS rule2 · PASS hidden `3 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-kit-Barbarian-92001, scen-kit-Samurai-92145, scen-normal-Samurai-92078 all PASS — brief showed 3 blocked vs the row's cited 2) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** artifact-resist arm still uses `xname` (live `Yobjnam2` export not wired; local clone at wield.js:1063 stays per the objnam "clones stay" note); CORPSE/`cant_wield_corpse` arm absent (`cant_wield_corpse` not ported — would return FALSE silently per C `:795–798`); `body_part(HAND)` is a 'hand' literal. No blocked session reaches any of these (map `turns.md` wield.c row updated).
**Next:** pop the next Open row (`do_wear.c` armoroff). Do not re-pop `can_twoweapon` for these sessions (all three PASS).
## 2026-09-08 — D-2075 uhitm.c mhitm_ad_stun mhitu arm never ported: second consecutive Pestilence touch fell into mhitm_adtyping_u default, so JS drew knockback rn2(3) where C drew the rn2(4) stun roll (queue owner mhitm_ad_stun)

**C locus:** `uhitm.c:4388–4422` `mhitm_ad_stun` — mhitu arm `:4403–4409` (`hitmsg(magr, mattk)` unconditional; `if (!magr->mcan && !rn2(4))` → `make_stunned((HStun & TIMEOUT) + mhm->damage, TRUE)` + `mhm->damage /= 2`; no phys call, no `done` check — unlike both sibling arms); dispatched by `mhitm_adtyping :4787` (`case AD_STUN`), reached from `hitmu` (`mhitu.c:1191` `mhitm_adtyping(mtmp, mattk, &gy.youmonst, &mhm)`); the AD_STUN itself comes from `getmattk` (`mhitu.c:337–347` — second consecutive same-type DISE/PEST/FAMN hit swaps to AD_STUN, aatyp untouched), already live in JS `get_mattk` (`mhitm.js:393–400`). `hitmu` order `d() → adtyping → knockback` (`mhitu.c:1187–1193`) was already C-faithful in JS, so this is a missing arm, not an order bug (same shape as D-2059 pest / D-2074 samu, which moved these exact sessions here).
**JS:** 2 files (mhitu.js +24/−2, mhitm.js +2/−2) — under the 600/10 caps. Small because the C mhitu arm is 7 lines and every callee was already live/imported; density exception applies (corpus-driven Open row with a 2-session recorded expectation, C that small — D-2071/D-2074 precedent).
**Change:** `js/mhitu.js` — new `mhitm_ad_stun_u` (`hitmsg` always; `!(mtmp.mcan|0) && !rn2(4)` → `make_stunned(((game.u?.HStun|0) & TIMEOUT) + (mhm.damage|0), true)` (the :3264 gaze-arm idiom) + `mhm.damage = Math.trunc((mhm.damage|0) / 2)` (C `/= 2` truncation); no `void mhm` — damage is conditionally written, unlike the keep-arms); `case AD_STUN` in `mhitm_adtyping_u` (+ doc-list `STUN`); no new imports (`hitmsg`, `make_stunned`, `rn2`, `TIMEOUT` all already live in-file — no new module edge, no TDZ). `js/mhitm.js` — `mhitm_ad_stun` doc «Named omit» line retires the mhitu arm (now points at `mhitm_ad_stun_u`); uhitm arm stays named.
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_stun` → PASS syntax (2 changed files: js/mhitm.js js/mhitu.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-genesis-Archeologist-91127 PASS; scen-genesis-Barbarian-91118 92→`disclose`@110, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** `mhitm_ad_stun` uhitm arm (`:4394–4402` — hero-as-attacker `!Blind` stagger pline + phys, no mcan gate — no blocked session reaches it); mhitm arm is D-1396 live. No blocked session reaches the `!rn2(4)` stun body itself (both sessions drew nonzero: 2 and 1), so `make_stunned`'s talk/botl path there is exercised only by its existing callers (gaze arm :3264).
**Next:** pop the next Open row (`wield.c` can_twoweapon). Do not re-pop `mhitm_ad_stun` for these sessions (Archeologist PASS; Barbarian→disclose@110 flows through the normal queue on rescore).
## 2026-09-08 — D-2074 uhitm.c mhitm_ad_samu never ported: Wizard AD_SAMU fell into mhitm_adtyping_u default, so JS drew knockback rn2(3) where C drew the rn2(20) steal roll (queue owner mhitm_ad_samu)

**C locus:** `uhitm.c:4570–4589` `mhitm_ad_samu` — uhitm arm `:4573–4576` (`mhm->damage = 0`); mhitu arm `:4577–4586` (`hitmsg(magr, mattk)`; `if (!rn2(20)) stealamulet(magr)` — 1/20 quest-artifact/Amulet/invocation-tool snatch when the Wizard or a quest nemesis hits; damage untouched so the base `hitmu` `d()` stands); mhitm arm `:4587–4588` (`mhm->damage = 0`); dispatched by `mhitm_adtyping :4823` (`case AD_SAMU`); `AD_SAMU = 252` (`monattk.h`). `hitmu` order `d() → adtyping → knockback` (`mhitu.c:1191–1193`) and `mdamagem` order `adtyping → knockback` (`mhitm.c:1059–1065`) were already C-faithful in JS, so this is a missing arm, not an order bug (same shape as D-2071 corr).
**JS:** 3 files (mhitm.js +35/−1, mhitu.js +24/−3, uhitm.js +7/−2) — under the 600/10 caps. Small because the C locus is 17 lines and every callee (`hitmsg`, `stealamulet`, `rn2`, `mhitm_knockback`) was already live; density exception applies (corpus-driven Open row with a 2-session recorded expectation, C that small).
**Change:** `js/mhitm.js` — file-local `const AD_SAMU = 252` (monattk.h cite, file idiom) + export-list row; sync `mhitm_ad_samu` (mhitm arm: zero damage, no message); `mdamagem` `AD_SAMU` case (ad func → `mhitm_knockback` → `return mhm.hitflags`, the AD_PHYS zero-damage precedent — knockback `rn2(3)` still burns exactly where C burns it). `js/mhitu.js` — `AD_SAMU` joins the existing `./mhitm.js` import, `stealamulet` joins the existing `./steal.js` import (steal.js imports neither mhitu nor mhitm — no new cycle; runtime call, no TDZ); new `mhitm_ad_samu_u` (`hitmsg` + `if (!rn2(20)) await stealamulet(mtmp)`, `void mhm` leftover-kept idiom); `case AD_SAMU` in `mhitm_adtyping_u` (+ doc-list row). `js/uhitm.js` — `AD_SAMU` joins the existing `./mhitm.js` import; `damageum_adtyping` `else if (adtyp === AD_SAMU) mhm.damage = 0` (+ doc-list row).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_samu` → PASS syntax (3 changed files: js/mhitm.js js/mhitu.js js/uhitm.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-tour-Samurai-91113 PASS; scen-tour-Wizard-92103 99→`dochug`@100, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** none new — all three `mhitm_ad_samu` arms are live. `stealamulet`'s own singleton omission (D-1945, map `turns.md` row) is pre-existing and untouched; the `mhitm_ad_samu caller wiring` named bit of that row is now live (map updated).
**Next:** pop the next Open row (`uhitm.c` mhitm_ad_stun). Do not re-pop `mhitm_ad_samu` for these sessions (Samurai PASS; Wizard→dochug@100 flows through the normal queue on rescore).
## 2026-09-08 — D-2073 engrave.c doengrave add-to ynq prompt never asked: same-type engraving auto-appended where C prompts, so JS skipped the recorded [ynq] screen (queue owner doengrave)

**C locus:** `engrave.c:1113–1167` — `de->type == HEADSTONE → c = 'y'` (no choice, only append); `de->type == de->oep->engr_type && (!Blind || BURN || ENGRAVE)` → `c = yn_function("Do you want to add to the current engraving?", ynqchars, 'y', TRUE)` (`decl.c:114` `ynqchars = "ynq"`), `c == 'q'` → `pline1(Never_mind)` + `goto doengr_exit`; `c == 'n' || Blind` wipe/overwrite arms unchanged; `else if Strlen(oep->engr_txt[actual_text]) >= BUFSZ-1` → `There("is no room to add anything else here.")` + `ret = ECMD_TIME` + exit (`:1162–1167`); exit `:1257–1263` newsyms iff `disprefresh`.
**JS:** 1 file (engrave.js +21/−6) — under the 600/10 caps. Small because the C locus is two short arms and every callee (`yn_function`, `pline`, `newsym`, `BUFSZ`, `Never_mind`, `ECMD_TIME`) was already live/imported; density exception applies (corpus-driven Open row with a 2-session recorded expectation).
**Change:** `js/engrave.js` — same `if (de.oep)` envelope now ports C branch order: HEADSTONE → `c = 'y'`; same-type → `c = await yn_function('Do you want to add to the current engraving?', 'ynq', 'y')` (default-true matches C TRUE; `yn_function` joins the existing `./getline.js` import — same module as `getlin`, no new edge, no TDZ) with `q` → `pline(Never_mind)` + disprefresh newsym + `return de.ret` (C `goto doengr_exit` preserves initial ret); added the `else if` BUFSZ-full arm via `String(de.oep.engr_txt?.actual_text ?? '').length >= BUFSZ - 1` → `pline('There is no room to add anything else here.')` + newsym + `return ECMD_TIME`; header/doc `yn add-to` omissions retired.
**Verify:** `node scripts/verify.mjs --fn doengrave` → PASS syntax (1 changed file: js/engrave.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-kit-Valkyrie-91116 PASS; scen-normal-Samurai-92078 44→`can_twoweapon`@118, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff.
**Named:** `doengrave` altar/jello/swallow/lava/pool setup arms, `livelog` literate log, Blind-feel engrave/burn path, full `surface`/`is_ice`, wipeout seeded path, `disturb_grave`-from-doengrave beyond kick callers, `set_levltyp` beyond GRAVE typ (all pre-existing map notes in `turns.md`, untouched — no blocked session reaches them).
**Next:** pop the next Open row (`uhitm.c` mhitm_ad_samu). Do not re-pop `doengrave` for these sessions (Valkyrie PASS; Samurai→can_twoweapon@118 flows through the normal queue on rescore — that row is already the next Open item).
## 2026-09-08 — D-2072 muse.c use_misc MUSE_POT_GAIN_LEVEL omitted trycall + grow_up stub never grew: pit-fiend uneasy never prompted Call, Grey-elf never became an elf-lady (queue owner use_misc)

**C locus:** `muse.c:2402` (`mquaffmsg` at `:292–302` uses `pline_mon`, not `pline`) + gain-level arm: rise `:2414–2416` (`pline_mon` + `ceiling(mx,my)` + `trycall`), skipmsg `:2424–2426` (`pline_mon` + `trycall`), uncursed `:2432–2437` (`pline_mon`, `makeknown`, `m_useup`, `grow_up(mtmp,NULL)`); `do.c:395–400` `trycall` (unknown → `docall` prompt); `makemon.c:2051–2178` `grow_up` (potion envelope `rnd(8)`, `little_to_big` form change with `pline_mon` «becomes», geno-death). Mechanism: C's third message (the `docall` prompt pline / the grow_up «becomes» pline) overflows the combined topline so `update_topl` (`wintty.c topl.c:262–279`) calls `more()` — that pending more is the recorded `--More--`; after dismissal the prompt/growth line paints. JS printed only the first two messages and stopped, so no more was ever pending.
**JS:** 1 file (muse.js +16/−14) — under the 600/10 caps. Small because the C arm is 40 lines and both callees were already live; density exception applies (Must-fix-equivalent corpus row, two arms one locus).
**Change:** `js/muse.js` — `mquaffmsg` vismon arm → `pline_mon` (C :297); rise arm → `pline_mon` + `ceiling(mtmp.mx,mtmp.my)` + `await trycall(otmp)`; skipmsg → `pline_mon` + `await trycall(otmp)`; uncursed → `pline_mon` + `if (!(await grow_up(mtmp, null))) return 1; return 2;` using the live `mhitm.js` port (D-1246, branch/RNG order already C-faithful incl. the `pline_mon` «becomes» message); `grow_up` joins the existing `./mhitm.js` import (same statement, no new module edge — runtime call, no TDZ); `trycall`/`ceiling`/`pline_mon` were already imported; `grow_up_potion` stub deleted (sole caller was this arm).
**Verify:** `node scripts/verify.mjs --fn use_misc` → PASS syntax (1 changed file: js/muse.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (92125 PASS incl. step-78 Call prompt; 92173 77→`obj_resists`@230, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Throwaway /tmp replay driver confirmed byte-identical row-0 at steps 77–78 on both sessions before the verify run (probe outside the repo, nothing committed). Tree was clean at preflight (`git status --short` empty); preflight full `verify --no-cohort` was skipped in favor of the in-loop `verify --fn` green+strict gates on the untouched tree state (post-fix green/strict/cohort all PASS).
**Named:** `Can_rise_up` Is_wiz1_level/In_W_tower/entry-lev special-stair (local clone `muse.js:2118` keeps its map note; both sessions take non-rise arms); `m_useup` local unlink loop vs the `mthrowu.js` export weight/extrinsics path (pre-existing drift noted in the export doc; untouched — 12 call sites); `mquaffmsg` unsee arm keeps the JS deaf disjunction + omits `Soundeffect` (no blocked session reaches it); `mon_set_minvis` worm/newsym polish (invis arm, untouched).
**Next:** pop the next Open row (`engrave.c` doengrave). 92173's new `obj_resists`@230 owner flows through the normal queue on rescore (parked symptom-owner row already covers that family; do not re-pop `use_misc` for either session).
## 2026-09-08 — D-2071 uhitm.c mhitm_ad_corr mhitu arm never ported: black-pudding AD_CORR fell into mhitm_adtyping_u default, so JS drew knockback rn2(3) where C drew erode_armor rn2(5) (queue owner erode_armor)

**C locus:** `uhitm.c:2338–2360` `mhitm_ad_corr`, mhitu arm `:2346–2351` (`hitmsg`; `magr->mcan → return`; `erode_armor(mdef, ERODE_CORRODE)` — mdef is `&gy.youmonst`, damage untouched so the base `hitmu` `d()` stands); dispatched by `mhitm_adtyping :4806` (`case AD_CORR`); `AD_CORR = 42` (`monattk.h:84` "corrode armor (black pudding)"); `ERODE_CORRODE = 3` (`obj.h:458`). `hitmu` order `d() → adtyping → knockback` was already C-faithful in JS (review 853), so this is a missing arm, not an order bug.
**JS:** 1 file (mhitu.js +23/−3) — under the 600/10 caps. Small because the C mhitu arm is 6 lines (D-2067 density precedent). Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes.
**Change:** `js/mhitu.js` — file-local `const AD_CORR = 42` (the file's local-AD_* idiom, cf `AD_RUST`); `ERODE_CORRODE` joins the existing `./const.js` import (no new module edge); new `mhitm_ad_corr_u` mirroring `mhitm_ad_rust_u` arm-for-arm (`hitmsg`, `mcan` return, `erode_armor(game.youmonst, ERODE_CORRODE)`, `void mhm`); `case AD_CORR` in `mhitm_adtyping_u` (+ doc-list `CORR`); rust-arm doc "CORR/DCAY … deferred" narrowed to DCAY.
**Verify:** `node scripts/verify.mjs --fn erode_armor` → PASS syntax (1 changed file: js/mhitu.js) · PASS rule2 · PASS hidden `0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Knight-92185 74→`collect_coords`@77, strictly later owner — C «It divides as you hit it! Your long sword corrodes!» vs JS «You hit it.» is the pudding-split/passive-corrode writer, a new family; Tourist-92044 re-attributed at step 106 to screen-first `do_statusline2` with matching toplines and RNG 3544/3544 — the erode divergence is fully gone) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** `mhitm_ad_corr` uhitm arm (`:2342–2345` — hero poly'd into a CORR attacker) and mhitm arm (`:2352–2359` — `mstrategy &= ~STRAT_WAITFORU` + damage zero); whole `mhitm_ad_dcay` (`:2363–2402`, brown pudding / wood-/leather-golem rot — no blocked session reaches it); passive `!rn2(30)` corroder wiring (pre-existing defer, cf mhitm.js:1806 rustm-side note).
**Next:** pop the next Open row (`muse.c` use_misc). Do not re-pop `uhitm.c` erode_armor for these sessions (Knight→collect_coords@77, Tourist→do_statusline2@106 flow through the normal queue on rescore).
## 2026-09-08 — Audit D-2064…D-2070 against pinned C (reviews 1034–1040) + cadence score 44/44

7 JS-touching SHAs since 84442f3a, each re-verified with
`hidden-proxy verify --base HASH~1` (all D-log claims reproduce
exactly; later-SHA moves are strictly forward, no WORSE). 7 ACCEPT, 0
Must-fix. Fortress 44/44 (Scr 11405/11405, RNG 792838/792838, speed
`60+0.39/turn` R² 0.80). Rule #2 clean. Queue holds 8 Open, no refill.
Next: pop `uhitm.c` erode_armor.
## 2026-09-08 — D-2070 queue owner xname_flags:864 is a literal-match bucket: the yawn is timeout.c sleep_dialogue with an HSleepy split-brain, the laugh is You_hear's dead u.Deaf gate (queue owner xname_flags, writers sleep_dialogue + You_hear)

**C locus:** `timeout.c:267–274` `sleep_dialogue` (`i = HSleepy & TIMEOUT; i == 4 → You("yawn.")`) called under `if (HSleepy & TIMEOUT)` at `:639–640` before the uprops `--` loop (pre-decrement read); `youprop.h:141` `HSleepy ≡ uprops[SLEEPY].intrinsic` (single storage); `wizcmds.c:945` `DEFAULT_TIMEOUT_INCR 30` + wiz_intrinsic default incr arm; `pline.c:436–452` `You_hear` (`(Deaf && !Unaware) || !flags.acoustics → return`) with `youprop.h:123–125` `Deaf = HDeaf || EDeaf || uroleplay.deaf`.
**JS:** 3 files (hack.js +8/−2, timeout.js +24/−3, wizcmds.js +5/−1) — under the 600/10 caps. Small because each C locus is tiny (8-line dialogue, 1-line gate, 2 table rows); density exception applies, and the verify-triage rule required fixing both causes in one round rather than one session per round. Rule #2 clean; no DIAG/FORCE/seed gates; probes lived in /tmp only.
**Change:** `js/wizcmds.js` — `PROP_FLAT += [SLEEPY]: 'HSleepy'` (youprop.h:141 cite); `js/timeout.js` — `TIMEOUT_FLAT += [SLEEPY]: 'HSleepy'` so the generic `--` keeps the flat synced, file-local `sleep_dialogue()` plus the `:639–640` call site after `phaze_dialogue` (region_dialogue stays deferred), header omissions updated; `js/hack.js` — `You_hear` uses the full disjunction (`HDeaf||EDeaf||uroleplay.deaf||u.Deaf`, the dbridge/do/detect D-1967 idiom). No new imports or module edges (SLEEPY/TIMEOUT already imported; verified `--can` not needed — zero new edges).
**Verify:** `node scripts/verify.mjs --fn xname_flags` → PASS syntax (3 changed files) · PASS rule2 · PASS hidden `0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Samurai-92017 38→`float_down`@43 — yawn prints, levitation expiry is the later owner; Priest-92035 179→`do_statusline2`@209 — laugh suppressed, read+prompt match) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2068 precedent).
**Named:** `region_dialogue` still deferred; SLEEPY-expiry `fall_asleep`+incr (`timeout.c` case SLEEPY — JS generic arm silently clears; Samurai will re-block there); Underwater «barely hear» + `You_feel`/`You_see` dream arms (D-2065 pre-existing); remaining `PROP_FLAT`/`TIMEOUT_FLAT` default-arm gaps for other props (no blocked session reaches them); file-local `You_hear` clones elsewhere (D-2065 list, untouched).
**Next:** pop the next Open row (`uhitm.c` erode_armor). Do not re-pop `objnam.c` xname_flags for these sessions (Samurai→float_down@43, Priest→do_statusline2@209 flow through the normal queue on rescore).
## 2026-09-08 — D-2069 polyself.c dohide never ported AND domonability `let c = '\0'` is truthy in JS, so #monster as a hider always fell to "purely reflexive" (queue owner m_move, writer dohide)

**C locus:** `polyself.c:1777–1874` `dohide` (ustuck/utrap refuse + reveal; eel-out-of-water; hides_under 'trice-pile petrify; no-ceiling / air-water floor refuses; already-hiding; mimic m_ap_type vs uundetected=1 + newsym + youhiding, ECMD_OK/ECMD_TIME); `insight.c:2022–2077` `youhiding` (mimic U_AP_TYPE detail / eel-in-pool / hides_under pile / ceiling / pit-floor trapper / surface; FALSE topline vs TRUE enlightenment `you_are`); `cmd.c:889–949` `domonability` (`char c = '\0'` is integer 0/falsy, so `c ? … : might_hide` takes the predicate arm); `youprop.h:253–255` `Flying`.
**JS:** 1 file (polyself.js +203/−8) — under the 600/10 caps; ~97-line C dohide + 57-line youhiding is the envelope, not padding. Rule #2 clean; temp arm-trace plines deleted before verify (grep-clean); no DIAG/FORCE/seed gates; frozen contracts untouched.
**Change:** `js/polyself.js` — exported async `dohide()` (full C branch order incl. nested You_cant reason ternary; You_cant/There/pline_The composed via `pline` per the zap.js `You` idiom; floor pile via `objects_at` nexthere + `mons(corpsenm)`/`touch_petrifies`; Stone_resistance H||E||flat; `has_ceiling`/`Is_airlevel`/`Is_waterlevel` on `u.uz`; `M_AP_TYPE(you)` for U_AP_TYPE; CORPSE/STRANGE_OBJECT via `objectNames.indexOf`) + exported async `youhiding(via_enlghtmt, msgflag)` (full C buf builder; FALSE arm plines `You are already/now ${buf}.`) + file-local `Flying()` (eat.js idiom) and `plur()` (end.js idiom); hide arm wired to `return dohide()`; `let c = 0` with a C-falsy comment (`yn_function` still yields one-char strings for the q/ESC/h/s compares). Imports: `sticks` (engrave.js), `ceiling`/`t_at`/`instapetrify` (trap.js), `has_ceiling` (dungeon.js) — all new edges hoisted-function SAFE per `imports.mjs --can`; everything else extends pre-existing edges.
**Verify:** `node scripts/verify.mjs --fn m_move` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden (Wizard-92076 step 94 → `rehumanize` at step 100, strictly later owner; Caveman-92202 @103 unchanged — parked cnt/mtrack writer per the `monmove.c` m_move Parked row) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green on a clean tree. Step-94 topline confirmed byte-identical («You are now hiding on the ceiling.») via a throwaway /tmp replay driver (outside the repo).
**Named:** `youhiding` TRUE (`you_are` enlightenment menu-line) arm — JS enlightenment (invent.js) never calls youhiding and the menu channel has no polyself-side endpoint; `dogaze`/`dospinweb` still deferred (queued); steed `pet_ranged_attk` still deferred; dohide furniture TODO kept as C's own comment.
**Next:** Caveman-92202 stays under the parked m_move cnt/mtrack writer (needs a C per-turn cnt/mtrack dump; do not re-pop m_move). Wizard-92076's new `rehumanize`@100 owner flows through the normal queue on rescore. Do not reintroduce `'\0'`-as-falsy anywhere a C `char` gates a branch.
## 2026-09-08 — D-2068 bones.c savebones skipped remove_mon_from_bones, so Medusa's 2× obj_resists draws never ran before the LEAVESTATUE statue

**C locus:** `bones.c:388–399` `remove_mon_from_bones` (iswiz / Medusa / MS_NEMESIS / MS_LEADER / `is_Vlad` (`monst.h:222` data-or-cham Vlad) / Oracle+`fixuporacle`) → `mongone` (`mon.c:3266–3283` unstuck + `mdrop_special_objs` + discard); `steal.c:849–871` `mdrop_special_objs` calls `obj_resists(obj,0,0)` per item; `zap.c:1457–1473` `obj_resists` returns TRUE for Amulet/invocation/Rider else `rn2(100) < chance` (2 ordinary Medusa items → 2 draws); `savebones :444–445` `iter_mons(remove_mon_from_bones)` + `dmonsfree()` run before the `:480–489` LEAVESTATUE `mk_named_object` statue (`mksobj` `next_ident`+`rndmonnum`/`rndmonst_adj`+`rn2(2)`); `fixuporacle :307–363` (Oracle-level gate + DELPHI room fixup via `enexto`+`rloc_to`); `iter_mons` (`mon.c:4526–4538` skips DEADMONSTER/mon_offmap).
**JS:** 1 file (end.js +76/−3) — under the 600/10 caps. C locus is ~60 lines (12-line remover + 50-line oracle fixup + 2-line iter call); 76 insertions is the faithful envelope, not padding. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes.
**Change:** `js/end.js` — file-local `fixuporacle` (Oracle-level gate, `mpeaceful=1`, DELPHI `roomno-ROOMOFFSET` keep, else centre `enexto`+`await rloc_to` and restore `rtype`, C `:307–363`) + file-local `remove_mon_from_bones` (exact C predicate incl. `is_Vlad` data-or-cham, C `:388–399`); `savebones` runs `for (...[...game.fmon]) { DEADMONSTER/mon_offmap skip; await remove_mon_from_bones }` + `dmonsfree()` before fid-negate (snapshot like C `mtmp2=mtmp->nmon` since `mongone` splices). Imports extend pre-existing edges only: `mongone,dmonsfree` from `./mon.js`, `mon_offmap` from `./monmove.js`, `DELPHI,ROOMOFFSET,Is_oracle_level` from `./const.js`, `enexto,rloc_to` from `./teleport.js` (both hoisted-function, cycle-safe per `imports.mjs --can`); `PM_MEDUSA/PM_ORACLE/PM_VLAD` via `monsterNames.indexOf`, `MS_LEADER 36/MS_NEMESIS 37` local (makemon.js:668-669).
**Verify:** `node scripts/hidden-proxy.mjs verify obj_resists` → `1 PASS, 0 moved past, 2 unchanged, 0 worse → PROGRESS` (scen-genesis-Valkyrie-92074 PASS; scen-tour-Samurai-92032 @59 + scen-tour-Wizard-92219 @115 unchanged — parked S2 fire-trap / S3 cube-paradox writers per the `zap.c obj_resists` Parked row, not this envelope). `node scripts/verify.mjs --fn savebones` → `PASS syntax 1 changed js file(s): js/end.js` · `PASS rule2` · `note hidden verify savebones: no corpus session is blocked on it at HEAD — vacuous, not a corpus PASS` (owner is still `obj_resists` until rescore) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** `unleash_all/unpunish/dismount_steed`, `forget_engravings`, `set_ghostly_objlist`/`resetobjs` known-strip, map-memory clear + `ux/uy` zero (frpx/frpy kept), undead-arise mtmp arm, `ebones`, `obj_attach_mid`, file compress + binary `savelev` (all pre-existing `savebones` defers, untouched); `dmonsfree` `impossible` purge-count check (JS `mon.js:2778` pre-existing); `mongone` `isgd/grddead` + `m_detach` wizdead/shk/worm arms (JS `mon.js:2876` pre-existing, unreached by Medusa).
**Next:** pop the next Open row (`monmove.c` m_move — 2 sessions, probe `verify m_move`). Do not re-pop `zap.c obj_resists` for Valkyrie-92074 (now PASS); S2/S3 stay parked under their writers.
