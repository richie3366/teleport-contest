# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-09 — D-2196 `sounds.c` dosounds deferred room/guard arms: swamp You1, barracks/court You_hear, temple_priest, oracle canseemon, beehive/morgue bodies (map-driven, vacuous corpus verify)

**C locus:** `sounds.c:201–339` `dosounds` (swamp `You1(swamp_msg[rn2(2)+hallu])` + return `:234–243`; barracks `You_hear1(barracks_msg[rn2(3)+hallu])` on mercenary msleeping/count>5 `:277–299`); `throne_mon_sound :33–62` (which=rn2(3)+hallu: 0/1 You_hear1, 2 pline off-with-`uhis()`); `beehive_mon_sound :65–91` (rn2(2)+hallu switch incl. uarmh bonnet); `morgue_mon_sound :94–119` (undead||vampshifter, `body_part(HAIR)` + NECK/HEAD + `vtense` arms); `temple_priest_sound :137–185` (ispriest+inhistemple+!helpless+outside-temple gate, do-loop rn2(3+hallu) with speechless/in_sight retries, flag-strip, halu_gname %s arms); `oracle_sound :188–208` (always TRUE; print gated on Hallu||!canseemon, ora_msg[rn2(3)+hallu*2]); `hack.h:1028` You1≡You("%s"); `monst.h:251` helpless≡msleeping||!mcanmove.
**JS:** 1 file (`js/sounds.js` +~130/−~25), under the 600/10 caps.
**Change:** port all seven arms in C order with identical draw sequences (same rn2 calls, same gates). Swamp → `pline('You '+msg)` (You1 idiom); barracks/throne/beehive/oracle → live `You_hear`; throne-2 → `pline(... uhis() ...)` (`uhis` from roles.js); morgue → `pline` You/pline_The idioms with live `body_part`+`vtense` + `HAIR/NECK/HEAD` consts; temple → live `inhistemple`/`temple_occupied` (priest.js, existing edge), `EPRI` (const.js, existing edge), same-file `helpless` (matches monst.h macro), `cansee` (vision.js; imports.mjs hoisted-safe), `align_gname` (roles.js; imports.mjs no-cycle SAFE) for the %s arms, raw `data.msound` read for speechless (C reads `mtmp->data->msound` directly); oracle print gated on `Hallucination() || !canseemon(mtmp)` (both live imports), removing the visible-oracle over-burn. New static edges: roles.js (uhis/align_gname, SAFE), vision.js (cansee, hoisted-safe); priest/monsters/const/display/objnam/polyself edges already existed.
**Verify:** `node scripts/verify.mjs --fn dosounds` → PASS syntax · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — map-driven row, ships on the public gates; row cited no N blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7; then `node scripts/verify.mjs --full` → PASS full 44/44. Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** Soundeffect on throne/beehive arms (no audio backend); `Is_sanctum` in the temple gate (pre-existing); `findgd` migrating_mons (pre-existing); temple Hallu pantheon RNG (pray.js `halu_gname` defers it too — same debt).
**Next:** do not re-pop `dosounds` (0 blocked). No refill owed (queue stays ≥ 8 Open after archive; singletons still Deferred while scen families < 90 %).
## 2026-09-09 — D-2195 `trap.c:6711–6773` thitm stone_missile/passes_rocks harmless arm + pline_mon/dealloc tail (map-driven, vacuous corpus verify)

**C locus:** `trap.c:6711–6773` `thitm` (strike `find_mac+tlev+spe <= rnd(20)`; miss `pline_mon` "almost hit"; `harmless = obj && stone_missile(obj) && passes_rocks(mon->data)`; hit `pline_mon` "is hit by %s%s" + `" but is not harmed."`/`"!"`; dam `d_override` else `dmgval` clamp≥1 computed before the harmless gate; `!harmless` → damage + `monkilled(mon, "", nocorpse ? -AD_RBRE : AD_PHYS)` + `DEADMONSTER` (`monst.h:214` `mhp < 1`) → `newsym`; harmless → `strike = 0`; tail `place_object+stackobj` iff `!strike || d_override` else `dealloc_obj`); `obj.h:274` `stone_missile` (mat GEMSTONE/MINERAL, oclass ≠ RING_CLASS); `mondata.h:208` `passes_rocks` (passes_walls && !unsolid); `pline.c:138` `pline_mon` (set_msg_xy + pline); `monattk.h:89` AD_RBRE=242. Live path: monster-branch `trapeffect_rocktrap` `:1393` `thitm(0, mtmp, otmp, d(2,6))` — ROCK (MINERAL) vs a rock-passer (xorn) struck and wrongly damaged before.
**JS:** 2 files (`js/trap.js` +~25/−~20, `js/dothrow.js` +1/−1), under the 600/10 caps. Small because the envelope is live (strike, dmgval clamp, monkilled ordinary path, place/stackobj all D-shipped); density exception applies (remaining unported C delta of this 64-line function).
**Change:** harmless arm in C order — `const harmless = !!(obj && stone_missile(obj) && passes_rocks(mon.data))` (same expression shape as live `js/dothrow.js hittmu`), suffix ternary, dam computed before the gate (preserves `dmgval` RNG on harmless hits), `if (!harmless)` damage/death envelope else `strike = 0`, C tail with live `dealloc_obj`. `stone_missile` exported from `js/dothrow.js` (its existing canonical home; was file-local) and imported in trap.js — no second clone (imports.mjs: already IN-SCC, no new edge); `passes_rocks` is the same-file `js/trap.js:3249` clone, called directly — no third clone. Miss/hit arms now `await pline_mon` (already imported; byte-identical to C `pline.c:138`).
**Verify:** `node scripts/verify.mjs --fn thitm` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — map-driven row, ships on the public gates; row cited no N blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (forced `--full`). Throwaway /tmp probe (deleted; no session reaches the arm): stone_missile ROCK true / ARROW false / DIAMOND-as-ring false / null false, passes_rocks xorn true / jackal false, combined harmless rock-vs-xorn true with both negatives false — all 9 C-predicted. Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** none new. `thitm` is now complete vs C (local `monkilled` clone kept as-is per D-0150 — switching to canonical `mhitm.js monkilled` would change death-message behavior beyond this row; pre-existing `passes_rocks`/`stone_missile` clones elsewhere in `js/` left untouched).
**Next:** do not re-pop `thitm` (0 blocked). No refill owed (queue stays at 8 Open after archive; singletons still Deferred while scen families < 90 %).
## 2026-09-09 — D-2194 `trap.c` m_harmless_trap residual arms: ANTI_MAGIC + defended, sqky cringe gate, rocktrap empty-door pline (map-driven, vacuous corpus verify)

**C locus:** `trap.c:1106–1187` `m_harmless_trap` (SLP_GAS `resists_sleep \|\| defended(AD_SLEE)` :1133–1136; FIRE `resists_fire \|\| defended(AD_FIRE)` :1141–1144; ANTI_MAGIC `resists_magm \|\| defended(AD_MAGM)` :1173–1176); `mondata.c:89–124` `defended` (wielded-artifact `defends`, adult-dragon own-scales suit, else worn W_ARM, dragon-armor `defends`); `mondata.c:214–244` `resists_magm` (species AD_MAGM/baby-gray/AD_RBRE + wielded-weapon artifact + worn-or-carried ANTIMAGIC/carry-artifact scan with the W_ARMOR\|W_ACCESSORY(+W_WEP)(+W_SWAPWEP) slotmask); `trap.c:1445–1457` sqky monster in-sight arms (`!Deaf` squeak pline else `!mindless` cringe pline :1453); `trap.c:1380–1388` rocktrap monster empty arm (`in_sight && see_it` → `pline_mon` "A trap door above %s opens, but nothing falls out!" + deltrap + newsym + Trap_Is_Gone).
**JS:** 3 files (`js/mondata.js` +89/−2, `js/trap.js` +28/−14, `js/artifact.js` +1/−1), under the 600/10 caps.
**Change:** canonical `defended` + full `resists_magm` (+ file-local `monsndx` = `(mndx ?? mnum ?? NON_PM)\|0`, the D-1933 monsndx-equivalent idiom) exported from `js/mondata.js` (their C home), wired into the three `m_harmless_trap` arms with `AD_SLEE`/`AD_MAGM` consts (`AD_FIRE` already local); `Is_dragon_armor` exported from `js/artifact.js` (was file-local, used by live `defends`); new edges `mondata.js ← artifact.js/worn.js/weapon.js/wield.js` all imports.mjs IN-SCC/hoisted-safe, function-scope use only; trap.js reuses its existing mondata edge. Sqky: `else` → `else if (!mindless(mtmp.data))`.
**Verify:** `node scripts/verify.mjs --fn m_harmless_trap` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — map-driven row, ships on the public gates; row cited no N blocks so no --base re-run owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (forced `--full`: 3 shared modules changed). Throwaway /tmp probe (deleted, no corpus session reaches these arms): gray ANTI_MAGIC→true, red FIRE→true, defended gray/red-vs-AD_MAGM/AD_FIRE true, cross/mismatch/human negatives false — all C-predicted. Final verify ran after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** `m_harmless_trap` default keeps returning FALSE (C `impossible()` on unknown ttyp stays named, D-1868-review precedent); sqky hero Levitation/Flying arm (hero dotrap deferred); sqky Soundeffect (no-op); rocktrap vault/shop ceiling labels, helm "hat", Yname2 verbose, `stone_missile`/`passes_rocks` thitm arm, full body_part table; `sleep_monst` `defended(AD_SLEE)` wiring (mhitm.c, its own future row — behavior-changing, not snuck in); pre-existing species-only `resists_magm` clones (explode.js/mhitm.js) + zap.js stub (drift, untouched); all other `defended` call sites (turns.md zhitm/sleep/touch_artifact/mhitm lines) stay named.
**Next:** do not re-pop `m_harmless_trap` (0 blocked). No refill owed (queue stays ≥ 8 Open after archive; singletons still Deferred while scen families < 90 %).
## 2026-09-09 — D-2193 `mhitu.c` expels tail: um_dist land-hard + spoteffects(TRUE) (queue owner look_here, 1 session moved past)

**C locus:** `mhitu.c:263–306` `expels`, reached from `mhitu.c:1578–1584` `gulpmu` (`You("get %s!", … : "expelled")` + `expels(mtmp, mtmp->data, FALSE)`). The tail is `unstuck(mtmp)` (step-121 C dice `rnd(2) @ unstuck(mon.c:3465)`) → `mnexto(mtmp, RLOC_NOMSG)` (step-121 C dice `collect_coords` shuffle) → `newsym` → `um_dist` land-hard pline (`:302–304`) → `spoteffects(TRUE)` (`:305`) → `pickup(1)` (`hack.c:3380–3381`) → `check_here` → `look_here(ct, lhflags)` (`pickup.c:452`), whose Blind arm prints the feel message (`invent.c:4207`). The queued row's own function `look_here` is already arm-for-arm (D-1835/D-1599/D-0399/D-0460) and `js/pickup.js spoteffects` already calls `pickup(1)` — a re-port of either would be NO MOVEMENT; the writer is the expels tail.
**JS:** 2 files (`js/mhitu.js` +14/−2, `js/mon.js` +1/−1), under the 600/10 caps. Small because the envelope is live (expels message arms, unstuck, mnexto, newsym, spoteffects, pickup, look_here all D-shipped); density exception applies (unported C delta is 7 lines, corpus-recorded expectation).
**Change:** port the C tail in order — `um_dist(mtmp.mx, mtmp.my, 1)` → `pline('Brrooaa... You land hard at some distance.')`, then `await spoteffects(true)` via dynamic `import('./pickup.js')` (pickup.js statically imports mhitu.js for mdamageu, so static would close the cycle; mhitu.js house idiom is dynamic here — invent/eat/spell precedents). `um_dist` is the canonical `js/mon.js` clone (verbatim C `apply.c:692–695` Chebyshev) now exported and added to mhitu.js's existing mon.js import — no new clone, no new static edge.
**Verify:** `node scripts/verify.mjs --fn look_here` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden (PROGRESS: Wizard-92223 moved 121 → 130; steps 121–129 now byte-match, RNG 4392/4392) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. Full `sessions` 44/44 PASS (run directly; speed `61+0.38/turn`). Direct single-session replay: RNG 4392/4392, screens 130/131, error null — the "js-throw" in the verify line is the null-owner fallback label (`hidden-proxy.mjs:388`), not an exception (same note as D-2189).
**Named:** none new. `expels` is now complete (message arms, unstuck, mnexto, newsym, land-hard, spoteffects).
**Next:** do not re-pop `look_here` (0 blocked). Residual Wizard-92223 step-130 screen diff (C topline empty vs JS «What are you looking for? The exit?» from `dosearch0` uswallow arm `detect.c:2022–2024`): C hero free by step 130 (swallowed at 125 «It engulfs you!», searches swallowed at 127, free at 129–130), JS hero still swallowed — a swallow-release-state writer (gulpmu tail / uswldtim / engulfer-death path), not look_here. Let the rescore queue it under its true writer.
## 2026-09-09 — D-2192 `do_wear.c` Helmet_on helm switch (dunce-cap glow/curse + sitting; queue owner accessory_or_armor_on, 1 session PASS)

**C locus:** `do_wear.c:434–516` `Helmet_on`, reached as `afternmv` from `hack.c:4177–4208` `unmul` (plines nomovemsg FIRST, then runs `(*afternmv)()`) after the `allmain.c:381–386` multi<0 completion fires in the same donning turn (delay magnitude 1; the `n`-turn draws the full EOT including dosounds `rn2(400)=357`). The queued row's own function `accessory_or_armor_on` (:2209–2428) is already arm-for-arm (nomul/nomovemsg/afternmv wiring per the brief) — a re-port would be NO MOVEMENT; the writer is Helmet_on.
**JS:** 2 files (`js/do_wear.js` +98/−9, `js/mkobj.js` +12/−0), under the 600/10 caps.
**Change:** full Helmet_on switch in C order: plain-helm breaks; CAUTION → live `see_monsters`; BRILLIANCE → inline `adj_abon` helm half (`:3331–3339`: makeknown iff spe, ABON INT/WIS, botl); CORNUTHAUM → ABON CHA ±1, botl, makeknown; DUNCE → Blind ? vibrate-pline : glow-black pline (pre-curse Tobjnam while known is still 0), `curse`, Blind→set_bknown 0 / Cleric→1 / bknown→update_inventory, botl, Hallu→"My brain hurts!" else the ACURR-vs-base INT `You_feel`; shared known=1+update_inventory tail in C position (`:509–513`); null-uarmh keeps the old find_ac no-op (C-unreachable). New canonical `set_bknown` in `js/mkobj.js` (its C home, mkobj.c:1864–1874) on the imports.mjs-SAFE invent edge. Edges: hcolor (do_name, existing), Hallucination (display, existing), curse/set_bknown (mkobj, existing), PM_WIZARD (monsters, existing), PM_CLERIC (generated/monsters_data, dothrow precedent data leaf), A_INT/A_WIS (attrib, existing).
**Verify:** `node scripts/verify.mjs --fn accessory_or_armor_on` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden (1 PASS, 0 moved/unchanged/worse → PROGRESS; Tourist-92144 RNG 7579/7579 + screens 109/109) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed). VERIFY: PASS. Direct single-session replay was already 109/109 before any docs edit (no D-1831 gap: no js/ edits after verify).
**Named:** HELM_OF_OPPOSITE_ALIGNMENT arm (`uchangealign` + FALLTHROUGH glow/curse unported; wears take the known tail) — map-deferred, not queued (no corpus session reaches it).
**Next:** do not re-pop `accessory_or_armor_on` (0 blocked). Refilled +4 (make_hallucinated, trapmove, do_screen_description, reveal_terrain) to 11 Open.
## 2026-09-09 — D-2191 end.c savelife release arms: curs_on_u + uswallow expels / ustuck release + unstuck rnd(2) (queue owner unstuck, 1 session)

**C locus:** (1) `end.c:743–758` `savelife` tail — `curs_on_u()`, `!mon_moving endmultishot(FALSE)`, `u.uswallow → expels(u.ustuck, u.ustuck->data, TRUE)` else `u.ustuck → Upolyd&&sticks(youmonst.data) ? You("release %s.") : pline("%s releases you.") + unstuck(u.ustuck)`. (2) `mon.c:3438–3467` `unstuck` body (already live via `js/mhitu.js:1661`, incl. the `rnd(2)` holder gate). (3) `mondata.c:654` `sticks` (STCK || (WRAP && !ENGL) || HUGS).
**JS:** 1 file (end.js +17/−3), under the 600/10 caps. Small because the envelope is live (unstuck body D-2093, expels, curs_on_u, Monnam, sticks); density exception applies (C locus 29 + 15 lines, corpus-recorded expectation).
**Change:** `js/end.js` `savelife` only — `await curs_on_u()` (live `js/display.js`, same-file edge already imported), then the `uswallow → expels(ustuck, ustuck.data, true)` / `ustuck → You-release vs Monnam-releases-you + unstuck(ustuck)` arms in verbatim C order using live `expels`/`unstuck` (new static `js/mhitu.js` edge; imports.mjs --can end.js mhitu.js unstuck → SAFE, hoisted fn decls, call-time use only), `Monnam` (same-file do_name edge), `sticks` (canonical `js/engrave.js` clone per its do-not-import-monmove note; same-file edge), and the house `pline('You release …')` idiom for C `You("release %s.")` (artifact.js:2356, hack.js:2084 precedent). No DIAG/FORCE/seed gates; no frozen files.
**Verify:** `node scripts/verify.mjs --fn unstuck` → PASS syntax (1 changed file: js/end.js) · PASS rule2 · PASS hidden `0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-genesis-Knight-92002 step 52 → `one_characteristic` at step 81, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree; final verify after the last js/ edit (map/docs/queue edits only after; no D-1831 gap).
**Named:** `savelife` make_sick TIMEOUT==1 cure, `!mon_moving endmultishot(FALSE)` (not live), livelog (pre-existing map line, kept).
**Next:** do not re-pop `unstuck` (0 blocked). Knight-92002's `one_characteristic`@81 flows through the normal queue.
## 2026-09-09 — D-2190 `trap.c:143–146` burnarmor case 3 passes literal "gloves", not `gloves_simple_name`

**C locus:** `trap.c:143–146` — case 3 calls `burn_dmg(item, "gloves")` with the string literal, never `gloves_simple_name(item)` (contrast case 0 `helm_simple_name` and case 1 `cloak_simple_name`, which do compute names).
**JS:** `js/trap.js` (+2/−1). Under the 600/10 caps.
**Change:** pass the `'gloves'` literal with a C citation comment. No import change: `gloves_simple_name` stays imported (still C-correct at the four `water_damage` sites). No new cross-module edge, no DIAG/FORCE/seed/coordinate gates.
**Verify:** `node scripts/verify.mjs --fn burnarmor` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: no corpus session blocked on burnarmor at HEAD — review-sourced Must-fix, not a corpus owner; ships on the public gates) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed). VERIFY: PASS. Pre-change `verify.mjs --no-cohort` baseline also PASS (clean tree).
**Named:** none new. Case-0 `materialnm` helm prefix stays named (D-2186).
**Next:** none from this row; resume popping the queue head.
## 2026-09-09 — Audit 46171803..d22f6c29 (reviews 1152–1155: 3 ACCEPT, 1 QUALITY-RISK, 0 Must-fix missed) + cadence 44/44

Review iteration, no js/ edits. 1152 (D-2186 armor nouns): QUALITY-RISK — burnarmor case 3 passes `gloves_simple_name(item)` where C `trap.c:143–146` passes literal `"gloves"` (dknown gauntlets would print "gauntlets smoulders!"; the deleted stub was C-correct there); Must-fix prepended, Next cluster set. 1153 (D-2187 aobjnam quan): ACCEPT — re-measured `verify start_tin --base` → 1 PASS PROGRESS, exact match. 1154 (D-2188 hurtle_step): ACCEPT — arm branch-for-branch vs C `:855–905`, `_id` glyph predicate justified, re-measured 110→112 PROGRESS. 1155 (D-2189 monflee): ACCEPT — three arms in C order, all callees LIVE/verified-local, `r.owner || 'js-throw'` fallback confirmed in source (`hidden-proxy.mjs:388`), re-measured 80→96 PROGRESS. Cadence full `sessions`: 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `58+0.37/turn` (R² 0.80) — fortress holds.
## 2026-09-09 — D-2189 `monmove.c` monflee release_hero/flees_light/Vrock arms — vrock `rn2(25)` gas cloud, 1 session moved past

**C locus:** `monmove.c:462–530` `monflee` (plus `:450–457` `flees_light` macro, `:361–372` `release_hero`). The diverging draw is the Vrock arm `:521–524` (`data == &mons[PM_VROCK] && !mspec_used` → `mspec_used = 75 + rn2(25)` + `create_gas_cloud(mx,my,5,8)`). Shipped in the same commit: `release_hero` wiring (`mtmp == u.ustuck` → `expels`/`unstuck` + `You("get released!")`, helper already live via D-1798) and the full `flees_light` gremlin message arm (`Unaware` → "is frightened."; `rn2(10) || Deaf` → "flees from the painful light of <lsrc>" with `lsrc` = `bare_artifactname(uwep)` / `yname(uarm)` / `"[its imagination?]"` (no lamplit re-check, per C); else `SetVoice` + `verbalize("Bright light!")`), macro order `PM_GREMLIN && ((uwep lamplit arti) || (uarm lamplit arti)) && mcansee && couldsee` preserved.
**JS:** `js/monmove.js` (+53/−10: flees_light helper + three arms + 2 consts + 5 import lines; doc omit list retired). Under the 600/10 caps.
**Change:** port the three arms in C order. New file-local `flees_light` mirroring the macro (`mndx === PM_GREMLIN`, `u.uwep`/`u.uarm` lamplit + `artifact_light`, `mcansee && couldsee`). `monflee` awaits the same-file hoisted `release_hero` (no new clone), uses canonical `Unaware` from `eat.js` (C-faithful `game.multi + unconscious/is_fainted`; hoisted, `imports.mjs --can` cycle-safe), existing local `hero_Deaf` (no exported Deaf exists; matches the youprop.h gate), `SetVoice` from `sndprocs.js` (no-cycle edge), `artifact_light`/`bare_artifactname`/`yname` on existing timeout/artifact/objnam edges, `PM_GREMLIN`/`PM_VROCK` consts via `monsterNames`.
**Verify:** `node scripts/verify.mjs --fn monflee` → PASS syntax (1 changed js file) · PASS rule2 · PASS hidden (PROGRESS: Monk-91117 moved 80 → 96) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed). VERIFY: PASS. Direct replay: RNG 23948/23948, screens 157/162, error null — the "js-throw" in the verify line is the null-owner fallback label (`hidden-proxy.mjs:388`), not an exception.
**Named:** none new. `monflee` is now complete (DEADMONSTER exit, fleetime bump/cap, flinch/flee messages, Vrock cloud, always `mon_track_clear`).
**Next:** residual Monk-91117 step-96 screen-first map/memory diff (row 3 C `|     .@###…` vs JS `|......@###…|`, owner null, stepFns `[exercise]`, 0 blocked RNG) — let the rescore queue it under its true writer; do not re-pop `monflee` (RNG stream fully matched end to end).
## 2026-09-09 — D-2188 `dothrow.c` hurtle_step monster-bump arm — x_monnam ARTICLE_A ("a little dog"), 1 session moved past

**C locus:** `dothrow.c:855–905` `hurtle_step` monster arm (`mnam = x_monnam(mon, ARTICLE_A, NULL, (has_mgivenname ? SUPPRESS_SADDLE : 0) | AUGMENT_IT, FALSE)`; `glyph_is_monster/invisible` → `You("find %s by bumping into %s.")` else `You("bump into %s.")`; `wakeup`; `!canspotmon → map_invisible`; `setmangry(mon, FALSE)`; both `touch_petrifies` checks; `wake_nearto`). The `#if 0` mundetected exceptions stay excluded (cannot know the range continues past this spot). Mechanism for the two trailing messages: `win/tty/topl.c:261–277` `update_topl` appends iff `n0 + len(toplines) + 3 < CO - 8` (room for `--More--`), else `more()` first — "a" (n0 27: 25+43+3=71 < 72 appends "hits!") vs "the" (n0 31: 25+47+3=75 ≥ 72 pages early), so the 4-char article flipped the More boundary and lagged every later screen by one message.
**JS:** `js/dothrow.js` (+46/−7: 4 import lines + full bump arm; doc omit list drops "petrify bump; setmangry"). Under the 600/10 caps.
**Change:** port the arm in C order — `glyph_at` read, `mon.mundetected = 0`, `x_monnam(ARTICLE_A, null, SUPPRESS_SADDLE|AUGMENT_IT, false)`, find-by-bumping branch via `noit_mhim`, `wakeup`, `canspotmon→map_invisible(mx,my)`, `setmangry(mon,false)`, hero-unarmored `touch_petrifies(mon.data)` → `instapetrify("bumping into " + an(pmname(mon.data, NEUTRAL)))` (JS `instapetrify` takes the killer string; same effect as C's `svk.killer.name` write), hero-form `touch_petrifies` + `!which_armor(mon, W_ARMU|W_ARM|W_ARMC)` → `minstapetrify(mon,true)`, `wake_nearto`. Imports: same-module extensions (`display.js` glyph trio + `map_invisible`, `do_name.js` x_monnam + pmname, `mon.js` setmangry, `trap.js` instapetrify, `const.js` ARTICLE_A/SUPPRESS_SADDLE/AUGMENT_IT/has_mgivenname/W_ARMU/W_ARM/W_ARMC) plus two IN-SCC edges (`mondata.js` noit_mhim + NEUTRAL, `worn.js` which_armor — `imports.mjs --can` confirms cycle-safe, runtime-only refs, no top-level TDZ read). For the unseen-glyph leg, `glyph_is_invisible_id` is the C-cited choice for a `glyph_at` glyph (per `display.js` doc).
**Verify:** `node scripts/verify.mjs --fn hurtle_step` → PASS syntax (1 changed js file) · PASS rule2 · PASS hidden (PROGRESS, moved 110 → make_stunned@112) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed). VERIFY: PASS. Spot replays: steps 110/111 toplines now byte-match C; step-112 status rows match both sides (Stun clears identically — residual is message-only).
**Named:** none new. Pre-existing `hurtle_step` omits stand (Passes_walls/may_passwall, bad_rock squeeze, Sokoban halt, drag_ball, check_special_room, drown/waterwall, I_SPECIAL jumping, trap pass-over dotrap, nh_delay_output).
**Next:** residual `make_stunned`@112 (stun-expiry «You feel a bit steadier now.» absent in JS though stun state matches — own `potion.c`/`timeout.c` row on rescore). Do not re-pop `hurtle_step`.
## 2026-09-09 — D-2187 `eat.c` start_tin via `objnam.c` aobjnam quan prefix — "6 orcish daggers", 1 session PASS

**C locus:** `eat.c:1769` `start_tin` (`pline("Using %s you try to open the tin.", yobjnam(uwep, (char *)0))`); `objnam.c:2242–2258` `aobjnam` (`bp = cxname(otmp)`; `if (otmp->quan != 1L)` prepend `"%ld "`; optional `otense` verb); `objnam.c:2260–2275` `yobjnam` (`aobjnam` + `shk_your` unless carried pname artifact). `xname` pluralizes but never adds the count — the count lives in `aobjnam` (same for `doname_base :1283`).
**JS:** `js/objnam.js` (+2/−2: `aobjnam` quan line + C ref), `js/eat.js` (+2/−7: import name, clone deleted, `null` verb at the pline). Under the 600/10 caps.
**Change:** `js/objnam.js aobjnam` prepends `` `${quan} ` `` when `((quan ?? 1)|0) !== 1` (missing quan reads as 1, same guard as `simpleonames`/`xname`; C always sets quan). `js/eat.js` deletes the local `yobjnam` clone, imports canonical `yobjnam` on the existing `./objnam.js` edge (no new module edge; `xname` import retained — still used by `singular(food, xname)` arms), and calls `yobjnam(uwep, null)` with C's NULL verb. No DIAG/FORCE/seed/coordinate gates.
**Verify:** `node scripts/verify.mjs --fn start_tin` → PASS syntax (2 changed js files: js/eat.js js/objnam.js) · PASS rule2 · PASS hidden: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS (scen-normal-Rogue-92115: PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed). VERIFY: PASS.
**Named:** none new. `artifact.js:1382` local `aobjnam` stays (now behavior-identical to the canonical; no live-arm stub).
**Next:** row addressed; queue head moves to `dothrow.c` hurtle_step. Do not re-pop `start_tin`.
## 2026-09-09 — D-2186 `objnam.c` armor simple names in trap.js burn/water paths — robe-vs-cloak, 1 session PASS

**C locus:** `objnam.c:5492–5509` `cloak_simple_name` (ROBE→"robe", MUMMY_WRAPPING→"wrapping", ALCHEMY_SMOCK→smock/apron by oc_name_known+dknown, else "cloak"); `:5512–5528` `helm_simple_name` (`hard_helmet`→"helm" else "hat"); `:5531–5547` `gloves_simple_name` (dknown + strstri gauntlets); `suit_simple_name` mail/jacket arms. Symptom path is `trap.c:88–170` `burnarmor` case 1 (`uarmc ? cloak_simple_name : uarm xname`, C `rn2(5)=1`) → `erode_obj(obj, ostr, ERODE_BURN, EF_GREASE)` with ostr non-null, so the `cxname`-vs-`xname` readout in the brief is innocent (both reduce to xname for armor — `objnam.c:1922–1930`). The priest's robe is ARM_CLOAK worn as uarmc on both sides; only the noun differed.
**JS:** `js/do_wear.js` (+13/−4: helm export + cloak-doc update + 2 ternary reuses), `js/trap.js` (+2/−22: 2 import lines, 4 stubs deleted). Under the 600/10 caps.
**Change:** trap.js deletes the four stubs and imports the canonicals — `helm_simple_name`/`cloak_simple_name`/`suit_simple_name` from `./do_wear.js`, `gloves_simple_name` from `./objnam.js` (both edges already exist; `imports.mjs --can` confirms no new edge). New one-function canonical `helm_simple_name` export in do_wear.js (C `:5512–5528` verbatim, via live `hard_helmet`), reused by `armor_simple_name`/`armor_doff_simple_name` (behavior-identical swap of the inline ternary). No DIAG/FORCE/seed/coordinate gates.
**Verify:** `node scripts/verify.mjs --fn erode_obj` → PASS syntax (2 changed js files: js/do_wear.js js/trap.js) · PASS rule2 · PASS hidden: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS (scen-normal-Priest-92020: PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed). Plus manual full `node frozen/ps_test_runner.mjs sessions` → **44/44 PASS** (message-surface change is display-neutral on the fortress; speed `59+0.36/turn`). VERIFY: PASS.
**Named:** `burnarmor` case-0 `materialnm` helm prefix ("iron helm" vs "helm") stays named (pre-existing); mhitu/uhitm `*_simple_name` twins stay local (C-matched, verified this iter).
**Next:** row addressed; queue head moves to `eat.c` start_tin. Do not re-pop `erode_obj`.
## 2026-09-09 — Audit 67f3f896..fb69e76e (reviews 1146–1151: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review-only, no js/. All six D-log corpus claims re-measured at parent baselines — exact matches. Spot-checks: ECMD_OK=0 ⇒ ^W tail verbatim; will_weld/offmap/IRON/METAL/See_invisible union all C-exact. No Must-fix (2 display-only observations). Cadence 44/44 (Scr 11405, RNG 792838). Queue 8 Open, no refill.
