# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — Audit reviews 1288–1291: 2 ACCEPT, 2 QUALITY-RISK + cadence 44/44

Every `js/` commit since 1287 (`95c162ad`; parks between are docs-only): **1288/3782e831 ACCEPT** (D-2322 zap_dig swallowed-pierce arm vs C `:1569-1582`; `unique_corpstat`≡`geno&G_UNIQ` measured, digests pure, all callees LIVE); **1289/0e191fab QUALITY-RISK** (D-2323 dighole 3 arms vs `:884-1024` — magical/by_magic/DRAWBRIDGE_UP exact, but the ROOM cop-out reuses the `surface` clone which prints "ground" where C prints water/molten lava/ice on all three normal drawbridge masks, measured via SURFACE_AT/db_under_typ/is_pool/is_lava/is_ice); **1290/90809a8c ACCEPT** (D-2324 mergable full port vs `:4379-4499` + same_price vs `shk.c:954-981`; MAIL_STRUCTURES live per global.h:430, Hallucination pick is the C-exact export, 37/37 probe claims check out); **1291/d43e86c1 QUALITY-RISK** (D-2325 erode_obj vs `:170-354` arm-for-arm incl. Monnam/mon_nam casing + s_suffix clone retirement, but the new BURN ward gate rides `u_adtyp_resistance_obj` which omits C's dwarvish-cloak 90 arm — RNG-live). Each re-ran `verify --base ~1` → 0/0, matching the D-logs. Full `sessions` 44/44 (Scr 11,405, RNG 792,838, `47+0.29/turn`, R² 0.79) at `6508dec4`. 2 Must-fix prepended (cloak first — RNG-live — then surface); Next cluster set. No REFILL: Open 10 + 2 Must-fix is inside band; queue tops remain parked/exhausted per prior parks.
## 2026-09-15 — D-2325 `trap.c` erode_obj full body: victim/vis arms, wards, grease, verbose, EF_PAY unwear/destroy + acid_damage ward gate

**C locus:** `nethack-c/upstream/src/trap.c` `erode_obj` `:170–354` (whole body, C order) + `nethack-c/upstream/src/zap.c` `inventory_resistance_check` `:5710–5718` (tight callee; `u_adtyp_resistance_obj` live file-local in `js/invent.js`) + `trap.c` `acid_damage` `:4630` hero acid-ward gate (same-file caller, one line).
**JS:** 2 files (`trap.js` +115/−71, `invent.js` +14/−0, C-cite comments), far under the 600/10 caps.
**Change:** `js/trap.js` — erode_obj rewritten arm-for-arm in C order: victim (carried_obj→youmonst / OBJ_MINVENT ocarry / null) + uvictim/vismon (local canseemon) / visobj (cansee bhitpos, !is_pool || next2u-dist2≤2 + u.uinwater ≡ C Underwater, youprop.h:279); type switch with hero AD_FIRE/AD_ACID ward gates + cost_type + crackers + default impossible; cxname default + visobj "the " strip (inline lowercase compare — no 4th strncmpi clone); EF_GREASE→live grease_protect (same file) with ER_GREASED; erosion_matters; verbose-gated not-affected arms (file idiom `game.flags?.verbose !== false`); oerodeproof||blessed-!rnl(4) with rknown+update_inventory; damage arm (adverb, Your/The/s_suffix(Monnam) pline, EF_PAY costly_alteration, hero update_inventory); DESTROY arm (shatters vs "<action> away", EF_PAY, carried→remove_worn_item(TRUE) / minvent→extract_from_minvent(ocarry,TRUE,FALSE) / else impossible strangely-worn + mask clear, delobj); max-eroded verbose completely arm (Your feel/look via Blind(), mon/floor look, msg[]; Your inlined via pline — no 5th Your clone). All added names join existing static imports (s_suffix joins do_name.js — the file-local drifted clone, z/x/sh/ch `'`, retired and its 2 sites rewired to canonical C behavior; costly_alteration joins shk.js; inventory_resistance_check joins invent.js — `imports.mjs --can` ALREADY for the shk edge, call-time use, no TDZ risk; COST_* join const.js; AD_ACID=8 joins the file-local monattk.h block). `js/invent.js` — new exported inventory_resistance_check beside u_adtyp_resistance_obj (prob + `rn2(100) < prob`, no draw when unprotected; C zap.c:5710). acid_damage hero ward gate wired (one line).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. No hand probe: every new arm sits behind erosion/grease/ward/shop/vision gates no corpus session is blocked on, and no maintained js unit harness exists for game logic (sessions are the suite); all wired callees are live shipped code. `node scripts/verify.mjs --fn erode_obj` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; plus full `frozen/ps_test_runner.mjs sessions` → 44/44 PASS, speed 46+0.29/turn (R² 0.79).
**Named:** none new — the data.md:1078 erode_obj omit line retires in this commit; zap.js:166/211 inventory_resistance deferral comments now cover only the unported maybe_destroy_item/destroy_items sites.
**Next:** do not re-pop erode_obj for this row. Queue head is now the `sounds.c` dosounds fountain/sink You_hear + shop You_hear row. Falsifier: a rescore or fresh `verify erode_obj`/`verify inventory_resistance_check` showing a session blocked with either as owner, or a grease/ward/verbose/shop-bill/unwear trace contradicting the new arms. No refill: archiving this row drops Open 12→11, inside the 8–12 band.
## 2026-09-15 — D-2324 `invent.c` mergable unpaid/erosion/candle arms + `shk.c` same_price port

**C locus:** `nethack-c/upstream/src/invent.c` `mergable` `:4379–4499` (whole body, C order) + `nethack-c/upstream/src/shk.c` `same_price` `:954–981` (tight callee cluster; `onbill`/`next_shkp` are live file-locals in `js/shk.js`).
**JS:** 2 files (`mkobj.js` +95/−24, `shk.js` +30/−0, C-cite comments), far under the 600/10 caps.
**Change:** `js/mkobj.js` — mergable rewritten in C order: unpaid/spe/no_charge/obroken/otrapped/lamplit block; FOOD arm kept (JS `orotten` is separate storage vs C obj.h:130 `#define orotten oeroded` — documented in comment); dknown + Blind/Hallucination-gated bknown (C `Role_if(PM_CLERIC)` ≡ urole.mnum, invent.js:1167) + oeroded/oeroded2/greased; erosion_matters-gated oerodeproof/rknown (same-file live callee); corpsenm narrowed to CORPSE/EGG/TIN (NON_PM default); EGG fuse arm (C `obj->timed` ≡ JS `obj.timed` count, start/stop_timer) + CORPSE reviver arm (`is_reviver` macro-clone on the file-local `is_rider` + `mlet === 'S_TROLL'`, LOW_PM = 0); Is_candle age/25 buckets (joins the existing `timeout.js` import); POT_OIL lamplit; unpaid `same_price`; has_omonst/has_omid (live `const.js`, already imported); oname length+content compare (live `safe_oname` joins the existing `do_name.js` import; `.length`+`!==` ≡ strlen+strncmp — equal-length-equal-content passes, every other shape refuses, same as C); SCR_MAIL spe>0 o_id-parity (MAIL_STRUCTURES live per global.h:430); oartifact; Blind/Hallucination-gated known. All added names join existing static imports (`imports.mjs --can mkobj.js shk.js same_price`: ALREADY, no new edge, call-time use, no TDZ risk); EGG/TIN join the file's objectNames const block; PM_CLERIC joins the monsters_data import; Hallucination joins the display.js import (invent-family convention); LOW_PM joins const.js. `js/shk.js` — new exported `same_price` in C order on the file-local `onbill`/`next_shkp` (bill `.price`, `===` shk identity, bare `impossible()` — the file's sync convention).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_mergable.mjs` 37/37 PASS (deleted before finishing; no corpus session is blocked on mergable — sessions are the acceptance suite): unpaid/spe/no_charge/obroken/otrapped/lamplit refuse; oeroded/oeroded2/greased/dknown refuse; bknown-seeing merges (Blind-gated per C); oerodeproof refuses on weapons but merges on food (erosion_matters gate); corpsenm narrowed (weapons with differing corpsenm merge); egg-timed refuses, plain eggs merge, egg corpsenm refuses; troll corpse refuses (reviver), lichen merges; candle close-bucket merges, far-bucket refuses; lit oil refuses; name/omonst/omid/mailcmd/mail-parity/artifact refuse correctly; known-seeing merges. `node scripts/verify.mjs --fn mergable` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; plus `verify --full` → PASS full 44/44 (mkobj-family rule).
**Named:** obj-worn combine guard stays (D-2207; merged setworn fixup unported); C `#if 0` bypass non-gate.
**Next:** do not re-pop mergable for this row. Queue head is now the `shk.c` dopay robbed/angry/debit row. Falsifier: a rescore or fresh `verify mergable`/`verify same_price` showing a session blocked with either as owner, or an unpaid/eroded/candle/oil/mail/artifact merge trace contradicting the new gates. No refill: archiving this row drops Open 9→8, at the min-8 floor.
## 2026-09-15 — D-2323 `dig.c` dighole magical-trap explode + DRAWBRIDGE_UP fluid + by_magic trap-convert

**C locus:** `nethack-c/upstream/src/dig.c` `dighole` `:884-1024` — magical-trap arm (`ttmp && is_magical_trap` → `explode(dig_x,dig_y,0,20+d(3,6),TRAP_EXPLODE,EXPL_MAGICAL)` + `deltrap` + `newsym`); DRAWBRIDGE_UP arm (`fillholetyp(FALSE)`; ROOM → too-hard cop-out; else `drawbridgemask &= ~DB_UNDER`, `|= LAVAPOOL?DB_LAVA:DB_MOAT`, `liquid_flow`); by_magic arm (`LANDMINE→LAND_MINE` / `BEAR_TRAP→BEARTRAP`, `cnv_trap_obj(otyp,1,ttmp,TRUE)` before the nohole check).
**JS:** 1 file (`dig.js`, +52/−10 with C-cite comments), far under the 600/10 caps.
**Change:** `js/dig.js` only. Magical arm in C position (after too-hard, before pool/lava) with live `explode` (new static `explode.js` edge — `imports.mjs --can` SAFE, hoisted fn in the same 90-module SCC, no TDZ risk) + `deltrap`/`newsym` (already imported); C type arg 0 preserved (the `-WAN_CANCELLATION` convention belongs to zap.c maybe_explode_trap, not this arm). DRAWBRIDGE_UP arm in C position (after IS_GRAVE, before IS_THRONE) with `fillholetyp(FALSE)`, the ROOM cop-out pline via `surface()` + here/t-here (the file's existing pattern), `drawbridgemask` bit ops in the `| 0` idiom (`DB_MOAT` is 0 — preserved no-op, cf. hack.js D-1090), and live `liquid_flow`. by_magic arm in C position (after the `t_at` re-read, before nohole) with `LANDMINE`/`BEAR_TRAP` (already imported) → `LAND_MINE`/`BEARTRAP` via `objectNames.indexOf` (iactions/eat/shk/apply/trap convention) + live `cnv_trap_obj` (joins the existing `trap.js` import).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. No hand probe: the arms are reachable only via pickaxe-down on magical-trap/DRAWBRIDGE_UP cells and wand-dig on settable traps — no corpus session reaches them and no maintained js unit harness exists (sessions are the suite); disclosed per skill. `node scripts/verify.mjs --fn dighole` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS.
**Named:** `spot_checks` tail (no JS counterpart anywhere in `js/` — new-function scope, pre-existing omission, untouched).
**Next:** do not re-pop dighole for this row. Queue head is now the `do.c` dodown stair-descent More-transient. Falsifier: a rescore or fresh `verify dighole` showing a session blocked with dighole as owner, or a dig-down-on-magical-trap/DRAWBRIDGE_UP / wand-dig-on-landmine trace missing the explode/fluid-fill/convert. Refill note: archiving this row drops Open 2→1 (below min 8); per the iteration order refilled +11 from map named omits (debt.md/data.md/absent.md D-families, all verified never live/archived/parked this iteration) back to 12 — see queue.
## 2026-09-15 — D-2322 `dig.c` zap_dig swallowed-pierce arm: pierce pline + mhp + expels

**C locus:** `nethack-c/upstream/src/dig.c` `zap_dig` `:1569-1582` (`u.uswallow` → `mtmp = u.ustuck`; `!is_whirly` gate; `digests` → `You("pierce %s %s wall!")`; `unique_corpstat` (mondata.h:174, `geno & G_UNIQ`) → `(mhp+1)/2` else `mhp = 1`; `expels(mtmp, mtmp->data, !digests(...))`).
**JS:** 1 file (`dig.js`, +30/−4 with C-cite comments), far under the 600/10 caps.
**Change:** `js/dig.js` only, no new static module edges (`is_whirly` + `G_UNIQ` join the existing `monsters.js` import; `STOMACH` joins the existing `const.js` import; `s_suffix`/`mon_nam`/`pline` already imported): the `u.uswallow` arm reads `u.ustuck`, skips whirly swallowers (vortex letter, air elemental — ≡ C `is_whirly`, mondata.h:57), prints the digests-only `You pierce <suffix> <stomach> wall!` (live `s_suffix`/`mon_nam` + `mbodypart(mtmp, STOMACH)`), applies the `geno & G_UNIQ` halve (`(((mhp|0)+1)/2)|0`, C truncation) else `mhp = 1`, then awaits live `expels(mtmp, mtmp.data, !digesting)` (mhitu.js, D-1970-family callee). `digests`/`expels`/`mbodypart` arrive via in-arm dynamic `import()` — this file's convention for caller-side modules (cf. the u.dz arm's `polyself.js` import + `use_pick_axe2`'s `uhitm.js` import), so no static edge and no TDZ risk. Single `digests` evaluation into a const (pure mattk scan, no RNG — identical to C's two calls).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_zapdig.mjs` 10/10 PASS (deleted before finishing; arm no corpus session reaches): `is_whirly` true on fire vortex / false on little dog, `digests` true on purple worm / false on dog, `geno & G_UNIQ` set on Wizard / clear on dog, `mbodypart` stomach string, whirly-swallowed `zap_dig` returns with mhp untouched and still swallowed (no expels), null-ustuck guard no-throw. The non-whirly `expels` tail rests on the live shipped callee (no game-state harness exists for full expulsion; sessions are the acceptance suite).
**Named:** none new — the `zap_dig` docstring omit retired to Hallucination draft only. `nhUse`-style no-ops N/A in this arm.
**Next:** do not re-pop zap_dig swallowed-pierce for this row. Queue head is now the `dig.c` dighole DRAWBRIDGE_UP + magical-trap row. Falsifier: a rescore or fresh `verify zap_dig` showing a session blocked with zap_dig as owner on a swallowed-dig step, or a swallowed-dig trace missing the pierce pline / mhp change / expulsion. Refill note: archiving this row drops Open 3→2 (below min 8); all three refill sources verified exhausted this iteration — the 25 live `hidden-proxy queue --limit 30` owners are all parked symptom/misattributed owners (proofs in LOOP-QUEUE.md Parked), archived/shipped, or the live dodown Open row, and every unqueued TOP30 row is likewise parked, shipped, or map-singleton material gated at corpus 85.7 % < 90 % (Constitution §10.13, LOOP-QUEUE header + Deferred); no filler invented (use_crystal_ball/use_figurine/flip_coin/dismount_steed precedent); the next rescore is the unblocker.
## 2026-09-15 — Audit reviews 1285–1287: 3 ACCEPT, 0 Must-fix + cadence 44/44

Every `js/` commit since 1284 (`f028aee2`; parks between are docs-only): **1285/df21066c ACCEPT** (D-2319 shop-wall add_damage + maze watch_dig drop vs C `:1686-1698`/`:1712-1729`); **1286/031ebb3a ACCEPT** (D-2320 doorbuster `Upolyd && is_giant()` M2_GIANT + 2 Soundeffect arms vs C `:930`/`:936-946`); **1287/95c162ad ACCEPT** (D-2321 adj_pit_checks + pit_flow + pitdig arm/tail vs `:1617-1662`/`:1742-1750`/`:1762-1838`/`:1843-1882`; 12-callee closure all LIVE). Each re-ran `verify --base ~1` → 0/0, matching the D-log. Full `sessions` 44/44 (Scr 11,405, RNG 792,838, `46+0.29/turn`) at `95c162ad`.
**REFILL hold:** Open still 3 — all 25 live queue owners verified parked (falsifiers in Parked) except live dodown; map refill barred at corpus 85.7 % < 90 % (§10.13). Next rescore is the unblocker.
## 2026-09-15 — D-2321 `dig.c` zap_dig pitdig branch: adj_pit_checks + pit_flow port

**C locus:** `nethack-c/upstream/src/dig.c` `zap_dig` `:1617-1662` (pitdig setup `:1617-1623`, adjacent-pit arm `:1631-1662`, pitflow tail `:1742-1750`) + `:1763-1838` `adj_pit_checks` + `:1844-1882` `pit_flow`.
**JS:** 1 file (`dig.js`, +177/−13 with C-cite comments), far under the 600/10 caps.
**Change:** `js/dig.js` only, no new module edges (all four added names join existing static imports — `imports.mjs --can` ALREADY for each: `s_suffix` joins the existing `do_name.js` import; `On_ladder` joins `mklev.js`; `DBWALL`/`xdir`/`ydir`/`N_DIRS` join `const.js`; `t_at`/`conjoined_pits`/`is_pool`/`is_lava`/`On_stairs`/`dighole`/`fillholetyp`/`liquid_flow`/`u_at`/`is_pit` already imported or same-file): new exported `adj_pit_checks(cc, msg)` in C branch order (null/isok gates leave msg untouched; `ltyp` save + `lev.flags = 0`; pool/lava silent-FALSE for the caller; foundation/trees/STONE-SCORR-W_NONDIGGABLE via `rm_wall_info` (≡ C after the flags clear)/IRONBARS/SINK/On_ladder/supporting fountain-throne-altar-stairs-DRAWBRIDGE_DOWN-DBWALL with `s_suffix`; `{ v: '' }` out-param for C `char *msg`, caller `await pline(buf.v)` for C `pline1`); new exported async `pit_flow(trap, filltyp)` (filltyp-ROOM + is_pit gates; by-value `{tx,ty,conjoined}` copy before `liquid_flow` which deltrap()s; `lev.typ/flags` assign; `u_at`-gated fillmsg; `N_DIRS` recursion over saved bits with the C `#if 0` back-check as a comment); `zap_dig` gains the C setup (`t_at` gate + `xytodir`), the full adjacent-pit arm in C order (`conjoined_pits` short-circuit preserves the C fall-through to `zx += dx` when `diridx == DIR_ERR` or already conjoined; `digdepth = 0`; `dighole(TRUE,TRUE)` + `t_at` re-read; conjoined-bit ORs with `| 0`; pool/lava flow-from-previous-cell; `break`), and the `pitflow` tail (`isok` + `t_at` + `is_pit` + `fillholetyp(...,true)` + `filltyp !== ROOM` → `await pit_flow`). Docstring omit retired to swallowed-pierce-only.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_pitdig.mjs` 14/14 PASS (kept in /tmp for re-run; arms no corpus session reaches): null/off-level msg-untouched, ROOM true + flags cleared, SDOOR/WALL foundation, STONE diggable-true vs nondiggable rock-glows, IRONBARS/SINK/FOUNTAIN/DRAWBRIDGE messages incl. exact `The fountain's supporting structures remain intact.`, pit_flow ROOM/non-pit/null gates. No maintained unit harness exists for game logic (only `scripts/*.test.mjs` for loop tooling + session replay); disclosed per skill.
**Named:** swallowed pierce (`u.uswallow` early-return; next Open row, untouched); `nhUse(digdepth)` no-op macro (dead store + break covers it); C `#if 0` pit_flow back-check (disabled both sides, comment-cited).
**Next:** do not re-pop adj_pit_checks/pit_flow/zap_dig-pitdig for this row. Queue head is now the `dig.c` zap_dig swallowed-pierce row. Falsifier: a rescore or fresh `verify adj_pit_checks`/`pit_flow`/`zap_dig` showing a session blocked with any of the three as owner, or a trapped-in-pit dig trace missing the adjacent-pit dig/conjoin/flow. Refill note: archiving this row drops Open 4→3 (below min 8); all three refill sources verified exhausted this iteration — the 25 live `hidden-proxy queue` owners are all parked symptom/misattributed owners (NOTES Parked) except live dodown, every unqueued TOP30 row is likewise parked/shipped/gated, and map-singleton refill is barred by Constitution §10.13 + the queue header + CURRENT.md while the corpus sits at 85.7 % (below the 90 % family gate); no filler invented (use_crystal_ball/use_figurine precedent); the next rescore is the unblocker.
## 2026-09-15 — D-2320 `dokick.c` kick_door residuals: giant doorbuster predicate + shatter/crash Soundeffect

**C locus:** `nethack-c/upstream/src/dokick.c` `kick_door` `:909–970` — `:924` (`doorbuster = Upolyd && is_giant(gy.youmonst.data)`, `mondata.h:107` M2_GIANT predicate), `:940` (`Soundeffect(se_kick_door_it_shatters, 50)`), `:946` (`Soundeffect(se_kick_door_it_crashes_open, 50)`).
**JS:** 1 file (`dokick.js`), far under the 600/10 caps.
**Change:** `js/dokick.js` only, no new module edges (`is_giant` joins the existing `monsters.js` import — `imports.mjs --can` ALREADY; `Soundeffect` joins the existing `sndprocs.js` import; the two `se_*` constants via `generated/seffects_data.js` — the `trap.js:145` convention): doorbuster uses the live `is_giant` M2_GIANT predicate (≡ C macro); both success arms call `Soundeffect` in C order before the message (draw-free no-op in this build per `sndprocs.js:36`, D-2318/D-2316/D-2315 convention). +9/−3 lines incl. C-cite comments; docstring omit retired.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_doorbuster.mjs` (deleted after run; no corpus session reaches the giant-poly kick arm): `mons(PM_FIRE_GIANT)` carries M2_GIANT with no `.is_giant` field — old predicate false, new true; `mons(PM_HUMAN)` false both sides. No maintained unit harness exists for game logic (only `scripts/*.test.mjs` for loop tooling + session replay); disclosed per skill.
**Named:** `vision_recalc(1)` after each success arm (JS-only display refresh, pre-existing, untouched); `loc.flags = loc.doormask` cell-rep sync (pre-existing); `get_iter_mons`/`get_iter_mons_xy` file-local clones vs `mon.c:4544` `mon_offmap` skip (review-14 risk, standing pattern); kick_nondoor/SDOOR (other rows).
**Next:** do not re-pop kick_door for the D-0947 line. Queue head is now the `apply.c` flip_through_book/flip_coin row (D-1024). Falsifier: a rescore or fresh `verify kick_door` showing a session blocked with kick_door as owner, or a giant-poly kick trace burning `rnl(35)` where C busts.
## 2026-09-15 — D-2319 `dig.c` zap_dig shop-wall arms: missing `add_damage(SHOP_WALL_COST)` + extra maze `watch_dig`

**C locus:** `nethack-c/upstream/src/dig.c` `zap_dig` beam arms — door-razed `:1664–1683` (`add_damage(SHOP_DOOR_COST)` + `watch_dig(zap)`), maze wall `:1686–1698` (`add_damage(SHOP_WALL_COST)` + `shopwall`, NO `watch_dig`), ordinary wall/SDOOR `:1712–1729` (`add_damage(SHOP_WALL_COST)` + `shopwall` + `watch_dig(zap)`), tail `:1751–1752` (`pay_for_damage("destroy"/"dig into")`).
**JS:** `js/dig.js` `zap_dig` maze + ordinary wall arms now arm-for-arm vs C. Rest of the D-0941 family re-verified live (all 7 C `watch_dig` call sites wired: still_chewing start/continue + wall/door `add_damage` + `pay_for_damage` at `js/hack.js:3012/3033/3053/3088/3106`; `mdig_tunnel` cost-0 at `js/dig.js:998-999/1042-1043`; `watch_on_duty` lock + live `is_digging` at `js/monmove.js:990-1017`; `do_break_wand` WAN_DIGGING + `shop_damage` + `pay_for_damage('dig into')` at `js/apply.js:1246-1253/1311-1313`; `dig()` occupation shopedge/dmgtxt/pay + 'too hard' at `js/dig.js:2013/2065-2104/2130-2137`; zap_dig u.dz + door + tail at `js/dig.js:1140/1179/1254-1257`).
**Change:** `js/dig.js` only (+ static `SHOP_WALL_COST` from `const.js`, already exported; `add_damage` via the file's existing per-arm dynamic `shk.js` import — the door arm's own convention, no new module edge, no TDZ risk): maze arm drops `watch_dig` and schedules `add_damage(zx, zy, SHOP_WALL_COST)` in C order; ordinary arm schedules it in C order and keeps `watch_dig`. +7/−1 lines plus a C-cite comment. `add_damage` draws no RNG on either side, so the RNG stream is untouched.
**Verify:** hand probe `/tmp/probe_zapdig_shop.mjs` (real `zap_dig` on a stub maze/shop level; deleted before finishing): pre-fix 4 FAIL (both arms convert the wall but `damagelist` stays null), post-fix 7/7 PASS (`damagelist` `{cost:200,x:10,y:10}` = `SHOP_WALL_COST`, conversions DOOR/ROOM, no warn latched). `node scripts/verify.mjs --fn watch_dig` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (tail pasted from the observed run, after the last `js/` edit). Full `sessions` 44/44 PASS on the ship tree.
**Named:** `SetVoice` (audio, draw-free); do_break_wand ICE `spot_stop_timers` + `check_unpaid` polish; zap_dig swallowed-pierce/pitdig-conjoined (other families); `add_damage` flags OR shape (review-08 note; untouched, no falsifier).
**Next:** queue head is now the `dokick.c` kick_door row (D-0947). Refill note: archiving this row drops Open 6→5 (below min 8); all three refill sources were verified exhausted this iteration — the 25 live corpus owners are all parked/archived/shipped, unqueued TOP30 rows likewise, every debt.md D-tag through D-1656 is indexed fixed — and map-singleton refill is barred by Constitution §10.13 + the queue header + CURRENT.md while the corpus sits at 85.7 % (below the 90 % family gate); no filler invented (mintrap precedent); the next audit-iter rescore is the unblocker.
## 2026-09-15 — Audit cb720cc9..f028aee2 (reviews 1282-1284: 3 ACCEPT, 0 Must-fix) + cadence 44/44

Audit, no `js/`: 1282/1283/1284 (D-2316/17/18) all ACCEPT, no Must-fix. Queue at 8 Open — no refill. Cadence 44/44, Scr 11,405/11,405, RNG 792,838/792,838, `46+0.29/turn`.
## 2026-09-15 — D-2318 `trap.c` launch_obj ROLL-path ship arms + `ship_object` worn/unhide residuals

**C locus:** `trap.c` launch_obj ROLL block (`:3423–3430` down_gate/ship_object; `:3505–3513` post-switch flooreffects; `:3514–3529` boulder-on-boulder); `dokick.c:1715` (`remove_worn_item(otmp, TRUE)` owornmask arm) + nodrop-impact arm (`impact_drop` then `maybe_unhide_at(x, y)`).
**JS:** 2 files (`trap.js`, `dokick.js`), far under the 600/10 caps.
**Change:** `js/trap.js` only (+2 static imports: `sndprocs.js` Soundeffect — `imports.mjs` SAFE, no cycle; `const.js` MIGR_NOWHERE; `dokick.js`/`do.js` via the file's in-function dynamic-import convention): ROLL gate-drop pre-block in C order (`down_gate(x, y) !== MIGR_NOWHERE` → `ship_object(singleobj, x, y, false)` → used_up + `launch_drop_spot` + break); post-switch `flooreffects(singleobj, x, y, 'fall')` used_up path; boulder-on-boulder with the `!isok/!dist/IS_OBSTRUCTED` "hits another" arm, draw-free `Soundeffect(se_loud_crash, 80)`, cansee-gated crash `You_hear`, `obj_extract_self` + otrapped pass-off + place + singleobj swap + `wake_nearto(x, y, 100)` (all callees already imported; `sobj_at` short-circuit preserves C's `otyp == BOULDER` guard order). `js/dokick.js` only: owornmask arm dynamically imports live `steal.js` `remove_worn_item` (the `breaktest` precedent — no new static edge; steel-body D-1086) and the local clone plus its now-unused `wield.js` import are deleted (D-1849: import the export); nodrop-impact arm awaits `maybe_unhide_at` (joins the existing `monmove.js` import — no new edge).
**Verify:** `node scripts/verify.mjs --fn ship_object` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD baseline+working — NOT a corpus PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue/NOTES/CURRENT edits only after; no D-1831 gap). Pasted tail is the observed output, not a claim.
**Named:** launch_obj `closed_door` crash-through (C `:3533–3541`; `pline_The` has no JS counterpart); STWALL/TREE `Thump!` + `wake_nearto(x2, y2, 16)` (C `:3555–3561`); LAUNCH_UNSEEN bowling/rumble msgs; dig-context clear; scatter MAY_*/VIS_EFFECTS; `curs_on_u` (all pre-existing); ship_object `Soundeffect` + shop_floor_obj polish + breaktest `obfree` (pre-existing; the break arm zeroes in place).
**Next:** queue head is now the `dokick.c` kick_object row; launch_obj leftovers above get their own row when a corpus session reaches them.
## 2026-09-15 — D-2317 `trap.c` openholdingtrap/openfallingtrap monster-arm `canspotmon`

**C locus:** `trap.c:6185` (`if (canspotmon(mon))`, openholdingtrap monster arm); `trap.c:6279` (`*noticed = cansee(t->tx, t->ty) || canspotmon(mon)`, openfallingtrap monster arm); `display.h:129` (`canspotmon(mon)` ≡ `canseemon(mon) || sensemon(mon)`).
**JS:** 1 file (`trap.js`, +4/−2), far under the 600/10 caps.
**Change:** both call sites now use the already-imported `canspotmon` (`display.js:1237` ≡ C; no new module edge, no TDZ risk). Both bodies re-audited arm-for-arm vs C otherwise clean: tdummy fallback, the_your/which article incl. `vowels` an/a, utraptype switch incl.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_holdfall_sense.mjs` 7/9 pre-fix (both noticed arms FAIL with predicates + happened PASS — authentic failure) → 9/9 post-fix (probe deleted after run; arms no corpus session reaches). No maintained unit harness exists for game logic (only `scripts/*.test.mjs` for loop tooling + session replay), so durable coverage is the `verify` gates below, not a committed test.
**Named:** `boxlock_invent` `update_inventory()` refresh (map-named; UI redraw, no corpus screen/RNG surface); `reward_untrap` stays file-local clone (`trap.js:6009`, standing pattern); nothing else new.
**Next:** do not re-pop openholdingtrap/openfallingtrap/boxlock_invent for the D-0981 line. Falsifier: a rescore or fresh `verify` showing a session blocked with a trap/zap owner on a sensed-monster release, or a telepathy release trace missing the message.
## 2026-09-15 — D-2316 `dbridge.c` open/close_drawbridge residuals: revive_nasty port, Soundeffect order, wall_info assign, nokiller wire

**C locus:** `dbridge.c:862-863` (`Soundeffect(se_gears_turning_chains_rattling,100)` before `You_hear`); `:790-795` (`Soundeffect(se_chains_rattling_gears_turning,75)`) + `:810-813` (OBJ_AT smash `Soundeffect(se_smashing_and_crushing,75)`); `:803` (`wall_info = W_NONDIGGABLE`); `:820-821` + `:868` (`revive_nasty(x,y[,x2,y2],NULL)`); `:833` + `:881` (`nokiller()`). `hack.c:104-137` `revive_nasty` (CORPSE + is_rider/Wizard → enexto/rloc_to shove, Norep(msg), revive_corpse; unsafe-revival re-seat). Full open `:839-882` + close `:774-834` re-read (brief output): every other arm verified matching — typ guards, get_wall_for_db, coming/going predicates, DOWN/DOOR+D_NODOOR vs UP/DBWALL+horizontal terrain, OBJ_AT/Deaf smash gate, delallobj (x-only on open, both on close), deltrap x2, del_engr x2, newsym x2, block/unblock vision, Is_stronghold uopened_dbridge.
**JS:** 2 files (`hack.js` +45/−2, `dbridge.js` +17/−12), far under the 600/10 caps.
**Change:** `js/hack.js` (C-file match): new export `revive_nasty` in C order — `mons()`+`is_rider` permonst (mondata.h:110), `monsterNames.indexOf('PM_WIZARD_OF_YENDOR')` (eat.js/objnam.js convention), `objects_at`/`m_at`/`Norep`/`enexto`/`goodpos`/`rloc_to`/`revive_corpse`; new names join existing edges, sole new edge `do.js revive_corpse` (`imports.mjs --can hack.js do.js revive_corpse`: SAFE, hoisted fn, same 90-module SCC, call-time use). `js/dbridge.js`: 3 `Soundeffect` calls in C order (join the existing `sndprocs.js` + `seffects_data.js` imports; se ids from the checked-in extractor, no new edge), `wall_info` plain assignment, `await revive_nasty` in C order (open `(x,y)` before delallobj; close `(x,y)`+`(x2,y2)` after smash-hear before the delallobj pair), `nokiller()` at both tails (live same-module). Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_revive.mjs` PROBE-PASS 8/8 (arms no corpus session reaches; deleted after run): imports resolve (new `do.js` edge TDZ-safe), empty/junk/non-rider-corpse → false + zero RNG, Rider-corpse path runs no-throw (staged `revive_corpse` bails false without live game state — full revival rests on the live reviewed callee; sessions are the acceptance test). `node scripts/verify.mjs --fn open_drawbridge` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared `hack.js` changed) · VERIFY: PASS.
**Named:** `set_entity`/`do_entity` crush on open/close (D-1967 open row — its iter wires); `moverock_core :450` `revive_nasty` caller (D-1859 row; callee now live, caller untouched — `// C: revive_nasty deferred` at `hack.js:516` stands); `recalc_block_point`+`vision_recalc(0)` vs C `block/unblock_point` (pre-existing superset, untouched — no screen/RNG surface); `delallobj` local vs invent unpaid/resist (review-46 debt, own envelope); `game.tune` hydration (review-46 debt, out of scope); `You_see`/`You_hear` local clones (standing pattern, untouched).
**Next:** do not re-pop open_drawbridge/close_drawbridge/dbridge-revive_nasty. Falsifier: a rescore or fresh `verify` showing a session blocked with a dbridge owner, or a Castle drawbridge trace missing gear/crush sounds, keeping `wall_info` bits, skipping a Rider revival, or carrying a stale killer.
## 2026-09-15 — Audit reviews 1275–1281 (6ce28063..f5d3798f) + cadence 44/44

Review iteration, no `js/` edits. Seven JS-touching SHAs since 1274, oldest-first, one file per SHA written as each finished: 1275 god_zaps_you fall-through (D-2309), 1276 clear_conjoined_pits (D-2310), 1277 maybe_dunk_boulders (D-2311), 1278 dig() occupation arms (D-2312), 1279 destroy_drawbridge tail (D-2313), 1280 goto_level falling (D-2314), 1281 do_earthquake arms (D-2315). All **ACCEPT**, no Must-fix (no LOOP-QUEUE prepend; Next cluster unchanged). Every D-log corpus claim re-measured with `verify <fn> --base HASH~1` — all matched (0 blocked, vacuous-honest). Findings worth keeping: D-2309 cites `32-e3c6cff4-desecrate-altar.md`, absent from both review dirs (citation gap, debt itself real+fixed); D-2315's C line pointers are stale by patch shift (`:268/:380/:420/:443` actual); pre-existing `wake_nearto` clone (`& ~0x01` vs `~0x30000000`, no G_UNIQ/wake_msg — own future row, correctly deferred); `hero_Deaf` `|| u.Deaf` disjunct dead (zero writers); leftover dig.js namer clones still serve web/rubble/thin-air arms. Also backfilled the missing 1274 index row. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, `47+0.29/turn` (R² 0.79) — identical aggregates to the ef75880a audit. Queue at 11 Open (df1c363c refilled to 12 from debt.md, f5d3798f consumed one) — no REFILL owed this iter; map singletons stay barred <90%.
**Next:** pop LOOP-QUEUE Open head (`dbridge.c` open_drawbridge, D-0977 row).
## 2026-09-15 — D-2315 `music.c` do_earthquake quake-arm residuals: SCORR unblock_point, ALTAR altarmask_at, se_thump/se_scream

**C locus:** `music.c:430` (`unblock_point(x, y)` after `typ = CORR` — `dig_point` + full-recalc flag, vision.c:899); `:416` (`amsk = altarmask_at(x, y)` — mimic `M_AP_FURNITURE`+`S_altar` corpsenm else `levl.altarmask`, pray.c:2490–2504); `:384` (`Soundeffect(se_thump, 50)` before `You_hear("a thump.")`); do_pit `:277` (`Soundeffect(se_scream, 50)` before `You_hear("a scream!")`). Full C re-read (`do_earthquake` :343–475 + `do_pit` :221–341, in brief output): every other arm verified matching — wakeup/mundetected/ceiling-hider/seemimic prefix, `rn2(14-force)` gate, all seven terrain arms with FALLTHRU order, DOOR newsym-before-pline + shop `add_damage`, do_pit boulder/KADOOM, `fillholetyp`+`liquid_flow` re-read, hero buried-ball/levitate/fall/jostle incl. jostle short-circuit (review-41 verified), `M_AP_TYPE` masks `M_AP_TYPMASK` both sides (C monst.h:73; js/const.js:3200).
**JS:** 1 file (`music.js`, +11/−4), far under the 600/10 caps.
**Change:** `js/music.js` only, no new module edges (all three source modules already imported): SCORR arm calls live `unblock_point` (joins the existing `vision.js` import; ≡ C); ALTAR arm calls live `altarmask_at(x, y)` (joins the file's existing dynamic `pray.js` import convention — no static-cycle risk; `pray.js:altarmask_at` ≡ C incl. mimic edge); `Soundeffect(se_thump, 50)` / `Soundeffect(se_scream, 50)` restored in C order (join the existing `sndprocs.js` + `seffects_data.js` imports; no-op in this build per `sndprocs.js:36`, same as the file's :1108 convention). Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Post-fix `node scripts/verify.mjs --fn do_earthquake` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool-gated: music.js not shared) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after).
**Named:** do_pit `set_levltyp(x, y, filltype)` side effects (no JS `set_levltyp` exists — own future `trap.c` row, not this `music.c` function); boulder-arm `flooreffects` full (pre-existing D-0972 thin, untouched); `cvt_sdoor_to_door`/`in_rooms` shared shapes (verified equivalent, untouched).
**Next:** do not re-pop do_earthquake/do_pit quake arms. Falsifier: a rescore or fresh `verify` showing a session blocked with a do_earthquake/do_pit owner, or a drum-quake trace mis-painting SCORR/altar/sound arms.
