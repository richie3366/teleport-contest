# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2067 youprop.h Invis read the magic-trap-only u.Invis flat, so quaffed invisibility never reached wildmiss/mattacku (queue owner wildmiss)

**C locus:** `youprop.h:195–198` — `HInvis`/`EInvis` are `uprops[INVIS].intrinsic/extrinsic`, `#define Invis ((HInvis || EInvis) && !BInvis)`; the quaff path writes HInvis + the uprops intrinsic (`potion.js set_HInvis` mirrors C `potion.c`); the `u.Invis` flat is only synced by the magic-trap toggle (`trap.c`, `js/trap.js:4260–4261`; `timeout.js` D-1999 note). Readers: `mhitu.c:181` wildmiss `unotseen`, `:713` mattacku `tmp -= 2`, `:2540` floating-eye `perceives || !Invis`; `youprop.h:279 Underwater (u.uinwater)` feeding `:182 usubmerged` and `:1171 !Underwater`.
**JS:** 1 file (mhitu.js +29/−6) — under the 600/10 caps. Small because the C locus is a 4-line macro plus one-line use sites (D-2066 precedent). Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes.
**Change:** `js/mhitu.js` — file-local `BInvis` + `Invis` now mirror the potion.js/zap.js idiom (`H = HInvis||intrinsic`, `E = EInvis||extrinsic`, mummy-wrapping `uarmc` stand-in for setworn `w_blocks`); `INVIS` joins the existing `./const.js` import (no new module edge); floating-eye arm `!u.Invis` → `!Invis()`; wildmiss `usubmerged` + `is_pool` arm → `u.uinwater`.
**Verify:** `node scripts/verify.mjs --fn wildmiss` → `PASS syntax 1 changed js file(s): js/mhitu.js` · `PASS rule2` · `PASS hidden verify wildmiss: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Healer-92147 moved 186→armoroff@196; Ranger-92155 moved 198→step 200, see below) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Ranger's `moved → js-throw` label is verify's `r.owner || 'js-throw'` fallback (hidden-proxy.mjs:388) for a null owner: `hidden-proxy show` + direct `frozen/ps_test_runner.mjs` replay both report a screen-only divergence (RNG 3881/3881, error null — C «...butts!--More--» vs JS «...butts!») — no throw occurred, no Must-fix throw row owed.
**Named:** other modules' file-local `Invis` variants untouched (potion/zap/timeout correct; trap.js flat-biased + do_wear `Invisible_dw` unreached by any blocked session — future rows if the corpus blocks there); `Some_Monnam` impossible still named (pre-existing); `is_waterwall` stays the `IS_WATERWALL` inline (no blocked session reaches the case-2 water arm).
**Next:** pop the next Open row (`bones.c` savebones `remove_mon_from_bones` + LEAVESTATUE statue arm). Healer-92147 now feeds the already-queued `do_wear.c armoroff` row (C «...taking off your dragon mail.» vs JS «...taking off your mail.» @196 — third session for that owner). Ranger-92155 now sits at an unattributed `--More--` paging diff @200 (null owner; candidate for a future queue row on rescore — do not hand-invent it here).
## 2026-09-08 — D-2066 monst.h M_AP_TYPE unmasked F_DKNOWN flips monster_nearby rest-safety (queue owner mcalcmove, writer M_AP_TYPE)

**C locus:** `monst.h:69–73` — `M_AP_TYPMASK 0x7`, `M_AP_F_DKNOWN 0x8`, `#define M_AP_TYPE(m) ((m)->m_ap_type & M_AP_TYPMASK)` → `10 & 7 = 2 = M_AP_OBJECT`; `hack.c monster_nearby :4106–4127` skips `M_AP_FURNITURE`/`M_AP_OBJECT` before the hostile/helpless/onscary/canspotmon gates, so the mimicking Large Mimic never counts as nearby.
**JS:** 1 file (const.js +5/−1) — under the 600/10 caps. Small because the C locus is a one-line macro. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes.
**Change:** `js/const.js` — `M_AP_TYPE` returns `((mon?.m_ap_type ?? 0) & M_AP_TYPMASK)` with the monst.h:73 citation; `M_AP_TYPMASK` is already exported from the same file (call-time reference, no TDZ, no new import/edge). All ~40 `M_AP_TYPE` call sites inherit the mask; the dozen call sites that already masked inline (hack/display/do_name/pager/uhitm `that_map_type`) are now consistent rather than workarounds.
**Verify:** `node -e` import check (`m_ap_type=10→2`, `8→0`, `3→3`, `0→0`, `null→0`) · `node scripts/verify.mjs --fn mcalcmove` → `PASS syntax 1 changed js file(s): js/const.js` · `PASS rule2` · `PASS hidden verify mcalcmove: 1 PASS, 1 moved past, 1 unchanged, 0 worse → PROGRESS` (scen-wish-Archeologist-92216 PASS; scen-death-Knight-92188 moved 17→mcalcmove@20; scen-wish-Rogue-92137 still mcalcmove@26 — both stay on the parked slime/lifesave writer per the Parked row) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt from the probe runs reverted before handoff (not committed).
**Named:** raw `m_ap_type` readers that bypass `M_AP_TYPE` (same latent class, no blocked session reaches them — future rows if the corpus blocks there): `objnam.js:3350` (`=== M_AP_OBJECT`), `sounds.js:1445` (`=== 1 || === 2`), `music.js:230/305–306`, `mhitu.js:3506/3525`, `eat.js:1628` (`!== M_AP_NOTHING`); `M_AP_FLAG`/`U_AP_TYPE`/`U_AP_FLAG` stay inline at call sites (all already mask; map `turns.md` monster_nearby section updated).
**Next:** pop the next Open row (`mhitu.c` wildmiss). Do not re-pop `mon.c` mcalcmove — Rogue/Knight remain on the parked slime/lifesave writer (Parked row falsifier: per-session savelife/slimed_to_death owner rows).
## 2026-09-08 — D-2065 pline.c You_hear Unaware dream arm (queue owner zapyourself, writer You_hear; sounds.js clone removed)

**C locus:** `pline.c` `You_hear` `:436–452` — `if ((Deaf && !Unaware) || !flags.acoustics) return;` then `Underwater → "You barely hear "`, `Unaware → "You dream that you hear "`, else `"You hear "`; `Unaware` ≡ `youprop.h:399` `gm.multi < 0 && (unconscious() [trap.c:6776: multi<0 && (u.usleep || wake-msg prefix)] || is_fainted() [eat.c:3347: uhs==FAINTED])`. The longer dream prefix (49 vs 32 chars) is load-bearing for frame timing: sleep(23)+2+dream(49)=74 ≥ CO−8=72 fails the `topl.c update_topl` append gate, so C `more()`s the pending sleep line first (frame 110) and shows the dream alone with the wake (frame 111); the short awake text (23+2+32=57 < 72) appended instead.
**JS:** 2 files (hack.js +13/−4, sounds.js +1/−8) — under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (diagnosis from recorded session steps/keys/screens/RNG + one throwaway `/tmp` replay probe, since deleted from repo scope).
**Change:** `js/hack.js` — `You_hear` ports the Unaware arm verbatim via `youprop.h:399` (`(game.multi|0)<0 && (unconscious() || is_fainted())`, importing `teleport.js` `unconscious` — hoisted-function, cycle-safe per `imports.mjs --can` — and extending the existing `eat.js` import with `is_fainted`; both JS bodies already match C) plus the `(Deaf && !Unaware)` gate; Underwater «barely hear» stays named-deferred. `js/sounds.js` — local `You_hear` clone deleted, `You_hear` joins the existing `./hack.js` import (no new edge; clone-drift per runbook §7).
**Verify:** `node scripts/verify.mjs --fn zapyourself` → `PASS syntax 2 changed js file(s): js/hack.js js/sounds.js` · `PASS rule2` · `PASS hidden verify zapyourself: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-death-Knight-92188 moved 17→mcalcmove@20 — D-2064's move, re-measured; scen-wish-Rogue-92210 moved 110→unstuck@120 — sleep-only + dream+wake frames now byte-exact) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight was `git status --short` (clean at `dda50839`) + `brief.mjs`; `--no-cohort` preflight skipped for call budget — green/strict/cohort/full all re-measured post-port. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed).
**Named:** Underwater «You barely hear»; `You_feel`/`You_see` dream arms (pre-existing defers); file-local `You_hear` clones still live in dbridge/do/dokick/dothrow/fountain/lock/mhitm/mhitu/mthrowu/music/trap/zap (+ `You_hear_apply`/`_meat`/`_yell` variants) — same one-line predicate, no blocked session reaches them (map `turns.md` pline section updated).
**Next:** pop the next Open row (`mon.c` mcalcmove). Rogue now feeds `mon.c` `unstuck`@120 (the `rnd(2)`@`unstuck(mon.c:3465)` vs `rn2(6)` pair `rng-diff` already flags post-divergence); Knight feeds the existing `mcalcmove` row.
## 2026-09-08 — D-2064 dozap self-zap fatal losehp never killed (queue owner zapyourself, writer dozap)

**C locus:** the queue owner names where C printed; the writer is the caller — `zap.c` dozap `:2658–2666` (`else if (need_dir && !u.dx && !u.dy && !u.dz) { if ((damage = zapyourself(obj, TRUE)) != 0) { ... losehp(Maybe_Half_Phys(damage), buf, NO_KILLER_PREFIX); } }`). C needs no death check there because `losehp()` ends the game inline (`done(DIED)`, noreturn); the `--More--`-pending «You die...» then surfaces after dismissal (step 18).
**JS:** 1 file (`zap.js` +8/−1) — under the 600/10 caps. Small because the C locus is that small (the omission is the missing death-processing, not a missing arm). Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (temporary `console.log` traces in `lock.js`/`zap.js` used to localize the stall were fully reverted; diagnosis from recorded session steps/keys/screens/RNG + replay probes only).
**Change:** `js/zap.js` — after the self-zap `losehp`, mirror the `backfire` arm: `if (game._losehp_needs_done || game.program_state?.gameover) { await finish_losehp_done(); if (game.program_state?.gameover) return 1; }`. No new import (`finish_losehp_done` already top-level from `./end.js`); no new module edge.
**Verify:** `node scripts/verify.mjs --fn zapyourself` → `PASS syntax 1 changed js file(s): js/zap.js` · `PASS rule2` · `PASS hidden verify zapyourself: 0 PASS, 1 moved past, 1 unchanged, 0 worse → PROGRESS` (scen-death-Knight-92188 moved 17→mcalcmove@20 — fire/smoulder/death screens now match; scen-wish-Rogue-92210 still zapyourself@110) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit. `hidden-corpus/scoreboard.json` dirt from probe `hidden-proxy` runs reverted before handoff (not committed).
**Named:** `dozap` `spe<0` turn-to-dust/`useupall`, `update_inventory`, `check_capacity`, `check_unpaid` (pre-existing defers, header + `c-js-map/turns.md` updated); Rogue sleep/dream path untouched (see Next).
**Next:** scen-wish-Rogue-92210 still blocked on zapyourself@110 — needs its own Open row for `sounds.c` dosounds: the fountain arm prints the awake `You_hear` while asleep (C dreams it: «You dream that you hear water falling on coins.») and concatenates behind the sleep ray instead of paging behind its `--More--`. Knight now feeds the existing `monmove.c` mcalcmove row (moved 17→20).
## 2026-09-08 — Audit reviews 1027–1033 (D-2057…D-2063) + cadence score

**Scope:** 7 JS-touching SHAs since audit 744aa202 (`a223472a`,
`61843507`, `0a8386e7`, `69ae5b11`, `9151bc52`, `a5d86687`,
`84dc0e34`); 2 park commits skipped (docs-only, no js/).
**Method:** pinned-C audit per SHA (`csym` bodies + callers,
`sym.mjs` on every re-pointed symbol, banned-pattern grep,
`imports.mjs --rulecheck`, independent `hidden-proxy verify
--base HASH~1` re-run for each claim). All 7 re-runs reproduce the
D-log summary lines exactly (1027/1028/1029/1030/1032 byte-identical
sessions; 1031 +1 later-queued row moving forward; 1033 0 PASS /
2 moved-past to later owners). Deepest checks: 1028
`glyph_is_monster` macro disjunct-for-disjunct + worm-tail
arithmetic; 1029 `Sick`=uprops-intrinsic TIMEOUT semantics;
1030 `mk_named_object` clone matched to `mkobj.c:2251–2267`;
1032 `enl_msg` present/past macro driving the survived block;
1033 do_vampyr RNG short-circuit case analysis + `mndx` provenance.
**Result: 7 ACCEPT, 0 Must-fix.** Observations only (unreachable
`make_sick` SICK_ALL fallback; loose Hate_silver citation).
**Next:** pop the next Open row (`zap.c` zapyourself).
## 2026-09-08 — D-2063 polyself controlled-name article + POLY_MONSTER vampire shape change (queue owner polyself)

**C locus:** `polyself.c` polyself `:596–615` (!polyok message arm: `pmname(flags.female)` then `the_unique_pm`→`the()` / `!type_is_pname`→bare / else `an()`; own-race-nonunique + own-role guard forces newman instead) + `:511` (`monsterpoly && isvamp` goto do_vampyr) + do_vampyr re-pick (leader `rn2(10)`→wolf else `rn2(4)`→fog/bat, cham `rn2(2)` override) + `controllable_poly` y_n «Become %s?» + PM_HUMAN→newman else polymon, goto made_change; `controllable_poly = Polymorph_control && !(Stunned || Unaware)` `:480`.
**JS:** 1 file (polyself.js +60/−10) — under the 600/10 caps. Extended existing edges only (objnam `the`/`the_unique_pm`; do_name `type_is_pname`; monsters `your_race`/`G_UNIQ`; getline `y_n`; const `NEUTRAL`) + one new SAFE edge (`eat.js` `Unaware`, hoisted-function per `imports.mjs --can`). Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (diagnosis from `brief.mjs` C body + recorded session steps/keys/screens/RNG only).
**Change:** `js/polyself.js` — article arm verbatim (`the_unique_pm`/`the`/`type_is_pname` + your_race/G_UNIQ guard); `controllable_poly` const (Stunned shape mirrors `hack.js` Stunned_prop `(u.HStun|0) || u.Stunned`); `vampyr_goto` skips getlin per `:511`; do_vampyr block with exact RNG short-circuit, NEUTRAL-gender y_n prompt (no getlin ran on this path), direct polymon/newman + return (no `sex_change_ok` wrap per C; `made_change` light bookkeeping stays deferred). Second disjunct covers C's `else if (draconian || iswere || isvamp)` when NOT (controllable || forcecontrol); dragon/were arms stay deferred with priority guards.
**Verify:** `node scripts/verify.mjs --fn polyself` → `PASS syntax 1 changed js file(s): js/polyself.js` · `PASS rule2` · `PASS hidden verify polyself: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (Archeologist moved 63→rehumanize@90; Caveman moved 67→dog_invent@115) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit.
**Named:** were/dragon-merge/POLY_REVERT; placeholder orc/elf/giant substitutes; mkclass_poly; controllable_poly getlin (non-force); post-loop isvamp/draconian goto (tryct<=0 random-name funnel); wizard rehumanize own-role; light-source bookkeeping (header + `c-js-map/turns.md` updated).
**Next:** pop the next Open row (`zap.c` zapyourself).
## 2026-09-08 — D-2062 final-disclosure attributes tail: were-form/Hate_silver + survived arms + set_uasmon DRAIN_RES (queue owner one_characteristic, misattributed)

**C locus:** the queue owner is a literal-match misattribution — `one_characteristic` (`insight.c:926` `enl_msg`, the matched topline literal) already matches byte-exact incl. «18/02 (limit:18/100)». True writers: (1) `attributes_enlightenment :1881–1896` were-form + `Hate_silver` arms; (2) `attributes_enlightenment :1980–2005` final<2 / final==2 survived/dead block (`enl_msg(You_, "have been killed ", p, buf, "")`); (3) `set_uasmon` DRAIN_RES PROPSET via `mondata.c resists_drli :201–211` (human-form ulycn arm `:206–207`) + `attrib.c is_innate` FROM_LYCN `:885` + `from_what` « due to your lycanthropy» `:946`. Healer chain: werewolf bite → `set_ulycn` → `set_uasmon` sets `uprops[DRAIN_RES]|=FROMFORM` → `Drain_resistance` true with `is_innate==FROM_LYCN`.
**JS:** 3 files (invent.js +52/−9, polyself.js +27/−2, insight.js +1/−1) — under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (diagnosis from recorded C menu pages/keys/recipes + rowDiffs + C bodies only).
**Change:** `js/invent.js` — (a) ulycn were-form arm (`an(pmname(mons(ulycn), female?FEMALE:MALE))` + « in beast form» + wizard `mtimedone` iff `umonnum==ulycn`) and `Hate_silver` arm (`ismnum(ulycn) || hates_silver(youmonst.data)`), in exact C order between `Polymorph_control` and Fast; (b) the full `:1980–2005` block (final<2 «survived» / «survived after being killed »+`N_times` / in-progress null when `umortality==0`; final==2 «are dead» + (N>1) `ordin` suffix). `js/polyself.js` — file-local `resists_drli_you(mdat)` (undead/demon/were form, ulycn, PM_DEATH via `monsterNames.indexOf` idiom, vampshifter) wired as `propset_fromform(DRAIN_RES, 'HDrain_resistance', …)` in C PROPSET order after STONE_RES. `js/insight.js` — `N_times` exported for the shared import.
**Verify:** `node scripts/verify.mjs --fn one_characteristic` → `PASS syntax 3 changed js file(s): js/insight.js js/invent.js js/polyself.js` · `PASS rule2` · `PASS hidden verify one_characteristic: 2 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-normal-Caveman-92006 PASS; scen-wish-Healer-92066 PASS; scen-normal-Barbarian-92208 moved 14→really_done@19; scen-normal-Tourist-91122 moved 31→save_dungeon@37) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit.
**Named:** Upolyd foreign-shape (`vampshifted`/`polymorphed into` + wizard `mtimedone`), `lays_eggs`, `Unchanging` block, `Free_action`, `Fixed_abil` (same envelope, no blocked session reaches them); `defended(AD_DRLI)` disjunct of `resists_drli` (no JS `defended` export); `impossible("dead without dying?")` (no JS counterpart); in-progress final=0 `umortality>0` «have been killed N times» arm ported but unreached by corpus.
**Next:** pop the next Open row (`monmove.c` distfleeck).
## 2026-09-08 — D-2061 really_done first-move death line "Do not pass Go" (queue owner really_done)

**C locus:** `end.c` really_done `:1186–1187` — `if (svm.moves <= 1 && how < PANICKED && !done_stopprint) pline("Do not pass Go.  Do not collect 200 %s.", currency(200L));` after `dump_open_log`/achievements, before `wait_synch`/signals/`bones_ok`. `how < PANICKED` separates deaths from non-deaths (`hack.h:480`); `currency` handles hallucination/plural.
**JS:** 1 file (`end.js` +9/−0) — under the 600/10 caps. No new imports (`pline`, `currency`, `PANICKED` already imported); Rule #2 clean; no DIAG/FORCE/seed gates; no committed probes (diagnosis from `brief.mjs` C body + `hidden-proxy show` recorded screens only).
**Change:** `js/end.js` — after `at_midnight`, `if (((game.moves | 0) <= 1) && how < PANICKED && !(game.program_state?.done_stopprint | 0)) await pline(\`Do not pass Go. Do not collect 200 ${currency(200)}.\`);` with C `:1186–1187` citation (`game.moves` ↔ `svm.moves` per `allmain.js:998`; `how < PANICKED` ordering matches `const.js:507–522` vs `hack.h:494`). Achievements + `dump_open_log` stay named omissions; `wait_synch`/signals noted as no-JS-counterpart (later `flush_topl_more` surfaces the line before `disclose`, matching C order).
**Verify:** `node scripts/verify.mjs --fn really_done` → `PASS syntax 1 changed js file(s): js/end.js` · `PASS rule2` · `PASS hidden verify really_done: 3 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-death-Ranger-92114, scen-genesis-Archeologist-91132, scen-wish-Monk-91136 all PASS) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit.
**Named:** final-achievement tracking (`uachieved`/beginner/`ASCENDED` → `record_achievement`) + `dump_open_log`/livelog + `wait_synch`/POSIX signals (no JS counterpart; header `dump/livelog` omit unchanged); killer-based arise (`end.c:326–340`), DUMPLOG, and the rest of the D-2060 list unchanged.
**Next:** pop the next Open row (`insight.c` one_characteristic).
## 2026-09-08 — D-2060 stoning deaths made a CORPSE where C statues: ugrave_arise maintenance + savebones LEAVESTATUE + drop_upon_death cont arm (queue owner drop_upon_death)

**C locus:** `end.c` really_done `:1206–1219` (maintain `ugrave_arise` even for `!bones_ok`: PANICKED → NON_PM−3, BURNING/DISSOLVED → NON_PM−2, STONING → LEAVESTATUE, TURNED_SLIME → PM_GREEN_SLIME unless genocided) gates the grave fork (`end.c:1306`: corpse via `mk_named_object(CORPSE,…)` only when `ugrave_arise == NON_PM`); `bones.c` savebones `:480–489` LEAVESTATUE arm (`mk_named_object(STATUE, &mons[u.umonnum],…)` then `drop_upon_death(0, otmp,…)`); `bones.c` drop_upon_death `:259–303` (`rn2(5)` curse per item; `add_to_container` statue arm with no `rn2(8)` gate; `cont->owt = weight(cont)` after the loop).
**JS:** 1 file (`end.js` +50/−19) — under the 600/10 caps. No DIAG/FORCE/seed gates; Rule #2 clean. No committed probes (diagnosis from recorded C stepFns/RNG/recipes + C bodies only).
**Change:** `js/end.js` — (1) `really_done` maintains `ugrave_arise` per C `:1206–1219` (PANICKED/BURNING+DISSOLVED/STONING/TURNED_SLIME+`G_GENOD` check via `game.mvitals`, `PM_GREEN_SLIME` via `monsterNames.indexOf` like D-2057; killer-based arise `end.c:326–340` stays named), which also un-breaks the existing corpse gate (`ariseUnset`) for statue/burnt deaths; (2) `savebones` ports the LEAVESTATUE arm (`mk_named_object(STATUE, mons(u.umonnum),…)` + `drop_upon_death(null, statue,…)` + `!statue` return, shared bones tail, no ghost); (3) `drop_upon_death` splits the `cont` arm to `add_to_container` (no `rn2(8)` gate, C `:294–295`) and appends `if (cont) cont.owt = weight(cont)` (C `:301–302`); `LEAVESTATUE/DISSOLVED/TURNED_SLIME/G_GENOD` join the existing const.js edge, `add_to_container`/`weight` the existing mkobj.js edge — no new module.
**Verify:** `node scripts/verify.mjs --fn drop_upon_death` → `PASS syntax 1 changed js file(s): js/end.js` · `PASS rule2` · `PASS hidden verify drop_upon_death: 3 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (all three blocked sessions PASS) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `VERIFY: PASS` (no shared file changed, full-suite auto skipped). Intermediate: first verify round failed on a `(void) expr` syntax slip (JS has no C-style void cast); fixed to `void expr`, final verify ran after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit.
**Named:** killer-based undead arise (`end.c:326–340` wraith/mummy/zombie/vampire/ghoul — no blocked session reaches it); savebones undead-arise mtmp arm (`makemon` + `add_to_minv` + `m_dowear`); `drop_upon_death` lamp `artifact_light`/`end_burn` arm, `obj_no_longer_held` (no JS equivalent), mtmp `add_to_minv` arm (still places).
**Next:** pop the next Open row (`end.c` really_done).
## 2026-09-08 — D-2059 mhitu AD_HEAL + AD_PEST: nurse hitmsg + Pestilence reach-out/diseasemu (queue owner use_pole, misattributed)

**C locus:** the queue owner is a literal-match misattribution — `c-index` `matchMessage` strips `--More--`, then matches «You miss Pestilence.» against every double-quoted C literal *including* the `apply.c:3530–3534` comment's `"you miss"` (fixed 7), which outranks the real `missum` `"miss %s."` (`uhitm.c:5209`, fixed 5). `use_pole`'s body never prints that line (its miss arm prints «You miss; there is no one there to hit.»). True writers, via `mhitu.c` `hitmu :1144` → `mhitm_adtyping`: (1) `uhitm.c` `mhitm_ad_pest :3808–3834` mhitu arm — `pline_mon` reach-out plus `diseasemu(pa)`, normal damage kept; (2) `uhitm.c` `mhitm_ad_heal :4307–4378` mhitu arm — armored non-healer → `hitmsg`; (3) `mhitu.c` `diseasemu :1032–1043`.
**JS:** 1 file (`mhitu.js` +131/−6) — under the 600/10 caps. No DIAG/FORCE/seed gates; Rule #2 clean. No committed probes (diagnosis from recorded C screens/RNG/keys/recipes + C bodies only).
**Change:** `js/mhitu.js` — file-local `diseasemu` + `mhitm_ad_pest_u` + `mhitm_ad_heal_u` in the `mhitm_ad_famn_u` shape, wired into `mhitm_adtyping_u` in exact C order. Pest: `pline_mon` reach-out + `diseasemu`, leftover `d()` kept, no `hitmsg` (C goes straight to `pline_mon`, like FAMN). Heal mhitu arm in full: `mcan`/poly-petrify → `hitmsg`; naked gate (no `WEAPON_CLASS`/`is_weptool` wielded and no `uarmu`/`uarm`/`uarmc`/`uarms`/`uarmg`/`uarmf`/`uarmh` — the oclass gate matches `sounds.c` `MS_NURSE`) → heal `pline_mon` + `rnd(7)` HP (`mh` variant when poly'd) + `rn2(7)` max-HP bump capped at `5*ulevel+d(2*ulevel,10)` with `uhppeak` + `rn2(3)` STR/CON exercise + Sick cure + disp/flags `botl` + `rn2(13)` `mongone` (`M_ATTK_DEF_DIED`) else `rn2(33)` `rloc`+`monflee` (`M_ATTK_HIT|M_ATTK_DEF_DIED`) else damage 0; armored healer-role verbalize every 5th move (`SetVoice` is a no-op without SND_LIB, D-1752), armored others `hitmsg`.
**Verify:** `node scripts/verify.mjs --fn use_pole` → `PASS syntax 1 changed js file(s): js/mhitu.js` · `PASS rule2` · `PASS hidden verify use_pole: 1 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (wish-Rogue-91138: PASS; genesis-Archeologist-91127: moved 62 → mhitm_ad_stun@67; genesis-Barbarian-91118: moved 87 → mhitm_ad_stun@92) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `VERIFY: PASS` (no shared file changed, full-suite auto skipped). Final verify ran after the last edit (no D-1831 gap).
**Named:** `use_pole` body untouched (already C-faithful; statue/boulder/furniture/miss arms live). The `mhitm_ad_stun` mhitu arm (the two Pestilence sessions' later owner) and the remaining unported `mhitm_ad_*` arms stay named in the dispatch comment.
**Next:** the two Pestilence residuals' later owner `mhitm_ad_stun`@67/@92 (stun mhitu arm: `hitmsg` + stun — own future row; D-2033's Tourist-92067 already pointed there). Pop the next Open row (`bones.c` drop_upon_death).
## 2026-09-08 — D-2058 #terrain browse describe: reveal int-glyph threading + gather_locs MONS displayed-glyph (queue owner def_char_is_furniture, misattributed)

**C locus:** the queue owner is a literal-match misattribution — «branch staircase up» is `defsym.h:124` PCHAR text, and C `def_char_is_furniture` (drawing.c:120–142) runs only on the crystal-ball gaze path (`detect.c:1342`), which none of these recipes touches (RNG fully matched both sides proves no gaze). True writers: (1) `detect.c` `reveal_terrain_getglyph` — returns the **int** glyph (C `show_glyph` stores it in gbuf; `do_screen_description`/`lookat` then reads `glyph_at`, the *shown* glyph). (2) `getpos.c` `gather_locs_interesting` GLOC_MONS — matches `glyph_is_monster(glyph_at(x,y))` on the **displayed** map, so during a monster-stripped browse nothing matches and `m` stays put.
**JS:** 2 files (`display.js` +64/−13, `getpos.js` +12/−4) — under the 600/10 caps. No DIAG/FORCE/seed gates; Rule #2 clean. No committed probes (diagnosis from recorded C screens + C bodies + one stash A/B replay).
**Change:** `js/display.js` — thread the C int id through every `reveal_terrain_getglyph` arm so gbuf matches C: `full` arm attaches `back_to_glyph(x,y)` inside the seenv-temp window; `levl_glyph` attaches the remembered int (`mem.glyph`, unseen-BSS-0 ≡ stone default) or `back_to_glyph`/stone-default id; trap-restore attaches `trap_glyph().glyph`; strip arms attach `back_to_glyph` (under the lastseentyp temp where C temps) or the stone default; unclassified cells keep `GLYPH_UNEXPLORED` (C `glyph_at`, never the stone default); new `copy_glyph_id` preserves the id across the swallowed/monster-strip copies (`copy_glyph` drops it — that cost one debug round); `reveal_terrain_cmap_hack` remaps the travelling id (`S_darkroom`→`S_room`, `S_litcorr`→`S_corr`) exactly like C's int compare. `js/getpos.js` — GLOC_MONS now matches `glyph_is_monster(glyph_at(x,y))` with the C long-worm-tail male/female-bank exclusion (`GLYPH_MON_MALE_OFF/FEM_OFF` join the existing display.js import — no new edge). No RNG calls added (all id constructors are pure); no behavior change outside reveal/browse (normal newsym path already stored ints per D-1765).
**Verify:** `node scripts/verify.mjs --fn def_char_is_furniture` → `PASS syntax 2 changed js file(s): js/display.js js/getpos.js` · `PASS rule2` · `PASS hidden verify def_char_is_furniture: 2 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (kit-Archeologist-92170: PASS; Knight-91100: PASS; Rogue-92209: moved 134 → peffect_sickness@174) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Intermediate: display-only fix gave kit PASS + Rogue 230→231 with Knight unmoved («2 food rations» — the live-monster jump), which localized the gather_locs hunk. Stash A/B: base Rogue loses 2 screens (first @134) vs fixed losing only @174 (RNG 4527/4527 both sides) — @174 is a quaff-message frame ~40 steps past the browse in potion logic this diff does not touch, with every intermediate screen green, so it reads as pre-existing-masked rather than a regression (machine verdict: PROGRESS, 0 worse).
**Named:** `def_char_is_furniture` itself untouched — the review-81 crystal-ball gap (JS `'<>_{|\\'`, missing `}` fountain + full defsyms scan) is a named map omit with no corpus session behind it (no gaze in any blocked recipe; a faithful port is provable NO MOVEMENT here). GLOC_OBJS/DOOR/EXPLORE/INTERESTING still read live/shown hybrids (`look_shown_at`, `shown_door_cmap`) rather than `glyph_at` ints — same bug class, unexercised by these sessions, own future row.
**Next:** Rogue-92209's later owner (`peffect_sickness`@174, quaff-message logic — revealed downstream, own future row). Pop the next Open row (`zap.c` obj_resists).
## 2026-09-08 — D-2057 statusline stat writers: acurr CHA floor + poly giant Str + ring-takeoff ABON (queue owner do_statusline1)

**C locus:** symptom owner `botl.c` `do_statusline1` `:47–98` (`:85` `St:%s…Ch:%-1d` via `get_strength_str()` + `ACURR()`); writers: (1) `attrib.c` `acurr` `:1214–1216` CHA arm — `if (tmp < 18 && (youmonst.data->mlet == S_NYMPH || u.umonnum == PM_AMOROUS_DEMON)) result = 18`; (2) `polyself.c` `uasmon_maxStr` `:1100–1114` + `polymon` `:820–832` — `newMaxStr = R ? R->attrmax[A_STR] : live_H ? STR19(19) : STR18(100)` with `live_H = is_giant(ptr) && !is_undead(ptr)`; (3) `do_wear.c` `armor_or_accessory_off` `:1809–1817` (`off_msg` then `Ring_off`) + `adjust_attrib` `:1223–1243` (`ABON(which) += val`) via `Ring_on/off` `:1316–1318`/`:1415–1417` (`RIN_GAIN_STRENGTH → adjust_attrib(A_STR, ±spe)`).
**JS:** 3 files (`attrib.js` +9/−1, `polyself.js` +6/−1 incl. 2 import names + 2 const names, `do_wear.js` +4/−10) — under the 600/10 caps. No DIAG/FORCE/seed gates; Rule #2 clean.
**Change:** `js/attrib.js` — CHA arm now `if (tmp < 18 && (game.youmonst?.data?.mlet === 'S_NYMPH' || (u.umonnum|0) === PM_AMOROUS_DEMON)) result = 18` (C `:1214–1216`; `PM_AMOROUS_DEMON` via `monsterNames.indexOf`, same idiom as `js/apply.js:255`; extends the existing `generated/monsters_data.js` edge, no new module). `js/polyself.js` — `uasmon_maxStr` strongmonst branch now `liveH = is_giant(ptr) && !is_undead(ptr); return R ? attrmax : liveH ? STR19(19) : STR18(100)` (C `:1100–1114`; `is_giant`/`is_undead` join the existing `monsters.js` import, `STR18/STR19` join the existing `const.js` import — no new edges).
**Verify:** `node scripts/verify.mjs --fn do_statusline1` → `PASS syntax 3 changed js file(s): js/attrib.js js/do_wear.js js/polyself.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify do_statusline1: 1 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-normal-Wizard-91114: PASS; scen-poly-Priest-92097: moved 125 → steal_it@152; scen-wish-Archeologist-92038: moved 185 → newcham@188) · `PASS green 2/2` · `PASS strict` ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Plus `node frozen/ps_test_runner.mjs sessions` → 44/44 PASS (fortress held). Final verify ran after the last edit.
**Named:** `acurr` Ogresmasher CON→25 still deferred (pre-existing map omit, untouched); `uasmon_maxStr` orc/elf/dwarf/gnome remap + `#if 0` human passthrough retained as in C; `armor_or_accessory_off` armor/amulet/blindfold paths untouched; `do_statusline1` pad/score arms untouched (row-22 spacing already matched on all three).
**Next:** the two later owners (steal_it@152; newcham@188) — revealed downstream, own future rows. Pop the next Open row (`drawing.c` def_char_is_furniture).
