# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — D-2308 `shk.c` shopdig anger arms: rile_shk bill surcharge + digactualhole make_angry_shk

**C locus:** `shk.c` `shopdig` `:5018–5110` (curse/growl arm calls `rile_shk`); `rile_shk` `:1362–1377` (NOTANGRY=FALSE + `(price+2)/3` bill walk); `dig.c` `digactualhole` `:821–822` (`if (mtmp->isshk) make_angry_shk(mtmp, 0, 0)` before `migrate_to_level`).
**JS:** 2 files (`shk.js` +8/−2, `dig.js` +7/−3 incl. doc-ledger updates), far under the 600/10 caps.
**Change:** `js/shk.js` only: `rile_shk` ports the walk verbatim in C order over the live `bill_p`/`bill` shape (same `||` fallback as `addupbill`; `| 0` integer idiom). `js/dig.js` only: the `teleport_pet` success arm calls the already-live `make_angry_shk` via the file's existing dynamic `shk.js` import (no new module edge, no `imports.mjs --can` needed); header envelope + Named-omit ledger updated. Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `verify --fn shopdig` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool-gated) · VERIFY: PASS. No hand probe: `rile_shk` is module-private (no production export added for testability) and both arms are straight-line C-order ports over live callees (`make_angry_shk` already exercised via its dokick/dothrow/teleport/trap callers); surcharge arithmetic spot-checked (price 10→14, 1→2, 0→0 both sides).
**Named:** `SetVoice` in `shopdig` warn (pre-existing); `#if 0` nolimbs curse/rile early-return (C-disabled, both sides); remaining `digactualhole` ledger (`buried_ball_to_punishment`, `ship_object`, shop `add_damage`, `liquid_flow` — other rows). Seen while reading, untouched: `dig()` `:520` `unblock_point` vs JS `recalc_block_point`, `:528–533` earth-debris elemental still deferred.
**Next:** do not re-pop shopdig. Falsifier: a rescore or fresh `verify` showing a session blocked with a shopdig/rile/make_angry owner.
## 2026-09-15 — D-2307 `dig.c` bury-family deferred arms: end_burn/maybe_unhide_at/buried-ball/delfloortrap/damage-chains/pooleffects

**C locus:** `dig.c` `bury_an_obj` `:1982–2047` (`end_burn` `:2011–2012`); `bury_objs` `:2049–2081` (`maybe_unhide_at` `:2074`); `unearth_objs` `:2084–2112` (`buried_ball` `:2091`, punish arm `:2096–2099`); `liquid_flow` `:837–879` (`u_at` `:843`, `delfloortrap` `:857`, damage chains `:868–872`, `pooleffects` `:875`). `buried_ball{,_to_punishment,_to_freedom}` + `rot_organic` re-read; bodies already faithful, untouched.
**JS:** 2 files (`dig.js` +56/−27 incl. doc-ledger updates, `zap.js` +1/−1 `await unearth_objs` in async `melt_ice`), far under the 600/10 caps.
**Change:** `js/dig.js` only (+ `js/zap.js` 1-line `await`): `end_burn` via dynamic `timeout.js` import (sync export; `stop_timer` runs `cleanup_burn` — C `timeout.c` order); `maybe_unhide_at` joins the existing `monmove.js` static import; `unearth_objs` now async with the `bball`/`TT_BURIEDBALL` arm verbatim (same-module `buried_ball_to_punishment`); `liquid_flow` in C order — `u_spot` hoisted pre-return, `delfloortrap` (already imported), `await unearth_objs`, `fire_damage_chain(objchain, TRUE, TRUE, x, y)` / `water_damage_chain(objchain, TRUE)` join the existing `trap.js` import, `pooleffects(FALSE)` via dynamic `pickup.js` import (file's convention for not-yet-imported modules). `imports.mjs --can` for the three cross-module names: monmove ALREADY, trap/pickup/timeout same 90-module SCC, call-time use only. Sanity-check `impossible` kept soft per file convention (`:1397` precedent).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_bury.mjs` PROBE PASS 16/16 (arms no corpus session reaches; deleted after run): bury/unearth round-trip with coords+where; ROCK merge; candle (burn timer + light) and magic lamp (light, no timer) bury with zero light-source leak; trapped ball within C's dist2≤8 search radius re-punished (`uball` set, not floor/buried); `liquid_flow` ROOM early-return + POOL path complete. Probe detours worth recording: `const { game } = await import(gstate)` snapshots `export let game` (took `{}`); static `import { game }` stays live — and C `buried_ball_to_punishment` searches from the *hero*, so a far-away test ball is correctly ignored (verified vs C before adjusting).
**Named:** sanity-check `impossible` text (file-convention soft); CORPSE-under-ice (C TODO `:2026–2028`, copied); RUST_METAL (`#if 0` both sides); `trap_ice_effects` in `melt_ice` (other row); `spoteffects`/`boulder_hits_pool` in `melt_ice` (pre-existing); `liquid_flow` caller in `digactualhole` (D-0954 row owns it).
**Next:** do not re-pop the bury family. Falsifier: a rescore or fresh `verify` showing a session blocked with a bury/unearth/liquid_flow owner.
## 2026-09-15 — D-2306 `cmd.c` `-`→fight binding missing from rhack dispatch (commands_init bind_key)

**C locus:** `cmd.c:2772` `(void) bind_key('-', "fight", FALSE)` in `commands_init` (unconditional — always installed, not num_pad-gated); extcmdlist `F` fight PREFIXCMD `:1722–1723`; `do_fight` `:1621–1634`; rhack `:3679` `cmdbind_get` dispatch; `cmd_from_func` `:3049–3052` skips `-` only for key display when `!num_pad` (behavior unaffected).
**JS:** 1 file (`cmd.js`, +9/−6), far under the 600/10 caps. Density note: the locus is the 1-line C bind over the already-ported `do_fight` arm — C is that small; value is the corpus PASS below.
**Change:** `js/cmd.js` only, no new module edge (`do_fight` same-module; no `imports.mjs --can` needed): `-` shares the `F` arm (`ch === 'F' || ch === '-'` → `do_fight`, `prefix_seen` = fight, C `:1621–1634` + `:2772` cited); the forcefight follow-up guard, the g/G rush-prefix guard, and the `menu_requested`-preserve gate all exempt `-` exactly like `F` (C: `-` IS the fight prefix, so F-then-`-` reaches `do_fight`'s "Double fight prefix, canceled." instead of the F-follow-up pline, and m-F-h keeps nopick); `rhack_repeat_command`/`rhack_repeat_txt` map `-`→`do_fight`/`fight` (Ctrl-A repeat parity with `F`). A user BIND= overlay on `-` still wins (overlay arm precedes the if/else, same as `F`). No RNG/message/control-flow change beyond C order; Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Direct replay `node frozen/ps_test_runner.mjs .cache/hidden/sessions/random-seed0015-valk-level2-pit-dog-wait-eb7e90ad.session.json`: before FAIL (RNG 8547/8547, Screen 70/72) → after PASS (8547/8547, 72/72). `node scripts/verify.mjs --fn rhack` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: ownerless step, 0 blocked at HEAD — NOT claimed as a corpus PASS; the row cited the session explicitly, proved by the direct replay) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (cmd.js not shared-gated) · VERIFY: PASS.
**Named:** `!`→doshell binding (2 corpus sessions, own future row); other number_pad/phone/swap_yz/pcHack dir layouts (pre-existing dokeylist omits); full CMD_gGF table (pre-existing turns.md:256).
**Next:** do not re-pop `-`→fight. Falsifier: a rescore or fresh `verify` showing a session blocked with a `-` Unknown-command step.
## 2026-09-15 — D-2305 `trap.c` disarm_squeaky_board + unsqueak_ok; try_disarm sobj_at; untrap door unblock_point

**C locus:** `trap.c` `unsqueak_ok` `:5606–5626`; `disarm_squeaky_board` `:5630–5660`; `try_disarm` boulder gate `:5456` (`sobj_at(BOULDER, …)`); untrap door set-off `:6079` (`unblock_point(x, y)`; C `vision.c:899` = `dig_point` + full-recalc flag). Audit basis: `disarm_holdingtrap` `:5553` / landmine `:5594` / shooting `:5664` / `help_monster_out` `:5700` / `disarm_box` `:5794` / `untrap_box` `:5821` vs the `js/trap.js` clones.
**JS:** 1 file (`trap.js`, +52/−14), far under the 600/10 caps. No committed probes (reads + `node --check` + `/tmp` probe only).
**Change:** `js/trap.js` only, no new module edge (`getobj`/`useup`/`consume_obj_charge` join the existing `invent.js` import; `GETOBJ_*` join `const.js`; `unblock_point` joins `vision.js`; `POT_OIL` follows the file's `objectNames.indexOf` convention; every callee same-module or already imported): `unsqueak_ok` verbatim (null→EXCLUDE; grease / dknown+`oc_name_known` oil→SUGGEST via `game.objects` table; other potions→DOWNPLAY; else EXCLUDE); `disarm_squeaky_board` verbatim (`getobj("untrap with", …)`; `bad_tool = cursed || ((not-oil || lamplit) && (not-grease || !spe))`; `try_disarm(ttmp, bad_tool)`; grease→`consume_obj_charge` else `useup`+`makeknown(POT_OIL)`; repair pline; `deltrap`; `newsym(ux+dx,uy+dy)`; `more_experienced(1, 5)`; `newexplevel`); `case SQKY_BOARD` wired in the `untrap` switch; boulder loop → `sobj_at(BOULDER, …)` (identical loop, canonical export); door set-off → `unblock_point` (identical outcome post-`D_NODOOR`, C-exact call). No RNG/message/control-flow change beyond C order; Rule #2 clean.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `verify --fn disarm_squeaky_board` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (trap.js not shared-gated) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `move_into_trap` (adjacent-Whoops; needs `test_move` export, D-1813); `stumble_on_door_mimic`. `disarm_squeaky_board` retired from the untrap doc ledger and data.md:176.
**Next:** do not re-pop untrap floor disarm_*/box. Falsifier: a rescore or fresh `verify` showing a session blocked with an untrap-envelope owner.
## 2026-09-15 — D-2304 `mklev.c` mktrap dispatcher + `mkmaze.c` populate_maze trap loop wired

**C locus:** `mklev.c:2036-2150` `mktrap` (invalid-args once-guard + paniclog; `m.x=m.y=0`; tm pool/lava abort; specified/rogue/hell-bias/`traptype_rnd`-loop kind select; hole→ROCKTRAP; tm-copy vs MAZEFLAG-mazexy/croom-somexyspace 200-try occupied/boulder loop; `maketrap`; post-tail) + static `traptype_roguelvl :2002-2030` (`rn2(7)` table); caller `mkmaze.c:1122-1123` (`mktrap(0, MKTRAP_MAZEFLAG, NULL, NULL)`).
**JS:** 1 file (`mklev.js`, +110/−7), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (reads only + `node --check`).
**Change:** `js/mklev.js` only, no new cross-module edge (every callee same-module or already imported — `MKTRAP_NOFLAGS/SEEN/MAZEFLAG` join the existing `const.js` flag import; `--can` correctly skipped): new `traptype_roguelvl` verbatim; new `mktrap(num, flags, croom, tm)` in C order (once-guard via module-level `mktrap_err`; tm pool/lava abort via the module `IS_POOL/IS_LAVA` idiom; specified → rogue (`Is_rogue_level` live import) → hellish `!rn2(5)` FIRE_TRAP → `traptype_rnd` loop; `is_hole`/`Can_fall_thru` ROCKTRAP; tm-copy vs mazexy/somexyspace 200-try `occupied`/`sobj_at(BOULDER)` loop; `maketrap`; tail delegated to live `mktrap_seen_victim` with `seen` derived from `MKTRAP_SEEN`); `populate_maze` gains the C 6th loop verbatim and the omit comment retires.
**Verify:** `node scripts/verify.mjs --fn mktrap` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` PASS on a clean tree before edits. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `mktrap` invalid-args `paniclog` write (file-only, no scored-JS equivalent under Rule #2; observable behavior — no trap — kept; C `mktrap_err` once-semantics mirrored); `traptype_rnd` LEVEL_TELEP `single_level_branch` (pre-existing Knox deferral, untouched); `makemaz` load_special-path `dmonsfree` (mon.c, own future row) and wizard SPLEVTYPE/`getenv` override (wizard-only env read, no plain-ESM equivalent; both already named at `js/mklev.js:1478/1511/1525`). Other `mktrap` call sites (`mklev.c:985` room path, `sp_lev.c:1845`) keep their existing live inlines — rewiring them is separate rows.
**Next:** do not re-pop populate_maze/mktrap (dispatcher + all six stock loops live). Future maze-level divergences attribute to their true writer.
## 2026-09-14 — Audit 66311ced..3010c0c3 (reviews 1261–1269: 8 ACCEPT, 1 ACCEPT-WITH-DEBT, 0 Must-fix) + cadence 44/44

**Scope:** nine JS SHAs since e6f16d72, one file per SHA written as each finished (D-2295 kill_genocided_monsters newcham; D-2296 mon_leave worm arm; D-2297 create_region box/nrects; D-2298 look_traps glyph-first; D-2299 replmon place_monster + birth flags; D-2300 place_wsegs callers; D-2301 materialnm helm; D-2302 SHOW skeleton + trim; D-2303 scatter restack). Every `hidden-proxy verify --base SHA~1` re-run by the reviewer (all 0-blocked, matching each D-log's vacuous note — no vacuous-PASS lie found). `imports.mjs --rulecheck` clean. Three `--can` verdicts came back stronger than the D-logs claimed (ALREADY, not same-SCC new edge: mon_leave/teleport, place_wsegs/do.js, sobj_at/explode).
**Findings:** 1268 ACCEPT-WITH-DEBT — MINVENT arm drops C's `ocarry->mx` nonzero gate (`zap.c:672–677`); pre-existing D-2157 debt, doubly-unreachable, display-only; map note, no Must-fix. 1269 draft flagged a verify gap from the commit message alone; the D-log entry holds the full matrix — retracted after re-reading (check the D-log, not the message, before calling a gap). D-2299 reviewed at its own tree (HEAD already had D-2300's follow-up).
**Cadence:** full `sessions` 44/44 at HEAD `3010c0c3` — Scr 11,405/11,405, RNG 792,838/792,838, `47+0.29/turn` (R² 0.79); aggregates identical to standing score. Green gate 2/2 re-run by reviewer.
**Queue:** 7 Open → 12 via 5 debt.md refill rows (bury family, shopdig, god_zaps_you, mkcavepos, conjoined_pits; all csym-confirmed + archive-deduped). Refill source per supervisor order (corpus queue exhausted); tension with the ≥90%-corpus map-singleton deferral noted — these rows are map-debt, pop corpus owners first if a rescore surfaces any.
**Next:** pop Open in order; do not re-pop audited arms (falsifiers in reviews).
## 2026-09-14 — D-2303 `explode.c` scatter MAY_FRACTURE boulder restack-to-top

**C locus:** `explode.c:776-790` (`fracture_rock(otmp); place_object(otmp, sx, sy); if ((otmp = sobj_at(BOULDER, sx, sy)) != 0) { obj_extract_self(otmp); place_object(otmp, sx, sy); }` with the `/* another boulder here, restack it to the top */` comment); callee `sobj_at` is `invent.c:1465-1475` (first `nexthere` match); `fracture_rock` is `zap.c:5536-5578` (its internal extract+place runs only when `where == OBJ_FLOOR`, a no-op on scatter's already-extracted otmp, so the scatter-level place is the single place — JS `js/dig.js:1557` matches via its `where === OBJ_FLOOR` gate).
**JS:** 1 file (`js/explode.js` only, ~+15/−7 with doc-ledger updates), far under the 600/10 caps. Density note: the locus is the 6-line C restack arm — C is that small; the rest of scatter shipped D-0986/D-1256/D-2274/D-2282.
**Change:** port the C arm verbatim in C order over live callees only — `otmp = sobj_at(BOULDER, sx, sy); if (otmp) { obj_extract_self(otmp); place_object(otmp, sx, sy); }` with the C comment cited. `sobj_at` joins the already-imported `mkobj.js` edge (`imports.mjs --can js/explode.js js/mkobj.js sobj_at`: ALREADY, no new edge, no TDZ risk); `obj_extract_self`/`place_object` already imported. No RNG/message/control-flow change beyond the C order; no DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn scatter` → PASS syntax (1 changed js file: js/explode.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Import smoke `node --input-type=module -e "import('./js/explode.js')/import('./js/mkobj.js')"` → `scatter: function`, `sobj_at: function` (nothing kept in tree; the arm is draw-free/message-free so no corpus movement is expected).
**Named:** VIS_EFFECTS (commented out in C too — `/* tmp_at ... */`).
**Next:** do not re-pop scatter restack. Falsifier: a rescore or fresh `verify scatter` showing a session blocked with it as owner.
## 2026-09-14 — D-2302 `light.c` do_light_sources hero at_hero_range trim + SHOW skeleton

**C locus:** `light.c:169–250` (`do_light_sources`): clear `LSF_SHOW` per entry (`:177`); `LS_OBJECT` range-0 flash short-circuits `get_obj_location` with SHOW set (`:187–190`), else locate via `get_obj_location` (`zap.c:653–689`, locflags 0); `LS_MONSTER` via `get_mon_location` (`zap.c:691–709`); the trim (`:204–211`, runs even when SHOW was cleared — stale positions still feed it); paint only when SHOW survives (`:213–250`, at-hero `COULD_SEE` arm vs `clear_path` arm).
**JS:** 1 file (`js/light.js` only, ~+55/−25), far under the 600/10 caps. Density note: the row's C scope is the 7-line trim plus the SHOW skeleton it rides on (D-2157 already shipped the 40-line ring) — C is that small; same class as the D-2157/D-2301 vacuous ships.
**Change:** port the C skeleton in C order over the existing inline refresh (conditions unchanged): file-local `const LSF_SHOW = 0x1` (light.c:41; the line-29 `COULD_SEE` precedent — no new module edge, no `imports.mjs --can` needed); `let at_hero_range = 0` + hoisted hero pos; clear-SHOW up front; FALSE arms zero `ls.{x,y}`; range-0 flash sets SHOW (trim then clears it at hero, since `0 >= 0`); the trim verbatim incl. running on cleared SHOW; `if (!(flags & LSF_SHOW)) continue` before the untouched ring/paint arms. No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn do_light_sources` → PASS syntax (1 changed js file: js/light.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Hand probe (`/tmp/probe_light_trim.mjs`, 24 checks, deleted before finishing — arms no corpus session reaches): r3+r2 dup trims to r3-identical cells with second SHOW cleared; r2+r3 both SHOW; range-0 flash at hero paints nothing, off-hero paints its cell; dead entries zeroed; unknown-type stale hero pos still feeds the trim (C order); migrating/undefined-mx monster entries zeroed with no paint.
**Named:** `LSF_NEEDS_FIXUP` (unchanged); `get_*_location` refinement arms — youmonst/usteed identity, `mburied` gate, `OBJ_BURIED`/`OBJ_CONTAINED` (JS refreshes inline, D-2157); the pre-existing `lamplit` gate on `LS_OBJECT` refresh (C locates unlit sources too, but nothing creates them — `end_burn`/`del_light_source` remove on unlight — so lifting it is out of scope).
**Next:** do not re-pop `do_light_sources` trim/SHOW. Falsifier: a rescore or fresh `verify do_light_sources` showing a session blocked with it as owner.
## 2026-09-14 — D-2301 `trap.c` burnarmor case-0 `materialnm` helm prefix ("iron helm")

**C locus:** `trap.c:116–123` (`mat_idx = objects[item->otyp].oc_material; Sprintf(buf, "%s %s", materialnm[mat_idx], helm_simple_name(item))`; no helm → literal `"helmet"`); table `decl.c:90–94` (22 words in objclass.h enum order, NO_MATERIAL=0 … MINERAL=21).
**JS:** 1 file (`js/trap.js` +22/−5: table + descr build + doc-ledger update), far under the 600/10 caps. Density note: the locus is one 8-line C arm plus its 22-word table — C is that small; the rest of burnarmor shipped D-1009/D-2186/D-2190.
**Change:** file-local `const materialnm` (22 words, C order, `decl.c` C-ref; eat.js `foodwords` precedent — no new module edge, no `imports.mjs --can` needed; the C `nhlobj.c:222` Lua "material" use is out of scope and noted at the table). Case 0 builds `descr` exactly like C (`item ? buf : 'helmet'`; `?? 'mysterious'` only guards a missing objects-table entry, which C never has). Display-only arm: review 1152 measured zero RNG on these paths either side.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn burnarmor` → PASS syntax (1 changed js file: js/trap.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Table check: inline `node -e` word-diff of the C `decl.c` table vs the JS clone → 22/22 TABLE MATCH (nothing kept in tree).
**Named:** `grease_protect` polish stays named (pre-existing); `nhlobj.c:222` Lua "material" entry stays out of scope (noted at the table).
**Next:** do not re-pop `materialnm`/`burnarmor` helm prefix. Falsifier: a rescore or fresh `verify burnarmor` showing a session blocked with it as owner.
## 2026-09-14 — D-2300 `worm.c` place_wsegs restore/replmon callers wired (replmon takeover + getlev occupancy)

**C locus:** `worm.c:614–635` `place_wsegs` (body already live in `js/worm.js`); callers `mon.c:2536–2537` (`if (mtmp2->wormno) place_wsegs(mtmp2, mtmp)` after `place_monster`, post-`relmon` → `mon_leaving_level` → `remove_worm`) with the `mtmp2->wormno = mtmp->wormno` inheritance at `zap.c:783` (JS `js/zap.js:2794` already live); `restore.c:1192–1197` getlev loop (`place_monster`, `place_wsegs(mtmp, NULL)`, `hideunder`); `worm.c:471` `cutworm` site already live (`js/worm.js:339`, D-1570).
**JS:** 3 files (`js/mon.js` +1 arm, `js/do.js` +1 arm + import, `js/worm.js` header ledger only), far under the 600/10 caps. Density note: the row's C scope is two 1-line call sites on an already-live 20-line body (cutworm site + zap wormno copy verified live) — C is that small; nothing else in the locus family is missing.
**Change:** both arms in C order over live callees only — `if ((mtmp2.wormno | 0)) place_wsegs(mtmp2, mtmp)` after the steed-gated `place_monster` in `replmon`; `if ((mtmp.wormno | 0)) place_wsegs(mtmp, null)` between `place_monster` and `hideunder` in `getlev_place_monsters`. `place_wsegs` joins the already-imported `worm.js` edge in `mon.js`; the `do.js` → `worm.js` edge is new but `imports.mjs --can` verdict SAFE (hoisted function, same 90-module SCC, call-time use only; `do.js` top-level reads only generated names). `void impossible` fire-and-forget inside `place_wsegs` keeps both callers sync like C.
**Verify:** preflight `node scripts/verify.mjs --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn place_wsegs` → PASS syntax (3 files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) · VERIFY: PASS. No hand probe: both arms are 1-line C-order wirings over live helpers with no corpus reach, and the shared-file full-44 green is the regression proof (same class as the D-2299 vacuous ship).
**Named:** replmon light-source swap, full `replshk` bill, `set_ustuck` botl edge, `dealloc_monst` (GC) stay named (D-2299); save/rest wseg chain (`rest_worm`/`save_worm`) stays named; getlev ghostly peace remap / `set_malign` stays named.
**Next:** do not re-pop `place_wsegs` restore/replmon for the shipped sites. Falsifier: a rescore or fresh `verify place_wsegs` showing a session blocked with it as owner.
## 2026-09-14 — D-2299 `mon.c` replmon place_monster arm + `makemon.c` birth flags (mgenmklev/MM_MINVIS live; makemon grid deferred to its own row)

**C locus:** `mon.c:2515–2563` `replmon` (inventory check `:2520–2524`, polearm `:2525–2527`, `relmon` `:2530` via `mon_leaving_level` `:2696–2720`, `place_monster(mtmp2, mtmp2->mx, mtmp2->my)` `:2533–2535`, `place_wsegs` `:2536–2537`, light, fmon prepend, ustuck/usteed, replshk, dealloc); `makemon.c:1295–1301` (place, `mcansee/mcanmove`, `mgenmklev = gi.in_mklev`, `seen_resistance`, `mpeaceful` `:1297` already live D-2294, `MM_MINVIS` `:1300–1301`); caller `zap.c:775–819` sets `m_id/mx/my/wormno` before `replmon`.
**JS:** 2 files (`js/mon.js` replmon arm + docs, `js/makemon.js` template field + birth flags + docs), under the 600/10 caps. Density note: the two C loci are ~50 + 7 lines; the shipped residual is the replmon place arm plus the makemon flag cluster — the deferred makemon grid is its own row.
**Change:** `replmon` ports C order over live callees only — `impossible` (display.js), `place_monster` (steed.js), `OBJ_MINVENT` (const.js) and `remove_worm` (worm.js) all join already-imported modules (`imports.mjs --can`: all ALREADY, no new edge; same-SCC call-time use, `void impossible` keeps the function sync like C). Grid clear is stale-safe (worm heads via `remove_worm`, else delete only when the cell still holds the old mon, per `mon_leaving_level`); the replacement places unless it is the steed; fmon prepend follows the place per C. `makemon` sets `mcansee/mcanmove/mgenmklev/seen_resistance` explicitly at the C position and handles `MM_MINVIS` via the live `mon_set_minvis` (worn.js ALREADY; `MM_MINVIS`/`M_SEEN_NOTHING` join the const.js import, same module).
**Verify:** preflight `node scripts/verify.mjs --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn place_monster` → PASS syntax (2 files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) · VERIFY: PASS. Bisect: makemon-grid ON regresses the pony ride (screen More at dismount + `rn2(3) @ corpse_chance` missing); grid OFF with flags ON fully passes that session (full RNG + screens) and the whole fortress.
**Named:** `place_wsegs(mtmp2, mtmp)` in `replmon` (next Open row `worm.c` place_wsegs restore/replmon callers); replmon light-source swap, full `replshk` bill, `set_ustuck` botl edge and `dealloc_monst` (GC) stay named; `makemon` grid `place_monster(mtmp, x, y)` deferred to its own Open row (movement-parity prerequisite).
**Next:** do not re-pop `place_monster` cutworm/makemon for the shipped arms. Falsifier: a rescore or fresh `verify place_monster` showing a session blocked with it as owner, or the makemon-grid row moving.
