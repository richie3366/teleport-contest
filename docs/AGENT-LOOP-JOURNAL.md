# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2155 `uhitm.c` mhitm_ad_cold uhitm arm: poly-hero cold touch now burns the MC gate (1 session PASS)

**C locus:** `uhitm.c:2626–2652` (`mhitm_ad_cold` uhitm arm: `mhitm_mgc_atk_negated(magr, mdef, TRUE)` first → damage 0 + return; `!Blind` «%s is covered in frost!»; `resists_cold || defended(AD_COLD)` → shieldeff + «frost doesn't chill %s!» + golemeffects + damage 0; `damage += destroy_items(mdef, AD_COLD, orig_dmg)`). Reached via `mhitm_adtyping :4793` → `damageum :4854` (poly hero as attacker). `mhitm_mgc_atk_negated` itself (`:75–99`) was already faithful in JS.
**JS:** `js/uhitm.js` only (2 import names + new arm + dispatch + header comment; no new file).
**Change:** new `damageum_ad_cold(mdef, mhm)` in `js/uhitm.js` in exact C order and short-circuit (negate-TRUE first; Blind-gated frost pline via house `Blind_that()`; resists_cold + shieldeff + chill pline then zero; `destroy_items(AD_COLD, orig)` added to leftover), wired as `AD_COLD` in `damageum_adtyping`; `resists_cold, destroy_items` join the existing `zap.js` edge and `shieldeff` the existing `display.js` edge (`imports.mjs --can`: ALREADY, no new edge).
**Verify:** `node scripts/verify.mjs --fn mhitm_mgc_atk_negated` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden (scen-poly-Monk-92164 PASS, scen-wish-Monk-92013 moved → mhitm_mgc_atk_negated at step 79, was 75) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · full skipped (no shared file changed). VERIFY: PASS.
**Named:** `defended(mdef, AD_COLD)` worn walk (no JS export; same omit on every defended site, commented at the call); `golemeffects(mdef, AD_COLD)` slow-only for flesh golem (no heal; slow named with `golemeffects_mm`); FIRE/ELEC uhitm arms stay named in `damageum_adtyping` header.
**Next:** `mkobj.c` hornoplenty (next Open row).
## 2026-09-08 — D-2154 `insight.c` list_vanquished: `pmnames[NEUTRAL]` display names (2 sessions PASS)

**C locus:** `insight.c:2784–2949` (`list_vanquished`: per-type line from `mons[i].pmnames[NEUTRAL]` — `an()` for nkilled==1 else `%3d makeplural()`, `N_times` for uniques; `vanqsort_cmp` alpha arms compare the same names). Callers `end.c:607/660` (`'d'`/disclose) and `insight.c:2771` (`dovanquished` `'A'`/`'y'`).
**JS:** `js/insight.js` only (1 import line + helper body; no new file).
**Change:** `pmname_neutral` now returns `pmnames[mndx]?.[NEUTRAL] ?? 'monster'` with the C cite (`mons[i].pmnames[NEUTRAL]`); `pmnames, NEUTRAL` join the existing `monsters.js` edge (`imports.mjs --can`: ALREADY, no new edge). Display (`an`/`makeplural`/`N_times`) and default `VANQ_MLVL_MNDX` sort order are untouched — `just_an` already yields «an Uruk-hai» / «a Keystone Kop» on the C-cased inputs.
**Verify:** `node scripts/verify.mjs --fn list_vanquished` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden (scen-genesis-Archeologist-92157 PASS, scen-genesis-Barbarian-92111 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · full skipped (no shared file changed). VERIFY: PASS. (Standalone `node -e` probe of `an()`/`makeplural()` was abandoned — `js/objnam.js` has a module-order TDZ when loaded outside the game bootstrap; pre-existing, unrelated to this change. The corpus screens are the probe.)
**Named:** unchanged from D-0126 and still named in `js/insight.js` header + map: `set_vanq_order` force_sort/`'a'`-sort menu, disclose `ask` yn path, `VANQ_MCLS_*` class-header modes (+`Rider`/`special_hdr`), dumplog `'d'`, Hallucination footer.
**Next:** `uhitm.c` mhitm_mgc_atk_negated (next Open row).
## 2026-09-08 — D-2153 `potion.c` peffect_paralysis: Levitation/air/water/steed/surface branches (1 session moved past)

**C locus:** `potion.c:881–898` (`peffect_paralysis`: `Free_action` → `You("stiffen momentarily.")`; else `Levitation || Is_airlevel || Is_waterlevel` → `You("are motionlessly suspended.")`, `u.usteed` → `You("are frozen in place!")`, else `Your("%s are frozen to the %s!", makeplural(body_part(FOOT)), surface(u.ux, u.uy))`; then `nomul(-(rn1(10, 25 - 12 * bcsign(otmp))))`, `multi_reason`, `nomovemsg = You_can_move_again`, `exercise(A_DEX, FALSE)`); caller `dopotion :1361` `POT_PARALYSIS`.
**JS:** `js/potion.js` only (`peffect_paralysis` + 3 import names; no new file).
**Change:** port the C branch order and short-circuit exactly: `Free_action()` resist arm; else Levitation (house reader, D-1419) / `Is_airlevel` / `Is_waterlevel` (const.js) → suspended, `u.usteed` → frozen in place, else feet + `surface(u.ux, u.uy)` via the shared `sit.js` export (`imports.mjs --can`: SAFE, hoisted function; no new cycle edge — sit.js never imports potion.js); `FOOT` joins the const.js edge; message tail (`nomul`, `multi_reason`, `nomovemsg`, `exercise`) unchanged and inside the else arm per C.
**Verify:** `node scripts/verify.mjs --fn peffect_paralysis` → PASS syntax (1 changed file) · PASS rule2 · PASS hidden (scen-wish-Valkyrie-92091 moved → `do_statusline2` at step 109, was 26) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · full skipped (no shared file changed). VERIFY: PASS.
**Named:** none in this function — every C branch is live. House mirrors stand: `Free_action()` flat/extrinsic reader (shared with `peffect_sleeping`), `Levitation()` (H||E)&&!B reader (D-1419), `You_can_move_again` as the `'You can move again.'` literal (hack.js:1114 house pattern).
**Next:** `insight.c` list_vanquished (next Open row).
## 2026-09-08 — D-2152 `uhitm.c` hmon_hitmon_weapon_melee: Healer anatomy + Rogue backstab + shatter + artifact doreturn (1 session PASS)

**C locus:** `uhitm.c:933–1067` (`hmon_hitmon_weapon_melee`: `:944–945` dmgval + train gate, `:947–951` Healer anatomy `min(3, died/6)`, `:953–963` Rogue backstab `You("strike %s from behind!")` + `rnd(u.ulevel)` + hittxt, `:964–1013` dieroll-2 two-handed shatter with `obj_resists` erosion gate + `m_useupall` + `rn2(4)` flee, `:1015–1034` artifact_hit doreturn killed→FALSE / dmg-0→TRUE); `hmon_hitmon :1797` doreturn early-return.
**JS:** 1 file (`js/uhitm.js` +~110/−12), under the 600/10 caps. Working-tree scoreboard rescore included (`hidden-corpus/scoreboard.json`: scen-normal-Rogue-92146 → passed true, 3782/3782 RNG, 166/166 screens).
**Change:** New async `hmon_hitmon_weapon_melee(mon, obj, ctx)` in `js/uhitm.js` in exact C order and short-circuit (Healer `P_KNIFE` + `mvitals.died` bonus; `!train || ustuck || twoweap || Cleaver` no-bonus gate; Rogue `backstabbable && !Upolyd && hand_to_hand` pline-then-`rnd(ulevel)`; dieroll-2 shatter with inlined `uwep_skill_type` (`twoweap ? P_TWO_WEAPON_COMBAT : weapon_type(uwep)`, no second clone of apply.js local), inlined `greatest_erosion` (`Math.max(oeroded, oeroded2)`, no fifth clone), live `MON_WEP`/`is_flimsy`/`obj_resists`/`setmnotwielded`/`Yobjnam2`/`Monnam`/`otense`/file-local `s_suffix` + `plur`, `extract_from_minvent` for C `m_useupall` (JS has no manual free), `rn2(4)` → `monflee(d(2,3))`; artifact_hit with C doreturn checks), wired into `hmon` with `hand_to_hand` mirroring `hmon_hitmon :1780` and `if (ctx.doreturn) return !!ctx.retval`. Same-SCC edges only (`dogmove.js` obj_resists used at call time, no top-level TDZ read); Rule #2 clean; reverted a `bimanual` import that collided with the file-local C-faithful clone (using the local instead).
**Verify:** `node scripts/verify.mjs --fn hmon_hitmon_weapon_melee` → PASS syntax (1 changed js file) · PASS rule2 · PASS hidden (1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-normal-Rogue-92146: PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full skipped: no shared file changed). No probes left in the tree.
**Named:** melee `silvermsg`/`silverobj` + `lightobj` flags (hmon has no msg_silver/msg_lightobj plumbing — map turns.md standing entry); `joust()` + `hmon_hitmon_jousting` damage (no JS joust port); thrown ammo/poison + permapoisoned `ispoisoned` flags (no `hmon_hitmon_poison` consumer in `hmon`); `m_useupall` obfree tail (GC, same as `m_useup`); C BUFSZ truncation (JS strings unbounded, no-op).
**Next:** queue row addressed; Rogue-92146 fully PASS with no later owner — nothing to re-queue from this session.
## 2026-09-08 — D-2151 `uhitm.c` mhitm_ad_dise mhitu arm + `mhitu.c` gulpmu AD_DISE: Demogorgon disease hits now sicken (1 session moved past)

**C locus:** `uhitm.c:4593–4619` (`mhitm_ad_dise`; mhitu arm `:4604–4608`: `hitmsg(magr, mattk); if (!diseasemu(pa)) mhm->damage = 0`); `mhitu.c:1032–1043` (`diseasemu`: Sick_resistance → `You_feel("a slight illness.")`, FALSE; else `make_sick(Sick ? Sick/3+1 : rn1(ACURR(A_CON),20), pmnames[NEUTRAL], TRUE, SICK_NONVOMITABLE)`, TRUE); `mhitu.c:1533–1536` (`gulpmu` AD_DISE: same `if (!diseasemu) tmp = 0`).
**JS:** 1 file (`js/mhitu.js` +21/−1), under the 600/10 caps. Small-C-end density: the row's complete C delta is `mhitm_ad_dise` mhitu arm (5 lines) + `gulpmu` DISE arm (3 lines) reusing the live `diseasemu` (C 12 lines, D-2059).
**Change:** New `mhitm_ad_dise_u` in `js/mhitu.js` in exact C order and short-circuit (`await hitmsg`; `if (!(await diseasemu(mtmp?.data))) mhm.damage = 0` — sickness keeps leftover hitmu d(), resistance zeroes it), wired as `case AD_DISE` in `mhitm_adtyping_u`; `gulpmu` AD_DISE split from AD_DREN to `if (!(await diseasemu(mtmp?.data))) tmp = 0` per C `:1533–1536`. Same-module calls only (`hitmsg`, `diseasemu`), no new cross-module edge, no DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn diseasemu` → PASS syntax (1 changed js file) · PASS rule2 · PASS hidden (0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-death-Valkyrie-92229 moved → slimed_to_death at step 37, was 23) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full skipped: no shared file changed). No probes left in the tree.
**Named:** `mhitm_ad_dise` uhitm arm (`:4599–4603`, hero never polymorphs into a DISE attacker) and mhitm arm (`:4610–4618`, S_FUNGUS/GHOUL/`defended` gate, lives in mhitm.js) stay named; `gulpmu`/`hitmu` AD_DREN (`drain_en`) stays named (map `turns.md`); remaining `mhitm_adtyping_u` uncovered adtyps still zero damage per C.
**Next:** scen-death-Valkyrie-92229 now blocked on `slimed_to_death` at step 37 — re-queue under that writer when the proxy names it; do not re-pop diseasemu.
## 2026-09-08 — D-2150 `read.c` do_class_genocide `iflags.cmdassist` default-On prompt + livelog/update_inventory arms (1 session PASS)

**C locus:** `read.c:2651–2658` (`Snprintf(" [enter %s]", iflags.cmdassist ? "the symbol or name representing a class, or '?'" : "'?' to see previous genocides")`); `iflags.cmdassist` default On (`optlist.h:233–234` NHOPTB cmdassist initval On; `dat/opthelp` `[True]`). Same pattern at `read.c:2857–2861` (do_genocide). Wipe-arm livelog `read.c:2673` (declined), `:2738–2745` (ll_done once-per-call first/genocided-class), `update_inventory` `:2750` (eggs & tins), `ll_done` decl `:2641`.
**JS:** 2 files (`js/read.js` +29/−7, `js/insight.js` +1/−1), under the 600/10 caps. Small-C-end density: the row's complete C delta, corpus-verified.
**Change:** Both genocide prompts use `!== false` default-On (house pattern from pickup.js/lock.js), C-cited. `do_class_genocide` gains `ll_done`, the declined-class livelog, the once-per-call first/genocided-class livelog (`num_genocides` exported from `insight.js`, read via dynamic import matching the existing `list_genocided` pattern in this function; `uhis` static from `roles.js` — SAFE, no cycle; class sym via `monsym({mlet})` on the existing display edge for `def_monsyms[class].sym`; C `%c` ≡ 1-char `%s` since livelog handles %s/%d), and `update_inventory()` on the existing invent edge in C order. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn do_class_genocide` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden (1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Tourist-92230 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full skipped: no shared file changed). No probes left in the tree.
**Named:** vampshifted `POLY_REVERT` stays named (map `turns.md:706`; JS `polyself` voids `POLY_REVERT` at polyself.js:1265, so wiring the call would run an interactive poly — measured, not assumed). `do_genocide` livelog/Hallucination/cham/newcham/update_inventory untouched (map `turns.md:706`).
**Next:** `js/zap.js:6636` wish path reads `game.flags?.cmdassist` (wrong bag — C is `iflags.cmdassist`; cf. do.js:1162 note) — candidate follow-up row, not this fix.
## 2026-09-08 — D-2149 `dogmove.c` dog_goal APPORT `can_carry` vs blast artifacts: local clone skipped `can_touch_safely`, pet apport-took the PYEC (1 session PASS)

**C locus:** `dogmove.c:550–559` APPORT arm (`can_carry(mtmp, obj) > 0` gate after `edog->apport > rn2(8)`); `mon.c` `can_carry` (`:1990–`) → `can_touch_safely` (corpse-petrify/rider, silver-hate, `!touch_artifact → 0`); `artifact.c` `touch_artifact` `:908–974`, monster arm `:931–936` (`!is_covetous && !is_mplayer`: `badclass = self_willed && role != NON_PM` save Excalibur; `badalign = RESTR && alignment != A_NONE && alignment != mon_aligntyp(mon)`) + silent refuse `:944–949` (`!yours → return 0`, no draws — `badalign && (!yours || !rn2(4))` short-circuits). PYEC is a real artifact (`artilist.h:291`: CREDIT_CARD, NOGEN|RESTR|INTEL|DEFN, Tourist, Neutral): vs the chaotic kraken `badclass` (INTEL + Tourist role) and `badalign` (RESTR + Neutral≠chaotic) are both true → `touch_artifact` returns 0 → `can_carry` 0 → follow branch. C-side proof the card is artifact-flagged: steps 125–126 print «You are blasted by the credit card named the Platinum Yendorian Express Card's [power!]» + «evades your grasp!» (hero blast + evade arms, which need `oartifact != 0`); JS screens matched there, so JS `oartifact` agrees.
**JS:** 4 files (`js/artifact.js` +38/−8, `js/monmove.js` +28/−7, `js/dogmove.js` +1/−29, `js/do_name.js` +1/−1; net +46), under the 600/10 caps. C locus is ~30 lines so density is at the small-C end.
**Change:** `js/artifact.js` — new sync `touch_artifact_mon(obj, mon)` porting C's monster-only decision (NONART gate, non-covetous role/align via `is_covetous`/`is_mplayer`/`mon_aligntyp_nam`, covetous pass-through, `bane_applies`, silent refuse; no messages, no RNG on the refuse path) on the extended `monsters.js`/`do_name.js` edges; async `touch_artifact` monster arm delegates to it (hero path byte-identical). `js/monmove.js` — full `can_touch_safely` port in C order (petrify/rider/silver/touch) + `SILVER`/`W_ARMG`/`BELL_OF_OPENING`/`touch_artifact_mon` imports. `js/dogmove.js` — deleted both clones, imports `can_carry` from `monmove.js` (both `dog_goal` and `dog_invent` sites; C `dog_invent` calls the same `can_carry`).
**Verify:** `node scripts/verify.mjs --fn dog_goal` → PASS syntax (4 changed js files) · PASS rule2 · PASS hidden (1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Priest-92136 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. No probes left in the tree (temporary `DG` console.error block in `dog_goal` + `getRngLog` import reverted; diagnosis also used /tmp-only replay scripts).
**Named:** `can_touch_safely` call inside `oselect` stays named (map `turns.md:3076`); outer `can_touch_safely` call in `m_search_items` stays named (map `turns.md:2693`); `can_carry` huge-quan `rn2` clamp stays deferred (pre-existing comment, untouched by this path).
**Next:** none for this session (PASS). Residual observations for other rows (not this fix): C status shows hero HP 99 at steps 167–168 while the pre-fix JS run printed 100 — a `regen_hp` formula drift on identical `rn2(100)` dice (the queued `allmain.c regen_hp` row's family); C step 168 also showed a wild sea-monster glyph one cell off the pre-fix JS run, which re-synced with the RNG.
## 2026-09-08 — Audit d850333f..04072151 (reviews 1108–1114: 7 ACCEPT, 0 Must-fix) + cadence 44/44

Review iter over the 7 JS-touching SHAs since 79844c1c, oldest first, each audited against pinned C with independent `hidden-proxy verify --base` re-runs: 1108 find_roll_to_hit encumbrance/utrap (uhitm.c:407–411 exact; PROGRESS 107→110 re-measured), 1109 runmode_delay_output (hack.c:2994–3017 verbatim incl. 4/4 call sites; prefix order confirmed vs options.c:3638–3642; hidden vacuously honest), 1110 SPFX_BEHEAD (artifact.c:1550–1644 arm-for-arm; ROLL_FROM draw parity proven by PASS), 1111 done_in_by killer-name arms (end.c:184–282; isshk append fixes a real latent overwrite; stale step-39 row honestly triaged, PASS 71/71), 1112 Mb_hit (artifact.c:1248–1434 RNG-order exact; all effect callees live+awaited; `return Mb_hit` matches :1537–1540), 1113 mondead prefix + 3→1 clone unification (set_mon_data syncs mnum per mondata.js:66, killing the staleness worry; lifesaved/grddead/vamprises confirmed absent = genuine named omits; PROGRESS 81→96), 1114 set_uasmon REGENERATION (polyself.c:105; reader allmain.js:561 reads both writes; clearing free via :696/:1127; 2 PASS re-measured). Noted non-queueables: artifact.js-local Hallucination clone omits uprops[HALLUC_RES] (pre-existing, no corpus reach); D-2145 subject's "2-col shift" was name-length-derived, no positioning changed. No C-wrongs → no Must-fix prepends, Next cluster unchanged. Cadence: full `sessions` 44/44, Scr 11,405/RNG 792,838 unchanged, speed `76+0.46/turn` (R² 0.79). Hot sum tipped 19 B over by this iter's CURRENT lines; trimmed audit crumb to 39981 B.
## 2026-09-08 — D-2148 polyself.c set_uasmon PROPSET(REGENERATION): M1_REGEN poly forms heal (do_statusline2-symptom writer, 2 sessions PASS)

**C locus:** `polyself.c:105` `PROPSET(REGENERATION, regenerates(mdat))` inside `set_uasmon` (`:38–112`; `regenerates ≡ mflags1 & M1_REGEN`, `mondata.h:80`); vampire form carries M1_REGEN (`mflags1s[PM_VAMPIRE]=0x10820401 & 0x00800000`). C `regen_hp` (`allmain.c:642–644`) heals a poly'd hero +1/turn whenever `U_CAN_REGEN()` (draw-free); JS `regen_hp` is faithful but no JS path ever set the bit. Turn-count drift falsified (every turn draws ≥1 die and dice matched positionally through the divergence, so `svm.moves` is equal and `moves%20` cannot explain heals on 4 consecutive combat turns); Sleepy+usleep falsified (hero awake, searching, no wake messages); combat-math gap falsified (JS damage 7/1/0/12 reproduces C dice exactly — the gap is purely the missing +1 heals).
**JS:** 1 file (`js/polyself.js` +8/−1), under the 600/10 caps.
**Change:** `js/polyself.js` only — import `regenerates` on the existing `monsters.js` edge + `REGENERATION` on the existing `const.js` edge (`imports.mjs --can`: ALREADY, no new edge); `set_uasmon` gains `propset_fromform(REGENERATION, 'HRegeneration', regenerates(mdat))` after the BLINDED arm with the `:105` cite (helper sets both the `uprops[].intrinsic` FROMFORM bit and the `H*` flat, matching siblings); omission comment retires REGENERATION. Clearing is free: `polymon`/`rehumanize` both route through `set_uasmon` (`:690`, `:1121`), whose off arm strips FROMFORM for non-regenerating forms. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn do_statusline2` → PASS syntax (1 changed js file) · PASS rule2 · PASS hidden (2 PASS, 2 moved past, 9 unchanged, 0 worse → PROGRESS: scen-poly-Priest-91137 PASS — JS now prints HP:44/44/45/34 at steps 214/217/220/222; scen-poly-Rogue-92026 PASS; scen-poly-Monk-92164 moved → `mhitm_mgc_atk_negated` at step 84, was 63; scen-wish-Ranger-92212 moved → `hornoplenty` at step 146, was 77, retained pre-existing movement) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS (full skipped: no shared file changed). No probes left in the tree (3 temporary `console.error` probes in `mhitu.js`/`allmain.js` reverted via `git checkout`; diagnosis also used /tmp-only replay scripts).
**Named:** the remaining `set_uasmon` PROPSETs (ANTIMAGIC/SICK_RES/STUNNED/HALLUC_RES/SEE_INVIS/TELEPAT/INFRAVISION/INVIS/LEVITATION/SWIMMING/PASSES_WALLS/REFLECTING/BLND_RES; vamp cham; polysense) stay named (map `turns.md`, untouched by this path); sibling do_statusline2 sessions with different writers (below) untouched.
**Next:** re-queue remainders under `do_statusline2` with true writers — scen-genesis-Tourist-92044 (unpoly'd 17-HP gap, not a FROMFORM writer), scen-poly-Healer-92107 (HD:5 form — check M1_REGEN; if set, why no move), scen-death-Tourist-92095 (AC:6v10) + scen-poly-Priest-92021 (AC:3v7) (`u.uac` writers), scen-intrinsic-Caveman-92138 (cond ORDER Burdened-vs-Slime contradicts pinned `botl.c:170–206` under any state — needs C-binary/capture audit, never touch the painter), scen-wish-Healer-92092 + scen-wish-Tourist-91125 (lembas `Satiated` pair — parked botl-parity falsifier stands), scen-wish-Priest-92035 + scen-poly-Ranger-92090 (unprobed). CAVEAT (harness, not game): the worker's flat `getRngLog()` for Priest-91137 ends at index 3605 while per-step `getRngSlices()` prove JS drew every C die through step 225+ with exact values — a post-fix session may attribute to `passiveum`@215 spuriously; the game-level draws are verified correct via slices, do not "fix" passiveum (already faithful) on that basis.
## 2026-09-08 — D-2147 `mon.c` mondead: Kops `rnd(5)` respawn + cham/were/quest/mail prefix; 3 clones → 1 export (queue row `mon.c` mondead, 1 session → moved past)

**C locus:** `mon.c:3081–3177` (`mondead`: be_sad flag `:3089–3101`, `lifesaved_monster`, vampshifter revert, steam vortex `:3103–3105`, vault guard `:3109–3110`, cham/were restore `:3115–3123`, mvitals `:3136–3138`, quest leader `:3141–3142`, mail daemon `:3146–3147`, S_KOP `rnd(5)`+`makemon` `:3149–3166`, `logdeadmon` `:3169`, unmap + `m_detach` `:3170–3175`). Callers confirm order: C `xkilled` runs `mondead` before its treasure `rn2(6)`, as does `js/uhitm.js` `xkilled` (`mondead` `:680`, `rn2(6)` `:700`).
**JS:** 3 files (`js/mhitm.js` +75/−6, `js/uhitm.js` +2/−19, `js/trap.js` +2/−24), under the 600/10 caps.
**Change:** `js/mhitm.js` export now ports the sync-safe prefix in exact C order and short-circuit (be_sad read+clear; cham/were restore via live `set_mon_data`/`ismnum`/`mons`/`NON_PM` + `monsterNames.indexOf` PM consts per file idiom; mvitals via `record_mvitals_died` on the restored form; quest-leader mark with the m_id-0-unset guard per `quest.js:413`/`dog.js:642`; mail-daemon `G_GENOD` on the ensured mvitals slot; S_KOP `rnd(5)` switch with `stairway_find_type_dir`+`makemon`+`NO_MM_FLAGS` including the case-1 FALLTHROUGH). `makemon`/`NO_MM_FLAGS` join existing edges; `stairway_find_type_dir` is one new `mklev.js` edge — `imports.mjs --can` SAFE (hoisted function, same 90-module SCC, no TDZ risk). `js/uhitm.js` + `js/trap.js` clones deleted; both import the export on their existing `mhitm.js` edges (no new edge).
**Verify:** `node scripts/verify.mjs --fn mondead` → PASS syntax (3 changed js file(s)) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: Barbarian-92111 moved → `list_vanquished` at step 96, was 81) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; plus `node scripts/verify.mjs --fn mondead --full` → PASS full 44/44. No hand probes in the tree (corpus expectation is the falsifier).
**Named:** `lifesaved_monster` + `DEADMONSTER` early return (callers keep their `mhp>=1` lifesaved checks); `vamprises` revert (no JS export); be_sad pline (needs async); steam-vortex gas cloud including its `rn2(10)+5` draw (async); `grddead` vault-guard return (no JS export); `logdeadmon`; full `m_detach` wizdead/shkgone/wormgone (map `data.md`, header comment).
**Next:** Barbarian-92111 now blocked at step 96 under `list_vanquished` — a later owner for a future row; do not re-pop `mondead` for it.
## 2026-09-08 — D-2146 `artifact.c` Mb_hit: Magicbane specials ported (queue row `artifact.c` Mb_hit, 1 session → PASS)

**C locus:** `artifact.c:1248–1434` (`Mb_hit`: `scare_dieroll` spe damping, `!spec_dbon_applies→dieroll+1`, `do_stun` rn2 gate, cumulative probe/stun/scare/cancel tiers each `rnd(4)`, hit pline, CANCEL/SCARE/STUN/PROBE switch, stun/confuse application, fakename side-effect plines) + `:1537–1540` (`artifact_hit` AD_STUN `dieroll<=MB_MAX_DIEROLL→Mb_hit`) + `decl.c:51` (`fakename[]={"mon","you"}`).
**JS:** 1 file (`js/artifact.js` +190/−5), under the 600/10 caps.
**Change:** `js/artifact.js` only — exported async `Mb_hit` in exact C order and short-circuit (tier cascade, `dr<=scare/2` cancel floor via `Math.trunc`, hit pline before effects, CANCEL `cancel_monst` + rehumanize `dmg=0` + uenmax/uen/uenpeak + clay-golem `mhp=1` + AT_MAGC absorb arms, SCARE Antimagic/`nomul(-3)`/`multi_reason`/`nomovemsg`/stuck-release vs `rn2(2)&&resist→monflee(3)` arms, PROBE `spe==0||!rn2(3*|spe|)` + `probe_monster`, redundant-STUN note, `(HStun&TIMEOUT)+3`/`(HConfusion&TIMEOUT)+4`, fakename `youdefend?raw:vtense('mon',·)` + `upstart` + `verbose` plines). `MB_INDEX_*`/`MB_MAX_DIEROLL`/`MB_VERB` consts + `PM_CLAY_GOLEM` via the existing `monsterNames.indexOf` idiom + file-local `attacktype`/`AT_MAGC=255` per eat.js/engrave.js precedent (uhitm edge would cycle); `You(...)` renders as full-text `pline('You ...')` (no new `You` clone per sym guidance); `sticks` (engrave.js), `set_ustuck` (mhitu.js), `monflee` (monmove.js), `make_stunned`/`make_confused` (potion.js), `upstart` (hacklib.js) are new names on new edges — `imports.mjs --can` all SAFE (hoisted functions; hacklib no cycle); `cancel_monst`/`resist`/`probe_monster` (zap.js), `canspotmon`/`map_invisible`/`shieldeff` (display.js), `nomul` (hack.js), `NOTELL` (const.js), `WEAPON_CLASS` (objects.js) join existing edges. AD_STUN gate now `return await Mb_hit(...)`.
**Verify:** `node scripts/verify.mjs --fn Mb_hit` → PASS syntax (1 changed js file(s): js/artifact.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify Mb_hit: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Archeologist-91134 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) · VERIFY: PASS. No hand probes in the tree.
**Named:** SPFX_DRLI arm stays named (map `data.md`, untouched by this path); destroy_items/ignite_items bodies stay named (gates still burned).
**Next:** do not re-pop `Mb_hit` (91134 PASS) or `artifact_hit` for it. The working-scoreboard refresh also re-based the header (bb24a32f→d2ce6b76) and re-attributed the parked Tourist-91125 row to its documented live state (do_statusline2@82, rngM 2745/scrM 83 — exactly the d2ce6b76 park note) plus ±few-count wiggles on three same-step/same-owner do_statusline2 rows (92095, 92164, one scrM+1); control replays with HEAD's `artifact.js` reproduce the refreshed numbers byte-for-byte (92164 RNG 3128/3399 Screen 63/175, 92095 RNG 8531/13187 Screen 61/111), so all of that predates this port — zero movement from `Mb_hit`.
## 2026-09-08 — D-2145 `end.c` done_in_by killer-name arms: NEUTRAL epitaph + 2-col overview shift (queue row `mkobj.c` start_corpse_timeout, 1 session → PASS)

**C locus:** `end.c:183–190` (`mptr`/`champtr` + `distorted`/`mimicker`/`imitator`), `:195–205` (G_UNIQ `the ` unless pname + `KILLED_BY`), `:209–212` (`minvis` / `hallucinogen-distorted` prefixes), `:270–282` (`pmname(mptr, Mgender(mtmp))` + `called`/`of` + `MGIVENNAME`). Read but NOT changed: `wintty.c:1908–1916` menu `offx` (`max(10, cols-maxcol-1)` production vs `min(min(82, cols/2), …)` H2344_BROKEN — H2344 is undefined in this build so C uses `max`, but no live session distinguishes the formulas at maxcol≈39; left as-is, see omissions).
**JS:** 1 file (`js/end.js` +39/−14), under the 600/10 caps.
**Change:** `js/end.js` only, in C order and short-circuit: `mptr`/`champtr`/`distorted`/`mimicker`/`imitator` decls; G_UNIQ gate with imitator + High-Cleric conditions and `type_is_pname` no-`the` arm; `minvis` + `hallucinogen-distorted` prefixes; `isshk` arm kept (now appending per C `Strcat` order); plain arm is `pmname(mnum, Mgender(mtmp))` + `has_ebones ? "of" : "called"` + `MGIVENNAME` suffix. Same-edge import extensions only (`canspotmon`/`Hallucination` join the existing `display.js` edge per the `mondata.js` precedent; `Mgender`/`type_is_pname` join `do_name.js`; `has_ebones`/`has_mgivenname`/`MGIVENNAME`/`M_AP_TYPE`/`M_AP_MONSTER` join `const.js`; `mons`/`G_UNIQ` join `monsters.js` — all hoisted functions or already-imported modules, no new edge, no TDZ risk); file-local `PM_HIGH_CLERIC` via the existing `monsterNames.indexOf` idiom; `pmnames` import dropped (now unused). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn start_corpse_timeout` → PASS syntax (1 changed js file(s): js/end.js) · PASS rule2 · PASS hidden (1 PASS: scen-wish-Barbarian-92102 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS; plus `node scripts/verify.mjs --full` → PASS full 44/44. Fresh-worker replays (post-change, `/tmp` probes deleted): 92102 PASS (2792/2792 RNG, 71/71 screens); parked `save_dungeon` member scen-genesis-Samurai-92080 PASS (2771/2771, 40/40) via the G_UNIQ arm; 92140/92050 still blocked (RNG full, overview header now C-left-of-JS — C lines carry the unported helpless suffix); 92075 unchanged (Sokoban `dunlev_ureached` content). No hand probes in the tree.
**Named:** `done_in_by` ghost arms / imitator+vampshifter / priest|minion `m_monnam` / `monhealthdescr` wound prefix (not live in `js/`) / `multi_reason` trim stay named (map `turns.md`, header comment); `wintty` H2344 offx formula left untouched (latent only — no corpus session reaches a distinguishing maxcol; changing it is untestable in-loop).
**Next:** 92140/92050 need the `topten.c formatkiller` `, while …` helpless suffix (`js/end.js` `formatkiller` `incl_helpless` still void per the `save_dungeon` park); 92075 needs its `dunlev_ureached` writer. The `save_dungeon` park stands for those three (92080's G_UNIQ member is now PASS). Do not re-pop `start_corpse_timeout` (stale row — wish path verified RNG-exact) or `done_in_by` for 92102.
## 2026-09-08 — D-2144 `artifact.c` artifact_hit SPFX_BEHEAD: JS never beheaded (queue row `artifact.c` artifact_hit, 1 session)

**C locus:** `artifact.c:1550–1644` (SPFX_BEHEAD: Tsurugi `dieroll==1` engulf-slice / `notonhead→FALSE` cutworm handoff / `bigmonst→×2` / else `2*mhp+200` halve + `observe_object`; Vorpal `dieroll==1 || Jabberwock` engulf-`FALSE` / headless→miss + `dmg=0` / noncorporeal+amorphous→slice-through / else `2*mhp+200` + `ROLL_FROM(behead_msg)` + Hallu/!female Anne pline + `observe_object`, mirrored youdefend arms) + `hack.h:1493` (`ROLL_FROM(array) ≡ array[rn2(SIZE)]` — the recorded `rn2(2)`) + `artifact.c:63` (`FATAL_DAMAGE_MODIFIER 200`).
**JS:** 1 file (`js/artifact.js` +~110/−3), under the 600/10 caps.
**Change:** `js/artifact.js` only — full BEHEAD arm in C order and short-circuit (Tsurugi `ART_TSURUGI_OF_MURAMASA` + Vorpal `ART_VORPAL_BLADE` from `generated/artifacts_data.js`; `PM_JABBERWOCK` via the existing `monsterNames.indexOf` convention; `FATAL_DAMAGE_MODIFIER = 200` with C cite; `NECK` joins the existing `const.js` edge; `Monnam`/`observe_object` join existing `do_name.js`/`invent.js` edges; `bigmonst`/`has_head`/`noncorporeal`/`amorphous` join the existing `monsters.js` edge; `mbodypart`/`body_part` new names on a `polyself.js` edge — `imports.mjs --can`: both hoisted functions, cycle-safe, no TDZ risk). `ROLL_FROM` is `['beheads','decapitates'][rn2(2)]` so the draw always fires; C `You(...)`/`pline_The` render as full-text `pline` (no new `You` clone per sym guidance); `gn.notonhead→game.notonhead`, `u.uswallow→u.uswallow`, `flags.female→game.flags?.female`, `Upolyd(u)?u.mh:u.uhp` per file convention; Vorpal `wepdesc` from `get_artifact(otmp)?.name`. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn artifact_hit` → PASS syntax (1 changed js file(s): js/artifact.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify artifact_hit: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-genesis-Rogue-92214 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) · VERIFY: PASS. Plus full `node frozen/ps_test_runner.mjs sessions`: fortress 44/44 PASS (no throws). No hand probes in the tree.
**Named:** SPFX_DRLI arm stays named (map `data.md`, untouched by this path); `Mb_hit` stays named; destroy/ignite bodies stay named (gates still burned).
**Next:** `Mb_hit` Open row (scen-wish-Archeologist-91134, same `mhitm_knockback` JS signature) is a different C function and stays queued on its own. Do not re-pop `artifact_hit` for it.
