# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-26 — D-2869 `maybe_absorb_item` lets a poked mimic take the hero's tool

**C locus:** `nethack-c/upstream/src/steal.c:772–810` `maybe_absorb_item`. Callees `obj_resists` (`zap.c:1458`, live `js/dogmove.js:174`), `touch_artifact` (`artifact.c:907`, live `js/artifact.js:1406`), `remove_worn_item` (`steal.c:213`, live `js/steal.js:271`), `subfrombill` / `shop_keeper` (`js/shk.js`), `cansee` (`js/vision.js:1171`), `Some_Monnam` / `Monnam` (`js/do_name.js`), `yname` / `otense` / `makeplural` (`js/objnam.js`), `body_part` (`js/polyself.js:626`), `bimanual` (`obj.h:257`, live `js/wield.js:1038`), `upstart` (`js/hacklib.js:242`), `freeinv` (`invent.c:8585` export `js/invent.js:8585`), `encumber_msg` (`js/invent.js:1173`), `mpickobj` (`js/makemon.js:2168`), `carried` (`obj.h:332`, live `js/eat.js:2558`). Call: `lock.c:575` inside `pick_lock`. `extern.h:3132` is the declaration.
**JS:** `js/steal.js` `maybe_absorb_item` `:354`, ball/chain/rock `:358`, `obj_resists` `:360`, `touch_artifact` `:361`, carried doff `:366`, bill `:368`, seen line `:373`, unseen line `:377`, `freeinv` `:379`, `encumber_msg` `:380`, floor line `:384`, `mpickobj` `:388`. Caller `pick_lock` `js/lock.js:1321`.
**Change:** One `maybe_absorb_item` in that C order. Ball, chain, or `ROCK_CLASS` returns before `obj_resists`. That call gets `100 - ochance` and `100 - achance`, so the lock-pick site (50, 10) resists on `rn2(100) < 50` for an ordinary object and `< 90` for an artifact.
**Verify:** `node scripts/verify.mjs --fn maybe_absorb_item` → PASS syntax (2 changed js files: js/lock.js js/steal.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (those files are not in the shared-file set) · VERIFY: PASS.
**Named:** A null object returns before any `rn2` (C is `NONNULL`; `pick_lock`'s missing tool is the dummy, not an object). `carried` is the live export, which also treats `invent[]` membership.
**Next:** `mkmaze.c` `set_levltyp_lit` (next Open — coverage row). `arti_light_radius` parked Stale. Seven Open rows remained after archive, below the floor of 8; five never-mentioned rows from `--rows 800 --min-c-lines 20` were appended, so the live queue is back at 12.
## 2026-09-26 — D-2868 `engulfer_digests_food` eats a swallowed drop; `dropz` gives the rest to the engulfer

**C locus:** `nethack-c/upstream/src/do.c:849–888` `engulfer_digests_food`. Callees `digests` (`mondata.h:71`, live `js/mhitu.js:1093`), `touch_petrifies` (`mondata.h:200`, live `js/monsters.js:453`), `polyfood` (`obj.h:321`, live `js/eat.js:415`), `pline`, `Tobjnam` (`objnam.c`, live `js/objnam.js:1816`), `newcham` (`mon.c:5276`, live `js/makemon.js:1981`), `minstapetrify` (`trap.c:3857`, live `js/trap.js:3536`), `grow_up` (`makemon.c:2049`, live `js/mhitm.js:3915`), `healmon` (`mon.c:4593`, live `js/mon.js:2315`), `mcureblindness` (`muse.c:2871`, live `js/muse.js:1800`), `delobj` (`invent.c:1429`, live `js/mkobj.js:3714`). Call: `do.c:823` inside `dropz`. `do.c:14` is the static declaration.
**JS:** `js/do.js` `engulfer_digests_food` `:2444`, food test `:2447`, corpse flags `:2459`, slime `:2464`, digested line `:2468`, `newcham` `:2472`, `minstapetrify` `:2478`, `grow_up` `:2480`, `healmon` `:2482`, `mcureblindness` `:2484`, `delobj` `:2486`. Caller `dropz` `:2399`, `stolen_value` `:2404`, digest call `:2406`, `mpickobj` `:2407`, `encumber_msg` `:2410`.
**Change:** One `engulfer_digests_food` in that C order. `digests(u.ustuck->data)` and a corpse, `globby` object, meatball, enormous meatball, meat ring, or meat stick enter. A corpse sets petrify (`touch_petrifies`), poly (`polyfood`), wraith growth, and nurse healing.
**Verify:** `node scripts/verify.mjs --fn engulfer_digests_food` → PASS syntax (1 changed js file: js/do.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** None inside `engulfer_digests_food`. `dropz` still skips `Blind && Levitation` `map_object` (`do.c:838–839`).
**Next:** `light.c` `arti_light_radius` (next Open — coverage row). Nine Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2867 `special_stock` keeps Izchak's candelabrum and counts candles; `shkcatch` takes a thrown pick

**C locus:** `nethack-c/upstream/src/shk.c:3103–3144` `special_stock`. Callees `is_izchak` (`shknam.c:907`, live `js/shknam.js:509`), `Deaf` (`youprop.h:125`, file-local `hero_deaf`), `muteshk` (`shk.c:58`), `pline`, `Shknam`, `SetVoice` (`sndprocs.h:276`, empty in this build, live `js/sndprocs.js:52`), `verbalize`, `plur` (`hack.h:1520`), `noit_mhis` (`you.h:330`), `mbodypart` (`polyself.c`, live `js/polyself.js:552`). Call: `shk.c:3994` inside `sellobj`. `shk.c:131` is the static declaration. Same file: `shk.c:4362–4396` `shkcatch`. Callees `shop_keeper`, `inside_shop`, `inhishop`, `helpless`, `dist2`, `mnearto` (`mon.c:4031`, live `js/mon.js:2160`), `cansee`, `the`, `xname`, `canspotmon`, `map_invisible`, `nh_delay_output`, `subfrombill`, `mpickobj`. Call: `zap.c:3886` inside `bhit`. `extern.h:2946` is the declaration.
**JS:** `js/shk.js` `special_stock` `:2704`, Izchak `:2711`, candle count `:2719`, won't-stock `:2727`, head shake `:2731`. Caller `sellobj` `:2891`. `shkcatch` `:2748`, `mnearto` `:2765`, catch line `:2772`, `subfrombill` `:2779`, `mpickobj` `:2780`. Caller `bhit` `js/zap.js:5955`.
**Change:** One `special_stock` in that C order. Both the candle-shop type and `CANDELABRUM_OF_INVOCATION` are required. When not quiet, Izchak before invocation is horrified or concerned if deaf or mute (`spe < 7`), otherwise tells the hero to hang onto it and, when `spe < 7`, how many candles are still needed (`7 - spe`, " more" only when `spe > 0`, `plur`).
**Verify:** `node scripts/verify.mjs --fn special_stock` → PASS syntax (2 changed js files: js/shk.js js/zap.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (those files are not in the shared-file set) · VERIFY: PASS. `node scripts/verify.mjs --fn shkcatch` → the same tail, REACH-OK, 0 regressed.
**Named:** The remark at `shk.c:3124–3125` (hero already carrying enough candles) is a comment, not an arm. `mark_synch` (`shk.c:4389`) is `tty_mark_synch` fflush (`wintty.c:3617`); this window has no stdout.
**Next:** `do.c` `engulfer_digests_food` (next Open — coverage row). Ten Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2866 `themerooms_post_level_generate` returns when themes are missing, then wallifies

**C locus:** `nethack-c/upstream/src/mklev.c:1174–1194` `themerooms_post_level_generate`. Callees `reset_xystart_size` (`sp_lev.c`, live `js/mklev.js:2811`), `lua_getglobal` / `nhl_pcall_handle` (`nhlua.c`, no Lua state), `wallification` (`mkmaze.c:290`, live `js/mklev.js:31951`), `free` of `gc.coder`. `iflags.in_lua` is an error-prefix flag. Calls: `mklev.c:1420` at the end of `makelevel`. `mklev.c:25` is the static declaration.
**JS:** `js/mklev.js` `themerooms_post_level_generate` `:30229`, themes return `:30233`, handler loop `:30241`, flag clear `:30248`, wallification `:30250`, coder free `:30252`. Callers `makelevel` `:26944`, `makelevel_ordinary` `:27095`. Tutorial wallification `load_tut1` `:19550`, `load_tut2` `:19635`.
**Change:** One `themerooms_post_level_generate` in that C order. `game._luathemes_loaded[dnum]` is `gl.luathemes[u.uz.dnum]`: missing or false returns before reset, the post hook, and wallification. Otherwise `reset_xystart_size`, `in_mk_themerooms` and `themeroom_failed = false`, the compiled `post_level_generate` handlers, flags cleared, `wallification(1, 0, COLNO-1, ROWNO-1)`, then `gc.coder = null`.
**Verify:** `node scripts/verify.mjs --fn themerooms_post_level_generate` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. The first reach run regressed seed8243 and seed0009 (tutorial row 4 `┌───────┐` vs `─────────`) because the themes-null return skipped the tail wallify those levels had been borrowing; `load_special`'s wallify on `load_tut1` / `load_tut2` restored REACH-OK and 44/44.
**Named:** `iflags.in_lua` (no reader). `lua_getglobal` / `nhl_pcall_handle` / `lua_gc` (no Lua heap; the handler list is the compiled `post_level_generate`).
**Next:** `shk.c` `special_stock` (next Open — coverage row). Four stale rows parked (`obj_timer_checks`, `mhitm_ad_dcay`, `walk_path`, `get_level_extends`). Three Open rows remained after archive, below the floor of 8; nine never-mentioned MISSING rows were appended, so the live queue is back at 12.
## 2026-09-26 — D-2865 `mhitm_ad_deth` is one function; first weapon hit and light-sear messages follow C

**C locus:** `nethack-c/upstream/src/uhitm.c:3836–3894` `mhitm_ad_deth`. Callees `pline_mon`, `Monnam`, `is_undead` (`mondata.h:95`), `pline`, `rn2`, `Antimagic` (`youprop.h:57`, live `js/mcastu.js` including `uprops`), `touch_of_death` (`mcastu.c`, `js/mcastu.js:425`), `You_feel`, `shieldeff`, `rnd`, `mhitm_ad_drli` (`uhitm.c:2489–2515`, `js/mhitm.js`). Same file: `uhitm.c:1963–1989` `first_weapon_hit` (`obj_is_pname`, `ONAME`, `simpleonames`, `bare_artifactname`, `livelog_printf`); `uhitm.c:1702–1730` `hmon_hitmon_msg_lightobj` (`canspotmon`, `mon_nam`, `highc`, `s_suffix`, `noncorporeal`, `amorphous`, `pline`). The light flag is `uhitm.c:1038–1040`.
**JS:** `js/mhitm.js` `mhitm_ad_deth` `:4365`, mhitu switch `:4376`, mhitm drain `:4404`. `js/mhitu.js` `mhitm_adtyping_u` `:3138`. `js/uhitm.js` `damageum_adtyping` `:2807`, `mdamagem` dispatch `js/mhitm.js:5268`. `first_weapon_hit` `js/uhitm.js:734`. `hmon_hitmon_msg_lightobj` `js/uhitm.js:1712`, flag `js/uhitm.js:1254`, call `js/uhitm.js:2098`.
**Change:** One `mhitm_ad_deth` in that C order. Hero-as-attacker falls through to the mhitm arm (C's `goto`). Hero-as-defender prints the reach-out line, halves undead damage rounding up, or switches on `rn2(20)`: 17–19 without Antimagic calls `touch_of_death` and zeroes damage; with Antimagic it falls through; 5–16 sets `permdmg = 1`; 0–4 shields when Antimagic and zeroes damage.
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_deth` → PASS syntax (3 changed js files: js/mhitm.js js/mhitu.js js/uhitm.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (those files are not in the shared-file set) · VERIFY: PASS.
**Named:** `hmon_hitmon_do_hit`'s `saved_oname` fill (`uhitm.c:1410–1413`, `cxname` when the object is not a lit light-artifact) is still absent; weapon `silvermsg`/`silverobj` (`uhitm.c:1035–1036`) stays unnamed. `mhitm_ad_dcay` stays the split trio (`mhitm_ad_dcay`, `mhitm_ad_dcay_u`, `damageum_ad_dcay`) and remains an Open row.
**Next:** `mkobj.c` `obj_timer_checks` (next Open — coverage row). Eight Open — coverage rows remain after archive, at the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2864 `init_dungeon_levels` reads each special level as a table

**C locus:** `nethack-c/upstream/src/dungeon.c:797–864` `init_dungeon_levels`. Callees `get_table_str` / `get_table_str_opt` / `get_table_int` / `get_table_int_opt` (`nhlua.c:1041–1076` and `:1016–1039`, file-local in `js/dungeon.js`), `get_dgn_align` (`dungeon.c:780–794`), `get_dgn_flags` (`dungeon.c:743–778`). `strcmp` is string equality. `emptystr` is `""`. Calls: `dungeon.c:1036` inside `init_dungeon_dungeons`. `dungeon.c:56` is the prototype. `extern.h:2139` is a comment.
**JS:** `js/dungeon.js` `get_table_str` `:348`, `get_table_str_opt` `:368`, `get_table_int` `:323`, `get_table_int_opt` `:335`, `get_dgn_flags` `:440`, `get_dgn_align` `:472`, `init_dungeon_levels` `:723`, boneschar `:763`, chain test `:766`, non-hash panic `:781`, `LEV_LIMIT` `:786`. Caller `init_dungeon_dungeons` `:889`.
**Change:** One `init_dungeon_levels` in that C order. The levels value is the generated table: `lua_len` is the array length and index `f+1` is `levels[f]`. Required `name` and `base` go through `get_table_str` / `get_table_int`; optional bonetag defaults to `emptystr`, chainlevel to NULL, range and nlevels to 0, chance to 100.
**Verify:** `node scripts/verify.mjs --fn init_dungeon_levels` → PASS syntax (1 changed js file: js/dungeon.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (dungeon.js is not in the shared-file set) · VERIFY: PASS.
**Named:** `debugpline1/2/4` true arm (`lint.h` `ifdebug(pline)`; `debugcore("dungeon.c", TRUE)` is the false-arm gate, and `pline` is not awaited because `init_dungeons` is synchronous). `impossible` on a bad flags value does not await its three plines; the scan still continues.
**Next:** `uhitm.c` `mhitm_ad_deth` (next Open — coverage row). Eleven Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2863 `keylist_putcmds` lists live binds; `extcmd_via_menu` narrows extended commands by letter

**C locus:** `nethack-c/upstream/src/cmd.c:2802–2863` `keylist_putcmds` and `:2784–2799` `keylist_func_has_key`. Callees `cmdbind_get` (`cmd.c:2109`, live `js/dokeylist.js:306`), `key2txt` (`cmd.c`, live `js/dokeylist.js:76`), `putstr` (the `dokeylist_lines` list). `flags.rest_on_space` is `game.flags.rest_on_space`. `bind->param` is `game.Cmd._bindParam`, stored by `bind_key` (`cmd.c:2705–2707`, `js/cmd.js:1387`). Calls: `cmd.c:2987`, `:2990`, `:2994`, `:2997`, `:3002`, `:3006` inside `dokeylist`; `:146` is the declaration. `dokeylist` `:2909` calls `keylist_func_has_key`. Same file: `cmd.c:752–882` `extcmd_via_menu`. Callees `select_menu` (live `select_menu_pick_one` in `js/options.js:6618`). Both `impossible` arms are compiled out (`patchlevel.h:33` `NH_STATUS_RELEASED`). Caller `win/tty/getline.c:301` `tty_get_ext_cmd`; `:24` is the extern.
**JS:** `js/dokeylist.js` `bind_param_get` `:321`, `keylist_func_has_key` `:577`, `keylist_putcmds` `:601`, general count `:768`, general list `:771`, game count `:774`, game list `:777`, wizard count `:782`, wizard list `:785`, header `keylist_func_has_key` `:716`. `js/cmd.js` param clear in `cmdbind_add` `:1280` and `:1304`, `cmdbind_remove` `:1323`, `bind_param_swap` `:1539`, `bind_param_set` `:1387`, `rhack` stamp `:1979`. `js/getline.js` `extcmd_via_menu` `:1274`, `get_ext_cmd` gate `:1396`.
**Change:** One `keylist_putcmds` and one `keylist_func_has_key` in that C order. Space is skipped only when `!flags.rest_on_space`. Binds come from `cmdbind_get`.
**Verify:** `node scripts/verify.mjs --fn keylist_putcmds` → PASS syntax (3 changed js files: js/cmd.js js/dokeylist.js js/getline.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS.
**Named:** `parsebindings` still strips `(param)`, so an rc BIND= does not fill `_bindParam`. `get_changed_key_binds` still prints the name-only BIND= line.
**Next:** `dungeon.c` `init_dungeon_levels` (next Open — coverage row). Six rows remained after archive, below the floor of 8; six never-mentioned gameplay rows from `--rows 800 --min-c-lines 20` were appended, so the live queue is back at 12.
## 2026-09-26 — D-2862 `get_coord` reads an x/y table or a length-2 array

**C locus:** `nethack-c/upstream/src/sp_lev.c:5319–5366` `get_coord`. Callees `nhl_error` (`nhlua.c:198`, now `js/mklev.js:21708`), `get_table_intarray_entry` (`sp_lev.c:5260`, `js/mklev.js:22015` `get_table_intarray_entry_unpacked`). `luaL_checkinteger` on the `x`/`y` fields is `js/mklev.js:21718`. Calls: `nhlua.c:518` `nhl_get_xy_params`; `sp_lev.c:3198` `get_table_xy_or_coord`; `:3262` `lspo_monster`; `:3608` `lspo_object`; `:3913` `lspo_engraving`; `:4419` `lspo_trap`; `:4439` launchfrom; `:4450` teledest; `:4496` `lspo_gold`; `:4866` `lspo_feature`; `:5007` `lspo_terrain`. `extern.h:3066` is the declaration.
**JS:** `js/mklev.js` `nhl_error` `:21708`, `luaL_checkinteger_unpacked` `:21718`, `get_coord` `:21738`, `x` field `:21751`, `y` field `:21758`, array arm `:21771`, `get_table_intarray_entry_unpacked` `:21777`. `get_table_xy_or_coord` `:21794`.
**Change:** One `get_coord` in that C order. A JS object or array is `LUA_TTABLE`; null and undefined are `LUA_TNIL` (`typeof null` is `"object"`). Nil returns false and does not write `xy`.
**Verify:** `node scripts/verify.mjs --fn get_coord` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** `nhl_get_xy_params` (`nhlua.c:506–522`) and its caller `nhl_getmap` (`nhlua.c:530`) have no JS function, so the `get_coord` at `nhlua.c:518` is unwired. `lspo_terrain` (`sp_lev.c:4978–5037`) has no JS function, so the `get_coord` at `:5007` is unwired; `lspo_terrain_sel` only walks `sel_set_ter`.
**Next:** `cmd.c` `keylist_putcmds` (next Open — coverage row). Eight Open — coverage rows remain after archive, at the floor of 8, so nothing was refilled.
## 2026-09-26 — audit 1812–1820 (D-2853…D-2861)

Reviewed `d15d25c20` through `4373171cb` (nine JS commits). Nine ACCEPT. No QUALITY-RISK. No REJECT. No Must-fix. No `js/` edits. Nine Open — coverage rows remain, above the floor of 8, so nothing was refilled. Next cluster stays `sp_lev.c` `get_coord`.

**Cadence:** `frozen/ps_test_runner.mjs sessions` on `4373171cb` — 44/44, screens 11,405/11,405, RNG 792,838/792,838, speed `247+1.60/turn` (R² 0.76). `leaderboard.mjs`: held-out still 12/44, 6,111/11,265 pts, RNG 29.6 %, screens 54.2 % (last scored 2026-09-26T07:02Z). `hidden-proxy.mjs score --jobs 8`: private board 12/12, RNG 75,151/75,151, screens 653/653. The 614/940 cache is still absent, so that board was not re-measured.
## 2026-09-26 — D-2861 `reset_commands` rebinds movement for number_pad, swap_yz, phone, and pcHack

**C locus:** `nethack-c/upstream/src/cmd.c:3344–3476` `reset_commands`. Callees `cmdbind_add` (`cmd.c:2125`, live `js/cmd.js:1264`), `cmdbind_swapkeys` (`cmd.c:2194`, `js/cmd.js:1521`), `cmdbind_remove` (`cmd.c:2161`, `js/cmd.js:1315`), `ext_func_tab_from_func` (`cmd.c:3015`, `js/cmd.js:1553`), `bind_key_fn` (`cmd.c:2731`, `js/cmd.js:1569`), `commands_init` (`cmd.c:2749`, `js/cmd.js:1584`), `bind_key` (`cmd.c:2661`, live `js/cmd.js:1345`), `update_rest_on_space` (`cmd.c:3483`, `js/cmd.js:1643`), `cmd_from_func` (`cmd.c:3035`, live `js/dokeylist.js`). `spkeys_binds` is `cmd.c:3161–3191`. `move_funcs` is `cmd.c:2070–2083` (`N_DIRS` 8, `N_MOVEMODES` 3). Calls: `options.c:2618` `optfn_number_pad` do_set; `options.c:5944` `handler_number_pad` after a menu pick; `options.c:7158` `initoptions_init` `reset_commands(TRUE)`. `cmd.c:2006` and `:3478` are comments. `number_pad()` at `:2619` and `:5945` is the `winprocs.h:161` tty no-op. `update_rest_on_space` is also called from `options.c:5426` (`optfn_boolean` `rest_on_space`) and `options.c:7364` (`initoptions_finish`).
**JS:** `js/cmd.js` `reset_commands` `:1667`, boot stand-in `:1671`, ylist swap `:1712`, pcHack `:1724`, phone `:1735`, dirchars `:1746`, backup/remove `:1752`, `bind_key_fn` `:1766`, `update_rest_on_space` call `:1775`, `extcmd_char` `:1777`. `update_rest_on_space` `:1643`, `commands_init` `:1584`, `bind_key_fn` `:1569`, `ext_func_tab_from_func` `:1553`, `cmdbind_swapkeys` `:1521`, `cmdbind_add` `:1264`. `js/dokeylist.js` `cmdbinds_live` `:272`. `js/options.js` `optfn_number_pad` `:2025`, `get_val` `:2040`, `handler_number_pad` `:2119`, `optfn_boolean_do_set` `:7336`.
**Change:** One `reset_commands` in that C order. Slots stay unset until the function runs, so a session that never calls it still uses `build_default_cmdbinds` (0 txt diffs against that table after `reset_commands(true)`). The first non-initial call, and `update_rest_on_space` when slots are missing, run the TRUE boot once: that is `options.c:7158`, which has no JS function, and the static `backed_dir_cmd` starts false.
**Verify:** `node scripts/verify.mjs --fn reset_commands` → PASS syntax (3 changed js files: js/cmd.js js/dokeylist.js js/options.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** `bind_mousebtn` (`cmd.c:2624`, called from `commands_init` `:2752–2753`); `click_to_cmd` still reads `game.Cmd.mousebtn` and stays inert. `initoptions_finish` `:7364`.
**Next:** `sp_lev.c` `get_coord` (next Open — coverage row). Nine Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2860 `peffect_oil` burns lit oil, then clears slime

**C locus:** `nethack-c/upstream/src/potion.c:1260–1294` `peffect_oil`. Callees `pline` (`pline.c`, live `js/display.js:7927`), `You` (`pline.c:355`, live `js/display.js:7671`), `likes_fire` (`mondata.h:196`, live `js/monsters.js:432`), `body_part` (`polyself.c:626`, live `js/polyself.js:626`), `losehp` (`hack.c:4255`, live `js/hack.js:1779`), `d` (`rnd.c`, live `js/rng.js:126`), `burn_away_slime` (`timeout.c`, live `js/timeout.js:1623`), `exercise` (`attrib.c:199`, live `js/attrib.js:199`). `Fire_resistance` / `Cold_resistance` are `youprop.h:28` and `:32` (`uprops[].intrinsic || extrinsic`). Calls: `potion.c:1412` inside `peffects`. `potion.c:34` is the static declaration.
**JS:** `js/potion.js` `peffect_oil` `:375`, `likes_fire` `:379`, `You` `:384`, `hero_element_resistance` `:387`, `losehp` `:391`, `finish_losehp_done` `:399`, `finish_maybe_wail` `:402`, `burn_away_slime` `:405`, `exercise` `:411`. Import `burn_away_slime` `:156`.
**Change:** One `peffect_oil` in that C order. Lit and `likes_fire(youmonst.data)`: "Ahh, a refreshing drink." and `good_for_you`. Otherwise `You("burn your %s.", body_part(FACE))`, then `losehp(d((!Fire_resistance || Cold_resistance) ? 4 : 2, 4), "quaffing a burning potion of oil", KILLED_BY)`.
**Verify:** `node scripts/verify.mjs --fn peffect_oil` → PASS syntax (1 changed js file: js/potion.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (potion.js is outside the shared-file set) · VERIFY: PASS.
**Named:** The green-slime extra damage in the comment at `potion.c:1269–1273` is not an arm. `losehp` still treats polymorphed `mh < 1` as fatal (`rehumanize` deferred), so a lit quaff that drops poly HP calls `finish_losehp_done`.
**Next:** `cmd.c` `reset_commands` (next Open — coverage row). Ten Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2859 `fall_asleep` stops the occupation, then sleeps

**C locus:** `nethack-c/upstream/src/timeout.c:951–974` `fall_asleep`. Callees `stop_occupation` (`allmain.c:683`, live `js/hack.js:1608`), `nomul` (`hack.c:4160`, live `js/hack.js:1582`). `You_can_move_again` is `decl.c:47` "You can move again." The `#if 0` block (`You_hear`, `incr_itimeout(&HDeaf)`, `Hear_again`) is not compiled. Calls: `eat.c:2595`, `hack.c:3045`, `mhitu.c:1862` (`#ifdef PM_BEHOLDER`, not defined), `potion.c:909`, `spell.c:491`, `timeout.c:790` (`nh_timeout` `SLEEPY`), `trap.c:1575` (`trapeffect_slp_gas_trap`), `uhitm.c:3502` (`mhitm_ad_slee` mhitu arm), `zap.c:2864` (`zapyourself`), `zap.c:4461` (`zhitu` sleep ray).
**JS:** `js/hack.js` `fall_asleep` `:1681`, `stop_occupation` `:1682`, `nomul` `:1683`, `usleep` `:1687`, `nomovemsg` `:1688`. `js/timeout.js` `incr_itimeout_HSleepy` `:398`, `Sleepy` `:408`, expiry arm `:1334`, `You` `:1344`, `fall_asleep` `:1346`. `js/trap.js` `trapeffect_slp_gas_trap` hero arm `:5133`, `fall_asleep` `:5141`, `steedintrap` `:5144`.
**Change:** One `fall_asleep` in that C order. `await stop_occupation()`, then `nomul(how_long)`, `multi_reason = "sleeping"`, `u.usleep = moves`, `nomovemsg` "You wake up." or "You can move again." `nh_timeout` on `SLEEPY` expiry: `unconscious()` or `Sleep_resistance` extends `HSleepy` by `rnd(100)`; else if `Sleepy`, `You("fall asleep.")`, `sleeptime = rnd(20)`, `fall_asleep(-sleeptime, TRUE)`, then `incr_itimeout(sleeptime + rnd(100))`. The hero sleep-gas arm `seetrap`s, then resistance or `breathless(youmonst.data)` says the cloud and `monstseesu(M_SEEN_SLEEP)`, else the gas line, `fall_asleep(-rnd(25), TRUE)`, `monstunseesu`, then `steedintrap`.
**Verify:** `node scripts/verify.mjs --fn fall_asleep` → PASS syntax (8 changed js files: js/eat.js js/hack.js js/mhitu.js js/potion.js js/spell.js js/timeout.js js/trap.js js/zap.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** The `#if 0` deafness / `Hear_again` block in `fall_asleep` is not in this build. `gazemu`'s beholder `AD_SLEE` (`mhitu.c:1856–1867`) stays out for the same `#ifdef`.
**Next:** `potion.c` `peffect_oil` (next Open — coverage row). Eleven Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2858 `status_initialize` enables every status field in C order

**C locus:** `nethack-c/upstream/src/botl.c:1683–1720` `status_initialize`. Callees `impossible` (`pline.c`, live `js/display.js:8116`), `init_blstats` (`botl.c:1758`, live `js/botl.js:167`), `(*windowprocs.win_status_init)()` (tty: `tty_status_init` `wintty.c:4336`, now `js/botl.js:315`, which calls `genl_status_init` `windows.c:893`), `status_enablefield` (`winprocs.h:185` → `tty_status_enablefield` `wintty.c:4364` → `genl_status_enablefield` `windows.c:922`), `panic` on reassess-before-init. Field order is `wintty.c:4278–4299`. Calls: `allmain.c:721` `status_initialize(FALSE)`; `options.c:5350` and `:5389` `REASSESS_ONLY`; `polyself.c:123` `REASSESS_ONLY`; `wintty.c:505` inside `new_status_window`. `winprocs.h:182` is a comment.
**JS:** `js/botl.js` `status_initialize` `:342`, `tty_status_init` `:315`, `genl_status_init` `:280`, `genl_status_enablefield` `:300`, `tty_status_enablefield` `:307`, `new_status_window` `:392`, reassess call `:405`.
**Change:** One `status_initialize` in that C order. A second full init calls `impossible` and continues: `init_blstats` returns, `tty_status_init` runs again, `blinit` stays true, then the field loop. Reassess throws `status 'reassess' before init` when `blinit` is false.
**Verify:** `node scripts/verify.mjs --fn status_initialize` → PASS syntax (4 changed js files: js/allmain.js js/botl.js js/options.js js/polyself.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** `display_nhwindow(WIN_STATUS, FALSE)` in `genl_status_init` (`windows.c:905`). `tty_clear_nhwindow` (`wintty.c:1034`) and `tty_destroy_nhwindow` (`wintty.c:2009`) in `new_status_window`; the second clear would also set `disp.botlx` (`wintty.c:1072`), which the reassess call sets on the next line.
**Next:** `timeout.c` `fall_asleep` (next Open — coverage row). Five coverage rows appended (`mhitm_ad_deth`, `obj_timer_checks`, `mhitm_ad_dcay`, `walk_path`, `get_level_extends`) so the band stays at 12 after archive. `--rows 8` is the never-re-pop Stale head.
## 2026-09-26 — D-2857 `unplacebc_core` lifts the ball and chain before the map update

**C locus:** `nethack-c/upstream/src/ball.c:147–177` `unplacebc_core`. Callees `Is_waterlevel` (`dungeon.h`, live `js/const.js`), `carried` (`obj.h:332` `where == OBJ_INVENT`), `obj_extract_self` (`mkobj.c:2556`, live `js/mkobj.js:3476`), `Blind` (`youprop.h:103`, file-local `Blind_bc`), `maybe_unhide_at` (`mon.c:4698`, live `js/monmove.js:1347`), `newsym` (`display.c`, live `js/display.js:5104`). Calls: `unplacebc` `ball.c:218`, `unplacebc_and_covet_placebc` `ball.c:230`. `ball.c:14` is the static declaration. `ball.c:302` and `:321` are the `BREADCRUMBS` wrappers; `config.h:644` leaves that macro undefined.
**JS:** `js/ball.js` `unplacebc_core` `:449`, `maybe_unhide_at` `:471` and `:477`, `newsym` `:472` and `:478`, `unplacebc` `:486`, core call `:491`, covet core call `:521`. Import `js/ball.js:51`.
**Change:** One `unplacebc_core` in that C order. Swallowed: the water level still extracts a ball that is not `OBJ_INVENT` and always extracts the chain, then returns before vision. Otherwise the same `where` test extracts the ball, a felt `BC_BALL` restores `u.bglyph` through `set_levl_glyph`, then `maybe_unhide_at` and `newsym`.
**Verify:** `node scripts/verify.mjs --fn unplacebc_core` → PASS syntax (7 changed js files: js/ball.js js/do.js js/mhitu.js js/shk.js js/teleport.js js/trap.js js/wizcmds.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS.
**Named:** A null ball or chain returns; C would dereference it. `maybe_unhide_at` still skips the hero `u_at` / `u.uundetected` arm (`mon.c:4706–4709`).
**Next:** `botl.c` `status_initialize` (next Open — coverage row). Eight Open — coverage rows remain after archive, at the floor of 8, so nothing was refilled.
## 2026-09-26 — D-2856 `pre_mm_attack` unhides both monsters before the blow

**C locus:** `nethack-c/upstream/src/mhitm.c:41–72` `pre_mm_attack`. Callees `seemimic` (`mon.c`, live `js/mon.js:1178`), `M_AP_TYPE` (`monst.h:73`, live `js/const.js:3218`), `canspotmon` (`display.c`, live `js/display.js:1366`), `map_invisible` (`display.c`, live `js/display.js:1374`), `newsym` (`display.c`, live `js/display.js:5104`). `gv.vis` is the file-local `_mm_vis` set in `mattackm`. Calls: `missmm` `mhitm.c:81`, `hitmm` `mhitm.c:657`. `mhitm.c:13` is the static declaration. `mhitm.c:745` is a comment inside `gazemm`, not a call.
**JS:** `js/mhitm.js` `pre_mm_attack` `:4022`, `seemimic` `:4029` and `:4036`, `mundetected = 0` `:4032` and `:4039`, `map_invisible` `:4045` and `:4049`, `newsym` `:4047` and `:4051`.
**Change:** One `pre_mm_attack` in that C order. Defender then attacker: `seemimic` when `M_AP_TYPE` is set, otherwise clear `mundetected`. `showit` becomes true only when a reveal happens while `_mm_vis` is set.
**Verify:** `node scripts/verify.mjs --fn pre_mm_attack` → PASS syntax (1 changed js file: js/mhitm.js) · PASS rule2 · note hidden (no corpus session blocked on it at baseline; the queue row cited 0 blocks) · PASS reach (no RNG-tagged reach; smoke 12/12, 0 regressed → REACH-OK) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS.
**Named:** None inside `pre_mm_attack`. `gazemm` (`mhitm.c:746–748`) still does its own mimic `seemimic` and unconditional `mundetected = 0`.
**Next:** `ball.c` `unplacebc_core` (next Open — coverage row). Nine Open — coverage rows remain after archive, above the floor of 8, so nothing was refilled.
