# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-06 — D-1917 were.c new_were mon_break_armor caller wiring

**C locus:** `were.c` `new_were` `:100–140` (`:129` `mon_break_armor(mon, FALSE)` before `:130` `possibly_unwield(mon, FALSE)` — opposite order to `mon.c` newcham `:5484–5485`); `worn.c` `mon_break_armor` `:1177–1335` re-read and stands as D-1914 ported (breakarm destroy incl. `obj.h:347–356` dragon-merge ranges re-checked against the JS, sliparm lose, gloves+shield, horns non-flimsy helm, slithy/centaur boots, saddle, DISMOUNT_FELL + `rnl(3)` tail).
**JS:** `js/were.js` (+14/−4); 1 js file (< 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** C-order wiring in `js/were.js`: `const mba = mon_break_armor(mon, false)` (canonical `js/worn.js` export, D-1914) then `possibly_unwield`, chained sync-or-async exactly like newcham `after_armor` (`if (mba) return Promise.resolve(mba).then(after_armor); return after_armor();`) so armor mutations run inline, message thunks flush first, and the `void|Promise` contract both existing callers rely on (`mhitu.js` fire-and-forget, `potion.js` `await`) is preserved. New import is imports.mjs-checked hoisted-SAFE (same 88-module SCC, function binding; `were.js` reads no worn binding at top level).
**Verify:** `node scripts/verify.mjs --fn mon_break_armor` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/were.js`; `PASS rule2`; `note hidden verify mon_break_armor: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Import smoke (`node --input-type=module -e`): `new_were: function`, `mon_break_armor: function` (no TDZ/cycle break).
**Named:** `monflee` onscary (`svc.context.mon_moving` + mux/muy scary-near arm); file-level howl `You_hear`/`wake_nearto`; Soundeffect; `impossible()` unknown-lycanthrope arm (unchanged).
**Next:** next Open queue row in order (`mhitm.c` mattackm remaining arms).
## 2026-09-06 — D-1916 uhitm.c hmonas polymorphed-hero weaponless attack-type envelope

**C locus:** `uhitm.c` `hmonas` `:5424–5860` (multi pre-count `:5436–5450` incl. `odd_claw=TRUE`; `use_weapon:` toggle `:5472`; weaponless hit `:5579–5668` — seduce `:5582–5594`, verb/silver `:5597–5643`, shade `:5645–5650`, grab/hit `:5651–5668`; BREA/SPIT/GAZE `:5812–5816`; default `impossible` `:5818`; knockback `:5831`; `passivedone` `:5835–5850`).
**JS:** `js/uhitm.js` (+98/−35), `js/mhitm.js` (+1/−1 export word); 2 js files, 99 insertions (< 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** C-order weaponless envelope in `js/uhitm.js`: shared-`dhit` discipline (WEAP + weaponless assign the outer; BREA/SPIT/GAZE reset 0); seduce keeps no-wakeup + `damageum(...,0)`; `wakeup` before the verb switch; per-aatyp verb with `odd_claw=!odd_claw` + `special_dmgval(W_ARMG|ring-mask)` for CLAW/TUCH (`W_ARMF` KICK, `W_ARMH` BUTT), shade `attack` override + `Your <verb> <vtense> harmlessly through` when `!specialdmg`, else canonical `failed_grab(game.youmonst,mon,mattk)` (newly exported from `js/mhitm.js`, no new edge — uhitm already imports mhitm) then TENT `Your tentacles suck` vs `You <verb>` (CLAW→`hit`) + verbose `silver_sears` + `damageum(...,specialdmg)`; pre-loop `multi_claw` count + `odd_claw=TRUE` + WEAP-arm toggle before the bimanual gate; `get_mattk(ym,i,mon,sum)` both loops (C `sum` arg; pre-loop all-MISS is a no-op, in-loop enables the DISE→STUN substitution the getter already models); unknown aatyp `await impossible('strange attack of yours (%d)')` with passive running; `if (mhitm_knockback(...)) break` (stub still FALSE — RNG burn order unchanged). `W_ARMF` added to the const.js import; no new clones (`failed_grab_you` stays for the hugs/ENGL callers).
**Verify:** `node scripts/verify.mjs --fn hmonas` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mhitm.js js/uhitm.js`; `PASS rule2`; `note hidden verify hmonas: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probes (`/tmp/probe-*.mjs`, deleted): poly CLAW vs shade → `Your claws pass harmlessly through it.` (C `:5645–5650` shape, non-humanoid shade keeps `claws`); silver-ring CLAW vs shade → `You hit it. Your silver ring sears it!`, mhp 100→85 (specialdmg reaches damageum).
**Named:** `gv.vis` set/reset (display global; hmonas messages always pline for youmonst magr); `getmattk` `alt_attk` out-param (unconcerned-hug copy already handled on the fresh `get_mattk` return); `failed_grab_you` retained for hugs/ENGL; remaining `mhitm_ad_*`.
**Next:** next Open queue row in order (`worn.c` mon_break_armor — canonical export shipped D-1914; close on pop or wire the `were.c` caller).
## 2026-09-06 — D-1915 steed.c dismount_steed remaining arms (poly/engulfed/water/lava steed death)

**C locus:** `steed.c` `dismount_steed` `:575–822` (save_utrap `:583`; POLY `:618`, ENGULFED `:623`, BONES `:626`; rain `:648`; noisy `:665`; mtrapped `:671`; steedcc 3-tier `:693–698`; place `:703`; BONES rloc `:708–713`; hero-first note `:718`; water/lava `:727–736`; teleds + boulder `:767` + mintrap `:771–772`; steed-move `:778`; no-room killed/monkilled `:791`; float_down `:809–814`; unweapon `:819–820`).
**JS:** `js/steed.js` (+140/−26), `js/mon.js` (+1/−1 export word); 2 js files, 141 insertions (< 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** C-order completion in `js/steed.js`: save_utrap before the switch; BYCHOICE nameless Hallu rain (`Hallucination()`); local `stealth_now()` youprop.h-Stealth mirror for the `:665` FALSE→TRUE noisy-now edge (monmove.js owns the same macro for movement); utraptype BEARTRAP/PIT/WEB → `mtrapped = 1`; steedcc + 3-tier enexto (mdat, `mons(PM_BAT)` flyer, `mons(PM_GHOST)` any) + `place_monster` under `in_steed_dismounting`; BONES enexto-rloc_to / rloc(ERR|NOMSG) early return; hero-first arm with grounded water/lava death (`killed` + `adjalign(-1)` unless cant_drown/likes_lava; `!Underwater`, `surface`, `hliquid` plines) and the [ALI] dead-steed gate around teleds_simple + boulder `sobj_at` sokoban_guilt + save_utrap mintrap; no-room BYCHOICE `killed` vs `monkilled('', 0)` (-AD_PHYS, AD_PHYS is 0); float_down block gains encumber_msg, the botl-only else arm, and polearm unweapon. `cant_drown` promoted to export in `js/mon.js` (one word; uhitm.js keeps its local). All new edges imports.mjs-checked (mon ALREADY, teleport ALREADY, monsters ALREADY, uhitm/mhitm/sit hoisted-SAFE); no new clones (sobj_at/which_armor_saddle/Stealth-macro reuse file locals).
**Verify:** `node scripts/verify.mjs --fn dismount_steed` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mon.js js/steed.js`; `PASS rule2`; `note hidden verify dismount_steed: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; full `sessions` 44/44 run explicitly (fortress holds; speed `50+0.39/turn` R² 0.845).
**Named:** uhitm DISMOUNT_KNOCKED `u.dx`/`u.dy` caller; update_mon_extrinsics; teleds_simple subset (ball/chain, utrap clear, swallow/hideunder/drag — canonical teleds owns them); artifact saddle `untouchable`.
**Next:** next Open queue row in order (`uhitm.c` hmonas remaining arms).
## 2026-09-06 — D-1914 mon.c newcham remaining body arms (mon_break_armor, boulders, monst_to_any)

**C locus:** `mon.c` `newcham` `:5276–5535` (`:5485` mon_break_armor call, `:5495–5514` boulder loop, light arms with `monst_to_any`); `worn.c` `mon_break_armor` `:1177–1335` + `m_lose_armor` `:1039–1051` (static); `hack.c` `monst_to_any` `:88–94`; `mthrowu.c` `m_useup` `:1161–1170` + `m_useupall` `:1153–1158`; `light.c` `del_light_source` `:99–140` (stored-union-bytes compare = monst pointer identity) + `new_light_source` `:88`; `obj.h` dragon macros `:347–356`.
**JS:** `js/worn.js` (+216/−5), `js/makemon.js` (+72/−14), `js/mthrowu.js` (+18), `js/hack.js` (+11), `js/do_wear.js` (+3/−2); 5 js files, 320 insertions (< 600 cap); `docs/c-js-map/turns.md` one section.
**Change:** canonical `export function mon_break_armor` + local `m_lose_armor` in `js/worn.js` in C order (breakarm destroy incl. dragon-merge silent arm, artifact-cloak lose vs rip, shirt rip; sliparm lose incl. whirly/cloak/shirt texts; gloves+shield; horns non-flimsy helm; slithy/centaur boots; saddle; DISMOUNT_FELL + touch-petrify `rnl(3)`); sync-or-async via ordered message thunks (text captured inline in C order, sync mutations inline, sequential flush) so NO_NC_FLAGS keeps its boolean contract (D-1648); canonical `export function m_useup` in `js/mthrowu.js` (quan/weight else extract_from_minvent — the arm the locals skip); `cloak_simple_name` promoted from `js/do_wear.js`; `export function monst_to_any` in `js/hack.js` (collapses to mtmp: C compares stored-union bytes, JS LS_MONSTER keys by the stable object); newcham chain possibly_unwield → after_armor → after_boulder → Elbereth with `newcham_drop_boulders` (pre-scan stays sync; bypass/extract/flooreffects/place under a DEADMONSTER gate). All imports imports.mjs-checked (hoisted-SAFE or lazy-only; se ids from `generated/seffects_data.js` like mhitu.js).
**Verify:** `node scripts/verify.mjs --fn newcham` → VERIFY: PASS — `PASS syntax 5 changed js file(s): js/do_wear.js js/hack.js js/makemon.js js/mthrowu.js js/worn.js`; `PASS rule2`; `note hidden verify newcham: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none for newcham and mon_break_armor — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44` (auto: shared files changed).
**Named:** W_ARMG `mselftouch` (newcham arm; trap.js live); ustuck expels/unstuck; poly_steed; muse/zap/mhitm/uhitm `m_useup` locals (predate the canonical export; untouched); mhitu/uhitm/trap `cloak_simple_name` locals (untouched); `worn.c` mon_break_armor Open queue row (same function now present — close on pop, or use for were.c `:129` caller wiring).
**Next:** next Open queue row in order (`steed.c` dismount_steed remaining arms).
## 2026-09-06 — D-1913 trap.c lava_effects remaining arms (Fire/Wwalking survive, invent burn, sink-and-die)

**C locus:** `trap.c` `lava_effects` `:6794–6987` (196 lines; brief `:6792–6987`); macros `youprop.h:28` Fire_resistance, `:260` Wwalking, `:387` Lifesaved, `flag.h:30,33` wizard/discover; callers `hack.c:3299` spoteffects, `trap.c:4108` spoteffects, `dbridge.c:412`; killer `lava_killer[]` `:6788` (`molten lava`).
**JS:** `js/trap.js` (+236/−20, 1 file, < 600 cap); `docs/c-js-map/data.md` one section.
**Change:** full C-order port in `js/trap.js`: `d(6,6)` first; `feel_newsym` + `burn_away_slime` + `likes_lava(youmonst.data)` early FALSE; `usurvive = Fire||(Wwalking&&dmg<uhp)` with uprops slots + JS flats and `Is_waterlevel`; `!usurvive` invent `in_use` flagging with one `protect_oid` + `impossible(%d)` (organic||POTION, `!oerodeproof`, `oc_oprop!=FIRE_RES`, SCR_FIRE/SPE_FIREBALL exempt, `obj_resists(0,0)`); boots burst first (`Yobjnam2 burst`, `in_lava_effects` guard, `Boots_off`, `useup` unless protected); `!Fire`: Wwalking `The lava here burns you!` + `losehp(dmg)`→burn_stuff else `You fall`; survive recompute `Lifesaved||discover`, wizard TRUE; guarded invent burn (protected re-flag, Book glow when survive+!Blind, worn burst+`remove_worn_item(TRUE)`+`useupall`, Another/Other/An/Some summary); boil-away poly check; 2x `uhp=-1`, killer rebuild, `urgent_pline boil/burn`, `done(BURNING)` (gameover→false), `safe_teleds(ALLOW_DRAG|TELEPORT)` break else `still burning`; `in_lava` decrement; `burncount==2` sets `HFire_resistance`/`HWwalking` TIMEOUT 5 (uprops slot + flat) →burn_stuff; `rescued_from_terrain(BURNING)` + `spoteffects(FALSE)` TRUE; `Fire+!Wwalking+!trapped` sink (`rn1(4,4)` + short-circuit `rn1(4,12)`, `set_utrap`, sink pline, `monstseesu/unseesu(M_SEEN_FIRE)`, `losehp 1|uhp/2`); burn_stuff `destroy_items(youmonst,AD_FIRE=2,dmg)` + `ignite_items(invent)` FALSE. `losehp` fatal via `finish_hero_losehp`. Dynamics for zap/pickup/timeout match file cycle pattern; same-edge helpers via dynamic in one locus edit (imports.mjs hoisted-safe).
**Verify:** `node scripts/verify.mjs --fn lava_effects` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/trap.js`; `PASS rule2`; `note hidden verify lava_effects: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 corpus blocks — TOP30 map row, brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`.
**Named:** none new — all arms live (no stub in a live arm); `sink_into_lava` needs no port here (C `lava_effects` never calls it; timeout burn path owns it).
**Next:** next Open queue row in order (`mon.c` newcham remaining body arms).
## 2026-09-06 — D-1912 role.c setup_rolemenu/racemenu/gendmenu/algnmenu extraction into js/player_selection.js

**C locus:** `role.c` `setup_rolemenu` `:2854–2902`, `setup_racemenu` `:2905–2940`, `setup_gendmenu` `:2943–2976`, `setup_algnmenu` `:2979–3012`; callers `genl_player_setup` pick arms (`:2312`, `:2407`, `:2495`, `:2582`) and `reset_role_filtering` (`:2740–2752`); callees `ok_role`/`ok_race`/`ok_gend`/`ok_align`/`an` (all live, imported not cloned).
**JS:** `js/player_selection.js` (+177/−85: four exports + five call-site rewirings, no new imports); `docs/c-js-map/startup.md` one section. js/ insertions 177 (< 600 cap), 1 js file.
**Change:** four canonical `export function setup_*menu` in `js/player_selection.js` in C order and C param order (minus `win`): `filtering && !*_ok` skip; `lowc`/`highc` accelerators (role: single key + `lastch`; race/gend/align: first-letter key + capital second accelerator when filtering, capital key when resetting); female-name arms (`gend == 1` replace, `gend < 0` slash-append); `filtering ? i + 1 : <name-string>` values (C `a_int`/`a_string`); `!filtering && !*_ok` preselect (C `MENU_ITEMFLAGS_SELECTED`); the C predicate asymmetries kept verbatim (race: no `ok_gend`; gender: no `ok_align`; align: no `ok_gend` — the inline loops already matched these). Neutral `{key, altkey, text, value, preselected}` entries; all five call sites (four pick menus + reset's four sections) map them onto body/choices and filter items. `lastch` is now C's case-sensitive update.
**Verify:** `node scripts/verify.mjs --fn setup_rolemenu` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/player_selection.js`; `PASS rule2`; `note hidden verify setup_rolemenu: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none for all four — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`. Hand probe (`/tmp/probe-setup.mjs`, deleted): old-vs-new key strings identical on current data (`abchkmprRstvw` both arms), values 1..13, race/gend/align key+alt pairs (`hH eE dD gG oO`, `mM fF`, `lL nN cC`), reset preselect all false unfiltered, `an()` texts unchanged (same `an` call, e.g. `a Caveman/Cavewoman`).
**Named:** `plsel_startmenu` (own named follow-up per D-1904; untouched); none new.
**Next:** next Open queue row in order (`trap.c` lava_effects remaining arms).
## 2026-09-06 — D-1911 mkmap.c finish_map wallify/lit/lava-ice + live MINES cutover to js/mkmap.js

**C locus:** `mkmap.c` `finish_map` `:330–363` (wallify `:340–341`, lit `:343–353`, lava-ice `:356–362`); callee `wallify_map` `sp_lev.c:2864–2891` (imported, not cloned); driver `mkmap` `:450–486` with the `:478` finish call and the `:480–484` walled+joined cavernous stamp; caller `sp_lev.c` `splev_initlev` LVLINIT_MINES arm.
**JS:** `js/mkmap.js` (+54/−17: finish export, `TREE`/`LAVAPOOL`/`ICE`/`ICED_POOL`/`ICED_MOAT`/`IS_OBSTRUCTED`/`IS_WALL` imports, driver word, header); `js/mklev.js` (+102/−338: `mkmap` import, `wallify_map` export word, 34 `async` + ~65 `await`, 240 deleted clone lines across three blocks); `docs/c-js-map/data.md` one section. js/ insertions 156 (< 600 cap), 2 js files.
**Change:** canonical `export function finish_map` in `js/mkmap.js` in C order (whole-map `wallify_map(1, 0, COLNO-1, ROWNO-1)` under `walled`; `!IS_OBSTRUCTED` fg/bg + `TREE` + `walled && IS_WALL` lit with per-room `rlit`; unconditional lava light; `icedpools ? ICED_POOL : ICED_MOAT`); `wallify_map` exported from `js/mklev.js` (one word) and imported, never cloned; `mkmap()` driver calls the local finish. Live cutover: `splev_initlev` is `async` and its MINES arm `await`s the canonical `mkmap` (cycle-safe per `imports.mjs --can`: hoisted-function edge only, no top-level reads); `async`/`await` propagated mechanically through the 30 MINES loaders + `hellfill_style_mines_lava`/`open_cavern` + `hellfill_run_style` + `load_hellfill` + `load_special_proto` (`makemaz` was already async and already awaited at its callers).
**Verify:** `node scripts/verify.mjs --fn finish_map` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mklev.js js/mkmap.js`; `PASS rule2`; `note hidden verify finish_map: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`. ESM smoke (`node -e import ./js/mkmap.js`): `finish_map function`, `mkmap`/`join_map AsyncFunction`, cycle loads clean.
**Named:** `litstate_rnd` `mklev.js` local retained (still used by the non-MINES `rlit` call site; canonical lives in `js/mkmap.js` — pre-existing D-1908 debt, unchanged); `MKMAP_WIDTH`/`HEIGHT` retained (`mkmap_flood_fill_rm` use); none new.
**Next:** next Open queue row in order (`role.c` setup_rolemenu/racemenu/gendmenu/algnmenu extraction).
## 2026-09-06 — D-1910 mkmap.c join_map + join_map_cleanup flood-fill regions + dig_corridor joins

**C locus:** `mkmap.c` `join_map_cleanup` `:245–255`, `join_map` `:257–328` (fill loop `:262–297`, `joinm:` join pass `:299–326`); callees `flood_fill_rm` `:153–242` (counter `gn.n_loc_filled`, bounds `gm.min/max_rx/ry`), `somexy`/`dig_corridor` (`mklev.c`), `add_room`.
**JS:** `js/mkmap.js` (+~120: 2 exports, 4 import lines, driver `async`+`await`, header); `js/mklev.js` (3 words); `docs/c-js-map/data.md` one section.
**Change:** canonical `export async function join_map` + `export function join_map_cleanup` in `js/mkmap.js` in C order: fill loop (`WIDTH`/`HEIGHT` bounds, `NO_ROOM` gate, bounds-object `n_filled` via `mkmap_flood_fill_rm`, `>3` → `add_room(..., FALSE, OROOM, TRUE)` + `irregular`, `>= MAXNROFROOMS*2` → labelled break for `goto joinm`, else tiny-hole erase against the un-incremented `nroom+ROOMOFFSET` stamp); join pass with C `:302` `somexy` short-circuit (second call only if the first succeeded — RNG order), the `await impossible(...)` arm with centre fallback (`|0` trunc of C int division), `dig_corridor(sm, em, null, false, fg_typ, bg_typ)` arg order, and the `:316–321` advance gate with load-bearing `||`/`&&` short-circuit on `rn2(3)`; cleanup strips roomnos then zeroes `nroom`/`nsubroom` with `hx = -1` tombstones on `rooms[0]` and `rooms[MAXNROFROOMS+1]` (= C `subrooms[0]`). `js/mklev.js` +3 words (`export` on `mkmap_flood_fill_rm`/`somexy`/`dig_corridor`, D-1908 precedent — same module edge `mkmap.js` already imports, no new cycle); `mkmap()` driver now `await`s the canonical join (async, still dead code — zero importers) and still delegates finish to the live clone.
**Verify:** `node scripts/verify.mjs --fn join_map` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mklev.js js/mkmap.js`; `PASS rule2`; `note hidden verify join_map: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`. Hand probe (`/tmp/probe-join.mjs`, deleted): both exports load in plain Node ESM, `join_map` is `AsyncFunction`, cleanup on a stub level zeroes `nroom`/`nsubroom`, tombstones `rooms[0]`/`rooms[41]`, strips `roomno` keeping `typ`.
**Named:** `finish_map` (own queued row; wallify/lit/lava-ice + maze/cavernous flags); live cutover of `splev_initlev` MINES to `js/mkmap.js` + deletion of the `mklev.js` clones (queued with the finish_map row — review-878 condition lands there, not here).
**Next:** next Open queue row in order (`mkmap.c` finish_map).
## 2026-09-06 — D-1909 pager.c Inhell_pager reads the dungeon hellish flag, not the Gehennom dnum

**C locus:** `dungeon.c` `In_hell` `:1941–1945` (`svd.dungeons[lev->dnum].flags.hellish`); `dungeon.h:140` `Inhell` = `In_hell(&u.uz)`; consumed in `pager.c:1232` (`idx != S_vibrating_square || Inhell || ...`).
**JS:** `js/pager.js` only (helper body + doc line + 1 import word); `docs/c-js-map/turns.md` one section.
**Change:** body now reads `!!(game.dungeons?.[game.u?.uz?.dnum | 0]?.flags?.hellish)` — the same flag expression as the `do.js`/`trap.js` siblings (same `| 0` undefined→0 fallback shape); doc comment cites `dungeon.c:1941–1945`. No new import (flag read is inline, zero TDZ risk); now-unused `GEHENNOM` dropped from the `const.js` import.
**Verify:** `node scripts/verify.mjs --fn Inhell_pager` → VERIFY: PASS — `PASS syntax 1 changed js file(s): js/pager.js`; `PASS rule2`; `note hidden verify Inhell_pager: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — review states latent/no live caller, brief confirms none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `skip full (no shared file changed)`. No hand probe: single boolean predicate, byte-identical shape to two session-covered siblings, no live caller yet.
**Named:** none new — `do_screen_description` showsyms scan + `describe_looked` rewiring still deferred (own rows); hoisting one shared `In_hell` export (e.g. from `js/dungeon.js`) left for a future iter that touches all three clones together.
**Next:** next queue row in order (`mkmap.c` join_map + join_map_cleanup).
## 2026-09-06 — Audit D-1901…D-1908 against pinned C (reviews 871–878) + cadence score

7 ACCEPT (871 domindblast deletion branch-exact; 872 mkmap passes, DIRS/macros byte-identical; 874 role parsers incl. end-anchored priest/priestess check; 875 tty_putstr wrap re-measured blocked→PASS PROGRESS; 876 wiz-goal 14-empty count; 877 mhitm AD arms, all import edges pre-existing; 878 mkmap driver, C-order exact), 1 QUALITY-RISK (873: `Inhell_pager` checks `dnum===GEHENNOM` instead of C `dungeon.c:1941–1945` hellish flag — clone #3 diverging from siblings `do.js:1202`/`trap.js:604`; Must-fix prepended, CURRENT Next cluster set to it). Full sessions 44/44 fortress holds (Scr 11,405/11,405, RNG 792,838/792,838); hidden proxy 255/265 excl. env (suit_simple_name closed by D-1905, +1).
## 2026-09-06 — D-1908 mkmap.c mkmap + init_map/init_fill cavern assembly + RNG fill envelope

**C locus:** `mkmap.c` `init_map` `:23–34`, `init_fill` `:36–52`, `N_P1_ITER`/`N_P2_ITER`/`N_P3_ITER` `:438–440` (=1/1/2), `litstate_rnd` `:442–448`, `mkmap` `:450–486`; caller `sp_lev.c` `splev_initlev` `:3002–3008` LVLINIT_MINES arm.
**JS:** `js/mkmap.js` (+~110: 3 consts, 4 exports, 4 import lines, header); `js/mklev.js` (+2 words: `export` on `join_map_fixed`/`finish_map`); `docs/c-js-map/data.md` one section.
**Change:** canonical exports in `js/mkmap.js`: `N_P1_ITER`/`N_P2_ITER`/`N_P3_ITER`, `init_map` (C field order NO_ROOM/typ/unlit), `init_fill` (limit `(WIDTH*HEIGHT*2)/5`=624, `rn1(WIDTH-1,2)`/`rnd(HEIGHT-1)`, occupied cells retry drawing no extra RNG), `litstate_rnd` (depth-gated `rnd(1+|depth|)<11 && rn2(77)` short-circuit, boolean passthrough), `mkmap` driver in C order (lit resolve → init_map → init_fill → N_P1/N_P2 loops → smooth-gated N_P3 loop → join-gated join → finish → walled+join cavernous stamp). C `:460`/`:485` new_locations alloc/free maps to the D-1902 per-call scratch (each pass fully rewrites its region before copy-back; strictly sequential calls) — documented, no shared buffer to own. join/finish delegate to the live `mklev.js` clones via aliased import (newly exported, behavior-neutral); their canonical ports are the next two queued rows.
**Verify:** `node scripts/verify.mjs --fn mkmap` → VERIFY: PASS — `PASS syntax 2 changed js file(s): js/mklev.js js/mkmap.js`; `PASS rule2`; `note hidden verify mkmap: no corpus session is blocked on it at HEAD` (vacuous; row cited 0 blocks — brief confirmed none — so no `--base` re-run owed); `PASS green 2/2`; `PASS strict` both gate sessions; `PASS cohort 7/7`; `PASS full 44/44 passing (auto: shared file changed)`. Import smoke: `node -e import('./js/mkmap.js')` → all four functions live, N_ITERS 1/1/2, no cycle. No hand probe: new exports are dead code (zero importers, like D-1902) and logic-identical to the session-covered live clones (same constants, same RNG call order).
**Named:** `join_map` + `join_map_cleanup` (own queued row; `somexy`-failure `impossible()` arm dropped by the `mklev.js` clone — adjudicated there, not here), `finish_map` (own queued row; ICED_POOL/ICED_MOAT encoding, wallify/lit arms), live cutover of `splev_initlev` MINES to `js/mkmap.js` + deletion of the `mklev.js` clones (queued with those rows).
**Next:** next Open queue row in order (`mkmap.c` join_map + join_map_cleanup).
## 2026-09-06 — Audit D-1909…D-1917 against pinned C (reviews 879–887, resume iter-2350) + cadence score

Resumed after iter-2350 transport death: leftover held 6 finished reviews (879–884) + Must-fix queue row + CURRENT Next-cluster edit; wrote the 3 missing SHA reviews (885–887) one-at-a-time with immediate files. 8 ACCEPT (879 Inhell_pager flag read byte-identical to do.js/trap.js siblings; 880 join_map; 881 finish_map; 882 setup_*menu; 884 newcham incl. Dragon formula re-verified for all 20; 885 dismount_steed branch-exact, -AD_PHYS≡0 verified; 886 hmonas envelope incl. WEAP odd_claw toggle at C line 50; 887 new_were :129 wiring), 1 QUALITY-RISK (883, lava Wwalking Must-fix — queued by the leftover, kept first). Re-measured every corpus claim with --base HASH~1: all vacuous-as-stated (0 blocked at baseline and now). Full sessions 44/44 fortress holds (Scr 11,405/11,405, RNG 792,838/792,838, speed 54+0.43/turn R² 0.85). Filled 878's missing Addressed hashes (D-1910 f7ff03a3, D-1911 373f2920). Queue 1 Must-fix + 10 Open, no refill owed.
