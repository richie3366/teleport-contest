# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-07 — D-2018 pickup.c tipcontainer_gettarget menu before empty check: empty sack asked "Where to tip", JS said "The bag is empty"

**C locus:** `pickup.c:3693–3760` `tipcontainer()` calls `tipcontainer_gettarget(box, &cancelled)` `:3706` before any check (even when empty; TODO comment explicitly defers an early empty give-up), then `tipcontainer_checks(box, targetbox, FALSE)` (`:4047–4050` prints `%s is empty` → TIPCHECK_EMPTY). `tipcontainer_gettarget` `:3871–3948`: `-` "on the floor" dummy SELECTED + blank + invent containers in order (skip box/non-containers/known BoT); first other container triggers `u_handsy()`; locked-known/no-hands excluded (a_obj 0, 4-space indent, invlet 0 else real invlet); `Where to tip the contents of <doname>` title; n>1 dummy-first quirk; n==-1 cancelled.
**JS:** 1 file (`pickup.js`, +140 diff lines incl. docs), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — all 4 corpus sessions reach the changed arm.
**Change:** `js/pickup.js` — new same-file `async tipcontainer_gettarget(box)` in exact C order (floor dummy + blank + invent scan with BoT/dknown/oc_name_known skip, one `await u_handsy()`, locked-known exclusion with 4-space indent, `doname(box)` title, corner-menu PICK_ONE loop with `-`/invlet selectors, Space/Return accept, ESC cancelled, n>1 dummy-first quirk); `tipcontainer()` calls it first and returns on cancelled, then runs the existing checks in C order; horn now receives `targetbox` (was `null`); new container-target arm prints `Objects tumble/An object tumbles into <the(xname)>` and `add_to_container`s each item. No new imports (doname/Is_container/paint_corner/dismiss/flush/nhgetch/u_handsy/ATR_INVERSE/theArt/xname all pre-existing); no new module edge, no TDZ.
**Verify:** `node scripts/verify.mjs --fn tipcontainer_gettarget` → `PASS syntax 1 changed js file(s): js/pickup.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify tipcontainer_gettarget: 1 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (92211 PASS; 92228 →lesshungry@54 was 40; 92236 →lesshungry@109 was 91; 92060 →formatkiller@69 was 55) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** BoH explode; ice-box thaw; shop billing (both floor and per-item targetbox addtobill/subfrombill); altarizing doaltarobj; cursed-mbag item-gone; otrapped chest_trap + nomul; dropy terse comma-list; toss_up; BoT-target apply; MENU_SEARCH/map_menu_cmd/multi-page (all pre-existing or carried in the `tipcontainer` doc comment).
**Next:** the three moved sessions' later owners are future rows for their owners (lesshungry ×2, formatkiller ×1).
## 2026-09-07 — D-2017 makemon.c newmonhp rider + home-elemental arms: Riders drew d(29,8), C draws d(10,8)

**C locus:** `makemon.c:1012–1054` `newmonhp()` — arm order golem `:1018–1020` → rider `:1021–1024` (`basehp = 10; d(basehp, 8)`, "low HP, but a high mlevel so they can attack well") → `mlevel>49` `:1025–1031` → adult dragon `:1032–1036` → `!m_lev` `:1037–1040` → else `:1041–1045` (`d(basehp, 8)` then `if (is_home_elemental(ptr)) mon->mhpmax = (mon->mhp *= 3)`, basehp untouched) → all-1s boost `:1047–1051`.
**JS:** 1 file (`makemon.js`, +6/−1), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — corpus sessions reach the rider arm (3 sessions) and the shared else body; the ×3 arm itself shares that verified body with no corpus session reaching it yet (same precedent as D-2014's dull/marker arms).
**Change:** `js/makemon.js` — `else if (is_rider(ptr))` inserted between golem and `mlevel>49` in exact C position (`basehp = 10; d(basehp, 8)`), and `if (is_home_elemental(ptr)) mon.mhpmax = (mon.mhp *= 3)` appended in the else arm verbatim (C `:1044–1045` comment included). No new imports: `is_rider` was already statically imported from `./monsters.js`, `is_home_elemental` is same-file, `d`/`In_endgame` already used — no new edge, no TDZ, `imports.mjs --can` not needed.
**Verify:** `node scripts/verify.mjs --fn newmonhp` → `PASS syntax 1 changed js file(s): js/makemon.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify newmonhp: 0 PASS, 3 moved past, 1 unchanged, 0 worse → PROGRESS` (Barbarian-91118 →use_pole@87 was 83; Knight-92149 →mhitm_ad_were@41 was 8; Tourist-92067 →exercise@140 was 133; Caveman-92118 still newmonhp@97, different mechanism) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 passing (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap). C is 42 lines; the +6 delta completes it (density exception: C is that small).
**Named:** none new — `newmonhp` is now complete vs C (all six arms + boost). `monhp_per_lvl` doc's stale "Named omit" line (it lists golem/high-mlevel/dragon arms the body already has) is pre-existing, untouched.
**Next:** Caveman-92118@97 is a different mechanism under the same owner label: both sides ROLL_FROM the same `nasties[]` (RNG matched) to GREEN_DRAGON with difcap `JABBERWOCK difficulty − 1` (green mlevel 15, `monsters.h:1540`), so both compute alt `big_to_little` = BABY_GREEN_DRAGON — but C `wizard.c pick_nasty` rejects the juvenile alt by name-string gate ("baby …"/hatchling/pup/cub) and keeps the adult (dragon arm `d(15,4)`), while JS `pick_nasty` (`js/makemon.js:1011`, doc-named omission "always accept non-geno alt") takes the baby (generic arm `d(13,8)`; baby mlevel 12 → m_lev 13). Queued as its own Open row (`wizard.c` pick_nasty juvenile gate). The three moved sessions' later owners (use_pole@87, mhitm_ad_were@41, exercise@140) are future rows for their owners.
## 2026-09-07 — D-2016 do.c doup ledger-1 escape yn: ask 'Beware, there will be no return!' instead of refusing the stairs

**C locus:** `do.c:1298–1344` `doup()` — `:1330–1335` ledger arm: `if (ledger_no(&u.uz) == 1) { if (iflags.debug_fuzzer) return ECMD_OK; if (y_n("Beware, there will be no return!  Still climb?") != 'y') return ECMD_OK; }`, then `next_to_u`, `at_ladder`, `prev_level(TRUE)` → `goto_level` ledger<=0 → `done(ESCAPED)` (`:1517–1519`).
**JS:** 1 file (`do.js`, +8/−6), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — corpus sessions reach the changed arm (4 sessions).
**Change:** `js/do.js` — ledger arm now C-verbatim: `game.iflags?.debug_fuzzer` early `ECMD_OK`, else `await y_n('Beware, there will be no return! Still climb?')` with `!== 'y'` → `ECMD_OK`; `'y'` falls through to `next_to_u`/`prev_level`, whose `goto_level` ledger<=0 → `done(ESCAPED)` arm is already live (D-1764). `y_n` added to the pre-existing static `./getline.js` import (same pattern as steed.js D-2000; hoisted function, no new module edge, no TDZ).
**Verify:** `node scripts/verify.mjs --fn doup` → `PASS syntax 1 changed js file(s): js/do.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify doup: 0 PASS, 4 moved past, 0 unchanged, 0 worse → PROGRESS` (Barbarian-92208 →disclose@10 was 9; Healer-92227 →disclose@13 was 12; Rogue-92160 →use_container@88 was 5; Valkyrie-92237 →dofire@88 was 11) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 passing (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** rooted, `stucksteed`, `u_stuck_cannot_go`, encumbrance load gate (all pre-existing; `stucksteed`/`u_stuck_cannot_go` have no JS export, `u_rooted`/`set_move_cmd` only local clones in cmd.js — kept in the `doup` doc comment, untouched).
**Next:** the four later owners are future rows for their owners (disclose ×2 — D-2015's arm now reached from these stair paths; use_container@88; dofire@88).
## 2026-09-07 — D-2015 end.c disclose conduct prompt: " and achievements" suffix via live count_achievements

**C locus:** `end.c:664–680` conduct arm — single `should_query_disclose_option('c', &defquery)`, then `int acnt = count_achievements()` (`insight.c:2493–2501`, counts `u.uachieved[]` to the zero terminator), `Sprintf(qbuf, "Do you want to see your conduct%s?", (acnt > 0) ? " and achievements" : "")`; `yn_function` asked only when `ask`, else `c = defquery`; `show_conduct((how >= PANICKED) ? 1 : 2)` on `'y'`.
**JS:** 1 file (`end.js`, +12/−8), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — corpus sessions reach the changed arm (7 sessions).
**Change:** `js/end.js` — `count_achievements` added to the pre-existing `./insight.js` import (`imports.mjs --can`: already statically imported, no new edge, no TDZ); conduct arm now does one `should_query_disclose_option('c')`, builds `qbuf` with the `(acnt > 0 ? ' and achievements' : '')` suffix in exact C position, and asks `yn_function` only when `ask`. `show_conduct` args untouched (`ENL_GAMEOVERALIVE/DEAD === 1/2`, already C-equal).
**Verify:** `node scripts/verify.mjs --fn disclose` → `PASS syntax 1 changed js file(s): js/end.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify disclose: 2 PASS, 3 moved past, 2 unchanged, 0 worse → PROGRESS` (Archeologist-92205 PASS; Priest-92082 PASS; Archeologist-92175 →name_to_monplus@81 was 77; Caveman-91109 →give_to_nearby_mon@78 was 73; Valkyrie-92074 →obj_resists@75 was 70; wish-Monk-92031 still disclose@49; wish-Priest-92098 still disclose@85) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `invent.c` `addinv_core1` uhave/ACH arms (`u.uhave.amulet/menorah/bell/book` + `record_achievement(ACH_AMUL/ACH_CNDL/ACH_BELL/ACH_BOOK)`, `invent.c:962–986`) stay named-deferred in the `js/u_init.js` `addinv` doc (questart/artitouch likewise, untouched).
**Next:** scen-wish-Monk-92031@49 and scen-wish-Priest-92098@85 are a different mechanism under the same owner label: both wish Bell/Candelabrum into inventory (C `addinv_core1` records ACH_BELL/ACH_CNDL, so C acnt > 0) while JS `addinv` never sets uhave/achievements (JS acnt = 0) — needs an `invent.c` `addinv_core1` uhave+`record_achievement` port as its own row. The three moved sessions' later owners (name_to_monplus@81, give_to_nearby_mon@78, obj_resists@75) are future rows for their owners.
## 2026-09-07 — D-2014 engrave.c engrave occupation: multi-action finish message and full wear/rate/room body

**C locus:** `engrave.c:1267–1493` `engrave()` occupation callback — teleport stop `:1286–1290`, hands/invent stylus walk `:1293–1305`, `actionct++` `:1311`, sanity impossibles `:1314–1318`, carving/marker rate `:1321–1331`, non-space rate scan `:1333–1337`, dulling split/dull `:1339–1395` + marker ink `:1396–1410`, `finishverb` switch `:1413–1433`, BUFSZ room `:1442–1446`, truncate print `:1452–1457`, `make_engr_at` + `eread`/`erevealed` `:1459–1467`, continue `:1469–1475` vs finish (`cannot write any more` / `finish %s.` iff `!firsttime`) `:1476–1492`; setup `doengrave` `:1237–1244` (`set_occupation(engrave, "engraving", 0)`, setup takes no time).
**JS:** 1 file (`engrave.js`, ~+200/−45), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — corpus sessions reach every changed arm (finger DUST finish in 5 sessions; dull/marker/room/truncate arms share the same verified body, no corpus session reaches them yet).
**Change:** `js/engrave.js` — full `async engrave()` in exact C order (renamed from `engrave_occupation` so `sym.mjs` finds the C name; `game.occupation` is awaited at `allmain.js:1174`). C `nextc` is a pointer into `text`; JS keeps `nextc` as the remaining suffix, so C `*endc = '\0'` is `eng.text = consumed + chunk`. Marker ink mirrors C (`ink_cost = spe` when dry, so `spe -= ink_cost` lands exactly 0).
**Verify:** `node scripts/verify.mjs --fn engrave` → `PASS syntax 1 changed js file(s): js/engrave.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify engrave: 2 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (Samurai-92043 PASS; Priest-92085 PASS; Caveman-92006 →x_monnam@80 was 74; Rogue-92209 →x_monnam@83 was 17; Barbarian-92152 →peffect_acid@71 was 65) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Direct replay quoted pre-verify: Samurai-92043 `PASS (RNG 3037/3037, Screen 106/106)`. Final verify ran after the last edit (no D-1831 gap).
**Named:** altar/jello/swallow/lava/pool floor gates; yn add-to (same-type defaults append); livelog (all pre-existing map defers, untouched). The `engrave` finish `newsym` uses `eng.pos` (== hero cell post-teleport-check), as C does.
**Next:** the three later owners are future rows for their owners (x_monnam ×2 — the `@`/farlook family D-2013 just extended; peffect_acid — new owner on Barbarian-92152@71).
## 2026-09-07 — D-2013 pager.c do_screen_description '@'-as-you arm: farlook at own square missed "or you" for non-human/elf heroes

**C locus:** `pager.c` `do_screen_description` check_monsters '@' special case `:1346–1353` (`looked ? sym==showsyms[S_HUMAN+SYM_OFF_M] && u_at : sym==def_monsyms[S_HUMAN].sym && !flags.showrace`, gated on `!(Race_if(PM_HUMAN) || Race_if(PM_ELF)) && !Upolyd` → `append_str(out_str, "you")`); `append_str` `:82–106` returns 1 so found 1→2, which fires the didlook block `:1595–1624` (`found > 1 || need_to_look` → `lookat()` → firstmatch=look_buf `" (dwarven … called wizard)"`, found back to 1). The S_HUMAN explain "human or elf" comes from the `:1329–1345` class loop (firstmatch preset — the comment's "firstmatch is assumed to already be set for '@'").
**JS:** 1 file (`pager.js` +9/−2), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — corpus sessions reach the arm (dwarven heroes self-farlooking in 5 sessions).
**Change:** `js/pager.js` — `describe_looked` self branch appends `' or you'` iff `urace.mnum !== PM_HUMAN && !== PM_ELF && !Upolyd(u)` (C `:1352` gate verbatim; `u_at` is the branch condition, the '@' sym its existing hardcoded prefix); `PM_HUMAN`/`PM_ELF` added to the existing `./generated/monsters_data.js` import (no new module edge, no TDZ — consts read at call time). `wishymatch` itself untouched: its C body (`objnam.c:3243–3338` dwarvish/elven/helmet/gauntlets/detect/ability/aluminum arms) plays no role in farlook output; the queue owner was the literal matcher hitting `"dwarven "` at `:3280`.
**Verify:** `node scripts/verify.mjs --fn wishymatch` → `PASS syntax 1 changed js file(s): js/pager.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify wishymatch: 0 PASS, 5 moved past, 0 unchanged, 0 worse → PROGRESS` (92175 →disclose@77 was 65; 92205 →disclose@123 was 48; 92023 →dosearch@65 was 58; 92040 →show_conduct@71 was 70; 92162 →step-70 map-cell screen diff, owner null — verify prints its null-owner fallback label `js-throw`, but a direct replay shows `error:null`, RNG 12144/14975, Screen 93/149, i.e. a later-owner screen move, not a throw) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `wishymatch` special-case arms (dwarvish/dwarven, elven/elvish/elfin, helmet→helm, gloves→gauntlets, detect↔detection, ability/abilities, aluminum/aluminium — already named in the map wish-subset row, untouched); `describe_looked` self branch keeps its pre-existing shape otherwise (no sym=='@'/invisible/swallowed re-modeling — different owners).
**Next:** the five later owners are future rows for their owners (disclose ×2 may join the queued `end.c` disclose row's set on rescore; dosearch; show_conduct — parked, this is a new session reaching it at 92040@71; 92162 step-70 map cell C ` ``` ` vs JS `^` at row 16 — unattributed, likely mimic/statue glyph territory).
## 2026-09-07 — D-2012 allmain.c interrupt_multi dropped its Norep message: full-health / full-energy never printed

**C locus:** `allmain.c:975–983` `interrupt_multi(const char *msg)` (`if (gm.multi > 0 && !travel && !run) { nomul(0); if (flags.verbose && msg) Norep("%s", msg); }`); call sites `:678` (`regen_hp` `reached_full`, `You are in full health.`) and `:617` (`regen_pw` `uen == uenmax`, `You feel full of energy.`); `regen_hp` body `:624–679` otherwise unchanged (heal/RNG arms already faithful per D-0035/D-1003); `youprop.h:143` `Sleepy` and `:345` `Regeneration` rechecked (JS `u_can_regen` matches).
**JS:** 1 file (`allmain.js`), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No throwaway probe — corpus sessions reach every changed arm.
**Change:** `js/allmain.js` — `interrupt_multi(msg)` is now `async`: live `nomul(0)` (`./hack.js`, pre-existing import edge extended — hoisted function declaration, no new module, no TDZ) then `if (msg && game.flags?.verbose !== false) await Norep(msg)` (`./display.js`, same — `!== false` is the repo idiom for verbose-default-on); `regen_hp`/`regen_pw` now `async` with `await interrupt_multi(...)`; both moveloop EOT call sites `await`ed (moveloop already async; both callees are local-only, no other callers). Small diff: C `interrupt_multi` is 9 lines.
**Verify:** `node scripts/verify.mjs --fn regen_hp` → `PASS syntax 1 changed js file(s): js/allmain.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify regen_hp: 2 PASS, 3 moved past, 1 unchanged, 0 worse → PROGRESS` (Knight-92232 PASS; Samurai-92057 PASS; Ranger-92234 → step 58 (was 42); Barb-92008 → toss_up@45 (was 33); Ranger-92090 → Blindf_off@211 (was 160); Arch-92084 still regen_hp@64) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** `rehumanize` on `mh < 1` (pre-existing defer in the `regen_hp` doc, untouched).
**Next:** scen-genesis-Archeologist-92084 is a different mechanism under the same owner label (stays `regen_hp` step 64): JS skipped the `:659` `rn2(100)` draw while C drew it, and C's message is owned by `hmonas(uhitm.c:5544)` with combat `stepFns` (`hitum`, `mattacku`, `hitmu`) — the nurse retaliation hit landed in C only, so the HP-gate inputs already differed before `regen_hp` ran; needs its own upstream-combat row (re-queue the `regen_hp` gate or the `mattacku`/`hitmu` retaliation). Ranger-92234 → step 58 `You suffocate.--More--` vs `You suffocate.` (RNG now 8585/8585 fully matched; choke/`dosearch` pager territory). Barb-92008 → `toss_up`@45; Ranger-92090 → `Blindf_off`@211 — each a future row for its owner.
## 2026-09-07 — Audit D-2003…D-2011 against pinned C (reviews 973–981) + cadence score 44/44

**Scope:** all 9 js/ SHAs since review 972 (`e673cee4`…`0e408b9f`), oldest-first, one file per SHA written as each finished. Every new/changed JS function walked against its C locus (`csym.mjs` body + `sym.mjs` resolutions); every hidden-verify claim re-measured with `--base HASH~1`.
**Result: 9 ACCEPT, 0 Must-fix.** Notable confirmations: D-2003 bare-`strstri` order/load-bearing female-first blanking (`read.c:3184–3194`); D-2004 `{mtype}` box + worm-tail exemption + triple-condition newcham; D-2005 four `_u` arms incl. plys `rn2(3)`-before-gate short-circuit + byte-identical prop clones; D-2006 lua draw order incl. bury-before-contents (`sp_lev.c:2428` vs `:3743`) + always-draw `obj_resists`; D-2007 twelve tips incl. `MS_SHRIEK = 18` vs `monflag.h:32`; D-2008 full `surface()` + `db_under_typ` internal mask; D-2009 confdir tail + impaired TIME (11-vs-11 slice); D-2010 single-`if` blast gate + complete async sweep (artifact↔hack new edge is hoisted-safe); D-2011 intrinsic-only per `youprop.h:108–113` + `PROP_FLAT` gap mechanism. Two precision notes only (D-2006 trap-retry comment off-frame; D-2010 "no new edge" overstates) — observations, not C-wrongs.
**Cadence:** full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `73+0.49/turn`. Queue steady at 9 Open, no Must-fix → no refill, no archiver run.
**Next:** port iter pops the live dosearch0 counted-search row (92191 residual).
## 2026-09-07 — D-2011 do.c danger_uprops: JS checked only the Stoned/Slimed/Strangled/Sick flats, missing uprops[].intrinsic where wiz_intrinsic STRANGLED lands

**C locus:** `do.c:2318–2322` `danger_uprops` (`Stoned || Slimed || Strangled || Sick`, i.e. `u.uprops[PROP].intrinsic` per `youprop.h:108–113`); `do.c:2325–2346` `cmd_safety_prevention` (unchanged, gate order kept); `wizcmds.c:1073–1079` default arm (`incr_itimeout(&u.uprops[p].intrinsic, amt)` — STRANGLED lands in uprops only, unlike SICK/SLIMED/STONED which go through `make_sick/make_slimed/make_stoned`); `timeout.c:32–36` menu order (`d` = STRANGLED).
**JS:** 1 file (`do.js` +14/−6), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). Throwaway probe `/tmp/probe_92191.mjs` (prefix replays dumping multi/occupation/uprops, deleted before finishing — kept out of the repo).
**Change:** `js/do.js` — `danger_uprops` checks flat `|0` OR `u.uprops[PROP].intrinsic` for STONED/SLIMED/STRANGLED/SICK (C `:2318–2322` cite; no H/E extrinsic — C checks intrinsic only); `STONED, SLIMED, STRANGLED, SICK` added to the existing `./const.js` import (no new module edge); stale `Named omissions: full danger_uprops bodies` dropped from the `cmd_safety_prevention` doc (visctrl/cmd_from_func beyond `'m'` stays named).
**Verify:** `node scripts/verify.mjs --fn cmd_safety_prevention` → `PASS syntax 1 changed js file(s): js/do.js` · `PASS rule2` · `PASS hidden verify cmd_safety_prevention: 3 PASS, 3 moved past, 1 unchanged, 0 worse → PROGRESS` (Tourist-92222 PASS; Arch-92178 PASS; Priest-91130 PASS; Caveman-92159 → step 59 (was 19); Monk-92121 → dosearch step 40 (was 20); Priest-92096 → dodrink step 118 (was 64); Monk-92191 still cmd_safety_prevention step 30) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `PASS full 44/44 (auto: shared file changed)` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** visctrl/cmd_from_func beyond `'m'` (pre-existing D-0228 defer, untouched).
**Next:** Monk-92191 step 30 is a different mechanism under the same owner label (new Open row this commit): probe shows JS STRANGLED 28→12 across steps 24–29 (16 turns burned where C burned ~1 — C's second `20s` multi-search ended within step 29 with no `You stop searching` print, i.e. silent `nomul(0)`-on-find, while JS kept ticking) yet still >0 at 30 with JS printing `''`, so the step-30 `s` never reached the gate in JS (counted-search multi/occupation lifecycle — `detect.c` dosearch0 / moveloop-cmdq territory, needs a C multi/occupation trace at steps 29–30 as falsifier). Caveman-92159 step 59 (`You suffocate.--More--` vs `You suffocate.` — strangulation-death pager arm) and the dosearch@40 / dodrink@118 moves are future rows for their owners.
## 2026-09-07 — D-2010 artifact.c touch_artifact hero blast: JS deferred the blast so wished quest artifacts never blasted

**C locus:** `artifact.c:907–974` `touch_artifact` (touch_blasted reset, NONART gate, yours/self_willed, badclass via `Role_if`/`Race_if`, badalign via SPFX_RESTR+align/record, `bane_applies`, blast gate `((badclass||badalign)&&self_willed)||(badalign&&(!yours||!rn2(4)))`, `d(Antimagic?2:4,self_willed?10:4)`, silver `rnd(10)`+`Maybe_Half_Phys`, `losehp` "touching …", `exercise(A_WIS,FALSE)`, evade/control pline); `:992–1005` `bane_applies` (DBONUS-only copy through `spec_applies`); `objnam.c:999–1008` xname ` named ` + downcase `The`→`the` for `oartifact`; `you.h` `Role_if`/`Race_if`, `youprop.h` Antimagic/Hate_silver/`Half_physical_damage`, `hack.h` `Maybe_Half_Phys`, `obj.h` `carried`; `retouch_object` `:2519+` reads `touch_blasted`.
**JS:** 7 files (`artifact.js` +78/−21, `objnam.js` +17/−1, `weapon.js` +6/−6, `mon.js` +2/−2, `invent.js`/`pickup.js`/`minion.js` 1-line awaits; +106/−33 total), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean).
**Change:** `js/artifact.js` — full hero `touch_artifact` in exact C order (now `async`; `Role_if`/`Race_if` badclass; bane via same-file `spec_applies`; single-`if` blast gate preserving `||`/`&&` short-circuit so `rn2(4)` draws only when C draws it; `You are blasted…` pline, `touch_blasted=true`, `d()`, silver `rnd(10)`+`maybe_half_phys`, `losehp`, `exercise(A_WIS,false)`; evade/control pline via live `carried`+`Tobjnam`); new `bane_applies`, `Race_if`, `Hate_silver_hero`, `Antimagic_hero`, module `touch_blasted`; `retouch_object` now `async`/`await` (its silver/bane-drop arms still deferred); `select_hwep` now `async` (monster bane denies in C order); every call site `await`s (`invent.js` hold_another_object, `pickup.js` pickup_object, `mon.js` meatmetal/meatobj, `weapon.js` mon_wield_item, `minion.js` guardian angel, `doinvoke`). `js/objnam.js` — C `:1006–1008` downcase at all three ` named ONAME` build sites (xname, doname, fake-arti lookup key). Import extensions only (`s_suffix`, `Tobjnam`, `KILLED_BY`/`LOW_PM`, `hates_silver`, `losehp`/`maybe_half_phys`, `exercise`/`A_WIS` — `imports.mjs --can` hoisted/cycle-safe or ALREADY, no new edge).
**Verify:** `node scripts/verify.mjs --fn touch_artifact` → `PASS syntax 7 changed js file(s)` · `PASS rule2` · `PASS hidden verify touch_artifact: 0 PASS, 7 moved past (1 still touch_artifact at a later step), 0 unchanged, 0 worse → PROGRESS` (91132 →losehp@23 was 22; 91135 →look_at_monster@148 was 67; 91110 →magic_whistled@78 was 49; 92094 →touch_artifact@73 was 72; 91134 →Mb_hit@110 was 50; 92081 →losehp@74 was 73; 92014 →losehp@46 was 45) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Final verify ran after the last edit (no D-1831 gap).
**Named:** monster covetous/mplayer role/align arms (`is_covetous`/`is_mplayer`/`mon_aligntyp` unwired — monster badclass/badalign stay false, bane still denies); `retouch_object` silver-hate/bane damage + remove_worn/drop arms; `spec_applies` pre-existing defers (`defended`, DFLAG1 mflags1, DFLAG2 yours/Upolyd/ulycn) unchanged.
**Next:** each later owner is its own future row (`losehp` death handling ×3, `look_at_monster`, `magic_whistled`, `Mb_hit`, second `touch_artifact` @73 in 92094 — likely a second artifact later in that session).
