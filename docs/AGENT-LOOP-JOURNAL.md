# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — D-2366 `zap.c` resist tell-shield `shieldeff_mon` arm

**C locus:** `nethack-c/upstream/src/zap.c` `resist` `:6100–6158` (`:6141` `resisted = rn2(100 + alev - dlev) < mr`; `:6143–6144` `if (tell) shieldeff_mon(mtmp)`; `:6145` `damage = (damage + 1) / 2`) + `nethack-c/upstream/src/mon.c` `shieldeff_mon` `:6056–6064` (`shieldeff(mx, my)` then `cansee` → `pline_mon "%s resists!"`).
**JS:** 1 js file (+7/−3 per `git diff --stat`: `js/zap.js` only; plus the 1-line `CURRENT.md` Next-cluster copy), under the 600/10 caps. Below the usual 40-insertion density floor because the C residual is genuinely two lines and the other 56 shipped in D-2116.
**Change:** `js/zap.js` only, no new modules, no new module edges (callee is a hoisted same-file `async function` declaration — no import, no TDZ read): the `if (resisted)` arm awaits `shieldeff_mon(mtmp)` when `tell`, before the halve, in C order/conjuncts; doc comment cites `:6100–6158`/`:6143–6144`. `shieldeff_mon` is display-only and the halve is unchanged, so zero RNG draws move. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn resist` → `PASS syntax 1 changed js file(s): js/zap.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify resist: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a tell+resisted `resist` arm with a visible shield delta (display-only arm; same disclosure as D-2363/D-2364).
**Named:** none new. Sleep `Resists_Elem` wielded/worn/artifact scan + `defended(mon, AD_SLEE)` stay deferred (D-2116 named, untouched); `music.js`/`pray.js` fixed-oclass `resist` clones stay exact-for-caller (untouched); R-1082 `sleep_monst` mimic-reveal debt stands (turns.md:563, untouched).
**Next:** do not re-pop `resist` for the tell-shield arm. Falsifier: a rescore or fresh `verify resist` showing a session blocked with `resist` as owner (not mere caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2365 `monmove.c` mon_would_consume_item body (corpse_eater corpse + pet dogfood)

**C locus:** `nethack-c/upstream/src/monmove.c` `mon_would_consume_item` `:1036–1050` (`CORPSE && !touch_petrifies(&mons[corpsenm]) && corpse_eater(mtmp->data)`; `mtame && has_edog(mtmp)` (guardian-angel exclusion) `&& (ftyp = dogfood(mtmp, otmp)) < MANFOOD && (ftyp < ACCFOOD || EDOG(mtmp)->hungrytime <= svm.moves)`) + `include/mondata.h:200` `touch_petrifies` (cockatrice/chickatrice) + `:243` `corpse_eater` (purple worm/baby, ghoul, piranha) + `include/mextra.h:162–165` `DOGFOOD=0/CADAVER=1/ACCFOOD=2/MANFOOD=3`. Sole C caller `m_search_items` `:1425` (`((take && carry>0) || consume) && touch` short-circuit, already live D-0183/D-0223).
**JS:** 2 js files (+~30/−8 per `git diff --stat`: `js/monmove.js` body + imports + 2 doc lines; `js/mon.js` 1 doc line), under the 600/10 caps. Below the usual 40-insertion density floor because the 14-line C function is that small and its caller wiring shipped in D-0183/D-0223.
**Change:** `js/monmove.js` only, no new modules, no new module edges (`imports.mjs --can` ALREADY on both: `dogfood` joins the existing `dogmove.js` import, `EDOG`/`has_edog`/`ACCFOOD`/`MANFOOD` join the existing `const.js` import — runtime-only reads inside the function body, no top-level TDZ read; `dogfood` is a hoisted export and the monmove↔dogmove cycle pre-exists): body in C order/conjuncts — `otmp.otyp === CORPSE && !touch_petrifies(mons(otmp.corpsenm)) && corpse_eater(mtmp.data)`, then `mtmp.mtame && has_edog(mtmp)` with `dogfood(mtmp, otmp) < MANFOOD && (ftyp < ACCFOOD || hungrytime <= moves)` using the repo dual-edog idiom (`EDOG(mtmp)?.hungrytime || mtmp.edog?.hungrytime || 0`, cf. `js/dog.js:614`) and `game.moves || 1` (cf. `js/dog.js:615`).
**Verify:** `node scripts/verify.mjs --fn mon_would_consume_item` → `PASS syntax 2 changed js file(s): js/mon.js js/monmove.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify mon_would_consume_item: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `PASS full 44/44` (auto: shared file changed) · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a corpse-eater-corpse or hungry-pet-food loot arm (same disclosure as D-2363/D-2364); the edited function is file-local, reachable only through level-state `m_search_items` dispatch, so a standalone probe would need full game state (disproportionate setup).
**Named:** none new. `can_touch_safely` silver/artifact/petrify arms stay stub-true (own row, untouched); `m_search_items` onscary inhishop/inhistemple + lminion/unique arms stay deferred in live `mon.js` arms (untouched).
**Next:** do not re-pop `mon_would_consume_item` for the corpse/pet-food arms. Falsifier: a rescore or fresh `verify mon_would_consume_item` showing a session blocked with `mon_would_consume_item` as owner (not mere `m_search_items` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2364 `muse.c` searches_for_item floor onscary + horn can_blow + unicorn is_unicorn

**C locus:** `nethack-c/upstream/src/muse.c` `searches_for_item` `:2706–2792` (floor `OBJ_FLOOR` + underfoot + `onscary` head; `UNICORN_HORN` `!cursed && !is_unicorn(data) && != &mons[PM_KI_RIN]`; `FROST_HORN/FIRE_HORN` `spe > 0 && can_blow(mon)`; `CORPSE` W_ARMG-`touch_petrifies` / `!resists_ston && cures_stoning(FALSE)`; `TIN` `mcould_eat_tin && !resists_ston && cures_stoning(TRUE)`; `EGG && ismnum` `touch_petrifies`) + `include/mondata.h:149` `is_unicorn(ptr) = mlet == S_UNICORN && likes_gems(ptr)` + `muse.c` `cures_stoning` (`POT_ACID`; slime `slimeproof`; corpse/openable-tin lizard-or-acidic) and `mcould_eat_tin` (animal reject; welded-mwep inventory scan for TIN_OPENER / P_DAGGER / P_KNIFE). Sole caller `monmove.c:1010` (`m_search_items` getitems gate).
**JS:** 1 js file (+13/−10 per `git diff --stat`: `js/muse.js` only; plus the 1-line `CURRENT.md` Next-cluster copy), under the 600/10 caps. Below the usual 40-insertion density floor because the rest of the 86-line C function shipped in D-0598 (potion/wand/scroll/amulet/tool) + D-0861 (Is_container/Is_mbag, macro re-verified: `obj.h:337-339` BAG_OF_HOLDING||BAG_OF_TRICKS both sides) and the FOOD arms were already live — the residual was genuinely three predicates.
**Change:** `js/muse.js` only, no new modules, no new module edges (all three names ride pre-existing edges, used only at runtime inside the function body — no top-level TDZ read): floor gate gains `&& onscary(obj.ox, obj.oy, mon)` + `return false` in C conjunct order; horns gain `&& can_blow(mon)` after the `spe > 0` check in C order; unicorn uses `!is_unicorn(ptr)` (C `mondata.h:149` exact). Docstring retires the three deferrals (now cites `:2706-2792`, C order/conjuncts, file-local helpers, no named omissions). No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn searches_for_item` → `PASS syntax 1 changed js file(s): js/muse.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify searches_for_item: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches an onscary-floor-loot / horn-blow / unicorn-horn loot arm (same disclosure as D-2363).
**Named:** none new. `Is_container && !cursed-mbag && !olocked` stands per D-0861 (macro re-verified, untouched); `WAN_POLYMORPH` `difficulty < 6` idiom stands (`ptr.difficulty` ≡ `mons[monsndx].difficulty`, untouched); file-local `cures_stoning`/`mcould_eat_tin` stay file-local (C home is `muse.c`, untouched).
**Next:** do not re-pop `searches_for_item` for the floor/horn/unicorn/FOOD arms. Falsifier: a rescore or fresh `verify searches_for_item` showing a session blocked with `searches_for_item` as owner (not mere `m_search_items` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2363 `mhitm.c` sleep_monst trap-path clone defended/shieldeff

**C locus:** `nethack-c/upstream/src/mhitm.c` `sleep_monst` `:1223–1246` (`:1226–1229` how>=0 mimic `seemimic`; `:1231–1234` `resists_sleep || defended(AD_SLEE) || (how>=0 && resist(how,0,NOTELL))` → `shieldeff(mx,my)` + return 0; `:1235–1244` `mcanmove` → `finish_meating` + `mfrozen=min(amt+mfrozen,127)` else `msleeping=1`). Callers `trap.c:1584/:3135` (how=-1), `music.c:93` (TOOL_CLASS, music path live D-2357).
**JS:** 1 js file (+11/−9 per `git diff --stat`: `js/trap.js` only), under the 600/10 caps. Matches the C residual (two conjuncts + shield on a 23-line C function).
**Change:** `js/trap.js` only (+11/−9, no new modules, no new module edges — `defended` already imported from `./mondata.js:138`, `shieldeff` from `./display.js`, `AD_SLEE=4` file-local at `:519`, all already used: `defended` at `:598`, awaited `shieldeff` at `:5017`): `sleep_monst` is now `async` with the C conjunct order `resists_sleep(mon) || defended(mon, AD_SLEE)` → `await shieldeff(mon.mx, mon.my)` + return 0; both how=-1 call sites (`steedintrap` SLP_GAS arm, `trapeffect_slp_gas_trap`) `await` it. `defended()` is RNG-free and `shieldeff` display-only (same argument as D-2357 music path), so no RNG draw moves. No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn sleep_monst` → `PASS syntax 1 changed js file(s): js/trap.js` · `PASS rule2` · `note hidden verify sleep_monst: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a defended-vs-sleep-gas arm (sleep-gas needs a dragon-armored/artifact-defended monster standing on a gas trap; same disclosure as D-2362).
**Named:** how>=0 `seemimic` reveal + `resist(how,0,NOTELL)` (trap clone stays how=-1-only; music path live D-2357; turns.md:563 R-1082 mimic-reveal debt stands for the shared arm); full `finish_meating` mimic-AP reset (inline `meating=0` only, pre-existing, untouched).
**Next:** do not re-pop `sleep_monst` for the trap defended/shieldeff arms. Falsifier: a rescore or fresh `verify sleep_monst` showing a session blocked with `sleep_monst` as owner (not mere `trapeffect_slp_gas_trap` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2362 `pray.c` pleased pat_on_head gift cases 1-4/6/7-8 + give_spell + force_learn_spell

**C locus:** `nethack-c/upstream/src/pray.c` `pleased` `:1071–1381` (gift switch `:1167–1354`: case 1 `:1170–1217` uwep repair; case 3 `:1218–1245` tune hints + FALLTHROUGH; case 2 `:1246–1282` golden heal; case 4 `:1283–1309` invent uncurse; case 5 `:1310–1338` intrinsic D-2219; cases 7/8 `:1340–1347` crowning + FALLTHROUGH; case 6 `:1348–1350` spell) + `give_spell` `:999–1068` (`mkobj(SPBOOK_no_NOVEL)` + `ulevel+1` unknown/unrestricted re-rolls via `rnd_class(bases[SPBOOK],SPE_BLANK_PAPER)`; 25% `force_learn_spell` unless Fresh else observe/makeknown/bless/`at_your_feet`/`place_object`/`newsym`) + `spell.c` `force_learn_spell` `:2391–2413` (sole caller `pray.c:1035`; blank/BotD/Fresh → `\0`; first NO_SPELL/same-otyp slot; `sp_id`/`sp_lev` + `incrnknow(i,0)` → `spellet`). Callers `pray.c:2333/:2340`.
**JS:** 4 js files (+243/−34 per `git diff --stat`: `js/pray.js` + `js/spell.js` + export words in `js/zap.js`/`js/weapon.js`), under the 600/10 caps. Matches the C residual (six arms + two helpers).
**Change:** `js/pray.js` (+~200): new async `give_spell` in C order/conjuncts next to `pleased` (C home; `SPBOOK_no_NOVEL = 0-SPBOOK_CLASS` per objclass.h:152; `spe_Unknown`/`spe_Fresh`/`spe_Forgotten` match the C spell.h enum arm-for-arm so `<= spe_Unknown` is exact; `OBJ_NAME` ≡ `objectNameStrs[otyp]` per the spell.js:711 idiom; `Your` local clone; `obfree(otmp,null)` per the apply.js idiom); gift switch arms in C source order — case 1 (`otense` erosion note, cursed-uncurse vs unblessed-bless glow with `amber`/`light blue` prefs + `iflags.last_msg = PLNMSG_OBJ_GLOWS`, `bknown = 1`, erosion clear gated on surviving `repair_buf`, `update_inventory`), case 3 (`uopened_dbridge`/`gehennom_entered` skip, `uheard_tune` 0/1 arms with `godvoice(g_align,null)` + mortal/creature via `is_human(youmonst.data)` + `se_divine_music`/`You_hear`/`game.tune` + `record_achievement(ACH_TUNE)`, FALLTHROUGH), case 2 (`You` golden glow, `ulevelmax-1` + `pluslvl(false)` vs `uhpmax+5`/`uhppeak`/`mhmax`, `uacurr/aamax` STR restore + `flags.botl` + `encumber_msg`, `uhunger<900` + `uluck<0` + `ucreamed` + `make_blinded(0,true)` + `botl`), case 4 (HOA-excepted invent loop, `any`-gated `update_inventory`, uncurse outside the Blind gate), cases 7/8 (`record >= PIOUS(20, pray.c:64)` + `!uhand_of_elbereth` → `await gcrownu()` + `break`, else FALLTHROUGH per review-1185 nit), case 6 → `await give_spell()`; `PIOUS`/`SPE_BLANK_PAPER`/`MAGIC_MARKER` consts via the file idiom. `js/spell.js` (+28): exported async `force_learn_spell` next to `spellet` (C home; file-local `SPE_BLANK_PAPER`/`SPE_BOOK_OF_THE_DEAD`/`KEEN`/`init_spl_book`/`incrnknow`/`spellet`, imported `impossible`). Export-only touches: `You` in `js/zap.js`, `P_RESTRICTED` in `js/weapon.js` (one word each; all four new import names ride pre-existing edges per `imports.mjs --can` ALREADY; `insight.js`/`shk.js`/`generated/objects_data.js` joins are runtime-call/leaf only).
**Verify:** `node scripts/verify.mjs --fn pleased` → `PASS syntax 4 changed js file(s)` · `PASS rule2` · `note hidden verify pleased: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed; `brief give_spell`/`force_learn_spell` likewise list 0 blocked) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a pat_on_head gift arm (prayer-gift RNG path).
**Named:** `moves>100000` `ublesscnt` incr (C tail; pre-existing JS gap, untouched — own row when a falsifier fires); on-altar wrong-god early-return polish (untouched); SetVoice pitch on gift verbalize/tune arms (file convention, untouched); case-5 `INTRINSIC` flat-field idiom stands per D-2219.
**Next:** do not re-pop `pleased`/`give_spell`/`force_learn_spell` for these arms. Falsifier: a rescore or fresh `verify pleased`/`verify give_spell` showing a session blocked with any of the three as owner (not mere `dosacrifice`/`dopray` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2361 `eat.c` doeat Strangled/uedibility/retouch-blast/rustproof arms + edibility_prompts

**C locus:** `nethack-c/upstream/src/eat.c` `doeat` `:2817–3084` (Strangled head; `floorfood`; `check_capacity`; `u.uedibility` + `edibility_prompts` + `Your nose stops tingling`; `hands_obj`; `is_edible`; worn `W_ARMOR|W_TOOL|W_AMUL|W_SADDLE`; `carried ? retouch_object(&otmp,FALSE) : touch_artifact` → `ECMD_TIME`; rustproof `rknown`/split/`Ulch`/`oerodeproof=0`/`make_stunned(rn2(10))`/welded-or-cursed-ring spit-out vs floor `dropy`+`stackobj` → `ECMD_TIME`; `RIN_SLOW_DIGESTION`; `doeat_nonfood`) + `edibility_prompts` `:2626–2731` (staticfn; worst-case `rotted` without `rn2(20)`; tainted → stone/slime → tainted-resistant → rotten → poisonous → sleep-apple → monk-meat → acidic → rustproof → vegan → vegetarian; `yn_function(buf,ynchars,'n',TRUE)` 1/2/0). Callers `cmd.c:40`, `context.h:65`.
**JS:** 1 js file (+144/−8 per `git diff --stat`: `js/eat.js` only), under the 600/10 caps. Matches the C residual (four arms + 106-line helper).
**Change:** `js/eat.js` only, no new modules: new async `edibility_prompts` in C order/conjuncts next to `doeat` (C home; `Tobjnam(otmp,'smell')` + quan it/they; `ismnum`/`flesh_petrifies`/`Stone_resistance`-triple/`poly_when_stoned(youmonst.data ?? hero_form_data())`; green-slime `Unchanging`+`slimeproof`; `nonrotting_corpse`/`peek_at_iced_corpse_age` locals; `moves`-based worst-case `Math.trunc(/10)` + cursed/blessed ±2; all ten prompt arms incl. `Role_if(PM_MONK)` as `urole.mnum === PM_MONK` and rustproof `Upolyd && umonnum === PM_RUST_MONSTER && is_metallic && oerodeproof`; `MAT_LEATHER/BONE/DRAGON_HIDE/WAX` file-local consts; `y_n` ≡ C `yn_function(...,ynchars,'n',TRUE)`); `doeat` gains the four arms in C order — `Strangled()` (local, first, before `floorfood_eat`), `uedibility` (`Your nose stops tingling...` via `body_part(NOSE)` + `uedibility = 0`, `res === 1` → `ECMD_OK`), carried→`retouch_object(otmp0,false)` else `touch_artifact(otmp0, game.youmonst ?? null)` → `ECMD_TIME`, rustproof (`rknown`, floor `splitobj(quan-1)` / carried `splitobj(1)` child idiom from `touchfood`, `Ulch` pline, `oerodeproof = 0`, `make_stunned((HStun&TIMEOUT)+rn2(10))`, welded-or-`(cursed&W_RING)` → `set_bknown` + spit-out else floor `surface` spit + `remove_worn_item`/`freeinv`/`dropy` + `stackobj` → `ECMD_TIME`); `const otmp0` → `let` for the split reassign. Imports: `W_RING`+`NOSE` join the existing `const.js` edge, `Tobjnam` the `objnam.js` edge, `y_n` the `getline.js` edge, `set_bknown` the `mkobj.js` edge, `welded` the `wield.js` edge, `freeinv` the `invent.js` edge, `surface` the existing `sit.js` edge, `retouch_object`/`touch_artifact` from `artifact.js` + `remove_worn_item` from `steal.js` (all three hoisted functions, cycle-safe per `imports.mjs --can`; used only at runtime, no top-level reads).
**Verify:** `node scripts/verify.mjs --fn doeat` → `PASS syntax 1 changed js file(s): js/eat.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify doeat: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions + corpus are the suite, and no public/corpus session reaches a Strangled/uedibility/blast/rustproof eat arm.
**Named:** slow-digestion `dknown → trycall` (C `:2906` arm; pre-existing JS gap, untouched — own row when a falsifier fires); worn-arm message (`You can't eat something you're wearing.` vs C `You_cant("eat %s you're wearing.", something)` — pre-existing wording, untouched); `COIN_CLASS` duplicate `is_edible` pre-check (pre-existing, behavior-identical, untouched); livelog first-time conduct lines (house convention, untouched); `retouch_object` silver-hate/bane damage + worn-drop paths stay deferred per its `js/artifact.js` docstring (the new blast gate inherits that thinness).
**Next:** do not re-pop `doeat` for the Strangled/uedibility/blast/rustproof arms. Falsifier: a rescore or fresh `verify doeat` showing a session blocked with `doeat` as owner (not mere `floorfood`/`eatfood` caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2360 `zap.c` bhitpile STRIKING/force-bolt STATUE_TRAP pre-activate + hidingunder (break_statue D-0997 envelope)

**C locus:** `nethack-c/upstream/src/zap.c` `bhitpile` `:2428–2500` (early return; hidingunder/first init; STRIKING/FORCE_BOLT STATUE_TRAP pre-activate + `learnwand` + head-changed `first = FALSE`; `hidingunder` in-walk skips with `zz > 0` first-item / `zz < 0` rest polarity; `poly_zapped`/`create_polymon`, boulder restack/`recreate_pile`, `maybe_unhide_at`, `fill_pit` tails). Callers `bhit` `:4046` (`zz = 0`), `zap_steed` `:3244`, `zap_updown` `:3384` (`dz > 0`; the `dz < 0` hideunder-top-item path stays inline, matching JS), `apply.c:4100/:4118` (`zz = 0`).
**JS:** 1 js file (+48/−5 per `git diff --stat`), under the 600/10 caps.
**Change:** `js/zap.js` only (+48/−5, no new modules): ported the `bhitpile` head in C order/conjuncts — `hidingunder` from `(zz|0) !== 0 && uundetected && hides_under(youmonst.data)`, `first = true`; STRIKING/FORCE_BOLT arm captures `topofpile`, awaits `activate_statue_trap(t, tx, ty, true)` then `learnwand(wand)` on success, sets `first = false` when the pile head changed; in-walk hidingunder skips with the C `zz` polarity (manual `next_obj` advance before each `continue`, matching the C `for` increment); `if (hidingunder) await maybe_unhide_at(tx, ty)` tail. Imports: `STATUE_TRAP` joins the existing `const.js` edge, `activate_statue_trap` the existing `trap.js` edge, `maybe_unhide_at` the existing `monmove.js` edge (`imports.mjs --can` ALREADY on both — no new module edges; `learnwand` file-local, `hides_under`/`objects_at`/`t_at` already imported). No DIAG/FORCE/seed gates (Rule #2 clean).
**Verify:** `node scripts/verify.mjs --fn bhitpile` → `PASS syntax 1 changed js file(s): js/zap.js` · `PASS rule2` · `note hidden verify bhitpile: no corpus session is blocked on it at HEAD — vacuous, NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. `/tmp/probe_bhitpile.mjs` (deleted after run, real modules, `initRng(1234)` seeding only): Deaf + unseen striking-`bhitpile` on a trap + single animatable statue → trap gone pre-walk / `wand.dknown` TRUE / exactly one monster / pile empty, 4/4; the same probe on the stashed (unpatched) tree FAILS exactly the learnwand check (other 3 pass via the mid-walk activate path), proving the probe discriminates and the pre-amble is the cause.
**Named:** `bhitpile` tails `create_polymon` after `poly_zapped` / `recreate_pile` restack / `fill_pit` (pre-existing docstring deferral, own rows); `bhito` STRIKING/STATUE Hallucination `rndmonnam` shatter variant (C `:2289–2292`, Hallu-only message+RNG arm, no corpus owner — own `bhito` row when a falsifier fires); `fracture_rock` shop-bill/`sokoban_guilt` (pre-existing `js/dig.js` doc deferral, untouched).
**Next:** do not re-pop `break_statue`/`bhitpile` for the debt.md D-0997 line. Falsifier: a rescore or fresh `verify break_statue`/`verify bhitpile` showing a session blocked with either as owner (not mere caller presence — `bhitpile` has 5 C call sites incl. lateral `bhit`); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2359 `zap.c` melt_ice deferred arms: trap_ice_effects + Underwater vision + boulder settle + spoteffects

**C locus:** `nethack-c/upstream/src/zap.c` `melt_ice` `:5040–5079` (`spot_stop_timers`; `t_at` → `trap_ice_effects(x,y,TRUE)`; `obj_ice_effects(x,y,FALSE)`; `unearth_objs`; `Underwater` → `vision_recalc(1)`; `newsym`; `cansee||u_at` → `Norep(msg)`; `sobj_at(BOULDER)` settle loop with `obj_extract_self` + `boulder_hits_pool(otmp,x,y,FALSE)` + `impossible("melt_ice: no pool?")` while `is_pool && sobj_at`; `u_at` → `spoteffects(TRUE)` else pool mon → `minliquid`); `nethack-c/upstream/src/trap.c` `trap_ice_effects` `:7175–7194` (trapped-mon release; LANDMINE/BEAR_TRAP → `cnv_trap_obj(LAND_MINE/BEARTRAP,1,ttmp,TRUE)` else `deltrap` unless `undestroyable_trap`). Callers `eat.c:3781`, `trap.c:1812/:4313`, `zap.c:5130/:5174`.
**JS:** 2 js files (+53/−23 per `git diff --stat js/`: `js/trap.js` +20, `js/zap.js` +33/−23), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean).
**Change:** `js/trap.js` — new exported async `trap_ice_effects(x, y, ice_is_melting)` in C order/conjuncts next to `undestroyable_trap` (C home; all callees `t_at`/`m_at`/`cnv_trap_obj`/`deltrap`/`undestroyable_trap`/LAND_MINE/BEARTRAP already in-file; async because `cnv_trap_obj` awaits bury/sellobj). `js/zap.js` `melt_ice` — `t_at` → `await trap_ice_effects(x, y, true)`; `Underwater` → `vision_recalc(1)`; boulder settle loop in C order (`cansee` pline before the loop, `obj_extract_self`, `await boulder_hits_pool(otmp,x,y,false)` + `await impossible`, re-check `is_pool` before `sobj_at` per the C `&&` short-circuit, trailing `newsym`); `u_at` → `await spoteffects(true)`; `minliquid` joins the existing static `mon.js` import (dynamic import deleted). Imports: `trap_ice_effects` joins the existing `trap.js` edge, `sobj_at` the `mkobj.js` edge, `vision_recalc` the `vision.js` edge, `boulder_hits_pool` the `do.js` edge, `minliquid` the `mon.js` edge (`imports.mjs --can` ALREADY on all five — no new edges except `spoteffects` from `./pickup.js`, hoisted function, cycle-safe).
**Verify:** `node scripts/verify.mjs --fn melt_ice` → `PASS syntax 2 changed js file(s): js/trap.js js/zap.js` · `PASS rule2` · `note hidden verify melt_ice: no corpus session is blocked on it at HEAD — vacuous, NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. `/tmp/probe_meltice.mjs` (deleted after run): WEB trap removed on melt / kept when not melting / LANDMINE→object + trap gone / ICED_POOL ice→POOL, 4/4 (probe-only `initRng(1234)` + `objects_globals_init()`; production draws come from the game seed).
**Named:** local `useupf` clone shop-bill (pre-existing `js/zap.js:845` named deferral, untouched — switching to the `invent.js` export would change shop billing, out of row); `game.u?.Underwater` macro shape (pre-existing, untouched); BEAR_TRAP melt arm (same code path as LANDMINE, probe-covered via LANDMINE only).
**Next:** do not re-pop `melt_ice`/`melt_ice_away`/`start_melt_ice_timeout`/`burn_floor_objects`/`trap_ice_effects` for the debt.md D-0965 line. Falsifier: a rescore or fresh `verify melt_ice`/`verify trap_ice_effects` showing a session blocked with either as owner (not mere caller presence — `melt_ice` has 6 C call sites incl. fire traps); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2358 `mthrowu.c` m_throw MT_FLIGHTCHECK: IRONBARS via hits_bars + IS_SINK + sink/misses plines

**C locus:** `nethack-c/upstream/src/mthrowu.c` `MT_FLIGHTCHECK` `:552–569` (edge / IS_OBSTRUCTED / closed_door / IRONBARS via `hits_bars(&singleobj, bhitpos, next, pre?0:forcehit, 0)` / `!(pre) && IS_SINK(current)`), pre-flight `:639–642` (`MT_FLIGHTCHECK(TRUE, 0)`), loop `:798–823` (`forcehit = !rn2(5)` before the `!range ||` short-circuit; `if (singleobj)` guard since hits_bars may destroy; sink `The(mshot_xname) otense(plop/drop) onto the sink` `:804–807` else multishot `The(mshot_xname) misses` `:808–813` gated on `m_shot.n > 1 && (!mesg_given || off-hero) && (cansee || marcher canseemon)`).
**JS:** 1 js file + 1 map line (+68/−20 total per `git diff --stat`, `js/mthrowu.js` only), under the 600/10 caps. Matches the C residual (18-line macro + pre-flight + 24-line tail). No DIAG/FORCE/seed gates (Rule #2 clean).
**Change:** `js/mthrowu.js` only, no new modules, no new module edges (`IRONBARS`/`IS_SINK` join the existing `const.js` import; `otense` joins the existing `objnam.js` import; `The`/`mshot_xname`/`canseemon`/`game.marcher`/`game._mesg_given` already in-file): pre-flight evaluates the C macro with `always_hit 0, whodidit 0` (sink arm correctly never fires pre-flight); in-loop preserves C order — `rn2(5)` first, then `!range` short-circuit (no bars/sink evaluation on the final cell), else wall/door/sink-current plus conditional `hits_bars(box, bx, by, nx, ny, forcehit, 0)` with the box back-write to `singleobj`/`game._thrownobj`; blocked tail keeps the C `if (singleobj)` guard (destroyed → drop nothing, break to the shared tail which clears `_thrownobj`) with the sink-vs-misses plines in C predicate order. Pre-flight destroy path clears `_thrownobj` instead of calling `drop_throw(null)` (C has no guard there; the guard is a js-throw avoidance, no RNG/screen delta — breaks() destroys nothing that hits bars at always_hit 0 in practice).
**Verify:** `node scripts/verify.mjs --fn hits_bars` → `PASS syntax 1 changed js file(s): js/mthrowu.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify hits_bars: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. `/tmp/probe_hitsbars.mjs` (deleted after run): import OK + check-only `hits_bars` true/false, no throw.
**Named:** thrwmu always_toss / polearm (pre-existing header names, untouched); `thitu` quan>1 doname, dmgval defender-null, can_blnd Blindfolded/ublindf/ucreamed/visor you-gates (pre-existing D-2215 names, untouched); pre-flight `drop_throw(null)` guard delta vs C (js-throw safety, above).
**Next:** do not re-pop `hits_bars`/`m_throw` for the debt.md D-0990 line. Falsifier: a rescore or fresh `verify hits_bars` showing a session blocked with hits_bars/m_throw as owner (not mere m_throw stepFns presence — it runs on every monster throw); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — Audit 2ac37870..d8cfa25c (reviews 1321–1323: 3 ACCEPT) + cadence 44/44

**Range:** three JS-touching SHAs since review 1320 (`2ac37870`); park commits skipped (docs-only, verified no `js/` in stat). Zero new Must-fix rows, so Next cluster stays on the Open head (`hits_bars`).
**1321** `aba73268` D-2355: lamp `check_unpaid` before on-pline + candle fresh-unpaid `verbalize`/`bill_dummy_object`; C `:1683-1685`/`:1690-1698` order verified, `SetVoice` omit is VOICEONLY-compiled-out — ACCEPT.
**1322** `c7041cb5` D-2356: real async `gulp_blnd_check` (conjuncts ≡ C `:1272-1285`, `Blinded` macro `youprop.h:92` verified) + Blindf_off impossible/takeoff/owornmask/gulp-gate; stub deleted, both new edges ALREADY, 2 of 3 C callers live (use_towel named follow-up) — ACCEPT.
**1323** `d8cfa25c` D-2357: `sleep_monst_music` middle `defended(mon, AD_SLEE)` + `shieldeff` in C `:1231-1234` order; AD_SLEE=4 ≡ `monattk.h:46`, defended RNG-free (0 hits), sole caller awaited — ACCEPT.
**Verify:** each SHA re-measured `hidden-proxy verify <fn> --base HASH~1` (all 0-blocked vacuous, matching D-logs); `imports.mjs --rulecheck` clean; added-line banned grep 0 ×3.
**Cadence:** `frozen/ps_test_runner.mjs sessions` → 44/44, Scr 11,405/11,405, RNG 792,838/792,838, `47+0.29/turn` (R² 0.78); CURRENT Score rewritten.
**Refill:** queue 7 → 12 Open from c-js-map named omits (turns.md:2000 sleep_monst trap clone; turns.md:2391 adj_pit_checks ladder arm; turns.md:2768 searches_for_item + mon_would_consume_item; turns.md:563 resist tell-shield), each deduped vs live/archive/parked. Corpus still 88.5% < 90% so these are map-grade by construction — pop Must-fix first, then Open in order; do not re-pop parked owners for these lines.
**Next:** pop Open in order from `hits_bars`. D-2356-crumb use_towel-gulp re-queue superseded by the use_towel park (TRUE arm unreachable) — do not re-queue. No seed/step/coordinate gates.
## 2026-09-15 — D-2357 `music.c` flute sleep-resist: `sleep_monst_music` defended(AD_SLEE) + shieldeff

**C locus:** `nethack-c/upstream/src/mhitm.c` `sleep_monst` `:1225–1250` (`:1231–1234` `resists_sleep || defended(AD_SLEE) || (how>=0 && resist(how,0,NOTELL))` → `shieldeff(mx,my)` + return 0); `monst.h:270/274` (`mon_resistancebits`, `resists_sleep ≡ Resists_Elem(SLEEP_RES)`), `mondata.c:91–124` (`defended`: wielded artifact + adult-dragon self-suit + worn W_ARM dragon armor), `artifact.c:636–668` (`defends`: only orange scales/mail map AD_SLEE; no artifact carries AD_SLEE — `artilist.h` has zero), `objects.h:517/544` (only SLEEP_RES oc_oprop items are orange scale mail/scales), `prop.h:17` (SLEEP_RES=3 → rsstmask 1<<2 = MR_SLEEP), `monattk.h:46` (AD_SLEE=4). Caller `music.c:596` `put_monsters_to_sleep(u.ulevel*5)` via `do_improvisation` MAGIC_FLUTE `:589–598`.
**JS:** 1 js file + 1 map line (+15/−7 total per `git diff --stat`), under the 600/10 caps. Small insertion matches the small C residual (2 arms of a 25-line function). No DIAG/FORCE/seed gates (Rule #2 clean); `node -e "await import('./js/music.js')"` loads OK (dynamic-import path, green sessions may not load it).
**Change:** `js/music.js` only, no new modules, no new module edges (`defended` joins the existing `mondata.js` import and `shieldeff` joins the existing `display.js` import — `imports.mjs --can music.js mondata.js defended` → ALREADY; AD_SLEE=4 file-local const in the trap.js sibling idiom): resist-`if` gains the C-middle `defended(mon, AD_SLEE)` disjunct in C order, the arm awaits `shieldeff(mon.mx, mon.my)`, function goes async and `put_monsters_to_sleep` awaits it (`d(10,10)` still evaluated pre-call, `mdistu` short-circuit unchanged). defended() is RNG-free and shieldeff display-only, so zero RNG draws move; inline MR_SLEEP bits kept (exact: `resists_sleep_slee` identical body; avoids a new mhitm.js edge for zero behavior change). Doc comment cites the C order + why bits suffice; header omit now names the music path live (trap.js `sleep_monst` keeps its own omit).
**Verify:** `node scripts/verify.mjs --fn sleep_monst` → `PASS syntax 1 changed js file(s): js/music.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify sleep_monst: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. `/tmp/probe-sleep-defended.mjs` (deleted after run): 3 ok / 0 FAIL on the real modules — adult orange dragon defended vs AD_SLEE (self-suit), giant ant not, orange dragon not vs AD_FIRE.
**Named:** trap.js `sleep_monst` defended/shieldeff/how>=0 arms (own trap.c row, header-named); shared local music `resist` clone mplayer dlev-floor arm (`mondata.c` `dlev<1 → is_mplayer?ulevel:1`; clone used by awaken_soldiers-row paths too — not widened); `finish_meating` vs inline `meating=0` (live `dogmove.js` export is exactly `meating=0` + deferred mimic reset — identical); music `resist` stays sync (canonical zap.js export defers the same shieldeff and adds damage machinery — no behavior delta at damage=0/tell=0).
**Next:** do not re-pop charm_snakes/calm_nymphs/charm_monsters or the flute/harp arms for the debt.md D-0974 line. Falsifier: a rescore or fresh `verify sleep_monst`/`verify charm_monsters` showing a session blocked with either as owner (not mere do_play_instrument caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — D-2356 `do_wear.c` Blindf_off residual arms + `mhitu.c` gulp_blnd_check live

**C locus:** `nethack-c/upstream/src/do_wear.c` `Blindf_off` `:1495–1534` (`:1503–1506` impossible, `:1507` takeoff.mask clear, `:1508` setworn(NULL, owornmask), `:1526` gulp gate) + `mhitu.c` `gulp_blnd_check` `:1273–1285` (`!Blinded` with `youprop.h:92 Blinded (HBlinded && !BBlinded)`, uswallow + `attacktype_fordmg(ustuck->data, AT_ENGL, AD_BLND)` + `can_blnd(ustuck, youmonst, aatyp, NULL)`, `++uswldtim`, gulpmu) + sibling callers `apply.c:178` (use_towel) and `do.c:2376` (wipeoff). Caller audit from pinned C: polyself `:1291` guards non-null, do_takeoff `:2889` / accessory-off `:1821` / towel `:154` pass worn non-null — the impossible arm is defensive-only.
**JS:** 3 js files + 1 map line (+44/−16 total per `git diff --stat`), under the 600/10 caps. Small insertion matches the small C residual (Blindf_off 4 arms + 12-line helper). No DIAG/FORCE/seed gates (Rule #2 clean); `impossible` was already imported in do_wear.
**Change:** `js/mhitu.js` — real exported async `gulp_blnd_check` in C order/conjuncts next to gulpmu (C home; `attacktype_fordmg`/`can_blnd` join the existing uhitm.js edge; AT_ENGL already imported, AD_BLND file-local const; `export async function` is hoisted and do_wear reads it lazily inside Blindf_off only, so the do_wear→mhitu edge is TDZ-safe in the shared 90-module SCC — the `imports.mjs --can` CHECK caveat satisfied, no top-level reads added). `js/do.js` — stub deleted, real one joins the existing mhitu.js import; wipeoff awaits it; dowipe doc drops the stale omit. `js/do_wear.js` — `gulp_blnd_check` on a new mhitu.js import (same reasoning); Blindf_off awaits impossible, clears takeoff.mask in the Armor_gone idiom, passes otmp.owornmask, gates see-again on the gulp check.
**Verify:** `node scripts/verify.mjs --fn Blindf_off` → `PASS syntax 3 changed js file(s): js/do.js js/do_wear.js js/mhitu.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify Blindf_off: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `PASS full 44/44 passing (auto: shared file changed)` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. `/tmp/probe-blindf-gulp.mjs` (deleted after run): 8 ok / 0 FAIL — gulp all four false-conjuncts (not-swallowed, HBlinded-blocked, no-AD_BLND, null-ustuck); true-arm stack proves the full gate passed into gulpmu (unseeded headless RNG stops inside gulpmu's d(), past the ++uswldtim); Blindf_off impossible arm reached (urgent_pline requested --More-- — no other pline is reachable on that null-eyewear path); takeoff W_TOOL cleared; see-again + still-blind no-throw.
**Named:** use_towel `apply.c:178` gulp arm (still inline always-false + map-named at apply.js:1886; own follow-up row) — wipeoff + Blindf_off now live; steal.js W_TOOL arm raw `setworn(null, W_TOOL)` without takeoff.mask clear or Blindf_off call (pre-existing steal.c:269 named omit; a stale mask there is the only known way the new impossible arm could fire where C would not); `setworn(null, otmp.owornmask)` ≡ W_TOOL for eyewear (fidelity-only change).
**Next:** (1) queue the use_towel gulp-blnd arm as its own Open row when refilling (apply.c:178; apply.js:1794/1886 still defer it). (2) steal.c take-off-mask sync on the W_TOOL arm. Do not re-pop Blindf_on/off for the debt.md D-1013 line. Falsifier: a rescore or fresh `verify Blindf_off`/`verify gulp_blnd_check` showing a session blocked with either as owner (not mere caller presence). No seed/step/coordinate gates.
## 2026-09-15 — D-2355 `apply.c` use_lamp shop billing: lamp check_unpaid + fresh-candle verbalize/bill_dummy

**C locus:** `nethack-c/upstream/src/apply.c` `use_lamp` `:1628–1700` (lamp arm `:1683–1685` `check_unpaid(obj)` before `pline("%s%s is now on.", Shk_Your(buf, obj), lamp)`; candle arm `:1686–1698` flame pline then `unpaid && costly_spot(u.ux, u.uy) && age == 20*objects[otyp].oc_cost` → `ithem = quan>1 ? "them" : "it"`, `shop_keeper(*in_rooms(...))`, `SetVoice(shkp, 0, 80, 0)`, `verbalize("You burn %s, you bought %s!", ithem, ithem)`, `bill_dummy_object(obj)`); caller `doapply` `:4347` + `use_candle` fallback (D-1025).
**JS:** 1 js file (+21/−6). Under the 600-insertion / 10-file caps. Small insertion matches the small C residual (lamp 3 lines + candle 9 lines).
**Change:** `js/apply.js` only, no new modules, no new module edges (`objects` joins the existing `objects.js` import — `imports.mjs --can` ALREADY; `check_unpaid`, `costly_spot`, `bill_dummy_object`, `verbalize`, `in_rooms` already imported): lamp arm awaits `check_unpaid(obj)` before the on-pline in C order; candle arm after the flame pline checks `unpaid && costly_spot(ux, uy) && age === 20*oc_cost` (shk.js `objects()?.[otyp]?.oc_cost` idiom) then awaits `verbalize("You burn them/it, you bought them/it!")` + `bill_dummy_object(obj)`; C `SetVoice` omitted (audio voice no-op; `use_candle` attach arm omits it the same way). Doc header now cites `:1628–1700`/`:1683`/`:1690–1698`.
**Verify:** `node scripts/verify.mjs --fn use_lamp` → `PASS syntax 1 changed js file(s): js/apply.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `note hidden verify use_lamp: no corpus session is blocked on it at HEAD — a vacuous verify is NOT a corpus PASS` (row cited 0 blocks so no `--base` owed) · `PASS green 2/2` · `PASS strict` both gate sessions · `PASS cohort 7/7` · `VERIFY: PASS`. Pre-change `node scripts/verify.mjs --no-cohort` on the clean tree → VERIFY: PASS. No committed unit test: no `tests/` dir and no test script in package.json — sessions are the suite; neither shop arm is reached by any public or corpus session (unpaid lamp/candle lighting in a shop).
**Named:** candle `SetVoice(shop_keeper(*in_rooms(...)), 0, 80, 0)` (audio voice no-op, use_candle precedent); `Shk_Your_apply` thin shop/mon prefix (pre-existing `js/apply.js:3887-3894` named); `shk_your`/`Yname2_oil`/`s_suffix_apply` local clones (pre-existing import debt, do not add another).
**Next:** do not re-pop `use_lamp` for the debt.md D-1023/D-1052 line. Falsifier: a rescore or fresh `verify use_lamp` showing a session blocked with use_lamp as owner (not mere doapply/caller presence); re-queue under the writer it names. No seed/step/coordinate gates.
## 2026-09-15 — Audit 736bd185..2ac37870 (reviews 1317–1320: 4 ACCEPT) + cadence 44/44

**Range:** four JS-touching SHAs since review 1316 (`b214fb72`); park/audit commits skipped (docs-only, verified no `js/` in stat). All four Must-fix/Open continuations, zero new Must-fix rows.
**1317** `736bd185` D-2351 (closes 1314 Must-fix): `!==` → `===` ≡ C `!strcmp(fakenm, "vampire bat")` — ACCEPT.
**1318** `9cc2b25f` D-2352 (closes 1316 Must-fix): PICK_ONE arm drops FEEL_COCKATRICE abort + SORTLOOT_PETRIFY; caller audit proves PICK_ANY path byte-identical — ACCEPT.
**1319** `589d9acb` D-2353: 3 prompt clones + 6 letter helpers retire to live `getobj` (`invent.js:8047`); all three C word/ok/flags verified (`do.c:35-36`, `apply.c:4226/:1793`); `sym.mjs` pasted (9 deletes NOT FOUND, edges ALREADY) — ACCEPT.
**1320** `2ac37870` D-2354: bad-bag `impossible`, two `update_inventory` tails, tipcontainer undiscovered-BoT arm; C order + gettarget exclusion verified arm-for-arm — ACCEPT.
**Verify:** each SHA re-measured `hidden-proxy verify <fn> --base HASH~1` (all 0-blocked vacuous, matching D-logs); `imports.mjs --rulecheck` clean ×4; added-line banned grep 0 ×4.
**Cadence:** `frozen/ps_test_runner.mjs sessions` → 44/44, Scr 11,405/11,405, RNG 792,838/792,838, `48+0.29/turn` (R² 0.78); CURRENT Score rewritten.
**Fix:** Next cluster pointed at `use_trap`, parked by `74797791` in the same commit that named it — repointed to first Open row (`paranoid_query`). Queue at 8 Open (floor, no refill); no `- [x]` leftovers.
**Next:** pop Open in order from `paranoid_query`. No seed/step/coordinate gates.
