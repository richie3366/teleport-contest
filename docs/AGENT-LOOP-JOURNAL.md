# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2083 potion.c peffect_sickness dropped poisontell and the Fixed_abil gate, so non-healer quaffs lost the attribute-loss message and its --More-- overflow (queue owner peffect_sickness)

**C locus:** `potion.c:964–1011` `peffect_sickness` — `:987–992` `if (!Fixed_abil) { poisontell(typ, FALSE); (void) adjattrib(typ, Poison_resistance ? -1 : -rn1(4, 3), 1); }`; `attrib.c:280–313` `poisontell` (poiseff[] delivery table — «very sick» is the A_CON row via You_feel; STR19(25)/CON-25 fixups; draw-free, so call order adds no RNG).
**JS:** 1 file (potion.js +14/−11), under the 600/10 caps. Small because the whole surrounding envelope (blessed stale-fruit, contaminant string, losehp arms, CON exercise, hallu-clear) was already live; density exception applies (corpus-driven Open row with a recorded expectation — D-2081/D-2082 precedent).
**Change:** `js/potion.js` — both calls gated on the local `Fixed_abil()` (`potion.js:1786`, C `youprop.h:385` cite) in verbatim C order (poisontell, then adjattrib); `poisontell` joins the pre-existing `./attrib.js` import (`imports.mjs --can`: ALREADY, no new edge, call-time await so no TDZ); header cite corrected to `:964–1011` with poisontell/Fixed_abil retired from omissions.
**Verify:** `node scripts/verify.mjs --fn peffect_sickness` → PASS syntax (1 changed file: js/potion.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-normal-Rogue-92209 PASS; scen-kit-Rogue-92225 47→show_conduct@82, strictly later step and owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last js/ edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2080/D-2082 precedent).
**Named:** none new — this retires two: poisontell wording, Fixed_abil gate. Untouched: full make_hallucinated body (flag-clear only; no blocked session reaches it).
**Next:** do not re-pop `peffect_sickness` (Rogue-92209 PASS; kit-Rogue→show_conduct@82 flows through the normal queue — show_conduct itself stays parked until its falsifier exists).
## 2026-09-08 — D-2082 pickup.c use_container lknown pre-branch was deleted by D-2028, so held-box lock discovery never stuck and the later tip menu lost its locked prefix (queue owner tipcontainer_gettarget)

**C locus:** `pickup.c:2972–3226` `use_container` — `:2992–2996` `if (!obj->lknown) { obj->lknown = 1; if (held) update_inventory(); }` runs *in advance* of the `:2997+` olocked arm (`pline("%s locked.", Tobjnam(obj, "are"))`, held-only put-down line). The Hmmm/«turns out to be locked» variant is the separate floor-only `do_loot_cont` `:2106–2111` copy; `use_container` always uses Tobjnam. D-2028 cited `:2994–2999`, read the arm as «no `lknown` touch», and deleted the flag set (its Named omissions even list «`lknown` pre-branch» as untouched) — the flag deletion is what this entry reverses; the Tobjnam/no-Hmmm message shape D-2028 fixed stays.
**JS:** 1 file (pickup.js +9/−5), under the 600/10 caps. Small because the whole surrounding arm (u_handsy, Tobjnam locked line, menus, tip target flow) was already live; density exception applies (corpus-driven Open row with a recorded expectation — D-2081 precedent). No DIAG/FORCE/seed gates; no frozen files; no new probes left behind.
**Change:** `js/pickup.js` — restored the 5-line C arm verbatim (`if (!obj.lknown) { obj.lknown = 1; if (held) update_inventory(); }`) ahead of the olocked check and corrected the comment to cite `:2992–2999` (Hmmm stays floor-only). `update_inventory` (sync, `js/invent.js:4067`) joins the pre-existing `./invent.js` import — `imports.mjs --can` reports ALREADY, same-SCC runtime-only read, no TDZ risk.
**Verify:** `node scripts/verify.mjs --fn tipcontainer_gettarget` → PASS syntax (1 changed file: js/pickup.js) · PASS rule2 · hidden `0 PASS, 1 moved past, 0 unchanged, 1 worse → REGRESSION`: scen-wish-Knight-92045 moved 79 → enlightenment@86 (screens 91→92/94, RNG 2637/2637 held, strictly later owner) · scen-wish-Healer-92092 «WORSE: now do_statusline2 at step 58 (was 130 at the 84dc0e34 committed baseline)» is pre-existing drift, not this port — the working scoreboard already showed Healer at do_statusline2@58 («Dlvl:1 $:1370 …» vs «… Satiated», the parked lembas pair per NOTES) *before* this iteration's js/ edit, and this arm first runs at Healer steps 121+ so it cannot affect step 58 (no RNG, display-only inventory refresh) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · `--base e55af311` re-run identical (falls back to the same 84dc0e34 scoreboard; working scoreboard now 0 blocked on tipcontainer_gettarget). Preflight `verify --no-cohort` was green before any edit. Final verify ran after the last js/ edit (no D-1831 gap).
**Named:** none new — chest trap, bag-of-tricks/horn, cursed-mbag, shop billing, altarizing (tipcontainer envelope) stay mapped; `thesimpleoname`/`Ysimple_name2` discovery-aware exports (D-2028 message shape) untouched. Map line `turns.md:1900` corrected («no `lknown` touch» → lknown pre-branch live, Hmmm stays floor-only).
**Next:** do not re-pop `tipcontainer_gettarget` (working: 0 blocked). Knight's enlightenment@86 flows through the normal queue. Healer's do_statusline2 lembas@58 stays parked (NOTES pair; needs its own writer, out of this cluster).
## 2026-09-08 — D-2081 detect.c monster_detect flushed a spurious --More-- between the sense pline and getpos's verbose tip, splitting C's single topline (queue owner monster_detect)

**C locus:** `detect.c:842` `You("sense the presence of monsters.")`, then the one-shot else-arm `browse_map(TER_DETECT | TER_MON, "monster of interest")` (`:855`) → `browse_map` (`:106–119`, verbatim getpos wrapper) → `getpos.c:843–846` (`if (flags.verbose) { pline("(For instructions type a '%s')", ...); msg_given = TRUE; }`). C never flushes between the two plines; `topl.c update_topl` NEED_MORE + room appends with a two-space join, so both land on one topline row with no --More--.
**JS:** 1 file (detect.js +5/−3) — under the 600/10 caps. Small because the remainder of the arm (fmon scan, cls, map_monst, display_self, EDetect I_SPECIAL wrap, browse_map, map_redisplay) was already live; density exception applies (corpus-driven Open row with a recorded expectation — D-2071/D-2074/D-2075/D-2076 precedent).
**Change:** `js/detect.js` — deleted the `flush_topl_more()` call and its import (no other user in this function); replaced the stale comment with the C cite (`detect.c:842` → `getpos.c:843–846`, two-space join at `js/display.js:7387–96`). No new module edge, no TDZ, no RNG/mutation change.
**Verify:** `node scripts/verify.mjs --fn monster_detect` → PASS syntax (1 changed file: js/detect.js) · PASS rule2 · PASS hidden `0 PASS, 3 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-kit-Priest-92122 → js-throw@131; scen-normal-Priest-91108 → js-throw@54; scen-normal-Priest-92020 → erode_obj@86 — all strictly later steps/owners) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last js/ edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072/D-2080 precedent).
**Named:** none new — cursed-otmp wake, blessed persistent display_nhwindow, and unconstrain underwater/buried/swallow stay named in the map section + function header. This retires nothing; it corrects a live arm.
**Next:** do not re-pop `monster_detect` (all three sessions left it). The two newly visible js-throws (92122@131, 91108@54) become Must-fix rows on the next `hidden-proxy queue` rescore; 92020→erode_obj@86 flows through the normal queue.
## 2026-09-08 — D-2080 read.c doread Blind formula/book gate, mail illiterate-conduct envelope and SCR_TAMING allowlist: blind spellbook read drew study_book «memorize» where C draws «cannot read the mystic runes», taming scroll drew «not implemented» where C draws disappear + «Nothing interesting happens» (queue owner doread)

**C locus:** `read.c:329–647` `doread` — `scroll->pickup_prev = 0` (`:359`); Blind gate `:561–576` (`Blind && otyp != SPE_BOOK_OF_THE_DEAD`: novel→«words», SPBOOK→«mystic runes», `!dknown` scroll→«formula on the scroll», else fall through; pline + `ECMD_OK`, placed BEFORE literate conduct and `study_book`); mail envelope `:579–597` (`MAIL_STRUCTURES` live per `global.h`: `confused = FALSE` override + `!uconduct.literate && !spe` → `y_n("Reading mail will violate \"illiterate\" conduct.  Read anyway?")` gate); `seffect_taming` `:1679–1719` (`!candidates`→«Nothing interesting happens», C `pickup.c:3673` lookalike is a different writer — proxy `cMsgOwners` noise); seffects dispatch `:2236–2239` (`SCR_TAMING`/`SPE_CHARM_MONSTER` → `seffect_taming`, no useup).
**JS:** 1 file (read.js +43/−8 across the doread envelope + header) — under the 600/10 caps. Small because the seffects arms (taming D-1502, mail D-1896/D-1958) were already live; density exception applies (corpus-driven Open row with a recorded expectation, C remainder that small — D-2071/D-2074/D-2075/D-2076 precedent).
**Change:** `js/read.js` — `scroll.pickup_prev = 0` (eat.js/apply.js idiom, C `:359`); Blind gate ports C branch order verbatim (`Zblind` uses the same `u.Blind || u.ublind` idiom as the disappear block below; Dead exempt; novel/words, book/mystic-runes, `!scroll.dknown`/formula; `return 0` = ECMD_OK before conduct); mail `y_n` gate with the exact C prompt (two spaces before «Read») under `!(uconduct.literate|0) && !scroll.spe`; `confused` computation gains `&& otyp !== SCR_MAIL` (C `:580` override); allowlist gains `SCR_TAMING` (dispatch + `maybe_tame` already live — no-candidate path is draw-free, so the Knight step needs no new RNG). No new module edge (`y_n` already imported, same-module consts); no TDZ.
**Verify:** `node scripts/verify.mjs --fn doread` → PASS syntax (1 changed file: js/read.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Rogue-92019 PASS; scen-wish-Knight-92105 27→`fprefx`@99, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** none new — this retires three: doread Blind formula/book gate, mail conduct/confused envelope, SCR_TAMING allowlist. Untouched: fortune/shirt/cap/credit/grease/marker/coin/orb/candy-read arms + their Braille lines (module header reworded, still named); T-shirt/apron/hawaiian, novel/Dead/blank conduct exclusions already live; `livelog_printf` literate lines still deferred (file convention).
**Next:** pop the next Open row (`detect.c` monster_detect). Do not re-pop `doread` for these sessions (Rogue-92019 PASS; Knight-92105→fprefx@99 flows through the normal queue on rescore).
## 2026-09-08 — Audit D-2071…D-2079 against pinned C (reviews 1041–1049) + cadence score 44/44.

9 JS-touching SHAs since a1ff924f, each re-verified with hidden-proxy
verify --base HASH~1: 8 reproduce the D-log claim exactly; 1043 shows 2
PASS vs claimed 1 PASS + 1 moved (explained: 3a388782 fixed the later
can_twoweapon owner mid-batch); 1049's baseline lists 1 session vs claimed
2 (explained: stale 84dc0e34 scoreboard; working-scoreboard rows corroborate
both moves: 92119 save_dungeon@123, 92076 mhitm_knockback@159). 0 worse
anywhere. Deep checks: Upolyd writer-closure (5 C writers all mirrored,
polyman gate self-consistent), suit/armoroff 7-arm dispatch + dragon ranges
vs obj.h:347–352, mdamagem knockback order vs mhitm.c:1060–1072, doengr_exit
vs :1259–1263, ynq literal = established idiom (query_menu-only delta, not
queued). 9 ACCEPT, 0 Must-fix — no LOOP-QUEUE change owed. Fortress 44/44
(Scr 11405/11405, RNG 792838/792838, speed 65+0.38/turn R² 0.80). Rule #2
clean. Scoreboard verify dirt reverted, not committed.
## 2026-09-08 — D-2079 you.h Upolyd was mtimedone-based, so timeout-expiry rehumanize kept the monster umonnum and lost «You can see again.» (queue owner rehumanize)

**C locus:** `you.h:554` `#define Upolyd (u.umonnum != u.umonster)`; `polyself.c:200–268` `polyman` `:207–212` (`if (Upolyd)` restores acurr/amax, `u.umonnum = u.umonster`, female) then `:259–263` (`was_blind && !Blind` → `set_itimeout(&HBlinded,1)` + `make_blinded(0,TRUE)` → `potion.c:282` «You can see again.»); `timeout.c:642–648` (`if (u.mtimedone && !--u.mtimedone) ... rehumanize()` — mtimedone is already 0 at polyman entry, but Upolyd is still true via umonnum, so C restores).
**JS:** 1 file (const.js +6/−3) — under the 600/10 caps. Small because the whole polyman/rehumanize envelope was already live; density exception applies (corpus-driven Open row with a recorded expectation — D-2066/D-2071 precedent).
**Change:** `js/const.js` — `Upolyd(player)` is now `((player.umonnum | 0) !== (player.umonster | 0))` with the `you.h:554` cite (plus why-mtimedone-fails note). Single canonical definition; all ~116 call sites in 40 files inherit with no new import/module edge. State maintenance already matches C: `umonster` written only at role init (`u_init.c:991` ↔ `u_init.js:1870`), `umonnum` only via role init / `polymon` / polyman restore / `change_sex` no-op.
**Verify:** `node scripts/verify.mjs --fn rehumanize` → PASS syntax (1 changed file: js/const.js) · PASS rule2 · PASS hidden `0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-poly-Archeologist-92119 90→`save_dungeon`@123; scen-poly-Wizard-92076 →`mhitm_knockback`@159, both strictly later owners) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · full `sessions` 44/44 run explicitly (verify skipped full; speed `66+0.38/turn` R² 0.80) · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Probe `/tmp/probe_rehumanize.mjs` kept outside the repo.
**Named:** none new. Untouched: rehumanize's other named omits (emits_light/del_light_source, uhp<1 done(DIED), steed-gentle-landing pline, retouch_equipment, selftouch, update_inventory — no blocked session reaches them) and polyman's inline blind predicate (uroleplay term matches the live `Blind()` idiom; not implicated).
**Next:** Archeologist now blocked on `save_dungeon`@123 — a Parked misattributed owner (print_mapseen content, not save code); needs its true-owner row, not a save_dungeon port. Wizard now blocked on `mhitm_knockback`@159.
## 2026-09-08 — D-2078 spell.c spelleffects_check dropped the energy-message yet/anymore suffix and the Amulet drain arm: level-1 Priest drew «...cast that spell.» where C draws «...cast that spell yet.» (queue owner spelleffects_check)

**C locus:** `spell.c:1219–1380` `spelleffects_check` — amulet arm `:1296–1312` (`u.uhave.amulet && u.uen >= *energy` → `You_feel("the amulet draining your energy away.")`, `u.uen -= rnd(2 * *energy)`, clamp 0, `botl`, `*res = ECMD_TIME` even if the spell then fails); energy-fail arm `:1314–1330` (`You("don't have enough energy to cast that spell%s.", (u.uen < u.uenmax) ? "" : (*energy > u.uenpeak) ? " yet" : " anymore")`, `*res` untouched so an amulet-drained attempt still costs a turn). Caller `spelleffects :1384–1394` inits `res = ECMD_OK` (JS already mirrors).
**JS:** 1 file (spell.js +26/−3) — under the 600/10 caps. Small because the other 150 lines of the C function were already live; density exception applies (corpus-driven Open row with a recorded expectation, C remainder that small — D-2071/D-2074/D-2075/D-2076 precedent).
**Change:** `js/spell.js` — amulet arm ports C branch order verbatim (`(game.u?.uhave?.amulet || game.u?.uhave_amulet) && uen >= energy` — the eat.js/teleport.js dual-field idiom — → `You_feel` (already imported) + `rnd(2 * energy)` + `Math.max(0, …)` clamp + `botl` + `res = ECMD_TIME`); energy-fail arm computes the C suffix ternary (`uen < uenmax → ''`, else `energy > uenpeak → ' yet'`, else `' anymore'`) — `uenpeak` is live/maintained (exper.js/u_init.js/potion.js/eat.js/mhitu.js/trap.js). No new module edge, no TDZ (`You_feel`/`rnd` already imported); module header retires "amulet drain" from named omissions.
**Verify:** `node scripts/verify.mjs --fn spelleffects_check` → PASS syntax (1 changed file: js/spell.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-normal-Priest-92113 PASS; scen-kit-Priest-92122 22→`monster_detect`@129, strictly later owner, already an Open queue row) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** none new — the amulet arm retires a named omission. Unchanged: `check_capacity` clone drift (near_capacity vs C `check_capacity(msg)`, D-1225 live), `Role_if(PM_WIZARD)` vs `urole.mnum` idiom, hunger/HDR arms already live.
**Next:** pop the next Open row (`polyself.c` rehumanize). Do not re-pop `spelleffects_check` for these sessions (Priest-92113 PASS; Priest-92122→monster_detect@129 flows through the normal queue on rescore).
## 2026-09-08 — D-2077 do_wear.c armoroff delay arm hardcoded generic nouns: Knight helm doff drew «helmet» where C draws helm_simple_name «helm», and dragon mail doff drew «mail» where C draws suit_simple_name «dragon mail» (queue owner armoroff)

**C locus:** `do_wear.c:1920–2008` `armoroff` — delay arm `:1936–1973` assigns `what` per `oc_armcat` from `suit/shield/helm/gloves/boots/cloak/shirt_simple_name` (`:1938–1966`), then `Snprintf(offdelaybuf, "You finish taking off your %s.", what)` + `nomovemsg` (`:1968–1973`). `helm_simple_name` (`objnam.c:5513–5528`) = `!hard_helmet ? "hat" : "helm"`; `suit_simple_name` (`objnam.c:5470–5489`) returns `"dragon mail"`/`"dragon scales"` first (obj.h `Is_dragon_mail` otyp `GRAY_DRAGON_SCALE_MAIL..YELLOW_DRAGON_SCALE_MAIL`, `Is_dragon_scales` `GRAY_DRAGON_SCALES..YELLOW_DRAGON_SCALES`), then the " mail"/" jacket" suffix arms, else "suit".
**JS:** 1 file (do_wear.js +30/−12) — under the 600/10 caps. Small because the C locus's `nomul`/`afternmv`/immediate arms were already live (D-0259/D-0883/D-0063); this iteration retires the six deferred noun arms. Density exception applies (corpus-driven Open row with a recorded expectation, C remainder that small — D-2071/D-2074/D-2075/D-2076 precedent).
**Change:** `js/do_wear.js` — `armor_doff_simple_name` now dispatches exactly the C arms: suit→`suit_simple_name`, shield→`shield_simple_name` (local, silver/smooth), helm→`hard_helmet(otmp) ? 'helm' : 'hat'` (same idiom as `armor_simple_name:1079`, C `helm_simple_name`), gloves→`gloves_simple_name` (existing objnam.js import), boots→`boots_simple_name` (local, shoes), cloak→`cloak_simple_name` (local, robe/wrapping/smock), shirt literal; `suit_simple_name` ports the C branch order verbatim (dragon-mail/dragon-scales range arms via new `GRAY_/SILVER_DRAGON_*` consts + existing `YELLOW_*` endpoints — otyp ranges verified contiguous in `js/objects.js`: mail 101–110, scales 111–120 — then suffix arms, else "suit" with the C comment). No new module edge, no TDZ (`imports.mjs --can` confirms the objnam edge pre-exists); `armor_simple_name` doc retires the "dragon deferred" note.
**Verify:** `node scripts/verify.mjs --fn armoroff` → PASS syntax (1 changed file: js/do_wear.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-kit-Knight-92106 PASS; scen-normal-Knight-92215 87→`save_dungeon`@102, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Direct `hidden-proxy.mjs verify armoroff` also shows the working-scoreboard third session scen-wish-Healer-92147 →`enlightenment`@239 (later owner). Preflight `verify --no-cohort` was green before any edit on a clean tree.
**Named:** `armoroff` unknown-armcat default still returns 'armor' where C leaves `what=0` (no nomovemsg) — unreachable (all seven ARM_* arms live), pre-existing behavior untouched; `takeoff.mask` clear still deferred with the full A-command path (pre-existing comment). `armor_simple_name` default (`simpleonames` + impossible) untouched.
**Next:** pop the next Open row (`spell.c` spelleffects_check). Do not re-pop `armoroff` for these sessions (Knight-92106 PASS; Knight-92215→save_dungeon@102 and Healer-92147→enlightenment@239 flow through the normal queue on rescore — save_dungeon is a parked misattributed owner, do not chase it from this row).
## 2026-09-08 — D-2076 wield.c can_twoweapon used xname where C prints Yname2: dual-wield refusal dropped the "Your " prefix, so JS drew «battle-axe isn't one-handed.» where C draws «Your battle-axe isn't one-handed.» (queue owner can_twoweapon)

**C locus:** `wield.c:760–804` `can_twoweapon` — TWOWEAPOK arm `:781–786` (`pline("%s %s suitable %s weapon%s.", Yname2(otmp), is_plural(otmp) ? "aren't" : "isn't a", primary/secondary, plur(quan))`); bimanual arm `:787–790` (`pline("%s isn't one-handed.", Yname2(otmp))`). `Yname2` = upstart(yname) = "Your \<name\>" for carried non-pname wielded gear; `is_plural` = obj.h:421 (`quan != 1` or undiscovered Eyes of the Overworld); `plur(quan)` is quan-only ("" iff 1).
**JS:** 1 file (wield.js +13/−8) — under the 600/10 caps. Small because the C locus is two one-line plines and every callee (`Yname2`, `is_plural`) was already live/exported; density exception applies (corpus-driven Open row with a recorded expectation, C that small — D-2071/D-2074/D-2075 precedent).
**Change:** `js/wield.js` — both arms now print `${Yname2(otmp)}` (`Yname2` joins the existing `./objnam.js` import — same module edge as `xname`, no new cycle, no TDZ); suitability arm uses `is_plural(otmp)` for aren't/isn't-a while `plur` stays quan-only (`((otmp.quan ?? 1) | 0) !== 1 ? 's' : ''`, matching C `plur(quan)`); C line cites in comments; `can_twoweapon` doc retires `Yname2` from the named-omission list.
**Verify:** `node scripts/verify.mjs --fn can_twoweapon` → PASS syntax (1 changed file: js/wield.js) · PASS rule2 · PASS hidden `3 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-kit-Barbarian-92001, scen-kit-Samurai-92145, scen-normal-Samurai-92078 all PASS — brief showed 3 blocked vs the row's cited 2) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** artifact-resist arm still uses `xname` (live `Yobjnam2` export not wired; local clone at wield.js:1063 stays per the objnam "clones stay" note); CORPSE/`cant_wield_corpse` arm absent (`cant_wield_corpse` not ported — would return FALSE silently per C `:795–798`); `body_part(HAND)` is a 'hand' literal. No blocked session reaches any of these (map `turns.md` wield.c row updated).
**Next:** pop the next Open row (`do_wear.c` armoroff). Do not re-pop `can_twoweapon` for these sessions (all three PASS).
## 2026-09-08 — D-2075 uhitm.c mhitm_ad_stun mhitu arm never ported: second consecutive Pestilence touch fell into mhitm_adtyping_u default, so JS drew knockback rn2(3) where C drew the rn2(4) stun roll (queue owner mhitm_ad_stun)

**C locus:** `uhitm.c:4388–4422` `mhitm_ad_stun` — mhitu arm `:4403–4409` (`hitmsg(magr, mattk)` unconditional; `if (!magr->mcan && !rn2(4))` → `make_stunned((HStun & TIMEOUT) + mhm->damage, TRUE)` + `mhm->damage /= 2`; no phys call, no `done` check — unlike both sibling arms); dispatched by `mhitm_adtyping :4787` (`case AD_STUN`), reached from `hitmu` (`mhitu.c:1191` `mhitm_adtyping(mtmp, mattk, &gy.youmonst, &mhm)`); the AD_STUN itself comes from `getmattk` (`mhitu.c:337–347` — second consecutive same-type DISE/PEST/FAMN hit swaps to AD_STUN, aatyp untouched), already live in JS `get_mattk` (`mhitm.js:393–400`). `hitmu` order `d() → adtyping → knockback` (`mhitu.c:1187–1193`) was already C-faithful in JS, so this is a missing arm, not an order bug (same shape as D-2059 pest / D-2074 samu, which moved these exact sessions here).
**JS:** 2 files (mhitu.js +24/−2, mhitm.js +2/−2) — under the 600/10 caps. Small because the C mhitu arm is 7 lines and every callee was already live/imported; density exception applies (corpus-driven Open row with a 2-session recorded expectation, C that small — D-2071/D-2074 precedent).
**Change:** `js/mhitu.js` — new `mhitm_ad_stun_u` (`hitmsg` always; `!(mtmp.mcan|0) && !rn2(4)` → `make_stunned(((game.u?.HStun|0) & TIMEOUT) + (mhm.damage|0), true)` (the :3264 gaze-arm idiom) + `mhm.damage = Math.trunc((mhm.damage|0) / 2)` (C `/= 2` truncation); no `void mhm` — damage is conditionally written, unlike the keep-arms); `case AD_STUN` in `mhitm_adtyping_u` (+ doc-list `STUN`); no new imports (`hitmsg`, `make_stunned`, `rn2`, `TIMEOUT` all already live in-file — no new module edge, no TDZ). `js/mhitm.js` — `mhitm_ad_stun` doc «Named omit» line retires the mhitu arm (now points at `mhitm_ad_stun_u`); uhitm arm stays named.
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_stun` → PASS syntax (2 changed files: js/mhitm.js js/mhitu.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-genesis-Archeologist-91127 PASS; scen-genesis-Barbarian-91118 92→`disclose`@110, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** `mhitm_ad_stun` uhitm arm (`:4394–4402` — hero-as-attacker `!Blind` stagger pline + phys, no mcan gate — no blocked session reaches it); mhitm arm is D-1396 live. No blocked session reaches the `!rn2(4)` stun body itself (both sessions drew nonzero: 2 and 1), so `make_stunned`'s talk/botl path there is exercised only by its existing callers (gaze arm :3264).
**Next:** pop the next Open row (`wield.c` can_twoweapon). Do not re-pop `mhitm_ad_stun` for these sessions (Archeologist PASS; Barbarian→disclose@110 flows through the normal queue on rescore).
## 2026-09-08 — D-2074 uhitm.c mhitm_ad_samu never ported: Wizard AD_SAMU fell into mhitm_adtyping_u default, so JS drew knockback rn2(3) where C drew the rn2(20) steal roll (queue owner mhitm_ad_samu)

**C locus:** `uhitm.c:4570–4589` `mhitm_ad_samu` — uhitm arm `:4573–4576` (`mhm->damage = 0`); mhitu arm `:4577–4586` (`hitmsg(magr, mattk)`; `if (!rn2(20)) stealamulet(magr)` — 1/20 quest-artifact/Amulet/invocation-tool snatch when the Wizard or a quest nemesis hits; damage untouched so the base `hitmu` `d()` stands); mhitm arm `:4587–4588` (`mhm->damage = 0`); dispatched by `mhitm_adtyping :4823` (`case AD_SAMU`); `AD_SAMU = 252` (`monattk.h`). `hitmu` order `d() → adtyping → knockback` (`mhitu.c:1191–1193`) and `mdamagem` order `adtyping → knockback` (`mhitm.c:1059–1065`) were already C-faithful in JS, so this is a missing arm, not an order bug (same shape as D-2071 corr).
**JS:** 3 files (mhitm.js +35/−1, mhitu.js +24/−3, uhitm.js +7/−2) — under the 600/10 caps. Small because the C locus is 17 lines and every callee (`hitmsg`, `stealamulet`, `rn2`, `mhitm_knockback`) was already live; density exception applies (corpus-driven Open row with a 2-session recorded expectation, C that small).
**Change:** `js/mhitm.js` — file-local `const AD_SAMU = 252` (monattk.h cite, file idiom) + export-list row; sync `mhitm_ad_samu` (mhitm arm: zero damage, no message); `mdamagem` `AD_SAMU` case (ad func → `mhitm_knockback` → `return mhm.hitflags`, the AD_PHYS zero-damage precedent — knockback `rn2(3)` still burns exactly where C burns it). `js/mhitu.js` — `AD_SAMU` joins the existing `./mhitm.js` import, `stealamulet` joins the existing `./steal.js` import (steal.js imports neither mhitu nor mhitm — no new cycle; runtime call, no TDZ); new `mhitm_ad_samu_u` (`hitmsg` + `if (!rn2(20)) await stealamulet(mtmp)`, `void mhm` leftover-kept idiom); `case AD_SAMU` in `mhitm_adtyping_u` (+ doc-list row). `js/uhitm.js` — `AD_SAMU` joins the existing `./mhitm.js` import; `damageum_adtyping` `else if (adtyp === AD_SAMU) mhm.damage = 0` (+ doc-list row).
**Verify:** `node scripts/verify.mjs --fn mhitm_ad_samu` → PASS syntax (3 changed files: js/mhitm.js js/mhitu.js js/uhitm.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-tour-Samurai-91113 PASS; scen-tour-Wizard-92103 99→`dochug`@100, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff (not committed, D-2072 precedent).
**Named:** none new — all three `mhitm_ad_samu` arms are live. `stealamulet`'s own singleton omission (D-1945, map `turns.md` row) is pre-existing and untouched; the `mhitm_ad_samu caller wiring` named bit of that row is now live (map updated).
**Next:** pop the next Open row (`uhitm.c` mhitm_ad_stun). Do not re-pop `mhitm_ad_samu` for these sessions (Samurai PASS; Wizard→dochug@100 flows through the normal queue on rescore).
## 2026-09-08 — D-2073 engrave.c doengrave add-to ynq prompt never asked: same-type engraving auto-appended where C prompts, so JS skipped the recorded [ynq] screen (queue owner doengrave)

**C locus:** `engrave.c:1113–1167` — `de->type == HEADSTONE → c = 'y'` (no choice, only append); `de->type == de->oep->engr_type && (!Blind || BURN || ENGRAVE)` → `c = yn_function("Do you want to add to the current engraving?", ynqchars, 'y', TRUE)` (`decl.c:114` `ynqchars = "ynq"`), `c == 'q'` → `pline1(Never_mind)` + `goto doengr_exit`; `c == 'n' || Blind` wipe/overwrite arms unchanged; `else if Strlen(oep->engr_txt[actual_text]) >= BUFSZ-1` → `There("is no room to add anything else here.")` + `ret = ECMD_TIME` + exit (`:1162–1167`); exit `:1257–1263` newsyms iff `disprefresh`.
**JS:** 1 file (engrave.js +21/−6) — under the 600/10 caps. Small because the C locus is two short arms and every callee (`yn_function`, `pline`, `newsym`, `BUFSZ`, `Never_mind`, `ECMD_TIME`) was already live/imported; density exception applies (corpus-driven Open row with a 2-session recorded expectation).
**Change:** `js/engrave.js` — same `if (de.oep)` envelope now ports C branch order: HEADSTONE → `c = 'y'`; same-type → `c = await yn_function('Do you want to add to the current engraving?', 'ynq', 'y')` (default-true matches C TRUE; `yn_function` joins the existing `./getline.js` import — same module as `getlin`, no new edge, no TDZ) with `q` → `pline(Never_mind)` + disprefresh newsym + `return de.ret` (C `goto doengr_exit` preserves initial ret); added the `else if` BUFSZ-full arm via `String(de.oep.engr_txt?.actual_text ?? '').length >= BUFSZ - 1` → `pline('There is no room to add anything else here.')` + newsym + `return ECMD_TIME`; header/doc `yn add-to` omissions retired.
**Verify:** `node scripts/verify.mjs --fn doengrave` → PASS syntax (1 changed file: js/engrave.js) · PASS rule2 · PASS hidden `1 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-kit-Valkyrie-91116 PASS; scen-normal-Samurai-92078 44→`can_twoweapon`@118, strictly later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · `skip full (no shared file changed)` · VERIFY: PASS, final verify after the last edit (no D-1831 gap). Preflight `verify --no-cohort` was green before any edit on a clean tree. Working `hidden-corpus/scoreboard.json` dirt reverted before handoff.
**Named:** `doengrave` altar/jello/swallow/lava/pool setup arms, `livelog` literate log, Blind-feel engrave/burn path, full `surface`/`is_ice`, wipeout seeded path, `disturb_grave`-from-doengrave beyond kick callers, `set_levltyp` beyond GRAVE typ (all pre-existing map notes in `turns.md`, untouched — no blocked session reaches them).
**Next:** pop the next Open row (`uhitm.c` mhitm_ad_samu). Do not re-pop `doengrave` for these sessions (Valkyrie PASS; Samurai→can_twoweapon@118 flows through the normal queue on rescore — that row is already the next Open item).
