# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2143 `hack.c` runmode_delay_output movement-delay frames: anim 123/1483 → 1440/1483 (queue row NEXT, supplemental, 0 sessions)

**C locus:** `hack.c:2995–3018` (`runmode_delay_output`: `(svc.context.run || gm.multi) && flags.runmode != RUN_TPORT` gate; leap `RUN_LEAP` updates every 7th `svm.moves`, walk/crawl every step; `disp.time_botl = flags.time`; `curs_on_u()` + `nh_delay_output()` + 4 more in crawl) + 4 call sites (`hack.c:2990` domove tail; `allmain.c:381` moveloop `multi<0` before `++gm.multi`; `allmain.c:509` post-occupation before `return`; `allmain.c:517` running branch after `lookaround()` before the `!gm.multi` check) + `options.c:3626–3659` (`optfn_runmode` prefix table) + `include/flag.h:548–551` (`RUN_TPORT/RUN_LEAP/RUN_STEP/RUN_CRAWL`) + `options.c:217–219` (`teleport/run/walk/crawl`).
**JS:** 3 files (`js/hack.js` +~40, `js/cmd.js` +~6, `js/allmain.js` +~6), under the 600/10 caps.
**Change:** new exported async `runmode_delay_output()` in `js/hack.js` (C-faithful home, `// src/hack.c:2995` cite): raw-string normalization with C's prefix table in C order (`teleport→TPORT, run→LEAP, walk→STEP, crawl→CRAWL`, case-insensitive prefix, empty/unrecognized → `RUN_LEAP` default); C gate order; leap `game.moves % 7` turn-counter version verbatim (not the ought-to-be); `time_botl` re-arm mirroring the `end_running` pattern (both `game.flags` and `game.disp`); `await curs_on_u()` + `await nh_delay_output()` (+4 crawl). Wired at all 4 C sites in C order: `domove()` try-tail after the `cause_delay` block (`js/cmd.js`, finally smudge untouched per D-0359); moveloop `multi<0` first statement before `g.multi++` (`js/allmain.js`); post-occupation after `monster_nearby→stop_occupation`, before `return` (`reset_eat` deferral untouched); `continue_run()` after `lookaround()`, before the multi/run clear check (`js/cmd.js`, this tree's home of `allmain.c:513–519`). `curs_on_u`/`nh_delay_output` join the existing `display.js` edge, `RUN_*` the existing `const.js` edge, `runmode_delay_output` the existing `hack.js` edges in `cmd.js`/`allmain.js` (`imports.mjs --can`: no new edge anywhere, no TDZ risk).
**Verify:** `node scripts/verify.mjs --fn runmode_delay_output` → PASS syntax (3 changed js file(s)) · PASS rule2 · note hidden (vacuous: no corpus session blocked on it at HEAD — NOT claimed as a corpus PASS; row cited 0 blocks) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Anim recipe `node frozen/ps_test_runner.mjs sessions`: total **1440/1483** (was 123/1483), bellwether `seed0014-dequa-fountain-explore` **995/995** (was 3/995), all 44 sessions still passing. No-session-falls holds by mechanism: the scorer counts per-step positional screen-equal frames and ignores JS extras, and this port only adds `animationFrame` emissions — it cannot un-emit a previously matched frame.
**Named:** the `optfn_runmode` options-table setter itself stays unported (normalization lives inside the new function; sessions only ever carry valid values per spec). No other new omits.
**Next:** residual 43 unmatched frames in 7 passing sessions for future rows: seed0002 126/128, seed0004 42/47, seed0030 15/40, seed0106 5/6, seed0108 3/4, seed1150 0/6, seed4500 34/37. Probed seed1150: its 6 C frames all sit at step 38 whose dice are `throw_obj(dothrow.c:233)` + `obj_resists`/`distfleeck` — thrown-object flight source, not movement-delay, so not this port's bug. Do not re-pop `runmode_delay_output`.
## 2026-09-08 — D-2142 `uhitm.c` find_roll_to_hit encumbrance/`utrap`: JS hit while encumbered where C misses (queue row `uhitm.c` passive, 1 session)

**C locus:** `uhitm.c:375–415` (`find_roll_to_hit`: `tmp = 1 + abon() + find_mac(mtmp) + u.uhitinc + sgn(Luck)*((abs(Luck)+2)/3) + maybe_polyd(form mlevel, ulevel)` + state adds, then `:407–411` `if ((tmp2 = near_capacity()) != 0) tmp -= (tmp2*2)-1; if (u.utrap) tmp -= 3;`) + `hack.c:4372–4388` (`calc_capacity`/`near_capacity`) + `worn.c:717–738` (`find_mac`) + `makemon.c:2016–2041` (`adj_lev`, via the D-2052 poly `abon` arm).
**JS:** 1 file (`js/uhitm.js` +8/−3), under the 600/10 caps. Density note: below the usual 40-line floor, but the missing C surface is exactly these two RNG-free statements — the whole `find_roll_to_hit` was already live (D-0272/D-2052 precedent for small-C-unit entries).
**Change:** `js/uhitm.js` only — `near_capacity` joins the existing static `invent.js` edge (`imports.mjs --can`: already imported, no new edge); `find_roll_to_hit` applies `if (cap) tmp -= cap*2-1; if (u.utrap) tmp -= 3;` in C order after the monster-state adds, with C cite `:407–411`. Monk-armor / orc-vs-elf stay named-deferred (no corpus demand). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn passive` → PASS syntax (1 changed js file(s): js/uhitm.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify passive: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-poly-Archeologist-92226 moved → hurtle_step at step 110, was 107) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) · VERIFY: PASS. Plus full `node frozen/ps_test_runner.mjs sessions`: fortress 44/44 PASS (no throws). Hand probes in `/tmp` only, none in the tree.
**Named:** `find_roll_to_hit` monk-armor + orc-vs-elf arms stay named (map `turns.md:3336`, untouched by this path); `weight_cap` Boots-on-Lev / strong-steed-MAX named omits stay (map, untouched — hero walks, no steed here).
**Next:** Archeologist-92226 now blocked at step 110 under `hurtle_step` — a later owner for a future row (do not re-pop `passive` for it). `passive:6019` live gate itself needs no change (JS `uhitm.js:1748` already draws it unconditionally for `malive && !mcan`).
## 2026-09-08 — Audit b9b2fdef..79844c1c (reviews 1103–1107: 5 ACCEPT, 0 Must-fix) + cadence 44/44

Review iter over the 5 JS-touching SHAs since 51283e0c, oldest first, each audited against pinned C with independent `hidden-proxy verify --base` re-runs: 1103 cursetxt (mcastu.c:62–85 branch-exact incl. `is_obj_mappear` macro match; PROGRESS 166→169 re-measured), 1104 set_uasmon TELEPORT pair (PROPSET bit semantics; PROGRESS 88→107), 1105 quivered-gold fall-through (canletgo dead-for-gold proven via do.c:664–711; freeinv single-decrement audited; D-log's vacuous-throw_obj note credited; PROGRESS 4→27), 1106 castmu FIRE/COLD/MAGM switch (arm-by-arm vs :230–304, ml≡m_lev, AD consts vs monattk.h; PROGRESS 124→145), 1107 refuse-arm dropy (line-identical to :1229; same-owner 136→168 PROGRESS honestly labeled). No C-wrongs → no Must-fix prepends, Next cluster unchanged (dog_goal@168 residual). Cadence: full `sessions` 44/44, Scr 11,405/RNG 792,838 unchanged, speed `63+0.39/turn` (R² 0.81).
## 2026-09-08 — D-2141 `invent.c` hold_another_object artifact-refuse `dropy`: JS ate refused wish-artifacts out of every chain (queue row `dogmove.c` dog_goal, 1 session)

**C locus:** `invent.c:1227–1231` (`hold_another_object` artifact-refuse arm: `obj_extract_self(obj)` then `dropy(obj)` then `return obj`) + `do.c:800–805` (`dropy` → `dropz(FALSE)`, RNG-free: unwield checks, `flooreffects`, place/stack/`newsym`, `encumber_msg`) + `zap.c` `makewish` (calls `hold_another_object` and ignores the return, relying on the floor side effect). Chain, all machine-recorded: step-125 wish «uncursed the Platinum Yendorian Express Card» → `touch_artifact` blast («You are blasted by ... power!», `rng2`) + evade («... evades your grasp!», step 126, `rng3`); C `dropy` floors the refused artifact at hero feet. At step 136 the pet evaluates it first in `fobj` order: PYEC is an artifact but not Priest-quest (`questarti` is the Mitre), so `dogfood` takes `obj_resists(obj,0,95)` (draws `rn2(100)=0`, `0<95` true → APPORT), then the apport gate passes and draws `rn2(8)=4`; the nearby gold-in-stone draws `rn2(100)=46` next and is skipped. `dogmove.c:554` and `dog_goal` need no change.
**JS:** 1 file (`js/invent.js` +5/−4), under the 600/10 caps. Density note: below the usual 40-line floor, but the missing C surface is exactly the one `dropy` call — the whole refuse arm was already live (D-2134; D-2139 precedent for small-C-unit entries).
**Change:** `js/invent.js` only — `await dropy(obj)` after `obj_extract_self(obj)` in the refuse arm (C `:1229`), with C cite. `dropy` joins the existing static `do.js` edge (`setnotworn` already imported from it; `imports.mjs --can` confirms no new edge, hoisted function export, no TDZ risk). wasUpolyd revert-grip arm + crysknife otyp/oerodeproof restore stay named (no poly/crysknife on this path). No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn dog_goal` → PASS syntax (1 changed js file(s): js/invent.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify dog_goal: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Priest-92136 moved → dog_goal at step 168, was 136; rngM 2440→2654, scrM 138→168) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) · VERIFY: PASS. Post-fix first-diff at the new point: C `rn2(8)=7 @ dog_goal:554` now MATCHES JS `rn2(8)=7 @ dog_goal`, then C `rn2(100)=91` matches, then C `rn2(4)=3 @ dog_goal(dogmove.c:575)` vs JS `rn2(3)=0 @ dog_move` (next unit, see Next).
**Named:** `hold_another_object` wasUpolyd revert-grip arm + crysknife restore + fatal-wished-corpse arm stay named (map `turns.md:1204`, untouched by this path); map `turns.md:1199/1204` updated (refuse-arm `dropy` live, no longer named).
**Next:** Priest-92136 now blocked at step 168 under `dog_goal` (`dogmove.c:575`, the `!rn2(4)` follow-player gate, empty toplines both sides) — re-queued as a fresh Open row. Do not re-pop `dog_goal@136` for it.
## 2026-09-08 — D-2140 `mcastu.c` castmu AD_FIRE/COLD/MAGM switch + `mdamageu` tail: JS dropped the spell-effect arm (queue row `mcastu.c` castmu, 1 session)

**C locus:** `mcastu.c:247–304` (`ret = M_ATTK_HIT` + `switch (mattk->adtyp)` AD_FIRE/AD_COLD/AD_MAGM/SPEL/CLRC in C order, then `if (dmg) mdamageu(mtmp, dmg)`); MAGM arm `:283–295` (`You("are hit by a shower of missiles!")`, Antimagic → `shieldeff` + `pline_The("missiles bounce off!")` + `monstseesu(M_SEEN_MAGR)` + `dmg = 0`, else `dmg = d(m_lev/2+1, 6)` + `monstunseesu(M_SEEN_MAGR)`, `mon_spell_hits_spot(mtmp, AD_MAGM, u.ux, u.uy)`); FIRE `:253–266` / COLD `:267–281` same shape with `M_SEEN_FIRE`/`M_SEEN_COLD` + `burn_away_slime` (FIRE only); `monattk.h:43–45` (`AD_MAGM 1`, `AD_FIRE 2`, `AD_COLD 3`).
**JS:** 1 file (`js/mcastu.js` +63/−6), under the 600/10 caps.
**Change:** `js/mcastu.js` only — full C switch in C order and short-circuit: FIRE (`pline("You're enveloped in flames.")`, `Fire_resistance()` → `shieldeff` + resist pline + `monstseesu(M_SEEN_FIRE)` + `dmg = 0` else `monstunseesu`, `burn_away_slime`, `mon_spell_hits_spot(AD_FIRE)`); COLD (same with local `Cold_resistance()` + `M_SEEN_COLD`); MAGM (`pline('You are hit by a shower of missiles!')` for C `You(...)`, `Antimagic()` → `shieldeff` + `pline('The missiles bounce off!')` for C `pline_The` + `monstseesu(M_SEEN_MAGR)` + `dmg = 0` else `dmg = d(ml/2+1, 6)` + `monstunseesu`, `mon_spell_hits_spot(AD_MAGM)`); SPEL/CLRC unchanged (`mcast_spell`, `dmg = 0`); then `if (dmg) await mdamageu(mtmp, dmg)` + `return ret`. No local `You`/`pline_The` clones (full-text `pline`, cf. `zap.js:1927`; sym guidance forbids clone #2).
**Verify:** `node scripts/verify.mjs --fn castmu` → PASS syntax (1 changed js file(s): js/mcastu.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify castmu: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-wish-Monk-92063 moved → ready_weapon at step 145, was 124) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS.
**Named:** `mcast_spell` FIRE_PILLAR/LIGHTNING/GEYSER `mon_spell_hits_spot` arms stay named (file-local comments); `buzzmu` (+ its `cursetxt(mtmp, FALSE)` call site) and `has_aggravatables` stay named (map `turns.md`); fumble `Soundeffect`/air-crackles pline stays deferred (pre-existing comment); mis-aimed `is_waterwall` "empty water" wording + `pline_mon` stay deferred (pre-existing comment); `m_seenres` gate stays as-is per the existing `M_SEEN_NOTHING` comment (out of cluster).
**Next:** Monk-92063 now blocked at step 145 under `ready_weapon` (`wield.c:206`) — a wield-path writer for a future row. Do not re-pop `castmu` for it.
## 2026-09-08 — D-2139 `dothrow.c` throw_obj quivered-gold arm: JS dropped the throw (queue row `makemon.c` makemon, 1 session)

**C locus:** `dothrow.c:112–116` (non-quiver coins → `throw_gold`; quivered coins fall through) + `:254–272` (m_shot loop: `splitobj` one coin / `freeinv` / `throwit` / `encumber_msg`); landing `breaktest` (`:1780` → `:2582–2593` → `obj_resists(zap.c:1469)` `rn2(100)`); `invent.c` `freeinv_core` coin arm (`disp.botl = TRUE`). `canletgo` (`do.c:665–709`) cannot refuse gold (worn-armor / uwep-welded / LOADSTONE / LEASH / SADDLE only) and the Mjollnir/BOULDER/too-heavy/welded/wet-towel gates are otyp-specific, so skipping them for gold is behavior-identical.
**JS:** 1 file (`js/dothrow.js` +20/−11), under the 600/10 caps. Density note: below the usual 40-line floor, but the missing C surface is exactly the early-return plus the cache arm — the whole m_shot/throwit/landing envelope was already live (D-2137/D-2138 precedent for small-C-unit entries).
**Change:** `js/dothrow.js` only — (1) coin gate is now `if COIN_CLASS && obj !== uquiver → throw_gold`; quivered coins fall through to the live m_shot loop (split/freeinv/throwit/encumber in C order); (2) file-local `freeinv` decrements `_goldCount` + sets `botl` for COIN_CLASS (C `freeinv_core` coin arm); (3) `throw_gold`'s manual decrement removed (now double-count). Callees `splitobj→next_ident` (`mkobj.js`) and `obj_resists→rn2(100)` (`dogmove.js`) already live and C-faithful; no new imports/edges, no TDZ risk. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn throw_obj` → PASS syntax (1 changed js file(s): js/dothrow.js) · PASS rule2 · note hidden (vacuous for `throw_obj` at HEAD and at base 6f87bee1 — no session ever blocked on it; the row's recorded owner is `makemon`, covered next) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (forced shared-path check) · VERIFY: PASS. Plus `hidden-proxy verify makemon` → 0 PASS, 1 moved past (step 4 → step 27 `legs_in_no_shape`, RNG now 3085/3085, screens 170/172), 0 unchanged, 0 worse → PROGRESS. Live-JS probe (kept in /tmp): steps 0–15 topline+status-line match C exactly post-fix (`$:1092 T:2` at step 6).
**Named:** map `turns.md:2543` paren retires «quivered gold» (live this entry); unsplitobj D-0720 / dungeon.c ceiling vault-temple-shop labels stay named. File-local `freeinv` update_inventory/artifact/timer arms stay named (pre-existing clone shape, out of cluster).
**Next:** Healer-91123 now blocked at step 27 under `legs_in_no_shape` (`do.c:2412`): C «Your right leg is in no shape for riding.» vs JS empty topline — a `#ride`-path writer for a future row. Do not re-pop `makemon`/`throw_obj` for it.
## 2026-09-08 — D-2138 `polyself.c` set_uasmon TELEPORT/TELEPORT_CONTROL FROMFORM: tengu-poly hero drew moveloop `rn2(85)` in C, JS skipped it (queue row `allmain.c` moveloop_core, 1 session)

**C locus:** draw site `allmain.c:307–309` (`if (Teleportation && !rn2(85)) tele(); …`); predicate-state writer `polyself.c:93–95` `set_uasmon` `PROPSET(TELEPORT, can_teleport(mdat))` / `PROPSET(TELEPORT_CONTROL, control_teleport(mdat))`; `mondata.h:82–83` (`M1_TPORT`/`M1_TPORT_CNTRL` on `mflags1`); `youprop.h` `Teleportation ≡ HTeleportation || ETeleportation` (intrinsic carries FROMFORM). Review 73 (D-1002) verified the moveloop arm order/short-circuit, so the arm was innocent — the FROMFORM bit was never set.
**JS:** 1 file (`js/polyself.js` +10/−1), under the 600/10 caps. Density note: below the usual 40-line floor, but the C unit is two PROPSET lines and the whole writer envelope (predicates + bit helper + both call sites) is ported.
**Change:** `js/polyself.js` only — `can_teleport`/`control_teleport` added to the existing `monsters.js` edge (the edge `eat.js`/`dokick.js` already use; hoisted function exports, no TDZ risk) and `TELEPORT`/`TELEPORT_CONTROL` to the existing `const.js` edge; two `propset_fromform` lines in C position (`:94–95`) setting `HTeleportation`/`HTeleport_control` plus the `uprops` FROMFORM bit (bit-preserving `|=` / `&= ~` like C PROPSET); header Named-omissions list retired for the two. JS data carries the bits (`PM_TENGU mflags1 0x6000000` = TPORT|TPORT_CNTRL). Revert path (`polyman`→`set_uasmon` with human mdat) clears FROMFORM exactly like C's else branch.
**Verify:** `node scripts/verify.mjs --fn moveloop_core` → PASS syntax (1 changed js file(s): js/polyself.js) · PASS rule2 · PASS hidden (verify moveloop_core: 0 PASS, 1 moved past → PROGRESS: scen-poly-Archeologist-92226 moveloop_core@88 → passive@107, later step + later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) + hand full `sessions` 44/44 PASS (speed `79+0.51/turn`) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** remaining `set_uasmon` PROPSETs (ANTIMAGIC/SICK_RES/STUNNED/SEE_INVIS/TELEPAT/INFRAVISION/INVIS/LEVITATION/…) stay named (`turns.md` row; no corpus session reaches them this iter); moveloop leash/CQ_REPEAT status unchanged (D-1002/D-1005).
**Next:** Archeologist-92226 residual at step 107 under `uhitm.c` `passive` (`:6019` `rn2(3)=1` vs JS `d(1,7)=5 @ damageum(uhitm.js:1467)`; C «You miss the little dog. The baluchitherium hits!» vs JS «You bite the little dog. You kill the poor little dog!») — an `hmonas` to-hit branch divergence (JS hits+kills where C misses, so JS never reaches `passive`), not the teleport gate; re-queued as an Open `passive` row this commit. Do not re-pop `moveloop_core` for it.
## 2026-09-08 — D-2137 `mcastu.c` cursetxt frustrated-cast feedback: JS dropped the pline, starving a `--More--` (queue row `objnam.c` readobjnam_postparse1, 1 session)

**C locus:** `mcastu.c:61–85` `cursetxt` + `:174–179` `castmu` unable-to-cast arm. The lich cast at 117 («casts a spell at you!», `mspec_used` set), chose an undirected spell at 126, failed on `mspec_used`, so `cursetxt(mtmp, is_undirected_spell()=TRUE)` took the `canseemon && couldsee` arm → `pline_mon("%s points %s.", "The master lich", "all around, then curses")` with ZERO RNG — queued behind shatter#2, which is exactly C's `--More--`. Step 127's topline is that curse text.
**JS:** 1 file (`js/mcastu.js` +45/−3), under the 600/10 caps. Density note: below the usual 40-line floor, but the C unit is one 24-line staticfn and the whole envelope (function + its live call site) is ported.
**Change:** `js/mcastu.js` only — module-local `async cursetxt(mtmp, undirected)` in exact C order and short-circuit (Invis/Displaced read from `game.u` like `mcast_summon_mons`; hero-mimic via `M_AP_TYPE`/`M_AP_OBJECT` from the existing `const.js` edge + `STRANGE_OBJECT = 0` per `objects.h` (cf. `readobjnam.js`); `u.uundetected`; moves via `game.moves`; local `Deaf()`/`perceives()`; `pline_mon`/`Norep` added to the existing `display.js` edge — no new module edges, no TDZ risk); castmu arm now `await cursetxt(mtmp, is_undirected_spell(spellnum))` before `return M_ATTK_MISS`. The `m_seenres` 4th disjunct is untouched (AD_SPEL/CLRC map to M_SEEN_NOTHING per the existing in-code analysis; the firing disjunct here is among the first three since JS took the same early return with matched RNG).
**Verify:** `node scripts/verify.mjs --fn cursetxt` → PASS syntax (1 changed js file(s): js/mcastu.js) · PASS rule2 · PASS hidden (verify cursetxt: 0 PASS, 1 moved past → PROGRESS: scen-poly-Tourist-92047 cursetxt@166 → fig_transform@169 — the blind-arm `rn2(4)` now burns in sync) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) · VERIFY: PASS. Plus `hidden-proxy verify readobjnam_postparse1` → 0 PASS, 1 moved past (same-step re-attribution, explicitly accepted: step 126 now `do_statusline2`, topline matches with `--More--` both sides) → PROGRESS. Post-fix JS replay: 126 = shatter#2+More, 127/128 = curse text, T:6, unknown-command cascade gone.
**Named:** `buzzmu` `cursetxt(mtmp, FALSE)` call stays named (`js/mcastu.js` buzzmu header; whole zap path deferred); `m_seenres` disjunct analysis unchanged; map `turns.md` castmu line retired `cursetxt` from the named list (this entry).
**Next:** Healer-92107 residual is PRE-EXISTING (proven: stashed pre-change tree replays JS HP:2 at 126 too) — C HP:7 vs JS HP:2, i.e. JS over-applies 5 = exactly the two destroy `rnd(4)`s (3+2) on top of the shared `d(3,6)=10` touch (step-123 `hitmu`) — prime suspect `js/mhitu.js:901` `mhitm_ad_cold_u`: C mhitu arm (`uhitm.c:2660–2661`) does `(void) destroy_items(...)` (return discarded; damage flows only via `maybe_destroy_item` losehp), JS does `mhm.damage += await destroy_items(...)`, double-counting destroy damage inside the touch application. One-line fix for the iter that pops the `do_statusline2` row; audit `fire_u`/`elec_u` arms for the same `+=` pattern. Do not re-pop `readobjnam_postparse1`/`cursetxt` for these sessions.
## 2026-09-08 — Audit 98e0bb9e..51283e0c (reviews 1097-1102: 6 ACCEPT, 0 Must-fix) + cadence 44/44

C-fidelity audit of the six JS-touching commits since 107514a9 (D-2131…D-2136), oldest first, one file per SHA. Every D-log corpus claim re-measured with `hidden-proxy verify <fn> --base HASH~1`: uunstick 1 PASS PROGRESS; gazemu 0+1 moved PROGRESS; dobuzz 1+1 PROGRESS; touch_artifact 1 PASS PROGRESS; burnarmor 0+1 moved PROGRESS; level_tele 0+1 moved PROGRESS — all true, none vacuous. C spot-checks: polyman sticking/entry+position vs polyself.c:199-268; stagger chain+locoindx tables vs mondata.c:1367-1407 (clone #2 matches mhitm.js:845 exactly, declared debt); dobuzz hero arm vs zap.c:4963-4991 + zhitu MM vs :4410-4419; refuse arm vs invent.c:1227-1231 (+map naming at turns.md:1193); LVL group 4 = mr (werejackal idx 15 → 10, 383=383); hells[7] origin shift via nhlsel.c:540/sp_lev.c:208 + lit via mkmaze.c:125-142. Rule #2 clean (`imports.mjs --rulecheck`). No Must-fix prepended; queue stays 9 Open (in the 8-12 band, no refill). Cadence full `sessions`: 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed 88+0.74/turn (R² 0.84).
## 2026-09-08 — D-2136 `dat/hellfill.lua` hells[7] border frame missed the coder-origin shift: JS painted lava at (0,0)-(78,20), C lands it at (1,0)-(79,20) — 12 lava/room cells on a Gehennom cavern arrival (queue row `teleport.c` level_tele, 1 session)

**C locus:** `dat/hellfill.lua` hells[7] «open cavern» (`local border = selection.rect(0,0, 78, 20); des.terrain({ selection = border, typ = wter, lit = 0 })`) + `nhlsel.c` `l_selection_rect` (`get_location_coord` per corner) + `sp_lev.c` `get_location` (`if (*x >= 0) { *x += mx; *y += my; }` with `mx = gx.xstart = 1` «column [0] is off limits», `my = gy.ystart = 0`). So C's frame lands at (1,0)-(79,20): west edge paints x=1 lava over the grown room fringe, east edge x=79 leaves x=78 grown rooms intact. Ground truth via C `#terrain` internals dump (`levltyp`, D:1,L:3 «maze gehennom»): (1,7-10)=LAVA vs JS ROOM; (78,5-7,10-14)=ROOM vs JS LAVA — exactly the 12 probe cells, nothing else on the level (full 80×21 C-vs-JS terrain diff clean otherwise).
**JS:** 1 file (`js/mklev.js` +19/−11), under the 600/10 caps. Density note: below the usual 40-line floor, but the C locus is one frame-placement rule and the fix retires it exactly (12/12 cells, full-level diff otherwise clean).
**Change:** `js/mklev.js` only — border now built with the shared `selection_rect_rel(0, 0, 78, 20)` (same `get_location_coord` origin shift C applies: `game.splev_xstart/ystart` reset to 1/0 like C) and painted via `selection_iterate` + `sel_set_ter`, plus C's `lit=0` rule applied explicitly (`mkmaze.c` `set_levltyp_lit`: lava forces lit=1, else lit=0 — JS `sel_set_ter` treats tlit 0 as nochange). No new imports/edges (same module; `selection_rect_rel` is a hoisted sibling, no TDZ risk); `selection_do_line` Bresenham already verbatim. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn level_tele` → PASS syntax (1 changed js file(s): js/mklev.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify level_tele: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS: scen-tour-Tourist-92100 level_tele@108 → goodpos@131, later step + later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** none new — sibling hell styles use selection ops (already origin-shifted) or have no hand-rolled frame (only hells[7] occurrence of the pattern in `js/mklev.js`); `wiz_map_levltyp` bodies stay deferred (`detect.js`, diagnostic only).
**Next:** Tourist-92100 now blocked on `goodpos` at step 131; re-queue (if at all) follows from the `hidden-proxy queue` refill, not hand-picked. Do not re-pop `level_tele` for it.
## 2026-09-08 — D-2135 `monsters.h` LVL permonst.mr dropped by extractor: JS `data.mr` always 0, so `resist()` never fired and a scroll-of-fire blast killed a werejackal C halves damage on (queue row `trap.c` burnarmor, 1 session)

**C locus:** `include/monsters.h` MON `LVL(lev, mov, ac, mr, aln)` group 4 = permonst.mr (magic-resistance %); werejackal `LVL(2, 12, 7, 10, -7)` → mr 10. `zap.c:6141` `resist`: `resisted = rn2(100 + alev - dlev) < mtmp->data->mr` (SCROLL_CLASS alev 9). Queue owner `burnarmor` is the symptom owner: `js/trap.js:3894` already matches the C `rn2(5)` slot loop arm-for-arm; the deciding branch is upstream in `explode.c:534–553` (resist → half damage → survive vs full damage → `xkilled`).
**JS:** 3 files (`scripts/extract-monsters.py` +5, `js/monsters.js` +4, `js/generated/monsters_data.js` +1 regenerated line), under the 600/10 caps. Density note: the C locus is one table field; the 383-value generated row carries the weight.
**Change:** extractor captures `lm.group(4)` as `mr` (+ fallback 0) and emits `export const mrs = [...]` (225/383 nonzero); `js/monsters.js` imports `mrs` (same leaf edge, no TDZ risk) and `mons()` sets `mr: mrs[mndx]` with C cite. Regenerated via the checked-in extractor: `monsters_data.js` diff is +1 line, no churn. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn burnarmor` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden (0 PASS, 1 moved past → PROGRESS: Ranger-92212 burnarmor@73 → do_statusline2@77, later step + later owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (script: no shared file changed) + full `sessions` 44/44 hand-run after the data change · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** none new — all six `data.mr` readers are pre-existing live call sites fixed by the data (no code change needed at any of them); `commit_pm_fixup`/ERINYS baseline untouched (C permonst is const; adj_erinys never mutates mr).
**Next:** Ranger-92212 now blocked on `do_statusline2` at step 77; re-queue (if at all) follows from the `hidden-proxy queue` refill below, not hand-picked.
## 2026-09-08 — D-2134 `invent.c` hold_another_object artifact-refuse arm: JS printed `drop_fmt` ("Oops! ...") where C prints nothing, leaving a dangling `--More--` (queue row `artifact.c` touch_artifact, 1 session)

**C locus:** `invent.c:1227–1231` `hold_another_object` artifact-refuse arm (`if (!touch_artifact(obj, &gy.youmonst)) { obj_extract_self(obj); dropy(obj); return obj; }` — no pline; `drop_fmt` sounds only in the wasUpolyd arm `:1232–1239` and `drop_it`) + `zap.c:6419–6421` (`hold_another_object(...)` then `u.ublesscnt += rn1(100, 50)`). Queue owner `touch_artifact` is the symptom owner: the evade pline (`artifact.c:966–970`) is byte-identical both sides (JS `artifact.js:1165` already faithful per D-2010); the divergence is the caller's extra trailing message. C step-72/73 screens+RNG pin it: 72 = «You are blasted ... power!--More--» with draws `[rnd(2) next_ident, rn2(1) readobjnam, d(4,10)=25 touch_artifact:953, rn2(2) exercise]`; 73 (space ack) = evade alone with `[rn2(100)=59 makewish]` = `rn1(100,50)` after `hold_another_object` returns.
**JS:** 1 file (`js/invent.js` +3/−1), under the 600/10 caps.
**Change:** `js/invent.js` only — refuse arm returns after `obj_extract_self` with no pline, exact C order, C cite `:1227–1231` in place. No new imports/edges (`hold_drop_msg` kept for `drop_it`); no TDZ risk. No DIAG/FORCE/seed gates; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn touch_artifact` → PASS syntax (1 changed js file(s): js/invent.js) · PASS rule2 (no fs/path/url/node: imports, no DIAG/FORCE/seed gates) · PASS hidden (verify touch_artifact: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS: scen-genesis-Valkyrie-92094 PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed) · VERIFY: PASS. Preflight `verify.mjs --no-cohort` was green before edits.
**Named:** `dropy` on the refuse arm + wasUpolyd arm + crysknife restore stay named (`turns.md` hold_another_object section; no corpus session reaches them this iter); makewish `wish_history_add`/`wishcmdassist` untouched.
**Next:** none — the session PASSES fully. Do not re-pop `touch_artifact` for it.
