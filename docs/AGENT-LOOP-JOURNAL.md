# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
## 2026-09-09 — Audit 5cd7c741..44843418 (reviews 1179–1184: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 5cd7c741 (D-2213..D-2218), oldest-first, one file per SHA written as each finished. Corpus claims re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: use_grapple PROGRESS (Priest-92041 146→weldmsg@158); givit PROGRESS (Rogue-91119 70→nh_timeout@108); dosearch 1 PASS (Archeologist-92012) + 2 moved (Samurai-92017 54→m_dowear_type@142, Wizard-92127 31→getpos@72) + 1 parked residual (92023@65 identical toplines) — zero worse; populate_maze/dosounds/findgd/u_entered_shop vacuous-0-confirmed exactly as each D-log labeled (no PASS claimed). C spot-checks held throughout (all 7 whip/grapple surface() sites vs apply.c:3051/:3183/:3788/:3795/:3819/:3855/:3857; givit "%s." format eat.c:1082–1095; m_throw envelope mthrowu.c:702–786 + blindinc tail :836–841 incl. something="something" decl.c:45 and vision_clears; populate_maze loops + fallback :1197–1222 + walkfrom :1306 vs :1293–1294 + mazexy gx/gy globals; throne/beehive/vault Soundeffect vols + temple gate :330–331 + halu_gname pray.c:2577–2619 with randrole(TRUE)≡rn2_on_display_rng(SIZE-1) and roles[NUM_ROLES+1] terminator accounting; findgd vault.c:204–232 line-for-line; shkname shknam.c:873–890 with 11+1 shtypes parity + bill_p consumer shk.c:5001). Notes, not C-wrongs: sym.mjs misses mhitm.js AT_SPIT/AT_WEAP const exports (resolved by direct read :235/:242/:688); D-2215/D-2217 "new edge" framings are ALREADY-static per --can (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.29/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (shk Soundeffect/Hallu remainder shipped D-2218; Open head pray.c gifts); Score refreshed from this run.
## 2026-09-09 — Audit 32f374ab..5cd7c741 (reviews 1176–1178: 3 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 3 JS-touching SHAs since 32f374ab (D-2210..D-2212), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: ustatusline PASS (Archeologist-92004, stronger than the D-log's 141→145 PROGRESS — later commits closed the tail); use_misc PASS (Archeologist-92004, same session); dosearch0 PASS (Knight-92130) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (ustatusline branch-for-branch insight.c:3402–3489 + macro reads youprop.h:81/93/96/138/376–377, monst.h:71, you.h:554; C-locus sticks engrave.js:346 over divergent monmove.js:1629; digests mondata.h:71–72; passive_obj AD_FIRE uhitm.c:6156–6162 gate-for-gate incl. steam-vortex carve-out, weapon≡C-resolved obj per preamble :6135–6143; dosearch0 Norep :2022–2024 + SDOOR :2050 + SCORR :2056/:2060, SDOOR correctly keeps recalc). Notes, not C-wrongs: Strangled/flat-OR broadening is the house dual-store idiom (mirrors display.js); AD_CORR passes raw obj vs AD_FIRE's weapon (equivalent — callers never pass Null); passive_obj carried→update_inventory tail still unported (pre-existing, shared with ACCEPTED AD_CORR arm). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.28/turn` (R² 0.79). No Must-fix prepended (3 ACCEPT). Next cluster unchanged (Open head use_grapple); Score refreshed from this run. Also backfilled D-2212 `5cd7c741` Addressed hash.
## 2026-09-09 — Audit 2b917b52..38bd5d4f (reviews 1170–1175: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since 2b917b52 (D-2204..D-2209), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: stairs_description PROGRESS (Healer-92109 208→do_statusline2@241); getpos PASS (Healer-91124); trapeffect_sqky_board PASS (Barbarian-92064); prinv PASS (Caveman-92059); enlightenment PROGRESS (Monk-92213 108→chwepon@109); steal_it PROGRESS (Priest-92097 152→do_statusline2@180) — zero worse throughout, no vacuous checks (every baseline had its session blocked). C spot-checks held throughout (wasinside arm mon.c:3632–3640: outside the accessible gate, link-zeroing N/A under GC; getpos exitgetpos :1151–1157 clear gate; sqky both arms trap.c:1402–1476 byte-exact formats + wake_nearby file-local clone; mergable body :4378–4499 holds zero owornmask reads + merged fixup obj-worn-only :877–913; innategend :474–479 + title :396–401, form arm keeps flags.female; steal_it call-for-call :2173–2278 incl. SITM→SEDU fallthrough :4798–4799). Notes, not C-wrongs: `sym.mjs` misses `export {}` lists (could_seduce false "NOT EXPORTED", resolved by direct read of mhitm.js:685–694); D-2206/D-2209 messages call extended imports "new edges" but `--can` reports both ALREADY (zero new cycle risk). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `64+0.40/turn` (R² 0.79). No Must-fix prepended (6 ACCEPT). Next cluster unchanged (Open head chwepon); Score refreshed from this run.
## 2026-09-09 — Audit ab784680..2b917b52 (reviews 1164–1169: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 6 JS-touching SHAs since ab784680 (D-2198..D-2203), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: legs_in_no_shape PROGRESS (Healer-91123 PASS); float_vs_flight vacuous-0-confirmed as labeled; make_hallucinated PROGRESS (Caveman-92052 PASS); do_statusline2 PROGRESS (2 PASS 92035/92091 + 2 moved, 9 unchanged, 0 worse — byte-identical to the D-log); next_ident PROGRESS (4 PASS + 92187 residual, 0 worse); make_stunned PROGRESS (Archeologist-92226 PASS). C spot-checks held throughout (fpostfx royal-jelly arm-for-arm incl. single-draw rnd(20); mount_steed gate + plural quirk; set_utrap/reset_utrap routing via C trap.c:1044–1057; HALLUC/STUNNED re-arm shapes; Stunned≡HStun youprop.h:81; statusline2 rank table botl.c:781–813 + cond_cmp + windows.c fieldorder read from pinned C; arise arm bones.c:457–478 + tail :505–511 + res_to_mr prop.h:25–27). Notes, not C-wrongs: TIMEOUT-masked wounded gate (house canonical, shared trap.js:2977), debug||wizard idiom, dual botl mirrors, end.js-local give_u_to_m clone (no mondata export exists). Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `83+0.59/turn` (R² 0.76). No Must-fix prepended (6 ACCEPT). Next cluster repointed to Open head stairs_description (make_stunned shipped D-2203, PASS at HEAD); Score refreshed from this run.
## 2026-09-09 — Audit 74944545..ab784680 (reviews 1156–1163: 8 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration over the 8 JS-touching SHAs since d22f6c29 (D-2190..D-2197), oldest-first, one file per SHA written as each finished. Every corpus claim re-measured with `hidden-proxy verify <fn> --base <SHA~1>`: burnarmor/thitm/m_harmless_trap/dosounds vacuous-0-confirmed as labeled; unstuck PROGRESS (Knight-92002 52→one_characteristic@81); accessory_or_armor_on PASS (Tourist-92144); look_here PROGRESS (Wizard-92223 121→130, residual null-owner label as disclosed); minliquid_core PASS (stale row, PASS pre/post as disclosed). C spot-checks held throughout (burnarmor literal, savelife tail, Helmet_on 7 arms + set_bknown/adj_abon, expels tail + um_dist, defended/resists_magm + sqky/rocktrap arms, thitm full body + AD_RBRE=242, dosounds 7 arms draw-for-draw, mdrop_obj/steed/flush). Minor message framings noted (ALREADY-edges called "new", D-2196/D-2194) — substance safe, no C-wrongs. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `92+0.57/turn` (R² 0.77). No Must-fix prepended; Next cluster unchanged (fountain gush/engulfed map-driven).

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-09 — D-2222 `worm.c` flip_worm_segs_vertical/flip_worm_segs_horizontal + `sp_lev.c` flip_level wormno arm (queue row, 0 blocked)

**C locus:** `worm.c:968–976` `flip_worm_segs_vertical` (`curr->wy = (maxy - curr->wy + miny)` over `wtails[worm->wormno]`); `worm.c:979–987` `flip_worm_segs_horizontal` (same for `wx`); sole caller `sp_lev.c:661–666` (`if ispriest / else if isshk / else if wormno { flp&1 → vertical(miny,maxy); flp&2 → horizontal(minx,maxx) }`, after the head's own FlipX/FlipY); `FlipX`/`FlipY` macros `sp_lev.c:516–517` (`(max-min)+min`, identical to the live JS closures).
**JS:** 2 files (`worm.js` +31, `mklev.js` +10/−3 net), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes. Density note: small insertion count, but C is that small (2×8-line walkers + 6-line call arm; D-1577/D-2214 precedent).
**Change:** `js/worm.js` — two exports mirroring C character-for-character (`wtails[worm.wormno | 0]` walk; callers gate wormno, C does not re-check inside). `js/mklev.js` — `else if (mtmp.wormno)` arm in C position/order after priest/shk, extending the pre-existing static `worm.js` edge (`clear_wormdata`/`remove_worm` already imported; `imports.mjs --can mklev.js worm.js flip_worm_segs_vertical` → ALREADY, no new edge, no TDZ read). Doc-comment omits updated on both files.
**Verify:** `node scripts/verify.mjs --fn flip_worm_segs_vertical` → PASS syntax (2 changed js files: js/mklev.js js/worm.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. `/tmp` smoke probe (deleted before finishing): grown chain count 3→3 across both flips, dummy-only + empty walkers no-op, both names load as ESM functions. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** save/rest wsegs (restore path; unchanged); `flip_level` isgd/vault-guard extras + ball/chain + `flip_visuals` (pre-existing named omits, untouched); mondead/dog `wormgone` callers; muse/mhitu `worm_move` callers.
**Next:** do not re-pop `flip_worm_segs_*` (both walkers + the only C caller arm live). A future flipped-worm divergence attributes to save/rest wsegs or the remaining `flip_level` omits. Do not invent a FAIL peel.
## 2026-09-09 — D-2221 `wield.c` weldmsg caller wiring: dowield/doswapweapon/doquiver_core printed "Your weapon" (queue row, 1 blocked)

**C locus:** `wield.c:1061–1074` `weldmsg` (`pline("%s welded to your %s!", Yobjnam2(obj, "are"), hand)` `:1072`, owornmask suppress/restore, bimanual plural) + its three in-file call sites `:383` (dowield), `:473` (doswapweapon), `:572` (doquiver_core newquiver==uwep) — all `weldmsg(uwep)`; dowield/doquiver add `reset_remarm()` (`:384–385`, `:573`).
**JS:** 1 file (`wield.js`, +8/−4 net), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes. Density note: below-40 insertions on an Open row, but C is that small (13-line function + three one-line call arms; D-2218/D-2214 precedent).
**Change:** `js/wield.js` only — the three arms now `await weldmsg(u.uwep)` in C position/order (dowield keeps weldmsg→reset_remarm→unsplit-undo; doquiver keeps `weld_res = !bknown` pre-`welded()` + `weld_res ? 1 : 0`); `reset_remarm` joins the pre-existing static `do_wear.js` edge (`setworn` already imported; hoisted-function, no new module edge, no TDZ read).
**Verify:** `node scripts/verify.mjs --fn weldmsg` → PASS syntax (1 changed js file: js/wield.js) · PASS rule2 · PASS hidden (verify weldmsg: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS; scen-wish-Priest-92041: PASS) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** local `Yobjnam2` (wield.js:1108, xname+`Your`+vtense) vs canonical objnam export (cxname+quan+otense+shk_your) — pre-existing named gap (chwepon D-0435/D-1692), untouched; `dothrow.c:151` throw_obj welded gate (named omit js/dothrow.js:1973), `do_wear.c:2201`, `do.c:724` dodrop caller wiring stay deferred — no corpus block on any.
**Next:** do not re-pop `weldmsg` for the "Your weapon" surface (all wield.c arms live). A future weld-message divergence attributes to its named arm (Yobjnam2 canonical import or the deferred non-wield.c caller). Do not invent a FAIL peel.
## 2026-09-09 — D-2220 `artifact.c` spec_applies defended()/DFLAG1 remainder (queue row, 0 blocked)

**C locus:** `artifact.c:1008–1060` `spec_applies` (DFLAG1 arm `:1024–1025`; `defended` guard `:1036–1037` ahead of the ATTK switch); callee `mondata.c:89–124` `defended` (already live at `js/mondata.js:135`, `sym.mjs` sync). Verified no `artilist.h` row sets SPFX_DFLAG1 (grep: DFLAG1 appears only at `src/artifact.c:1024`; the artilist uses DFLAG2/DCLAS/DMONS) — the arm is unreachable today but live per C.
**JS:** 1 file (`artifact.js`, +5 net — the unported C remainder is 4 lines plus the already-live 33-line `defended` callee it wires in, i.e. C is that small), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. Probe lives in `/tmp/probe_spec.mjs` only (not committed).
**Change:** `js/artifact.js` only + one new import edge (`defended` from `./mondata.js`; `imports.mjs --can artifact.js mondata.js defended` → IN-SCC, hoisted-function SAFE; `mondata.js` already imports `artifact.js` and every cross-use both ways is function-local, so no top-level TDZ read — module-load smoke passes). DFLAG1 in C order with the neighboring DFLAG2-arm idiom: `(((ptr?.mflags1 | 0) & (weap.mtype | 0)) !== 0) ? 1 : 0`. Guard in C position before the switch: `const ad` hoisted above, `if (defended(mtmp, ad)) return 0;`.
**Verify:** `node scripts/verify.mjs --fn spec_applies` → PASS syntax (1 changed js file: js/artifact.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Functional probe 5/5 vs C branch semantics: DFLAG1 match (injected onto the Cleaver record) draws bonus dice 1–3, mismatch gives 0; Mjollnir (AD_ELEC) vs bare hero draws dice, vs blue-dragon-scaled hero gives exactly 0 (guard fires), vs red-scaled hero draws dice (guard correctly narrow). Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** DFLAG2 yours/Upolyd/ulycn hero-as-target arms; `resists_*` artifact/worn grants (zap.js/monsters.js bit subsets); hero Poison/Stone uprops fallback (H/E/sticky-flat convention, shared with the Antimagic arm) — all unchanged.
**Next:** do not re-pop `defended`/`defends`/`spec_applies` for this surface (both arms live). A future artifact-bonus divergence attributes to its named arm (DFLAG2 hero arms, `touch_artifact` D-2010, `artifact_hit` D-1873, `Mb_hit` D-2146). Do not invent a FAIL peel.
## 2026-09-09 — D-2219 `pray.c` pleased pat_on_head case-5 intrinsic gift-grant (queue row, 0 blocked)

**C locus:** `pray.c:1167–1354` `pleased` pat_on_head switch — dispatch `switch (rn2((Luck + 6) >> 1))` `:1167`; case 5 `:1310–1338` (godvoice + `HTelepat/HFast/HStealth & INTRINSIC` ladder, `|= FROMOUTSIDE`, `rn1(3, 2)` first-protection / `ublessed++` repeat, Blind `see_monsters()`, `SetVoice` + `verbalize`); cases 7/8 `:1340–1347` (`gcrownu`), case 6 `:1348–1350` (`give_spell`), default `:1352` (`impossible`).
**JS:** 1 file (`pray.js`, +65/−8), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/pray.js` only, no new cross-module edge (`see_monsters` extends the pre-existing static `display.js` edge — `imports.mjs --can pray.js display.js see_monsters` → ALREADY; `INTRINSIC` extends the pre-existing `const.js` edge; `godvoice`/`Blind()` file-local, `verbalize`/`pline`/`rn1` already imported). `if (pat_on_head) switch (rn2((Luck() + 6) >> 1))` in C order with case 5 live: `godvoice(u.ualign.type)` first, `(u.HTelepat|0) & INTRINSIC` ladder per the `sit.js:374` flat-field idiom, `|= FROMOUTSIDE`, `rn1(3, 2)` / `ublessed++`, Blind-only `see_monsters()` (sync, no await), pre-formatted gift literals (JS `pline(msg)` takes no format args), closing `verbalize('Use it wisely in my name!')`. Cases 0–4/6–8 are explicit `break` with C-line deferral comments (no stub in a live arm: unported arms behave exactly as before plus the now-C-faithful dispatch draw).
**Verify:** `node scripts/verify.mjs --fn pleased` → PASS syntax (1 changed js file: js/pray.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** pat_on_head cases 1–4 (uwep erosion/bless/uncurse repair `:1170–1217`; Castle tune hints `:1218–1245`; golden-glow heal + level restore `:1246–1282`; invent uncurse `:1283–1309`); cases 7/8 `gcrownu` caller wiring (live below, still unwired); case 6 `give_spell` (no JS export); `moves>100000` ublesscnt incr tail; case-5 `SetVoice` pitch (file convention — verbalize shipped).
**Next:** do not re-pop `pleased` for the gift surface (dispatch + case 5 live; remaining arms are independent units). A future prayer-gift divergence attributes to its named arm. Do not invent a FAIL peel.
## 2026-09-09 — D-2218 `shk.c` u_entered_shop Soundeffect/Hallu-shkname remainder (queue row, 0 blocked)

**C locus:** `shk.c:751–917` `u_entered_shop` (robbed Soundeffect `:834`; bill_p poison `:776`; tended `bill_p = &bill[0]` `:785`); `shknam.c:853–897` `shkname` (Hallu arm `:873–890`: count prob!=0 types, `rn2(num)` type ignoring generation probabilities, count names, `rn2(num)` name; `letter()` prefix strip; `noit_mon_nam` fallback + impossible/panic error arms unported — error paths).
**JS:** 2 files (`shk.js` +13/−4 incl. header/doc lines, `shknam.js` +10/−1 ≈ +22/−7), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (list-length checks were read-only `python3 -c` against both trees). Density note: +22 lines — the 166-line C function was already ~95 % ported by D-1080, so completing the remaining C-cited arms is the whole unit (C-is-that-small clause, D-2214 precedent).
**Change:** `js/shk.js` — `Soundeffect(se_mutter_imprecations, 50)` in C order (before the mutter pline; contest C macro is equally empty without SND_LIB, `sndprocs.h:272`, so zero behavior change, strictly more faithful wiring — D-2217 class; se id from `generated/seffects_data.js` per dbridge precedent, extending the pre-existing `sndprocs.js` static edge with a leaf data import); `eshkp.bill_p = -1000` on the `!inhishop` path (matches the established `-1000` sentinel the live `after_shk_move` reset consumes). `js/shknam.js` — `shkname` Hallu arm in C order (`Hallucination()` already imported, same call as sibling `is_izchak`; `!game.program_state?.gameover` per allmain/cmd precedent; prob!=0 break-loop over the local `shtypes` instead of a hardcoded 11; `rn2` already imported). List parity measured: all 11 JS `shk*` name lists match C base lengths exactly (incl. 31-entry `shkhealthfoods` — `"Ga'er"` is double-quoted — and 40-entry `shktools` without platform-`#ifdef` extras, matching the unix contest build), so both `rn2` ranges match C.
**Verify:** `node scripts/verify.mjs --fn u_entered_shop` → PASS syntax (2 changed js files: js/shk.js js/shknam.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `shkname` impossible/panic arms (non-shopkeeper / missing-eshk error paths — JS keeps the `''` fallback); `after_shk_move` occupancy `check_special_room` (still omitted; the poison producer is now live); platform-`#ifdef` `shktools` extras (OVERLAY/WIN32/MACOS9/AMIGA/TOS/OS2/VMS — not in the unix contest build); Soundeffect stays a no-op (no SND_LIB, same as contest C).
**Next:** do not re-pop `u_entered_shop`/`shkname` (welcome envelope complete: deserted latch, Invis, angry/surcharge/robbed, Hello `visitct++`, doorway `dochug`, Soundeffect, Hallu names, bill_p poison). Shop divergences now attribute to their true writer. Do not invent a FAIL peel.
## 2026-09-09 — D-2217 `sounds.c` dosounds feature gates: Soundeffect sites, Is_sanctum, findgd migrating, temple halu_gname (queue row, 0 blocked)

**C locus:** `sounds.c:202–339` `dosounds` (vault `:262–289` Soundeffect volumes 30/30, throne `:45–50` vols 30/100, beehive `:69–79` vols 30/100/100, temple gate `:330–331` `!(Is_astralevel||Is_sanctum)`); `vault.c:204–232` `findgd` (fmon heal `!mx && !gddone → mhp=mhpmax`; migrating unlink + prepend-fmon + `mon_track_clear` + mux/muy + mx=my=0 + `parkguard`); `pray.c:2577–2619` `halu_gname` (`randrole(TRUE)` ≡ `rn2_on_display_rng(SIZE(roles)-1)`, `rn2_on_display_rng(9)` slot table, 14-entry `hallu_gods`, `Moloch`, `'_'` strip); `role.c:719` `randrole` (for_display → display stream).
**JS:** 3 files (`sounds.js` +31/−18, `pray.js` +66/−12, `vault.js` +30/−7 ≈ +127/−37), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (throwaway /tmp hallu/findgd probe reverted).
**Change:** `js/sounds.js` — seven `Soundeffect(se,vol)` sites in C order/volumes (contest C macro is equally empty without SND_LIB, `sndprocs.h:272`, so zero behavior change, strictly more faithful wiring; se ids from `generated/seffects_data.js` per dbridge precedent); temple gate `&& !(Is_astralevel(uz) || Is_sanctum(uz))` (Is_sanctum already live in const.js); `%s` arm `await halu_gname(shralign)`. `js/pray.js` — exported `async halu_gname` in C order (non-Hallu → align_gname; do/while `rn2_on_display_rng(roles.length)` re-rolling null-lgod roles — JS roles[] has no terminator so roles.length ≡ SIZE(roles)-1, Priest lgod:null verified; 14-entry hallu_gods inline; `rn2_on_display_rng(9)` switch incl. both `await impossible` defensive arms per mthrowu precedent; `'_'` strip); doturn awaits it. `js/vault.js` — findgd fmon heal + migrating arm (array splice/unshift per dog.js precedent, `mon_track_clear` via static monmove import, mux/muy, mx=my=0, local parkguard).
**Verify:** `node scripts/verify.mjs --fn dosounds` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. `hidden-proxy verify findgd` → vacuous-hidden note (0 blocked at baseline and working). Extra `verify --full` (vault.js shared-vault behavior) → PASS full 44/44.
**Named:** priest.c:364 pname still on its current path (C `Strcat(pname, halu_gname(...))` — unreached by the ported callers); findgd fmon null-gdlevel tolerance kept as-is (pre-existing, out of scope); Soundeffect stays a no-op (no SND_LIB, same as contest C).
**Next:** do not re-pop dosounds/findgd (all four deferred gates live; envelope complete). Vault-guard divergences now attribute to their true writer. Do not invent a FAIL peel.
## 2026-09-09 — D-2216 `mkmaze.c` populate_maze + makemaz fallback tail were deferred; walkfrom/mazexy had two small C-wrongs (queue row walkfrom family, 0 blocked)

**C locus:** `mkmaze.c:1097-1124` `populate_maze` (rn1/rn2 loop counts, `mazexy` + `mkobj_at`/`mksobj_at`/`makemon`/`mkgold`/`mktrap` in order); `makemaz :1194` (`impossible` then fall through) + `:1197-1222` tail (is_maze_lev=1, `corrmaze=!rn2(3)`, `create_maze(-1,-1,!rn2(5))` vs `(1,1,FALSE)`, wallification, stairs or vibrating square, `place_branch`, `populate_maze`); `walkfrom :1306` (wall cell takes typ only; flags cleared on the start cell `:1293-1294`); `mazexy :1330-1339` (reads `gx.x_maze_max`/`gy.y_maze_max`); active `walkfrom` is the `!MICRO` recursive `:1279-1310` (MICRO is defined only in `amiconf.h`/`pcconf.h`, not the unix build); `okay :297-305` already matched `maze_okay`.
**JS:** 1 file (`mklev.js`, +81/−9), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (bare-process link probe was kept out of tree).
**Change:** `js/mklev.js` only, no new cross-module edge (every callee same-module or already imported: `rn1`/`mkobj_at`/`mksobj_at`/`mkgold`/`makemon`/`mons`/`NO_MM_FLAGS`/`GEM_CLASS`/`RANDOM_CLASS`/`impossible`/`maketrap` — `--can` correctly skipped): new `populate_maze` in C order (gem/object, boulder, minotaur, random monsters, gold loops); new `makemaz_maze_fallback` (`:1197-1222` verbatim: `corrmaze = rn2(3) === 0`, `create_maze(-1,-1,rn2(5)===0)` vs `(1,1,false)`, `wallification(2,2,maze_x_max(),maze_y_max())`, `mkstairs(mm.x,mm.y,1/0,0)`, vibrating square via live `pick_vibrasquare_location` + `svi_inv_pos` + `maketrap`, `place_branch(is_branchlev(),0,0)` per the line-604 idiom, `populate_maze()`), wired into both `makemaz` maze paths with C's `impossible("Couldn't load ...")` on the load-failure path; `walkfrom` wall cell takes typ only; `mazexy` reads `maze_x_max()/maze_y_max()` (behavior-identical today — `game.x_maze_max` is null outside `create_maze` — strictly more faithful inside it).
**Verify:** `node scripts/verify.mjs --fn populate_maze` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (vacuous: no corpus session blocked at HEAD — NOT claimed as a corpus PASS; the row cited 0 blocks so no `--base` re-run is owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** `populate_maze` trap loop — C `mktrap(0, MKTRAP_MAZEFLAG, NULL, NULL)` picks a random type and maze-aware spot internally, but JS has no `mktrap` (only `maketrap(x, y, type)` with an explicit type); faking one would invent RNG (map data.md:883); `dmonsfree` on the load_special path (mon.c); wizard SPLEVTYPE/`getenv` override.
**Next:** do not re-pop the walkfrom family (all four builders + populate + fallback tail live; only the named trap/`dmonsfree`/getenv arms remain). Future maze-level divergences now attribute to their true writer instead of the deferred fallback. Do not invent a FAIL peel.
## 2026-09-09 — D-2215 `mthrowu.c` m_throw hero-hit arm dropped `stop_occupation`, «You stop searching.» lost (queue row dosearch, 1 PASS + 2 moved)

**C locus:** queue owner `detect.c:2099` (`return dosearch0(0) ? ECMD_TIME : ECMD_OK`) is the symptom owner — a topline-literal tie-break (cMsgOwners dosearch/stop_donning/trapmove, none of which print it). True writer is `mthrowu.c` m_throw hero-hit arm `:786` unconditional `stop_occupation()` → `allmain.c:688` `You("stop %s.", occtxt)` with occtxt="searching" (counted-`s` `set_occupation(dosearch,"searching",multi)` per `cmd.c:206–215` / `js/cmd.js:2797`). Same envelope shipped: `:702–785` otyp switch (EGG impossible/petrifier-FALLTHROUGH, pie/venom `thitu(8,0)`, default elf-bow/elven/bigmonst/acid-venom arms), poisoned `:745–754`, can_blnd messages `:755–778` (`mondata.c:305–359`), EGG make_stoned `:779–785`, blindinc tail `:836–841` (`ucreamed` + `make_blinded(BlindedTimeout+inc)` + vision-clears).
**JS:** 2 files (`mthrowu.js` +129/−22, `uhitm.js` one-word export), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/mthrowu.js` — full `:702–786` envelope in C order (EGG impossible/petrifier-FALLTHROUGH via live `touch_petrifies`; pie/venom `thitu(8,0)`; default elf `oc_skill==-P_BOW`/ELVEN_BOW/ELVEN_ARROW/bigmonst arms + acid-venom half-phys gate; poisoned via live `attrib.js` export with `oldumort` baseline; pie/venom messages via `can_blnd`, newly exported from `js/uhitm.js` (one word) with the C-faithful per-otyp AT_SPIT/AT_WEAP; EGG make_stoned via live `potion.js` export; unconditional `await stop_occupation()`); `blindinc` tail at flight end (`ucreamed` + `make_blinded` via static `./do.js` import + vision-clears). `imports.mjs --can mthrowu.js do.js make_blinded` → SAFE hoisted-function; all other names extend pre-existing static edges.
**Verify:** `node scripts/verify.mjs --fn dosearch` → PASS syntax (2 changed js files) · PASS rule2 · PASS hidden PROGRESS (Samurai-92017 54 → m_dowear_type@142; Archeologist-92012 → PASS; Wizard-92127 31 → getpos@72; Archeologist-92023 still dosearch@65 gremlin --More-- residual, parked mattacku per D-2035-Next) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · full `sessions` 44/44 (extra run: combat-math change, fortress holds) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** can_blnd Blindfolded/ublindf/ucreamed/visor you-gates (per the uhitm subset, map turns.md); `thitu` quan>1 doname; forcehit MT_FLIGHTCHECK (spoteffects park (a), untouched); dmgval defender-null (pre-existing).
**Next:** do not re-pop `dosearch` (0 blocked except the parked gremlin residual). Samurai-92017 now rests at `m_dowear_type`@142 — re-queued as Open this commit; Wizard-92127 at `getpos`@72 left to the rescore (getpos row archived D-2205, do not duplicate). Do not invent a FAIL peel.
## 2026-09-09 — D-2214 `eat.c` givit ACID/STONE arms dropped C's `"%s."` period, «petrified» lost its full stop (queue row givit, 1 session PROGRESS 70→108)

**C locus:** `eat.c:1078–1095` ACID_RES/STONE_RES arms — `You_feel("%s.", Hallucination ? "secure from flashbacks" : "less concerned about being harmed by acid")` and `You_feel("%s.", Hallucination ? "unusually limber" : "less concerned about becoming petrified")`. The period comes from the `"%s."` format, not the argument.
**JS:** 1 file (`eat.js`, 2 lines, 4 chars), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes.
**Change:** `js/eat.js` only — appended `.` to all four ACID/STONE literals to match C `"%s."`. Density note: +2/−2 lines — the 97-line C function was already ~95 % ported, so completing the remaining C-cited punctuation is the whole unit (C-is-that-small clause).
**Verify:** `node scripts/verify.mjs --fn givit` → PASS syntax (1 changed js file: js/eat.js) · PASS rule2 · PASS hidden PROGRESS (scen-wish-Rogue-91119 step 70 → nh_timeout@108, strictly later step, different owner) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (no shared file changed per runner) · VERIFY: PASS. Final verify ran after the last js/ edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** unchanged (debugpline only; `should_givit`/`temp_givit`/`incr_itimeout` wiring already live).
**Next:** do not re-pop `givit` for Rogue-91119 (0 blocked there). It now rests at `nh_timeout`@108 — leave to the rescore queue; do not invent a FAIL peel.
