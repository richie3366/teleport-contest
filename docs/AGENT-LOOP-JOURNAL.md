# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.
The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-20 — D-2626 `invent.c` sortloot_cmp whole-body port (class/subclass/disco, invlet, loot-name, BUCX/grease/erosion/erodeproof/enchant tail)

**C locus:** ``nethack-c/upstream/src/invent.c:403–547`` (sortloot_cmp, staticfn); caller ``:634`` (sortloot qsort) + dead ``#if 0`` 3.6.0-revamp direct call ``:671``.
**JS:** ``js/invent.js`` ``export function sortloot_cmp`` (``:2275``) + file-local ``greatest_erosion`` (``:2257``, obj.h macro mirror — dig/lock/u_init/weapon precedent, no new import edge) + ``let sortlootmode`` (``:2265``); ``sortloot`` rewired (``:2442``), ~50 inline lines deleted. No new cross-module import (all callees in-file: inuse_classify/loot_classify/invletter_value/loot_xname).
**Change:** exported ``sortloot_cmp(sli1, sli2)`` in C order with per-arm ``:line`` cites — INUSE classify-once + bigger-first + indx tiebreak (``:412–428``); PACK|INVLET class gate (``:430–432``), loot_classify-once + orderclass/subclass/disco asc (``:435–468``); invlet letter (``:471–476``); LOOT==0 → tiebreak (``:478–479``); format-once name cache + A-Z-fold compare (``:481–499``); BUCX 3/2/1/0 bigger-first (``:501–504``); grease desc (``:506–510``); greatest_erosion asc, bigger-is-WORSE (``:512–516``); erodeproof 1/0 desc (``:517–523``); enchant unknown→-1000 desc under oc_uses_known && !=FOOD_CLASS (``:525–539``, obj1-gated per C); indx tiebreak (``:544–546``). ``sortloot`` sets module ``sortlootmode`` (= C gs.sortlootmode ``decl.h:873``) around ``items.sort`` and resets to 0 after (``:634–636``); post-sort str free is GC (``:638–640``).
**Verify:** ``node scripts/verify.mjs --fn sortloot_cmp`` → VERIFY: PASS (syntax 1 file js/invent.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK, same under --reach-all; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). No committed unit test: repo has no tests/ layout (D-2606 precedent); fortress gates cover.
**Named:** ``dupstr`` (identity — JS strings are immutable values, the loot_xname result is already an owned copy); ``maybereleaseobuf`` (no-op — no static obuf; loot_xname builds fresh per call); ``unsortloot`` (separate C fn — GC, own coverage row if emitted).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2625 `cmd.c` readchar_core whole-body port (fuzzer/queue/in_doagain/nh_poskey, NR_OF_EOFS, EOF-hangup, ALTMETA, click) + hangup/click_to_cmd/readchar/readchar_poskey

**C locus:** ``nethack-c/upstream/src/cmd.c:5213–5272`` (readchar_core, staticfn) + ``:5159–5181`` (hangup) + ``:4905–4913`` (click_to_cmd) + ``:5275–5295`` (readchar, readchar_poskey wrappers); callers ``:5282`` (readchar) and ``:5293`` (readchar_poskey).
**JS:** ``js/cmd.js`` ``export async function readchar_core/readchar/readchar_poskey`` + ``export function hangup/click_to_cmd`` + file-local ``nh_poskey_read``/``readchar_queue_peek`` (~190 lines); stale named-omit comments in ``js/invent.js:8661``/``js/lock.js:559`` now point at the live export; map section updated.
**Change:** ported the whole C body in C order, async only because pgetchar/nhgetch await input — fuzzer arm ``:5217–5220`` via live ``randomkey()``, still landing on the ``input_state=otherInp`` tail; ``readchar_queue`` ``:153`` as string+cursor (upstream has no writer — always empty; ``charCodeAt||0`` mirrors the NUL terminator); ``in_doagain→pgetchar`` ``:5223–5224``, else ``nh_poskey→nhgetch`` (wintty.c tty_nh_poskey is ``i = tty_nhgetch()``; mouse coords have no key-stream source — clicks arrive via cmdq/clicklook_cc — so pos passes through); NR_OF_EOFS=20 ``:5228–5241`` (``clearerr`` omitted — no stdio, C says omit if undefined); EOF→``hangup(0)``+ESC ``:5243–5247``; ALTMETA ``:5248–5260`` (live ``iflags.altmeta``, ``input_state!==otherInp``, queue-first second read, EOF/0→ESC, else ``|=0x80``); click arm ``:5261–5265`` (clicklook_cc={-1,-1} then ``click_to_cmd``). ``hangup`` in C order (exiting→in_moveloop=0, done_hup++, defer while in_moveloop worth saving, else live ``end_of_input``). ``click_to_cmd`` stamps clicklook_cc + queues ``game.Cmd.mousebtn[mod-1]`` (run+tab shape matches cmdq_add_ec's live shapes).
**Verify:** ``node scripts/verify.mjs --fn readchar_core`` → VERIFY: PASS (syntax 3 files js/cmd.js+js/invent.js+js/lock.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). /tmp scratch probe 26/26 on the real code (ordinary/poskey/in_doagain keys; ALTMETA M-c + ESC-ESC + 0→ESC with no click side effect; no-altmeta ESC with leftover queued; click stamp+queue via bound button; EOF×21 drain→hangup→ESC+gameover+done_hup; hangup defer + exiting paths; fuzzer arm resets input_state). No committed unit test: repo has no tests/ layout (D-2606 precedent); exports are fortress-covered.
**Named:** ``nhwindows_hangup`` (windowport teardown — no JS windows layer, same class as end_of_input's omits); ``bind_mousebtn`` (cmd.c:2624 — no JS port, click queue arm inert); ``parse``/``get_count``/``getpos``/``getdir`` are ALTMETA-comment context only, not calls.
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2624 `trap.c` trapeffect_hole whole-body restart (impossible arms, tt local, count_wsegs, forcetrap/yank messages, level_telep tail)

**C locus:** ``nethack-c/upstream/src/trap.c:2013–2067`` (trapeffect_hole, staticfn); callers ``:2964`` (trapeffect_selector HOLE/TRAPDOOR arm) and ``uhitm.c:1857`` (comment only, no call).
**JS:** ``js/trap.js:4046`` ``async function trapeffect_hole`` (file-local, staticfn like C) + map line; header doc updated (prior named omissions resolved).
**Change:** restarted the export in C order with per-arm ``:line`` cites — hero ``:2018–2024`` (seetrap + ``await impossible('dotrap: %ss cannot exist on this level.', trapname(trap.ttyp, true))`` + Finished; else ``fall_through(true, TOOKPLUNGE)``); monster locals ``:2026–2031`` (``tt`` first, then mptr/in_sight/forcetrap/Sokoban/inescapable); bad-level ``:2033–2036`` (``await impossible('mintrap: …', trapname(tt, true))`` — ``%s`` substitution is the live display.js impossible envelope); big/floater arm ``:2037–2061`` with ``count_wsegs(mtmp) > 5`` (already top-level imported from worm.js), forcetrap ``:2042–2055`` seetrap-then-``pline_mon`` TRAPDOOR/HOLE pair in C message order, inescapable ``:2056–2061`` yank ``pline_mon``-then-seetrap with the ``else return Finished`` restored; tail ``:2062`` ``return trapeffect_level_telep(mtmp, trap, trflags)`` (file-local, monster path identical — recomputes the same in_sight/forcetrap). No new imports; no cycle (imports.mjs untouched).
**Verify:** ``node scripts/verify.mjs --fn trapeffect_hole`` → VERIFY: PASS (syntax 1 file js/trap.js; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). No committed unit test: repo has no tests/ layout (D-2606 precedent); the function is file-local like its C staticfn and is covered by the fortress gates.
**Named:** none new — every arm and callee is live (seetrap/impossible/pline_mon display.js, trapname file-local, fall_through file-local, count_wsegs worm.js, grounded/monsters.js, Monnam/mon_nam do_name.js, trapeffect_level_telep file-local).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2623 `quest.c` chat_with_leader whole-body restart (cheater, got_thanks/questart, banished gate, livelogs)

**C locus:** ``nethack-c/upstream/src/quest.c:282–368`` (chat_with_leader, staticfn); callers ``:390`` (leader_speaks, peaceful ``!pissed_off`` gate) and ``:476`` (quest_chat, leader ``m_id`` arm + setmangry tail).
**JS:** ``js/quest.js:365`` ``async function chat_with_leader`` (file-local, staticfn like C) + 3 import lines; header + finish_quest docs updated (Rules 1/3 now call finish_quest).
**Change:** restarted the export in C order with per-arm ``:line`` cites — Rule 0 cheater (``:287–289``, ``u.uhave?.questart`` per zap.js/trap.js precedent, ``qs.met_nemesis``/``qs.cheater`` new quest_status keys); got_thanks Rule 1 ``finish_quest(null)`` (``:297``) / Rule 2 ``qt_pager('posthanks')`` (``:301``); Rule 3 invent scan over the JS array for the first ``is_quest_artifact`` hit, null when absent (``:305–312``, gi.invent/nobj shape noted) + ``finish_quest(otmp)``; Rule 4 encourage (``:315–316``); Rule 5 met_leader/not_ready (``:322–327``), qstart portal gate (``:332``), badlevel (``:334–337``), banished arm re-gated on ``!pissed_off`` (``:339`` — an already-pissed leader now does nothing, per C) with ``com_pager('banished')`` + expulsion + ``livelog_printf(LL_ACHIEVE, '%s has expelled…', noit_mon_nam(mtmp))`` (``:340–347``), badalign (``:349–353``), assignquest + got_quest + granted-access livelog (``:354–365``). ``is_pure`` stays awaited (async wizard-yn variant); ``purity`` is ``let`` for the C assignment-in-condition (``:338``). Imports: ``livelog_printf`` (pline.js — imports.mjs SAFE, no cycle), ``noit_mon_nam`` + ``LL_ACHIEVE`` joined existing do_name.js/const.js edges.
**Verify:** ``node scripts/verify.mjs --fn chat_with_leader`` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). /tmp scratch probe 10/10 on the real code via quest_chat (hostile/pissed guards silent; cheater latched only when !met_nemesis; posthanks + encourage miss no-ops; questart scan-miss reaches finish_quest; first-meet latches met_leader/not_ready=0 with the off-level portal gate returning before badlevel). No committed unit test: repo has no tests/ layout (D-2606 precedent); the function is file-local like its C staticfn.
**Named:** posthanks/banished pager texts (questpgr extractor's job — calls are live and still burn the C nhl_init shuffle on a miss, so RNG matches C either way); nemesis/guardian chat + setmangry tail (pre-existing map lines, untouched).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2622 `mkobj.c` obj_meld whole-body restart (holder guards, cansee/newsym, maybe_unhide_at, impossible)

**C locus:** ``nethack-c/upstream/src/mkobj.c:3768–3814`` (obj_meld); callers ``do.c:312`` (flooreffects, ``(void)`` discard) and ``mon.c:727`` (make_corpse pudding→GLOB loop, ``obj =`` assign).
**JS:** ``js/mkobj.js:2821`` ``export async function obj_meld`` + 1-line ``await`` at each caller (``js/do.js:861``, ``js/mhitm.js:2943`` — both already async).
**Change:** restarted the export in C order with per-arm ``:line`` cites — ``result = null`` (``:3771``); holder-level ``if (p1 && p2)`` guard (``:3774`` — the ``struct obj **``, not the pointees); pointee ``otmp1 && otmp2 && !==`` gate (``:3777``, null/same falls through returning null); ``:3789`` floor+free veto + ``:3791–3792`` heavier-or-``rn2(2)`` tiebreak; absorbed-away floor coords (``:3793–3794`` / ``:3797–3798``); ``if (ox)`` tail with the ``cansee`` gate (``:3803–3805``) and awaited ``maybe_unhide_at`` (``:3809``); ``else impossible(...)`` misuse arm (``:3811–3813``). The ``:3779–3788`` FIXME (mid-drop free glob, shore/pool) is ported as-is — C handles neither. Async: ``maybe_unhide_at`` (monmove home) and ``impossible`` (display home) are async exports, both already top-level imported in mkobj.js — no new cross-module edge, no imports.mjs change.
**Verify:** ``node scripts/verify.mjs --fn obj_meld`` → VERIFY: PASS (syntax 3 files; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full 44/44 passing — shared file changed). No committed unit test: repo has no tests/ layout (D-2606 precedent); both live callers are async and covered by the fortress.
**Named:** none new — every arm and callee is live and cited (cansee vision.js, newsym/impossible display.js, maybe_unhide_at monmove.js, obj_absorb file-local, rn2 rng.js).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2621 `invent.c` loot_xname whole-body port (sort-key suppress/restore + grouping suffixes)

**C locus:** ``nethack-c/upstream/src/invent.c:308–387`` (loot_xname, staticfn); callers ``:490``/``:496`` (sortloot_cmp SORTLOOT_LOOT name path); ``flag.h:30`` (``wizard`` ≡ flags.debug).
**JS:** ``js/invent.js:1124`` ``loot_xname`` (file-local, staticfn like C) + 1 import line (POT_WATER).
**Change:** ported the whole body in C order with per-arm ``:line`` cites — save odiluted/blessed/cursed/spe/owt + oname + flags.debug (``:320–325``); potion dilute + water holy/unholy suppress (``:328–332``); towel spe=0 (``:335–336``); glob owt=20 fresh-glob weight (``:339–340``); oname suppress except artifacts (``:342–343``); wizard-debug off + something_worth_saving=0 (``:345–350``); cxname_singular (``:352``); debug restore (``:354–357``); potion restore (``:359–363``); towel spe-restore-then wet-x/moist-y/dry-z (``:364–370``, restore before is_wet_towel per C order); glob owt-restore-then size a/b/c/d (``:371–382``, restore before suffix per C order); oname restore (``:383–384``). Callees: has_oname/ONAME (live const.js, already imported), cxname_singular (live objnam.js, already imported), is_wet_towel (live weapon.js, already imported), POT_WATER (added to the existing objects.js import — imports.mjs ALREADY, no new edge), TOWEL via file-local OTYP_TOWEL index; C Strcat into the cxname buffer is ``+=`` (JS strings are values); ``ONAME(obj)=0`` is ``oextra.oname=null`` (do_name.js:1408 idiom).
**Verify:** ``node scripts/verify.mjs --fn loot_xname`` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). No committed unit test: repo has no tests/ layout (D-2606 precedent); loot_xname is module-local like its C staticfn.
**Named:** sortloot_cmp body (own queued Open row — BUCX/grease/erosion tail, strcmpi shape, dupstr/maybereleaseobuf buffer idiom all live in that caller, not here); ``hack.h:836`` sortloot_item.str field (plain JS ``sli.str``, already wired in sortloot).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2620 `cmd.c` act_on_act whole-body port (menu-action queueing, cmdq DIR/USERINPUT, doclicklook)

**C locus:** ``nethack-c/upstream/src/cmd.c:4658–4838`` (act_on_act, staticfn) + ``:294–311`` (cmdq_add_dir) + ``:316–331`` (cmdq_add_userinput) + ``:5381–5392`` (doclicklook, same-file staticfn) + ``:2070–2078`` (move_funcs table) + ``:4379–4418`` (menucmd enum); callers ``:4880`` (there_cmd_menu K==1 fast path) + ``:4892`` (menu pick).
**JS:** ``js/cmd.js:225`` ``cmdq_add_dir`` + ``js/cmd.js:238`` ``cmdq_add_userinput`` (both exported, C globals) + ``js/cmd.js:1089`` ``doclicklook`` + ``js/cmd.js:1100`` ``move_funcs_walk`` + ``js/cmd.js:1122`` ``act_on_act`` (file-local, staticfn like C); MCMD/otyp consts ``js/cmd.js:958–998``; 5 import lines extended.
**Change:** ported the whole body in C order with per-arm ``:line`` cites — sgn clamp keeping throw/travel/look deltas raw (``:4666–4677``, live eat.js ``sgn`` ≡ ``hacklib.c:650``); TRAVEL travelcc+u.tx/ty stamp (``:4680–4688``, game.iflags/game.u precedent); THROW ec+userinput+dir (``:4689–4693``); OPEN/LOCK (skeleton→pick→card chain, ``:4694–4710``)/UNTRAP/KICK/CLOSE (``:4711–4722``); SEARCH (``:4723``); LOOK_TRAP ec+dir (``:4726``); MOVE_DIR/ATTACK_NEXT2U via ``move_funcs_walk`` (``:4734``/``:4756``, C ``[MV_WALK]`` column in xytodir order); RIDE/REMOVE_SADDLE (m-prefix reqmenu, ``:4738–4748``)/APPLY_SADDLE (``:4749``); TALK/NAME ('m', ``:4760–4768``); QUAFF/DIP (userinput, ``:4769–4777``)/SIT/UP/DOWN/DISMOUNT/MONABILITY (``:4778–4792``); PICKUP/LOOT/TIP/EAT/DROP/INVENTORY/REST/LOOK_HERE with C 'y' keys (``:4793–4818``); LOOK_AT clicklook_cc stamp+ec (``:4819``); UNTRAP_HERE dir(0,0,1) (``:4824``); OFFER ec+userinput (``:4828``); CAST_SPELL (``:4832``). New live ``cmdq_add_dir``/``cmdq_add_userinput`` (exact C append shape; DIR nodes read by lock.js getdir, USERINPUT by getline.js/invent.js — shapes verified by /tmp probe); new file-local ``doclicklook`` (isok guard, ``context.move=0``); MCMD enum completed + ``SKELETON_KEY/LOCK_PICK/CREDIT_CARD/SADDLE`` otyp consts. dountrap/dodip/dosit/doride/domonability/dosacrifice use the file's dynamic-import idiom; dotalk/docallcmd/sgn/cmdq_add_key join existing edges (imports.mjs ALREADY, sounds.js SAFE hoisted).
**Verify:** ``node scripts/verify.mjs --fn act_on_act`` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden ``no corpus session is blocked`` — coverage row, no cited blocks; reach smoke 24/24 PASS 0 regressed → REACH-OK; green 2/2; strict seed8000+seed0900; cohort 7/7; full skipped — no shared file per gate). /tmp scratch probe 8/8 on the real code (sgn values, DIR/USERINPUT node shapes, getdir consuming a queued DIR east → 'l', dotalk/docallcmd/cmdq_add_key live). No committed unit test: repo has no tests/ layout (D-2606 precedent); act_on_act/doclicklook are module-local like their C staticfns.
**Named:** ``doidtrap`` (``pager.c:2336`` — MCMD_LOOK_TRAP arm queues a lazy import from its 1:1 home, resolves when it lands); ``auto_describe`` (``getpos.c:640`` — same treatment inside doclicklook); C ``:4880``/``:4892`` caller paths (next2u/far builders, own rows); ``there_cmd_menu_far`` is a sibling, not a callee.
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2619 `botl.c` get_hilite whole-body port (rule chain, best-fit trackers, textmatch/noneoftheabove, critical-hp)

**C locus:** ``nethack-c/upstream/src/botl.c:2363–2570`` (get_hilite, staticfn) + ``:2333–2344`` (noneoftheabove, same-file staticfn, only caller :2545–2546) + ``:673``/``:2572`` (has_hilite macro + undef) + ``pray.c:114–157`` (critically_low_hp); callers ``:1597`` (eval_notify_windowport_field) and ``:2117`` (exp_percent_changing).
**JS:** ``js/botl.js:390`` ``noneoftheabove`` + ``js/botl.js:408`` ``get_hilite`` (file-local, staticfn like C; rule nodes use C field names behavior/rel/value/textmatch/coloridx/next); ``fuzzymatch``/``LARGEST_INT``/``critically_low_hp`` imports; ``js/pray.js:299`` export-only change.
**Change:** ported the whole body in C order with per-arm ``:line`` cites — out-of-range early return without touching colorptr (``:2374–2375``); has_hilite macro inlined (``:2377``); best-fit trackers (``:2380–2388``, LARGEST_INT live from const.js, LONG_MAX as MAX_SAFE_INTEGER with cite); crit_hp/updown-changed/perc_or_abs precedence gates (``:2400/2405/2409``); percentage EQ/best-fit LT/LE/GT/GE (``:2414–2443``); updown down/up/changed with up-beats-changed (``:2449–2457``); absolute int/long twins (``:2466–2531``); textmatch with BL_TITLE ``strlen(plname)+5-1`` skip (``:2535–2538``, game.plname per botl.js:1020 precedent) + fuzzymatch + exactmatch + Upolyd+noneoftheabove (``:2539–2547``); always (``:2551``); critical-hp with flag reset (``:2555–2558``); none/default (``:2561–2564``); colorptr write + return (``:2568–2569``); new file-local ``noneoftheabove`` (``:2338–2343``, ignore sets ``'" -_'``/``'"()'``/``'" -_()'``). Callees: ``fuzzymatch`` joined the existing hacklib.js import (ALREADY edge); ``critically_low_hp`` is the C-exact pray.js clone, now exported (``js/pray.js:299``) and imported — no clone #2, lazy call inside the same 98-module SCC, no top-level read. ``Upolyd(game.u)`` per const.js:3184.
**Verify:** ``node scripts/verify.mjs --fn get_hilite`` → VERIFY: PASS (syntax 2 files; rule2 clean; hidden ``no corpus session is blocked``; reach fixed 24-session smoke spread 24 PASS 0 regressed → REACH-OK; green 2/2; strict 2/2; cohort 7/7; full skipped — no shared file changed). /tmp scratch probe (11 cases through the wired caller: null thresholds, ALWAYS, UPDOWN down/changed, PERCENTAGE EQ + LT best-fit, ABSOLUTE int/long EQ, TEXTMATCH fuzzy title, noneoftheabove poly-title, CRITICALHP + ALWAYS-skip) 11/11 on the real code. No committed unit test: repo has no tests/ layout (D-2616 precedent).
**Named:** ``exp_percent_changing`` caller (``botl.c:2117``); ``hilite_reset_needed``/``status_update`` stays named (windowport registry); rule-node producers (threshold chains are null until the status-hilite option parser lands — gate reads false, returns null + NO_COLOR).
**Next:** pop the next Open — coverage row.
## 2026-09-20 — Audit 30ca950e..7e80d890 (reviews 1569–1577: 8 ACCEPT, 1 WITH-DEBT) + cadence 44/44

**Scope:** every `js/`-touching SHA since review 1568 (D-2610 leading-space fix + D-2611…D-2618 whole-body ports), one SHA at a time, each re-measured with `hidden-proxy verify <fn> --base HASH~1 --reach-all` + `csym`/`sym` + diff grep + Rule #2.
**Findings:** 8 ACCEPT (1569–1575, 1577). 1576 carry_count ACCEPT-WITH-DEBT: zero-lift `if (game.invent || umoney)` takes the wrong message branch for a naked-and-broke hero (empty array truthy vs C NULL `gi.invent`); return/state identical, message-only — review-debt, unqueued, one-line fix `(game.invent?.length || umoney)`. Notable confirmations: 1574 throw_obj real reach set (22 baseline-PASS reach it, 22 PASS 0 regressed — mid-iter PM_HUMAN ReferenceError fully healed); 1575 weapon_type retained clone proven behaviorally C-exact (all non-P_NONE skills live in W/T/G macros, P_NONE=0); 1577 noadjust unsplitobj-vs-merged predates this window (untouched, not queued).
**Cadence:** full `sessions` 44/44 (seed0107 back to 98/98 — D-2610 breach healed); Scr 11,405/11,405, RNG 792,838/792,838, speed `58+0.34/turn` (R² 0.79). Held-out 11/44, 5,972 pts, RNG 26.7 %, screens 53.0 % (+196 pts / +1.7 pp screens vs last audit). Corpus 497/540 PASS (92.0 %) excl. 13 env-only — +0/−0 in the D-2610…D-2618 window.
**Next:** pop the live Open — coverage head (queue refilled to ~12 below).
## 2026-09-20 — D-2618 `invent.c` doorganize_core whole-body restart (split detect, floating truncate, C letter(), Your pack-full, nobj head-link)

**C locus:** `nethack-c/upstream/src/invent.c:5068–5286` (`doorganize_core`); `hacklib.c:69–73` (`letter`); `hack.c:4496` (`inv_cnt`); `mkobj.c:2596` (`extract_nobj`); `invent.c:739` (`reorder_invent`); `invent.c:1627` (`compactify`); `cmd.c:5471` (`yn_function` 4th arg TRUE = addcmdq).
**JS:** `js/invent.js` `is_c_letter` (new file-local) + restarted `doorganize_core`; `Your` added to the existing `display.js` import (no new module edge).
**Change:** restarted the export in C order with per-arm `:line` cites — first-predecessor split detect (`:5092–5096`); full `lets[]` layout + `:5110` truncate via `invlet_constant()`/`inv_cnt(false)` before blanking, with past-truncation blank writes dropped (past the C NUL); `quitchars`/`QUITCHARS` unchanged (verified identical `" \\r\\n\\033"`); new file-local `is_c_letter` (exact `hacklib.c:69` range); `Your('pack is too full.')` via live `Your` (same `display.js` edge); `obj.nobj`/`bumped.nobj` linked onto the head before `unshift` (C `:5262–5269`, `:5270–5277`).
**Verify:** `node scripts/verify.mjs --fn doorganize_core` → VERIFY: PASS (syntax 1 file; rule2 clean; hidden `no corpus session blocked on it`; reach fixed 24-session smoke spread 24 PASS 0 regressed → REACH-OK; green 2/2; strict 2/2; cohort 7/7; full skipped — no shared file changed).
**Named:** none new — every arm and callee is live or file-local and cited in the map (`docs/c-js-map/turns.md` `doorganize` line): `mergable`/`merged`/`unsplitobj`/`clear_splitobjs` live (mkobj.js); `inv_cnt` live (steal.js); `assigninvlet` live (u_init.js); `prinv`/`Your` live (display.js); `yn_function` live (getline.js, 4th-arg default `addcmdq=true` = C TRUE); `display_used_invlets` live (D-1591); `compactify_invlets`, `reorder_invent_adjust`, `extract_invent`, `prinv_adjust`, `invent_merged`, `names_ok_for_adjust_merge` pre-existing file-locals; `eos()` as concat.
**Next:** pop the next Open — coverage row.
## 2026-09-20 — D-2617 `pickup.c` carry_count whole-body restart + delta_cwt port (container arms, gold re-weigh, You/There messages)

**C locus:** ``nethack-c/upstream/src/pickup.c:1570–1701`` (carry_count, staticfn) + ``:1544–1568`` (delta_cwt, staticfn callee); callers ``:1736`` (lift_object, container through) and ``:1839`` (pickup_object scare arm, NULL container).
**JS:** ``js/pickup.js:1162`` ``function delta_cwt(container, obj)``; ``js/pickup.js:1198`` ``async function carry_count(obj, container, count, telekinesis, wts)``.
**Change:** restarted the export in C order with per-arm ``:line`` cites — ``adjust_wt = container && carried(container)`` (``:1576``, live eat.js export of the ``obj.h:332`` macro); provisional weigh + ``delta_cwt`` subtract (``:1593–1595``) + merged-gold correction (``:1596–1598``); early full-lift return (``:1606–1607``); gold plain-capacity vs carried-container 100-coin re-weigh loop with ``GOLD_WT(umoney+qq)`` + per-step ``delta_cwt`` (``:1610–1635``); stack loop with per-quan ``weight()`` + delta (``:1636–1654``); single unliftable (``:1655–1656``); container ``in <the(xname)>``/``carry`` vs floor ``lying here``/``acquire``|``lift`` message setup (``:1661–1675``); partial ``You('can only %s %s of the %s %s.')`` (``:1677–1682``, live display.js export — review-1554 file convention); zero-lift ``There('%s %s %s, but %s%s%s%s.', otense(obj,'are'), …)`` (``:1685–1697``, live objnam/display exports); ``money_cnt(game.invent)`` (live shk.js export, first-stack like C). New file-local ``delta_cwt`` (``js/pickup.js:1162``) — face value off-BoH (``:1549–1550``), unlink/weigh/relink over ``cobj``/``nobj`` for BoH (``:1560–1564``), C ``panic`` as a loud throw with the C message (botl.js convention). Retired the now-unused ``money_cnt_invent`` sum; ``otense_pickup`` stays (scare-arm user).
**Verify:** ``node scripts/verify.mjs --fn carry_count`` → VERIFY: PASS — syntax 1 file · Rule #2 clean · hidden ``no corpus session is blocked`` (coverage row, 0 cited blocks) · REACH-OK (RNG-0 function: fixed 24-session smoke spread 24/24 PASS, 0 regressed) · green 2/2 + strict · cohort 7/7; full sessions skipped (non-shared file per gate).
**Named:** none new — every arm and callee is live, inlined-literal (GOLD_WT/GOLD_CAPACITY locals, ``carried`` macro via the eat.js export), or ported in this commit.
**Next:** doorganize_core (`invent.c:5068–5286`) is the next Open — coverage row.
## 2026-09-20 — D-2616 `invent.c` loot_classify whole-body port (class order, armor/weapon/tool/food/gem subclasses, discovery status)

**C locus:** ``nethack-c/upstream/src/invent.c:149–305`` (loot_classify); callers ``:436``/``:438`` (sortloot_cmp) and ``o_init.c:587`` (sortloot_descr).
**JS:** ``js/invent.js:2075`` ``export function loot_classify(sort_item, obj)``; callers at ``js/invent.js:2243–2256`` (sortloot comparator).
**Change:** new exported ``loot_classify(sort_item, obj)`` (``js/invent.js:2075``) in C order with per-arm ``:line`` cites — def_srt_order table (`:155`); persistent module-level armcat (`:160`); discovered read before observe (`:164`); observe when !Blind, seen after (`:171–172`); sortpack?inv_order:def_srt_order with VENOM +1 (`:174–180`); armor armcat via oc_skill (`:184–202`, JS stores oc_armcat in oc_skill); weapon skill groups + is_pole (`:204`, is_pole added to the existing wield.js import — ALREADY edge); tool known pseudo-container vs container vs 11 instruments (`:214`); food kinds + globby (`:240`); gem GEMSTONE/GLASS/MINERAL × seen × discovered (`:261–289`); default subclass 1 (`:290–294`); disco unseen 1 / undiscovered 2 / named 3 / discovered-or-undescribable 4 (`:298–302`, OBJ_DESCR via the do_name.js ``objectDescrs[di] || oc_descr`` idiom — objectDescrs added to the existing objects.js import); inuse 0 (`:304`). Wired ``:436``/``:438`` by replacing the inline orderclass with classify-once ``loot_classify`` calls plus the `:446–467` subclass/disco compares (INVLET-gated) in the sortloot comparator.
**Verify:** ``node scripts/verify.mjs --fn loot_classify`` → VERIFY: PASS — syntax 1 file · Rule #2 clean · hidden ``no corpus session is blocked`` (coverage row, 0 cited blocks) · REACH-OK (RNG-0 function: fixed 24-session smoke spread 24/24 PASS, 0 regressed) · green 2/2 + strict · cohort 7/7; full sessions skipped (no shared file per gate). /tmp scratch probe (10 synthetic objects: weapon groups 5/4/2, armor helm 1 vs suit 7, container 1, gem glass/mineral, rock 8, sortpack-on/off orders) matches C by hand-read. No committed unit test: repo has no tests/ layout (D-2605 precedent).
**Named:** ``o_init.c:587`` sortloot_descr ('s' discosort path — review 921 ACCEPT-WITH-DEBT row); sortloot_cmp BUCX/grease/erosion/erodeproof/enchant tail (``:503–541``); sortloot_cmp LOOT-name arms stay as-is.
**Next:** carry_count (`pickup.c:1570–1701`) is the next Open — coverage row.
## 2026-09-20 — D-2615 `dothrow.c` throw_obj whole-body restart in C order (Mjollnir / petrify / welded / wet-towel / multishot extras / unsplit)

**C locus:** ``nethack-c/upstream/src/dothrow.c:87–293`` (throw_obj); callers ``:375`` (dothrow) and ``:582`` (dofire).
**JS:** ``js/dothrow.js:964`` ``export async function throw_obj(obj, shotlimit)``.
**Change:** restarted the export in C order with per-arm ``:line`` cites — ``let res = ECMD_TIME`` (`:93`); objsplit snapshot + ``unsplit_stack()`` closure on every early return (`:94`, `:274–292`, via live ``unsplitobj``); Mjollnir wielded/too-heavy gates (`:122–132`, live ``is_art``/``acurr``/``throws_rocks``); ``You cannot throw…`` via ``You()`` (`:133–137`); ``u_wipe_engr(2)`` (`:138`, kept); petrify arm live (``corpse_xname(obj, null, CXN_PFX_THE)`` + ``makeplural(body_part(HAND))`` + ``instapetrify("throwing <killer_xname> bare-handed")``, falls through per C); ``welded → weldmsg`` (live wield.js export); ``is_wet_towel → dry_a_towel(obj, -1, false)`` (live weapon.js exports); racial switch on ``Race_switch`` with otyp consts incl. HUMAN/DWARF no-op default (`:195–214`); quest-artifact ``++multishot`` (`:216–220`, live quest.js ``is_quest_artifact``); crossbow ``acurrstr() < 16/18`` gate (`:222–226`); volley loop with ``:line`` cites ending ``return unsplit_stack()`` (`:293`). Retired the file-local ``The`` / ``is_quest_artifact`` / ``multishot_class_bonus`` clones for the canonical exports (bodies identical except the clone lacked the C NINJA fallthrough, now live from weapon.js).
**Verify:** ``node scripts/verify.mjs --fn throw_obj`` → VERIFY: PASS — syntax · Rule #2 · hidden ``no corpus session is blocked`` · REACH-OK (22 baseline-PASS sessions reach it, 0 regressed) · green 2/2 + strict · cohort 7/7. First run caught ``PM_HUMAN is not defined`` (case label after dropping it from the monster import; C HUMAN/DWARF arms are no-ops, folded into ``default``) — cohort seed1800 + 9 REACH workers aborted on the ReferenceError; re-run after the one-line fix is fully green.
**Named:** ``weapon_type`` / ``freeinv`` file-local clones retained (live exports differ: weapon.c class gate + ``P_BARE_HANDED_COMBAT`` default; invent freeinv_core/update_inventory + gold-cache D-2139); ``oldslot`` stays the invent-array successor (C `:262` ``obj->nobj`` feeds C ``addinv_before``; JS ``addinv_before_throw`` indexes ``game.invent``); local ``Role_if``/``Race_if``/``P_SKILL`` helpers retained (identical one-liners, pre-existing).
**Next:** e_died (`dbridge.c:402–480`) is the next Open — coverage row.
