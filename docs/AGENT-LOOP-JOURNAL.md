# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-14 — D-2291 `artifact.c` spec_applies DFLAG2 yours/Upolyd/ulycn arms (queue row `artifact.c` artilist DFLAG2)

**C locus:** `artifact.c:1026–1031` (`spec_applies` SPFX_DFLAG2: `(ptr->mflags2 & weap->mtype) || (yours && ((!Upolyd && (gu.urace.selfmask & weap->mtype)) || ((weap->mtype & M2_WERE) && ismnum(u.ulycn))))`); `you.h:554` (`Upolyd` ≡ `u.umonnum != u.umonster`); `monst.h:285` (`ismnum(x)` ≡ `x >= LOW_PM && x < NUMMONS`); `monflag.h:125` (`M2_WERE` 0x4).
**JS:** 1 file, +12/−4 (`js/artifact.js` only: 2 import names + DFLAG2 arm + doc-comment truth), under the 600/10 caps. Density note: small-C exception to the ~40-line floor — the row's entire remaining C gap is ~5 lines (`:1026–1031` tail).
**Change:** the C `||` chain verbatim in C order (mflags2 first, then the yours gate): `!Upolyd(u)` + `(game.urace.selfmask & mtype)` selfmask arm, then `(mtype & M2_WERE) && ismnum(u.ulycn)` were arm with no Upolyd gate, per C short-circuit. `Upolyd`/`ismnum` join the existing `./const.js` import, `M2_WERE` joins the existing `./monsters.js` import — no new module edge (same SCC, call-time use only, no top-level TDZ read, so no `imports.mjs --can` owed); `game.urace?.selfmask ?? 0` matches the `your_race` convention (`js/mklev.js:18530`, `js/monsters.js:479`); `ismnum(u.ulycn)` matches the `allmain.js:130` call shape (JS canonical drops the `< NUMMONS` upper bound — ulycn only ever holds NON_PM or a valid PM, so the bound never binds). `| 0` integer idiom throughout, 1/0 return shape kept.
**Verify:** `node scripts/verify.mjs --fn spec_applies` → PASS syntax (1 changed js file: js/artifact.js) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_dflag2.mjs` (out of tree, deleted): 8/8 through the exported `spec_dbon` → local `spec_applies` path — orc hero unpoly Orcrist applies / human misses / orc poly misses (Upolyd gate) / human lycanthrope Werebane applies unpoly AND poly (no Upolyd gate on the were arm) / human non-lycanthrope misses / monster M2_WERE-flagged applies / monster unflagged misses.
**Named:** none new (resists_* artifact/worn grants and hero Poison/Stone H/E/sticky flats stay in the `spec_applies` doc comment; SPFX_DRLI destroy/ignite stays with the `artifact_hit` Open row).
**Next:** do not re-pop `artifact.c` artilist DFLAG2 yours/Upolyd/ulycn for any arm shipped here. Falsifier: a rescore or fresh `verify spec_applies` showing a session blocked with spec_applies as owner.
## 2026-09-14 — D-2290 `trap.c` steedintrap dart/arrow/landmine/poly caller wiring (queue row steedintrap)

**C locus:** `trap.c:1211` (arrow hero `u.usteed && !rn2(2) && steedintrap(trap, otmp)` before `thitu(8,…)`); `trap.c:1276` (dart hero, same gate before `thitu(7,…)` with the `!rn2(6)` poison roll kept ahead); `trap.c:2528–2597` (landmine hero else-arm: `recursive_mine` static guard, KAABLAMM pline, `steed_mid` capture, steedintrap, `sobj_at(SADDLE)` + `keep_saddle_with_steedcorpse` tail `:2591–2592`, then wounded legs); `trap.c:2453–2525` (`trapeffect_poly_trap` hero verbbuf/iron-shoes/Antimagic/Unchanging/steedintrap+deltrap+polyself + monster iron-shoes/resists_magm/newcham arms); `hack.c:1817–1829` (`u_locomotion` Levitation→float / Flying→fly / `locomotion(data, def)`).
**JS:** 1 file, +155/−34 (`js/trap.js` only), under the 600/10 caps. Density: ~155 lines of C-faithful JS for ~110 row-named C lines (dart/arrow gates + landmine guard + full poly hero/monster + selector + `u_locomotion_verb`).
**Change:** dart/arrow hero arms gate `u.usteed && !rn2(2) && await steedintrap(trap, otmp)` ahead of `thitu` in C order (t_missile → poison → dmgval → gate → thitu/place), steed-hit arm a bare `;` with the otmp-consumed note, miss arm re-indented under `else` (unmounted: `u.usteed` short-circuits before `rn2(2)` — zero RNG delta); new file-local `trapeffect_poly_trap` in C position (before landmine, matching C `:2453` before `:2528`) with the hero verbbuf triple (`trigger` / `lead <x_monnam> onto` / `<u_locomotion_verb('step')> onto`), iron-shoes `poly_obj` + `update_inventory` + `prinv`, Antimagic/Unchanging `shieldeff` + `You_feel`, else steedintrap + deltrap + newsym + `polyself(POLY_NOFLAGS)`, and the monster forcible-unwear/poly_obj/re-equip + resists_magm + `newcham` arms; landmine else-arm gains the module-level `recursive_mine` guard, `steed_mid` capture, steedintrap call, and `sobj_at(SADDLE)` in C order ahead of the existing wounded-legs lines; selector gains `case POLY_TRAP`. New file-local `u_locomotion_verb` over the imported live `locomotion` (C `hack.c` order; "step" is lowercase so the capitalize arm never fires). Imports join existing edges only (shieldeff→display, Yname2→objnam, POLY_NOFLAGS→const, prinv→invent, Unchanging+polyself→polyself, extract_from_minvent+update_mon_extrinsics→worn, poly_obj→zap, locomotion→monmove — same SCC, hoisted declarations, call-time use, no top-level TDZ read).
**Verify:** `node scripts/verify.mjs --fn steedintrap` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Preflight `verify --no-cohort` green on a clean tree before edits. No hand probes: no corpus session reaches the new arms with a recorded expectation (mounted- trap hits, poly-trap squares); behavior proved by C-order reading against the brief bodies.
**Named:** `keep_saddle_with_steedcorpse(steed_mid, fobj, saddle)` (C `:2591–2592` — no live importer; steed_mid/saddle captured with `void` notes, floor saddle stays); monster poly resists_magm `shieldeff_mon` flash (display-only, no live exporter — same deferral as mhitm.js:615); slp-gas hero arm incl. its steedintrap call (C `:1578` — stays with the Sleep_resistance/fall_asleep body, separate C function).
**Next:** do not re-pop steedintrap dart/arrow/landmine/poly caller wiring or `trapeffect_poly_trap` for any arm shipped here. Falsifier: a rescore or fresh `verify steedintrap` / `verify trapeffect_poly_trap` showing a session blocked with either as owner.
## 2026-09-14 — D-2289 `trap.c` water_damage residuals: `pot_acid_damage` boom + scroll/book/potion message arms + chain acid_ctx/bhitpos; `zap.c` `blank_novel` + cancel wiring (queue row `apply.c` splash_lit residuals)

**C locus:** `trap.c:4657–4710` (`pot_acid_damage`: Blind off-invent `dknown = 0`, `acid_ctx` dkn/unk_boom `exploded` word, described vs `simpleonames`+`vtense` arms, ctx counters, `setnotworn`+`delobj`+invent `update_inventory`); `trap.c:4712–4851` (`water_damage`: grease `described = TRUE` + acid call `:4745–4751`, SCR_MAIL early-out under MAIL_STRUCTURES `:4780`, scroll/book/potion `Your` fades+dilutes + `update_inventory` sites `:4793/:4807/:4828/:4838/:4845`, Book steam + `CONTAINED_TOO|BURIED_TOO` locate `:4796–4806`, `blank_novel` on SPE_NOVEL `:4819–4820`); `trap.c:4854–4890` (`water_damage_chain`: acid_ctx init/reset, `bhitpos` save/restore, `CONTAINED_TOO` locate, otmp snapshot before the call); `zap.c:1367–1380` (`blank_novel`: `novelidx = 0`, `free_oname`, `container_weight`) + `:1329–1331` (`cancel_item` novel arm).
**JS:** 3 files, +160/−34 (`js/trap.js` pot_acid_damage + arms + chain; `js/zap.js` blank_novel + cancel arm + 2 import names; `js/potion.js` comment truth), under the 600/10 caps. Density: ~125 lines of C-faithful JS for ~95 row-named C lines plus the shared `blank_novel` helper.
**Change:** new `export function blank_novel(obj)` in `js/zap.js` directly after `cancel_item` (C position `:1367` after cancel), wired at both C call sites (cancel `:1330`, water_damage `:4820`); new file-local `pot_acid_damage(obj, in_invent, described)` in `js/trap.js` directly before `water_damage` (C staticfn position), C order (Blind gate, ctx-valid-guarded `exploded`, described `The potion…` via `otense` vs `simpleonames`+`vtense` with Another/More, ctx counters, `setnotworn`+`delobj`+invent update); `water_damage` gains `described`, the grease acid call, SCR_MAIL early-out (MAIL_STRUCTURES on per `global.h:430`, same idiom as the file's acid-damage arm), all five `Your` arms + mutation `update_inventory`s, Book steam via `get_obj_location(CONTAINED_TOO|BURIED_TOO)` + `isok`/`cansee`, `blank_novel` on SPE_NOVEL; `water_damage_chain` inits/resets `game.acid_ctx`, saves/restores `game.bhitpos` around a head `CONTAINED_TOO` locate, snapshots otmp before the call (C loop shape). Imports join existing edges only (`blank_novel`→zap, `setnotworn`→do, `otense`/`simpleonames`→objnam, `CONTAINED_TOO`/`BURIED_TOO`→const, `free_oname`→do_name/zap, `container_weight`→mkobj/zap — all `imports.mjs --can … → ALREADY`, same SCC, hoisted declarations, call-time use, no top-level TDZ read). C `&&` short-circuit preserved at the potion.js:2488 `ER_DESTROYED && obj.in_use` site (frees never read — verified against `potion.c:2355`); stale `pot_acid_damage … (ER_DESTROYED without delete)` comment retired.
**Verify:** `node scripts/verify.mjs --fn water_damage` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Plus forced `node frozen/ps_test_runner.mjs sessions` → **44/44** (new-pline/delete blast radius — fortress holds, speed `47+0.29/turn`). Preflight `verify --no-cohort` green on a clean tree before edits.
**Named:** none new (zap `cancel_item` corpse revive→rot timer swap stays; `You/Your/pline_The`-as-`pline` is the house idiom).
**Next:** do not re-pop splash_lit/pot_acid_damage/blank_novel/water_damage for any arm shipped here or in D-1337. Falsifier: a rescore or fresh `verify water_damage`/`verify blank_novel` showing a session blocked with either as owner.
## 2026-09-14 — Audit 8c4803e9..e210b23d (reviews 1250–1254: 5 ACCEPT, 0 Must-fix) + cadence 44/44

Review iteration, no js/ edits. Audited one SHA at a time against pinned C
(csym bodies + callers, sym.mjs on every re-point, --can on every new edge,
hidden-proxy verify --base re-measure per SHA): 1250 drown (trap.c:5058–5199
branch-verbatim; iron-golem-lifesave-continue follows the house
finish_losehp_done idiom, not queued); 1251 sobj_at 10-clone retire
(canonical mkobj.js:2201; mon.js negative-guard deletion dead for all call
sites; objects_at removals verified zero-use); 1252 doclose Blind arms
(isok→nodoor order kept, glyph-half drop matches D-2167 doopen_indir idiom);
1253 set_moreluck verbatim + 4 bless-family luck heads preserving C else-if
exclusion (remaining arms pre-existing named omits); 1254 mlevel_tele_trap
(pline_mon is real C at all 5 sites; dng_bottom is a trap.c staticfn so the
file-local is correct; onscary keep justified by monmove.c:240–303 room
resist covering (0,0)). Strongest verify of the five: D-2288's 13/13 hand
probe. Queue stays 8 Open (no refill owed); filled the D-2288 Addressed
short hash (`e210b23d`).
## 2026-09-14 — D-2288 `teleport.c` mlevel_tele_trap hole residuals: valley assign, botlevel avoids pline, pline_mon, impossible, canonical clamp + clone retire (queue row `trap.c` mlevel_tele_trap)

**C locus:** `teleport.c:2006–2098` (`mlevel_tele_trap`; stronghold valley `:2018`, botlevel avoid `:2021–2029` with `(tt == HOLE) ? "hole" : "trap"`, dst + `clamp_hole_destination` `:2030–2032`, `impossible("mlevel_tele_trap: unexpected trap type (%d)")` `:2079–2081`); `pline.c:137–150` (`pline_mon` msg_xy); `trap.c:591–599` (`clamp_hole_destination` = min(dlevel, `dng_bottom`)) + `:417–438` (`dng_bottom` quest-locate / Gehennom-invocation cutoffs); `trap.c:3577–3584` (`seetrap`); `wizard.c:106–114` (`mon_has_amulet`); `makemon.c:33–50` (`is_home_elemental`).
**JS:** 2 files, +43/−62 (`js/trap.js` +9 clamp export; `js/teleport.js` imports + arms, −44 clone lines), under the 600/10 caps. Density: small-C exception — the row's entire remaining gap is ~12 lines of C (botlevel pline 7 + impossible 2 + valley 3) plus the canonical wiring.
**Change:** `js/trap.js`: new `export function clamp_hole_destination(dlev)` beside `hole_destination`, over the existing quest/hell-aware file-local `dng_bottom` (C `trap.c` staticfn position). `js/teleport.js` `mlevel_tele_trap`: unconditional valley assign (codebase `v?.dnum | 0` idiom, C order); botlevel avoids arm with C-exact `tseen` gate and hole/trap noun (TRAPDOOR → "trap"); all five in_sight lines via imported `pline_mon`; `await impossible` with the C format; hole-dst clamp via the canonical import; three clones deleted, replaced by static imports (`seetrap` + `clamp_hole_destination` from `trap.js`, `mon_has_amulet` from `apply.js`, `is_home_elemental` from `makemon.js` — all `imports.mjs --can` hoisted/cycle-safe, call-time use only, no top-level reads). Local `onscary` deliberately kept: C `monmove.c:280–284` applies the shk/priest own-room resist to auditory `(0,0)` too, which the `mon.js` export defers — importing it would regress NO_TRAP priest/shopkeeper stays.
**Verify:** `node scripts/verify.mjs --fn mlevel_tele_trap` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_mlevel.mjs` (out of tree, deleted): 13/13 — valley migrate dest mux/muy, botlevel seen/unseen + trapdoor-noun Finished, clamp simple/no-op/quest-cutoff/hell-cutoff, impossible-arm Finished, amulet LEVEL_TELEP stay (canonical `mon_has_amulet`), portal migrate dest + mconf.
**Named:** none new (hero `level_tele_trap`/`domagicportal` are different C functions; `assign_level` stays manual field copies — `dungeon.c:1977` trivial, no 5th clone added; local `onscary` kept per above with the reason in code).
**Next:** queue head moves to `artifact.c` arti_cost.
## 2026-09-14 — D-2287 `fountain.c` dipfountain residuals: mkobj bless/curse luck → `set_moreluck`, dip awaits + case-26 `body_part` (queue row `fountain.c` dipfountain residuals)

**C locus:** `attrib.c:439–451` (`set_moreluck`: `stone_luck(TRUE)`; `!luckbon && !carrying(LUCKSTONE)` → 0 else ±`LUCKADD`); `mkobj.c` bless `:1755–1756` / unbless `:1774–1775` / curse `:1803–1804` / uncurse `:1829–1830` (`carried(otmp) && confers_luck(otmp)` → `set_moreluck()`, `obj.h:332` `carried` == `OBJ_INVENT`); `artifact.c:526–533` (`confers_luck`: LUCKSTONE otyp or `oartifact` + `SPFX_LUCK`); `fountain.c` dipfountain lawful `bless`, unaligned `curse` + `spe--`, cases 16–20, case 26 `body_part(ARM)`.
**JS:** 3 files, +60/−20 (`js/attrib.js` producer; `js/mkobj.js` 4 luck gates + header truth; `js/fountain.js` 4 awaits + case-26 + comment truth), under the 600/10 caps. Density: 60 insertions for ~25 lines of row-named C (small-C exception — the row's entire remaining gap is the 11-line producer + four 2-line arms + dip-local one-liners).
**Change:** new `export function set_moreluck()` in `js/attrib.js` in C position (after `stone_luck`, matching `:441` after `:423`), C order (`stone_luck(TRUE)` first, `carrying` short-circuited); luck-first else-if gates in all four mkobj functions preserving C arm order (figurine/bag arms kept; still-named arms untouched); 4 `await`s in dipfountain (state changes already precede the first await per the mkobj contract, so non-lamplit behavior is identical and lamplit light plines now keep C order); case 26 uses the already-imported `body_part(ARM)`. Imports: `carrying` from `hack.js` (`imports.mjs --can … carrying` → SAFE, hoisted declaration); `set_moreluck` into `mkobj.js` (CHECK verdict → call-time use only, satisfied — no top-level read); `confers_luck` joins the existing `artifact.js` import (no new edge). No DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean.
**Verify:** `node scripts/verify.mjs --fn dipfountain --full` → PASS syntax (3 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (forced: Luck-engine blast radius) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. Hand probe `/tmp/probe_moreluck.mjs` (out of tree): 21/21 — empty/blessed/cursed/uncursed luckstone moreluck 0/+3/−3/+3, floor luckstone untouched, non-luck item preserves moreluck, flip flags exact.
**Named:** none new (artidisco save/rest stays Deferred; full `set_levltyp` ice/lava/count; COIN_CLASS guards, bless-side BAG weight, curse uwep-bimanual/uswapwep/SPBOOK; invent addinv/remove `set_moreluck` — different C function, own row on falsifier; drinkfountain case-24 `curse` await — different function, parked row, untouched; WIN_INVEN/`consume_obj_charge`).
**Next:** do not re-pop dipfountain/bless/curse/unbless/`set_moreluck` luck for shipped arms. Falsifier: a rescore or fresh `verify` showing a session blocked with a Luck topline at a dip/bless/curse step (or an addinv/remove row for the invent arms).
## 2026-09-14 — D-2286 `lock.c` doclose: Blind feel_location/mapseen + feel/see nodoor arms (queue row `lock.c` pick_lock feel/see arms)

**C locus:** `lock.c:957–1020` (`doclose`): `!isok(x,y)` goes to `nodoor` while `res` is still `ECMD_OK` (the `Confusion || Stunned → ECMD_TIME` cost comes later, with `stumble_on_door_mimic` between); `if (Blind) { oldglyph/oldlastseentyp = …; feel_location(x,y); if (glyph/lastseentyp changed) res = ECMD_TIME; }` before the `portcullis || !IS_DOOR` dispatch; `nodoor: You("%s no door there.", Blind ? "feel" : "see")` (`:1015`).
**JS:** 1 file, +18/−6 (`js/lock.js` only: doc comment + `doclose` !IS_DOOR envelope), under the 600/10 caps. Density note: small-C exception to the ~40-line floor — the row's entire remaining C gap is ~15 lines (two ternaries + one side-effect block); non-Blind rendering is provably unchanged (false-branch yields the exact old literal, checked by the render probe below).
**Change:** C order in C position — isok check first, returning `res` while still `ECMD_OK`; Confusion/Stunned cost after (stumble stays deferred between them, noted in the comment); new Blind block calling the already-imported `update_mapseen_for` + `feel_location` with the lastseentyp-compare promoting `res` (C's glyph half is dead — no `lev->glyph` in JS cells — same idiom as `doopen_indir` D-2167); both nodoor sites use the file-local hoisted `Blind()` ternary. No new module edge (no new cross-module import, so no `imports.mjs --can` owed); no DIAG/FORCE/seed-gate/coordinate/RNG-index reads; Rule #2 clean. Portcullis/drawbridge close arms stay deferred per the row's scope.
**Verify:** `node scripts/verify.mjs --fn doclose` → PASS syntax (1 changed js file: js/lock.js) · PASS rule2 (no fs/path/url/node:, no DIAG/FORCE/seed gates) · note hidden (vacuous: no corpus session blocked on doclose at HEAD — NOT a corpus PASS; cited session already fully PASS at HEAD per `show`, so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed). Render probe (`node -e` template check, no file): non-Blind renders exactly `You see no door there.`, Blind renders `You feel no door there.` — render-ok. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); no corpus session reaches the Blind doclose arm with a recorded expectation, and the non-Blind arm is string-identical.
**Named:** none new (`stumble_on_door_mimic`, portcullis/drawbridge close arms, steed close path, `feel_newsym` mapseen gating stay in the `doclose` doc comment — only the Blind feel_location/mapseen item is retired by this commit).
**Next:** do not re-pop `pick_lock`/`doopen_indir`/`doclose` feel/see for any arm shipped here, in D-2002, or in D-2167. Falsifier: a rescore or fresh `verify doclose` showing a session blocked with doclose as owner (e.g. a Blind-hero close-to-nothing topline).
## 2026-09-14 — D-2285 `invent.c` sobj_at: residual 10 clones rewired to the canonical import (queue row `invent.c` sobj_at residual clones, D-2281 follow-up)

**C locus:** `invent.c:1466–1475` (`sobj_at`: walk `svl.level.objects[x][y]` nexthere chain, `otyp ==` match, first hit or NULL). Rewired call sites match pinned C call sites: `dbridge.c:926` drawbridge-fill boulder, `music.c:234` quake-pit boulder, `steed.c:547/:766` dismount-spot boulders, `monmove.c:872` bee royal-jelly, `shk.c:910–911` pick-axe/mattock gate, `apply.c` bullwhip-wrap (`:3089`), statue-trap/glyph (`:3523/:3543`), revive (`:203`), figurine-fit (`:2534`), jump-clearance (`:1887`), `dothrow.c:815` hurtle boulder, `pager.c:295–300` `object_from_map` chest/box + glyphotyp, `mon.c:2329` scare-scroll (`onscary`) + `mon.c:1668` `meatcorpse` CORPSE loop with `nxtobj(otmp, CORPSE, TRUE)` (invent.c:1477 traversal idiom, already canonical).
**JS:** 9 files, +22/−99 (`js/apply.js`/`js/dbridge.js`/`js/dothrow.js`/`js/mon.js`/`js/monmove.js`/`js/music.js`/`js/pager.js`/`js/shk.js`/`js/steed.js`: import lines + clone deletes + caller renames), under the 600/10 caps. Density note: dedup-only residual completion of D-2281 (small-C exception — shipped C is 9 lines; the value is zero-clone convergence, proved by full+cohort green).
**Change:** all 10 defs deleted; `sobj_at` joins each file's existing static `mkobj.js` import — no new module edge (all 9 files already import `mkobj.js`; same SCC; the canonical is a hoisted exported function declaration used at call time only, no top-level TDZ read — D-2281 `imports.mjs --can … → ALREADY` precedent). `music.js`/`steed.js`/`dothrow.js`/`pager.js` dropped the now-unused `objects_at` from the import (verified: zero remaining code uses; pager's 2 hits are comments). All rewired call sites pass int otyps (or `|0`'d glyphotyp), so the canonical `(otmp.otyp|0) === (otyp|0)` is C-exact and behavior-identical except for the three clone-wrongs above, which now match C.
**Verify:** `node scripts/verify.mjs --fn sobj_at` → PASS syntax (9 changed js files) · PASS rule2 (no fs/path/url/node:, no DIAG/FORCE/seed gates) · note hidden (vacuous: no corpus session blocked on sobj_at at HEAD — NOT a corpus PASS; row cited 0 blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Preflight `verify --no-cohort` green on a clean tree before edits. No hand probes: every rewired call site keeps the same walk and C-exact compare (full + cohort green confirms no behavior change); no corpus session reaches the fixed crash/type arms with a recorded expectation.
**Named:** none new (`mon.js:156` `bad_rock` Sokoban boolean loop and `mon.js:918` behind-monster boulder loop stay local — boulder-specific predicates, not the generic otyp walk; all other pile walks untouched).
**Next:** do not re-pop `sobj_at` for any arm shipped here or in D-2281. Falsifier: a rescore or fresh `verify sobj_at` showing a session blocked with sobj_at as owner.
## 2026-09-14 — D-2284 `trap.c` drown: wade / Amphibious / teleport / steed / drowning-loop arms (queue row `trap.c` drown)

**C locus:** `trap.c:5059–5199` (`drown`, 140 lines). In C order: `feel_newsym(ux,uy)`; uinwater + `is_pool(ux-dx,uy-dy)` + (Swimming||Amphibious||Breathless) wade (`!rn2(5)` inpool_ok else early FALSE); first-entry `plunge/fall into <waterbody>%c` (`.` iff Amphibious||Swimming||Breathless) + `!Swimming && !is_solid` Titanic/rock sink; `water_damage_chain(invent,FALSE)`; gremlin `rn2(3)`→`split_mon` / iron-golem `Maybe_Half_Phys(d(2,6))` rust (`mhmax` decrement + `losehp`); inpool_ok early FALSE; leash slip (`pline_The`, `unleash_all`); Amphibious||Breathless||Swimming survive (verbose aren't-drowning, non-waterlevel bottom/keel, Punished `unplacebc/placebc`, `vision_recalc(2)`, `set_uinwater(1)`, `under_water(1)`, `vision_full_recalc`) early FALSE; (Teleportation||`can_teleport(data)`) + `!Unaware` + (Teleport_control||`rn2(3) < Luck+2`) `dotele` escape; `dismount_steed(GENERIC)`; `usleep` unmul / `reset_faint`; mmove + `rnd_nextto_goodpos` + waterlevel/`emergency_disrobe` crawl (`Pheew/teleds(ALLOW_DRAG)` or `But in vain`); `set_uinwater(1)` + `urgent You drown` + 2x `done(DROWNING)`/`safe_teleds(ALLOW_DRAG|TELEPORT)` loop with deep-water/limitless format arms + `set_uinwater(0)` + `rescued_from_terrain(DROWNING)` TRUE.
**JS:** 1 file, +163/−31 (`js/trap.js` only: 6 import lines + full `drown` body), under the 600/10 caps. Density note: ~130 lines of C-faithful JS for the 140-line C function.
**Change:** the C arms verbatim in C order in `js/trap.js` `drown`, preserving short-circuit, RNG (`rn2(5)` wade, `rn2(3)` gremlin/teleport, `d(2,6)` rust), list, ownership, mutation and integer (`|0`, `Luck+2`) semantics. `is_solid` is now `is_waterwall` (dbridge.js); Swimming/Amphibious/Breathless via `hero_*` (youprop.h:264–279); Punished ≡ `u.uball` (D-1786); Unaware via `eat.js` canonical (`:399` unconscious||faint — the file-local `Unaware()` is usleep-only, so a dynamic-aliased import avoids the static clash); Luck ≡ `uluck+moreluck`; Teleportation/Teleport_control flats + uprops-slot mirror; `You/Your/pline_The` render as plain `pline` (house idiom, no second You/Your clone per the brief); iron-golem fatal `losehp` finishes via `end.js` and returns; drowning loop mirrors `lava_effects` (killer rebuilt per pass, lifesave `safe_teleds` break, `You're still drowning`, post-loop `set_uinwater(0)` + `rescued_from_terrain`). New static edges (all `imports.mjs` cycle-safe, same SCC, hoisted-function call-time use, no top-level TDZ read): `under_water` (display.js), `can_teleport` (monsters.js), `DISMOUNT_GENERIC`/`TELEDS_TELEPORT`/`TELEPORT`/`TELEPORT_CONTROL` (const.js), `safe_teleds`/`noteleport_level`/`dotele` (teleport.js), `number_leashed`/`unleash_all` (apply.js), `is_waterwall`/`hero_Swimming`/`hero_Amphibious`/`hero_Breathless` (dbridge.js); `split_mon` (sit.js) + `Unaware` (eat.js) + lifesave `finish_losehp_done` (end.js) via dynamic import (the `./sit.js` idiom already in this file).
**Verify:** `node scripts/verify.mjs --fn drown` → PASS syntax (1 changed js file: js/trap.js) · PASS rule2 (no fs/path/url/node:, no DIAG/FORCE/seed gates) · note hidden (vacuous: no corpus session blocked on drown at HEAD — NOT a corpus PASS; row cites no N blocks so no `--base` owed) · PASS green 2/2 · PASS strict seed8000 + seed0900 · PASS cohort 7/7 · VERIFY: PASS (skips full: no shared file changed per its list). Preflight `verify --no-cohort` green on a clean tree before edits. No maintained unit test: no `js/` unit harness exists (`sessions/` + `hidden-proxy` are the suite); no corpus session reaches the new arms so no hand probe with a recorded expectation exists — the arms are verified by C-order reading against the brief body plus the green/cohort gates proving no regression on the 44-session fortress.
**Named:** none new (`lava_effects` stays its own live D-1913 port; `You/Your/pline_The`-as-`pline` is the house idiom, not an omission).
**Next:** do not re-pop `drown` for any arm shipped here. Falsifier: a rescore or fresh `verify drown` showing a session blocked with drown as owner.
## 2026-09-14 — Audit 80a22605..7942532c (reviews 1244–1249: 6 ACCEPT, 0 Must-fix) + cadence 44/44

Six JS SHAs since 1243, each reviewed against pinned C with `csym` body + `--callers`, `sym.mjs`, `--can`, added-line banned-pattern scan, and an independent `hidden-proxy verify --base HASH~1` re-run. 1244 splitbill (C fall-through decrement preserved, ALREADY edge), 1245 light split (snapshot+unshift = C head-insert order, callees file-local), 1246 obj_move_timers (dead API in C too, panic→throw order kept), 1247 sobj_at canonical (9 clones retired, `|0` C-exact for int otyps, full 44/44 auto), 1248 credit_report + scatter arms (strchr-NUL quirk preserved, only 2 C callers both awaited), 1249 make_sick kpfx (re-measured PROGRESS: Archeologist-92015 → PASS, 1/0/0/0). Five vacuous verifies all honestly disclosed; zero C-wrongs → no Must-fix, queue stays 11 Open (≥8, no refill). Cadence full `sessions`: 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `47+0.29/turn` (R² 0.79).
