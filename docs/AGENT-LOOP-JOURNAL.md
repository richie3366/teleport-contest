# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-15 — D-2343 `pray.c` offer_different_alignment_altar altar-wrath offering + `attrib.c` uchangealign (turns.md:399 dosacrifice family)

**C locus:** `nethack-c/upstream/src/pray.c` `offer_different_alignment_altar` `:1630–1695` (conversion test `ugod_is_angry() || (altaralign == A_NONE && Inhell)`, convert arm `:1640–1650`, reject arm `:1651–1660`, consume+conflict `:1662–1694` with `rn2(8+ulevel)>5` glow vs decrease), dispatched from `offer_corpse` `:2025–2028` (`u.ualign.type != altaralign`); callee `attrib.c` `uchangealign` `:1319–1362` (CONVERT `:1331–1342`, helm on/off `:1343–1359`, record-wipe tail `:1360–1362`).
**JS:** 2 js files (`attrib.js` +78/−1, `pray.js` +112/−9; +190/−10 total), far under the 600/10 caps. One new cross-module edge (pray→priest); rest join existing edges.
**Change:** `js/attrib.js` — exported `uchangealign` (1:1 with C `attrib.c`): ublessed=0 + `game.flags.botl` (pray.js-guarded idiom), CONVERT livelog `permanently converted to <adj>` via `aligns[1-newalign]` (roles.js order matches C role.c:697–700) + `ualignbase.current` write (JS `{current,original}` object, u_init.js:1963 shape) + helm-block check + sudden/new-direction pline; helm-on `adjalign(-7)` + oscillate pline + `make_confused(rn1(2,3))` + astral/abuse gate + helm livelog; helm-off pline; record-wipe tail. `js/pray.js` — local `offer_different_alignment_altar` in C order (convert: strong-feeling + consume + allegiance-accepted + `uchangealign(CONVERT)` + luck−3 + blesscnt+300; reject: ugangr+3/adjalign−5/rejects-pline/`godvoice(altaralign,'Suffer, infidel!')`/luck−5/`adjattrib(WIS,−2)`/`angrygods`-if-!Inhell; else consume + conflict-sense + `rn2(8+ulevel)>5` glow arm — `You_feel` increase, luck+1, `Align2amask` + shrine-bit + `newsym`, Blind-gated `The altar glows <hcolor white/black/gray>` (decl.h NH_WHITE/NH_BLACK are c_color_names "white"/"black", artifact.js:147 precedent; C `type?black:gray` gives neutral gray, matched), `rnl(ulevel)>6 && record>0 && rnd(record)>3*ALIGNLIM()/4` summon + priest check — vs decrease arm with luck−1 and the 7/8 summon gate), wired into `offer_corpse` replacing the bare return. Imports: `uchangealign` + `Align2amask`/`A_CG_CONVERT` join existing attrib/const edges (ALREADY); `findpriest`/`temple_occupied`/`p_coaligned` new pray→priest edge (`imports.mjs --can`: SAFE, hoisted declarations); `aligns`/`make_confused`/`livelog_printf`/`Hallucination` join attrib's roles/potion/pline/display edges (all SAFE, no cycles); no `You`/`pline_The` clones (pline literals per pray.js idiom + D-1678 review).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_altar.mjs` (uchangealign: convert type/base/record/ublessed/RNG-silence, helm-block keeps type+record but sets base, helm-on abuse+7 + wipe, helm-off wipe — the pray.js local is unreached by any session so no corpus falsifier exists) → 13/13 PASS after (kept in /tmp, out of tree). `node scripts/verify.mjs --fn offer_different_alignment_altar` → PASS syntax (2 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 · VERIFY: PASS.
**Named:** `angry_priest` (C priest.c:876–911) at the conversion-glow priest check — own next Open queue row, not this cluster (same precedent as D-1678's sacrifice_your_race arms); `summon_furies` (C makemon.c:2605, helm-on astral/abuse arm) + `retouch_equipment` (C artifact.c:2639, align-change tail) inside `uchangealign` — no live JS counterparts; helm caller wiring in do_wear stays deferred (turns.md:1070 notes `uchangealign` live); `bestow_artifact` / amulet-offer rows untouched.
**Next:** do not re-pop offer_different_alignment_altar/uchangealign for these arms. Falsifier: a fresh `verify offer_different_alignment_altar` showing a session blocked with it as owner, or a foreign-altar sacrifice trace contradicting the C order above. No seed/step/coordinate gates.
## 2026-09-15 — D-2342 `sp_lev.c` spo_end_moninvent m_dowear for Kni-strt King Arthur + Mon-strt Grand Master (D-2335 follow-up)

**C locus:** `nethack-c/upstream/src/sp_lev.c` `spo_end_moninvent` `:3031–3036` (`m_dowear(invent_carrying_monster, TRUE)`, then clears to NULL), dispatched from `lspo_monster` `:3390–3393` after the lua invent function runs (mechanism verified in review 1301: `create_monster` sets `invent_carrying_monster` on CUSTOM_INVENT `:2183–2185`); `m_dowear` body `worn.c:757–796` already live in `js/worn.js:978`, no re-port.
**JS:** 1 file (`mklev.js`, +11/−5: 2×3-line call sites + 2 docstring lines), far under the 600/10 caps. Small because the C delta is two one-line `spo_end_moninvent` applications of an already-ported 39-line locus (density note, said plainly — same class as D-2335/review-1301-ACCEPT).
**Change:** `js/mklev.js` only, zero new module edges (`imports.mjs --can mklev.js worn.js m_dowear` → ALREADY: file already statically imports worn.js). One `m_dowear(mtmp, true)` after each invent loop, in C position (after invent, before chest/abbot spawns), C-cited. Sync-through without await: the creation path takes no awaits (all pline/autocurse/artifact-light arms are gated on `!creation`), is RNG-free and message-free, and nothing later in the same sync stacks reads the leaders' worn state — same shape as the D-2335 sites and the `makemon.js` precedent.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn m_dowear` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** other loaders' `spo_end_moninvent m_dowear` Named-omission lines (Kni-loca/fila/filb, Mon-loca/goal/fila/filb, etc.) stay: those levels spawn no CUSTOM_INVENT monster, so `spo_end_moninvent` is a no-op there. humidity-aware `get_location`, light-region wall expansion, `ensure_way_out`/`map_cleanup` stay named per loader.
**Next:** do not re-pop m_dowear / spo_end_moninvent for Kni-strt King Arthur or Mon-strt Grand Master (data.md:786 retires to this D-id alongside D-2335). Falsifier: a quest-start spawn with leader armor loose in minvent (wornmask unset) where C wears it, or a fresh `verify m_dowear` showing a session blocked with m_dowear/spo_end_moninvent as owner. No seed/step/coordinate gates.
## 2026-09-15 — D-2341 death-disclosure overview epitaph writers behind misattributed `save_dungeon` (imitator / priest-minion / helpless / Sokoban ureached)

**C locus:** `end.c` `done_in_by` `:184-344` — imitator arm `:230-255` (`imitator = mptr != champtr || mimicker`; realnm=champtr, shape article rules pname-bare / unique-"the" / else `an()`, `"%s imitating %s"` / vampshifter `"in %s form"` / mimicker `"disguised as %s"`), ispriest/isminion `m_monnam` arm `:262-267`, killer multi trim `:288-316` (`multireasonbuf` "m_id:reason", truncate at first space on m_id match); `topten.c` `formatkiller` `:89-162` — helpless arm `:145-155` (`incl_helpless && multi<0`: `", while <reason>"` else `", while helpless"`, NUL-counted siz guards); `uhitm.c` `dynamic_multi_reason` `:104-124` (`"m_id:verb by <mon>"`, `x_monnam ARTICLE_A` + IT/INVISIBLE/HALLUCINATION/SADDLE/NAME, gaze `s_suffix+" gaze"`) + call sites `:3459` (mhitu paralyze), `:6045` (gaze frozen), `:6062` (cube frozen); `do.c` `goto_level` `:1678-1684` (non-builds_up max-track, builds_up `==0 || dl<reached` min-track). `monhealthdescr` (`pager.c:139`) is exact-omit: body `#if 0`'d to `*outbuf='\0'`.
**JS:** 4 js files (`end.js` +72/−21, `uhitm.js` +22/−6, `mhitu.js` +5/−5, `do.js` +8/−5; ≈+107/−37), far under the 600/10 caps. No new modules; zero new cross-module edges (all ALREADY).
**Change:** `js/end.js` — 5 names join existing imports (no new edges: `m_monnam`, `is_vampshifter`, `strstri`, `the_unique_pm`, `BUFSZ`; all `imports.mjs --can` ALREADY); imitator arm in C order incl. mimicker `mons(mappearance)` + vampshifter bat-form + `mptr=mtmp->data` equivalence noted (JS arise already reads `mtmp.data`); ispriest/isminion `m_monnam` arm; m_id trim with tail-identity check (string analogue of C's pointer-range check) truncating both `multireasonbuf` and `multi_reason`; helpless arm with BUFSZ-remaining guards (8/17 with NUL). `js/uhitm.js` — exported `dynamic_multi_reason` (x_monnam+s_suffix already imported; SUPPRESS_HALLUCINATION joins existing const edge) + gaze/cube call sites in C position. `js/mhitu.js` — `dynamic_multi_reason` joins existing uhitm edge; static string replaced.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Diagnosis probes `/tmp/probe_overview.mjs` (grids), `/tmp/probe_all8.mjs` (all 8 diffs — grouped 4 writers), `/tmp/probe_killer.mjs` (post-death cham/isminion state — deleted before finishing). `node scripts/verify.mjs --fn save_dungeon` → PASS syntax (4 changed js files) · PASS rule2 · PASS hidden (7 PASS: 91118/92140/92119/92050/92005/92063/92098; 92075 moved 78→80 owner-null single-cell `^F`-map glyph, error null — the verify line's "js-throw" label is its null-owner print, NOT a throw: direct replay runs clean, 141 screens, and `hidden-proxy show` reports error null with a 1-cell row-10 screen diff) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) · VERIFY: PASS.
**Named:** `done_in_by` ghost arms (no corpus reach); vampshifter-alt + mimicker sub-arms (ported, unreached); gaze/frozen `dynamic_multi_reason` sites (ported, unreached); `uhitm` gaze-site `nomovemsg=0` (display-only, unreached); turns.md:1745 ghost clause stays, imitator/priest/trim clauses retire to this D-id; `goto_level` min-track noted at turns.md:307.
**Next:** do not re-pop save_dungeon/done_in_by/formatkiller/dynamic_multi_reason/dunlev for these arms. Rescore notes (all fail→pass or fail→later-fail; zero was-PASS→fail): baseline pre-edit rescore on the clean tree already flipped 2 unrelated sessions to PASS (random-seed0015-eb7e90ad `-`-command flake, scen-genesis-Tourist-92044 do_statusline2@106 — environment flakes, not this port); post-fix rescore surfaced 2 pre-existing RNG diffs from behind cleared transients (Priest-92163 hmonas@193 More-transient → live queue row mhitm_knockback@248 whose index-3744 mismatch the hmonas park already documented; Valkyrie-92162 None@70 → m_move@72, already in that row's session list) — neither is reachable from this port (zero new RNG draws; death-text/overview-listing only). 92075's step-80 owner-null single cell is its own future row on rescore. Queue refill re-measured exhausted (all 24 fresh tops parked/archived; TOP30 remainder likewise) — queue drops to 6 Open, no filler invented (D-2337 precedent). No seed/step/coordinate gates.
## 2026-09-15 — Audit ce42f088..09146ec5 (reviews 1298–1306: 9 ACCEPT, 0 Must-fix) + cadence 44/44

Review-only iteration, no `js/` edits. Every JS-touching SHA since audit 1292–1297 re-audited against pinned C with `csym.mjs` + `--callers`, `sym.mjs` on every deleted/re-pointed symbol, `--can`/`--rulecheck`, added-lines banned grep, and an independent `hidden-proxy verify <fn> --base HASH~1` re-run (all 9 confirm 0 blocked; all rows cited 0 so no deeper `--base` owed). Notable depth: 1301 proves D-2335's un-awaited `m_dowear` converges (all 7 `m_dowear_type` awaits sit under `!creation`; creation path RNG/message-free; same shape as `makemon.js:3472`); 1302 proves D-2336's `delarea −1` ≡ C `l_get_lregion` default and placement now rides C's own fixup slot (`mkmaze.c:585-646`); 1303 proves D-2337's split eligible/fallback arrays ≡ C shared-array `n`/`altn` and `oname` subsumes the dropped `artifact_exists`; 1306 traces the `Stone_resistance` triple-OR to C's H||E (flat field has zero writes). Prior QUALITY-RISKs closed: 1289 surface nouns by D-2327, 1291 cloak-90 by D-2326 — Must-fix drained, 0 outstanding. Cadence: full `sessions` 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `46+0.28/turn` (R² 0.78). CURRENT Next cluster advanced past shipped D-2340 to queue head `worn.c` mon_adjust_speed — flag: NOTES.md symptom-owner-parks line still lists it while the Open row says never-parked; port iter re-measures via `verify mon_adjust_speed` before trusting either.
## 2026-09-15 — D-2340 `mon.c` hideunder concealer arms: can_hide_under_obj coins + pet cursed + cockatrice skip (data.md:462, D-0630 residual)

**C locus:** `nethack-c/upstream/src/mon.c` `hideunder` `:4725–4802`, concealer arm `:4752–4772` (`hides_under && objects && can_hide_under_obj && (!mtame || !cursed_object_at) && !is_pool_or_lava`, `seenobj` from the pre-walk pile head `:4763–4764`, cockatrice walk gated on `is_u ? !Stone_resistance : !resists_ston` `:4767–4770`, `undetected` only if post-walk `otmp` non-null `:4771–4772`); `monmove.c` `can_hide_under_obj` `:2121–2167` (10-coin threshold over the `nexthere` chain); `dogmove.c` `cursed_object_at` `:145–153` (any `cursed` on the pile); `makemon.c` `:1307–1313` (S_SPIDER/S_SNAKE births call the real `hideunder`, not an inline).
**JS:** 4 js files (`dogmove.js` +1/−1, `makemon.js` +19/−7, `mon.js` +23/−7, `monmove.js` +18/−8; +61/−23 total), far under the 600/10 caps. C arms are ~12 lines × 3 sites, so the residual is dense by construction.
**Change:** 4 js files (+61/−23), no new modules. `dogmove.js`: 1-word `export` on `cursed_object_at` (body already exact vs C). `monmove.js`: 1-word `export` on `can_hide_under_obj`; `cursed_object_at` joins the existing dogmove import (`imports.mjs --can` ALREADY — no new edge); local `hideunder` gains the pet gate + cockatrice walk in C order (seenobj pre-walk, `!resists_ston` walk, post-walk null check).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Hand probe `/tmp/probe_hideunder.mjs` (10 cases over the primary `hideunder`: plain-hide, pet-cursed-refuse, wild-cursed-hide, cockatrice-only-refuse, cockatrice-mixed-hide, stoneres-hide, 9-coin-refuse, 10-coin-hide, hero-refuse, hero-stoneres-hide) → 4 FAIL before (pet-cursed, cockatrice-only, 9-coin, hero-cockatrice all wrongly hid), 10/10 after; deleted before finishing. `node scripts/verify.mjs --fn hideunder` → PASS syntax (4 changed js files) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared files changed) · VERIFY: PASS.
**Named:** `You_see "%s %s under %s"` pline + `set_msg_xy` / `PLNMSG_HIDE_UNDER` / `last_hider` (async boundary; both `hideunder` locals stay silent — own future row); data.md:462 retires to this D-id; turns.md:2777/2821/3683 pet/cockatrice/`can_hide_under_obj` clauses retire with it (their `You_see` clauses stay).
**Next:** do not re-pop can_hide_under_obj/hideunder/cursed_object_at for coins/pet-cursed/cockatrice. Falsifier: a fresh `verify hideunder` showing a session blocked with hideunder as owner, or a hide-decision trace contradicting the C order above. No seed/step/coordinate gates.
## 2026-09-15 — D-2339 `makemon.c` makemon MM_NOCOUNTBIRTH born tally via propagate (data.md:1098, D-0538 residual)

**C locus:** `nethack-c/upstream/src/makemon.c` `makemon` — `countbirth = ((mmflags & MM_NOCOUNTBIRTH) == 0)` (`:1160`, beside the `anymon`/`byyou`/`allow_minvent`/`allowtail` locals) and `(void) propagate(mndx, countbirth, FALSE)` (`:1233`, after the ptr/random `mndx` arms, before `newmonst`). `propagate` (`:957–982`, already live in JS) still marks `G_UNIQ`→`G_EXTINCT` and extinct-at-limit when the tally is skipped. `mongone` (`mon.c:3267–3283`) is born-neutral by construction — `mon.c` contains zero `born` refs; the only birth decrement is `unmakemon` (`makemon.c:1518–1527`, wizard-only callers, already map-named in `js/wizard.js`).
**JS:** 1 js file (`makemon.js`, +10/−1), far under the 600/10 caps. No new modules. The C locus is 2 lines, so the residual is dense by construction.
**Change:** `js/makemon.js` only, zero new module edges (`MM_NOCOUNTBIRTH` joins the existing const.js import; `propagate` is same-file — no new cross-module import, so no `imports.mjs --can` owed). `countbirth` const added beside the other `mmflags` locals in C-relative position; `propagate(ptr.mndx, countbirth, false)` placed after the random-`ptr` arm closes, before `*mtmp = cg.zeromonst`, mirroring C `:1233`. Short-circuit preserved (every early `null` return above never tallies, as in C); RNG-neutral (`propagate` draws nothing; wizard `debugpline` stays omitted as draw-free).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn mongone` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Hand probe for the reached arm (no corpus session blocks on mongone): `node frozen/ps_test_runner.mjs sessions/seed0373-barbarian-quest-tour.session.json` (D-0538's STATUE_TRAP session) → PASS RNG 35386/35386, Screen 124/124, identical before and after the change.
**Named:** `makemon` ptr-arm `G_GENOD` veto + wizard extinct `debugpline` (`makemon.c:1204–1212`, own future row); full `unmakemon` born-decrement (wizard-only callers; already named in `js/wizard.js:76,140`); `trap.js` `mongone_statue_donor` local subset stays (sync `maketrap` cannot await async `mongone`; observationally equivalent — seed0373 124/124 with the donor path live); data.md:1098 retires to this D-id except those.
**Next:** do not re-pop mongone/makemon for the MM_NOCOUNTBIRTH born tally. Falsifier: a fresh `verify mongone`/`verify makemon` showing a session blocked with either as owner, or a creation trace contradicting the C order above. No seed/step/coordinate gates.
## 2026-09-15 — D-2338 `trap.c` blow_up_landmine drawbridge/fillholetyp-liquid/fill_pit/maybe_dunk + trapeffect_landmine fill_pit/unconscious (data.md:1070, D-0874 residual)

**C locus:** `nethack-c/upstream/src/trap.c` `blow_up_landmine` `:3172–3219` (scatter `:3176–3179` → del_engr/wake/door `:3180–3185` → drawbridge `:3186–3192` → t_at + water/air deltrap vs `fillholetyp(x,y,FALSE)` + `ROOM`-gated terrain-set + `liquid_flow(x,y,typ,trap,cansee?"The hole fills with %s!":0)` `:3193–3210` → `fill_pit`/`maybe_dunk_boulders`/`recalc_block_point`/`spot_checks(x,y,old_typ)` `:3214–3219`); `trapeffect_landmine` `:2527–2657` (hero `fill_pit(u.ux,u.uy)` `:2600`, monster `tx/ty` capture + `fill_pit(tx,ty)` + `unconscious()` multi/nomovemsg `:2649–2653`).
**JS:** 1 js file (`trap.js`, +60/−26), far under the 600/10 caps. No new modules. C is 47 lines so the residual is dense by construction.
**Change:** `js/trap.js` only, zero new module edges (`imports.mjs --can` ALREADY ×7: `fillholetyp`/`liquid_flow`/`maybe_dunk_boulders` join the existing dig.js import, `is_drawbridge_wall`/`find_drawbridge`/`destroy_drawbridge` join the existing dbridge.js import, `unconscious` joins the existing teleport.js import, `DRAWBRIDGE_DOWN` joins the existing const.js import — all hoisted sync/async exports, no TDZ risk). `blow_up_landmine` ported in C order: `old_typ` capture pre-scatter, drawbridge arm (`DRAWBRIDGE_DOWN || is_drawbridge_wall>=0` → `{x,y}` + `find_drawbridge` → `await destroy_drawbridge`), `t_at` null-after-destruction + oil-melt comment, water/air `deltrap` vs `fillholetyp(x,y,false)` + `ROOM`-gated `lev.typ=typ` + `await liquid_flow(x,y,typ,t,cansee?"The hole fills with %s!":null)`, `fill_pit(x,y)`, `await maybe_dunk_boulders(x,y)`, `recalc_block_point`, `void old_typ` for the spot_checks call. `trapeffect_landmine` hero tail gains `fill_pit(u.ux,u.uy)` after recursive `dotrap`; monster branch captures `tx/ty` at entry and gains `fill_pit(tx,ty)` + `if (unconscious()) { game.multi=-1; game.nomovemsg='The explosion awakens you!'; }` (teleport.js idiom) in C position.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn blow_up_landmine` → PASS syntax (1 changed js file) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · VERIFY: PASS. `node scripts/verify.mjs --fn trapeffect_landmine` → same PASS tail (sibling in the same cluster).
**Named:** `spot_checks(x,y,old_typ)` (C `:3218`) — no JS counterpart anywhere in `js/` (own future row when it lands; `old_typ` captured for that call); `keep_saddle_with_steedcorpse(steed_mid,fobj,saddle)` (C `:2591–2592`) — no JS counterpart anywhere in `js/`; data.md:1070 retires to this D-id except those two.
**Next:** do not re-pop blow_up_landmine/trapeffect_landmine for fill_pit/drawbridge/fillholetyp/liquid_flow/maybe_dunk/unconscious/which_armor/steedintrap. Falsifier: a fresh `verify blow_up_landmine`/`verify trapeffect_landmine` showing a session blocked with either as owner, or a landmine-detonation trace contradicting the C order above. No seed/step/coordinate gates.
## 2026-09-15 — D-2337 `artifact.c` mk_artifact by_align gift path + gift_value gate + gen_spe data (data.md:272, D-0759 residual)

**C locus:** `nethack-c/upstream/src/artifact.c` `mk_artifact` `:172–309` (eligibility gather `:188–245` with exists / SPFX_NOGEN|unique / gift_value-vs-`Role_if` gates, A_NONE otyp match vs by_align alignment+race gate with role first-choice break `:223–229` and `rn2(3)`/`rn2(4)`/`rn2(2)` skill-compatibility short-circuit `:231–244` in exact evaluation order, `altn` fallback `:246–247`, `rn2(n)` pick + `mksobj(otyp,TRUE,FALSE)`/dispose `:250–273`, `oeroded` clear + `oname` + `oartifact` + `artifact_origin(RANDOM)` `:276–281`, `gen_spe` clamp `:282–296`, stray-dispose `:299–307`, `permapoisoned` tail `:306–307`); static `dispose_of_orig_obj` `:312–319` (`obj_extract_self` + `obfree(obj,0)`); `mondata.h:118` `race_hostile` (mflags2 & hatemask); `skills.h` `P_MAX_SKILL`; `artilist.h` A() `gs`/`gv` fields (e.g. Excalibur 0/10, Stormbringer 0/9, Sting 3/1).
**JS:** 4 js files (+~146/−~60: artifact.js +120 port, generated data 34 field rows, makemon.js + weapon.js 1-word exports each) + extractor (+19) + CURRENT Next-cluster (1 line), far under the 600/10 caps. No new modules, no new cross-module edges beyond hoisted-function imports inside the existing SCC.
**Change:** extractor parses A() args[12]/args[13] as `genSpe`/`giftValue` and emits them; `js/generated/artifacts_data.js` regenerated via the checked-in extractor (34 rows gain the two fields, values spot-checked vs C); `artifacts_globals_init` maps `gen_spe`/`gift_value`; `mk_artifact` ported arm-for-arm (shared gift_value gate, A_NONE otyp match, by_align alignment+race gate with role break, P_SKILLED-default skill compatibility from `oc_class==WEAPON_CLASS` ? `P_MAX_SKILL(|oc_skill|)` with C short-circuit order, split eligible/fallback arrays equivalent to C's shared-array `n`/`altn`, `mksobj(a.otyp,TRUE,FALSE)` + file-local `dispose_of_orig_obj` + `oname(otmp,name,0)` + `oartifact` + `artifact_origin(RANDOM)` + clamped `gen_spe`, stray-dispose, `permapoisoned` tail). New cross-module names all hoisted `export function` (cycle-safe in the 90-module SCC; `imports.mjs --can artifact.js mkobj.js mksobj`: SAFE; do_name/shk/makemon/monsters edges already exist): `mksobj`+`obj_extract_self` (mkobj.js), `oname` (do_name.js), `obfree` (shk.js), `race_hostile` (makemon.js, 1-word export added), `mons` (monsters.js), `P_MAX_SKILL` (weapon.js, 1-word export added), `P_SKILLED`/`P_BASIC` (const.js, edge exists).
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Probe `/tmp/probe_mkartifact.mjs` 31/31 PASS (deleted before finishing; by_align arms no corpus session reaches): neutral-Valkyrie gift always Mjollnir ×5 (role-break, christened, erosion-clear, spe clamp), all-exist gift → null, chaotic max0 gate ×20 draws (all C-legal nulls — no chaotic gv≤0 non-role artifact exists), A_NONE ORCISH_DAGGER → Grimtooth same-ptr + opoisoned tail + erosion clear, adjust_spe=FALSE keeps spe, A_NONE ELVEN_DAGGER → Sting spe 0+gs3=3. Probe-methodology note: two early runs failed only on probe-side assumptions (assumed mksobj_init leaves weapon spe 0 — C sets it via blessorcurse/rne before the gen_spe add; assumed Grimtooth gs=3 — C gs=0, Sting gs=3 used instead); shipped-code assertions passed throughout.
**Named:** pray.c `bestow_artifact` caller wiring (pray.js:2200 comment stays — own row; this port ships the callee only); none new in `mk_artifact` — data.md:272 retires to this D-id.
**Next:** queue refill refused again this iteration (measured, not skipped): all 25 `hidden-proxy queue --limit 30` owners are parked symptom/misattributed owners (each verified with a Parked row in LOOP-QUEUE.md), archived/shipped, or the live `save_dungeon`/`mon_adjust_speed` Open rows; every unqueued TOP30 row spot-checked (`newuhs` parked+archived D-1791/D-1988, `nh_timeout` presence-park, `dmgval` fired-park, `make_corpse` D-1794 park, `mattacku`/`xkilled`/`dochug`/`spoteffects`/`test_move`/`x_monnam` parks) is likewise parked, shipped, or map-singleton material gated at corpus < 90 % (Constitution §10.13, LOOP-QUEUE Deferred). Queue drops to 6 Open after this archive; do not invent filler — a rescore must surface a new owner.
## 2026-09-15 — D-2336 `sp_lev.c` flip_level lregion coord update: quest-start/soko branch levregions stored pre-flip (data.md:773 residual)

**C locus:** `nethack-c/upstream/src/sp_lev.c` `flip_level` `:698–734` (lregion arm flips inarea+delarea for every region, no islev gate — already live in JS); `load_special` epilogue `:6038–6050`/`:6471–6491` (wallification → `flip_level_rnd(allow_flips, FALSE)` → `fixup_special`); `mkmaze.c` `fixup_special` `:585–646` (leftover loop: BRANCH/PORTAL/STAIR goto `place_it`, TELE copies updest/dndest, then frees `gl.lregions`); `mkmaze.c` `place_lregion` `:356–410` (oneshot `rn1(1)` draws are RNG draws on both sides, unchanged by this move); `sp_lev.c` `l_get_lregion` `:5420–5435` (omitted exclude stays `-1,-1,-1,-1` with `del_islev=TRUE`); per-level regions from pinned `dat/*.lua` (`Bar-strt.lua:61` {62,02}, `Pri/Mon-strt` {05,04}, `Arc-strt` {63,06}, `Kni-strt` {20,14}, `Rog-strt` {19,09}, `Val-strt` {66,17}, `Sam-strt.lua:46` rect, `Hea-strt` (04,12), `Tou-strt` {68,14}, `Cav-strt` {71,09}, `Ran-strt.lua:47` islev rect, `soko4-2.lua:22` {03,01}).
**JS:** 1 file (`mklev.js`, +152/−84), far under the 600/10 caps. Thirteen repetitions of one 8-line C-cited idiom plus docstring retirements — one C locus family (load_special lregion epilogue), not unrelated items.
**Change:** `js/mklev.js` only, zero new module edges (all names file-local; no `imports.mjs --can` needed — no new cross-module import). Each loader now pushes `{ rtype: LR_BRANCH, rname: null, inarea, delarea: {-1,-1,-1,-1} }` (C omit-exclude sentinel) onto `game.lregions` pre-flip — absolute cells for Ran-strt (C `in_islev`), `mx/my`-relative for the 11 quest starts, `xstart/ystart`-relative for soko4-2 — then the existing `flip_level_rnd` flips them and the existing `fixup_special()` places them (12 sites); soko4-2 keeps the soko4-1/knox manual BRANCH drain idiom after `solidify_map()` (C places inside fixup, likewise post-solidify). Docstring `flip_level lregion coord update` clauses retire at all six sites that named them (Bar/Pri/Kni/Rog/Mon/Cav); data.md:773 retires to this D-id.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. Probe `/tmp/flip-lregion-probe.mjs` 104/104 PASS (deleted before finishing; arms no corpus session reaches): per loader — BRANCH push precedes flip, fixup follows flip, zero post-flip literal-coordinate `place_lregion` calls, delarea −1 sentinel — plus lua cross-checks (pushed cells equal the pinned `dat/*.lua` region numbers, Ran absolute-islev vs the rest map-relative). Probe-methodology note: two early runs failed only on the probe's own `region_islev` regex (lazy-optional group matched empty across the lua `}`) — fixture fault, fixed in-probe; the shipped-code assertions passed throughout.
**Named:** medusa-1/3 pre-flip pushes with manual TELE/BRANCH drains (behaviorally exact duplicates of the fixup leftover loop — pre-existing clones, untouched); knox/soko4-1/Wiz-strt manual BRANCH drains (correct flipped-consume idiom, untouched); earth/fire/water/air/astral portal pushes (already pre-flip; earth's "flip lregion coord remap" docstring note refers to portal-name/exclude handling, own row); `flip_level` regions (poison-cloud bounding-box/rects), trap pit-conjoined dir bits, MELT_ICE timer arm, `vision_reset` tail (separate C arms, own rows); Hea/Tou docstring "after flip" phrasing stays (descriptively true — fixup places after flip).
**Next:** do not re-pop flip_level lregion / branch-shortcut labels. Refill triage re-run this iter (`hidden-proxy queue --limit 30`): all 25 tops are parked symptom/misattributed owners with dedicated falsifiers (do_statusline2, obj_resists, distfleeck, one_characteristic, m_move, rloc, spoteffects, zapyourself, dodown, hmonas, doturn, dopush, mdrop_obj, slimed_to_death, that_is_a_mimic, were_change, break_armor, mcast_death_touch, disclose, drinkfountain, list_vanquished, dog_invent, collect_coords) or live-Open (save_dungeon ×8, mon_adjust_speed ×1); TOP30-reached rows likewise parked; map singletons stay gated at corpus 85.7 % < 90 % — no valid filler, queue sits at 7 Open until a rescore surfaces a new owner (same exhaustion as D-2335; do not invent filler). Falsifier: a quest-start/soko branch landing off the flipped inarea where C lands it on, or a fresh `verify flip_level` showing a session blocked with flip_level as owner. No seed/step/coordinate gates.
## 2026-09-15 — D-2335 `sp_lev.c` spo_end_moninvent m_dowear for Pelias/Lord Carnarvon/Arch Priest quest invents (data.md:785 residual)

**C locus:** `nethack-c/upstream/src/sp_lev.c` `spo_end_moninvent` `:3031–3036` (`m_dowear(invent_carrying_monster, TRUE)`, then clears to NULL), dispatched from `lspo_monster` `:3390–3393` after the lua invent function runs; `m_dowear` body `worn.c:757–796` already live, no re-port.
**JS:** 1 file (`mklev.js`, +17/−8: 1 import + 3×3-line call sites + 3 docstring updates), far under the 600/10 caps. Small because the C delta is three one-line `spo_end_moninvent` applications of an already-ported 39-line locus (density note, said plainly — same class as D-2330/D-2334).
**Change:** `js/mklev.js` only. New import `m_dowear` from `./worn.js` (`imports.mjs --can mklev.js worn.js m_dowear` → SAFE: hoisted function declaration, same 90-module SCC, no TDZ risk — same shape as the existing 1866 edges). One `m_dowear(mtmp, true)` after each of the three invent loops, in C position (after invent, before chest/guard spawns), C-cited.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn m_dowear` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** Kni-strt King Arthur + Mon-strt Grand Master identical `spo_end_moninvent m_dowear` patterns stay deferred (own future rows; untouched). `flip_level` lregion coord update and all other per-loader omits stay named in place.
**Next:** do not re-pop m_dowear / Pelias / Carnarvon / Arch Priest for data.md:785. Archiving drops Open 8→7 and `check-hot-docs` reports REFILL, but refill triage finds no valid filler: live `hidden-proxy queue --limit 30` lists 25 owners and every one is live-Open (save_dungeon, mon_adjust_speed), archived (were_change D-2113, collect_coords D-2184, …), or parked with a dedicated falsifier (do_statusline2, obj_resists, distfleeck, one_characteristic, m_move, rloc, spoteffects, zapyourself, dodown, hmonas, doturn, dopush, mdrop_obj, slimed_to_death, that_is_a_mimic, break_armor, mcast_death_touch, disclose, drinkfountain, list_vanquished, dog_invent, collect_coords, were_change); TOP30-reached rows are likewise parked (nh_timeout, mattacku, xkilled, dochug, test_move, make_corpse D-1794); map singletons stay gated at corpus 85.7 % < 90 % — no filler invented, queue sits at 7 Open until a rescore surfaces a new owner. Falsifier: a quest-start spawn with leader armor loose in minvent (wornmask unset) where C wears it, or a fresh `verify m_dowear` showing a session blocked with m_dowear/spo_end_moninvent as owner. No seed/step/coordinate gates.
## 2026-09-15 — D-2334 `sp_lev.c` map_cleanup deltrap/del_engr arms (D-0774 residual)

**C locus:** `nethack-c/upstream/src/sp_lev.c` `map_cleanup` `:328–356` (28-line body; callers `:6029`/`:6471` create_des_coder paths — JS call sites `js/mklev.js:3627–3631/:3814–3818/:3976/:4720–4723/:4915` untouched).
**JS:** 1 file (`mklev.js`, +14/−5 incl. 3 import-name joins + docstring), far under the 600/10 caps. Small because the C delta is two `if` arms of a 28-line locus (density note, said plainly — same class as D-2330).
**Change:** `js/mklev.js` only, zero new module edges (`imports.mjs --can` ALREADY ×2: `deltrap` joins the existing trap.js import, `engr_at`/`del_engr` join the existing engrave.js import): trap arm `const ttmp = t_at(x, y); if (ttmp && !undestroyable_trap(ttmp.ttyp)) deltrap(ttmp)` then engr arm `const etmp = engr_at(x, y); if (etmp) del_engr(etmp)`, in C arm order after the boulder loop inside the liquid branch, C short-circuit preserved. `undestroyable_trap` matches the C macro exactly (`trap.h:116` MAGIC_PORTAL || VIBRATING_SQUARE); all four callees are live sync exports, so `map_cleanup` stays sync. Boulder arm untouched.
**Verify:** preflight `verify --no-cohort` PASS on a clean tree before edits. `node scripts/verify.mjs --fn map_cleanup` → PASS syntax (1 changed js file: js/mklev.js) · PASS rule2 · note hidden (vacuous: 0 blocked — NOT a corpus PASS; row cited 0 blocks so no --base owed) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · PASS full 44/44 (auto: shared file changed) · VERIFY: PASS. Final verify ran after the last `js/` edit (map/D-log/queue edits only after; no D-1831 gap).
**Named:** shared-`deltrap` Sokoban PIT/HOLE `maybe_finish_sokoban` sub-arm (`trap.c`; callee not live in `js/` — only `sokoban_guilt` is, `js/trap.js:1516` — own row when the corpus reaches it). Pre-existing D-0774 shape kept: boulder arm unlinks manually instead of `obfree(otmp, NULL)` (avoids a new shk.js edge; untouched).
**Next:** do not re-pop map_cleanup for data.md:667. Archiving this row drops Open 8→7 with no refill: every live `hidden-proxy queue` top is a parked symptom/misattributed owner per CURRENT (do_statusline2/save_dungeon/obj_resists/distfleeck/…) or the live mon_adjust_speed Open row, and map singletons stay gated at corpus 85.7 % < 90 % — queue sits at 7 Open until a rescore surfaces a new owner; do not invent filler. Falsifier: a special level keeping a destroyable trap or an engraving on a lava/pool cell past load_special, or a fresh `verify map_cleanup` showing a session blocked with map_cleanup as owner. No seed/step/coordinate gates.
