# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
`node scripts/rotate-journal.mjs` (or `check-hot-docs.mjs --fix`).
## 2026-09-08 — D-2116 `zap.c` resist: mplayer arms + `zhitm` ZT_SLEEP `resist(how)` gate + explode.js `olet` alev table (queue row `zap.c` resist, 2 sessions)

**C locus:** `zap.c:6100–6158` `resist` — RING_CLASS/0/NOTELL fake-player (`is_mplayer`) early pass; alev table (WAND 12 / TOOL 10 / WEAPON 10 / SCROLL 9 / POTION 6 / RING 5 / default `u.ulevel`); dlev clamp with `is_mplayer → u.ulevel else 1`; `rn2(100+alev-dlev) < mr`. Call protocol: `zhitm` ZT_SLEEP → `sleep_monst(mon, d(nd,25), type==ZT_WAND(ZT_SLEEP) ? WAND_CLASS : '\0')` (`mhitm.c:1231–1232`: `resists_sleep || defended(AD_SLEE) || resist(how)`); `explode.c:534` `resist(mtmp, olet, 0, FALSE)` with variable `olet`.
**JS:** `js/zap.js` resist + zhitm sleep arm (+2 import names, no new edges); `js/explode.js` clone (+6 import names, no new edges); `js/mhitm.js` one-word export.
**Change:** `js/zap.js` — canonical `resist` gains the Conflict early pass + mplayer dlev (needs `is_mplayer`, same-module import extension, no new edge); ZT_SLEEP arm draws `amt=d(nd,25)` first (C arg order) then `if (!resists_sleep_slee(mon) && !await resist(mon, how, 0, NOTELL)) sleep_monst_zap(mon, amt)` with `how = type===ZT_SLEEP ? WAND_CLASS : 0` (`ZT_WAND(x)=(x)`, `zap.c:55`). `js/explode.js` — clone gains the full C alev table + both mplayer arms (sync shape kept: sole caller passes damage 0 + FALSE, so HP/death/shield arms cannot fire).
**Verify:** `node scripts/verify.mjs --fn resist` → PASS syntax (3 files) · PASS rule2 · PASS hidden (0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS: Priest moved → `zapyourself` at step 60 (was 59); Ranger moved → `burnarmor` at step 73 (was 72)) · PASS green 2/2 · PASS strict ×2 · PASS cohort 7/7 · skip full (tool: no shared file changed). Preflight `verify.mjs --no-cohort` before edits was green.
**Named:** `tell → shieldeff_mon` display stays deferred (pre-existing `void tell`); sleep `Resists_Elem` wielded/worn/artifact scan + `defended(mon, AD_SLEE)` stay deferred (no live `defended` in `js/`); `music.js:182` clone (callers always TOOL_CLASS) and `pray.js:2200` clone (callers always `'\0'`) are exact for their call sites — untouched; `potion.js` `resist_potion` (fixed POTION alev=6) untouched.
**Next:** Priest-92235 now blocked on `zapyourself` (step 60); Ranger-92212 on `burnarmor` (step 73) — later owners for future rows; no new queue rows added (9 Open remain, in the 8–12 band).
## 2026-09-08 — D-2115 `dig.c` zap_dig u.dz arm: bounce-off-stairs + ceiling rock (queue row `dig.c` zap_dig, 1 session)

**C locus:** `dig.c:1583–1612` — the `if (u.dz)` arm: air/waterlevel/`Underwater` (`u.uinwater`, youprop.h:279) skip; `u.dz < 0 || On_stairs` → bounce pline (`stairway_at` ladder/stairs + `ceiling`), `You loosen a rock`, `It falls on your head` (`body_part(HEAD)`), `rnd(hard_helmet(uarmh) ? 2 : 6)` + `losehp(Maybe_Half_Phys, "falling rock", KILLED_BY_AN)`, `mksobj_at(ROCK)` + `xname` + `stackobj` + `newsym`; else (down, off stairs) `watch_dig(0, ux, uy, TRUE)` + `dighole(FALSE, TRUE, NULL)`; unconditional `return`.
**JS:** 1 file (`dig.js`, +48/−6), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the corpus session reaches the changed arm.
**Change:** `js/dig.js` only, exact C order and guards (`On_stairs` called twice as in C): `Is_airlevel`/`Is_waterlevel` (pre-existing `const.js` edge) + `!(u.uinwater | 0)` (C `Underwater`; matches the `zap.c:3311` striking twin's `!(game.u?.uinwater | 0)` in `js/zap.js:6060`); bounce pline via `stairway_at` (pre-existing `mklev.js` edge, `.isladder` as in `dig_check`) + `ceiling` (extends the existing `trap.js` edge); `body_part(HEAD)` (`polyself.js`, `imports.mjs` SAFE hoisted) and `hard_helmet(u.uarmh)` (`do_wear.js`, SAFE hoisted) via call-time dynamic imports (this function's `shk.js` convention); `losehp(maybe_half_phys(dmg), 'falling rock', KILLED_BY_AN)` (`HEAD`/`KILLED_BY_AN` extend the existing `const.js` edge); fatal losehp finishes via `finish_losehp_done` (`end.js`, SAFE hoisted) and returns — C `done(DIED)` is noreturn, same as the striking twin; else `mksobj_at(ROCK, ux, uy, false, false)` + `xname` + `stackobj` (both pre-existing `mkobj.js` edges) + `newsym`, or the down/off-stairs `watch_dig` + `dighole(false, true, null)` (same-module).
**Verify:** `node scripts/verify.mjs --fn zap_dig` → `PASS syntax 1 changed js file(s): js/dig.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify zap_dig: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-death-Wizard-92187: moved → next_ident at step 48, was 20) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** unchanged — swallowed pierce; pitdig conjoined / `adj_pit_checks` / `pit_flow` (map + `zap_dig` header).
**Next:** none for `zap_dig` — the session now diverges at `next_ident` step 48 (later owner, `mkobj.c` row family); do not re-pop `zap_dig` for it.
## 2026-09-08 — D-2114 `quest.c` prisoner_speaks + quest_talk MS_DJINNI arm (queue row `quest.c` prisoner_speaks, 1 session)

**C locus:** `quest.c:451–470` prisoner_speaks + `quest_talk :495–511` MS_DJINNI arm — guard `data == &mons[PM_PRISONER] && mstrategy & STRAT_WAITMASK`; `canseemon` → `pline("%s speaks:", Monnam)`; `SetVoice(mtmp, 0, 80, 0)`; `verbalize("I'm finally free!")`; clear waitmask; `mpeaceful = 1`; `adjalign(3)`; `(void) angry_guards(FALSE)`.
**JS:** 1 file (`quest.js`), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the corpus session reaches the changed arm.
**Change:** `js/quest.js` only — `prisoner_speaks` in exact C order: `mndx` compare for the `data` identity (JS `mtmp.data` is a value, not a pointer — `sounds.js:1132` pattern), `canseemon` (extends the existing `display.js` edge), `Monnam` (`do_name.js`, `imports.mjs` SAFE hoisted), `SetVoice` (`sndprocs.js` !SND_LIB no-op, no cycle — call kept for C order), `adjalign` (extends the existing `attrib.js` edge), `await angry_guards(false)` (`mon.js`, same 90-module SCC, SAFE hoisted; C `FALSE` = not silent). `quest_talk` gains the C-order `switch (msound)` with the `MS_DJINNI` arm plus the C-order leader `return`; local `const MS_DJINNI = 29` (`monflag.h`; `sounds.js`/`monmove.js` local-const convention) and top-level `PM_PRISONER` (`monsters.js`, no cycle).
**Verify:** `node scripts/verify.mjs --fn prisoner_speaks` → `PASS syntax 1 changed js file(s): js/quest.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify prisoner_speaks: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Rogue-92210: PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `MS_NEMESIS` → `nemesis_speaks` (no live export — `quest.js` header + map).
**Next:** none — the blocked session fully PASSes; row archived.
## 2026-09-08 — D-2113 `were.c` were_change unseen-howl arm: `You_hear` + `wake_nearto` after human→beast change (queue row `were.c` were_change, 1 of 2 sessions)

**C locus:** `were.c:18–38` — after `new_were(mon)` + `gw.were_changes++` in the human→beast arm: `if (!Deaf && !canseemon(mon))` → `switch (monsndx(mon->data))` on the POST-change data (`PM_WEREWOLF`→«wolf», `PM_WEREJACKAL`→«jackal», default null) → `if (howler) { Soundeffect(se_canine_howl, 50); You_hear("a %s howling at the moon.", howler); wake_nearto(mon->mx, mon->my, 4 * 4); }`.
**JS:** 1 file (`were.js`, +40/−11), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — the Ranger session reaches the changed arm; the Healer residual is diagnosed from recorded C data (see Next).
**Change:** `js/were.js` only — `were_change` is now `async` and awaits `new_were` (C is fully sequential; the armor tail must settle before the `canseemon` read), then runs the howl block in exact C order: local `Deaf()` (youprop.h `HDeaf||EDeaf||uroleplay.deaf`, same formula as the `You_hear` gate in `js/hack.js:149` — no exported `Deaf` exists, matching the repo's local-clone convention) `&& !canseemon(mon)` (pre-existing `display.js` import) → `switch (mon.data?.mndx | 0)` (C `monsndx(mon->data)`; `set_mon_data` sets both `data` and `mnum`, `mons()` builds `mndx`) → `Soundeffect(se_canine_howl, 50)` (live no-op without SND_LIB, `js/sndprocs.js:36`) + `await You_hear('a … howling at the moon.')` (extends the existing `hack.js` edge) + `await wake_nearto(mx, my, 4 * 4)` (`js/mon.js:1267`; `were.js`↔`mon.js` are in the same import SCC — `imports.mjs` confirms call-time-only use is safe, and the `dokick.js:57`→`mon.js` precedent imports the same binding). Only caller `mon.js:840` already awaits (`await were_change(mtmp)`), so the sync→async flip changes no call site. The `uhitm.c:3067` mhitm-gaze `were_change(mdef)` call site stays unwired (different envelope, no corpus coverage — untouched, still named by its absence only there).
**Verify:** `node scripts/verify.mjs --fn were_change` → `PASS syntax 1 changed js file(s): js/were.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify were_change: 1 PASS, 0 moved past, 1 unchanged, 0 worse → PROGRESS` (scen-tour-Ranger-92177: PASS — 30739/30739 RNG, 135/135 screens; scen-intrinsic-Healer-92124: still were_change at step 69) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** none new in this function (Soundeffect is a live no-op, not an omission). `uhitm.c:3067` call-site wiring untouched (see Fix).
**Next:** do not re-pop `were_change` for Healer-92124 (parked this commit with the falsifier below). Misattribution proof, all C-side measured: the session's recorded step-69 dice are exactly 15 draws (`distfleeck`×3, `m_move rn2(32)`, `mattacku rnd(20)`, `hitmu d(1,2)`, `mhitm_knockback rn2(3)=2`→distance 1 then `rn2(6)=4`→`return FALSE` (no knockback), `mcalcmove`×2, `maybe_generate_rnd_mon`, `regen_hp`, `gethungry`, `moveloop_core`×2) — ZERO dice in `were.c`, so no transform, no howl, no summon ran on either side (positional RNG 2729/2729 matched throughout). The owner string comes from the topline word «jackal» matching the `were.c:28` literal (same for `were_summon :168` in `cMsgOwners`), while the actual row diff is a gold-`# Agent loop journal

Append-only crumbs for `scripts/agent-port-loop.sh` iterations.
Each agent process should add a short dated entry **at the top** (after
this header) before exiting. Keep entries tight; detailed hypothesis
lives in `NOTES.md` / `CURRENT.md`.

The next agent reads **only this file** (latest ~10 entries), not the
archive under `docs/archive/`. Do not copy crumbs by hand. Overflow is
 reveal at map (57,0) with identical toplines. Prescribed falsifier for unparking: a JS prefix-state probe of cell (57,0) at step 69 (monster covering gold that C's `m_move` step moved away vs a gold-paint gap in `newsym`/`show_glyph`), then re-queue under the writer that probe names — never under `were_change`.
## 2026-09-08 — D-2112 `insight.c` status_enlightenment Blind `!haseyes` gate reads current form, not race (queue row `wield.c` chwepon, 2 sessions)

**C locus:** `insight.c:1070–1071` — `you_are(buf, !haseyes(gy.youmonst.data) ? "" : from_what(BLINDED))` (Blind arm `:1059–1074`; `set_uasmon` points `youmonst.data` at `mons[umonnum]`, live in `js/polyself.js:558`).
**JS:** 1 file (`invent.js`, +4/−3), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No hand probes — both corpus sessions reach the changed arm (final disclosure).
**Change:** `js/invent.js` only — `const noeyes = !haseyes(game.youmonst?.data)` in exact C position. `haseyes` is null-safe (`monsters.js:379`), both names already imported (`:303`) — no new edge, no TDZ. Race fallback dropped deliberately: every playable race has eyes and C never has NULL `youmonst.data` here.
**Verify:** `node scripts/verify.mjs --fn chwepon` → `PASS syntax 1 changed js file(s): js/invent.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify chwepon: 0 PASS, 2 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-death-Monk-92000 → `from_what`@69 was 68; scen-death-Wizard-92120 → `attributes_enlightenment`@50 was 49) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** Infravision race-`mons` fallback (`invent.js:5519–5527`, `display.js hero_has_infravision`) stays — different arm, `set_uasmon`-deferred debt, untouched; `chwepon` named omissions (Hallucination hcolor; `useupall` clone; Yobjnam2 local) untouched.
**Next:** do not re-pop `chwepon` (0 blocked — both sessions rest at strictly later owners). Both rest at the next disclosure row C « You could survive without air.» vs JS « Your luck was zero.» (magical-breathing/luck disclosure writer, separate arm): Wizard-92120 is already covered by the `attributes_enlightenment` queue row (2 sessions, in the refreshed top-30); Monk-92000's `from_what` single-session row surfaces via the next `hidden-proxy queue` refill — do not invent rows.
## 2026-09-08 — D-2111 dothrow.c multishot_class_bonus PM_NINJA arm: ninja shuriken volley 1→2 (queue row `mkobj.c` next_ident, 1 of 3 sessions)

**C locus:** `dothrow.c:68–71` `multishot_class_bonus` PM_NINJA arm — `if (skill == -P_SHURIKEN || skill == -P_DART) multishot++` with FALLTHROUGH into the SAMURAI ya+yumi arm. C `monsters.h:3867–3875` NINJA has 2× `AT_WEAP`; C `mthrowu.c:238` `monmulti` drew `rnd(1)=1` then the ninja bonus made multishot=2.
**JS:** 1 file (`weapon.js`, +5/−0), under the 600/10 caps. Rule #2 clean; no DIAG/FORCE/seed gates. No committed probes (diagnosis via recorded-session RNG dumps + a reverted temporary `monshoot` caller trace + /tmp replay scripts, outside the repo).
**Change:** `js/weapon.js` only — `const PM_NINJA = monsterNames.indexOf('PM_NINJA')` beside the existing PM_PONY/SHADE/BALROG locals (pre-existing `monsters_data.js` edge, no new module, no TDZ) + the NINJA case in exact C position (before SAMURAI, with the C FALLTHROUGH). `P_SHURIKEN`/`P_DART` were already imported. No other arm touched.
**Verify:** `node scripts/verify.mjs --fn next_ident` → `PASS syntax 1 changed js file(s): js/weapon.js` · `PASS rule2` · `PASS hidden verify next_ident: 0 PASS, 1 moved past, 2 unchanged, 0 worse → PROGRESS` (scen-tour-Samurai-92161 → `spoteffects`@35 was 34; scen-wish-Archeologist-92238 still `next_ident`@42; scen-wish-Knight-92130 still `next_ident`@90) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Structural no-regression: the added case only fires for PM_NINJA (ninjas are quest-genus, absent from the fortress); all other species take bit-identical paths. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `monmulti` mplayer `+1` arm (`mthrowu.c:220`, no corpus coverage — ninja is not mplayer, untouched); `monmulti` racial elf/orc/gnome bow bonus (pre-existing `weapon.js` deferral, untouched).
**Next:** wish pair → new Open row `objnam.c readobjnam` (C resolves "3 slime mold"/"cursed slime mold" with zero draws; JS draws `rn2(76) @ rnd_otyp_by_namedesc`; D-2021/D-2022 dead-ended on the documented paths — prescribed falsifier stands: a C-recorder wish experiment, not more reading). Samurai → new Open row `hack.c spoteffects` (screen-first at step 35 with positional RNG matched ~361 draws past the fix point — through the step-35 volley; C owner `spoteffects` `hack.c:3434` falling-object arm; writer to localize there, not in the throw path).
## 2026-09-08 — D-2110 `mhitu.c` gulpmu AD_BLND: swallowed hero blinded with "You can't see in here!" (queue row `invent.c` dolook, 1 session)

**C locus:** `mhitu.c` `gulpmu` AD_BLND `:1471–1484` (`can_blnd(mtmp, &youmonst, mattk->aatyp, NULL)` → `!Blind ? (was=Blinded; !was → You_cant; make_blinded(tmp,FALSE); !was && !Blind → vision_clears) : incr_itimeout(&HBlinded,1)`; `tmp = 0`) + `mondata.c` `can_blnd` `:305–380` (haseyes; raven-vs-raven; AT_ENGL arm `is_you && (Blindfolded||Unaware||ucreamed) → FALSE`; WEAP/SPIT/NONE with NULL obj → FALSE; EXPL/BOOM/GAZE/MAGC/BREA → `!mcan && !resists_blnd`; CLAW → `!ublindf`; TUCH/STNG → `!mcan`) + `youprop.h:92–103` (`Blinded ≡ HBlinded && !BBlinded`, `Blindfolded ≡ EBlinded`, `Blind ≡ (H||E) && !B`) + `potion.c` `incr_itimeout` TIMEOUT-bits-only semantics.
**JS:** 1 file (`mhitu.js`, +63/−3), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — the corpus session reaches the changed arm (step-108 engulf).
**Change:** `js/mhitu.js` only, no new imports (all helpers pre-existing or same-module locals — no new edge, no TDZ). New `gulpmu_can_blnd(mtmp, mattk)` mirrors the C `can_blnd` switch in exact C order for obj NULL (`EBlinded` for `Blindfolded` per the `potion.js` idiom, local `Unaware()`, `u.ucreamed`, `ublindf`, `mcan`, live `resists_blnd_you()`; raven-vs-raven via `mons()` identity). AD_BLND arm in exact C order: `was_blinded ≡ HBlinded && !BBlinded` → `!was → pline("You can't see in here!")` → `make_blinded(tmp, false)` (talk FALSE, like C) → `!was && !Blind() → pline('Your vision clears.')`, else TIMEOUT-bits-only `HBlinded` incr replicating `do.js` `set_itimeout_HBlinded` arithmetic.
**Verify:** `node scripts/verify.mjs --fn dolook` → `PASS syntax 1 changed js file(s): js/mhitu.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify dolook: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-genesis-Wizard-92223 → `look_here`@121 was 108) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Direct replay confirms full positional RNG 4392/4392 with screens 128/131 (was 108/131). Honestly noted: `hidden-proxy verify gulpmu` is NO MOVEMENT on its one other blocked session (scen-wish-Monk-92194 `gulpmu`@87 «You feel your magical energy drain away.» — the AD_DREN `drain_en` arm, a pre-existing named omission below, not this arm).
**Named:** `gulpmu` AD_DISE/AD_DREN arms (`diseasemu`/`drain_en` — doc-comment list, Monk-92194 needs DREN); worn visored-helmet check in `gulpmu_can_blnd` (same debt as `can_blnd_u`); DGST `Slow_digestion` early-out + total-digest `Half_physical tmp *= 2`; Punished ball/limbo; steed dismount; leashes; petrify; `snuff_lit` invent; `ugolemeffects`/`monstseesu`.
**Next:** do not re-pop `dolook` (0 blocked; body faithful — misattribution, not a port gap). The moved session rests at `look_here`@121 (C «You try to feel what is lying here on the floor.--More--» vs JS «It hits!», RNG fully matched 4392/4392 — blind-state/turn-order interplay after the new blinding, separate writer) — leave the recorded owner to the queue refill; do not invent rows.
## 2026-09-08 — D-2109 `weapon.c` weapon_descr full switch + makesingular: wielded dwarvish mattock prints "mattock", not skill-name "pick-axe" (queue row `insight.c` one_characteristic, 1 session)

**C locus:** `weapon.c` `weapon_descr` `:90–142` (`skill = weapon_type(obj)`; `descr = P_NAME(skill)`; P_NONE → OBJ_NAME specials / globby «glob» / `def_oc_syms[oclass].name`; P_SLING ammo → «stone»/«gem»/oclass; P_BOW ammo → «arrow»; P_CROSSBOW ammo → «bolt»; P_FLAIL hook; P_PICK_AXE → «mattock» «even if undiscovered»; `return makesingular(descr)`) + `obj.h:413` `is_graystone` (LUCKSTONE/LOADSTONE/FLINT/TOUCHSTONE) + `defsym.h` OBJCLASS names (`drawing.c` `def_oc_syms`) + C `weapon_type` `:1517–1529` (matches the live JS shape, incl. `!obj → P_BARE_HANDED_COMBAT`).
**JS:** 2 files (`invent.js` +56/−26, `objects.js` +17), under the 600/10 caps. No DIAG/FORCE/seed gates (Rule #2 clean). Hand probe `/tmp/wdescr-probe.mjs` (kept out of tree; game-entry import — a direct-`objnam.js` entry hits the pre-existing polyself↔objnam top-level cycle, reproduced on the clean tree via `git stash`): mattock→«mattock», pick-axe→«pick-axe», hook→«hook», corpse→«corpse», boulder→«boulder», amulet→«amulet», long sword→«long sword»; `makesingular` vocabulary («tools»→«tool», «polearms»→«polearm», «large stones»→«large stone», «chains»→«chain») matches C stripping.
**Change:** `js/invent.js` — full C switch in exact C order with the `makesingular` return; P_NONE specials via `objectNameStrs` (verified «corpse|tin|egg|statue|boulder|towel|tin opener» ≡ OBJ_NAME) / `obj.globby` / live `def_oc_syms[].name` (already imported from `objects.js`); otyp consts via the file's `objectNames.indexOf` idiom (`EGG`/`STATUE`/`OTYP_CORPSE` reused); `is_ammo` joins from `wield.js` (`imports.mjs --can` hoisted/cycle-safe, `dothrow.js` precedent) and `makesingular` joins the existing `objnam.js` edge — both call-time use only. `js/objects.js` — canonical `is_graystone` export, `obj.h:413` verbatim (`is_axe`/`is_pick`/`is_spear` precedent; `apply.js`/`iactions.js` file-locals stay as named debt). Every new arm is draw-free, so zero RNG movement by construction.
**Verify:** `node scripts/verify.mjs --fn one_characteristic` → `PASS syntax 2 changed js file(s): js/invent.js js/objects.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify one_characteristic: 1 PASS, 0 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-wish-Priest-92180: PASS) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `VERIFY: PASS`. Plus manual `node frozen/ps_test_runner.mjs sessions` → **44/44** (runner's auto-full heuristic said `skip full (no shared file changed per runner)`; ran it anyway since `weapon_descr` feeds hack/do_wear/polyself/enlightenment). Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `weapon_insight` wet-towel + shield-of-reflection arms (`insight.c:1293–1295`) live in neither JS enlightenment caller (status `invent.js:5338`, final `:5977`) — separate C function, own row when a session needs it; `throwit_weapon_descr` (`dothrow.js`) keeps its hand-throw clone (different call path, now overlapping the bow/crossbow arms — not refactored); `apply.js`/`iactions.js` `is_graystone` clones stay (named debt, same predicate).
**Next:** do not re-pop `one_characteristic` for this session (0 blocked — PASS). D-2062 tail (were-form/Hate_silver/survived arms) stays open — different arms, different sessions.
## 2026-09-08 — D-2108 `do_name.c` distant_monnam ARTICLE_NONE: farlook of a named pet prints "little dog called Slasher", not bare "Slasher" (queue row `dog.c` makedog, 1 session)

**C locus:** `pager.c` `look_at_monster` `:422–444` (`"tame "` + `distant_monnam(mtmp, ARTICLE_NONE, monnambuf)`) + `do_name.c` `distant_monnam` `:1168–1186` (astral HIGH_CLERIC conceal, else `x_monnam(mon, article, NULL, 0, TRUE)`) + `x_monnam` named+called arm `:967` (`"%s called %s", pm_name, name`).
**JS:** 1 file (`do_name.js`, +7/−8), under the 600/10 caps. Small diff because the C function is ~12 lines (density exception: C is that small). No DIAG/FORCE/seed gates (Rule #2 clean). No hand probes — the corpus session reaches the changed arm (only caller is the pager look path).
**Change:** `js/do_name.js` only — `distant_monnam_none` keeps the null→`it` guard and the astral conceal first (C order), then delegates to the live `x_monnam(mtmp, ARTICLE_NONE, null, 0, true)` (same module, no new edge, no TDZ). RNG-neutral on this path (no hallu/unseen draws on the taken arm; the `nextmbuf` ring slot matches C's own `x_monnam` consumption).
**Verify:** `node scripts/verify.mjs --fn makedog` → `PASS syntax 1 changed js file(s): js/do_name.js` · `PASS rule2 no fs/path/url/node: imports, no DIAG/FORCE/seed gates` · `PASS hidden verify makedog: 0 PASS, 1 moved past, 0 unchanged, 0 worse → PROGRESS` (scen-normal-Caveman-92053 → `distfleeck`@32 was 31) · `PASS green 2/2` + strict ×2 · `PASS cohort 7/7` · `skip full (no shared file changed per runner)` · `VERIFY: PASS`. Final verify ran after the last js/ edit (map/D-log/queue edits only after).
**Named:** `look_at_monster` health/stuck/leashed/trapped/hallu suffixes stay deferred as mapped (turns.md look lines); `x_monnam_tame` bare-name subset for displace/pet plines untouched (different callers, no corpus row).
**Next:** do not re-pop `makedog` (0 blocked; body faithful — misattribution, not a port gap). The moved session rests at `distfleeck`@32 — leave the recorded owner to the queue refill; do not invent rows.
## 2026-09-08 — Audit 3516098b…67985652 (reviews 1067–1073) + cadence 44/44

6 ACCEPT, 1 ACCEPT-WITH-DEBT (1070 XP-delta `final||wizard` gate debt pointer), 0 Must-fix — no LOOP-QUEUE prepend, Next cluster unchanged. Every D-log corpus claim re-measured with `--base <sha>~1`, all matched exactly (checkfile 0/3 moved, done_in_by 0/2, enlightenment 2/2+2, disclose 1/2+1unchanged, ghitm 0/2, zap_hit 0/1 re-attributed same-step); 1065 Must-fix confirmed closed by D-2101 (`goto outhere` exits both loops, `sp_lev.c:5241–5251` read verbatim). Extra probes: Barbarian-92024 step-97 `js-throw` worker label refuted by `show` (`error: null`, screen diff) — no §10.14 row owed; D-2102 stub→import re-point verified via aliased pre-existing edge (monmove.js:78); D-2104 Invisible trio checked against youprop.h:198–199 + potion.js co-write proof. Cadence: fortress 44/44, Scr 11,405/11,405, RNG 792,838/792,838, speed `60+0.48/turn` (R² 0.87).
